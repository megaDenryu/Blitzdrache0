# 命名検査報告 区画104

- 区画名: `crates/blitz_render/src/vulkan/headless`・`crates/blitz_render/src/vulkan/indirect_lighting`・`crates/blitz_render/src/vulkan/instance_transform`(区画番号104、23ファイル。一覧は `block_104.txt` の指定どおり)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、db815090時点。このツリーは編集していない)
- 直読したファイル数: 23/23(全数を直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`の「命名」節、`_doc/計画/ユビキタス言語.md`(該当語のみgrep照合)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_render/src/vulkan/headless/validation.rs:64` | `層がある`(let束縛、bool) | 条件4(品詞の規律: 真偽=「〜か」) | 真偽値を持つ変数なのに断定文の形で終わっており、値であるかのように読める。同じファイル内で真偽値を返す設計をするなら「〜か」で終える書き方が既定であるべきで、実際に`headless/mod.rs`の`対応か`や`inputs.rs`系の`焼くか()`群はすべて「〜か」で終わっている。この変数だけが規則から外れている | `層があるか` |
| `crates/blitz_render/src/vulkan/headless/validation.rs:74` | `拡張がある`(fn、戻り値`Result<bool, レンダラーエラー>`) | 条件4(品詞の規律) | 真偽値を返す関数名が「〜か」で終わっていない。呼び出し側の`!拡張がある(entry, None)?`は文だけ見ると「拡張が無いことを確定させる処理」であるかのように読め、実際は「拡張があるかを問う処理」であることが名前から一意に取れない | `拡張があるか` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 内容 |
|---|---|---|---|
| `crates/blitz_render/src/vulkan/headless/validation.rs:61` | `在否を調べる`(fn名) | 条件7(直訳・造語の検出) | 「在否」は一般に定着した日本語の熟語ではない(標準は「存否」または「有無」)。関数の実体は「validation層とdebug utils拡張の両方がそろっているか」を調べる処理であり、造語「在否」だけからこの実体を復元できるかが疑わしい。改名候補: `層と拡張の有無を調べる` |
| `crates/blitz_render/src/vulkan/headless/mod.rs:29` | フィールド`検証: validation::検証つきインスタンス` | 条件1・7の疑い | フィールド名「検証」だけを取り出すと「検証という行為・結果」に読め、実体(生成済みのインスタンス+メッセンジャー+層の状況をまとめた値)が名詞句から一部落ちる。ただし宣言行で型`検証つきインスタンス`を同時に読めるため completion が効いており、同型の省略(`遠方環境: 遠方環境一式`等)は本区画・隣接コードで広く使われている慣行でもある。一括での改名は推奨せず、疑いに留める |
| `crates/blitz_render/src/vulkan/instance_transform/reference.rs:32` | メソッド`buffer(&self, フレーム添字: フレームスロット添字) -> vk::Buffer` | 命名の質(日本語命名の積極使用、CLAUDE.md「命名」節) | 受理条件8つは日本語識別子が対象のため直接の違反ではないが、同じ型の兄弟メソッド`範囲(&self)`は日本語であるのに、このメソッドだけ英語「buffer」のままで一貫していない。ドメイン層は日本語命名を積極使用する方針との整合を疑う。改名候補: `バッファ(&self, フレーム添字: フレームスロット添字) -> vk::Buffer` |

## 違反ゼロの範囲

上記2件の違反と3件の疑いを除き、23ファイル全体(headless配下5ファイル、indirect_lighting配下12ファイル、instance_transform配下6ファイル)の型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝を受理条件8つ・命名の質・呼称と語の選択・実例3形(実体と合わない語・名詞1語の圧縮・詰め込みすぎ)・ASCII混入の観点で検査し、違反は見つからなかった。特に`検収の注入`・`間接照明の焼く組`・`焼かせる注入`・`個体変換内容`・`個体レコード参照`のような判別共用体・値オブジェクトは、型定義とコメントを照合したうえで実体と名前が一致していることを確認した。真偽値を返す既存の関数群(`焼くか()`系)は「〜か」規則に従っており、今回の2件の違反はその規則からの逸脱として際立つ。
