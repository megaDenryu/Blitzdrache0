# 命名検査 区画085

- 区画名: crates/blitz_render/src/distant_environment/derived/derived_tests ほか(区画番号085、40ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main, db815090時点。編集していない)
- 直読したファイル数: 40 / 40(全ファイルを直読。grepだけで済ませたファイルは0件)
- 検査に用いた定義: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

1. `crates/blitz_render/src/distant_environment/derived/derived_tests/resolution_tests.rs:36` | `既定のbrdf積分表は256の正方である`(テスト関数名) | 落ちた条件: 日本語命名の質(頭字語混入禁止)、受理条件7(直訳の検出質問) | 「brdf」は内部の技術概念(双方向反射率分布関数)を指すASCIIの頭字語であり、日本の非専門家は読めない。しかも同じ関数群がすでに「反射率積分表」という訳語を確立して使っている(brdf_tests.rs・constant_environment_tests.rs等)のに、この1箇所だけ英語の頭字語のまま日本語文へ混ぜており、1語1義の原則にも反する | `既定の反射率積分表は256の正方である`
2. `crates/blitz_render/src/frame_composition.rs:15,27`(定義は`frame_composition/validation.rs:5`) | `検証する`(自由関数、ASCIIモジュール`validation`直下) | 落ちた条件: 受理条件2(ASCIIモジュールは補完力ゼロ)・3(5W1Hの必要格)・5(裸の動詞の禁止) | `use validation::検証する;`で取り込み、呼び出し側では`検証する(段階一覧)?;`と裸の動詞1語だけが残る。ASCIIモジュール名「validation」は補完に使えないため、名前単独では何を検証するのか(段階列の依存順序と必須段階)が読めない。同じファイル内の他の関数(`長さを検証する`・`添字を検証する`等、cloth_material配下)は目的語を含めており、この1箇所だけが慣行を破っている | `段階列を検証する`
3. `crates/blitz_render/src/frame_input/sky_input/validation.rs:8,26` | `確かめる`・`単位化する`(自由関数、ASCIIモジュール`validation`直下、呼び出し側は`validation::確かめる(&材料)`・`validation::単位化する(材料.太陽方向)`) | 落ちた条件: 受理条件2・3・5 | ASCIIモジュール修飾子「validation」は補完力ゼロのため、名前単独で読む必要があるが、「確かめる」は何を確かめるのか(空入力の材料の値域)を、「単位化する」は何を単位化するのか(太陽方向)を名前が持たない。同ファイル内の`非負を求める(値一覧, 名前)`・`有限を求める(値一覧, 名前)`は対象を引数名でなく関数名にも部分的に示せているが、この2つは完全に裸である | `空入力の材料を確かめる`・`太陽方向を単位化する`
4. `crates/blitz_render/src/frame_input/primitive_issue_tray.rs:42` | `引く`(`プリミティブ発行受け皿`のメソッド) | 落ちた条件: 受理条件7(直訳の検出質問) | グローバルCLAUDE.mdが直訳失敗の実例として名指ししている「lookup→引く」そのものであり、CLAUDE.md本体も「オーナー確定の訳語: lookup/sampleの『引く』→『参照する』」と明記済みである。レシーバ`プリミティブ発行受け皿`があっても、「引く」自体が英語lookupの直訳調であることは変わらない | `参照する`
5. `crates/blitz_render/src/frame_composition.rs:56` | `含む`(`フレーム構成`のメソッド、`-> bool`) | 落ちた条件: 受理条件4(品詞の規律。真偽は「〜か」) | 戻り値がboolなのに動詞終止形のままで、疑問形になっていない。同じ型・同じファイル内の`深度プリパスを積むか`、同モジュール群の`ゼロ寸法か`・`同じ描画対象か`・`有効か`・`影へ積むか`・`このパスへ積むか`はすべて「〜か」で終えており、この1箇所だけが規律を破っている | `含むか`

## 違反の疑い

1. `crates/blitz_render/src/error/frame_input_mismatch.rs:44-45` | `圧縮前のHDRの読み戻しにポスト処理が要る`(enum枝名) | 条件に対して: 日本語命名の質(頭字語混入禁止) | 「HDR」は一般消費者にもある程度通用しつつあるが、白リストの「UI・CPU・GPU・API・シェーダー」には含まれない専門用語であり、初出の意味の取れる訳(高輝度な明るさの範囲、等)を当てるべきか判断が割れる。断定はしない

## 違反ゼロの範囲

- `error/`配下の`cascade.rs`・`cloth.rs`・`conversions.rs`・`generate_request.rs`・`lighting_query.rs`・`material_table.rs`・`pipeline_ledger.rs`・`renderer_error.rs`・`sky.rs`・`texture_format.rs`・`device_requirement/`(`feature_item.rs`・`mod.rs`・`shortfall.rs`)
- `extent/non_zero.rs`
- `frame_composition/`の`depth_prepass.rs`・`frame_composition_tests.rs`・`stage.rs`(`validation.rs`と親の`frame_composition.rs`の一部を除く。上記違反1・5を参照)
- `frame_input/`の`cloth_tick_count.rs`・`moving_instance_supply.rs`・`moving_instance_turn.rs`・`moving_instance_turn_tests.rs`・`primitive_issue.rs`・`primitive_issue_section.rs`・`shadow_casters.rs`・`sky_input.rs`(`primitive_issue_tray.rs`と`sky_input/validation.rs`を除く。上記違反3・4を参照)
- `frame_lighting/`の`decomposition_tests.rs`・`direct_light.rs`・`direct_shadow.rs`・`indirect_light.rs`・`validity.rs`
- `gpu_memory_stats/usage.rs`
- `distant_environment/derived/derived_tests/`の`brdf_tests.rs`・`constant_environment_tests.rs`・`directional_environment_tests.rs`・`texel_lookup_tests.rs`(`resolution_tests.rs`を除く。上記違反1を参照)

省略した検査は無い。40ファイル全件を直読し、全識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)を1件ずつ受理条件に当てた。
