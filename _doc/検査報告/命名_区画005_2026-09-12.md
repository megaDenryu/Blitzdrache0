# 命名検査報告: 区画005 crates/blitz_app/src/app/scene_load ほか

## 範囲・時点

- 区画名: crates/blitz_app/src/app/scene_load (区画番号005)
- 対象ディレクトリ: `crates/blitz_app/src/app/scene_load/`(サブディレクトリ`convert/`・`render_input/`含む)、`crates/blitz_app/src/app/streaming/`(サブディレクトリ`bundle_sync/`・`measurement/`含む)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、db815090時点。このツリーは編集していない)
- ファイル一覧の出典: `scratchpad/roller/block_005.txt`

## 直読・省略の内訳

- 対象ファイル数: 37
- 直読したファイル数: 37(全件)
- grepだけで済ませたファイル数: 0(省略なし)

必読とした規約: グローバルCLAUDE.mdの「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、および`_doc/計画/ユビキタス言語.md`。

## 違反の一覧

1. `crates/blitz_app/src/app/scene_load/convert.rs:59` | `頂点変換する` | 日本語命名の質(単語らしく見せるための助詞省略の禁止) | 同じファイルの兄弟関数`メッシュを変換する`(55行目)は目的語に「を」を付けているのに、この関数だけ「頂点」+「変換する」を助詞なしで直結しており、「大気LUT生成指示」型の膠着圧縮になっている | 改名素案: `頂点を変換する`

2. `crates/blitz_app/src/app/scene_load/render_input/anchor.rs:11` | `導出する`(`anchor::導出する`として呼ばれる) | 受理条件3(5W1Hの必要格)・条5(裸の動詞の禁止) | `anchor`はASCIIモジュール名であり補完力ゼロ。呼び出し側は`anchor::導出する(シーン, 束座標, ...)`と裸の動詞だけで呼んでおり、「導出する」だけでは何を導出するのか(基準原点)が単独で読めない。`create::生成する`型の違反と同型 | 改名素案: `基準原点を導出する`

3. `crates/blitz_app/src/app/scene_load/render_input/layout.rs:8` | `配置する`(`layout::配置する`として呼ばれる) | 受理条件3・条5 | ASCIIモジュール`layout`直下の自由関数で目的語が無く、何を配置するのか(格子内でのワールド変換)が名前だけで読めない | 改名素案: `格子内の配置変換を求める`

4. `crates/blitz_app/src/app/scene_load/render_input/material_slots.rs:14` | `変換する`(`material_slots::変換する`として呼ばれる) | 受理条件3・条5 | ASCIIモジュール直下の自由関数で目的語が無く、材質集合を何へ変換するのかが読めない | 改名素案: `材質集合を材質スロット素材一覧へ変換する`

5. `crates/blitz_app/src/app/scene_load/render_input.rs:58` | `変換する`(`render_input::変換する`として呼ばれる) | 受理条件3・条5 | ASCIIモジュール`render_input`直下の自由関数で目的語が無い。`scene_load.rs`側で`シーンをレンダラー入力に変換する`という正しい名のラッパーを別に持っているのに、内側のこの名前自体は裸のまま残っている | 改名素案: `シーンを束の描画入力へ変換する`

6. `crates/blitz_app/src/app/scene_load/render_input/instance_transforms.rs:10` | `組み立てる`(`instance_transforms::組み立てる`として呼ばれる) | 受理条件3・条5 | ASCIIモジュール直下の自由関数で目的語が無く、何を組み立てるのか(個体変換列)が読めない | 改名素案: `個体変換列を組み立てる`

7. `crates/blitz_app/src/app/streaming/bundle_sync.rs:42` | `適用する`(`bundle_sync::適用する`として呼ばれる) | 受理条件3・条5 | ASCIIモジュール直下の自由関数で目的語が無く、何を適用するのか(そのフレームの進行)が読めない | 改名素案: `フレームの進行を束へ適用する`

8. `crates/blitz_app/src/app/streaming/integration_route.rs:38` | `経路端点` | 受理条件4(品詞の規律) | 関数(処理)なのに名詞句で命名されている。同ファイルの`統合経路を作る`は動詞句なのに、これだけ名詞のままで、値を返す計算なのか単なる定数保持なのか名前から判断できない | 改名素案: `xメートルから経路端点を求める`

9. `crates/blitz_app/src/app/streaming/integration_route.rs:29` | `統合経路の経過` | 受理条件4 | 関数なのに名詞句。同じ問題を持つ`route.rs`側の兄弟(`位置を求める`)は動詞句で統一されているのに、ここだけ名詞形になっている | 改名素案: `統合経路の経過を求める`

10. `crates/blitz_app/src/app/streaming/integration_route.rs:34` | `整定込みの経過` | 受理条件4 | 同上。関数なのに名詞句 | 改名素案: `整定込みの経過を求める`

11. `crates/blitz_app/src/app/streaming/route.rs:20` | `統合Dod経路`(`プレイヤー位置決定`の枝) | 受理条件7(直訳・未定義語の検出)、専門用語の表記(初出補足なしの新語) | 「Dod」は`_doc/計画/ユビキタス言語.md`に未登録で、grepでも他に定義が見当たらない。英語のDefinition of Doneを指すと推測されるが、この名前だけからは復元できず、ASCII文字が日本語識別子へ直接混入してもいる | 改名素案: `統合検収経路`(検収の文脈をそのまま日本語で表す)。もし別の意味で使っているなら、まずユビキタス言語へ定義を足してから使う

## 違反の疑い

1. `crates/blitz_app/src/app/scene_load/render_input/layout.rs:37` | `usizeをf32へ変換する` | 受理条件2に関連(ASCII識別子の混入) | Rustの実際の型名`usize`・`f32`をそのまま日本語の助詞でつないだ形。型名としては正確だが、識別子へASCII英字を持ち込んでいる点は「オーナーが不合格にした実例」の混入禁止に触れうる。ただし`as`キャスト禁止の代替として型変換ヘルパーをこの形で名付けるのは本プロジェクトで広く使われている可能性があり、断定はしない

2. `crates/blitz_app/src/app/streaming/integration_route.rs:12-13,38` | `統合検収経路の始点Xメートル`・`統合検収経路の終点Xメートル`・仮引数`xメートル` | ASCII文字(大文字X・小文字x)の混入 | `位置.x()`のような小文字の座標アクセサは本プロジェクト全体で定着した数学記法だが、ここは大文字と小文字が同じファイル内で揺れており(定数はX、引数はx)、単なる座標記号として読めているか迷う

3. `crates/blitz_app/src/app/streaming/measurement.rs:19,35` | `処理時間一覧ms` | ASCII単位略語の混入 | 「ms」はミリ秒の略として広く通用するが、フィールド名へ直接英字の単位記号を貼り付けている。数学DDD(単位をプリミティブへ密輸しない)の観点とも重なるため、命名だけでなく型設計側の再検討が必要かもしれない

## 違反ゼロの範囲

上記9ファイル(`convert.rs`・`render_input.rs`・`render_input/anchor.rs`・`render_input/instance_transforms.rs`・`render_input/layout.rs`・`render_input/material_slots.rs`・`streaming/bundle_sync.rs`・`streaming/integration_route.rs`・`streaming/route.rs`)を除く残り28ファイルは、日本語識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)を全件直読して受理条件と照合した結果、違反・疑いともに検出しなかった。該当ファイル: `catalog_setup.rs`、`conversion_materials.rs`、`distant.rs`、`storage_byte_agreement_tests.rs`、`surface_layer_tiles.rs`、`convert/material.rs`、`render_input/anchor_tests.rs`、`render_input/error.rs`、`render_input/layout_tests.rs`、`render_input/reader_tests.rs`、`render_input/tray.rs`、`render_input/tray_fixture.rs`、`render_input/tray_tests.rs`、`render_input/visibility_material.rs`、`streaming/advance.rs`、`streaming/build.rs`、`streaming/bundle_sync_tests.rs`、`streaming/directory_replace.rs`、`streaming/lod_override.rs`、`streaming/lod_sync.rs`、`streaming/presence.rs`、`streaming/route_tests.rs`、`streaming/bundle_sync/bundle_add.rs`、`streaming/bundle_sync/old_world_destruction.rs`、`streaming/bundle_sync/transfer_without_draw.rs`、`streaming/measurement/ledger_count.rs`、`streaming/measurement/lod_change.rs`(`streaming/measurement.rs`は疑いを1件検出したため上のリストから除いた)。

GPU・LOD・PBR・IDの各略語は`_doc/計画/ユビキタス言語.md`に登録済みであり、フィールド・引数名を小文字(`gpu`・`lod`・`pbr`・`id`)にする書き分けも既存の裁定と一致するため、違反として扱わなかった。
