# 命名検査報告 区画043: crates/blitz_collision/src/oriented_box

- 区画番号: 043
- 対象: `crates/blitz_collision/src/oriented_box`(32ファイル。作業ツリー `C:/devs/Blitzdrache0_roller`、時点 main の db815090。このツリーは編集していない)
- 直読したファイル数: 32/32(全ファイルを全文直読した。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`の「命名」節、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_collision/src/oriented_box/query_fixture.rs:35` | `局所の点`(関数) | 受理条件4(品詞の規律: 処理は動詞句) | `[f64; 3]`を受け取り`形の局所座標の位置::生成する()`を呼んでpanicで包む処理(値オブジェクトの生成に失敗しうる工程)であるにもかかわらず、名前が動詞を持たない名詞句になっている。同じファイル内の兄弟関数`直方体を作る`・`線分を作る`・`カプセルを作る`はすべて「〜を作る」の動詞句であり、この関数だけが動詞を欠く。読み手は「局所の点」を既存の値を指す変数や定数だと誤読しうる | `成分から局所の点を作る` |
| `crates/blitz_collision/src/oriented_box/query_fixture.rs:46` | `局所の変位`(関数) | 受理条件4(品詞の規律: 処理は動詞句) | 同上と同じ構造の違反。`[f64; 3]`から`形の局所座標の変位`を生成する処理が名詞句のままであり、兄弟関数群の動詞句と不整合 | `成分から局所の変位を作る` |

## 違反の疑い

該当なし。以下は検討したが、違反と判定しなかった項目である。

- `位置の成分(位置: 形の局所座標の位置) -> [f64; 3]`・`方向の成分(方向: 方向<ローカル>) -> [f32; 3]`(`query_fixture.rs`): 名詞句だが、値からその構成要素(成分)を読み出す働きであり、この crate 全体で広く使われている「〜.軸()」「〜.半径のメートル()」のようなgetter系の命名慣習と同型である(受け手が値を読み出しているだけと判断できる)。上の2件(`局所の点`・`局所の変位`)とは逆方向(値→成分の分解であり、成分→値の構築ではない)であるため区別した
- `直方体自身の座標軸::X軸`・`Y軸`・`Z軸`(enum variant): ASCII 1文字(X/Y/Z)を含むが、この型自体は本区画(`oriented_box`)の外(`crate::shape`)で定義されており、本区画のファイルは参照するだけである。区画043の識別子として新規に検査する対象ではないため、違反として計上しなかった。ただし座標軸の型を定義する区画を検査する回では、この点をあらためて検査するべきである
- `直方体のカプセルの掃引の問い合わせ`(`sweep_query.rs`の型名): 「直方体の・カプセルの・掃引の・問い合わせ」と属格が3段連なるが、兄弟型`直方体の線分の問い合わせ`と対になる命名であり、ファイル冒頭の`//!`が「カプセルを変位の分だけ動かそうとしたとき、直方体に触れるまでにどこまで動けるかを求める操作サービス」と実体を明記しているため、読み違いは生じないと判定した

## 違反ゼロの範囲

- 残る30ファイル(`error.rs`・`feature.rs`・`feature_enumeration.rs`・`feature_geometry.rs`・`feature_geometry_tests.rs`・`feature_normal.rs`・`feature_place.rs`・`segment_axis_aligned_tests.rs`・`segment_entry.rs`・`segment_hit.rs`・`segment_query.rs`・`segment_rotated_tests.rs`・`segment_span.rs`・`segment_tangent_tests.rs`・`slab.rs`・`start_inside.rs`・`sweep_candidate.rs`・`sweep_contact.rs`・`sweep_direction.rs`・`sweep_dispatch.rs`・`sweep_fixture.rs`・`sweep_geometry.rs`・`sweep_place.rs`・`sweep_query.rs`・`sweep_solve_edge.rs`・`sweep_solve_edge_axis.rs`・`sweep_solve_face.rs`・`sweep_solve_vertex.rs`・`sweep_solver.rs`・`sweep_start_tests.rs`)は、日本語識別子(型名・関数名・メソッド名・フィールド名・変数名・enumの枝)を受理条件8つ全部で検査し、違反を検出しなかった
- 特に、`直方体で当たった特徴`・`直方体との接触の特徴の候補`・`線分が直方体の中に居る時刻の区間`・`掃引`は`_doc/計画/ユビキタス言語.md`の登録句(2026-08-28追加分)と綴りが完全一致しており、実体との照合も取れている
- `query_fixture.rs`の残りの識別子(`直方体を作る`・`線分を作る`・`カプセルを作る`・`位置の成分`・`方向の成分`・`三成分が近いことを確かめる`等)は違反ゼロである

## 省略の有無

省略なし。32ファイル全部を全文直読し、grepだけで済ませたファイルは無い。
