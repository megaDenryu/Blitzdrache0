# 命名検査 区画049

- 区画: `crates/blitz_engine/src/animation`(14ファイル)
- 対象作業ツリー: `C:/devs/Blitzdrache0_roller`(main の db815090。編集していない)
- 検査日: 2026-09-12
- 直読したファイル数: 14 / 14(全ファイルを直読。grepだけで済ませたファイルは0件)
  - blend.rs / blend_factor.rs / blend_tests.rs / evaluate.rs / evaluate_bind_pose_tests.rs /
    evaluate_channel.rs / evaluate_tests.rs / joint_pose.rs / keyframe_interpolation.rs /
    mod.rs / pose.rs / skin_matrix.rs / skin_matrix_tests.rs / test_support.rs
- 参照した必読リスト: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」
  「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`
  (TRS・ジョイント・静的TRS・アニメーションクリップ・姿勢の項を照合)

## 違反の一覧

1. `crates/blitz_engine/src/animation/blend.rs:11`(再エクスポート: `mod.rs:26`)
   | 識別子: `ブレンドする`
   | 落ちた条件: 2(補完してよい文脈の限定)・3(5W1Hの必要格)
   | なぜ読めないか: ASCIIモジュール`blend`直下のpub自由関数であり、モジュール名は補完力ゼロとして扱う規定に当たる。名前単独では「何をブレンドするか」(姿勢)が書かれておらず、「これは何?」に「ブレンドする処理です」としか答えられず対象が特定できない。引数名`姿勢a`・`姿勢b`は関数の受け手ではなく単なる引数であり、条件2が補完に使ってよいと定める「型名::関連関数()の型名」「変数.メソッド()のレシーバ」のどちらにも当たらないため補完に使えない。同ファイル内の私有関数`関節姿勢をブレンドする`は目的語「関節姿勢を」を含み対比が付く
   | 改名の素案: `姿勢をブレンドする`

2. `crates/blitz_engine/src/animation/evaluate_channel.rs:10`(呼び出し: `evaluate.rs:7,36,41`)
   | 識別子: `vec3を評価する`
   | 落ちた条件: 2・5(ASCII混入)・7(直訳・専門語の検出)
   | なぜ読めないか: 日本語識別子の中にASCIIの英字列`vec3`をそのまま混入している。プロンプト指定の「識別子へのASCII・ギリシャ文字・記号の混入を必ず当てる」に直接該当する。`vec3`はグラフィックスプログラミング特有のジャーゴン(3成分ベクトル型の慣用名)で、専門用語の表記が求める「非専門家が読んで意味が取れるか」を満たさない。実際に評価しているのは平行移動またはスケールの3成分チャンネルであり、その情報も名前に無い
   | 改名の素案: `三成分チャンネルを評価する`(または呼び出し元で用途を分けるなら`平行移動またはスケールの成分を評価する`)

3. `crates/blitz_engine/src/animation/joint_pose.rs:45`(識別子の定義はblitz_math側、本区画では呼び出しのみ)
   | 識別子: `変換::trsから生成する`
   | 落ちた条件: 5(ASCII混入)
   | なぜ読めないか: 動詞`から生成する`の直前に小文字ASCII`trs`が直接連結されている。TRSという略語自体は`_doc/計画/ユビキタス言語.md`に登録済みで独立した大文字トークン(`関節TRS`のように)としてなら許容されるが、ここでは小文字化されて動詞へ埋め込まれており、指定された「識別子への英字混入」の禁止形にそのまま当たる。改名は定義側(blitz_math)の作業になるため、区画049の是正としては呼び出し箇所の存在だけを記録する
   | 改名の素案: `TRSから生成する`(定義はblitz_math側)

4. `crates/blitz_engine/src/animation/blend_tests.rs:54`
   | 識別子: `係数`(`fn 係数(値: f32) -> 姿勢のブレンド係数`)
   | 落ちた条件: 4(品詞の規律)
   | なぜ読めないか: この関数は入力値を検証し、受理範囲外なら`panic!`する処理(コメントに「受理範囲の外を書いた検査はここで落ちる」と明記)であるにもかかわらず、名前が名詞1語`係数`で値オブジェクトのように見える。品詞の偽装であり、「これは何?」への答えが「係数です」になってしまい、検証してpanicするという処理の性質が名前から消えている
   | 改名の素案: `受理される係数を作る`

## 違反の疑い(断定しない)

1. `crates/blitz_engine/src/animation/evaluate.rs:46`
   | 識別子: `位置化する`(`fn 位置化する(値: [f32; 3]) -> 位置<ローカル>`)
   | 疑いのある条件: 3(5W1Hの必要格)
   | 理由: 私有関数で、入力`値: [f32; 3]`が何を表すか(呼び出し元では平行移動成分)が名前に無い。「位置化する」は「〜化する」という自然な日本語の複合動詞で条件4の品詞は満たすが、材料の格が抜けており、単独では「何から」位置を作るのか分からない。ただし私有ヘルパーで呼び出し箇所が1つの狭い範囲に閉じているため、断定はしない

2. `crates/blitz_engine/src/animation/skin_matrix.rs:12`
   | 識別子: `内部`(`struct スキン行列一覧 { 内部: Vec<[[f32; 4]; 4]> }`)
   | 疑いのある条件: 1(受理テスト)
   | 理由: private フィールドで「これは何?」に「内部です」としか答えられない。公開アクセサ`列優先配列一覧()`と名前がそろっていないため、フィールド名だけを読んでも中身(列優先行列の列)を復元できない。newtype的な単一フィールドのラッパーで実害は小さく、断定はしない

## 違反ゼロの範囲

上記を除く識別子(`姿勢`・`姿勢のブレンド係数`・`関節TRS`・`スキン行列を計算する`・`スキン行列一覧`・
`姿勢を評価する`・`関節姿勢を評価する`・`区間を求める`・`区間`(`単一`・`二点間`)・
`時刻をループさせる`・`関節姿勢をブレンドする`・`回転を評価する`・`線形補間する`・
`生の成分から変換する`・各テストファイルの試験名・アセット由来の`アニメーションクリップ`・
`ジョイントアニメーションチャンネル`・`静的TRS`・`スキンデータ`等)は、受理条件8つ・命名の質・
呼称と語の選択・ユビキタス言語との照合のいずれにも抵触しない。特に`姿勢を評価する`・
`スキン行列を計算する`・`関節姿勢をブレンドする`は目的語(格)を名前に含み、`関節TRS`・
`静的TRS`はユビキタス言語に登録済みの略語をそのまま使っており合格側の根拠として確認した。
