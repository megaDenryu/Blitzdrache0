# 命名検査報告 区画090

- 区画名と範囲: `crates/blitz_render/src/renderer`(直下38ファイル。`cpu_timing/`配下の3ファイルを含む)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090`
- 直読したファイル数: 38 / 38(全ファイルを直読した。grepだけで済ませたファイルは無い)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」、`_doc/計画/ユビキタス言語.md`(該当語をgrepで照合)

## 違反の一覧

該当なし。38ファイルの全識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)を受理条件8つで検査したが、確定できる違反は見つからなかった。識別子へのASCII・ギリシャ文字・記号の混入は無い(CPU・GPU・UI・PCFのような一般に通用する頭字語のみで、いずれも「専門用語の表記」節の許容範囲)。実体と合わない語・名詞1語の圧縮・詰め込みすぎの3形も検出しなかった。

## 違反の疑い

1. `crates/blitz_render/src/renderer/lighting_query_write.rs:82-84` | `件数にする` | 条件1・3(受理テスト・5W1Hの必要格) | ASCIIモジュール直下の自由関数(補完力ゼロ)。引数`レコード数`が既に「件数」であり、名前の動詞部分「にする」が何をしているか(usizeからu32への型の狭め、かつ`u32::MAX`で飽和させる)を示さない。「これは何?」に単独で答えると「件数を件数にする処理」という同語反復になり、実際の動作(型を狭めて飽和させる)を復元できない。改名の素案: `件数をu32へ切り詰める`(確定はリネーム員判断)
2. `crates/blitz_render/src/renderer/destroy.rs:76` | `基盤を破棄する` | 条件1(受理テスト) | 実体は`提示`(スワップチェーン等)と`環境`(GPUデバイス・インスタンス)の2つを特定の順で破棄する処理だが、「基盤」は用語定義ファイルでは「シミュレーション基盤層」「ゲームサイクル基盤」のように別の意味(手法の数学だけを持つ汎用層)で登録されており、ここでの用法(Vulkanの提示資源とデバイス環境)とは指す実体が違う。直前の同ファイル内コメントが対象を明示しているため受理テストは辛うじて通るが、コメント無しで名前だけを読むと何を破棄するか特定できない。改名の素案: `提示と環境を破棄する`

いずれも用語の抽象度がやや高いだけで、5W1Hの必須格の欠落や品詞の誤りではないため断定はしない。

## 違反ゼロの範囲

上記2件の疑いを除く、区画090の全38ファイル(`aerial_composite_input.rs`・`atmosphere_lut_write.rs`・`cascade_uniform_write.rs`・`chunk_bundle.rs`・`cloth_readback.rs`・`cloth_write.rs`・`cpu_timing.rs`・`cpu_timing/clock.rs`・`cpu_timing/cpu_timing_tests.rs`・`cpu_timing/store.rs`・`destroy.rs`(疑い1件を除く)・`draw_dispatch.rs`・`draw_execute.rs`・`draw_issue_breakdown.rs`・`draw_stage_resources.rs`・`frame_dispatch_inputs.rs`・`frame_progress.rs`・`generate.rs`・`injection.rs`・`lighting_query_write.rs`(疑い1件を除く)・`measurement_control.rs`・`mod.rs`・`optional_frame_inputs.rs`・`pass_tally.rs`・`present_resources.rs`・`presentation.rs`・`queries.rs`・`readback_buffer.rs`・`reconstruct.rs`・`replace_scene.rs`・`replace_shader.rs`・`scene_draw_resources.rs`・`skin_write.rs`・`sky_uniform_write.rs`・`ui_dispatch.rs`・`ui_texture.rs`・`uniform_write.rs`・`view_uniform_write.rs`)は、受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも違反を検出しなかった。
