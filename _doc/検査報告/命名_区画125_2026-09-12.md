# 命名検査報告 区画125

- 区画名: crates/blitz_sim/src/contact/scene/numeric_contract_record/tolerance_dependence/row_admission、同stage_c、同unexplained_axes、crates/blitz_sim/src/contact/static_friction(全35ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main、時点 db815090。このツリーは編集していない)
- 検査日: 2026-09-12
- 直読したファイル数: 35/35(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 検査方法: 必読(受理条件・命名の質・呼称と語の選択・ユビキタス言語の該当節)を読んだ後、35ファイルの全文を直読し、型・関数・メソッド・フィールド・変数・定数・列挙の枝の識別子を受理条件8つで判定した。加えて、識別子中のASCII英字・ギリシャ文字・数学記号の混入をgrepで機械的に洗い出し、直読の結果と突き合わせた。

## 違反の一覧

### 1. 型`段階A1の場面からの一つの変更`(ASCII混入)

crates/blitz_sim/src/contact/scene/numeric_contract_record/tolerance_dependence/row_admission/acceptance_scale.rs:11 | `段階A1の場面からの一つの変更` | 落ちた条件: 受理条件の付記事項(識別子へのASCII英字・数字の混入の禁止。オーナー実例「段階A2の候補_…」と同型) | 「A1」というASCII英数字が日本語識別子の内部に混ざっており、日本語として読めない(中国語的圧縮の禁止と同根の、ASCII圧縮記号の混入)。この型は本区画の外(`scene_variation.rs`)で定義されているが、本区画の下記15ファイルで識別子として直接書かれて使われている: row_admission/acceptance_scale.rs:11,26 / row_admission/six_axes.rs:13,22,27,52-69 / row_admission/stacked_counterexample.rs:14,33 / row_admission/unresolved_locator.rs:13,28,36 / stage_c/failure_probe.rs:13,21,73,85 / stage_c/matrix.rs:9,23,41,51,55,63 / stage_c/variants.rs:14,27,49,50,65,77,84,90 / unexplained_axes/doubled_ulp_translation.rs:10,26 / unexplained_axes/mod.rs:23,37 / unexplained_axes/rhs_tolerance_zero.rs:10,36 / unexplained_axes/rhs_zero_across_axes.rs:13,28,33,58-75 / unexplained_axes/rhs_zero_failure_locator.rs:12,39 / unexplained_axes/rotated_acceptance.rs:8,19 / unexplained_axes/sixteen_substeps.rs:9,19 / unexplained_axes/tilt_after_rest.rs:11,29 / unexplained_axes/upper_tangent_scan.rs:8,19。改名の素案: `第一段階の場面からの一つの変更`(第◯段階の呼称は「呼称と語の選択」条1が定める元の完全な語に沿う)。定義の改名は本区画の外(`scene_variation.rs`)で行う必要があるため、リネームは区画125単独では完結しない

### 2. 型`試験の許容差の当て方`(既に不合格と裁定済みの実例そのもの)

crates/blitz_sim/src/contact/scene/numeric_contract_record/tolerance_dependence/stage_c/classification.rs:9,45 | `試験の許容差の当て方` | 落ちた条件1・3(受理テスト・5W1Hの必要格) | 検査定義の「オーナーが不合格にした実例」に挙げられている名称そのもの(適用先・倍率・判定条件を束ねた設定を「当て方」という動詞由来の名詞で圧縮しており、初見で「何が束ねられているか」を復元できない)。本区画では下記2ファイルで使われている: stage_c/classification.rs:9(import),45(引数の型) / stage_c/variants.rs:17(import),39,40,45,46(戻り値の型・関連関数呼び出し)。定義は本区画の外(`normal_tangential_system`)にある。改名の素案(定義済みの裁定): `試験の許容差の適用規則`

### 3. 列挙の枝`接線の行への許容差の当て方::許容差の内側の点の行を積まず右辺を零にする`(既に不合格と裁定済みの実例そのもの)

crates/blitz_sim/src/contact/scene/numeric_contract_record/tolerance_dependence/row_admission/acceptance_scale.rs:25 | `許容差の内側の点の行を積まず右辺を零にする` | 落ちた条件1・3(詰め込みすぎで本質でない条件が混入) | 検査定義の「オーナーが不合格にした実例」に挙げられている枝名そのもの(右辺の扱いは本質でないのに名前に残っている)。本区画での出現箇所: row_admission/acceptance_scale.rs:25 / row_admission/six_axes.rs:44 / row_admission/stacked_counterexample.rs:23 / row_admission/unresolved_locator.rs:27,35 / stage_c/failure_probe.rs:28 / stage_c/matrix.rs:30 / stage_c/variants.rs:34。定義は本区画の外(`normal_tangential_system`)にある。改名の素案(定義済みの裁定): `許容差以下の接線変位の拘束行を追加しない`

### 4. 型`接触点集合の法線と接線の連立`(既に不合格と裁定済みの実例そのもの)

crates/blitz_sim/src/contact/static_friction/manifold_row_build.rs:17,27 | `接触点集合の法線と接線の連立` | 落ちた条件1・3(名詞1語の圧縮。何が連立されているか不明) | 検査定義の「オーナーが不合格にした実例」に挙げられている型名そのもの(「連立」だけでは方程式か行列か分からない)。定義は本区画の外(`normal_tangential_system`)にあるが、本区画の`manifold_row_build.rs`が引数の型・importとして直接書いている。改名の素案(定義済みの裁定): `接触点集合の法線と接線の連立方程式`

### 5. ASCII1文字を混ぜた識別子群(パラメータ・フィールド・関数名)

- crates/blitz_sim/src/contact/static_friction/manifold_row_build.rs:28-29 | `錨a`, `錨b`(引数名) | 落ちた条件: ASCII混入の禁止 | 「錨」という日本語名詞にASCIIの`a`/`b`を添字として直結しており日本語として読めない。同ファイル内で32,33,38,42行目でも使用。改名の素案: `第一の錨`, `第二の錨`(または`参加者一の錨`, `参加者二の錨`)
- crates/blitz_sim/src/contact/static_friction/projection.rs:23-24 | `錨a`, `錨b`(引数名) | 落ちた条件: ASCII混入の禁止 | 同上。同ファイル内27,28,33,34,37,41,42,45,46行目でも使用。改名の素案: 同上
- crates/blitz_sim/src/contact/static_friction/manifold_cone_tests.rs:17 | `x軸向きの接線の乗数`(関数名) | 落ちた条件: ASCII混入の禁止 | 「x軸」というASCII文字「x」を関数名の内部に直結しており、識別子として日本語で完結していない。同ファイル19,26,59行目でも使用。改名の素案: `エックス軸向きの接線の乗数`は不可(カタカナでも同じ問題)。方向を名指す既存の型(`方向<ワールド>`)の生成元を関数名へ書く形で `一軸向きの接線の乗数` へ一般化するか、「エックス軸」という固有の軸の呼称自体をユビキタス言語へ登録してから使う
- crates/blitz_sim/src/contact/static_friction/tangential_multiplier.rs:17-19 | `x成分`, `y成分`, `z成分`(フィールド名) | 落ちた条件: ASCII混入の禁止 | ASCIIの軸名`x`/`y`/`z`を「成分」という日本語語幹に直結している。同ファイル26-28(生成関数)・35-37(向きへの増分を足す)・43,49(大きさ・三つの成分)・58-60(Add実装)でも使用。改名の素案: `横成分`, `縦成分`, `奥成分`等ワールド座標系の名を日本語の方位語で表すか、既存のユビキタス言語に軸の日本語呼称があればそれに従う
- crates/blitz_sim/src/contact/static_friction/result.rs:18-19 | `補正a`, `補正b`(列挙の枝が持つフィールド名) | 落ちた条件: ASCII混入の禁止 | 「補正」にASCIIの`a`/`b`を添字として直結している。crates/blitz_sim/src/contact/static_friction/projection.rs:45-46(構築)、isotropy_tests.rs:34,40(分解・使用)、projection_tests.rs:23,29,30(分解・使用)でも同名が使われている。改名の素案: `第一の参加者の補正`, `第二の参加者の補正`

## 違反の疑い

- crates/blitz_sim/src/contact/scene/numeric_contract_record/tolerance_dependence/row_admission/six_axes.rs:18 | `候補一ダッシュの一本`(構造体名。candidate(i')を指す) | 条件1に対する疑い | 数式の記号(プライム記号)を「ダッシュ」というカタカナへ変換して埋め込んだ命名で、ASCII・ギリシャ文字は含まないため機械検査は通るが、「候補一ダッシュ」だけを見て「候補(i')の別名」であると初見で復元できるかは怪しい。ファイル冒頭のコメントが無いと意味が取れない。ただし同種の構造体`候補一の一本`(rhs_zero_across_axes.rs:24)と対で使われ、コメントで明示されているため、確定の不合格とはしない
- crates/blitz_sim/src/contact/scene/numeric_contract_record/tolerance_dependence/unexplained_axes/rhs_zero_failure_locator.rs:80-84 | `向き{k}`という書式文字列内の変数`k`(for文のループ変数) | 条件2の対象外の疑い | 数式の添字をそのままASCII1文字の変数名にしている。ただし対象は日本語識別子ではなく、数学の添字としての慣用(for i in ...と同型)であり、今回の検査対象(日本語識別子)の枠内では確定の違反として扱わない

## 違反ゼロの範囲

- crates/blitz_sim/src/contact/static_friction/anchor.rs、mod.rs、manifold_cone.rs、manifold_row_result.rs、manifold_tentative.rs、projection_fixture.rs、substep_state.rs、tentative_point_multiplier.rs の8ファイルは、直読の結果、日本語識別子がすべて受理条件8つを満たしており違反は無い
- crates/blitz_sim/src/contact/scene/numeric_contract_record/tolerance_dependence/stage_c/mod.rs、unexplained_axes/amplifier_formula.rs の2ファイルは、モジュール宣言または独立した計器の型のみを持ち、識別子はすべて受理条件を満たしている
- 上記「違反の一覧」で挙げた識別子を除き、残る27ファイルの他の識別子(場合を組む・記録・要約を綴る・分類を綴る・一組を綴る・場面を組む等の関数群、傾きの正接・鉛直軸まわりの回しの度・許容差の比等のフィールド群、法線反力が無い・接線変位が零・両方が動かせない等の列挙の枝)は、型名・レシーバによる補完も含めて受理条件8つを満たしており違反は無い
