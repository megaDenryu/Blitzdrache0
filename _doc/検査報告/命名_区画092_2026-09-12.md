# 命名検査報告 区画092

- 区画名: crates/blitz_render/src/renderer/generate/generate_resources 他(指示の区画名と実際のファイル一覧に食い違いがあったため、`block_092.txt`のファイル一覧をそのまま検査範囲とした)
- 対象ファイル数: 36
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 36 / 36(全ファイルを`Read`で全文読み、grepだけで済ませたファイルは無い)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「専門用語の表記」「呼称と語の選択」「日本語命名の質」「日本語命名の受理条件」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

1. `crates/blitz_render/src/renderer/generate/generate_resources/base_resources/shared/create.rs:19` | `生成する`(`pub(in crate::renderer::generate) fn 生成する`。呼び出し側は`shared::create::生成する(...)`) | 条件2・5 | モジュール`create`はASCIIでありゼロ補完。呼び出し元(`base_resources.rs:40`)では`shared::create::生成する(...)`と読まれ、何を生成するか名前単独で答えられない。受理条件5が挙げる不合格例そのもの(`create::生成する`→`共有資源を確保する`)と同型の実例である | 改名案: `共有資源を確保する`

2. `crates/blitz_render/src/renderer/generate/generate_resources/base_resources.rs:31` | `組み立てる`(`pub(super) fn 組み立てる`。呼び出し側は`base_resources::組み立てる(...)`) | 条件2・3・5 | モジュール`base_resources`はASCII。「組み立てる処理です」は答えになるが「何を」が無く、受理テストの5W1H最小要件(何を+どうする)を満たさない。`生成する・作成する`と同種の裸動詞である | 改名案: `基礎資源を組み立てる`

3. `crates/blitz_render/src/renderer/generate/generate_resources/bundle.rs:25` | `束ねる`(`pub(super) fn 束ねる`。呼び出し側は`bundle::束ねる(...)`) | 条件2・3・5 | モジュール`bundle`はASCII。段別資源をフレーム資源へ束ねる処理だが、名前だけでは何を束ねて何になるか読めない | 改名案: `段別資源をフレーム資源へ束ねる`

4. `crates/blitz_render/src/renderer/generate/generate_resources/cloth_resources.rs:15` | `組み立てる`(`pub(super) fn 組み立てる`。呼び出し側は`cloth_resources::組み立てる(...)`) | 条件2・3・5 | モジュール`cloth_resources`はASCII。返り値は`Option<布一式>`だが名前に現れない | 改名案: `布一式を組み立てる`

5. `crates/blitz_render/src/renderer/generate/generate_resources/particle_resources.rs:15` | `組み立てる`(`pub(super) fn 組み立てる`。呼び出し側は`particle_resources::組み立てる(...)`) | 条件2・3・5 | モジュール`particle_resources`はASCII。返り値は`Option<粒子リソース一式>`だが名前に無い | 改名案: `粒子リソース一式を組み立てる`

6. `crates/blitz_render/src/renderer/generate/generate_resources/screen_linked_resources.rs:21` | `組み立てる`(`pub(super) fn 組み立てる`。呼び出し側は`screen_linked_resources::組み立てる(...)`) | 条件2・3・5 | モジュール`screen_linked_resources`はASCII。返り値は`画面連動資源`だが名前に無い | 改名案: `画面連動資源を組み立てる`

7. `crates/blitz_render/src/renderer/generate/generate_resources/simulation_resources.rs:18` | `組み立てる`(`pub(super) fn 組み立てる`。呼び出し側は`super::cloth_resources::組み立てる`とは別に、自身も`simulation_resources::組み立てる`) | 条件2・3・5 | モジュール`simulation_resources`はASCII。戻り値は`(Option<スキニング一式>, Option<布一式>)`だが名前に無い | 改名案: `スキニングと布の資源を組み立てる`

8. `crates/blitz_render/src/renderer/scene_draw_resources/work_area.rs:44` | `プリミティブ発行.引く(チャンク.id(), 対象添字)`(定義は本区画外の`frame_input/primitive_issue_tray.rs:42`) | 条件7 | 「引く」はlookupの直訳調であり、CLAUDE.mdが既に確定した訳語表(`lookup/sampleの「引く」→「参照する」`)に反する。英語を知らないと動作(添字引きの照会)を復元できない | 改名案: `プリミティブ発行.参照する(...)`(定義側`primitive_issue_tray.rs`のメソッド名も同時に改名する必要がある)

## 違反の疑い(断定しない)

- `crates/blitz_render/src/renderer/generate/generate_resources/post_process_resources.rs:37` | `描画先構成::組み立てる(&self, ...)` | 条件1・3に対する疑い | レシーバは`描画先構成`だが、実際に組み立てて返すのは`Option<ポスト処理一式>`であり、レシーバ自身を組み立てるのか・レシーバを条件にして別の資源を組み立てるのかが名前だけでは紛れる。値オブジェクト様式の「型名が目的語を担う」パターンとは方向が逆(生成するのが自分自身でない)であるため、機械的に合格とは判定しない
- `crates/blitz_render/src/renderer/scene_draw_resources/work_area.rs:56` | `object_entry::積む(...)` | 条件2・3に対する疑い | 呼び出し元(本区画内)では`object_entry`というASCIIモジュール名の直下の裸動詞に見えるが、定義自体(`work_area/object_entry.rs`)は本区画のファイル一覧に含まれていないため、レシーバ補完や目的語の有無を定義側まで見て断定できない
- `crates/blitz_render/src/renderer/present_resources/create.rs`(と`destroy.rs`・`presentation/permit.rs:46`) の構造体フィールド`swapchain`(型`vk::SwapchainKHR`) | 条件2に対する疑い | 同じ構造体の他フィールドはすべて日本語(`深度バッファ`・`提示同期`)であるにもかかわらずここだけASCIIの生名詞である。`_doc/計画/ユビキタス言語.md`は「スワップチェーン」をカタカナのまま維持する方針(Vulkan仕様書の綴りに合わせる)であり、フィールド名自体を英語綴りにする方針ではないため、`スワップチェーン`への改名候補として報告する(受理条件の直接の条文というより「日本語命名を積極使用」原則との不整合)
- `crates/blitz_render/src/renderer/generate/generate_resources/base_resources/shared/create.rs:21-22` の引数`queue: vk::Queue, queue_family_index: u32` | 条件2に対する疑い | 同じ関数の他の引数(`確保係`・`表容量`・`影の一辺`・`照明束縛`)はすべて日本語であり、ここだけ英語のローカル識別子である。改名候補: `キュー: vk::Queue, キューファミリ添字: u32`

## 違反ゼロの範囲

以下のファイル群は受理条件8つ・命名の質・呼称と語の選択のいずれにも抵触する識別子を確認しなかった: `request.rs`、`base_resources/scene_bundle.rs`、`base_resources/shared.rs`、`injection/`配下3ファイル(`indirect_lighting.rs`・`synthetic_depth.rs`・`temporal_reconstruction.rs`、いずれも`レンダラー`型のメソッドとして完全な日本語名で書かれている)、`lighting_query_write/origin_tests.rs`と`origin_tests/fixture.rs`、`pass_tally/atmosphere.rs`・`pass_tally/indirect_lighting.rs`(「記録」の1語1義もこの2ファイルで確認済み。型名`大気のベイク済み画像生成パス数の記録`・`間接照明生成パス数の記録`が対象を明示しており、旧オーナー裁定の「記録」誤読の再発は無い)、`presentation/lifecycle.rs`・`presentation/state.rs`・`presentation/state/state_tests.rs`、`queries/`配下3ファイル、`scene_draw_resources/bundle_lifecycle.rs`・`chunk_draw_resources.rs`・`create.rs`・`render_object_resources.rs`、`bundle_lifecycle/pending_bundle.rs`・`replace.rs`、`chunk_draw_resources/create.rs`・`descriptor_pick.rs`。
