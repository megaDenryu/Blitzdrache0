# 命名検査報告 区画068

- 区画名: crates/blitz_game/src 第1区画(区画番号068、40ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main, db815090時点。編集していない)
- 直読したファイル数: 40 / 40(grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`

## 直読したファイル一覧(40件、全件を1行ずつ目視した)

body_capsule.rs / camera_occlusion.rs / camera_occlusion_input.rs / camera_occlusion_test_fixture.rs /
camera_occlusion_tests.rs / camera_recovery_tests.rs / confirmed_input.rs / crest_edge.rs / destination.rs /
display_distance_decision.rs / facing_azimuth.rs / facing_azimuth_tests.rs / forward_azimuth.rs /
fox_tour_route.rs / game_intent.rs / game_intent_tests.rs / game_state.rs / game_state_tests.rs /
ground_height.rs / ground_height_tests.rs / half_space_face.rs / horizontal_unit_vector.rs / lib.rs /
moved_fraction.rs / occlusion_verdict.rs / operation_axis.rs / planar_test_world.rs / planar_test_world_tests.rs /
player_move.rs / player_move_facing_tests.rs / player_move_tests.rs / player_placement.rs / player_state.rs /
previous_display_distance.rs / progress_stage.rs / stage_transition.rs / stage_transition_tests.rs /
sweep_answer.rs / sweep_completeness.rs / sweep_contact.rs

## 違反の一覧

### 違反1: `ゲームインテント`(型名) — 未翻訳の英語借用語(業界カタカナ語)

- ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案
- `crates/blitz_game/src/game_intent.rs:12` | `ゲームインテント`(pub struct) | 呼称と語の選択・条3(カタカナ語は小学生が分かるものだけ)/ 受理条件7(直訳の検出質問) | 「インテント」は英語 intent のカタカナ転写であり、フォールバック・ラウンドトリップ・マイルストーンと同じ業界カタカナ語である。小学生は元より、英語を知らない大人もこの語だけから「意図」を復元できない。しかも同じファイルのコメント(1行目)は既に「操作の意図」という正しい日本語で同じ概念を説明しており、識別子だけが未翻訳になっている | `ゲーム操作の意図`(ユビキタス言語.md630行の定義文「ゲームロジック層が消費する操作の意図」をそのまま名詞化)
- `crates/blitz_game/src/game_intent.rs:21` | `impl ゲームインテント` | 同上 | 同上 | 同上に追従

この型は `_doc/計画/ユビキタス言語.md`630行に既に登録されている用語であり、新規の命名ではない。ただし登録済みであることは受理条件を免除しない(呼称と語の選択の条2「新しい言い換えを発明しない」は識別子側が業界カタカナ語のまま登録された経緯までは正当化しない)。ローラー検査の対象として報告する。

### 違反2: `インテント`(フィールド・変数・引数名として単体使用) — 同型の未翻訳借用語、この区画内の使用箇所

`一刻みの移動の入力`型のフィールド`インテント`自体の定義は`crates/blitz_game/src/movement/movement_input.rs:12`にあり、この区画(068)の対象ファイル一覧に含まれないため定義箇所は対象外である。ただし、この区画に含まれるファイルがこのフィールドを読み書きしている箇所は対象内であり、フィールド名の借用語がそのまま識別子として登場するため違反として報告する。

- `crates/blitz_game/src/game_state.rs:48` | `入力.インテント` | 受理条件7(直訳) | 「インテント」は意図の未翻訳。同じ型`一刻みの移動の入力`の他フィールドは`刻み`・`前へ進む向き`と平易な日本語であり、この1フィールドだけ未翻訳の借用語になっている(1語1義以前に、同じ構造体内で訳語の方針が割れている) | フィールド名を`操作の意図`へ(構造体定義側の是正が必要)
- `crates/blitz_game/src/player_move.rs:25,30` | `入力.インテント` | 同上 | 同上 | 同上
- `crates/blitz_game/src/stage_transition.rs:12,14,15,18,25,26,31,37,38,47,49,52` | 引数・変数名`インテント`(`ゲームインテント`型の値を受ける仮引数) | 同上 | 型名`ゲームインテント`自体が未翻訳であるため、そのレシーバ変数名も同じ借用語をそのまま踏襲している | 型の改名(違反1)に追従して`意図`または`操作の意図`
- `crates/blitz_game/src/game_state_tests.rs:35,46,53` | `インテント`(フィールド初期化子・関数名`前へ進むインテント`・`決定だけのインテント`) | 同上 | 同上 | `前へ進む意図`・`決定だけの意図`
- `crates/blitz_game/src/player_move_tests.rs:19,26,43,48,56,74,81,89,103` | `インテント`(関数名`前へ倒したインテント`・`右へ倒したインテント`・仮引数`インテント`) | 同上 | 同上 | `前へ倒した意図`・`右へ倒した意図`
- `crates/blitz_game/src/player_move_facing_tests.rs:18,25,34,38,57,64,71` | 同上のパターン | 同上 | 同上 | 同上
- `crates/blitz_game/src/stage_transition_tests.rs:6,13` | 関数名`決定だけのインテント`・`キャンセルだけのインテント` | 同上 | 同上 | `決定だけの意図`・`キャンセルだけの意図`

違反1・違反2は同一の根本原因(借用語`インテント`の不採用な訳語選択)であるため改名は1回のリネーム作業(型名→フィールド名→変数名の順にLSP rename)で連鎖的に解消できる。この区画の外(`movement/`配下、区画068の対象外)にも同型の使用が多数あることをgrepで確認済みだが、それらは別区画の担当であるため本報告では列挙しない(対象: `crates/blitz_game/src/movement/*.rs`)。

## 違反の疑い

なし。

## 違反ゼロの範囲

上記2件の違反(いずれも`インテント`という1つの借用語に起因)を除き、40ファイル中の残る識別子(型名・関数名・メソッド名・フィールド名・変数名・定数・列挙の枝)はすべて受理条件8項目・日本語命名の質・呼称と語の選択・オーナー不合格実例の3形(実体と合わない語・名詞1語の圧縮・詰め込みすぎ)・英字/ギリシャ文字/記号混入のいずれにも該当しなかった。特に`胴体カプセル`・`カメラの遮蔽と復帰`・`表示距離の決定`・`動く個体が向いている方位角`・`前へ進む向きの方位角`・`目的地`・`足元の地面の高さ`・`半空間の面`・`水平の稜`・`動けた割合`・`操作軸の倒し量`・`遮蔽の判定`・`掃引の答え`・`掃引の完全性`・`掃引の接触`・`ゲームの進行段階`・`終了確認から戻る段階`・`場所巡りのゲームの状態`・`確定済みの操作入力`・`プレイヤーの位置と向き`・`プレイヤーの状態`・`前の描画の表示距離`は、名前の語が定義済みの実体(フィールド・枝)と一致しており、5W1Hの必要格を満たし、品詞の規律(値=名詞句・処理=動詞句・真偽=〜か)にも従っていた。
