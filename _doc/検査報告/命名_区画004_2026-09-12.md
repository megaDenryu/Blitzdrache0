# 命名検査報告 区画004: crates/blitz_app/src/app/frame_dump ほか(39ファイル)

- 区画名と範囲: 区画番号004。`crates/blitz_app/src/app/frame_dump/`・`frame_timing/`・`handler/`・`hot_reload_asset_apply/`・`persistent_bundles/`・`primitive_draw_item_registry/`・`queries/`・`scene_camera/`・`scene_lighting/`配下の39ファイル。一覧は`scratchpad/roller/block_004.txt`。
- 時点: 作業ツリー`C:/devs/Blitzdrache0_roller`、mainのdb815090。このツリーは編集していない。
- 直読したファイル数: 39/39(全ファイル直読)。grepだけで済ませたファイルは0件。
- 参照した定義: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(HDR・描画束ID・ディスクリプタセット等の登録語を確認)。

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `frame_dump/presentation_dump.rs:50-53` | `rawパス`(変数) | 条件2・条7(直訳検出)・呼称と語の選択条1 | 英語「raw」をローマ字のまま使い、日本語訳(生・未加工・提示用)を与えていない。同じディレクトリの`depth_dump.rs`は同じ役割の変数を`深度パス`と日本語訳しており、この工程だけ英語のまま残っている | `提示画像のパス` |
| `frame_timing/frame_timing_tests.rs:11` | `統計.平均ms` | 条件2・呼称と語の選択条1 | 単位「ミリ秒」をASCII記号「ms」へ切り詰めている。同じ区画の`scene_camera`配下は`距離倍率`・`注視点の水平メートル`のように単位を全部仮名で書く慣習であり、この型だけ逸脱する | `平均ミリ秒` |
| `frame_timing/frame_timing_tests.rs:12` | `統計.p50ms` | 条件1・条件2・条件7 | 統計記号「p」と数字「50」とASCII単位「ms」が1つの識別子へ結合しており、パーセンタイルの知識が無い読み手はもちろん、知っていても「p50ms」だけから「50パーセンタイル値をミリ秒で表したもの」だと即座に復元できない | `五十パーセンタイル値ミリ秒` |
| `frame_timing/frame_timing_tests.rs:13` | `統計.p95ms` | 同上 | 同上 | `九十五パーセンタイル値ミリ秒` |
| `frame_timing/frame_timing_tests.rs:14` | `統計.p99ms` | 同上 | 同上 | `九十九パーセンタイル値ミリ秒` |
| `frame_timing/frame_timing_tests.rs:15` | `統計.最大ms` | 条件2 | 単位をASCII記号へ切り詰めている | `最大ミリ秒` |
| `frame_timing/frame_timing_tests.rs:16` | `統計.二十五ms超過数` | 条件2・呼称と語の選択条1(圧縮の禁止) | 漢数字「二十五」とASCII単位「ms」と漢字「超過数」が1つの語の中で3種の文字体系を圧縮結合しており、「二十五ミリ秒を超えた回数」だと一読で読み取れない | `二十五ミリ秒超過数` |
| `frame_dump/cluster_assignment_check.rs:10` | `報告する`(`pub(super) fn`、ASCIIモジュール`cluster_assignment_check`直下) | 条件2・条件3・条件5 | 呼び出し側は`presentation_dump.rs`から`cluster_assignment_check::報告する(アプリ, 視点情報)`と読む。モジュール名`cluster_assignment_check`はASCIIであり受理条件2により補完力ゼロのため、「報告する」だけでは何を報告するか(クラスタ選別の割り当て統計)が識別子から分からない。`create::生成する`型の裸の動詞そのものである | `クラスタ選別の割り当て統計を報告する` |
| `frame_dump/sky_pixel_check.rs:14` | `照合する`(`pub(super) fn`、ASCIIモジュール`sky_pixel_check`直下) | 条件2・条件3・条件5 | `presentation_dump.rs`から`sky_pixel_check::照合する(アプリ, &画像, 視点情報)`と呼ばれる。何と何を照合するか(空の代表画素と天空の再現条件)が名前に無い | `空代表画素を照合する` |
| `frame_dump/indirect_probe_check.rs:14` | `照合する`(同上、モジュール`indirect_probe_check`) | 条件2・条件3・条件5 | `indirect_probe_check::照合する(...)`と呼ばれ、何を照合するか(間接照明代表板の解析入力)が名前に無い | `間接照明代表板を照合する` |
| `frame_dump/depth_dump.rs:18` | `読み戻して書き出す`(`pub(super) fn`、モジュール`depth_dump`) | 条件2・条件3・条件5 | 呼び出し側は`depth_dump::読み戻して書き出す(...)`と読む。何を読み戻して書き出すか(最終深度)が名前に無い | `最終深度を読み戻して書き出す` |
| `frame_dump/hdr_dump.rs:17` | `読み戻して書き出す`(モジュール`hdr_dump`) | 条件2・条件3・条件5 | `hdr_dump::読み戻して書き出す(...)`。何を読み戻すか(明るさの圧縮前の中間画像)が名前に無い | `明るさ圧縮前画像を読み戻して書き出す` |
| `frame_dump/presentation_dump.rs:16` | `読み戻して書き出す`(モジュール`presentation_dump`) | 条件2・条件3・条件5 | `presentation_dump::読み戻して書き出す(...)`。何を読み戻すか(提示画像)が名前に無い | `提示画像を読み戻して書き出す` |
| `frame_dump/dump_destination.rs:18` | `決める`(`pub(super) fn`、モジュール`dump_destination`) | 条件2・条件3・条件5 | `dump_destination::決める(アプリ, 基準名, アクション)`と呼ばれる。何を決めるか(ダンプ先のファイル名)が名前に無い | `ダンプ先のパスを決める` |
| `primitive_draw_item_registry/refill.rs:13` | `詰め直す`(`pub(super) fn`、モジュール`refill`) | 条件2・条件3(境界は弱いが該当) | `refill::詰め直す(台帳, 受け皿)`と呼ばれる想定。何を詰め直すか(プリミティブ描画発行)が名前に無い | `プリミティブ描画発行を詰め直す` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `handler/dev_ui_toggle.rs:11` | `f3押下を確認する` | 条件2(ASCII混入) | 「F3」は実在するキーボードのキー名で固有名詞に近く一律禁止とは言い切れないが、識別子側は同ファイルのコメント「F3キー押下」から「キー」を落としており、単独では押しているのがキーだと確信を持って読めない可能性がある。改名案: `f3キー押下を確認する` |
| `frame_dump/depth_dump.rs:37`、`frame_dump/hdr_dump.rs:55`、`frame_dump/presentation_dump.rs:39` | `書き出す`(private fn、各ファイル内でのみ呼ばれる) | 条件1・条件3 | 同一ファイル内だけで呼ばれる私有関数であり、ファイル冒頭のモジュール文書が対象を説明しているため文脈は強い。ただし識別子だけを見た受理テストでは「何を書き出すか」が答えられない。上表の`読み戻して書き出す`と対で直すのが自然なため、確定違反でなく疑いとして分けた |
| `frame_dump/hdr_dump.rs:62-65` | `hdrパス`(変数) | 条件2 | 「HDR」自体は`_doc/計画/ユビキタス言語.md`に登録・注釈済みの用語であり頭字語混入の禁止には当たらない可能性が高い。ただし同じ区画の`depth_dump.rs`が同役割の変数を`深度パス`と全訳しているのに対し、この変数だけ英字表記のまま`パス`と結合しており、区画内の一貫性の観点で違反の疑いを残す |

## 検査したが違反ゼロと判定した範囲

- `scene_camera/`配下の11ファイル(`fox_tour_view.rs`・`fox_view.rs`・`night_lights_view.rs`・`part_frame_row_view.rs`・`part_house_row_view.rs`・`part_tree_row_view.rs`・`prop_view.rs`・`shadow_view.rs`・`stone_hut_doorway_view.rs`・`terrain_origin_view.rs`・`vegetation_view.rs`・`village_view.rs`・`visual_sample_view.rs`。数え直すと13ファイル、以下も参照)。定数名(`方位度`・`俯角度`・`距離倍率`・`注視点の水平メートル`・`注視点の高さメートル`)、関数名(`姿勢を与える`)が単位・対象を仮名で明示しており、条件1〜8をすべて満たす
- `scene_lighting/cascade_policy.rs`・`indirect_probe.rs`・`large_world_bands.rs`・`light_count_tests.rs`・`night_lights.rs`・`night_lights/lamp_field.rs`・`night_lights/lamp_field_tests.rs`・`night_lights/lamp_field/order.rs`・`stone_hut_interior.rs`・`stone_hut_interior/lamp_row.rs`・`vegetation_verification.rs`: 関数名(`方針を作る`・`灯の一覧を作る`・`中心から近い順の格子添字`等)がすべて対象語を含み、値を返す関数は名詞句、処理は動詞句という品詞の規律も守られている
- `queries/renderer_metrics.rs`・`queries/report_request.rs`: `gpu`・`ui`はユビキタス言語およびグローバル規約の許容一覧(GPU・UI)に含まれる登録語であり、小文字化はプロジェクト全体の一貫した命名慣習(Rustのsnake_case)であるため違反としない
- `handler/resume.rs`・`hot_reload_asset_apply/validation.rs`: 変数・関数名はすべて対象語を伴う動詞句または名詞句であり違反なし
- `persistent_bundles/placement.rs`・`primitive_draw_item_registry/refill.rs`の`束id`/`描画束ID`: `_doc/計画/ユビキタス言語.md`に「描画束ID」として登録済みの用語のsnake_case表記であり、条件2の「ASCII頭字語の無登録混入」には該当しない
