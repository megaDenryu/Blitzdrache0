# 命名検査(全域ローラー) 区画009: crates/blitz_app/src/cli 第2区画

- 対象: 「crates/blitz_app/src/cli 第2区画」(区画番号009、15ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main、db815090時点。編集はしていない)
- 直読したファイル数: 15/15(全ファイルを直読。grepだけで済ませたファイルは0件)
- 対象ファイル一覧:
  1. crates/blitz_app/src/cli/screen_pixel_args.rs
  2. crates/blitz_app/src/cli/setting_apply.rs
  3. crates/blitz_app/src/cli/shadow_args.rs
  4. crates/blitz_app/src/cli/streaming_settings.rs
  5. crates/blitz_app/src/cli/streaming_value_args.rs
  6. crates/blitz_app/src/cli/temporal_reconstruction_settings.rs
  7. crates/blitz_app/src/cli/time_args.rs
  8. crates/blitz_app/src/cli/time_args_tests.rs
  9. crates/blitz_app/src/cli/time_of_day_flags.rs
  10. crates/blitz_app/src/cli/time_of_day_settings.rs
  11. crates/blitz_app/src/cli/types.rs
  12. crates/blitz_app/src/cli/value_args.rs
  13. crates/blitz_app/src/cli/verification_plan.rs
  14. crates/blitz_app/src/cli/xpbd_solver_bench_request.rs
  15. crates/blitz_app/src/cli/xpbd_solver_bench_request_tests.rs
- 使った必読リスト: グローバルCLAUDE.mdの「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、CLAUDE.mdの「命名」節、`_doc/計画/ユビキタス言語.md`(該当語を grep で照合)。判定範囲はこの4節+ユビキタス言語の一致確認に限定し、それ以外の観点(構造・分割・型安全性等)は対象外とした。

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| streaming_settings.rs:22 | `Ow3Dod経路` (enum `プレイヤー位置源` の枝) | 受理条件1・識別子へのASCII英字/数字の混入禁止(実例3参照) | 「Ow3Dod」は大文字小文字と数字を混ぜた短縮ラベルで、`cargo xtask ow3-dod`という外部コマンド名を知らないと1文字も復元できない。オーナーが不合格にした「段階A2」と同じ「コード化した略号を識別子に持ち込む」形である | `大規模世界定点巡回経路`(またはOW3のDoD検証で使う経路であることを名前へ書いた別案。ASCIIのxtaskコマンド名は`参照:`コメントへ移す) |
| value_args.rs:18 | `frames引数を処理する` | 受理条件2・7 | ASCII語「frames」は英語を知らない読み手には読めず、モジュール名(value_args、ASCII)も補完に使えない。同じ概念を指す日本語「フレーム数」がすぐ下の行(`起動引数エラー::フレーム数不正`)に既にあり、1つの値に英語表記と日本語表記が両方存在する状態になっている | `フレーム数引数を処理する` |
| value_args.rs:24 | `benchmark_frames引数を処理する` | 受理条件2・7 | 同上。「benchmark」を知らないと復元できない | `ベンチのフレーム数引数を処理する` |
| value_args.rs:30 | `scene引数を処理する` | 受理条件2・7 | 「scene」は英単語のまま。戻り値型`起動時シーン`があるのに関数名は英語のまま | `シーン引数を処理する` |
| value_args.rs:34 | `object_count引数を処理する` | 受理条件2・7 | 「object_count」は英語のまま。同じ概念の日本語「描画対象数」がファイル内に既出 | `描画対象数引数を処理する` |
| value_args.rs:74 | `exposure引数を処理する` | 受理条件2・7 | 「exposure」は英語のまま。戻り値型`露出倍率`が日本語であるのに関数名だけ英語 | `露出引数を処理する` |
| value_args.rs:80 | `blend引数を処理する` | 受理条件2・7 | 「blend」は英語のまま。戻り値型`アニメーションのブレンド係数`は日本語(カタカナ)であるのに関数名だけ英語 | `ブレンド引数を処理する` |
| shadow_args.rs:45 | `shadow_resolution引数を処理する` | 受理条件2・7 | 「shadow_resolution」は英語のまま。戻り値型`影の一辺解像度`は日本語であるのに関数名だけ英語 | `影の一辺解像度引数を処理する` |
| shadow_args.rs:53 | `max_shadow_distance引数を処理する` | 受理条件2・7 | 同上。戻り値型`メートル`だが対応する設定フィールドは`最大影距離` | `最大影距離引数を処理する` |
| shadow_args.rs:63 | `shadow_caster_range引数を処理する` | 受理条件2・7 | 同上。戻り値型`影の視距離`が日本語 | `影の視距離引数を処理する` |
| shadow_args.rs:70 | `caster_margin引数を処理する` | 受理条件2・7 | 同上。対応する設定フィールドは`キャスター余白` | `キャスター余白引数を処理する` |
| time_args.rs:12 | `time_of_day引数を処理する` | 受理条件2・7 | 「time_of_day」は英語のまま。対応するフィールドは`一日内時刻の秒` | `一日内時刻引数を処理する` |
| time_args.rs:22 | `time_scale引数を処理する` | 受理条件2・7 | 同上。戻り値型`時間倍率`が日本語 | `時間倍率引数を処理する` |
| screen_pixel_args.rs:19 | `report_sky_pixel引数を処理する` | 受理条件2・7 | 「report_sky_pixel」は英語のまま。戻り値`Vec<画面画素位置>`、対応フィールドは`空の代表画素` | `空の代表画素引数を処理する` |
| streaming_settings.rs:27, setting_apply.rs:52-58 | `LOD継ぎ目検査設定`(型)/`lod継ぎ目検査`(フィールド)/`Lod継ぎ目検査不正`(使用箇所のエラー枝) | 日本語命名の質(頭字語の禁止)・受理条件6(1語1義) | 「LOD」はCPU/GPU/UI/APIのような一般に通用している語ではなく、非専門家には復元できない頭字語である。しかも同じ概念を指す綴りが`LOD`(型名)・`lod`(フィールド名)・`Lod`(setting_apply.rs内のエラー枝呼び出し)の3通りに揺れている。同じクレート内に「地形詳細段」「個体詳細段」というLODの確立した訳語が既にあるのに、この一群だけ英語の頭字語のまま残っている | `詳細段継ぎ目検査設定`/`詳細段継ぎ目検査`/`詳細段継ぎ目検査不正`のように確立訳語へ統一する |
| screen_pixel_args.rs:20,26(使用箇所) | `空代表画素不正` | 日本語命名の質(助詞を省いた膠着圧縮の禁止) | 同じ概念を指す設定フィールド名は`空の代表画素`(の入り)なのに、エラー枝の名前だけ助詞を落として`空代表画素`と圧縮されている。「大気LUT生成指示」を「大気のベイク済み画像の生成指示」に直した規範と同じ違反であり、フィールド名と1文字も揃わないため同じ概念だと即座には気づけない | `空の代表画素不正` |

## 違反の疑い

| ファイル:行 | 識別子 | 疑いの条件 | 理由 |
|---|---|---|---|
| types.rs各所(例: 74-78行`インスタンス可視判定有効`・`インスタンス段選択有効`・`インスタンス影キャスター有効`・`影キャスター全体有効`・`遠景影キャスター有効`、86行`ポスト処理有効`、80行`開発ui初期有効`) | 「〜有効」を末尾に置く真偽値フィールド群 | 受理条件4(品詞の規律: 真偽は「〜か」) | 規約の文言どおりなら「〜が有効か」の形が要る。ただし`types.rs`だけでなくこのクレート全体で「〜有効」という言い切り形の真偽値フィールドが多数(この区画だけで7件以上)一貫して使われており、独立の造語ではなく確立した命名習慣に見えるため、本区画だけの是正でなく既存の命名規範文書との整合を先に確認すべき事項として疑いに留める |
| streaming_settings.rs:23 | `インスタンスストリーム経路`(enumの枝) | 呼称と語の選択(カタカナ語は小学生が分かるものだけ) | `--instance-stream`というxtaskコマンド名の直訳で、確立訳語「ストリーミング」ではなく短縮形「ストリーム」を使っている。「インスタンス」「ストリーミング」自体はユビキタス言語に登録済みの語だが、「ストリーム」(ing抜き)は同じファイル内の`ストリーミング起動設定`他と表記が割れている |
| streaming_value_args.rs:19 | ローカル変数`メートル`(f64の生値) | 受理条件6(1語1義) | 同じファイルが依存する`blitz_math::メートル`は検証付きの値オブジェクト型であり、隣接ファイル(shadow_args.rs等)では実際にその型として使われている。ここでは同名を未検証の生の`f64`のローカル変数名として使っており、読み手が「型のメートルか、ただの数値か」を文脈だけで判別する必要がある |

## 違反ゼロの範囲

- `setting_apply.rs`の分岐テーブル自体・`streaming_value_args.rs`の`固定経路引数を反映する`/`読込引数を反映する`・`temporal_reconstruction_settings.rs`・`time_of_day_flags.rs`・`time_of_day_settings.rs`(型名・フィールド名・列挙の枝すべてに助詞が通り、コメントの説明と実体が一致)・`time_args_tests.rs`・`verification_plan.rs`・`xpbd_solver_bench_request.rs`と`xpbd_solver_bench_request_tests.rs`(`XPBD`はユビキタス言語969行に登録済みの確立語であり、方式・グラフの各枝は「原子加算」「グラフ彩色」「二段階」「規則格子」「不規則」と日本語へ翻訳済み)は、識別子の受理条件・命名の質のいずれにも違反が見当たらなかった。
