# 命名検査報告 区画157: xtask/src/instance_lod/judgment 他(instance_stream・large_world_assets・large_world_bench・local_visibility・lod_crack・material_check)

- 区画番号: 157
- 作業ツリー: C:/devs/Blitzdrache0_roller(main、db815090時点。編集していない)
- ファイル一覧: block_157.txt に列挙された39ファイル
- 直読したファイル数: 39/39(grepだけで済ませたファイルは0件)
- 検査軸: 日本語命名(受理条件8つ・命名の質・呼称と語の選択)
- 参照した用語定義: `_doc/計画/ユビキタス言語.md`(該当語の登録有無をgrepで確認。以下で個別に記載)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| xtask/src/local_visibility/term_measure.rs:48-50, term_tally.rs:11,17,24 | `零` `中` `一`(関数引数・ローカル変数、型`[f32; 3]`) | 条件1(受理テスト)・条件3(5W1Hの必要格) | 「零」「中」「一」はそれ自体は完成した日本語の単語だが、ここでは「全遮蔽時のその画素の色」「中間符号値でのその画素の色」「遮蔽なし時のその画素の色」という3つの撮影条件下の画素値を指しており、単独では数値の0/中間/1としか読めず、何の値かが復元できない。呼び出し元(term_measure.rs内の`測る`)では`全遮蔽`・`中間`・`遮蔽なし`という完全な語の画像から取り出しているのに、値そのものの変数名では対応する語が消えている | `全遮蔽時の画素`・`中間時の画素`・`遮蔽なし時の画素`(またはそれぞれ`零側画素`のような圧縮でなく、関数内でも呼び出し元と同じ完全語を使う) |
| xtask/src/lod_crack/image_check.rs:15,16,28,41,44,45(struct`検査結果`のフィールドおよび関数`継ぎ目を検査する`内のローカル変数・関数`番兵背景の露出画素の判定名`の引数) | `roi画素数` | 日本語命名の質(英語の頭字語を日本語へ混ぜない)・条件8関連の助詞省略 | `roi`(region of interest)は画像処理の専門用語で、UI・CPU・GPU・APIのように非専門家が通用語として知っている語ではない。しかも`roi`と`画素数`の間に助詞が無く、「大気LUT生成指示」と同型の膠着圧縮になっている | `関心領域の画素数`または`固定投影帯の画素数`(コメントが実際に指す対象は「固定投影帯」であるため、既存のドメイン語をそのまま使う案を推奨) |
| xtask/src/local_visibility/parse.rs:20(struct`形の行`のフィールド)、judgment.rs:12、tolerance.rs:18(定数名) | `p95誤差` | 日本語命名の質(英語の頭字語+数字を日本語へ混ぜない)・条件8関連の助詞省略 | `p95`は統計用語(95パーセンタイル)の略記で非専門家には読めない。しかも同じ値をjudgment.rsのコメント上では「正本と写しの誤差の第95百分位」という完全な日本語で呼んでおり、フィールド名だけが略記のまま取り残されている(1つの概念に2つの表記が並立) | `第95百分位誤差`(tolerance.rsの`第95百分位誤差の許容`と語を揃える) |
| xtask/src/large_world_bench/arguments.rs:19-20(struct`大規模世界の計測指定`のフィールド)、launch.rs:27-28,34-35 | `ram上限` `vram上限` | 日本語命名の質(単語らしく見せるための助詞省略・膠着圧縮) | 同じ構造体の隣接フィールド`ワーカー本数`・`要求容量`は完全な日本語句だが、`ram上限`・`vram上限`だけは英語略語と漢語を助詞無しで直結しており、「大気LUT生成指示」と同型の圧縮になっている。同ファイル内でも`cpuの性質`・`gpuの性質`(local_visibility側)は助詞「の」を挟んでいるのに、こちらは挟んでいない不統一もある | `ramの上限バイト数`・`vramの上限バイト数` |
| xtask/src/local_visibility/parse.rs:21(struct`形の行`のフィールド) | `静止一致` | 条件4(品詞の規律。真偽は「〜か」) | 型は`bool`だが名詞句で終わっており、「静止一致」という値(名詞)を保持しているのか、一致したかどうかの真偽なのかが型を見るまで分からない。summary.rsでは同じ名前がラベルとしてそのまま出力されてもいる | `静止2フレームが一致したか`(または`静止したかどうか`と同種の完全な疑問形) |
| xtask/src/large_world_bench/arguments.rs:29(struct`大規模世界の計測指定`のフィールド) | `計画だけ` | 条件4(品詞の規律。真偽は「〜か」) | `bool`型だが「計画だけ」という体言止めであり、「計画だけを表示するモードか」という意味を型を見ずに復元できない | `計画だけを表示するか` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| xtask/src/lod_crack/image_check.rs:8 | `帯半幅画素`(定数) | 日本語命名の質(助詞省略) | 「帯・半幅・画素」の3語が助詞無しで連結されており、「帯の半幅を画素数で表した値」であることが1回で読み取りにくい。全部が漢語のため頭字語混入の実害は無く、既存コードにも同種の圧縮定数名(`容量上限バイト`等)が他区画にも見られるため、プロジェクト全体の慣用表記である可能性がある |
| xtask/src/lod_crack/cases.rs:73 | `存在側段`(ローカル変数) | 日本語命名の質(助詞省略) | 「存在する側の段」の圧縮。ループ変数として使用範囲が数行に限られており実害は小さい |
| xtask/src/instance_stream・local_visibility・material_check の各所で参照される`要約.lod変更フレーム数`・`要約.gpu解除件数`・`計数.validation件数`等 | `lod変更フレーム数` `gpu解除件数` `validation件数` | 日本語命名の質(頭字語混入) | この区画のファイルは`ストリーミング要約報告`・`計数報告`という他区画で定義された型のフィールドを読んでいるだけであり、フィールド自体の定義はこの区画の39ファイルに含まれない。同じ概念を指す`地形詳細段が変わったフレーム`という完全な日本語の判定名が同じファイル群に共存しており(invariance.rs)、定義元を含む区画で改めて検査する必要がある |

## 違反ゼロと確認した範囲

上記に挙げた行を除く、39ファイルの残り全体(型名・構造体宣言・enum・判定名を組み立てる関数群・判定を課すメソッド呼び出し・エラー型とそのDisplay実装・テストコード)は受理条件8つ・命名の質・呼称と語の選択のいずれにも抵触しない。特に判定名を組み立てる`〜の判定名`系関数群、`〜を検査する`/`〜を課す`系の動詞句、エラーenumの枝(`検収の器が破れた`等)は、5W1Hの必要格を満たし単独で「これは何」に答えられる作りになっている。
