# 命名検査報告 区画128

- 区画名: crates/blitz_sim/src/rigid_xpbd
- 範囲: ファイル一覧(block_128.txt)に列挙された38ファイル(rigid_xpbd直下29ファイル、rigid_xpbd/reference配下9ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main、時点 db815090。編集していない)
- 直読したファイル数: 38 / 38(全数を直読。grepだけで済ませたファイルは無い)
- 検査した識別子: 型・関連関数・メソッド・フィールド・変数・定数・enumの枝を各ファイルで全数目視した(概算で400識別子超)
- 参照した用語定義: `_doc/計画/ユビキタス言語.md`の「姿勢自由度」「接続拘束と目標拘束」「目標位置」「剛体接触拘束」等の項

## 違反の一覧

なし(0件)。

## 違反の疑い(条件2: 識別子へのASCII混入)

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_sim/src/rigid_xpbd/twist_test_fixtures.rs:19 | `x軸のねじり拘束`(関数名) | 識別子へのASCII混入の禁止(オーナー実例と同型: `段階A2`・`Σλ_n`) | 数学の軸名`x`をアルファベット1文字のまま日本語の語幹へ埋め込んでいる。日本語の名前として完結しておらず、cargo clippy以前の様式違反に当たる | `エックス軸のねじり拘束`、または軸を引数化して`ねじり拘束を軸から作る(軸: 方向<ローカル>, 静止の相対姿勢: ...)`のように具体のx軸を専用関数名に固定しない形 |
| crates/blitz_sim/src/rigid_xpbd/twist_invariance_tests.rs:12,20 | 上記の呼び出し(use文・呼び出し式) | 同上 | 同上(定義の伝播) | 定義の改名に追従 |
| crates/blitz_sim/src/rigid_xpbd/twist_convergence_tests.rs:13,34,93 | 上記の呼び出し(use文・呼び出し式) | 同上 | 同上(定義の伝播) | 定義の改名に追従 |

判定を「違反」でなく「疑い」に留めた理由: このリポジトリでは`.x()`・`.y()`・`.z()`のような成分アクセサに単独のアルファベット1文字を使う数学ベクトルの語彙が全域で確立しており(`メートル::生成する(x)`等)、`x軸`という組み合わせもその延長として読める可能性がある。ただしこの関数は検査専用のprivateなテスト材料であり、対象は「軸(x)」という1文字の変数でなく識別子トークンの一部としての「x軸」であるため、オーナーが不合格にした`段階A2`・`Σλ_n`と同型に近いと判断し、断定はせず疑いとして報告する。

## 違反ゼロの範囲

上記の疑い1件を除き、rigid_xpbd直下(拘束の引数・一刻みの係数・一回の射影の結果・補正・予測の状態・前の状態・細分の予測器・細分数・ジャイロ項の扱い・回転のコンプライアンス・回転のラグランジュ乗数・剛体の参照計算エラー・姿勢自由度の参加者)、およびreference配下(各拘束のバッチ・点の細分の状態・細分の中の剛体・剛体の参照計算・一刻み進める等の工程)の識別子は受理条件8つ・命名の質・呼称と語の選択のいずれにも違反しない。品詞は値=名詞句・処理=動詞句「〜する」・真偽=「〜か」の規律を一貫して守り、拘束種別ごとの型名(接続拘束・目標拘束・ねじり拘束・点と剛体の距離拘束)はユビキタス言語の定義と1対1で一致する。「記録」「文脈」のような直訳調・裸の動詞・1語1義違反・詰め込みすぎの実体不一致は検出しなかった。
