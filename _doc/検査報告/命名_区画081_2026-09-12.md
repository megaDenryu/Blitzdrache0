# 命名検査報告 区画081

- 区画名: crates/blitz_render/src/cloth_material
- 対象: block_081.txt に列挙された19ファイル
- 作業ツリー: C:/devs/Blitzdrache0_roller(main の db815090。編集していない)
- 時点: 2026-09-12
- 直読したファイル数: 19 / 19(全ファイルを直読した。grepだけで済ませたファイルは0件)

対象ファイル一覧: bending_check.rs, bending_tests.rs, coloring.rs, constants.rs,
constraint_check.rs, error.rs, length_check.rs, material_input.rs, particle_check.rs,
readback.rs, skin_vertex_index.rs, target_check.rs, target_index.rs, target_tests.rs,
target_update_pair.rs, target_update_pair_tests.rs, test_fixtures.rs, tests.rs,
time_step_width.rs

必読リストは `.claude/global-reference/全プロジェクト共通CLAUDE.md` の「日本語命名の受理条件」
「日本語命名の質」「呼称と語の選択」「専門用語の表記」と `_doc/計画/ユビキタス言語.md` を用いた。
「スキン」「スキン頂点添字」「目標の更新対応」「布の目標拘束添字」等は `_doc/計画/ユビキタス言語.md`
へ登録済みの語であり、登録内容と実装のフィールド構成を照合したうえで合格と判定した。

## 違反の一覧

1. `crates/blitz_render/src/cloth_material/readback.rs:9` | `ラグランジュ乗数一覧`(構造体`布の読み戻し`のフィールド) | 条件3(5W1Hの必要格) |
   同じ構造体の兄弟フィールドが `目標拘束のラグランジュ乗数一覧`・`曲げ拘束のラグランジュ乗数一覧` と
   拘束の種類を明示しているのに、この1件だけ種類を持たない。ファイル冒頭の説明が
   「目標拘束と曲げ拘束の乗数は距離拘束の乗数の後ろに続く1本のGPUバッファから写し」と書いており、
   この値が距離拘束の乗数であることは文書を読まないと分からない。3つの中で1つだけ無印にすると、
   読み手は「無印は距離拘束か、それとも何かの既定か」を都度確かめることになる。 |
   改名の素案: `距離拘束のラグランジュ乗数一覧`

## 違反の疑い(断定しない)

1. `crates/blitz_render/src/cloth_material/target_check.rs:67` | `定義域を検証する`(モジュール直下の自由関数) | 条件3に対して疑い |
   同名の処理が `bending_check.rs`(`曲げ拘束の引数の読み::定義域を検証する`)と
   `constraint_check.rs`(`拘束の引数の読み::定義域を検証する`)にもあるが、そちらはレシーバの型名が
   「何の定義域か」を補っている。`target_check.rs`だけはレシーバを持たない自由関数であり、
   名前だけからは目標拘束の定義域だと分からない(引数の1つが`目標: &[u8]`であることからは類推できるが、
   名前自体は補っていない)。呼び出し範囲がファイル内に閉じるため実害は小さく、断定はしない。 |
   改名の素案: `目標拘束の定義域を検証する`

2. `crates/blitz_render/src/cloth_material/constants.rs:17` | `布定数`(構造体名) | 条件7に対して疑い |
   フィールドの実体を定義まで開いて照合すると、重力・粒子間隔・グリッド原点・速度減衰・刻み幅という
   数値の定数群に加え、`自己衝突: 布の自己衝突`という「自己衝突を積むかどうかの選択」を1つ持つ。
   「定数」という語は数値の固定値を指す語感が強く、実行の分岐を選ぶ列挙型を同じ名前の中へ含めることは、
   オーナーが不合格にした「実体と合わない語」の形に近い。ただし型の冒頭コメントが「自己衝突を積むかどうかの
   別」と明記しており、素材の生成時に1回だけ決まって以後変わらない値という点では両者とも「その生成の間は
   不変な設定」であるため、「定数」を「生成1回ぶん不変な設定値の意味で使っている」と読めば許容範囲でもある。
   断定しないが、`布の生成時設定`のような名前も検討に値する。

## 違反ゼロの範囲

上記2件(違反1件・疑い2件)を除く残りの識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝。
テストの構造体・関数名も含む)は受理条件8つ・命名の質・呼称と語の選択のいずれにも違反しなかった。
特に、エラー列挙`布素材エラー`の全34枝は長い複合名詞句だが、条件8(長さは結果であって制約ではない)に
照らして問題なく、実体(検証で拒む理由)と1対1で対応していることを定義に当たって確認した。
`u32`・`f32`・`usize`のような言語組み込みの型名がJapanese識別子中に現れる箇所(例:
`添字をusizeへ変換する`)は、既存の全リポジトリでの用法(数値型の正式名としてのASCII使用)と同一であり、
略語・頭字語の混入とは区別して不合格にしなかった。
