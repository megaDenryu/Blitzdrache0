# 命名検査報告: 区画082「crates/blitz_render/src/clustered_lighting 第1区画」

- 区画: crates/blitz_render/src/clustered_lighting 第1区画(区画番号082、40ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main、時点 db815090。編集していない)
- 検査軸: 命名(日本語命名の受理条件8つ・日本語命名の質・呼称と語の選択)
- 直読したファイル数: 40 / 40(全ファイル直読。grepだけで済ませたファイルは0件)
- 対象一覧: block_082.txt に列挙された40ファイルすべて(assignment_audit.rs / assignment_readback_check.rs /
  assignment_statistics.rs / assignment_tally.rs / attenuation_tests.rs / attenuation_window.rs /
  cell_boundary_margin.rs / cell_correspondence.rs / cell_face_quad.rs / cell_frustum.rs /
  cell_frustum_tests.rs / cell_index.rs / cell_index_tests.rs / cell_light_capacity.rs /
  cell_light_interval.rs / cell_mapping.rs / cell_mapping_tests.rs / cutoff_threshold.rs /
  depth_range.rs / depth_slice.rs / exact_sphere_intersection.rs / exact_sphere_intersection_tests.rs /
  frustum_sampling.rs / grid_division.rs / half_field_of_view_tangent.rs / independent_construction.rs /
  influence_radius.rs / input_error.rs / interval.rs / light_sphere.rs / narrowing.rs /
  point_light_radiance.rs / point_light_radiance_tests.rs / quad_distance.rs / recommended_radius.rs /
  recommended_radius_tests.rs / screen_tile.rs / single_precision_agreement_tests.rs /
  single_precision_sample.rs / single_precision_transcript.rs)
- 省略なし。全ファイルの型・関数・メソッド・フィールド・変数・定数・列挙の枝を1つずつ受理条件へ当てた。
  「パターンが一貫しているので省略した」箇所は無い。

## 前提として確認した用語定義(ユビキタス言語.md)

`クラスタ格子`・`クラスタ光添字列`・`クラスタ写像`・`クラスタ格子のセル添字`・`クラスタ格子のセルの錐台`・
`区間`・`局所光の影響範囲の球`・`減衰の窓`・`点光源の放射`・`打ち切りの閾値`・`推奨の影響半径`・
`影響半径`・`クラスタ格子の分割数`・`クラスタの奥行き範囲`・`視野の半角の正接`・`クラスタの選別`・
`クラスタ選別の割り当て統計`・`絞りの比`・`偽陽性の率`・`厳密な交差判定`・`セルの錐台を広げる余裕`の
定義文を確認した。本区画の識別子はこれらの登録語と綴り・構造ともに一致しており、実体との食い違いは
見つからなかった(特に `クラスタ選別の割り当て統計` の6フィールドは定義文の列挙順・語のすべてが
`assignment_statistics.rs`・`assignment_tally.rs` の実装と一致している)。

## 違反の一覧

該当なし(確定した違反は0件)。

## 違反の疑い(条件に対して。断定はしない)

1. `crates/blitz_render/src/clustered_lighting/cell_face_quad.rs:19` | `六面を並べる` | 条件3(5W1Hの必要格)
   に対する疑い | ASCIIモジュール `cell_face_quad` 直下の自由関数であり、名前単独で読む規律の対象になる。
   引数`錐台`は補完に使えない(条件2により引数名は文脈にならない)。「六面」とだけ書かれると「何の六面か」
   (この関数が答える形の錐台の六面であること)が名前だけでは決まらない。呼び出し側は
   `cell_face_quad::六面を並べる(self)` という形で呼ぶため、英語(ASCII)モジュール名からの補完もできない。
   改名の素案: `錐台の六面を並べる`、またはクラスタ格子のセルの錐台へのメソッド化(`錐台.六面を並べる()`)
   にしてレシーバへ対象を担わせる。

2. `crates/blitz_render/src/clustered_lighting/frustum_sampling.rs:55` | `向きへ離した位置` | 条件3に対する
   疑い | 「向きへ離した」だけでは起点(`基点`)と離す量(`距離`)が名前に無く、単独では「何から」「どれだけ」
   離した位置かが読めない。関数の直上のコメント(「基点から向きの側へ与えた距離だけ離れた位置」)を読めば
   復元できるが、コメントに頼らず名前だけで読める形にするなら改名が要る。改名の素案:
   `基点から向きへ距離だけ離した位置`。

3. `crates/blitz_render/src/clustered_lighting/frustum_sampling.rs:63` | `内分する` | 条件5(裸の動詞の禁止)
   に対する疑い | 数学用語としては確立した語(内分点を求める操作)だが、名前には「何を内分するか」
   (`下限`と`上限`の区間)が一切現れず、動詞1語だけで立っている。呼び出し元でも`super::narrowing`と
   同様に短い私有ヘルパーとして使われており実害は小さいが、条件5の趣旨(日本語の補完が働かない位置での
   単独動詞)に厳密には抵触する。改名の素案: `区間を内分する`。

4. `crates/blitz_render/src/clustered_lighting/single_precision_sample.rs:53` | `等分の割合` | 条件3に対する
   疑い | 「等分の割合」は「何を等分した割合か」を名前に持たない。実装は`検査する奥行き一覧`内で
   奥行きの範囲を2000等分する媒介変数を返しており、値オブジェクトでも公開APIでもない私有ヘルパーだが、
   単独では「何の等分か」が読めない。改名の素案: `奥行き範囲の等分の割合`。

5. `crates/blitz_render/src/clustered_lighting/assignment_tally.rs:9` | `割り当ての数え` (構造体名) | 条件1・8
   (受理テスト・日本語命名の質)に対する疑い | 「数え」は「数える」の連用形を名詞的に転用した語であり、
   文法的には成立するが「これは何?」への答えとして単独で座りが悪い(「割り当ての数えです」という言い切りが
   不自然な日本語になる)。中身は4つの走行中の集計値であり、`統計へ写す`で完成した統計へ変換する前段の
   作業領域である。改名の素案: `割り当ての集計` または `割り当ての数え上げ`。

## 違反ゼロの範囲

上記5件の疑いを除く、区画082の40ファイルすべての型名・関数名・メソッド名・フィールド名・変数名・
定数名・列挙の枝は、日本語命名の受理条件8つ、日本語命名の質、呼称と語の選択のいずれにも違反しない
ことを確認した。識別子への英字・ギリシャ文字・記号の混入は0件(数値を含む識別子`半径10メートル`
[attenuation_tests.rs・point_light_radiance_tests.rsのテスト用ヘルパー]はArabic数字を含むが、
「A2」のような不透明な段階コードではなく「10メートルの半径」という値をそのまま読める記述であるため、
違反とはみなさなかった)。単一漢字のフィールド名`近`・`遠`・`横`・`縦`は、長い語の圧縮ではなく
それ自体が完結した日本語の単語(近い/遠い/横/縦)であり、ユビキタス言語の`クラスタの奥行き範囲`の
定義文自身が「近と遠の2つの...奥行きの組」と同じ語で説明しているため、`順4`・`段3`型の圧縮の
禁止には抵触しない。`クラスタ選別の割り当て統計`・`減衰の窓`・`点光源の放射`・`打ち切りの閾値`・
`推奨の影響半径`・`クラスタ格子の分割数`・`クラスタの奥行き範囲`・`視野の半角の正接`・`区間`など、
登録済みの用語を使う識別子はすべて定義文と一致していた。
