# 命名検査報告: 区画099「crates/blitz_render/src/vulkan/descriptor」

- 区画番号: 099
- 対象範囲: `crates/blitz_render/src/vulkan/descriptor` 配下36ファイル(`descriptor_indexing`配下3ファイルを含む)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、時点db815090。このツリーは編集していない)
- 検査日: 2026-09-12
- 直読したファイル数: 36 / 36(全ファイルを直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| descriptor/alloc.rs:8 | `割り当てる`(関数) | 条件2・3・5 | ASCIIモジュール`alloc`直下の自由関数で、何を割り当てるかが名前に無い。`create::生成する`と同型の裸動詞 | `ディスクリプタセットを割り当てる` |
| descriptor/object_sets.rs:60 | `位置を求める`(関数) | 条件2・3 | ASCIIモジュール`object_sets`直下の自由関数。「位置」が何の位置か(セット配列内の添字)が名前に無く、引数名でしか復元できない | `セット一覧内の位置を求める` |
| descriptor/scene_set_layouts.rs:30(使用箇所: 71・76、初期化: scene_set_layouts/create.rs:40) | `空`(フィールド) | 条件6 | 同じ`descriptor`の木の中で`空`は既に「空(sky)」を指す語として確立している(`view_pass_set.rs:16`の`空パスの定数の束縛番号`、ユビキタス言語の「空パス」「空パス定数」「空背景」)。ここでの`空`は「未使用のレイアウト」の意味であり、読み手は空パス(sky)の何かと誤読する | `空きレイアウト`または`未使用レイアウト` |
| descriptor/scene_set_layouts/create.rs:18(呼び出し: scene_set_layouts.rs:40 `create::生成する(...)`) | `生成する`(モジュール直下の自由関数) | 条件5 | ASCIIモジュール`create`直下の裸の`生成する`。受理条件が明示する不合格例`create::生成する`とまったく同じ形 | `シーンセットレイアウト一式を組み立てる` |
| descriptor/shared_sets/create.rs:13(呼び出し: shared_sets.rs:43 `create::生成する(...)`) | `生成する`(モジュール直下の自由関数) | 条件5 | 上と同型。ASCIIモジュール`create`直下の裸の`生成する`で、何を生成するかが名前に無い | `共有ディスクリプタセットを生成する` |
| descriptor/object_sets/create.rs:50 | `割り当てて書き込む`(関数) | 条件3・5 | ASCIIモジュール`object_sets::create`直下の自由関数。何を割り当てて書き込むか(ジオメトリのセット)が名前に無い | `ジオメトリのセットを割り当てて書き込む` |
| descriptor/lighting_set/pool.rs:84 | `件数`(関数) | 条件1・4 | 戻り値は`vk::DescriptorPoolSize`(種別+件数の組)であり、名前の「件数」は値の半分しか表さない。同じファイル内の型`セット1つの件数`と紛れる | `種別ごとのプールサイズ` |
| descriptor/geometry_set.rs:54 | `頂点段のストレージバッファバインド`(関数) | 条件6 | `バインド`はユビキタス言語で既に「スキンのバインド時姿勢」(`_doc/計画/ユビキタス言語.md:486-488`)を指す確立語であり、ここでのVulkanディスクリプタ束縛とは別概念。同じファイル内で`個体レコードの束縛番号`など正本語`束縛`も使っており、1つのファイル内で語が割れている | `頂点段のストレージバッファ束縛` |
| descriptor/lighting_set.rs:41(局所変数`バインド一覧`) | `バインド一覧` | 条件6 | 同上。`束縛`が正本語であり、`バインド`は別概念(スキンのバインド時姿勢)と衝突する | `束縛一覧` |
| descriptor/lighting_set.rs:58 | `画素段のバインド`(関数) | 条件6 | 同上 | `画素段の束縛` |
| descriptor/material_set.rs:36(局所変数`バインド一覧`) | `バインド一覧` | 条件6 | 同上 | `束縛一覧` |
| descriptor/material_set.rs:54(局所変数`バインドフラグ一覧`) | `バインドフラグ一覧` | 条件6 | 同上。`vk::DescriptorBindingFlags`の意味でも「束縛のフラグ」であり`バインド`ではない | `束縛フラグ一覧` |
| descriptor/view_pass_set.rs:42(局所変数`バインド一覧`) | `バインド一覧` | 条件6 | 同上 | `束縛一覧` |
| descriptor/view_pass_set.rs:53 | `定数バインド`(関数) | 条件6 | 同上 | `定数の束縛` |
| descriptor/lighting_set/cluster_grid.rs:20 | `バインド一覧`(関数) | 条件2・6 | ASCIIモジュール`cluster_grid`直下の自由関数でもあり、名前が何の一覧かを名乗れていない上に`バインド`が語として衝突する | `クラスタ格子の束縛一覧` |
| descriptor/lighting_set/cluster_grid.rs:35 | `画素段の記憶バインド`(関数) | 条件6 | 同上 | `画素段のストレージ束縛` |
| descriptor/lighting_set/distant_environment.rs:55 | `バインド一覧`(関数) | 条件2・6 | 上と同型 | `遠方環境の束縛一覧` |
| descriptor/lighting_set/distant_environment.rs:63 | `画素段の画像バインド`(関数) | 条件6 | 同上 | `画素段の画像束縛` |
| descriptor/lighting_set/local_visibility.rs:37 | `バインド`(関数) | 条件1・2・6 | ASCIIモジュール直下で単語1つの裸名。何の束縛かも名乗れず、語も衝突する | `局所可視度の束縛` |
| descriptor/lighting_set/point_light_shadow_map.rs:36 | `バインド`(関数) | 条件1・2・6 | 同上 | `点光源の影の束縛` |
| descriptor/lighting_set/pool/count_tests.rs:24 | `定数近似の枝の件数はレイアウトの8つのバインディングと一致する`(テスト関数) | 条件6 | テスト名の文中で`束縛`でなく`バインディング`を使い、正本語と食い違う | `定数近似の枝の件数はレイアウトの8つの束縛と一致する` |
| descriptor/lighting_set/pool/count_tests.rs:30 | `遠方環境の枝の件数はレイアウトの11のバインディングと一致する`(テスト関数) | 条件6 | 同上 | `遠方環境の枝の件数はレイアウトの11の束縛と一致する` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| descriptor/binding_number.rs:20 | `gpu境界値`(メソッド) | 条件2(ASCII混入) | 小文字`gpu`をそのまま識別子へ混ぜている。プロジェクトの他所は`GPU`と大文字で書く(`GPU資源の確保係`等)ため表記が割れている。ただしGPUは一般に通用する語であり、意味の復元自体は妨げていないため断定はしない |
| descriptor/scene_set_layouts.rs:27・51(フィールド/メソッド`ジオメトリ`) | `ジオメトリ` | 条件6 | ユビキタス言語の正式名は「ジオメトリと可視のセット」(set1)であり、`シーンセットレイアウト一式`の中で`ジオメトリ`と省略している。この区画の外で`ジオメトリ`が別の意味(メッシュ本体等)で使われていないかは本区画のファイルだけでは確認できない |
| descriptor/object_sets.rs:23-24 と geometry_set.rs:16 等 | `可視id列`(小文字id)と`可視ID列`(大文字ID) | 条件8寄りの品質メモ | 同じ概念を指す語が、型名・定数名では`ID`大文字、フィールド名・引数名では`id`小文字と表記が割れている。意味の衝突ではなく表記揺れであり、受理条件の直接の不合格には数えない |

## 違反ゼロの範囲

- `descriptor/binding_number.rs`(`gpu境界値`を除く全識別子)、`declared_bindings.rs`とその子`allocated_set.rs`・`bound_resource.rs`・`set_layout.rs`・`write_target.rs`、`empty_set.rs`、`mod.rs`、`sink.rs`、`shared_sets.rs`(`create.rs`を除く)、`geometry_set.rs`(`頂点段のストレージバッファバインド`を除く)、`material_set.rs`・`material_set/allocated_set.rs`・`material_set/set_write.rs`(バインド系局所変数を除く)、`lighting_set/allocated_set.rs`・`buffer_group.rs`・`set_write.rs`・`pool.rs`(`件数`関数を除く)、`lighting_set/distant_environment/sampler.rs`、`scene_set_layouts/allocate.rs`、`descriptor_indexing/mod.rs`・`query.rs`・`descriptor_indexing_tests.rs`は、上表の該当箇所を除き受理条件8つを満たす。特に`declared_bindings`配下は`束縛`という正本語を一貫して使い、`バインド`衝突が1件も無い唯一の下位区画である
