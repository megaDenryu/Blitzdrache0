# 命名検査報告: 区画057

- 区画名と範囲: crates/blitz_engine/src 直下(39ファイル、サブディレクトリ配下は対象外。作業ツリー C:/devs/Blitzdrache0_roller)
- 時点: main の db815090
- 直読したファイル数: 39/39(全ファイルを直読した。grepだけで済ませたファイルは無い)
- 検査対象の識別子: 型・関連関数・メソッド・フィールド・変数・定数・enumの枝・モジュールのre-export。判定は`.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」8条、「日本語命名の質」、「呼称と語の選択」、「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(登録語との照合)で行った

## 違反の一覧

該当なし(0件)。

## 違反の疑い(断定しない)

1. `crates/blitz_engine/src/streaming_reset_generation_tests.rs:23` | `格納済みテクスチャ::rgba8の原寸の画素列を返す` | 条件6(1語1義)に対する疑い | `_doc/計画/ユビキタス言語.md`589行が定義する登録語は大文字の`RGBA8`であり、このメソッド名だけ小文字`rgba8`で綴られ日本語へ直結している(「rgba8の」)。同じ語が大文字/小文字の2表記を持つ状態であり、意味は破綻していないが表記の一貫性が崩れている。定義箇所はこのファイルではなく`texture_storage`(区画057の対象外)にあるため、断定はそちらの区画の検査に委ねる。改名の素案: `RGBA8の原寸の画素列を返す`
2. `crates/blitz_engine/src/streaming_ledger_cancel_tests.rs` 他複数ファイル(`streaming_ledger_tests.rs`・`streaming_reset_tests.rs`・`streaming_transfer_tests.rs`・`streaming_usage_tests.rs`・`streaming_priority_eviction_tests.rs`・`streaming_observed_read_size_tests.rs`) | `gpu資源解除待ち一覧`・`gpu転送のためにシーンデータを取り出す`・`gpu転送の完了を報告する`・`gpu資源の解除を報告する`・`gpu転送を開始する`・`gpu転送完了を反映する`・`gpu使用完了後に解除する`・`gpu解除件数`・`cpuデータ破棄一覧` 等 | 条件6(1語1義)に対する疑い | 同じクレート内で列挙`GPU転送完了結果`・定数`GPU転送完了結果::GPU資源解除待ち`は大文字の`GPU`を使うのに対し、これらのメソッド・変数名だけ小文字`gpu`/`cpu`を日本語へ直結している。GPU/CPU自体は「一般に通用している語」であり頭字語の意訳は不要だが、同一クレート内で大文字/小文字の2表記が混在しており紛れの芽がある。定義箇所(`streaming/coordinator.rs`等)は区画057の対象ファイルに含まれないため、この区画では使用箇所としてのみ記録し、断定と改名はその定義ファイルを持つ区画の検査に委ねる

## 違反ゼロの範囲

上記の疑い2件を除き、区画057の39ファイル全域(chunk.rs・frame_composition.rs・height_field.rs・instance_lod.rs・instance_lod_tests.rs・lib.rs・lighting.rs・local_visibility.rs・lod_threshold.rs・primitive_draw_item.rs・static_shape.rs・streaming.rs・streaming_budget_overflow_tests.rs・streaming_budget_tests.rs・streaming_chunk_world_fixture.rs・streaming_coordinator_fixture.rs・streaming_coordinator_tests.rs・streaming_directory_replace_tests.rs・streaming_directory_tests.rs・streaming_estimate_tests.rs・streaming_eviction_tests.rs・streaming_fixed_route_tests.rs・streaming_ledger_cancel_tests.rs・streaming_ledger_tests.rs・streaming_loader_tests.rs・streaming_observed_read_size_tests.rs・streaming_priority_eviction_tests.rs・streaming_reset_generation_tests.rs・streaming_reset_tests.rs・streaming_tests.rs・streaming_transfer_tests.rs・streaming_usage_tests.rs・surface_layer_textures.rs・terrain_lod.rs・terrain_lod_tests.rs・visibility.rs・visibility_tests.rs・world_query.rs・world_shape_query.rs)は違反ゼロと判定した。

判定の根拠として次を照合した。
- `個体LODエラー`・`個体LOD選択設定`・`地形LODエラー`・`地形LOD選択設定`・`地形LOD選択器`は`_doc/計画/ユビキタス言語.md`215行が定義する登録語`LOD`をそのまま使っており、頭字語の意訳義務(LUT等)には該当しない(登録済みで初出説明も備わる)
- `退避`・`縮退`・`予算判定`・`ストリーミング調停`・`ストリーミング進行`・`観測済み読込量`は`_doc/計画/ユビキタス言語.md`396〜401行の定義と実装(enumの枝・フィールド)が一致することを確認した
- `既定フレーム構成を作る`・`既定ライティングを作る`・`既定の影響半径を作る`等の関連関数は、値オブジェクト様式の`型名::生成する()`または目的語を含む動詞句であり、裸の動詞の禁止(条件5)に抵触しない
- `x, y, z`(座標成分の関数引数。`lighting.rs`の`大域位置を作る(x, y, z: f64)`、`streaming_fixed_route_tests.rs`の`位置(x: f64)`等)はASCII単独のパラメータ名であり、日本語識別子へ英字を混入させた圧縮形(「段A2」のような形)には当たらないため対象外とした
