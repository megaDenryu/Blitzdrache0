# 命名検査報告 区画027

- 区画名と範囲: `crates/blitz_asset_compiler/src/height_field`(区画番号027、指定は37ファイル。実体は`height_field`配下7ファイル・`height_grid`配下6ファイル・`loader/animation`配下5ファイル・`loader`直下19ファイルの計37ファイル)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090`(このツリーは編集していない)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`の「命名」節、`_doc/計画/ユビキタス言語.md`(高さ場・高さ格子・UV・RGBA8等の登録語を確認)
- 直読したファイル数: 37/37(grepだけで済ませたファイルは無い。全ファイルをReadツールで全文直読した)

## 違反の一覧

なし(この区画には確定できる違反は無かった)。

## 違反の疑い

1. `crates/blitz_asset_compiler/src/loader/material.rs:58-69` | `情報から取り出す`(関数名)、引数名`情報: Option<gltf::texture::Info<'_>>` | 条件1(受理テスト)・条件3(5W1Hの必要格)・条件7(直訳の検出) | 「情報から取り出す」は動詞「取り出す」の目的語(何を取り出すか)が名前に無い。返り値は`Option<格納済みテクスチャ>`であり、実質「テクスチャを取り出す」なのに主語・目的語とも欠落し「情報」から「情報」を取り出すように読める。引数名「情報」も`gltf::texture::Info`の直訳で、glTFの`Info`(テクスチャ参照+UV添字)を知らない読み手には「何の情報か」が単独で復元できない。兄弟関数`画像バイト列を取り出す`(texture_decode.rs)は目的語を明示しており対比が付く。改名の素案: 関数名を`テクスチャ情報から格納済みテクスチャを取り出す`、引数名を`テクスチャ情報`
2. 同ファイル:71-81 | `法線情報から取り出す` | 条件3(5W1Hの必要格) | 上と同型。修飾語「法線」で情報の種類は補われているが、動詞「取り出す」の目的語(格納済みテクスチャ)がまだ無い。改名の素案: `法線情報から格納済みテクスチャを取り出す`
3. `crates/blitz_asset_compiler/src/loader/animation/channel_group.rs:21` | `関節解決.グローバルノードから新添字`(フィールドの参照。定義本体は本区画の範囲外の`loader/skin/mod.rs:20`) | 条件1(受理テスト)・条件4(品詞の規律) | 型は`HashMap<usize, usize>`であり値(対応表)なのに、名前が「AからBへの対応」の「への対応」を欠いた膠着圧縮に見える。単独で読むと「グローバルノードから求めた新添字」という単一の値のように読め、これが対応表(索引)であることが名前だけでは分からない。定義が本区画の対象ファイル一覧に含まれないため確定はできないが、参照側のこのファイルで読める範囲内では受理テストに落ちる疑いがある。改名の素案: `グローバルノードから新添字への対応`または`グローバルノードの新添字索引`(是正は定義ファイル`loader/skin/mod.rs`を触る際に行う)

## 違反ゼロと確認した範囲

上記の疑い3件を除く全37ファイルの型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝は、受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも違反しなかった。具体的に確認した観点:

- 実体と合わない語(オーナー不合格実例1件目の形): `高さ場`(実行時形式)と`高さ格子`(コンパイラ入力)がユビキタス言語の定義どおりに使い分けられており、混同・誤読を招く語は無かった(`貼り合わせた格子の並び`・`チャンクごとの高さ格子から高さ場を組み立てる`等、名前が実体の工程・入出力と一致することを定義と照合して確認した)
- 名詞1語の圧縮(実例2件目の形): `チャンクの矩形`・`高さ場コンパイルエラー`・`材質スロット語彙`等はいずれも何が束ねられているかを名前が示しており、動作や対象を省いた1語圧縮は見当たらなかった
- 詰め込みすぎ・本質でない条件の混入(実例3件目の形): 列挙の枝(`格子点数が表現できない`・`チャンクの矩形に欠けがある`等)はどれも1つの破れだけを名指ししており、本質でない条件を継ぎ足した名前は無かった
- 識別子へのASCII英字・ギリシャ文字・数学記号の混入: `x軸方向の中心の原点からのずれ`・`z軸方向の中心の原点からのずれ`(mesh_bounds.rs)の単一英字x/zは、実例で禁じられた`A2`・`Σλ_n`のような段階番号や添字記号ではなく、X軸・Z軸という日本の学校教育でも通用する空間軸のラベルであり、非専門家が読んで意味が取れる。`uv座標を持つ`のuvと`rgba8`はユビキタス言語に登録済みの語(UV・RGBA8)であり、専門用語の表記の基準を満たす。他にASCII・ギリシャ文字・数学記号を含む識別子は無かった
- 1語1義: `参照ファイル一覧`(ホットリロード監視用パスの集積)・`材質スロット`・`原型`等、複数ファイルにまたがって使われる語はどれも同じ意味で一貫していた
- 裸の動詞の禁止: `生成する`・`読み込む`等はすべて型名または材料格が付いた形(`高さ場::生成する`・`高さ格子を読み込む`・`部品の原型ソースを読み込む`等)で使われ、ASCIIモジュール直下の裸の動詞は無かった
