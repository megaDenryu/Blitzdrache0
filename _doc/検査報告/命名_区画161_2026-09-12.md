# 命名検査報告 — 区画161 xtask/src/reverse_depth ほか

- 区画番号: 161
- 区画名: xtask/src/reverse_depth（実際の一覧には sample_world_region・shader_copy・shader_reload_draw・shadow_diagnostic_image・shadow_loss の各ディレクトリも含む。block_161.txtの40件を対象とした）
- 作業ツリー: C:/devs/Blitzdrache0_roller（このツリーは編集していない）
- 時点: main の db815090
- 直読したファイル数: 40 / 40（全件を直読。grepだけで済ませたファイルは0件）

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| xtask/src/sample_world_region/ground_mask.rs:56-67,74-75 | `x開始`・`x終端`・`y開始`・`y終端`（変数）、`x`・`y`（関数`画素の色の判定名`の引数） | ASCII混入禁止（オーナー実例の型「A2」「n」と同型） | ASCIIの英字`x`/`y`を日本語の語幹（開始・終端）へ直結し、または引数名として単体で使っている。プロジェクトの他ファイル（edge.rs等）は同じ横座標・縦座標を「横」「縦」という日本語で呼んでおり、同じ概念に英字名と日本語名が両方存在する状態（1語1義違反も伴う） | `横開始`・`横終端`・`縦開始`・`縦終端`、引数は`横`・`縦` |
| xtask/src/shadow_loss/region_count.rs:12-15,25,27,33-34 | `x開始`・`x終端`・`y開始`・`y終端`（構造体`受光面の領域`のフィールドと関連関数`生成する`の引数） | ASCII混入禁止 | 型のフィールド名という永続的な識別子にASCII英字を直結している。呼び出し側の`range_world.rs`は同じ矩形を「画面の幅」「画面の高さ」という日本語で述べており、フィールドだけが英字のまま取り残されている | `横開始`・`横終端`・`縦開始`・`縦終端` |
| xtask/src/sample_world_region/sphere_mask.rs:66,68,71,74,77 | `x`・`y`（タプル分解した変数） | ASCII混入禁止 | 分解代入で作った変数がASCII単体名。同じファイルの他関数は「添字」「幅」「高さ」等の日本語を使っており、この2行だけが英字で浮いている | `let (横, 縦) = (添字 % 幅, 添字 / 幅);` |
| xtask/src/shadow_loss/comparison_fixture.rs:44 | `候補に残す: bool`（関数`影を塗る`の引数） | 品詞の規律（真偽は「〜か」） | 名前が命令文的な動詞句であり、真偽値であることが名前から分からない。呼び出し側の`if 候補に残す { ... }`はまだ読めるが、宣言だけを見ると処理を表す動詞句に見え、bool型だと初見で判定できない | `候補に残すか: bool` |
| xtask/src/shadow_loss/compare.rs:79 | `基準の影: bool`・`候補の影: bool`（関数`積む`の引数） | 品詞の規律（真偽は「〜か」） | 「影」という名詞だけでは「影の量」なのか「影の中にいるかどうか」なのか区別できない。実体は後者の真偽値である | `基準の影かどうか`は冗長なので`基準は影の中か: bool`・`候補は影の中か: bool` |
| xtask/src/reverse_depth/compare.rs:56-61 | `対照幾何`・`候補幾何`（bool変数） | 品詞の規律（真偽は「〜か」） | 「幾何」という名詞に見えるが実体は「その画素が幾何（背景でなくジオメトリ）であるかどうか」を持つ真偽値。名詞のままでは値の種類（f32の幾何情報なのか判定結果なのか）が復元できない | `対照は幾何か`・`候補は幾何か` |
| xtask/src/reverse_depth/compare/color/edge.rs:50 | `幾何: [bool; 3]`（配列変数） | 品詞の規律（真偽は「〜か」） | 3画素分の「幾何かどうか」の配列だが、名前が単数名詞のままで複数の真偽値を持つ配列だと読み手が気づきにくい | `幾何か一覧: [bool; 3]` |
| xtask/src/shadow_diagnostic_image/decode.rs:18 | `判別する`（`pub(super) fn`） | 裸の動詞の禁止（条件5）／5W1Hの必要格（条件3） | モジュール名`decode`はASCIIで補完力ゼロ。関数は「何を」「何から」判別するのかを名前に持たない。単独で読むと「判別する処理です」としか言えず、入力が色で出力が診断画素であることが分からない | `色から診断画素を判別する` |
| xtask/src/shadow_loss/diff_image.rs:28 | `書き出す`（`pub(super) fn`） | 裸の動詞の禁止（条件5） | モジュール名`diff_image`はASCIIで補完力ゼロ。同じファイルの兄弟関数`前の実行が残した画像を消す`は目的語を持つのに、この関数だけ目的語（差分画像）を欠く | `差分画像を書き出す` |
| xtask/src/shadow_loss/report.rs:7 | `表示する`（`pub(super) fn`） | 裸の動詞の禁止（条件5） | モジュール名`report`はASCIIで補完力ゼロ。「何を表示するか」が名前に無い。ファイル冒頭コメントには「比較結果を人が読める表にする」とあり、関数名がその情報を落としている | `比較結果を表にして表示する` |
| xtask/src/shadow_loss/args/exclusivity_tests.rs:20 | `rangeの構図は最大影距離の候補を受けない`（テスト関数名） | ASCII混入禁止 | 英単語`range`がそのまま日本語識別子へ混ざっている。`構図::影視距離の検収`の`綴り()`が返す文字列`"range"`をそのまま関数名に転記したためだが、識別子としては英語直書きになっている | `影視距離の検収構図は最大影距離の候補を受けない` |
| xtask/src/shadow_loss/args/exclusivity_tests.rs:62 | `rangeの構図は影の視距離の候補を受ける`（テスト関数名） | ASCII混入禁止 | 同上 | `影視距離の検収構図は影の視距離の候補を受ける` |

## 違反の疑い（断定しない）

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| xtask/src/reverse_depth/compare/color.rs:67 | 無名クロージャの引数`x`・`y` | ASCII混入禁止 | `(左..=...).any(\|y\| (左..=...).any(\|x\| 輪郭[y * 幅 + x]))`。1行に閉じた無名クロージャの束縛名であり寿命が極小だが、ASCII単体名である点は他の違反と同型 |
| xtask/src/reverse_depth/compare/color/edge.rs:63,65 | `隣接位置`内の無名クロージャ引数`x`・`y` | ASCII混入禁止 | 同上の理由。`.map(\|x\| 縦 * 幅 + x)`・`.map(\|y\| y * 幅 + 横)` |
| xtask/src/reverse_depth/run.rs:45 | `撮る`（private fn） | 裸の動詞の禁止（条件5） | `fn 撮る(環境, 色名: &str, 深度名: &str)`。目的語を欠くが、privateかつ呼び出し元が同一ファイル内近接（`対照を採る`/`候補を採る`のすぐ下）にあり、文脈補完の余地がある |
| xtask/src/shadow_loss/final_color.rs:37 | `撮る`（private fn） | 裸の動詞の禁止（条件5） | 同上の理由。`fn 撮る(実行環境, 実行名の綴り, 構図, 候補の起動指定)`。「最終色を撮る」のような目的語を足すと明確になる |
| xtask/src/shadow_loss/diff_image.rs:29,39,43,47 | `rgba8`（ローカル変数） | 直訳・専門用語の表記（条件7、非専門家の理解可能性） | 完全なASCII単語であり日本語識別子への「混入」ではないため確定は避けるが、周囲のコードは徹底して日本語識別子であり、この変数だけが色形式の略号のまま浮いている。`RGBA8`という符号の意味は非専門家に自明でない |
| xtask/src/shadow_loss/judgment.rs:38 | `描く前に確かめる` | 5W1Hの必要格（条件3） | 「何を確かめるか」が省かれている。ファイル冒頭コメントの「絵を見なくても分かる破れの判定」を読まないと対象が復元できない |

## 違反ゼロの範囲

上記に挙げた識別子・ファイルを除く、40ファイルの残りの識別子（型名・関連関数`生成する`/`定数から生成する`・エラー列挙の枝・判定名の組み立て関数・構図や計測軸のenumとメソッド群など）は受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも抵触しなかった。特にエラー型（`逆Z検収エラー`・`シェーダーの一時コピーの破れ`・`契約別のシェーダー束の差し替えの検収エラー`・`影の欠落計器の引数の破れ`・`影の欠落計器のエラー`）は枝ごとに目的語と契機を具体的に持っており模範的だった。
