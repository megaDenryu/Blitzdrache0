# 命名検査報告 区画144: xtask/src/atmosphere_lut(および同時割当の隣接区画)

- 区画名: xtask/src/atmosphere_lut(区画番号144、割当ファイル一覧は39件。内訳は atmosphere_lut/13・auto_exposure/8・bake_building/2・cloth_empty/3・cloth_night/3・cloth_shadow_order/4・cloth_xpbd_reference/6)
- 対象作業ツリー: C:/devs/Blitzdrache0_roller(main、時点 db815090。このツリーは編集していない)
- 直読したファイル数: 39/39(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 使用した規約: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(XPBD・コンプライアンス・歩行の器・刻みの所要時間の分布(p50/p99)等の登録語を照合に使用)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| xtask/src/bake_building/source_export.rs:10, 12, 21, 25 | `ビン名`(定数)、`編集サーバーのビンで書き出す`(関数) | 呼称と語の選択(カタカナ語の基準「小学生が分かるか」)/受理条件1(受理テスト) | 「ビン」は「バイナリ(実行可能ファイル)」を2モーラへ切り詰めた業界略語であり、単独で読むと同じ発音の「瓶」(容器)と衝突する。関数名・定数名だけを見た読み手は「編集サーバーの瓶で書き出す」「瓶の名前」と誤読しうる。「順4→順4」のような圧縮呼称の禁止と同じ構造(完全な語を切り詰めて別の日常語と衝突する語にした)である | `ビン名`→`実行ファイル名`、`編集サーバーのビンで書き出す`→`編集サーバーの実行ファイルで書き出す` |
| xtask/src/cloth_empty/judgment.rs:21(定義は本区画外の`xtask/src/report_parse.rs`にある`計数報告`構造体のフィールド。本区画内では読み出し参照のみ) | `計数.validation件数` | 受理条件6(1語1義)・7(直訳/未訳語の検出) | 同じ`cloth_empty/judgment.rs`の66〜67行目は同じ対象を`検証層の指摘`という訳語で呼んでおり、`cloth_xpbd_reference/parse.rs`の`検証件数`も同じ対象を訳語で呼んでいる。1箇所だけ英単語`validation`を訳さず日本語の助数詞`件数`へ直結しており、英語を知らない読み手には「validation」の意味が復元できず、しかも同じ検収ドメイン内で「検証層」「検証件数」「validation件数」の3通りの呼び方が並立している | `validation件数`→`検証層の指摘件数`(同ファイル66行目の`検証層の指摘の判定名`と語を揃える) |

## 違反の疑い(断定しない)

- `xtask/src/atmosphere_lut/thresholds.rs:57` `スカイビュー地平線近傍の許容` — 条件8(助詞省略)に対する疑い。「スカイビュー」と「地平線近傍」の間に「の」が無く、コメント側の表現「スカイビューの許容(縦53・54のみ、地平線近傍)」と比べると圧縮に見える。ただし「地平線近傍」自体が既に定着した複合名詞であり、名詞+名詞の日本語の通常の複合(「駅前広場」等)とも読めるため、断定はしない
- `xtask/src/cloth_xpbd_reference/judgment.rs:42` 関数名`一刻みのgpu時間を判定する` — 同ファイル内で型名は`GPU`(大文字、`布の一刻みのGPU時間`)なのに関数名は`gpu`(小文字)であり、同一概念の大小文字表記が揺れている。CPU/GPUは専門用語の表記の節で補足不要の一般語として明示的に許容されているため受理条件そのものへの違反とは言えないが、綴りの不統一として指摘する

## 違反ゼロの範囲

上記2件の違反と2件の疑いを除く39ファイル全域(atmosphere_lut配下13ファイル全部・auto_exposure配下8ファイル全部・bake_building配下のlaunch.rs・cloth_empty/night/shadow_order/xpbd_referenceの各error.rs系とjudgment.rs系・run.rs・conditions.rs・parse.rs・gpu_time.rs)は、型・関数・メソッド・フィールド・変数・定数・列挙の枝のいずれについても受理条件8つ、日本語命名の質、呼称と語の選択に照らして違反を検出しなかった。「XPBD」「コンプライアンス」「p50」「歩行の器」等の一見英語・記号混じりに見える識別子は、いずれも`_doc/計画/ユビキタス言語.md`に定義済みの登録語であることを照合して合格と判定した。
