# 命名検査報告 区画110

- 区画: crates/blitz_render/src/vulkan/shadow_map ほか(sky_stage・skinning・swapchain・sync・temporal_reconstruction を含む区画110)
- 範囲: block_110.txt に列挙された40ファイル(shadow_map 3・skinning 8・sky_stage 3・swapchain 5・sync 5・temporal_reconstruction 16)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 40/40(全ファイルを直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、ルートCLAUDE.mdの「命名」節、`_doc/計画/ユビキタス言語.md`「描画の語彙」を読んだ上で検査した

## 違反の一覧

1. `crates/blitz_render/src/vulkan/shadow_map/create.rs:16` | `生成する`(モジュール`create`直下の自由関数。呼び出し側は`create::生成する(確保係, 一辺)`) | 条件5 | ASCIIモジュール名`create`は補完力ゼロであり、`生成する`単独では「これは何?」に答えられない(何を生成するか名前に無い)。受理条件が挙げる不合格実例`create::生成する`と同型 | `シャドウマップの一式を確保する`
2. `crates/blitz_render/src/vulkan/sky_stage/create.rs:12` | `生成する`(モジュール`create`直下の自由関数。呼び出し側は`create::生成する(確保係, 要求)`) | 条件5 | 同上。ASCIIモジュール直下の裸の`生成する` | `空段階資源を確保する`
3. `crates/blitz_render/src/vulkan/swapchain/create.rs:10` | `生成する`(モジュール`create`直下の自由関数。呼び出し側は`create::生成する(...)`) | 条件5 | 同上 | `スワップチェーンを確保する`
4. `crates/blitz_render/src/vulkan/temporal_reconstruction/images/create.rs:11` | `生成する`(モジュール`create`直下の自由関数。呼び出し側は`create::生成する(確保係, 形式, 寸法)`) | 条件5 | 同上 | `時間再構成の画像を確保する`
5. `crates/blitz_render/src/vulkan/skinning/descriptor.rs:52` | `生成する`(モジュール`descriptor`直下の自由関数。呼び出し側は`descriptor::生成する(device, &バッファ)`) | 条件5 | ASCIIモジュール直下の裸の`生成する`。何のディスクリプタを作るのか名前に無い | `スキニングディスクリプタを確保する`
6. `crates/blitz_render/src/vulkan/skinning/buffers.rs:22`, `:26`, `:30` および `crates/blitz_render/src/vulkan/skinning/descriptor/write.rs:13-15` | `レスト頂点buffer` / `属性buffer` / `行列buffer` | 日本語命名の質(助詞を省いた膠着圧縮の禁止、英語直訳調の混在) | 日本語の名詞へ英単語`buffer`を助詞無しで直接連結しており、「単語らしく見せるための膠着圧縮」に当たる。`レスト頂点のバッファ`のように自然な句にすべき箇所を英語のまま貼り付けている | `レスト頂点のバッファ`・`属性のバッファ`・`行列のバッファ`
7. `crates/blitz_render/src/vulkan/skinning/pipeline.rs:20-21`, `:30` | `ディスクリプタlayout`(引数名)・`コンピュートspirv`(引数名)・`ディスクリプタlayout一覧`(ローカル変数) | 日本語命名の質(助詞を省いた膠着圧縮の禁止) | 日本語の名詞にASCII単語`layout`・`spirv`を助詞無しで直接連結しており、1つの識別子の中で言語が跳躍する。読み手は「ディスクリプタの何か」としか読めず、`layout`だと英語を知らないと分からない | `ディスクリプタのレイアウト`・`コンピュートのSPIR-Vコード`・`ディスクリプタのレイアウト一覧`
8. `crates/blitz_render/src/vulkan/sky_stage/aerial_composite.rs:27`, `:33` および `crates/blitz_render/src/vulkan/sky_stage/create.rs:17` (`要求.ビューとパスlayout`) | `ビューとパスlayout`(引数名・フィールド参照)・`layout一覧`(ローカル変数) | 日本語命名の質(助詞を省いた膠着圧縮の禁止) | 「ビューとパスのセット」というユビキタス言語の登録語(84行目付近で言及されるset 0の通称)から「のセット」を落とし、末尾へ英単語`layout`を助詞無しで貼っている。`layout一覧`も同様に英語→日本語の接続に助詞が無い | `ビューとパスのセットのレイアウト`・`レイアウトの一覧`

## 違反の疑い

1. `crates/blitz_render/src/vulkan/temporal_reconstruction/assemble.rs:18` | `組み上げる`(モジュール`assemble`直下の自由関数) | 条件3・条件2の疑い | ASCIIモジュール直下で「何を組み上げるか」が名前に無い(戻り値は`時間再構成一式`だが名前には現れない)。ファイル冒頭のコメントを読めば分かるが、条件2は英語モジュール名からの補完もコメントからの補完も認めていない。ただし同ファイル内で唯一の公開関数であり、呼び出し文脈(`assemble::組み上げる`)自体は本区画の外(`temporal_reconstruction/mod.rs`、区画外)にあるため断定は避ける | `時間再構成一式を組み上げる`
2. `crates/blitz_render/src/vulkan/temporal_reconstruction/images.rs:87` | `組へ分ける`(自由関数。`Vec<時間再構成の画像>`を`時間再構成の画像組`へ変換する) | 条件7の疑い | 動詞「分ける」は「分割する」を連想させるが、実体は4要素のVecを分解して名前付きフィールドへ割り当てる操作であり「組み立てる」に近い。誤読の余地はあるが実害は小さいため断定しない | `画像組へ組み立てる`

## 違反ゼロの範囲

- `crates/blitz_render/src/vulkan/shadow_map/sampler.rs`・`crates/blitz_render/src/vulkan/shadow_map/create/image.rs`
- `crates/blitz_render/src/vulkan/skinning/create.rs`・`inputs.rs`・`mod.rs`・`buffers/create.rs`・`buffers/create/attribute_bytes.rs`(型に属する`生成する()`はいずれも型名が目的語を担うため合格)
- `crates/blitz_render/src/vulkan/sky_stage/draw_input.rs`
- `crates/blitz_render/src/vulkan/swapchain/image_index.rs`・`mod.rs`・`select.rs`・`image_index/image_index_tests.rs`
- `crates/blitz_render/src/vulkan/sync/`配下5ファイル全部(`frame_slot_index.rs`・`frame_sync.rs`・`mod.rs`・`present_sync.rs`・`frame_slot_index/frame_slot_index_tests.rs`)。`生成する`はすべて型の関連関数であり、フィールド名・メソッド名とも目的語・格が明示されている
- `crates/blitz_render/src/vulkan/temporal_reconstruction/bind.rs`・`descriptor.rs`・`fill.rs`・`history_state.rs`・`images.rs`(`組へ分ける`を除く)・`injection.rs`・`input.rs`・`pipeline.rs`・`resize.rs`・`setting.rs`・`injection/buffers.rs`・`injection/byte_layout.rs`・`injection/pass.rs`
- 識別子へのASCII英字単独混入(頭字語)・ギリシャ文字・数学記号の混入は40ファイル中に検出なし(唯一の英字混入は上記違反6・7・8の助詞省略型であり、頭字語や記号の直接混入ではない)
