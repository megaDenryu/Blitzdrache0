# ローラー検査(命名の軸) 区画014

- 区画: `crates/blitz_app/src/reports/cloth_xpbd_reference` を含む `reports/` 配下の下位ディレクトリ群(cloth_xpbd_reference・cube_seam・derived_environment・distant_environment・draw_issue・indirect_probe・local_visibility)
- 対象ファイル数: 37(block_014.txtの一覧どおり)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`、時点: main の `db815090`(このツリーは編集していない)
- 直読したファイル数: 37 / 37(全数直読。grepだけで済ませたファイルは0件)
- 必読(受理条件・命名の質・呼称と語の選択・専門用語の表記)は `.claude/global-reference/全プロジェクト共通CLAUDE.md` の該当節、プロジェクト側は `CLAUDE.md`「命名」節を読んでから検査した

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_app/src/reports/indirect_probe/specular_lookup.rs:60` | `添字を実数へ` | 条件1(受理テスト)・条件4(品詞の規律) | 「添字を実数へ」は格助詞「へ」で終わり、着地する動詞が無いため文として完結しない。単独で読むと「添字を実数へ……(どうする?)」で止まり、「これは何?」への答えとして成立しない。同じファイルの他関数(`段を混ぜる`・`手前の段を探す`・`面の添字`)は動詞で終わるか名詞で完結しており、この関数だけが途中で切れている | `添字を実数へ直す`(または`添字をf64へ変換する`) |
| `crates/blitz_app/src/reports/distant_environment/scale.rs:24` | `円盤だけ変えた尺度` | 条件4(品詞の規律) | この識別子は関数名であり、空描画方針から新しい尺度を組み立てる処理(処理=動詞句であるべき)なのに、名詞句「変えた尺度」で止まっている。同じファイルの姉妹関数`尺度を作る`(15行目)は正しく動詞「作る」で終わっており、対比するとこの関数だけ動詞が欠落している品詞の偽装だと分かる | `円盤だけ変えた尺度を作る` |
| `crates/blitz_app/src/reports/distant_environment/bake.rs:51` | `空中遠近の条件` | 条件4(品詞の規律) | 関数が観測条件から`空中遠近観測条件`を新規に組み立てて返す処理なのに、名前は「空中遠近の条件」という結果の名詞句で止まっている。同ファイルの`焼く`・`不一致数`は動詞または計測値の名詞で正しく書かれており、この関数だけ「組む」に類する動詞が欠けている | `空中遠近の条件を組む` |
| `crates/blitz_app/src/reports/distant_environment/key_rows.rs:59` | `夜空を変えた鍵` | 条件4(品詞の規律) | 空描画方針と観測条件から新しい`遠方環境キー`を導出して返す処理なのに、名詞句「変えた鍵」で止まっている。同ファイルの`鍵を導く`(50行目)は動詞で終わっており、対比するとこの関数だけ動詞が欠けている | `夜空を変えた鍵を導く` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑われる条件 | 理由 |
|---|---|---|---|
| `crates/blitz_app/src/reports/distant_environment/key_rows.rs:36` | `提示したフレーム` | 条件1(受理テスト)・条件4(品詞の規律) | この関数は`判定.判定する`と`判定.確定する`を呼んで`遠方環境の生成指示`を返す処理であり、戻り値の型は「フレーム」ではない。名前は「提示に到達した1フレームぶんの判定」という物語的な状況説明であって、戻り値そのものの名でも処理を表す動詞句でもないため、実体との対応が名前から一意に読み取れない。ただし直上の`///`コメントが意図を明示しており、モジュール内の私有関数(呼び出し範囲が同一ファイルの3か所に限られる)であるため、断定はせず疑いとして記す |

## 違反ゼロと判定した範囲

上記4件(+疑い1件)を除く、37ファイルの残り全識別子(型名・enum・関連関数・メソッド・フィールド・変数・定数)は受理条件8つ・日本語命名の質・呼称と語の選択・専門用語の表記のいずれにも抵触しなかった。特に次の点を個別に確認した。

- ASCII英字・ギリシャ文字・数学記号の識別子への混入: 0件(`gpu`・`p95`等は既存の一般語彙`GPU`の表記ゆれの範囲であり、専門用語の表記が許す一般通用語である。識別子の意味把握を妨げる記号・略号の混入は無い)
- 「オーナーが不合格にした実例」の3形(実体と合わない語・名詞1語の圧縮・詰め込みすぎ)への該当: 上記4件の品詞の偽装(処理を名詞句で止める)以外に該当なし
- 1語1義の衝突(同じ語が2つの意味): 検査範囲内では未検出(`布の参照比較`・`布の参照計算`など、似た語が別ファイルにまたがるが、いずれもユビキタス言語で区別済みの別概念として使われており、範囲内で意味の衝突は無い)
- 直訳調(record→記録、lookup→引く等): 未検出。`specular_lookup.rs`の`鏡面の色を参照する`系は「参照する」を採っており、禁止された「引く」は使われていない
- 裸の動詞の禁止(`生成する`単独名): 未検出。型に属する`生成する()`はすべて型名またはレシーバが目的語を担っている
