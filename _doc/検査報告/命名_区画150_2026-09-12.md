# 命名検査報告 区画150: xtask/src/conform/source_lexing/token_read ほか

- 区画番号: 150
- 対象: `xtask/src/conform/source_lexing/token_read`(定義上の区画名。実際の一覧はconform配下4領域39ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(このツリーは編集していない)
- 時点: main の `db815090`
- 直読したファイル数: 39 / 39(grepだけで済ませたファイルは0)
- 検査した4領域: `source_lexing/token_read`(4)・`type_metrics_ledger`(18)・`wording_contract`(9)・`workspace_dependency_features`(4)

## 検査の前提

読んだ必読リスト: グローバルCLAUDE.mdの「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`。

対象領域はxtask(ASCII crate名)配下だが、モジュール・ファイル名がASCIIでも中身の型・関数・フィールド・変数はすべて日本語識別子で書かれている。ASCIIモジュール名は受理条件2により補完力ゼロとして扱い、日本語識別子は単独で読める前提で検査した。

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `xtask/src/conform/type_metrics_ledger/ledger/table/blitz_sim.rs:14` | `接触点集合の法線と接線の連立`(台帳の行が指す実体は`crates/blitz_sim/src/contact/normal_tangential_system/system.rs`の`pub struct 接触点集合の法線と接線の連立`) | 条件1(受理テスト)・条件3(5W1Hの必要格) | 「連立」だけでは何が連立されているのか(連立方程式か連立の状態を表す値の組か)が名前から復元できない。これはCLAUDE.mdの「オーナーが不合格にした実例」に載っている当該そのものの語であり、2026-09-12時点でも是正されずに実装・台帳の両方に残っている | `接触点集合の法線と接線の連立方程式` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `xtask/src/conform/type_metrics_ledger/ledger/table/blitz_collision.rs:14` と同ファイル:20行目 | `カプセルと三角形の接触の求解`(実体は`crates/blitz_collision/src/height_field/capsule_sweep/contact_solver.rs`の型と`crates/blitz_collision/src/triangle/sweep_solver.rs`の型の2つ、別モジュールに存在する別の`struct`) | 条件6(1語1義) | 台帳が「定義ファイルと型名の組」を鍵にしていることからも分かるとおり、同じ日本語名を持つ意味の異なる2つの型が実在する。一方は地形の高さ場が持つ三角形(地表)との接触、もう一方は一般の三角形形状との接触であり、ドキュメントコメントの記述も「地表の三角形」対「三角形」で区別しているが、型名自体にはその区別が無い。読み手が型名だけを見て2つを取り違える余地がある。台帳の役割(この2箇所)は照合の鍵であって命名の正本ではないため、実装側の型名を見て初めて確定する判断であり、ここでは疑いにとどめる |
| `xtask/src/conform/type_metrics_ledger/ledger/table/blitz_asset_compiler.rs:21` | `開いた文書の契約検査`(実体は`crates/blitz_asset_compiler/src/loader/contract/inspection.rs`の型) | 条件1(受理テスト) | 「開いた文書」がglTF等のソースアセットのファイルを指すのか、一般的な意味の文書を指すのか、この台帳ファイルの中だけでは確定できない。区画150の外(loader/contract配下)の定義を読まないと実体との一致を確認できないため、疑いにとどめる |

## 違反ゼロと確認した範囲

- `source_lexing/token_read`配下4ファイル(comment.rs・escape.rs・escape_tests.rs・string.rs): 字句読み取りの識別子(`行コメントを読む`・`逃がしを復号する`・`生文字列を読む`等)はすべて動詞句+目的語の形で受理条件を満たす。違反なし
- `type_metrics_ledger`配下18ファイルのうち、上記1件の違反(データとして写された型名)を除く識別子(`型の分量`・`宣言の食い違い`・`台帳`・`区画の一覧`・`型ごとの上限`等の構造体・メソッド・フィールド・テスト関数名)はすべて受理条件を満たす。特に構造体の分量を表す値(名詞句)と、真偽を返すメソッド(`〜か`)の品詞規律が一貫している
- `wording_contract`配下9ファイル(table.rs・tests.rs・unregistered_key.rsおよび9つの領域別台帳): `綴りの契約`・`鍵の終わりの字`・`登録の無い鍵を探す`等はすべて受理条件を満たす。台帳が持つ文字列リテラル(`"XPBD並列方式計測"`等)は出力される日本語の見出し文そのものであり識別子ではないため主対象から外したが、目視した範囲で条件7(直訳)・日本語命名の質(頭字語混入)の明白な違反は無い(`XPBD`は`_doc/計画/ユビキタス言語.md`に既登録の定着語)
- `workspace_dependency_features`配下4ファイル: `依存節の行`・`ワークスペース依存の宣言`・`機能一覧を読む`・`機能台帳`等はすべて受理条件を満たす

## 省略した範囲とその理由

省略はしていない。一覧にある39ファイルを全数直読した。ただし2件は、識別子の実体(型定義そのもの)が区画150の外(`crates/blitz_sim`・`crates/blitz_collision`・`crates/blitz_asset_compiler`)にあり、区画150内のファイルは台帳としてその型名を文字列で写しているだけである。実体側の定義ファイルはこの区画の担当外のため、実体の全文検査は行っていない(該当箇所だけをgrepで照合した)。
