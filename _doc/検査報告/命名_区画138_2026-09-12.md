# 命名検査報告 区画138

- 区画名: crates/editor_server/tests/integration/common
- 範囲: 9ファイル(building_grid_fixture.rs / building_outline_catalog_fixture.rs / chunk_height_cutout_fixture.rs / mod.rs / music_fixture.rs / scatter_export_fixture.rs / scatter_fixture.rs / source_asset_export_fixture.rs / world_layout.rs)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 9/9(全ファイルを全文直読。grepだけで済ませたファイルは無い)
- 参照した必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| music_fixture.rs:9 | `名乗り`(関数、`fn 名乗り(綴り: &str) -> 楽曲ID`) | 条件4(品詞の規律) | 実体は「文字列を検証して楽曲IDへ包む処理」であり、`楽曲ID::生成する(綴り).unwrap()`を呼ぶだけの処理関数である。名前が名詞形のままのため、読み手は`名乗り`という値(識別情報そのもの)を素朴に受け取る名詞だと誤読し、これが検証付きの構築処理であることが名前から読めない。同じファイル内の`試験の建物定義の識別子を作る()`など他の構築ヘルパーは動詞句で書かれており、この関数だけ品詞が浮いている | `名乗りを作る` |
| music_fixture.rs:13 | `パターンの名乗り`(関数、`fn パターンの名乗り(綴り: &str) -> パターンID`) | 条件4(品詞の規律) | 上と同じ理由。`パターンID::生成する(綴り).unwrap()`を呼ぶ処理が名詞句のままで、値なのか処理なのか名前だけでは決まらない | `パターンの名乗りを作る` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| building_grid_fixture.rs:41 | `初期の格子のjson`(関数) | 日本語命名の質(英語頭字語・略語の日本語への混入禁止) | `専門用語の表記`節が明示的に許すのは UI・VR・CPU・API・シェーダー等であり、JSON はその列に無い。JSON がその分野の非専門家に意味が取れる語かどうかは判定が割れうるため断定を避けるが、無変換のまま英字3文字がそのまま日本語識別子の末尾に膠着している点は`日本語命名の質`の懸念に触れる。改名の素案(参考): `初期の格子の生データ` |
| building_grid_fixture.rs:63 | `升目を空にした格子のjson`(関数) | 日本語命名の質 | 同上の理由。改名の素案(参考): `升目を空にした格子の生データ` |
| world_layout.rs:13 | `区画割りJson`(関数) | 日本語命名の質 | 同上の理由(大文字化のみで英字表記自体は変わらない)。改名の素案(参考): `区画割りの生データ` |
| music_fixture.rs（`楽曲`/`パターン`構造体の`名乗り`フィールドの用法） | `名乗り`(フィールド) | 条件6(1語1義) | `_doc/計画/ユビキタス言語.md`の登録語「建物の名乗り」は「識別子・表示名・入口のローカル方向の組」という束ねた値として定義されている。ところが本ファイルが組み立てる`楽曲`・`パターン`の`名乗り`フィールドは、`表示名`が別フィールドとして独立して存在するため、識別子(ID)だけを指す語として使われている。同じ語「名乗り」が「束ねた値」と「識別子単体」という2つの異なる粒度で使われており、読み手がどちらの意味かを名前だけから決められない。ただし`名乗り`フィールド自体の定義は`editor_server`クレート側(本区画の外)にあり、本ファイルは既存フィールド名を使っているだけのため、断定でなく疑いとして報告する |

## 違反ゼロの範囲

上記を除く識別子(型名・構造体フィールド・enum枝・定数・大多数の関数名。例: `一時プロジェクト`・`ルーターを作る`・`保管庫を作る`・`格子の台帳を読む`・`外部アセットの置き場があるか`・`建物外形カタログを作る`・`高さ格子を取得する`・`高さ一覧へ解く`・`マザーの高さ`・`散布の個体一覧を作る`・`散布を載せたチャンク構造を保存する`・`道路の中心線までの距離`・`建物までの距離`・`フォックスのソースを配置する`・`地表層のタイルを配置する`・`大域世界を保存する`・`マザーを一意な値で保存する`・`零のマザーを保存する`・`区画割りを保存する`ほか9ファイル全体)は、受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも抵触しない。単一漢字の変数・定数(`東`・`南`・`元`・`先`・`親`)は中国語的圧縮ではなく、いずれも日本語として完結した独立語であり違反に当たらない。ASCII・ギリシャ文字・数学記号の識別子への混入は上記のJSON表記(略語)の疑い以外に検出しなかった。
