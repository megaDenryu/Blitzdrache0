# 命名検査報告(区画084)

- 区画: crates/blitz_render/src/cube_image ならびに crates/blitz_render/src/distant_environment(サブディレクトリ consume/・derived/ を含む)
- 対象ファイル数: 38(block_084.txt に列挙された全件)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない。書き込みは本報告ファイルのみ)
- 直読したファイル数: 38 / 38(grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(遠方環境・派生表現・拡散照度の立方体画像・鏡面畳込みの立方体画像・反射率積分表・粗さ段・ハンマーズリー列・立方体画像の一辺・立方体の面 等の登録語を照合に使用)

## 違反の一覧

該当なし。38ファイルの型・関数・メソッド・フィールド・変数・定数・列挙の枝を受理条件1〜8で当てたが、受理条件に落ちる識別子は見つからなかった。ASCII英字・ギリシャ文字・数学記号の混入も無い(モジュールファイル名はASCIIだが、これは規約が許すクレート/ファイル名の例外であり識別子そのものはすべて日本語)。

## 違反の疑い

- `crates/blitz_render/src/distant_environment/derived/cube_texel_lookup.rs:34-36` | `面内の要素数`(私有関数) | 条件1・条件7に対する疑い | この関数は「面内の要素数」という名の値を返すのではなく、任意の`u32`を`usize`へ境界検査つきで変換する汎用ヘルパーである。呼び出し元は`一辺`(辺の長さ)・`縦`・`横`(添字)へこの関数を適用しており(29〜31行目)、いずれも「面内の要素数」という実体を表さない。名前が実体(汎用の型変換)と合わない点で、オーナーが例示した「実体と合わない語」の形に近いが、この関数はモジュール内だけで完結する私有ヘルパーで、呼び出し側から見える公開APIではないため、断定はせず疑いとして報告する。改名の素案: `usizeへ変換する`または`u32をusizeへ狭める`

上記1件以外に、疑いに分類した識別子は無い。

## 違反ゼロの範囲

- `crates/blitz_render/src/cube_image/`(face.rs・face_mapping.rs・face_mapping_tests.rs)の3ファイル: 型`立方体の面`・関数`層番号`・`層番号から求める`・`面と面内位置から向きを求める`・`向きから面と面内位置を求める`・`テクセル中心の面内位置`等はいずれも受理条件を満たす。ユビキタス言語の「立方体の面」の定義と実装が一致することを確認した
- `crates/blitz_render/src/distant_environment/`直下の12ファイル(analytic_input.rs・analytic_input_error.rs・bake_source_input.rs・consume.rs・derived.rs・derived_probe.rs・frame_input.rs・input.rs・pass_counts.rs・probe.rs・shader_set.rs・sun_direction.rs): 型`遠方環境の解析入力`・`遠方環境の焼かせる解析入力`・`遠方環境の入力`・`遠方環境の生成指示`・`間接照明生成パス数`・`ワールドの太陽方向`等、フィールド`拡散照度`・`鏡面畳込み`・`反射率積分表`・`尺度`・`太陽方向`・`スカイビュー条件`等は、いずれもユビキタス言語の「遠方環境」「派生表現」「天空放射輝度の尺度」の定義と一致し、名詞句(値)と動詞句(処理)の品詞も実体と合っている
- `crates/blitz_render/src/distant_environment/consume/`の5ファイル(consume.rs・consume_tests.rs・diffuse.rs・fresnel.rs・specular.rs): 関数`拡散の取り分を求める`・`間接拡散の色を求める`・`垂直入射の反射率を求める`・`粗さ対応の反射率を求める`・`間接鏡面の色を求める`・`粗さから縮小段の位置を求める`はすべて「何を+どうする」の格を満たし、直訳調・膠着圧縮も無い
- `crates/blitz_render/src/distant_environment/derived/`の18ファイル: 型`立方体画像の一辺`・`反射率積分表の解像度`・`拡散照度の解像度`・`鏡面畳込みの解像度`・`粗さ段`・`粗さ`・`遠方環境の内容`・`派生表現エラー`、関数`反射率積分表を焼く`・`拡散照度を焼く`・`鏡面畳込みの段を焼く`・`ハンマーズリー標本を求める`・`接基底を組む`・`接基底の向きを戻す`・`天頂余弦と方位の割合から接空間の向きを組む`・`ggxの半ベクトルを標本する`・`半ベクトルで反射させる`・`定数の遠方環境`・`方向性の強い遠方環境`は、いずれもユビキタス言語の「拡散照度の立方体画像」「鏡面畳込みの立方体画像」「反射率積分表」「粗さ段」「ハンマーズリー列」の定義と実装(何を焼くか・何を返すか)が一致する。唯一の疑いは上記`面内の要素数`のみである
