# 命名検査報告: 区画156(xtask/src/indirect_cost 系4検収モジュール)

## 範囲・時点

- 区画名: `xtask/src/indirect_cost`(区画番号156)。対象は `indirect_cost`・`indirect_probe`・`instance_cull`・`instance_draw`・`instance_lod` の5モジュール、計40ファイル。
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の `db815090` 時点。編集していない)。
- 一覧の出典: `C:/Users/POKR30~1/AppData/Local/Temp/claude/C--devs-Blitzdrache0/612e37aa-a38d-4d29-a7a3-b0e9508d9f15/scratchpad/roller/block_156.txt`。

## 読み方の内訳

- 直読したファイル数: 40/40(全ファイル)。
- grepだけで済ませたファイル数: 0。省略はしていない。
- 直読の根拠は各ファイルの全行を読んだこと(`wc -l`で最大100行と確認済みの範囲)。加えて `validation` という語の出現箇所を横断grepし、識別子への混入を裏取りした。

## 必読(定義側)の要点

- `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」(8条件、この順で判定)。
- 同「日本語命名の質」(頭字語・直訳調・膠着圧縮の禁止)。
- 同「呼称と語の選択」(漢字1文字への切り詰め禁止・カタカナ語は小学生基準)。
- `_doc/計画/ユビキタス言語.md`: LOD・GPU・validation layer・可視個体の選別・段別描画範囲・可視ID列 等の登録語を確認した。

## 違反の一覧

### 1件

`xtask/src/instance_cull/judgment/common.rs:39`(定義)・同ファイル22行目(使用) | `validationの指摘の判定名` | 条件2・条件6・条件7(日本語命名の質の頭字語・直訳調禁止も併せて違反) | 生の英単語`validation`を日本語識別子へそのまま混ぜており、英語を知らない読み手はこの名前だけから「検証層の指摘」という意味を復元できない(条件7の直訳検出質問に失敗)。加えて、同じ概念を指す同型の関数が兄弟ファイルに存在し、そちらは正しく翻訳されている(`xtask/src/instance_lod/judgment/resource.rs:23`の`検証層の指摘の判定名`、`xtask/src/instance_draw/count_check.rs`と`single_instance.rs`が使う定数`検証層の指摘`)。同じ語が1つのモジュール群の中で「validation」と「検証層」の2つの綴りを持ち、1語1義(条件6)にも違反している。 | `検証層の指摘の判定名`(兄弟ファイルの命名へ揃える)

## 違反の疑い(断定しない)

- `xtask/src/indirect_probe/parse.rs:18`・`face_judgment.rs`各所の構造体フィールド`面: u32`(条件1に対する疑い): 「面」は正しい日本語の1語(圧縮ではない)だが、フィールド単独では「立方体のどの面を参照したか」まで復元できるか読み手によって差が出る。同じ構造体内の`面の端からの余裕`が隣接して読める限りは補完が効くため、断定の違反とはしない。改名するなら`参照した面`程度で足りる。
- 各所の`計数.validation件数`(`instance_cull/judgment/common.rs:22`、`instance_draw/single_instance.rs:73`、`instance_draw/count_check.rs:31`、`instance_lod/judgment/resource.rs:18`)への参照(条件2・条件7に対する疑い): このフィールド自体の定義は区画156の外(`report_parse`系、未確認)にあるため断定の違反として数えないが、区画156の4ファイルがこの綴りをそのまま識別子として使い続けている。定義側が`validation件数`のままなら同根の違反であり、定義側の是正とセットでの改名を推奨する。

## 違反ゼロの範囲

- 上記2件を除く、40ファイルの残り全識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)は受理条件8つを通過した。特に、各モジュールが繰り返し使う値オブジェクト様式の関連関数`生成する()`(`検収の実行名::生成する`等)は型名が目的語を担うため合格、`判定の名前::組み立てた綴りから生成する(format!(...))`のような動的な判定名の組み立ては、条件3(何を+どうする)を満たす具体的な文面を都度生成しており違反なし。エラーenumの枝(`検収の器が破れた`・`判定が破れた`等)は品詞の規律(条件4)に沿い、`display`サブモジュールへのトレイト実装分離は切り出しの根拠義務(ライフサイクル局面でなくトレイト実装の分離)を満たす。
