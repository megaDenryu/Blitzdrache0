# 命名検査報告: 区画050「crates/blitz_engine/src/asset 第1区画」

- 区画番号: 050
- 対象パス: `crates/blitz_engine/src/asset` 配下の直下ファイル(サブディレクトリ配下は別区画のため対象外)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, コミット db815090。このツリーは編集していない)
- ファイル一覧の出所: `scratchpad/roller/block_050.txt`(40ファイル)
- 検査時点: 2026-09-12
- 直読したファイル数: 40/40(全ファイルを直接読んだ。grepだけで済ませたファイルは0件)
- 必読リストの参照: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」、`_doc/計画/ユビキタス言語.md`(該当語をgrepで確認: PBR・LOD・TRS・マテリアル・材質系・カタログ系・インスタンス群系・スケルタルアニメーション系)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_engine/src/asset/height_field_sample_count.rs:18` | `F32のバイト数`(ローカル定数) | 「日本語命名の質」(英語の頭字語・省略語を日本語の中に混ぜない)、識別子へのASCII混入の禁止 | `F32`はRustのプリミティブ型名の頭字語をそのまま漢字仮名交じりの識別子へ混入したものであり、非専門家はもちろん専門家でも「なぜF32という英字が単語として現れるのか」を日本語の文法から読み取れない。値自体は「f32が占めるバイト数」であり日本語だけで表現できる | `浮動小数点数1個のバイト数` または `高さの標本1個のバイト数`(高さ場の標本がf32で格納される事実は型のコメントに既に書かれているため、定数名は用途で言い切れる) |

## 違反の疑い(条件に対する疑い。断定はしない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `crates/blitz_engine/src/asset/catalog.rs:40` | `詳細を登録する`(`カタログ`のメソッド) | 条1(受理テスト)・条3(5W1Hの必要格) | 実際に登録する内容は「実行時パス・ソース依存一覧・メタデータ」の3点だが、名前は「詳細」という抽象名詞1語で束ねている。呼び出し側のシグネチャを見なければ「詳細」が何を指すか復元できない。姉妹メソッドの`登録する(id, パス)`との対比で「詳細版」と読めなくはないが、`実行例の実例3`(詰め込みすぎ/本質でない語で束ねる)に近い形であり、名前の語が実体(3種の引数)を表しているとは言い切れない。改名素案(参考): `パスと依存とメタデータを登録する` |
| `crates/blitz_engine/src/asset/runtime_catalog_tests.rs:75`付近の`登録する`(試験用ヘルパー) / `runtime_catalog_tests.rs`の`試験用id`・`runtime_chunk_directory_tests.rs`の`試験用目録`・`試験用id` | 条4(品詞の規律) | `試験用id`・`試験用目録`はいずれも内部で`生成する`を呼び失敗時にpanicする「処理」だが、名前は動詞を持たない裸の名詞句である。同じファイル内の`試験用カタログを版1で格納する`(動詞句)と品詞が食い違っており、一貫していない。ただしテスト内部のprivateヘルパーであり読み手の混乱は小さいため断定しない。改名素案(参考): `試験用idを作る`・`試験用目録を作る` |

## 違反ゼロの範囲

上記2件の「疑い」と1件の確定違反を除く、区画050の残り37ファイルの識別子(型名・関連関数・メソッド・フィールド・列挙の枝・定数)はすべて受理条件8つを通過した。特に次の点を個別に確認した。

- `PBR`・`LOD`・`TRS`は`_doc/計画/ユビキタス言語.md`(32行目・215行目・489行目)に初出補足つきで登録済みの用語であり、日本語識別子への混入(`金属粗さPBRデータ`・`地形LODメッシュ群`・`静的TRS`)は「日本語命名の質」の頭字語禁止の例外(定着した専門用語としてユビキタス言語に登録済み)に当たるため違反としなかった
- `番号を返す`・`文字列を返す`(`材質スロットID`・`描画対象ID`・`アセットID`等)は、境界(永続化・ログ)での生値抽出だけに使う動詞句として型コメントに明記された一貫した規約であり、同種の値取得に使う裸の名詞アクセサ(`実行時パス()`・`メタデータ()`・`値()`)と役割が異なるため1語1義の衝突ではない
- `骨格`という識別子は本区画のどのファイルにも現れず、`_doc/計画/ユビキタス言語.md`が指摘するスケルタルアニメーションとの1語1義の衝突は発生していない
- `カタログ::世界の由来を記録する`は、フィールドが処理(コールバック)でなく値であり、動詞句のメソッドで値を設定する形として品詞規律に沿っている(2026-08-09に不合格となった「記録」というコールバックフィールドの実例とは別物)

## 検査していない範囲

区画050は`crates/blitz_engine/src/asset`直下の40ファイルのみを対象とした。`texture_storage/`・`runtime_format/`・`runtime_scene_multi_material_tests/`等のサブディレクトリ配下のファイル(`RGBA8`等のASCII混入が別途複数見つかっている)は別区画の担当であり、本報告の対象に含めていない。
