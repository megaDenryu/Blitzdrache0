# 命名検査報告: 区画096

## 区画名と範囲
`crates/blitz_render/src/vulkan/atmosphere_lut/readback_buffer` から `crates/blitz_render/src/vulkan/unsent_command_buffers.rs` までの37ファイル(一覧は `scratchpad/roller/block_096.txt` に固定)。作業ツリーは `C:/devs/Blitzdrache0_roller`、時点は main の `db815090`。このツリーは編集していない。

## 時点
検査実施日 2026-09-12。読んだコードは `db815090` 時点のもの。

## 直読・省略の件数
37ファイル中37ファイルを直読した(grepだけで済ませたファイルは0件)。省略は無い。

## 違反の一覧

該当なし。全域を直読した結果、8条件のいずれにも明確に落ちる識別子は見つからなかった。

## 違反の疑い

- `crates/blitz_render/src/vulkan/device.rs:35,41,44` | `vulkan11機能` / `vulkan12機能` / `vulkan13機能` | 条件8に対する疑い(ASCII・数字の混入禁止) | この3つの変数名は、Rust側の型名(`PhysicalDeviceVulkan11Features`等)が持つバージョン番号をそのまま識別子へ埋め込んでいる。オーナーが不合格にした実例の「識別子へASCII+数字を混ぜたもの(`段階A2`等)」と表面上は同じ形(ASCII語+数字+漢字)であり、Vulkanのバージョン番号を知らない読み手はこの名前だけから中身を復元できない。ただし本コードベースには`GPU`(型名は大文字・フィールド名は`gpu`)や`HDR`(ユビキタス言語に定義済み)のように、Rustのsnake_case制約とセットで英語の頭字語を許容してきた先例があり、`Vulkan`もユビキタス言語で「Vulkan仕様書の綴りのまま維持する」第3群語彙として扱われている。`vulkan11`等がその先例の範囲内(バージョン番号は仕様書の表記そのもの)か、A2型の恣意的な圧縮と同じ違反かは、リネーム員の判断に委ねる。改名の素案(バージョン番号でなく機能の中身で呼ぶ案): `vulkan11機能`→`頂点ID描画機能`(コメントにある「SV_VertexIDの再現に使うSPIR-V DrawParameters機能」から)、`vulkan12機能`→`材質表索引機能`(非一様添字と部分束縛の2機能)、`vulkan13機能`→`動的レンダリングと同期2の機能`
- `crates/blitz_render/src/vulkan/point_light_shadow_cull.rs:20,26` | `面ごとの絞り`(型名) | 条件1に対する疑い(受理テスト) | 「絞り」は単独では「何を絞るのか」が定まらず、カメラの絞り(露出)や範囲の限定など複数の読みがありうる。ただし型直上のドキュメンテーションコメントが「1つの灯の1つの面が描く対象を絞る判定」と説明しており、フィールド(灯の位置・影響半径・錐台)と`球が候補か`という公開メソッドの組から実体(シャドウ発行前の球ごとの候補判定)は復元できる。実体と乖離してはいないため強い違反とまでは言えないが、単独で読んだときの曖昧さは残る。改名の素案: `面ごとの影の候補判定`

## 違反ゼロの範囲

上記2件の疑いを除く、区画096の全37ファイル・全識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)に受理条件・命名の質・呼称と語の選択の違反は見つからなかった。特に次の点を確認した。

- 値オブジェクト様式の関連関数`生成する()`はすべて型名が目的語を担う形で合格しており、`create::生成する`のような裸の動詞の単独名は無かった(`readback_buffer/create.rs`の`四成分で生成する`/`二成分で生成する`は材料の格で複数ファクトリーを区別する条件5の合格例)
- 処理を保持するフィールド(`command_sink.rs`の`積み先`等)は動詞句のまま保たれており、旧`記録`問題(名詞形が処理を保持していた違反)の再発は無かった
- `記録`の語は`memory_ledger.rs`の`確保記録`(確保1件ぶんのデータの記録)にのみ現れ、GPUコマンド積み込みの意味とは衝突していない(1語1義を維持)
- `GPU`・`HDR`・`ID`の頭字語は、型名で大文字・フィールド/変数名でRustのsnake_case制約により小文字にする既存の書き分け(ユビキタス言語に明記済みの先例)の範囲内で一貫して使われており、新規の直訳調・膠着圧縮は見つからなかった
- 「試験の許容差の当て方」型(実体と名前の乖離)・「連立」型(名詞1語の圧縮)・詰め込みすぎ型のいずれの形も、この区画の識別子には見当たらなかった
