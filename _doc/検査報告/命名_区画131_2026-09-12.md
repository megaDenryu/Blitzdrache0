# 命名検査報告: 区画131(crates/editor_server/src)

- 区画番号: 131
- 範囲: `crates/editor_server/src` 配下39ファイル(一覧は`block_131.txt`)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、mainブランチのコミット `db815090`
- 直読したファイル数: 39/39(grepだけで済ませたファイルは0件。全ファイルを冒頭から末尾まで直読した)
- 検査した規約: グローバルCLAUDE.md「日本語命名の受理条件」8条・「日本語命名の質」・「呼称と語の選択」・`CLAUDE.md`「命名」節・`_doc/計画/ユビキタス言語.md`(該当語のみ照合、全文は404KBのため`grep`で該当語を都度突き合わせた)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/editor_server/src/bin/bake_one_building.rs:27` | `fn 書き出す() -> Result<String, String>` | 条件3(5W1Hの必要格)・条件5(裸の動詞の禁止) | ASCIIモジュール(`bake_one_building.rs`)直下の自由関数であり、名前単独で答える必要があるが「書き出す」だけでは何を書き出すのか(建物1棟の検証世界のソース)が無い。呼び出し元`main()`も同じASCII文脈のため補完できない | `一棟検証の世界ソースを書き出す` |
| `crates/editor_server/src/bin/contract_export.rs:23` | `fn 書き出す(出力先: &PathBuf, 本文: String) -> std::io::Result<()>` | 条件3・条件5 | 同上。ASCIIモジュール直下の裸の動詞。引数に「本文」はあるが、関数名自体は目的語を持たない単独の「書き出す」であり、名前だけでは「何を」書き出す処理か答えられない | `本文をファイルへ書き出す` |
| `crates/editor_server/src/export/chunk_directory_text.rs:10` | `pub(super) const ファイル名: &str = "chunk_directory.txt";` | 条件1(受理テスト)・条件3 | 「ファイル名」だけでは何のファイル名か単独で答えられない汎用名。現に呼び出し側(`export/command.rs:16`)が`use ... {ファイル名 as 目録ファイル名, ...}`とインポート時に改名しており、名前だけでは意味が伝わらないことを実装自身が示している | `目録ファイル名`(インポート側が実際に使っている名を正本にする) |
| `crates/editor_server/src/export/chunk_directory_text.rs:19` と `crates/editor_server/src/contract/mod.rs:28` | 両方とも `本文を組み立てる` | 条件6(1語1義) | 同じクレート内に「本文を組み立てる」という名の関数が2つ存在し、片方はTypeScript型契約の本文を、もう片方はチャンク目録テキストの本文を組み立てる。全く別の生成物に同じ名前を与えており、grep等で識別子を追う読み手が取り違える。ASCIIモジュール直下の自由関数でもあり単独で意味が取れる必要があるが、「本文」がどの本文か対象語が無い | `目録本文を組み立てる`(chunk_directory_text.rs側)。`contract/mod.rs`側は型契約の本文であるため据え置きでもよいが、区別のため`型契約の本文を組み立てる`も検討可 |
| `crates/editor_server/src/bake_editor_world.rs:15` | `エディターの世界をベイクする` | 条件6(1語1義)・日本語命名の質(直訳調・カタカナ語の基準) | 同じファイルのコメント(1-7行目)は一貫して「焼く」(エディターの世界を実行時形式へ焼く局面、層割当を焼く、既定の割当を使う…)を使っており、正本の`_doc/計画/ユビキタス言語.md`でも動詞形の主流は「焼く」である。公開メソッド名だけが英語からの直訳的カタカナ「ベイクする」になっており、同一ファイル内で同じ動作に2つの語が混在している | `エディターの世界を焼く` |
| `crates/editor_server/src/building_grid_store.rs:94` | `fn 定義の識別子が空である() -> 建物定義ID` | 条件4(品詞の規律) | この関数は`unwrap_or_else`から呼ばれ、実際には値を返さず`panic!`で異常終了する処理(処理=動詞句「~する」であるべき)なのに、名前が状態を述べる叙述文(「~である」)になっている。名前だけでは「これは処理を実行する関数だ」と読み取れず、値を返す通常の関数のように誤読されうる | `識別子が空の不変条件違反でパニックする` |
| `crates/editor_server/src/export/chunk_directory_text.rs:15`(構造体`目録項目`のフィールド)と `crates/editor_server/src/export/command/chunk_write.rs:84` | `アセットid` | 条件6(1語1義。用語定義ファイルとの照合で確定) | 同じクレート内で識別子の型は一貫して大文字の`ID`を使う(`建物定義ID`・`楽曲ID`・`パターンID`・`アセットID`(型名))。このフィールドだけ小文字の`id`を素の英語のまま日本語へ接着しており、同じ「ID」という語が大文字/小文字で綴り分かれ、grepでの一貫追跡ができない | `アセットID`(構造体へ`#[allow(non_snake_case)]`を付けて他の構造体と同じ様式にする) |
| `crates/editor_server/src/project_root.rs:66` | `fn projectフラグの次の引数を使う()`(テスト関数名) | 日本語命名の質(英語の直訳調・呼称と語の選択のカタカナ語基準) | 英単語`project`を訳さずそのまま日本語の文へ接着している。オーナーが「英語名は読まない」「カタカナ語は小学生が分かるものだけ」と裁定した規律に反し、この関数名だけ素のASCII単語が混入している | `プロジェクトフラグの次の引数を使う` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `crates/editor_server/src/chunk_height_supply.rs:42` | `チャンクの高さ格子を読み未保存ならマザーから切り出す` | 条件3・条件8 | 「読み未保存なら」は連用中止法による分岐条件の圧縮であり、文法的には成立するが一読で区切りを取りにくい。ただし実装(自分の保管庫を読み、無ければマザーから切り出す)を過不足なく写しており、詰め込みすぎの実例(判断条件を本質でない形で足す)には該当しないため、断定はしない |
| `crates/editor_server/src/building_grid_store.rs:86,57` | `一覧項目へ写す` | 条件2・条件3 | ASCIIモジュール(`building_grid_store.rs`)直下の自由関数として`.map(一覧項目へ写す)`の形で呼ばれ単独で意味が取れる必要があるが、変換先(一覧項目)は書かれているのに変換元(格子由来の建物定義)が名前に無い。ただし同種の「〜へ写す」は`ソースの識別子へ写す`等プロジェクト内で複数使われる型変換の慣用パターンであり、シグネチャで型が明示されるため致命的な紛れとまでは言えない |
| `crates/editor_server/src/building_grid_store/error.rs:47` と `crates/editor_server/src/export/response.rs:18` | 両方とも `状態と種別を決める` | 条件6 | 別々のエラー型に対する同名の非公開ヘルパー関数が2箇所にある。役割は同一(HTTP状態コードと種別文字列を決める)で意図的な並行命名の可能性が高く、引数の型で区別できるため1語1義の実害は小さいと判断し、疑いに留める |

## 違反ゼロの範囲

上記の違反・疑いを除く残り全ての識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも抵触しなかった。特に`建物の格子の保存係`・`ソースアセット書き出しコマンド`・`エディターのチャンク中心座標`・`実行時のチャンク内座標`・`出力世界名`・`世界ソース出力先`等の値オブジェクト/コマンド型は、型名がレシーバとして目的語を担う形で一貫しており、条件5(裸の動詞の禁止)を満たしていた。エラー型の列挙の枝(`建物の格子の保存エラー`・`書き出しエラー`等)も5W1Hの必要格を満たす具体的な名前が付いていた。
