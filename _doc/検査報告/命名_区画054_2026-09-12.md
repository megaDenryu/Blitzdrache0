# 命名検査報告: 区画054 crates/blitz_engine/src/asset/runtime_format/scene/read_element

- 区画名と範囲: `crates/blitz_engine/src/asset/runtime_format/scene/read_element/` 配下17ファイルと、`scene/v1〜v7`配下15ファイル(read.rs・write.rs・conversion_tests.rs・roundtrip_tests.rs)。合計32ファイル
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090`(このツリーは編集していない)
- 直読したファイル数: 32/32(grepだけで済ませたファイルは0)
- 検査に使った必読: グローバルCLAUDE.mdの「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(TRS・PBR・地表の層の重ね合わせ・シーン等の登録語を照合に使用)

## 違反の一覧

### 1. ASCIIモジュール直下の自由関数が裸の`読む`になっている(条件2・3。5W1Hの「何を」欠落)

`read_element/`配下のファイルは全部ASCII名(material.rs等)であり、条件2により「ASCIIモジュール直下の自由関数は名前単独で答える」ことが要求される。以下の関数はモジュール名(読まれない前提)に頼って初めて対象が分かる、裸の`読む`である。呼び出し元(`read_element::マテリアルを読む`等、v1/read.rs等で確認)は既に正しい完全名で再輸出しているため、**定義側だけがこの規約を満たしていない二重名になっている**(条件6の1語1義にも触れる: 同じ処理に「裸の読む」と「完全な名前」の2つの名前が存在する)。

- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/material.rs:10` | `読む` | 条件2・3 | 「material.rs」というASCII名は補完に使えないため、この識別子だけでは何を読むか分からない。呼び出し元は`マテリアルを読む`という完全名で再輸出している | `マテリアル1件を読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/material_set.rs:17` | `読む` | 条件2・3 | 同上。材質集合(複数件の対応表)を読む処理だが名前に対象が無い | `材質集合を読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/mesh.rs:12` | `旧版を読む` | 条件2・3 | 「旧版」だけでは何の旧版か分からない(メッシュの旧版であることはmesh.rsという読まれない前提のファイル名でしか分からない)。呼び出し元は`旧版のメッシュを読む`という完全名で再輸出している | `旧版のメッシュを読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/mesh.rs:25` | `読む` | 条件2・3 | 同上(版4のメッシュを読む処理)。直上のドキュメンテーションコメントには対象が書いてあるが、識別子自体は単独で答えられない | `版4のメッシュを読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/mesh_list.rs:12` | `読む` | 条件2・3 | メッシュのLOD段の列を読む処理だが対象が無い。呼び出し元は`メッシュ列を読む`という完全名で再輸出している | `メッシュ列を読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/mesh_primitive.rs:12` | `読む` | 条件2・3 | プリミティブ列を読む処理だが対象が無い | `プリミティブ列を読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/mesh_skin_vertex.rs:9` | `読む` | 条件2・3 | スキン頂点属性の一覧(有無つき)を読む処理だが対象が無い | `スキン頂点属性一覧を読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/pbr_material_body.rs:13` | `読む` | 条件2・3 | 金属粗さPBRの本体を読む処理だが対象が無い | `金属粗さPBR本体を読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/surface_layer_body.rs:14` | `読む` | 条件2・3 | 地表の層の重ね合わせの本体を読む処理だが対象が無い | `地表の層の重ね合わせ本体を読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/skin.rs:11` | `読む` | 条件2・3 | スキンデータを読む処理だが対象が無い。呼び出し元は`スキンを読む`という完全名で再輸出している | `スキンを読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/animation/channel.rs:11` | `読む`(ジェネリック関数) | 条件2・3 | 1つのキーフレームチャンネルを読む処理だが対象が無い | `チャンネルを読む`

これら11件は同一の欠落パターン(ASCIIファイル名への依存)であり、パターンの一貫を理由に省略せず全件を挙げた。

### 2. 版ごとの読み方を束ねる構造体のフィールドとゲッターが、処理(関数)を名詞1語へ圧縮している(条件4。品詞の偽装、記録フィールドと同型)

`crates/blitz_engine/src/asset/runtime_format/scene/read_element/version_codec.rs`の`版ごとの要素の読み方`構造体は、3つのフィールドいずれも型が関数(読む処理)であるにもかかわらず、名前は処理を表す動詞を持たない名詞1語になっている。受理条件4は「処理を保持するフィールド=動詞句のまま」を要求しており、これは2026-08-09制定の経緯にある「`記録`フィールドが処理なのに名詞形で、ログと誤読された」実例と同型の欠陥である。

- `version_codec.rs:13`(フィールド)・`version_codec.rs:51-53`(同名ゲッター) | `テクスチャ1件` | 条件1・4 | 型は`テクスチャを読む工程`(関数)だが、名前は「1個のテクスチャ」という値に読める。`material_set.rs:27`の呼び出し`読み方.テクスチャ1件()`は関数を返すため直後に別の関数へ渡されるが、名前だけでは関数だと分からない | `テクスチャ1件を読む処理`
- `version_codec.rs:14`(フィールド)・`version_codec.rs:55-57`(同名ゲッター) | `材質1件` | 条件1・4 | 同上。`material_set.rs:27`の`(読み方.材質1件())(入力, ...)`という二重呼び出しの形自体が、名前が関数を隠していることの証拠である | `材質1件を読む処理`
- `version_codec.rs:15`(フィールド)・`version_codec.rs:59-61`(同名ゲッター) | `静的物理形状の節` | 条件1・4 | 型は`静的物理形状の節を読む工程`(関数)だが、名前は「節(データそのもの)」に読める | `静的物理形状の節を読む処理`

### 3. 識別子へのASCII文字の混入(呼称と語の選択・命名の質)

- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/skin.rs:54` | `trsを読む` | ASCII混入 | 小文字ASCII「trs」が日本語のひらがなへ直接接着している。ユビキタス言語での正式表記は大文字「TRS」(登録済み略語)であり、綴りも不統一。関連実例(段A2の候補_等)と同型の禁止パターン | `静的TRSを読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/v4/conversion_tests.rs:29` | `版4のテクスチャは格納形式rgba8の1段として読める`(テスト関数名) | ASCII混入 | 小文字ASCII「rgba8」が日本語識別子へ直接混入している。エンジン側の正式な列挙子名は大文字の`RGBA8`であり綴りが不統一 | `版4のテクスチャは格納形式RGBA8の1段として読める`

## 違反の疑い(断定しない)

- `crates/blitz_engine/src/asset/runtime_format/scene/read_element/animation.rs:19` | `一覧を読む` | 条件2・3に対する疑い | 「一覧」は対象を部分的にしか示さず(何の一覧かはanimation.rsというASCIIファイル名に依存する)、上の「1. 裸の読む」と同系統だが「一覧」という語がわずかに対象を絞っているため、他の完全に裸な`読む`より軽微と判断し疑いに留めた。呼び出し元は`アニメーション一覧を読む`という完全名で再輸出している | `アニメーションクリップ一覧を読む`
- `crates/blitz_engine/src/asset/runtime_format/scene/v5/conversion_tests.rs:15`、`v6/conversion_tests.rs:16` | `材料のシーン`(関数名) | 条件4に対する疑い | この関数は内部で`静的シーンを作る()`を呼んで値を組み立てる処理だが、名前が名詞1語で動詞を持たない。同じファイル内の兄弟関数`版5バイト列を作る`・`版6バイト列を作る`は動詞「作る」を持ち、命名の一貫性が崩れている。ただし呼び出し箇所には必ず`()`が付き関数呼び出しであることは構文上明白なため、致命的な誤読とまでは断定しない | `材料のシーンを作る`

## 違反ゼロと確認した範囲

- `read_element/instance.rs`・`read_element/matrix.rs`・`read_element/texture.rs`・`read_element/static_shape.rs`・`read_element/material_element.rs`・`read_element/material_kind.rs`は、いずれも自由関数名に対象語を伴っており(`配置列を読む`・`行列を読む`・`版4までのテクスチャを読む`・`版7の静的物理形状を読む`・`係数を読む`等)、条件2〜5を満たす。これらのファイルの存在自体が、上記「裸の読む」10件超が個別の判断ミスであって書式上の制約でないことの根拠になる
- `v1〜v7`の`read.rs`・`write.rs`の関数群(`シーン内容を読む`・`描画対象を読む`・`形状を読む`・`シーン内容を書く`・`描画対象を書く`・`形状を書く`)は全て対象語を伴い、版番号も「版ごとの要素の読み方」型の関連関数として型名が目的語を担う形(条件2の値オブジェクト様式と同型)になっており、違反は無い
- `conversion_tests.rs`・`roundtrip_tests.rs`の大半のテスト関数名(`版1のシーンを通常メッシュとして最新版へ変換して読む`等)は動詞句として完結しており、5W1Hの必要格を満たす
