# 命名検査報告: 区画101 crates/blitz_render/src/vulkan/frame/record

- 区画名と範囲: crates/blitz_render/src/vulkan/frame/record 配下、38ファイル(block_101.txtの一覧全件)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 38 / 38(全ファイルを直読。grepだけで済ませたファイルは無い)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`(grepで関連語のみ照合。407KBのため全文は読まず、識別子ごとに該当語を検索した)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_render/src/vulkan/frame/record/tonemap_pass.rs:17, 25 | `光のにじみa`(引数名) | 受理条件1(受理テスト)、識別子への英字混入の禁止 | 末尾の`a`が日本語のどの格にも対応しない裸のASCII添字である。かつて2枚(a/b)を区別していた名残と見られるが、現在の呼び出し元(`post_passes.rs`)は`光のにじみ最終ハンドル()`が返す1本しか渡しておらず、実体(唯一の最終結果)と名前(a/bの一方を示す添字)が一致していない。読み手は「aとは何の一方か」を復元できない | `光のにじみ最終` または `光のにじみ結果`(呼び出し元の`光のにじみ最終ハンドル()`と語を揃える) |

## 違反の疑い

疑いに留めた項目は無い。以下は検討したが合格と判定した(根拠を残す)。

- `画像用途::焼いた画像の画素段参照`・`焼いた画像`(aerial_composite_pass.rs 等複数箇所): ユビキタス言語.md 547行に「焼いた画像」が独立の登録語(フレームをまたいで中身を保ちGENERALで休む画像の総称)として定義されており、「ベイク済み画像」(個別の生成物)とは指す範囲が異なる。1語1義の衝突ではなく、上位概念と下位概念の別語であるため合格
- `拘束の引数`・`目標拘束の引数`・`曲げ拘束の引数`(cloth_passes.rs 等): ユビキタス言語.md 973・990・1006行に同名の登録語があり、XPBD拘束の静的パラメータを指す確立した用語。「引数」は一見プログラミング用語の直訳に見えるが、ドメイン語彙として登録済みのため合格
- `viewport一覧`・`シザー一覧`・`セット一覧`等、ash/vkの型名(英語)へ日本語の助数詞「一覧」を付けた変数名: `ash`や外部クレートの型・関数はそのままの英語名で扱う規約により、vk型名(loanword)+日本語助詞の合成は補完文脈の限定条件2の対象外(型名自体が外部語彙として許容されている)。区画全体で一貫しており、実体(vk::Viewportの配列等)とも一致する
- `hdr`・`hdr画像`・`hdrハンドル`(auto_exposure_passes.rs, graph_build/post_setup.rs 等): HDRはユビキタス言語.md 118行で登録済みの一般に通用する専門用語(専門用語の表記の基準を満たす)であり、識別子への混入としての違反対象ではない
- `pass_names.rs`の`深度プリパス`・`シーン描画`・`深度プリパスとシーン描画の合計`: 定数名として実体(パス名の正本)と一致し、名詞句として品詞規律にも合致

## 違反ゼロの範囲

- 38ファイルのうち37ファイルは日本語識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)に受理条件8条の違反が無かった。特にコールバック引数の名前が動詞句(`記録する`・`宣言する`・`積む`)で統一され、値オブジェクト様式の`生成する()`は型名・列挙名が目的語を担っており条件5に合格していることを確認した
- `graph_build`配下の登録関数(`登録する`)はASCIIモジュール名直下だが、いずれも`pub(super)`でモジュール内呼び出しに限定され、実際の呼び出しは`base_images::登録する`のように日本語型名を伴う形で行われているため条件2の補完に問題は無い

## 検査の範囲外(この検査がしていないこと)

- コメント文中の日本語散文の質(直訳調・膠着圧縮)は識別子検査の対象外としたため精査していない(受理条件は識別子が対象)
- `_doc/計画/ユビキタス言語.md`は407KBのため全文を通読せず、各識別子について該当しそうな語をgrepで検索する形で照合した。検索語を外した場合、登録済みなのに未検出のまま合格/違反を誤判定した可能性がある
