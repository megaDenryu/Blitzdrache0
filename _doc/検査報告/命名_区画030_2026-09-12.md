# 命名検査報告: 区画030 crates/blitz_asset_compiler/src/placement + runtime_compilation

## 範囲・時点

- 区画番号: 030
- 区画名: `crates/blitz_asset_compiler/src/placement`(実際の対象は`placement/`配下24ファイルと`runtime_compilation/`配下20ファイルの合計40ファイル。一覧ファイル`block_030.txt`の記載どおり)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(このツリーは編集していない)
- 時点: main の `db815090`
- 検査対象ファイル数: 40
- 直読したファイル数: 40 / 40(grepだけで済ませたファイルは0)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(オープンワールドの語彙・データの語彙ほか)

## 結論

**違反0件。** 40ファイル全部を直読し、型・関数・メソッド・フィールド・変数・定数・列挙の枝を受理条件8つで当てたが、受理条件を落とす識別子は見つからなかった。

## 検査の観点別メモ(合格側の根拠)

- **品詞の規律**: 値は名詞句(`原型と置き方の指定`・`密度場の散布`・`焼く世界の指定`)、処理は動詞句「〜する」(`密度場で散らした配置列を作る`・`原型1つ分の群の描画対象を作る`・`縁の一致を確かめる`)で一貫しており、値と処理の取り違えは無い
- **5W1Hの必要格**: `密度場で散らした配置列を作る`・`個数を決めて並べた配置列を作る`のように、材料(密度場・個数)と対象(配置列)と動作(作る)が名前に揃っている。`原型1つ分の群の描画対象を作る`も対象の単位(1つ分)を明示している
- **裸の動詞の禁止**: モジュール直下の自由関数に裸の`生成する`・`作成する`は無い。`配置の混合の通し番号::描画対象番号から生成する()`・`配置の種::種の由来から作る()`は型名が目的語を担う値オブジェクト様式であり合格。複数のファクトリーは`散布の一覧の先頭()`・`描画対象番号から生成する()`のように材料・条件の格で区別されている
- **実体との一致(オーナー実例1の観点)**: `密度場の散布`(`scatter_specification.rs`)は走査の列数・密度場・出現の割合・傾きの好み・大きさの範囲・据え方の6欄を束ねた設定であり、名前がその実体(散布の仕方の指定一式)と一致している。`焼き方の指定`・`焼く世界の指定`も同様に、複数欄を束ねた設定であることを名前の「指定」が正しく予告している
- **名詞1語の圧縮の禁止(オーナー実例2の観点)**: `個体数の勘定`・`置いた個体の勘定`・`原型1つ分の群`はいずれも何を集計・所有するかの語を伴っており、「勘定」「群」だけの単独名詞で終わる箇所は無い
- **詰め込みすぎの禁止(オーナー実例3の観点)**: 長い名前(`部品で組む散布の指定一覧を作る`等)はあるが、各部分が実装の各欄と対応しており、本質でない条件を混ぜて意味が壊れている例は無い
- **ASCII・ギリシャ文字・数学記号の混入**: 型名・関数名・フィールド名の大部分は日本語で統一されている。`原型id`・`安定id`・`材質見本の立体の安定id`・`遠景地面の安定id`のようにフィールド名の末尾へ小文字の`id`が付く箇所が複数ある(`archetype_placement.rs`・`archetype_identity.rs`・`compile_target.rs`・`source_kind.rs`等)。`id`は一般に通用する略語(UI・API等と同列)として扱い、単独では受理条件7(直訳の検出)にも条件5(裸の動詞)にも当たらないため違反とはしない。ただし用語定義ファイルの`アセットID`・`安定ID`は大文字表記であり、大文字小文字の不統一が読める(下記「違反の疑い」参照)
- **1語1義**: `個体数の勘定`(実行時カタログからの集計)と`置いた個体の勘定`(焼く工程が積み上げた集計)は、同じ「勘定」という語を使うが指すものが違う。ファイル冒頭のコメントが「2つが同じ1つの計算から出ると突き合わせが自分自身との比べになる」と明示的に別概念であることを説明しており、語の意味が1つに定まっているため1語1義の違反ではない(意図的な二重計測)
- **直訳の検出**: `record`→`記録`、`resolve`→`解決`、`lookup`→`引く`のような英語直訳調の語は見当たらない。`ソースパスを参照する`・`地表高さを求める`のように動作をそのまま表す日本語が選ばれている

## 違反の一覧

該当なし(0件)。

## 違反の疑い

1. `crates/blitz_asset_compiler/src/placement/archetype_placement.rs:13` | `原型id`(構造体`原型と置き方の指定`のフィールド) | 受理条件2(補完してよい文脈の限定)に対する疑い | 用語定義ファイルでは`アセットID`・`安定ID`のように大文字で書く語が、このフィールドでは小文字の`id`になっており、同じ概念を指す語の大文字小文字表記がリポジトリ内で割れている。読めなくなるわけではないが、1語1義の徹底という観点では表記の統一が望ましい | 改名の素案: `原型ID`(既存の`アセットID`表記へ揃える)
2. `crates/blitz_asset_compiler/src/runtime_compilation/archetype_identity.rs:16,22,28`ほか多数(`安定id`というフィールド名・関数名・仮引数名) | 同上、条件2に対する疑い | 同上(この綴りはプロジェクト全体で多用されており、区画030だけの局所是正では直せない可能性が高い) | 改名の素案: 是正するなら`安定ID`へ全体一括ではなく、ユビキタス言語の表記(大文字ID)とコード側の慣用(小文字id)のどちらを正本にするか方針を確認してから、作業のついでに直す
3. `crates/blitz_asset_compiler/src/runtime_compilation/source_kind.rs:55-57`ほか(`Gltfシーン`・`寸法を合わせるGltfシーン`という列挙の枝名) | 受理条件7(直訳の検出)ではなく質の節「英語の頭字語を日本語の中に混ぜない」に対する疑い | `glTF`は業界の標準規格名であり一般に通用する語とみなせるが、大文字小文字の綴り(`Gltf`)が正式表記`glTF`と異なる。読解を妨げるほどではないため違反とまでは言い切らない

## 違反ゼロの範囲

- `crates/blitz_asset_compiler/src/placement/`配下24ファイル全部(`archetype_placement.rs`・`assembled_scatter.rs`・`density_scatter.rs`・`deterministic_value.rs`・`fixed_count.rs`・`ground_preference.rs`・`group_draw_object.rs`・`layout.rs`・`mixing_serial.rs`・`orientation.rs`・`scatter_ranges.rs`・`scatter_specification.rs`・`scatter_tests.rs`・`seed.rs`・`scatter_ranges/range_tests.rs`・`scatter_tests/bounds_and_grounding.rs`・`scatter_tests/density.rs`・`scatter_tests/determinism.rs`・`scatter_tests/fixture.rs`・`scatter_tests/slope.rs`)
- `crates/blitz_asset_compiler/src/runtime_compilation/`配下20ファイル全部(`archetype_identity.rs`・`bake_report.rs`・`building_outline_catalog.rs`・`catalog.rs`・`chunk_ledger.rs`・`chunk_world.rs`・`compilation.rs`・`compilation_specification.rs`・`compile_target.rs`・`distant_terrain.rs`・`error.rs`・`height_field.rs`・`instance_tally.rs`・`mod.rs`・`placed_instance_tally.rs`・`source_kind.rs`・`source_location.rs`・`surface_layer_texture_set.rs`・`vegetation_definition.rs`・`world.rs`)

上の「違反の疑い」3件を除き、40ファイルの全識別子が受理条件8つを満たす。
