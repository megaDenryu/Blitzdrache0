# 命名検査 区画059

- 区画名: crates/blitz_engine/src/lod_threshold, crates/blitz_engine/src/primitive_draw_item(区画番号059)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090（このツリーは編集していない）
- 直読したファイル数: 12/12（全ファイルを直読した。grepだけで済ませたファイルは無い）
  - crates/blitz_engine/src/lod_threshold/band.rs
  - crates/blitz_engine/src/lod_threshold/error.rs
  - crates/blitz_engine/src/primitive_draw_item/build.rs
  - crates/blitz_engine/src/primitive_draw_item/build_fixture.rs
  - crates/blitz_engine/src/primitive_draw_item/build_tests.rs
  - crates/blitz_engine/src/primitive_draw_item/draw_range.rs
  - crates/blitz_engine/src/primitive_draw_item/error.rs
  - crates/blitz_engine/src/primitive_draw_item/fragment.rs
  - crates/blitz_engine/src/primitive_draw_item/item.rs
  - crates/blitz_engine/src/primitive_draw_item/list.rs
  - crates/blitz_engine/src/primitive_draw_item/material_binding.rs
  - crates/blitz_engine/src/primitive_draw_item/visible_interval.rs
- 用語定義ファイルの参照: `_doc/計画/ユビキタス言語.md` を grep で確認した。「静的描画片」「可視個体区間参照」「材質束縛参照」「プリミティブ描画項目」は登録済み。「描画束ID」の項が「束」の正式な意味（レンダラーがチャンク単位を区別する番号）を定義している。「段閾値帯」「ヒステリシス幅」は未登録（今回の8条件による違反ではないが、登録漏れとして参考記載する）

## 違反の一覧

1. `crates/blitz_engine/src/primitive_draw_item/list.rs:22-23` | `束`（`積む(&mut self, 束: プリミティブ描画項目)` の引数名、`self.一覧.push(束)`） | 条件6（1語1義） | `_doc/計画/ユビキタス言語.md`の「描画束ID」項が「束」をチャンク単位でレンダラーが区別する番号の意味に確定しており、同じ`list.rs`冒頭の型コメント（「1つの束が持つ全描画対象の…」「束の読込時に一度だけ組み立て…」）でも束はその意味で使われている。ところがこの引数名は単一の`プリミティブ描画項目`（1件）を指しており、読み手は「複数個をまとめたもの」と誤読する。実体はチャンクではなく1件の描画項目である
   - 改名の素案: `項目`（`積む(&mut self, 項目: プリミティブ描画項目)`）
2. `crates/blitz_engine/src/primitive_draw_item/build_tests.rs:10` | `二材質のプリミティブごとに束を作り可視個体区間を共有する`（テスト関数名） | 条件6 | 同上。「プリミティブごとに束を作り」は、プリミティブ1つにつき`プリミティブ描画項目`が1件できるという意味だが、「束」を使うとチャンク単位の概念（描画束ID）と衝突し、1プリミティブ＝1チャンクであるかのように読める
   - 改名の素案: `二材質のプリミティブごとに描画項目を作り可視個体区間を共有する`
3. `crates/blitz_engine/src/primitive_draw_item/build_tests.rs:28,30` | `束`（コメント「束が指すのは参照されたスロットだけである」、テスト名`参照されないスロットの束を作らない`） | 条件6 | 上記2件と同型。ここでの「束」は個々の`プリミティブ描画項目`を指しており、登録済みの「束＝チャンク識別」の意味と衝突する
   - 改名の素案: コメントを「描画項目が指すのは参照されたスロットだけである」、テスト名を`参照されないスロットの描画項目を作らない`
4. `crates/blitz_engine/src/primitive_draw_item/build_fixture.rs:56` | `組み立てる`（`pub(super) fn 組み立てる(...)`） | 条件1・条件5（裸の動詞の禁止） | このcrateの命名規約上、ファイル名（`build_fixture`）はASCIIでモジュール境界を作るだけで意味の補完に使えない。呼び出し元（`build_tests.rs:6,11,31,44,52`）は`build_fixture::組み立てる(...)`と読むが、`組み立てる`単独では「これは何?」に答えられない（何を組み立てるのかが名前に無い。`create::生成する`と同型の違反）。同ファイル内の`二材質の集合`・`形状を作る`は目的語を持つのに、この関数だけ目的語が落ちている
   - 改名の素案: `プリミティブ描画項目一覧を組み立てる`

## 違反の疑い

1. `crates/blitz_engine/src/lod_threshold/band.rs` 全体（`ヒステリシス幅`・型コメントの「ヒステリシス」） | 呼称と語の選択・条件3（カタカナ語は小学生が分かるものだけ） | 「ヒステリシス」は小学生には分からないカタカナ語である一方、遅れを伴う閾値切り替えを指す確立した物理・工学用語であり、平易な日本語だけでは「基準距離の前後で往復しても切替わらない現象」という精度を保てない。マイルストーン→節目のような単純な言い換えが利かない専門語であるため、直ちに違反とは断定しない。**判断の分かれ目は「非専門家が読んで意味が取れるか」であり、`_doc/計画/ユビキタス言語.md`に「ヒステリシス幅」の項を立てて初出補足を与えれば解消する**（現状は未登録）
2. `crates/blitz_engine/src/lod_threshold/`全体の`段閾値帯`・`段番号`という語彙 | 条件6の前提条件（用語定義ファイルとの照合） | 「段閾値帯」「ヒステリシス幅」がユビキタス言語.mdに未登録のため、他の箇所（例えば地形詳細段・個体詳細段の「段」）と語が衝突していないかを機械的に照合できなかった。今回読んだ範囲では「段」の用法（0が最詳細、番号が増えるほど粗い）はユビキタス言語.mdの「地形詳細段」「個体詳細段」の定義と整合しており、衝突は無いと判断したが、登録の欠落自体は指摘する

## 違反ゼロの範囲

- `crates/blitz_engine/src/lod_threshold/error.rs`、`crates/blitz_engine/src/primitive_draw_item/build.rs`、`error.rs`、`draw_range.rs`、`fragment.rs`、`item.rs`、`material_binding.rs`、`visible_interval.rs`の識別子（型名・関連関数`生成する()`・フィールド名・アクセサ・エラー枝）は、値=名詞句/処理=動詞句の品詞規律を守り、型名またはレシーバによる目的語の補完が効いており、直訳調・膠着圧縮・裸の動詞・英字/ギリシャ文字/記号の混入のいずれにも該当しない
