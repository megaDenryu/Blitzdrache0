# 命名ローラー検査 区画093

- 区画名: `crates/blitz_render/src/renderer/scene_draw_resources/render_object_resources` を中心とする40ファイル(`render_object_resources`本体14件・`work_area`9件・`ui_dispatch`1件・`shader_set/position_invariant`5件・`temporal_reconstruction`1件・`terrain_detail`2件・`texture_material`7件)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller` の main、コミット `db815090`(このツリーは編集していない)
- 一覧の出どころ: `scratchpad/roller/block_093.txt`(40ファイル)
- 直読したファイル数: 40/40。grepだけで済ませたファイルは0件(全ファイルをReadで全文読んだ)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、ルート`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(該当語のgrep照合)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `render_object_resources/slot_material_ids.rs:36` | `スロット番号で引く` | 条件7(直訳の検出) | 「引く」はlookupの直訳であり、CLAUDE.mdの「命名」節が既に確定した訳語表で`lookup/sampleの「引く」→「参照する」`へ改めると決めている語そのもの。英語のlookupを知らない人には「引く」が何を指すか(GPUのバッファ操作か、単なる引き算か)復元できない。同じクレート内の同種の操作は`材質スロット番号から大域材質idを参照する`のように「参照する」を使っており、同じ概念に2つの動詞が併存している(条件6 1語1義にも抵触) | `スロット番号で参照する` |
| `render_object_resources/slot_binding.rs:20` | 呼び出し `スロット番号で引く(...)` | 条件7 | 上と同一識別子の呼び出し。定義を直すと連動して直る | (定義側の改名に追従) |
| `work_area/object_entry.rs:60` | `可視個体選択一覧.引く(材料.束id, 材料.対象添字)` | 条件7 | 呼び出し先(`可視個体選択一覧`型、本区画外で定義)のメソッド名が「引く」。束idと対象添字から計画を検索する操作をlookupの直訳で表しており、非専門家が「引く」から検索・参照だと復元できない | `束idと対象添字で参照する`(定義側の改名が必要。本区画では呼び出し箇所として検出) |
| `work_area/stage_issue.rs:49` | `台帳.引く(シャドウのキー)` | 条件7 | パイプライン台帳からキーで実体を検索する操作が「引く」。同ファイル中の他の検索操作(`材質を解決する`等)と語が揃っておらず、lookupの直訳がそのまま残っている | `台帳.キーで参照する` |
| `work_area/stage_issue.rs:65` | `台帳.引く(シーンのキー)` | 条件7 | 同上 | 同上 |
| `work_area/stage_issue.rs:66` | `台帳.引く(台帳.深度プリパスのキー(...))` | 条件7 | 同上 | 同上 |
| `work_area/tally.rs:58` | 引数名 `発行数を引く: impl Fn(可視パス) -> usize` | 条件7 | コールバック引数名が「パスから件数を引く」という説明どおりlookupの意味で使われているが、「引く」は減算とも読め、usizeを返す関数名として紛れる。CLAUDE.mdの確定訳語では「参照する」を使うべき語 | `発行数を参照する` |
| `work_area/tally.rs:59` | 引数名 `記録側を引く: impl Fn(可視パス) -> 記録側の計数` | 条件7 | 同上 | `記録側を参照する` |
| `shader_set/position_invariant.rs:22,24,26,28` および `decoration_scan.rs:7`・`word_stream.rs`(importで参照)・`position_invariant_tests.rs:7,17,19,20,40,42` | 定数 `命令_OP_DECORATE`・`装飾_BUILT_IN`・`装飾_INVARIANT`・`組み込み_POSITION` | 質(頭字語・省略語を日本語へ混ぜない)、条件2 | `OP_DECORATE`・`BUILT_IN`・`INVARIANT`・`POSITION`はSPIR-V仕様書の生の英語トークンをそのまま日本語の接頭辞へ結合した名前であり、UI/CPU/GPU/APIのように非専門家が読める一般語ではない(SPIR-V仕様を知らない読み手には意味が復元できない)。ASCII部分は「読まれないものとして扱う」規律にも反し、定数名の後半が実質補完不能な暗号になっている | `命令コード_装飾指定`・`装飾種別_組み込み変数`・`装飾種別_揺らし禁止`・`組み込み変数種別_位置`等、SPIR-Vの語を意訳するか、値の意味(装飾コード18=Invariant等)を日本語で書く。トレーサビリティが要るなら行末コメントで`OpDecorate`等の仕様上の綴りを添える形にする |
| `shader_set/position_invariant/word_stream.rs:43` | `命令一覧を数える(語一覧: &[u32]) -> Vec<命令の範囲>` | 条件4(品詞の規律)・条件1(受理テスト) | 名前は「数える」(count)だが、戻り値は個数(usize等)ではなく命令ごとの範囲を並べた`Vec<命令の範囲>`である。呼び出し元の変数名も`let 命令一覧 = ...`であり、「一覧」を代入する動作に「数える」という動詞は実体と食い違う。ファイル冒頭コメントも「命令の先頭位置を列挙する」と書いており、実装者自身が「数える」でなく「列挙」と説明している | `命令一覧を列挙する`(自身の関数コメント「ヘッダの後ろに並ぶ命令を先頭から列挙する」と一致させる) |
| `ui_dispatch/merge.rs:32` | `self.ui一式.setを取得する(メッシュ.テクスチャid)` | 質(頭字語・省略語を日本語へ混ぜない)、条件2 | `set`は英単語がそのまま日本語の助詞「を」に接続されており、UI/GPU等の一般に通用する頭字語ではない生の英単語である。ディスクリプタセットを指すなら「セット」または「ディスクリプタセット」に訳すべきで、ASCII部分は補完力ゼロという規律にも反する | `self.ui一式.ディスクリプタセットを取得する(メッシュ.テクスチャid)` |

## 違反の疑い

| ファイル:行 | 識別子 | 疑う条件 | 理由(断定しない根拠) |
|---|---|---|---|
| `work_area/stage_issue/material_resolve.rs:26` | `材質資源表.束縛した世代で材質を解決する(...)` | 条件7(直訳の検出。resolve→解決は受理条件の失敗例として名指しされている) | `_doc/計画/ユビキタス言語.md:557`に「世代内材質解決」という戻り値の型が登録語として定義されており、動詞「解決する」はその登録名詞の自然な活用形とも読める。ただし受理条件7は「resolve→解決」を直訳の典型失敗例として明記しており、動詞そのものが英語のresolveを知らないと「何を解決したか(問題の解消なのか、IDから実体への変換なのか)」を復元しづらい。定義は本区画外(`vulkan::material_table`)にあり、改名の要否はその型の文脈を含めて判定すべきため断定を避ける |
| `work_area/stage_issue.rs:66`・`work_area/fill_input.rs`(構造体は本区画外定義) | フィールド/メソッド名 `深度プリパスpipeline`・`台帳.シーンlayout()` | 質(頭字語・省略語を日本語へ混ぜない) | 「深度プリパス」+「pipeline」、「シーン」+「layout」が助詞なしで直結し、ASCII単語が生のまま日本語へ膠着している。ただしこれらの名前(フィールド名`pipeline`・メソッド`シーンlayout`)は本区画外の`crate::vulkan::frame`・`crate::vulkan::pipeline_ledger`で定義されており、本区画のファイルは呼び出し・構造体リテラルとして使っているだけである。定義側の型を読まずに改名を断定するのは越権のため疑いに留める |
| `texture_material/storage_format.rs`・`mod.rs`・`texture_material_tests.rs`ほか | `RGBA8`・`BC1`・`rgba8の原寸から生成する` | 質(頭字語・省略語を日本語へ混ぜない) | RGBA8・BC1はテクスチャ格納形式の業界標準の符号であり、UI/GPU/APIと同格の「専門分野で広く通用する記号」とも扱えるが、「非専門家が読んで意味が取れるか」を厳密に当てると復元できない読み手もいる。プロジェクト全体でVulkanの`vk::Format::R8G8B8A8_UNORM`等ASCII形式名を境界で使う慣行と地続きであるため、断定でなく疑いとする |
| `renderer/scene_draw_resources/work_area/readout.rs:31` | ライフタイム引数 `'計器` | (受理条件の対象種別「型・関数・メソッド・フィールド・変数・定数・列挙の枝」に厳密には含まれないため対象外の可能性) | `<'計器 self>`が指すのは`シーン描画資源`(レンダラー内部の作業領域)の借用であり、「計器」(測定器)という語がその実体を予告しない。対象種別の定義次第で検査範囲に入るかが変わるため、違反と断定せず記録だけ残す |

## 違反ゼロの範囲

以下は40ファイルのうち、直読して受理条件8つ・質の3観点(頭字語・直訳・膠着圧縮)・呼称選択・実例3形(実体不一致/名詞1語の圧縮/詰め込みすぎ)・ASCII/ギリシャ文字/記号混入のいずれにも該当する識別子が見つからなかった範囲である。

- `render_object_resources/bounding_sphere.rs`・`create.rs`・`frame_write.rs`・`geometry_list.rs`・`instance_record_storage.rs`・`list.rs`・`seen_record.rs`・`shared_single_column.rs`・`shared_single_column_tests.rs`・`visible_id_content.rs`・`visible_id_content_tests.rs`・`visible_id_source.rs`・`bounding_sphere/combine.rs`
- `work_area/fill_input.rs`・`point_light_shadow_issue.rs`・`readout.rs`(ライフタイム名を除く)・`sort.rs`・`sort/order_tests.rs`
- `temporal_reconstruction/synthetic_input.rs`
- `terrain_detail/level.rs`・`selection.rs`
- `texture_material/error.rs`・`level_extent.rs`・`level_sequence_check.rs`(定数・関数名とも整合しており実体と一致)

省略した範囲は無い。全40ファイルを冒頭から末尾まで読み、識別子ごとに受理条件を当てた。「パターンが一貫しているので省略した」という判断は行っていない。
