# 命名検査報告: 区画120「crates/blitz_sim/src/contact/pipeline 第1区画」

- 区画: crates/blitz_sim/src/contact/pipeline 第1区画(区画番号120、40ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main の db815090、このツリーは編集していない)
- 直読したファイル数: 40/40(grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

### 違反1: `当て方`という圧縮名が、ユビキタス言語が改称した後もコードに残っている

`_doc/計画/ユビキタス言語.md`の「試験の許容差の適用規則」の項目は次のように記す。

> 試験の許容差の適用規則(2026-09-09 実装、試験と計器の専用、2026-09-12 に本番の形と項目名を改訂。旧称: 当て方(2026-09-12 に改名。理由: 実体が適用先・倍率・判定条件を束ねた規則であるため))

これはオーナーが不合格にした実例集の「試験の許容差の当て方方」→「試験の許容差の適用規則」の改称そのものであり、用語定義の側では改称が確定している。しかし本区画の40ファイルの識別子は、改称前の`当て方`のまま残っている。「当て方」は会話では「どう当てるか」としか読めず、実体(適用先・倍率・判定条件を束ねた規則)を予告しない。この語だけでは受理条件7(直訳・不透明語の検出)と条件3(5W1H)の両方に落ちる。用語定義ファイル自身が実体を理由に改称した語であるため、1語1義(条件6)の観点でも、定義側の正本の語と食い違ったままの識別子である。

以下、`当て方`を含む識別子が現れる箇所を全部挙げる(圧縮でパターンが繰り返されているが、省略せず全件を書く)。

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| solver_gauge.rs:19 | `試験の許容差の当て方`(型のインポート) | 3・6・7 | 「当て方」だけでは適用先・倍率・判定条件を束ねた規則という実体を予告しない。定義ファイルは既に「適用規則」へ改称済み | `試験の許容差の適用規則` |
| solver_gauge.rs:27 | フィールド`許容差の当て方: 試験の許容差の当て方,` | 3・6・7 | 同上 | `許容差の適用規則: 試験の許容差の適用規則,` |
| solver_gauge.rs:43 | `許容差の当て方: 試験の許容差の当て方::本番と同じ当て方(),` | 3・6・7 | 同上。関連関数名`本番と同じ当て方()`も同じ語を含む | `試験の許容差の適用規則::本番と同じ規則()` |
| solver_gauge.rs:100-101 | メソッド`許容差の当て方を差し替える`、引数`当て方: 試験の許容差の当て方` | 3・6・7 | 同上 | `許容差の適用規則を差し替える(規則: 試験の許容差の適用規則)` |
| stacked_slope_cone_record.rs:20,28,33,57-64 | 型`試験の許容差の当て方`、関数`一つの当て方を綴る`、引数`当て方`、関連関数`本番と同じ当て方()`・`倍率と行への当て方から生成する(...)` | 3・6・7 | 同上。「一つの当て方を綴る」は「1件の適用規則を綴る」の意だが「当て方」の圧縮のため、規則が何を束ねているか名前から読めない | `一つの適用規則を綴る`、`試験の許容差の適用規則::...` |
| stacked_slope_probe.rs:16,29,34,45(?),48,53,61-62,108-113 | 同上のパターン(型・関数名・引数名・関連関数名) | 3・6・7 | 同上 | 同上 |
| stacked_slope_record.rs:13-14,29,34,53,61-62,65-70 | 同上のパターン | 3・6・7 | 同上 | 同上 |
| stacked_slope_tests.rs:16,45-46 | `試験の許容差の当て方`、`許容差の当て方を差し替える` | 3・6・7 | 同上 | 同上 |

### 違反2: ASCIIモジュール直下の自由関数`一人を組む`が単独で答えられない

`pipeline_body_rows.rs:41`

```rust
fn 一人を組む(予測: Option<予測の状態>, 台帳剛体: &剛体) -> 姿勢自由度の参加者 {
```

この関数はASCII名のファイル(`pipeline_body_rows.rs`)直下のprivate自由関数であり、レシーバも日本語型名の関連関数でもないため、受理条件2により名前単独で答える必要がある。しかし「一人を組む」は「誰の・何を組むか」(戻り値の`姿勢自由度の参加者`)が名前に無く、「これは何をする処理?」への答えとして単独で成立しない(「一人」が剛体を指すのか参加者を指すのかも曖昧)。

- 落ちた条件: 2・3
- 改名の素案: `姿勢自由度の参加者を一体ぶん組む`

### 違反3: `side_by_side_fixture.rs:48`の自由関数`登録する`が裸の動詞

```rust
fn 登録する(
    台帳: &mut 剛体の台帳,
    工程: &mut 剛体の接触の一刻みの工程,
    半分の長さ: 直方体の軸ごとの半分の長さ,
    質量: f32,
    重心: 位置<ワールド>,
    種別: 運動種別,
) -> 剛体の識別子 {
```

ASCII名ファイル直下のprivate自由関数であり、レシーバによる補完が効かない。名前は「登録する」のみで、何を・どこへ登録するかが無い(実体は剛体を台帳と工程の両方へ登録する)。同じ区画の他のfixtureファイル(`stacked_slope_fixture.rs`の`床を置く`・`箱を置く`、`pipeline_fixture.rs`の`一辺1メートルの箱を台帳へ登録する`)はいずれも目的語と着点を書いており、この関数だけが受理条件5の禁止する裸の動詞形になっている。

- 落ちた条件: 2・5
- 改名の素案: `剛体を台帳と工程へ登録する`

## 違反の疑い(断定しない)

- `acceptance_breach_reading.rs:20`のフィールド`比: f32`、および`:15`の定数`比の群の境`。姉妹ファイル`acceptance_tally.rs`は同種の値を`残差と許容差の比の最大`と完全な語で命名しているのに対し、こちらは「比」1語へ圧縮されており、何と何の比かが名前だけからは読めない(型のドキュメントコメントを読めば分かる)。同じ概念に対して区画内で命名の粒度が割れている点が気になるため、断定はせず疑いとして挙げる。落ちうる条件: 3・8。改名の素案: `残差と許容差の比`

## 違反ゼロと確認した範囲

上記3件(+疑い1件)を除く、40ファイルの残り全部の識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は受理条件8つを満たした。特に次の点を確認した。

- ASCII・ギリシャ文字・数学記号を識別子に混ぜた例は無い
- 漢字1文字への圧縮(順4・段2型)は無い
- カタカナ語は「テスト」「バッチ」等、小学生が分かる語のみで業界カタカナ語は無い
- `生成する`・`組む`等の裸の動詞は、値オブジェクト様式の関連関数(型名が目的語を担う)か、目的語を伴う自由関数としてのみ使われている(上記違反3を除く)
- フィールド・列挙の枝の間にコメントだけの行を挟む違反は無い(命名検査の対象外だが目視で確認)
