# ローラー検査(命名): 区画022 crates/blitz_asset_compiler/examples/part_assembly ほか

- 区画名と範囲: `crates/blitz_asset_compiler/examples/part_assembly`(18ファイル)、`crates/blitz_asset_compiler/examples/part_catalog`(2ファイル)、`crates/blitz_asset_compiler/examples/watch_assets`(2ファイル)。合計22ファイル
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, db815090時点)。このツリーは編集していない
- 直読したファイル数: 22/22(grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(この区画に固有の登録語は無かった。`共有バッファ`項目が`blitz_asset_compiler`を触れるのみ)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_asset_compiler/examples/part_assembly/part_boxes.rs:35 | `番号で写した箱を引く` | 条件7(直訳の検出) | 「引く」はlookupの直訳調として受理条件が名指しで禁止する語そのもの(グローバルCLAUDE.md「日本語命名の質」の失敗例に`引く`が明記されている)。英語のlookupを知らない読み手は「引く」から「索引で探し出す」を復元できない | `番号に対応する箱を求める` または `番号で写した箱を参照する` |
| crates/blitz_asset_compiler/examples/part_assembly/comparison.rs:72 | `正解表の余りを数える` | 条件1(受理テスト。実体との不一致) | 名前は「数える」であり計数(カウンタの加算や列挙)を予告するが、実装は`据えた件数 < 正解の件数`のときに食い違いの1行を`push`するだけの条件付き記録処理であり、何も数えていない(比較対象の`姿勢1件を数える`は実際に`一致した件数 += 1`を行い名前と一致するが、この関数は一致しない)。定義を開いて実体と照合して初めて分かる不一致であり、`試験の許容差の当て方`の実例と同型 | `正解表の余りを食い違いとして記録する` |
| crates/blitz_asset_compiler/examples/part_assembly/tavern_recipe.rs:34 | `積む` | 条件3(5W1Hの必要格) | ASCIIモジュール(`tavern_recipe.rs`)直下のprivate自由関数で、レシーバも型名補完も無い。名前単独では「何を積むのか」「何を作るのか(接合の指示を生成する処理であること)」が answer できない。呼び出し側`積む(0, "上面", ...)`も位置引数だけで、名前が読み手に運ぶ情報が「積む」という動作だけになる | `積む指示を作る`(同クレート内の`frame_bay_instruction.rs`が使う`〜指示を作る`の命名規約に合わせる) |
| crates/blitz_asset_compiler/examples/part_assembly/tavern_recipe.rs:39 | `付ける` | 条件1・3(受理テスト、5W1H) | 「付ける」は日本語として動作対象を問わない汎用動詞で、「これは何?」に単独で答えられない(何を何へ付けるのかが名前に無い)。実装は接合の指示を1件作るだけであり、`名前`だけでは「差込の接合指示を作る処理」だと復元できない | `差し込む指示を作る` |
| crates/blitz_asset_compiler/examples/part_assembly/tavern_recipe.rs:65 | `名前` | 条件4(品詞の規律) | 実体は綴りを検証して`接合点名`を組み立てる処理(失敗しうる`Result`を返す)であるにもかかわらず、名前が単なる名詞「名前」になっている。「記録」フィールドが処理なのに名詞形だった過去の不合格例と同型の品詞の偽装 | `綴りから接合点名を作る` |
| crates/blitz_asset_compiler/examples/part_assembly/tavern_recipe.rs:69 | `識別子` | 条件4(品詞の規律) | 同上。実体は綴りを検証して`部品ID`を組み立てる処理だが、名前は名詞「識別子」のみ | `綴りから部品idを作る` |
| crates/blitz_asset_compiler/examples/part_assembly/tree_recipe.rs:36 | `継ぐ` | 条件3(5W1Hの必要格) | `積む`と同型。ASCIIモジュール直下のprivate自由関数で、名前単独では「何を継いで何を作るのか(接合の指示を生成する処理であること)」が読み取れない | `継ぐ指示を作る` |
| crates/blitz_asset_compiler/examples/part_assembly/tree_recipe.rs:47 | `名前` | 条件4(品詞の規律) | tavern_recipe.rs:65と同じ問題。同じ処理が別ファイルで同じ名詞1語のまま繰り返されている | `綴りから接合点名を作る` |
| crates/blitz_asset_compiler/examples/part_assembly/tree_recipe.rs:51 | `識別子` | 条件4(品詞の規律) | tavern_recipe.rs:69と同じ問題 | `綴りから部品idを作る` |
| crates/blitz_asset_compiler/examples/watch_assets/main.rs:22 | `実行する` | 条件5(裸の動詞の禁止) | 受理条件が禁止語の実例として名指ししている`実行する`そのもの。ASCIIモジュール(`main.rs`)直下の自由関数で引数も無く、型名やレシーバによる補完が一切効かない。実装は「引数を読み、監視状態を読み込み、変更をループで待って再コンパイルする」という具体的な仕事であり、名前がそれを何も語っていない | `アセットの変更を監視して再コンパイルし続ける` |

## 違反の疑い

| ファイル:行 | 識別子 | 落ちた条件(疑い) | 理由 |
|---|---|---|---|
| crates/blitz_asset_compiler/examples/watch_assets/state.rs:10 | `一覧`(構造体`監視状態`の唯一のフィールド) | 条件3(5W1Hの必要格) | 実体は「ソース依存パスと、そのファイルの更新時刻」の対の列であり、「一覧」だけでは何の一覧か読み取れない。ただしフィールドが1つしか無く`self.一覧`という形でしか現れないため、読み違いの実害は小さいと判断し断定はしない。改名するなら`ソース依存の更新時刻一覧` |

## 違反ゼロの範囲

上記10件の違反と1件の疑いを除く、全22ファイルの残りの識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は受理条件8つ、日本語命名の質、呼称と語の選択のいずれにも抵触しなかった。特に`frame_bay_face.rs`・`frame_bay_instruction.rs`・`frame_bay_spelling.rs`・`frame_chimney_instruction.rs`・`frame_recipe.rs`・`frame_shared_layout.rs`・`frame_wall_ornament_instruction.rs`・`assignment.rs`・`pose_difference.rs`・`tolerance.rs`・`recipe_choice.rs`・`report.rs`(part_assembly側)・`part_catalog/main.rs`・`part_catalog/report.rs`は、動詞句には目的語が、値には名詞句が一貫して与えられており、「〜の指示を作る」「〜を求める」「〜を測る」のような型に忠実な命名が徹底されていた。ASCII混入・ギリシャ文字混入・記号混入は22ファイルのいずれにも見つからなかった(`部品id`のような小文字の英語略語混在は`ID`が非専門家にも通用する語である点と、型名`部品ID`とRustの関数命名慣習(スネークケース)の差でしかない点から違反に数えていない)。
