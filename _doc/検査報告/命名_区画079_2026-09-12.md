# 命名検査報告: 区画079 crates/blitz_render/src/atmosphere/quantity ほか

- 区画番号: 079
- 対象範囲: `crates/blitz_render/src/atmosphere/quantity`(16ファイル)・`crates/blitz_render/src/atmosphere/supply`(2ファイル)・`crates/blitz_render/src/atmosphere/table`(4ファイル)。合計22ファイル
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller` の main、コミット db815090(このツリーは編集していない)
- 直読したファイル数: 22/22(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 参照した必読文書: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「専門用語の表記」「呼称と語の選択」「日本語命名の質」「日本語命名の受理条件」、`CLAUDE.md`の「命名」節、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_render/src/atmosphere/table/bilinear.rs:11` | `添字と比`(自由関数) | 条件4(品詞の規律)・条件1(受理テスト) | この関数は双一次補間の添字と混ぜ具合を計算する処理(処理=動詞句「〜する」が必須)だが、名前が名詞の並びで止まっており「〜する処理です」への答えとして成立しない。ASCIIモジュール`bilinear.rs`直下の自由関数のため文脈補完もゼロである | `添字と比を求める` |
| `crates/blitz_render/src/atmosphere/table/bilinear.rs:20` | `混ぜる`(自由関数) | 条件3(5W1Hの必要格)・条件2(ASCII直下の補完ゼロ) | 「何を」混ぜるかが名前に無い。呼び出し側は `bilinear::混ぜる(a, b, c, d, 横比, 縦比)` の形で位置引数だけを見るため、名前単独では四隅の値を混ぜていることが分からない。ASCIIモジュール直下の自由関数のため引数名にも頼れない | `四隅の値を比で混ぜる` |
| `crates/blitz_render/src/atmosphere/table/multiscatter_table.rs:50` | `テクセル`(privateメソッド) | 条件4(品詞の規律) | 境界チェックとpanicを行う処理(処理)なのに名前が名詞1語で止まっている。値=名詞句/処理=動詞句の規律に反する | `テクセルを参照する` |
| `crates/blitz_render/src/atmosphere/table/skyview_table.rs:62` | `テクセル`(privateメソッド) | 条件4(品詞の規律) | 同上(同一パターンが3ファイルで反復している) | `テクセルを参照する` |
| `crates/blitz_render/src/atmosphere/table/transmittance_table.rs:44` | `テクセル`(privateメソッド) | 条件4(品詞の規律) | 同上 | `テクセルを参照する` |
| `crates/blitz_render/src/atmosphere/quantity/normalized_density.rs:21` | `検証済みの値から`(関連関数) | 条件4(品詞の規律) | 生成の処理(処理)なのに動詞「生成する」に相当する語尾が無く、名詞句「〜から」で止まっている。型名が目的語を担う条件5の適用対象は動詞「生成する」自体がある場合であり、動詞そのものが欠けた本例には及ばない | `検証済みの値から生成する` |
| `crates/blitz_render/src/atmosphere/quantity/phase_value.rs:20` | `検証済みの値から`(関連関数) | 条件4(品詞の規律) | 同上(同一パターンが4ファイルで反復している) | `検証済みの値から生成する` |
| `crates/blitz_render/src/atmosphere/quantity/zenith_cosine.rs:20` | `検証済みの値から`(関連関数) | 条件4(品詞の規律) | 同上 | `検証済みの値から生成する` |
| `crates/blitz_render/src/atmosphere/quantity/transmittance_rgb.rs:25` | `検証済みの成分から`(関連関数) | 条件4(品詞の規律) | 同上 | `検証済みの成分から生成する` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 疑いの内容 |
|---|---|---|---|
| `crates/blitz_render/src/atmosphere/quantity/sample_count.rs:10,20` | `積分標本数`(型)と`区間数`(アクセサ) | 条件6・条件7(実体との照合) | 型のコメントは「台形則で経路を刻む区間の数」であり実体は区間数(n)だが、型名は「標本数」(標本点はn+1個)を名乗る。アクセサ名`区間数`は実体と一致するが、型名`積分標本数`だけを読むと標本点の数だと誤読しうる。ただし`_doc/計画/ユビキタス言語.md`の「積分標本数」の登録定義が「何区間に刻むかの指定」とすでに明記しており、プロジェクトが認めた語である可能性があるため断定しない |
| `crates/blitz_render/src/atmosphere/table/bilinear.rs:11`、`skyview_table.rs:49`、`transmittance_table.rs:31` | 引数名 `uv`・`横uv`・`縦uv` | ASCII混入の検査項目 | 日本語識別子の中にASCIIの英字略語「uv」がそのまま混じっている。「UV」はグラフィックス分野の専門語であり小学生が分かる語ではないが、`_doc/計画/ユビキタス言語.md`に「UVのU方向」「地形のUV」等ですでに登録された語であるため、既存合意語として許容される可能性がある。断定は避け、指摘に留める |
| `crates/blitz_render/src/atmosphere/supply/sky_radiance.rs:66` | private `視線の放射輝度を求める` | 条件6(1語1義) | 公開メソッド`ワールドの視線の放射輝度を求める`・`太陽相対の視線の放射輝度を求める`と語幹が同じで、私有版だけ限定語(ワールドの/太陽相対の)が無い。ただし引数の型`スカイビューのベイク済み画像の視線`(日本語型名)が文脈を補うため受理条件2の範囲内であり、違反とまでは言えない |

## 違反ゼロの範囲

以下のファイルは日本語識別子(型・関連関数・メソッド・フィールド・変数・定数)を全数点検し、受理条件8つ・命名の質・呼称と語の選択のいずれにも違反が見つからなかった:
`aerial_voxel.rs`・`albedo_rgb.rs`・`asymmetry.rs`・`attenuating_component.rs`・`azimuth_cosine.rs`・`extinction_rgb.rs`・`multiscatter_rgb.rs`・`scattering_cosine.rs`・`sky_radiance_rgb.rs`・`skyview_rgb.rs`・`unit_direction.rs`・`radiance_scale.rs`。
これらの型名・フィールド名は`_doc/計画/ユビキタス言語.md`の登録語(消散係数RGB・位相非対称係数・減衰成分・多重散乱RGB・散乱角余弦・地表反射率RGB等)とそのまま一致し、生成関数`生成する()`はすべて型名が目的語を担う値オブジェクト様式(条件5)に適合する。
