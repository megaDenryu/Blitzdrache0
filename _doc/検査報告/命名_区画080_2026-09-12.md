# 命名検査報告(区画080: crates/blitz_render/src/cascade)

## 範囲・時点

- 区画: `crates/blitz_render/src/cascade`(区画番号080、23ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、コミット `db815090` 時点。このツリーは編集していない)
- 対象ファイル一覧: `block_080.txt` に記載の23件全部

## 検査件数

- 直読したファイル数: 23 / 23(全数直読、grepだけで済ませたファイルは0件)
- 検査した日本語識別子: 型・列挙・構造体フィールド・関数・メソッド・定数・変数を合わせて約140件(具体は下の違反・疑いの列挙と、違反ゼロの節に列挙した範囲で構成する)

## 違反の一覧

1. `crates/blitz_render/src/cascade/build.rs:16` | `組み立てる`(`pub(crate) fn 組み立てる(...)`) | 条2・条3・条5 | ASCIIモジュール`build.rs`直下の自由関数で、レシーバも型名による補完も無い。名前が動詞のみで目的語が無く、「何を組み立てるか」が単独で読めない(戻り値`多段一式`は名前に出ていない)。受理条件が明示する`create::生成する`と同型の違反。 | 改名案: `多段一式を組み立てる`
2. `crates/blitz_render/src/cascade/split.rs:18` | `分割する`(`pub(super) fn 分割する(近深度, 遠深度, 設定) -> 分割結果`) | 条2・条3・条5 | ASCIIモジュール`split.rs`直下の自由関数。呼び出し側の引数名(近深度・遠深度・設定)は受理条件が認める補完源(型名・レシーバ)に当たらないため、名前単独では「何を分割するか」が読めない(視錐台の深度区間を距離区分へ割るという実体が名前に無い)。 | 改名案: `視錐台の深度区間を距離区分へ分割する`
3. `crates/blitz_render/src/cascade/snap.rs:57` | `位置から生成する`(`fn 位置から生成する(ベクトル: DVec3) -> 大域ワールド位置`) | 条1・条3 | 「〜から生成する」は日本語として「位置を材料にして何かを作る」と読めるが、実際の引数は`ベクトル`であり、生成される側が`位置`(大域ワールド位置)である。入力と出力が名前の中で逆転しており、単独では復元できない。 | 改名案: `ベクトルから大域ワールド位置を生成する`
4. `crates/blitz_render/src/cascade/camera_frustum.rs:84` | `中心`(`fn 中心(隅一覧: &[Vec3; 4]) -> Vec3`) | 条4 | 4隅の平均座標を計算する処理(値を求める関数)なのに、名前が結果の値を指す名詞1語になっている。呼び出し箇所を見ないと「中心という値を返すフィールド」なのか「中心を計算する処理」なのか区別できない。 | 改名案: `中心を求める`
5. `crates/blitz_render/src/cascade/cascade_tests/snap_tests.rs:71` | `テクセル座標`(`fn テクセル座標(一式, 番号, 相対フレーム原点, 静止物) -> [f64; 2]`) | 条4 | 光源クリップ座標をテクセル単位へ変換する処理なのに名詞形。値を保持するフィールドではなく毎回計算する関数であるため、動詞句であるべき。 | 改名案: `テクセル座標を求める`
6. `crates/blitz_render/src/cascade/cascade_tests/snap_tests.rs:85` | `光空間の成分`(`fn 光空間の成分(位置, 基準, 光の向き) -> [f64; 3]`) | 条4 | 光空間の3軸へ射影する処理なのに名詞形。5番と同型の品詞偽装。 | 改名案: `光空間の成分を求める`
7. `crates/blitz_render/src/cascade/cascade_tests.rs:15` | `m`(`fn m(値: f32) -> メートル`) | 条1(受理テスト)+日本語命名の原則(CLAUDE.md「命名」節「やってることをそのまま日本語で書く」) | 単独のASCII1文字。「これは何?」に単独で答えられず、日本語の識別子の中に英字1文字の裸名が混じっている。テストヘルパーとして全ファイルから使われ続けるため影響範囲が広い。 | 改名案: `メートルにする`

## 違反の疑い(断定しない)

1. `crates/blitz_render/src/cascade/split.rs:57` | `実用分割で求める`(`fn 実用分割で求める(近深度, 遠深度, 混合率) -> [メートル; N]`) | 条3への疑い | 「実用分割で(手段)+求める(動詞)」はあるが、求める対象(分割深度)が省略されている。呼び出し元`分割深度を求める`から見れば紛れないが、この関数名だけを読むと「何を求めるか」が復元しにくい。
2. `crates/blitz_render/src/cascade/split.rs:76` | `視錐台へ収める`(`fn 視錐台へ収める(境界, 近深度, 遠深度) -> [メートル; N]`) | 条3への疑い | 「境界を視錐台へ収める」の「境界を」(目的語)が省略されている。着点(視錐台へ)はあるが主目的語が無く、1番の`分割する`ほどではないが同系統の欠落。
3. `crates/blitz_render/src/cascade/band_mask.rs:47` | `ビット(番号)`(`fn ビット(番号: 距離区分番号) -> u8`、型`距離区分マスク`の関連関数) | 条3への疑い | 型名`距離区分マスク`による補完で「マスクのビット」と読めなくはないが、実体は「番号に対応するビット位置」であり、名前だけでは番号との対応が読み取りにくい。値オブジェクト様式の`生成する()`ほど型名が目的語を明確に担ってはいない。
4. `crates/blitz_render/src/cascade/camera_frustum.rs:13` | `半径比`(構造体`視錐台の軸`のフィールド) | 条3への疑い | 「視軸からの隅の距離を深度で割った比」という定義は直上のドキュメンテーションコメントで補っており、名前単独では「何と何の比か」が完全には復元できない。ドキュメント込みで読めば実体と一致するため、断定はしない。

## 違反ゼロと確認した範囲

- `band.rs`(距離区分番号・全距離区分・添字・シャドウパス名・次の距離区分・距離区分の区間・近深度・遠深度)
- `band_assignment.rs`(距離区分の割り当て・生成する・既定・最大影距離を差し替える・明示の境界で分ける・最大影距離・分け方 ―`_doc/計画/ユビキタス言語.md`の「距離区分の割り当て」「距離区分の分け方」の定義と一致を確認した)
- `band_mask.rs`(距離区分マスク・空・全距離区分・距離区分を立てる・立っているか・空か・ビット列。「ビット」のみ上の疑いへ計上)
- `blend.rs`(距離区分のブレンド・主距離区分・副距離区分・主重み・距離区分を選ぶ)
- `bounding.rs`(距離区分の包囲球・中心・半径・包囲球を求める)
- `build.rs`(多段設定・組み立てる以外の識別子。組み立てるのみ違反へ計上)
- `camera_frustum.rs`(視錐台の軸・前方・近深度・遠深度・カメラ視錐台・近隅一覧・遠隅一覧・復元する・軸・スライスの隅。中心のみ違反、半径比のみ疑いへ計上)
- `cascade_set.rs`(多段一式・距離区分のライトビュー射影・区間・分割深度・遷移幅・隅一覧・解像度密度・解像度・カメラ前方)
- `diagnostic.rs`(受光距離帯の幅メートル・受光距離帯数・距離区分の可視化の番号・影の欠落計器の番号・画素診断・出さない・距離区分の可視化・影の欠落計器・番号)
- `explicit_boundaries.rs`(明示の距離区分境界・生成する・距離一覧・最も遠い境界 ― ユビキタス言語の「明示の距離区分境界」と一致を確認した)
- `ortho.rs`(近クリップ・正射影を作る)
- `ratio.rs`(実用分割混合率・距離区分の重なり率・生成する・値・既定 ― ユビキタス言語の「実用分割」と一致を確認した)
- `resolution.rs`(影の一辺解像度・既定のテクセル数・下限のテクセル数・上限のテクセル数・生成する・既定・テクセル数・実数のテクセル数・テクセル尺度、テスト内の識別子全部)
- `settings.rs`(多段設定・生成する・既定・割り当てを差し替える・キャスター余白を差し替える・解像度を差し替える・割り当て・最大影距離・分け方・重なり率・キャスター余白・解像度)
- `snap.rs`(光空間の格子へ寄せる・光空間の基底・ベクトルにする ―位置から生成するのみ違反へ計上)
- `split.rs`(分割結果・分割深度・遷移幅・距離区分の区間一覧・分割深度を求める ―分割する・実用分割で求める・視錐台へ収めるのみ違反または疑いへ計上)
- `split_policy.rs`(距離区分の分け方・実用分割・明示の境界 ― ユビキタス言語の「距離区分の分け方」と一致を確認した)
- `texel_density.rs`(影の解像度密度・生成する・距離区分ごとの世界メートル毎テクセル)
- `cascade_tests.rs`(方向光・ビュー射影・大域 ―mのみ違反へ計上)
- `cascade_tests/explicit_split_tests.rs`(大規模世界の設定、および全テスト関数名)
- `cascade_tests/ortho_tests.rs`(全識別子・全テスト関数名)
- `cascade_tests/snap_tests.rs`(テクセル座標の許容差・静止物一覧 ―テクセル座標・光空間の成分のみ違反へ計上)
- `cascade_tests/split_tests.rs`(全識別子・全テスト関数名)

ASCII・ギリシャ文字・数学記号の混入は、`cascade_tests.rs`の`m`(違反7)以外に検出しなかった。
