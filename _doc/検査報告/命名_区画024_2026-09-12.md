# 命名検査報告: 区画024

- 区画: `crates/blitz_asset_compiler/src/assembly_reference`(区画番号024、40ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(このツリーは編集していない)
- 時点: main の `db815090`
- 対象一覧: `block_024.txt` に列挙された40ファイル

## 直読数・省略の有無

- 直読したファイル数: 40 / 40(全ファイルを最後まで直読した)
- grepだけで済ませたファイル数: 0(全ファイルの本文を直読して検査した。grepは「引く」の用例確認と、エラー生成の私有関数の命名慣行を同一クレート内で突き合わせる目的で補助的に使っただけで、検査対象の識別子の読み取りには使っていない)
- 「パターンが一貫しているので残りは省略した」は行っていない

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_asset_compiler/src/chunk_directory_source/entry.rs:49` | `行不正`(private free function、`fn 行不正(行番号: usize, 行: &str) -> アセットコンパイルエラー`) | 条件4(品詞の規律) | この識別子は関数(処理)だが、名前が「行が不正である」という状態を述べる名詞句のままで、動詞句になっていない。受理テスト「〜する処理です」に「行不正する処理です」は成立しない。同じクレート・同じ役割(私有のエラー生成ヘルパー)の兄弟である`building_grid_source/opening_source.rs`の`欄が不正なエラーを作る`、`building_grid_source/grid_file.rs`の`解釈できないエラーを作る`・`書き出せないエラーを作る`、`building_grid_source/source.rs`の`升目が成り立たないエラーを作る`はいずれも「〜エラーを作る」という動詞句で統一されており、`行不正`だけがこの型から外れている | `行不正のエラーを作る` |

## 違反の疑い

なし。検討したが見送った点を記録する。

- `部品で全件引く`(`assembly_reference/entry.rs:40`)、`識別子で引く`・`格子ソースを識別子で引く`(`building_grid_source/ledger.rs:52,57`): グローバルCLAUDE.mdの受理条件7は「lookup→引く」を直訳の失敗例として明示するが、`_doc/計画/ユビキタス言語.md`の複数の登録語(「候補の並びから種で引く形」「名前で引く経路」「表面物性を引くための番号」「点から…を引く表」等、136・455・522・1013・1066行)が、鍵から値を取り出す操作全般に「引く」を定着した日本語として既に使っている。これは「定着した比喩は用語定義があれば使ってよい」の対象であり、このプロジェクトでは「引く」がLUTのサンプリングに限らず索引操作一般の登録語として定着していると判断し、違反に数えなかった
- `Jsonを解釈できない`・`Jsonを組み立てられない`(`building_grid_source/error.rs`)、`正解表のJSONを解けない`(`assembly_reference/error.rs:11`): 頭字語の混入に見えるが、`_doc/計画/ユビキタス言語.md`が説明文中で「JSON」を注釈なしに多用しており(717・726・727・734・735・739・770行)、このプロジェクトではAPI・CPU等と同列の一般に通用する語として扱われている。したがって条件の「日本語の頭字語混入禁止」には抵触しないと判断した。ただし`JSON`(全角英大文字)と`Json`(先頭のみ大文字)で綴りが割れている点は、命名の受理条件でなく表記の不統一であり、この検査の対象外として記録だけする
- `座標系がglTFでない`(`assembly_reference/error.rs:20`)ほか複数箇所の`glTF`: ASCII文字の混入だが、glTFは3Dモデル交換形式の固有名詞であり翻訳できないため、Vulkan・ash等と同じ「翻訳不能な固有名詞は英語のまま扱う」慣行の範囲内と判断した

## 違反ゼロの範囲

上記1件の識別子(`行不正`)を除く、区画024の全40ファイルの型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝(数百識別子規模)は、日本語命名の受理条件8つ・呼称と語の選択・日本語命名の質のいずれにも抵触しなかった。特に、値オブジェクト様式の`生成する()`/`作る()`はすべて型名が目的語を担う形で使われており、パス・テキスト・名前を裸のstd型でなくnewtypeで包む規律(プリミティブ執着禁止の拡張)も一貫して守られていた。
