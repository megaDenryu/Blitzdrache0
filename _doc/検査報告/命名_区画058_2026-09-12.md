# 命名検査報告: 区画058

- 区画: `crates/blitz_engine/src/chunk` 他(一覧ファイルの実体は `chunk` 単体でなく、`chunk`・`frame_composition`・`height_field`・`instance_lod`・`instance_lod_tests`・`lighting`・`local_visibility` の7ディレクトリ計40ファイル。区画名は台帳上の代表名であり、実際の走査範囲は `block_058.txt` に列挙された40ファイルである)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、`main` の `db815090`(このツリーは編集していない)
- 直読したファイル数: 40 / 40(grepだけで済ませたファイルは0件。全ファイルを全文直読した)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(該当箇所をgrepで照合)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_engine/src/instance_lod/error.rs:6` | `個体LODエラー` | 日本語命名の質(頭字語・省略語を日本語へ混ぜない)/受理条件6(1語1義) | `LOD`は非専門家が読めない英語の頭字語であり、質の節が挙げるLUT等と同じ扱いを要る。同じ区画内の`level.rs`が既にこの概念を`個体詳細段`という日本語へ訳して`_doc/計画/ユビキタス言語.md`へ登録済みであるにもかかわらず、`error.rs`と`settings.rs`だけ英語の頭字語のまま残っており、同じ概念が2つの綴りを持っている | `個体詳細段エラー` |
| `crates/blitz_engine/src/instance_lod/settings.rs:11` | `個体LOD選択設定` | 日本語命名の質(頭字語混入禁止)/受理条件6(1語1義) | 同上。`個体詳細段`という訳語が既に在るのに、この型と`個体LODエラー`だけが英語の頭字語を残し、読み手は「LODと詳細段は同じものか」を型定義まで遡らないと確認できない | `個体詳細段選択設定` |

`個体LODエラー`・`個体LOD選択設定`は`instance_lod_tests/fixture.rs`・`instance_lod_tests/independence.rs`からも参照されているため、直せば波及先も一致する(直読した限りでは呼び出し側の綴りが上記2つの型名をそのまま使っているだけで、追加の頭字語混入は無い)。

## 違反の疑い

- `crates/blitz_engine/src/instance_lod/error.rs` の`個体LODエラー`と`crates/blitz_engine/src/height_field/*`一帯の判断は、`_doc/計画/ユビキタス言語.md`が`LOD`自体を用語集の見出し語として定義している(215行目)こととどう折り合うかに依存する。用語集にある語だから許容という読み方も可能だが、「日本語命名の質」の条文がLUT等の定義済み専門語すら訳語へ置換すると明記しているため、今回は違反側で報告した。断定はリネーム員と親に委ねる

## 違反ゼロの範囲

- 上記2件を除く38ファイル・数百件の識別子(型名・列挙の枝・メソッド名・フィールド名・変数名・定数)は、受理条件8つ・質の3禁則(頭字語混入・直訳調・膠着圧縮)・呼称の語選択・ASCII/ギリシャ文字/数学記号の混入のいずれにも抵触しなかった。特に`chunk_coordinate.rs`の`チャンク座標`、`height_field`配下の`カプセルと地表の接触`・`世界の地表の面`・`高さ場の読み口`・`地表の三角形の所在`等は、いずれもユビキタス言語.md(212・645・796・842行付近)の定義語と一致し、フィールド・メソッドの格(何を+どうする、着点、材料)も揃っていた。`lighting`・`local_visibility`・`frame_composition`配下も同様に、値=名詞句/処理=動詞句/真偽=「〜か」の品詞規律を満たしていた
