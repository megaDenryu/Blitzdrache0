# 命名検査報告: 区画086

- 区画名: crates/blitz_render/src/gpu_pass_timing 他(indirect_lighting・lighting_input 一式)
- 範囲: block_086.txt に列挙された23ファイル
- 作業ツリー: C:/devs/Blitzdrache0_roller(main の db815090、編集していない)
- 直読したファイル数: 23/23(全ファイルを直読。grepだけで済ませたファイルは0件)
- 参照した必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(grepで該当語を照合)

## 違反の一覧

1. `crates/blitz_render/src/indirect_lighting/bake_plan.rs:11-13` | フィールド `遠方環境用スカイビューを焼く` `遠方環境を焼く` `反射率積分表を焼く`(いずれも`bool`) | 落ちた条件: 4(品詞の規律) | なぜ読めないか: 受理条件4は「真偽=「〜か」」を要求する。この3フィールドは真偽値なのに「〜を焼く」という裸の動詞句で、宣言だけを見ると処理(焼く動作そのもの)を保持するフィールドに見える。同じ型が公開する読み出しメソッド`遠方環境用スカイビューを焼くか()`・`遠方環境を焼くか()`・`反射率積分表を焼くか()`は正しく「か」を付けており、フィールド名だけがこの型の中で作法を破っている。兄弟型`焼き始めの記録`(bake_record.rs)のフィールド`遠方環境を焼いたか`・`反射率積分表を焼いたか`は「か」を付けており、同じ間接照明の焼き上げ領域の中で命名規則が一致していない | 改名の素案: `遠方環境用スカイビューを焼くか` `遠方環境を焼くか` `反射率積分表を焼くか`(自身のアクセサと同じ形に揃える)
2. `crates/blitz_render/src/indirect_lighting/bake_plan.rs:33`(関数`組み立てる`の仮引数) | `遠方環境用スカイビューを焼く: bool, 遠方環境を焼く: bool, 反射率積分表を焼く: bool` | 落ちた条件: 4(品詞の規律) | なぜ読めないか: 上と同じ理由。この関数の仮引数がそのままフィールドへ束縛されるため、引数の時点で真偽値が動詞句のまま渡っている | 改名の素案: フィールド名と揃えて`〜か`にする

## 違反の疑い(条件7・条件「日本語命名の質」の頭字語混入に対して)

1. `crates/blitz_render/src/indirect_lighting/contract.rs:19,27,52,58,63` | 列挙の枝 `遠方環境LambertGGX`(型`照明問い合わせ契約`) | 疑う条件: 日本語命名の質「英語の頭字語・省略語を日本語の中に混ぜない」、および識別子へのASCII混入の一律不合格方針 | なぜ読めないか: 「Lambert」(人名由来の反射モデル)と「GGX」(法線分布関数の略称)という2つの専門用語をローマ字のまま日本語の直後へ連結しており、その分野を知らない読み手はこの部分だけ意味を復元できない。ただし`_doc/計画/ユビキタス言語.md`312行目にこの識別子そのものが登録済みで、「拡散照度と鏡面畳込みで答える」という定義が明記されている。用語定義ファイルに事前登録された正本用語であり、質的な違反と登録済み用語である事実が両立するため断定しない
2. `crates/blitz_render/src/indirect_lighting/scene_contract_shaders/variant_stages.rs:12-13,18,20-21,24,29-31` および `crates/blitz_render/src/indirect_lighting/scene_contract_shaders.rs:30,32,50,52,55,57` | フィールド・引数 `標準金属粗さpbr`(小文字)、列挙の枝(材質変種キー、参照のみ)`標準金属粗さPBRの不透明両面`(大文字) | 疑う条件: 上と同じ(頭字語混入)。加えて大文字`PBR`と小文字`pbr`が同一概念に対して混在している | なぜ読めないか: 「PBR」(Physically Based Rendering)は`_doc/計画/ユビキタス言語.md`32行目で定義済みの登録用語であり、命名の質が許す「一般に通用している語」の例示(UI・CPU・GPU・API・シェーダー)には入っていないが、プロジェクトの用語集がこの語を正本として採用し多数のファイルで使い続けている。大文字PBR(型名・列挙の枝)と小文字pbr(snake_case のフィールド・引数)の食い違いはRustの命名規約(型はUpperCamel寄り、フィールドはsnake_case)から来る表記差であり、意味の二義化ではないと判断した

## 違反ゼロの範囲

上記2件(疑いを除く)以外の識別子(gpu_pass_timing/frame_sample.rs、indirect_lighting/bake_forecast.rs・bake_record.rs・contract_shaders.rs・error.rs・input_error.rs・layer_error.rs・scene_contract_shaders.rs 本体の型名とメソッド名、lighting_input 配下の cascade_assembly.rs・components.rs・directional_light.rs・local_lights.rs・point_light.rs・point_light_visibility.rs・shadow.rs・shadow_casting.rs、および local_lights/declaration_fixture.rs・local_lights_tests.rs・shadow_assignment.rs・shadow_assignment_tests.rs の型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は受理条件8つを通過した。特に真偽値のメソッド名(`〜か`)・値オブジェクトの`生成する()`(型名が目的語を担う形)・エラー型の枝(不正・不一致・なし等)は一貫して条件を満たしている。
