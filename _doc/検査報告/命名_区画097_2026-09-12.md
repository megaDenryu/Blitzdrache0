# 命名検査報告: 区画097「crates/blitz_render/src/vulkan/auto_exposure」ほか

## 範囲・時点

- 区画番号: 097
- 対象ディレクトリ群: `crates/blitz_render/src/vulkan/auto_exposure`・`bloom`・`cloth`・`cloth_shadow`・`cluster_light_assignment`・`depth`・`depth_injection`
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(このツリーは編集していない)
- 時点: main の `db815090`
- 対象ファイル一覧: `C:/Users/POKR30~1/AppData/Local/Temp/claude/C--devs-Blitzdrache0/612e37aa-a38d-4d29-a7a3-b0e9508d9f15/scratchpad/roller/block_097.txt`(38件)

## 検査した件数

- 直読したファイル数: 38 / 38(全件を直読した。grepだけで済ませたファイルは0件)
- 検査した識別子: 型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝を含め、38ファイルの宣言をすべて目視した

## 違反の一覧

### 分類A: 確立済みのカタカナ語(パイプライン・レイアウト・セット・プール・バッファ・サンプラー)を使わず、無翻訳の裸の英単語を日本語識別子へ膠着させた形

このリポジトリの同じファイル・同じクレート内で「レイアウト」「セット」「パイプライン」等のカタカナ表記が既に使われているにもかかわらず(例: `descriptor.rs`の`レイアウトのハンドル()`・`セットのハンドル()`、`pipelines.rs`の`集計レイアウト`)、同じ概念を指す別の識別子が英単語をそのまま接続している。受理条件2(英語ASCII識別子は補完力ゼロ)・条件6(1語1義。同じ語が2つの綴りを持ってはならない)・「日本語命名の質」条1(英語を日本語へ混ぜない)に落ちる。

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| auto_exposure/input.rs:13 | `集計pipeline` (フィールド) | 条件2・6 | 同じクレートの`pipelines.rs`は同じ概念を`集計`(型`vk::Pipeline`)で持ち、こちらだけ英語「pipeline」を貼り付けている。読み手はカタカナに揃うと期待するが揃わない | `集計パイプライン` |
| auto_exposure/input.rs:14 | `集計layout` | 条件2・6 | 同ファイル内で他の値は日本語だが、この語だけ英語のまま。`pipelines.rs`の`集計レイアウト`と綴りが食い違う | `集計レイアウト` |
| auto_exposure/input.rs:15 | `導出pipeline` | 条件2・6 | 同上 | `導出パイプライン` |
| auto_exposure/input.rs:16 | `導出layout` | 条件2・6 | 同上 | `導出レイアウト` |
| bloom/create.rs:21 | `単一読みlayout`・`二読みlayout` (ローカル変数) | 条件2・6 | 同じ関数内で`レイアウト2種を作る`・`セットレイアウトを確保する`は日本語なのに、受け取った変数名だけ英語が残る | `単一読みレイアウト`・`二読みレイアウト` |
| bloom/mod.rs:26 | `単一読みlayout` (フィールド、型`宣言から作ったセットレイアウト<1>`) | 条件2・6 | 型名自体は「セットレイアウト」という日本語なのに、保持するフィールド名だけ英語 | `単一読みレイアウト` |
| bloom/mod.rs:27 | `二読みlayout` | 条件2・6 | 同上 | `二読みレイアウト` |
| bloom/mod.rs:25 | `sampler` (フィールド、型`vk::Sampler`) | 条件2・6 | 同じ関数`create.rs`内で`線形サンプラーを作る()`と日本語のメソッド名で確保しているのに、受け皿のフィールド名だけ英語 | `サンプラー` |
| bloom/descriptor.rs:28 | `pool` (フィールド、型`vk::DescriptorPool`) | 条件2・6 | 同じ型の別フィールドはすべて日本語(`前処理set`除く)。「プール」はユビキタス言語にも登場する確立語 | `プール` |
| bloom/descriptor.rs:29 | `前処理set` (フィールド) | 条件2・6 | 同じメソッド`束縛するセットのハンドル()`は日本語「セット」を使うのに、フィールド名だけ英語 | `前処理セット` |
| bloom/descriptor.rs:30 | `縮小set一覧` | 条件2・6 | 同上 | `縮小セット一覧` |
| bloom/descriptor.rs:31 | `拡大set一覧` | 条件2・6 | 同上 | `拡大セット一覧` |
| bloom/descriptor/allocate.rs:14-15 | 引数`単一読みlayout`・`二読みlayout` | 条件2・6 | mod.rs由来のフィールド名をそのまま引数名へ持ち込み、同じ食い違いを広げている | `単一読みレイアウト`・`二読みレイアウト` |
| cloth/descriptor.rs:52 | `layout` (フィールド、型`宣言から作ったセットレイアウト<..>`) | 条件2・6 | 同じ構造体のメソッド`レイアウトのハンドル()`は日本語なのに保持フィールドは英語のまま | `レイアウト` |
| cloth/descriptor.rs:53 | `pool` (フィールド) | 条件2・6 | 同上 | `プール` |
| cloth/descriptor.rs:54 | `set一覧` (フィールド) | 条件2・6 | 同上 | `セット一覧` |
| cloth/pipelines.rs:17 | `layout` (フィールド、`布パイプライン群`) | 条件2・6 | 他の13フィールドはすべて日本語(`介入`・`積分`・`拘束`等)で、レイアウトだけ英語のまま孤立している | `レイアウト` |
| cloth/pipelines.rs:35 | 引数`ディスクリプタlayout` | 条件2・6 | 「ディスクリプタ」はカタカナへ訳しているのに「layout」だけ訳し忘れており、1つの識別子の中で翻訳方針が割れている | `ディスクリプタレイアウト` |
| cloth/pipelines.rs:44 | `layout_info` (ローカル変数) | 条件2 | 英語のみの命名。日本語命名を積極使用する規約に反する | `レイアウト情報` |
| cloth/inputs.rs:19-32 | `layout`・`介入pipeline`・`積分pipeline`・`目標の確定pipeline`・`乗数零化pipeline`・`拘束pipeline`・`曲げ拘束pipeline`・`目標拘束pipeline`・`ハッシュ消去pipeline`・`ハッシュ格納pipeline`・`分離pipeline`・`床とカプセルの押し出しpipeline`・`仕上げpipeline`・`頂点生成pipeline`・`描画pipeline`・`描画layout` (合計16個、`布描画入力`構造体リテラルのキー) | 条件2・6 | この関数の中だけで16回、同じ「パイプライン」「レイアウト」という語が英語のまま繰り返される。読み手は12本の`vk::Pipeline`フィールドの意味を1つずつ拾うのに、番号を英語で拾い直すことになる | 各`xxxpipeline`→`xxxパイプライン`、`layout`→`レイアウト`、`描画layout`→`描画レイアウト` |
| cluster_light_assignment/input.rs:26 | `pipeline` (フィールド、`クラスタ選別の描画入力`) | 条件2・6 | 同じ構造体の他フィールド(`格子`・`光添字列`・`セット`・`即時定数`・`班数`)はすべて日本語で、ここだけ英語が孤立している | `パイプライン` |
| cluster_light_assignment/input.rs:27 | `layout` (フィールド) | 条件2・6 | 同上 | `レイアウト` |
| cloth_shadow/mod.rs:28 | 引数`ビューとパスlayout` | 条件2・6 | 「ビューとパス」は日本語で正しく訳しているのに「layout」だけ訳し忘れている | `ビューとパスのレイアウト` |
| cloth_shadow/mod.rs:37-38 | `pipeline`・`layout` (構造体リテラルのキー、`布シャドウ描画入力`) | 条件2・6 | 同上の一般形 | `パイプライン`・`レイアウト` |

### 分類B: 処理(関数)なのに動詞を持たず、名詞句とも読めない中途半端な名前

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| bloom/descriptor/allocate.rs:58 | `fn usizeをu32へ(値: usize) -> u32` | 条件1・4 | 名前が助詞「へ」で終わり動詞が無い。「〜する処理です」に当てはめられない。同じファイル・同じ役割の他の変換関数(`cloth/readback_receptacle.rs`の`件数をusizeへ変換する`・`バイト数をu64へ変換する`)はすべて「変換する」を持つのに、この関数だけ動詞を欠く | `usizeをu32へ変換する` |

## 違反の疑い

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| cloth/readback.rs:71、depth_injection/pass.rs:22,26-29 | `文脈` (クロージャの引数名、型は`GPU命令の積み先と宣言済み資源の取り出し口`) | 条件7(直訳の検出質問) | 「context→文脈」は受理条件が明示する直訳の失敗例そのものである。ただし`_doc/計画/ユビキタス言語.md`687行目が「レンダーグラフのパスの文脈」という語を既に登録しており、`vulkan::graph`モジュール全体でこの略称が広く使われている(このため区画097だけの是正では閉じない)。型注釈が無いクロージャ引数として使われる箇所では、読み手が型を辿らないと「文脈」が何を指すか分からない点は残る |
| cloth/buffers.rs:17,18、cloth/buffers/create.rs:37-39 | `セルカウント` (フィールド名・変数名) | 条件6(1語1義) | 同じファイル・同じ構造体が別の場所で件数を「件数」「数」という native な語で表す(`目標拘束の数`・`曲げ拘束の数`・`目標の更新対応の件数`)のに、ここだけ英語由来のカタカナ語「カウント」を使っており、1つの概念(件数)に2つの綴りが並立している |
| cloth_shadow/mod.rs:37、cloth/inputs.rs:57 | `self.パイプライン.handle`・`self.描画パイプライン.handle` (フィールドアクセス) | 条件2 | アクセスしている`handle`フィールドの定義は本区画のファイルに無く(`パイプライン`型の定義側)、区画097の範囲では宣言そのものを検査できない。参照している綴りが英語のみである点だけを記録する |

## 違反ゼロの範囲

- `auto_exposure/bind.rs`・`descriptor.rs`・`storage.rs`・`storage_buffer.rs`・`readback.rs`・`pipelines/layout.rs`は分類Aの型に該当する箇所が無く、識別子はすべて受理条件を満たす
- `cloth/buffers.rs`・`create.rs`・`params.rs`・`mod.rs`・`write.rs`・`readback_receptacle.rs`・`pipelines/destroy.rs`・`buffers/create.rs`・`buffers/create/input_buffers.rs`・`buffers/create/target_buffers.rs`は、上表に挙げた個別の指摘(セルカウント)を除き違反なし
- `cluster_light_assignment/descriptor.rs`・`descriptor/binding.rs`・`descriptor/pool.rs`・`view_rows.rs`・`pipeline.rs`(構造体`クラスタ選別のパイプライン`自体は`パイプライン`・`レイアウト`という日本語フィールドで正しく統一している)は違反なし
- `depth/create.rs`・`depth/mod.rs`・`depth_injection/pass.rs`(`文脈`の疑いを除く)は違反なし

## 省略の有無

省略はしていない。38ファイルすべてを直読し、宣言された識別子を1つずつ受理条件へ当てた。
