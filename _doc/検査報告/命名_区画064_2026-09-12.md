# 命名検査報告: 区画064「crates/blitz_engine/src/streaming/coordinator」ほか

- 区画: crates/blitz_engine/src/streaming/coordinator（および同一区画に含まれる streaming/loader・streaming_chunk_world_fixture・streaming_coordinator_tests・streaming_loader_tests・surface_layer_textures）
- 区画番号: 064
- 作業ツリー: C:/devs/Blitzdrache0_roller（時点: main の db815090。このツリーは編集していない）
- 対象ファイル数: 28
- 直読したファイル数: 28（全件を直読。grepだけで済ませたファイルは0件）
- 参照した必読文書: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`

## 検査した識別子の範囲

型・関数・メソッド・フィールド・変数・定数・列挙の枝を含め、次のファイルの全識別子を検査した。

1. streaming/coordinator/candidates.rs
2. streaming/coordinator/degradation.rs
3. streaming/coordinator/directory_replace.rs
4. streaming/coordinator/generation.rs
5. streaming/coordinator/gpu_handoff.rs
6. streaming/coordinator/load_dispatch.rs
7. streaming/coordinator/overuse_eviction.rs
8. streaming/coordinator/prepared_data.rs
9. streaming/coordinator/priority_eviction.rs
10. streaming/coordinator/reset.rs
11. streaming/coordinator/static_shape_registry.rs
12. streaming/coordinator/transfer_record.rs
13. streaming/loader/error.rs
14. streaming/loader/result.rs
15. streaming/loader/worker.rs
16. streaming_chunk_world_fixture/edge_length.rs
17. streaming_chunk_world_fixture/pixels.rs
18. streaming_chunk_world_fixture/static_shape_world.rs
19. streaming_coordinator_tests/static_shape_tests.rs
20. streaming_loader_tests/setting_tests.rs
21. surface_layer_textures/engine_material_name.rs
22. surface_layer_textures/error.rs
23. surface_layer_textures/layer_index.rs
24. surface_layer_textures/load_error.rs
25. surface_layer_textures/reader.rs
26. surface_layer_textures/stable_id.rs
27. surface_layer_textures/texture_set.rs
28. surface_layer_textures/tile.rs

`vramバイト数`・`ramバイト数`（`streaming/memory_amount.rs`等、本区画の外で定義）を参照する箇所は、本区画のファイルが識別子を新設していないため定義側の区画で検査するべき対象とし、ここでは参照のみと扱って違反に数えていない。

## 違反の一覧

該当なし（0件）。

## 違反の疑い

| ファイル:行 | 識別子 | 疑う条件 | 疑いの内容 | 参考の改名案 |
|---|---|---|---|---|
| streaming/coordinator/candidates.rs:13 | `目録とカタログで解決する` | 条3(5W1Hの必要格) | 名前が「何を使って解決するか」(目録とカタログ)だけを述べ、「何を何へ解決するか」(要求一覧を予算候補一覧へ)を欠く。ファイル冒頭の`//!`を読めば復元できるが、名前だけでは「要求」も「予算候補」も出てこない。ただし`解決する`自体は`_doc/計画/ユビキタス言語.md`「チャンク目録」の項が「目録で座標をアセットIDへ解決し」と同じ動詞で定義済みの確立語であり、条7の直訳調(resolve→解決)には当たらない。関連関数でなく`pub(super)`の自由関数でASCIIモジュール直下のため、条2の補完規則で型名による目的語の補完も効かない | `目録とカタログで要求を予算候補へ解決する` |

上記1件は、既存のドメイン語彙(解決する)との整合は取れているため断定の違反とはせず、疑いとして報告する。

## 違反ゼロと判定した範囲

上記1件の疑いを除く、区画064の全28ファイルの日本語識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は、受理条件8つ・命名の質・呼称と語の選択のいずれにも抵触しなかった。特筆点:

- 「オーナーが不合格にした実例」の3形態(実体と合わない語・名詞1語への圧縮・詰め込みすぎ)は、値=名詞句(`既定画素`・`世代状態`)/処理=動詞句(`目録を差し替える`・`退避へ倒す`・`収容余地を作る`)の品詞規律が一貫しており該当なし
- `記録する`(`ディスク読込を記録する`・`gpu転送を記録する`・`gpu解除を記録する`、transfer_record.rs)は、過去に不合格になった「処理を名詞形で持たせて記録=ログと誤読された」実例とは異なり、動詞句のまま累計への加算を表し、`_doc/計画/ユビキタス言語.md`「移動の観測の記録」の用法とも一致するため1語1義(条6)に反しない
- ASCII識別子(英字・ギリシャ文字・数学記号)の識別子への混入は0件。`gpu`・`cpu`・`id`はグローバルCLAUDE.mdが許容する一般流通語の範囲に収まる、またはドメイン語彙(日本語)を伴わない純粋なASCII変数(`id`)であり日本語命名の検査対象外
- 直訳調(record→記録、resolve→解決、lookup→引く等)に該当しそうな`解決する`は、上表の1件を除き、いずれも`_doc/計画/ユビキタス言語.md`が定義済みの確立語として使われている
