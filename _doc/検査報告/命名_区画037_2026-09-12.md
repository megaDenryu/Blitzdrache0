# 命名検査報告: 区画037 crates/blitz_collision/src/contact_set 第1区画

- 区画: crates/blitz_collision/src/contact_set 第1区画(区画番号037、40ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main、db815090時点。編集していない)
- 直読したファイル数: 40 / 40(全数直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`(接触点集合まわりの登録語を照合に使用)

## 直読したファイル(40件、全件)

axis_interval.rs, box_clip_parts.rs, box_feature_support.rs, box_pair_answer_fixture.rs,
box_pair_axis.rs, box_pair_axis_preference_tests.rs, box_pair_axis_search.rs,
box_pair_completeness_tests.rs, box_pair_edge_tests.rs, box_pair_face_tests.rs,
box_pair_feature.rs, box_pair_invariant_tests.rs, box_pair_manifold.rs, box_pair_query.rs,
box_pair_stack_tests.rs, box_projection.rs, box_triangle_answer_fixture.rs, box_triangle_axis.rs,
box_triangle_axis_preference_tests.rs, box_triangle_axis_search.rs, box_triangle_edge_contact.rs,
box_triangle_face_manifold.rs, box_triangle_feature.rs, box_triangle_manifold.rs,
box_triangle_margin_tests.rs, box_triangle_query.rs, box_triangle_tests.rs, clip_feature_pair.rs,
clip_plane.rs, clip_polygon.rs, clip_reference_feature.rs, cross_axis_margin.rs,
cross_axis_preference.rs, deepest_axis.rs, determinism_tests.rs, edge_pair_contact.rs, error.rs,
feature_order_tests.rs, generation_margin.rs, manifold.rs

## 違反の一覧

1. `crates/blitz_collision/src/contact_set/determinism_tests.rs:91` | `第1のx, 第1のy, 第1のz, 第2のx, 第2のy, 第2のz`(let分解の束縛名) | 識別子への英字混入(検査指示の明示対象。実質は受理条件4「品詞の規律」寄りの逸脱でもある: 座標成分という値を指す名詞句の中へ生の英字x/y/zを裸で挟んでいる) | 「x」「y」「z」は日本語の語ではなく、直前の文脈(`位置の成分`が`[f64; 3]`の配列を返す)を読まないと何番目の成分かが復元できない。座標軸の意味を持つ英字を、助詞も付けずに名詞の後ろへ貼り付けている点が「単語らしく見せるための膠着圧縮」に近い | `第1の東西成分, 第1の上下成分, 第1の南北成分`等、実際の意味(この場面ではメートル座標の3成分)に沿った名か、`第1の位置の1番目`のような添字表現に改める(具体の名は文脈——地形の東西南北基準か任意軸か——を実装者が確認して決める)
2. `crates/blitz_collision/src/contact_set/feature_order_tests.rs:26-28` | `x側, y側, z側`(ループ変数名) | 同上(識別子への英字混入) | `直方体の面の向き`を3回列挙する三重ループの変数名であり、x/y/zがどの座標軸に対応するかはコード外の暗黙の慣習(直方体自身の座標軸の並び順)に依存する。読み手は`直方体自身の座標軸::並べる()`の並び順を別途確認しないと対応が取れない | `軸0の側, 軸1の側, 軸2の側`、または`直方体自身の座標軸::並べる()`を回して`軸`と`側`の組で回す実装に変え、裸の英字を無くす

## 違反の疑い

1. `crates/blitz_collision/src/contact_set/box_pair_axis.rs:51`、`crates/blitz_collision/src/contact_set/box_triangle_axis.rs:49` | `fn 外積(...)-> Self`(列挙の枝を組む私有の関連関数) | 受理条件4(品詞の規律: 処理は動詞句であるべきだが名詞「外積」のみ) | 私有の構築ヘルパーであり`Self::外積(...)`の形でレシーバ相当の型が文脈を補ってはいるが、値オブジェクト様式の`生成する()`ではなく名詞1語を関数名にしている。「外積を求めて分離軸の枝を組む」という処理の実体に対し、名詞だけでは「これは何?」に単独で答えられるか微妙(呼び出し側の`Self::外積(軸[0], 軸[1])`は読めるが、定義だけを見ると値なのか処理なのか一瞬迷う) | `外積の軸を組む`程度への改称を検討(断定はしない。両ファイルとも私有かつ同型のパターンであり、直すなら対で直すことになる)
2. `直方体自身の座標軸::X軸`・`Y軸`・`Z軸`(enumの枝。本区画のファイルでは`box_feature_support.rs`・`box_clip_parts.rs`・`box_pair_face_tests.rs`等で使用)、および`形の局所座標の位置`が持つ`.xのメートル()`・`.yのメートル()`・`.zのメートル()`、`方向<ローカル>`の`.x()`・`.y()`・`.z()`(box_pair_invariant_tests.rs・box_pair_face_tests.rs・determinism_tests.rs等で使用) | 識別子への英字混入 | いずれも本区画のファイルでは定義されておらず、`crate::shape`側で定義された既存の型・enumを使っているだけである。使用箇所だけを見て改名を求めると、この区画外の型定義まで踏み込むことになる。X/Y/Zという1文字は数学の直交座標軸の慣習として非専門家にも比較的復元しやすく、オーナー実例の`A2`や`Σλ_n`ほど不透明ではない | 断定を避け疑いに留める。是正するならこの区画でなく`crate::shape`(座標軸enum・位置/方向型)の定義側を対象にした別区画のローラーで扱うべき事項

## 違反ゼロの範囲

上記の英字混入(1件の違反2箇所+疑い2件)を除き、40ファイルの型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝はすべて受理条件8つを満たした。特に次の点を確認した。

- 品詞の規律: 値(接触点集合・接触生成の余白メートル・外積の軸へ切り替える優位幅メートル等)は名詞句、処理(生成する・求める・組む・積む等)は動詞句、真偽(〜か)は「軸の向きを反転するか」「二つの形を分離しているか」のように正しく使い分けられている
- 値オブジェクト様式の`生成する()`はいずれも型名(レシーバ)が目的語を担う形で使われており、裸の動詞の禁止(条件5)への違反は無い
- 直訳調・頭字語混入・助詞省略の膠着圧縮(「日本語命名の質」)は見当たらない。長い名前(`外積の軸へ切り替える優位幅メートル`等)はいずれも実体(`_doc/計画/ユビキタス言語.md`の該当語)と一致しており、実例で不合格になった「名前の語が実体と合わない」「名詞1語の圧縮」「詰め込みすぎ」のいずれの形も見つからなかった
- ユビキタス言語との照合: 接触点集合・接触解法の候補点・符号付き貫通量・接触生成の余白・外積の軸へ切り替える優位幅・最小の貫通の軸・参照面と入射面・切り抜き等の用語は、いずれも`_doc/計画/ユビキタス言語.md`の定義と実体が一致していた
