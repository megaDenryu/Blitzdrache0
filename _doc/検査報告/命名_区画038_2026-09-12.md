# 命名検査報告 — 区画038

- 区画名と範囲: crates/blitz_collision/src/contact_set 第2区画(区画番号038、12ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller
- 時点: main の db815090(このツリーは編集していない)
- 直読したファイル数: 12/12(全ファイルを直読。grepだけで済ませたファイルは0件)

対象ファイル:
1. manifold_builder.rs
2. manifold_capacity.rs
3. penetration_depth.rs
4. penetration_depth_tests.rs
5. query_fixture.rs
6. random_box_fixture.rs
7. reference_frame.rs
8. reference_role.rs
9. segment_pair_point.rs
10. solver_candidate_point.rs
11. triangle_clip_parts.rs
12. triangle_projection.rs

## 違反の一覧

なし(0件)。

## 違反の疑い

- `crates/blitz_collision/src/contact_set/solver_candidate_point.rs:30` | フィールド `貫通量`(型 `符号付き貫通量メートル`) | 条件6(1語1義)に対する疑い | `_doc/計画/ユビキタス言語.md`は「貫通量」と「符号付き貫通量」を別語として定義し、「接触生成の余白を入れた後は、この量だけを持つ型は無く、符号付き貫通量が正の側でこれを表す」と明記している(貫通量は退役した語)。このフィールドは符号付きの値を保持し、同じ型のアクセサは`符号付き貫通量()`という正しい名前を持つのに、フィールド名だけが退役語の「貫通量」を使っている。フィールドはprivateでこのファイル内にしか現れず外部の読み手を惑わせる実害は薄いが、型名と食い違うため用語定義との厳密な照合では引っかかる | フィールド名を`符号付き貫通量`へ改める(アクセサ名と揃える)

## 違反ゼロの範囲

上記12ファイルの全識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・enumの枝)について、受理条件8つ・日本語命名の質・呼称と語の選択・専門用語の表記の観点で違反は検出しなかった。ASCII文字・ギリシャ文字・数学記号の識別子への混入も無い。実体の照合は`_doc/計画/ユビキタス言語.md`の「接触点集合」「接触解法の候補点」「貫通量」「符号付き貫通量」「接触生成の余白」「参照面と入射面」「接触点の上限」等の既存登録語と各ファイルの型・フィールドを突き合わせて行い、名前と実体(型のフィールド・分岐・不変条件)の一致を確認した。
