# 命名検査報告(区画048)

## 区画名と範囲

- 区画: `crates/blitz_collision/src/triangle_mesh`(区画番号 048)
- 対象ファイル数: 27
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の db815090 時点。このツリーは編集していない)

## 直読したファイル数とgrepだけで済ませたファイル数

- 直読: 27 / 27(全ファイル)
- grepだけで済ませたファイル: 0(省略なし。全ファイルの全文を読んだ)

対象ファイルの内訳: 実装9件(`mesh.rs`・`error.rs`・`hierarchy.rs`・`hierarchy_builder.rs`・`hierarchy_split.rs`・`node.rs`・`mesh_contact.rs`・`mesh_hit.rs`・`mesh_triangle.rs`・`mesh_validation.rs`・`number.rs`・`covering_scan.rs`・`segment_query.rs`・`sweep_query.rs`・`traversal_stack.rs`)、試験・反証と材料18件(`brute_force_agreement_tests.rs`・`brute_force_fixture.rs`・`build_determinism_tests.rs`・`covering_scan_tests.rs`・`hierarchy_tests.rs`・`hierarchy_walk_fixture.rs`・`mesh_fixture.rs`・`mesh_validation_tests.rs`・`query_fixture.rs`・`query_tests.rs`・`rotated_shared_edge_fixture.rs`・`rotated_shared_edge_tests.rs`)。

## 違反の一覧

違反0件。

## 違反の疑い

いずれも受理条件4(品詞の規律: 値=名詞句)に対する軽度の疑いであり、断定はしない。

1. `crates/blitz_collision/src/triangle_mesh/rotated_shared_edge_tests.rs:43` | `何の網か: &str`(関数`共有辺の全部の標本で当たることを確かめる`の引数) | 条件4に対する疑い | 値(`&str`のラベル)は名詞句で書く規律に対し、疑問形「〜か」をそのまま名詞的に使っている。実体はパニック文言へ差し込む網の呼び名であり、読み手は文脈(呼び出し側で`"回した平らな四角形"`等の文字列が渡ること)を合わせて読めば意味は取れるため、誤読は起きにくいと判断し疑いに留める | 改名案: `網の呼び名`
2. `crates/blitz_collision/src/triangle_mesh/rotated_shared_edge_tests.rs:48` | `どちらから`(タプル分解の変数、値は`"上から"`/`"下から"`) | 条件4に対する疑い | 同上の理由で、値を表す変数名が疑問の形を取っている。パニック文言の埋め込み先としてすぐ下で使われており実害は小さい | 改名案: `狙う向きの呼び名`

## 違反ゼロの範囲

上記2件の疑いを除く全27ファイルの型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝は、受理条件1〜8のいずれにも抵触しなかった。確認した根拠は次のとおりである。

- **実体との照合**: `_doc/計画/ユビキタス言語.md`に登録済みの「三角形網」「頂点の番号で表した三角形」「三角形網の境界の木」「葉が持つ三角形の範囲」「覆いに触れる三角形の走査」「走査のスタックの上限」の各語を開き、対応する型・フィールド・定数(`三角形網`構造体、`頂点の番号で表した三角形`構造体、`三角形網の境界の木`構造体、`葉が持つ三角形の範囲`構造体、`覆いに触れる三角形の走査`構造体、`走査のスタックの上限`定数=64)の定義と実装を照合し、名前と実体(フィールド・振る舞い)が一致することを確認した
- **品詞の規律**: 値の型・フィールドはすべて名詞句(`頂点の位置`・`三角形ごとの面の向き`・`境界の木`等)、処理はすべて動詞句「〜する」(`頂点と三角形の列から生成する`・`覆いを縮める`・`次の三角形の番号を取り出す`等)で統一されている
- **裸の動詞の禁止**: ASCIIモジュール直下の自由関数(`三角形に外接する箱を求める`・`二つの三角形を中央の座標で比べる`・`三角形ごとの幾何を組みながら検査する`・`網を組む`・`点を局所座標へ写す`等)はいずれも目的語を含み、名前単独で「これは何をする処理か」に答えられる
- **1語1義**: 「網」「境界」「覆い」「範囲」「番号」はこの区画内で終始1つの意味に固定されており、ユビキタス言語の他区画での用法(照明・地形等)と衝突しない
- **直訳調・膠着圧縮・頭字語混入**: 検出せず。助詞を省いた圧縮(例:「網三角形数」のような形)も無い
- **英字・ギリシャ文字・記号の混入**: 27ファイル中の型名・関数名・フィールド名・定数名に英字・ギリシャ文字・数式記号を単体の語として使った識別子は無い。ただし試験コード内で、この区画の外(`crates/blitz_collision/src/shape/local_position.rs`・`local_displacement.rs`)が定義する`xのメートル()`・`yのメートル()`・`zのメートル()`という、単一のアルファベット`x`・`y`・`z`を軸名として含むメソッドを`hierarchy_tests.rs`(29〜37行)が呼んでいる。これは区画048の外で定義された既存の識別子であり、本区画のファイルはそれを呼ぶ側でしかないため、本報告の違反一覧には計上しない(検査対象は区画048の27ファイルが持つ識別子)。この既存識別子自体の要否は、その定義ファイルが属する区画のローラー検査が判定すべき事項として申し送る
- **長さ**: 多くの識別子が長いが、いずれも格・助詞を削った結果ではなく、必要な格(何を・どうする・どこへ)を保持したままの結果であるため条件8には抵触しない

省略した検査は無い。27ファイルすべてを冒頭から末尾まで直読し、grepによる部分確認だけで済ませたファイルは無い。
