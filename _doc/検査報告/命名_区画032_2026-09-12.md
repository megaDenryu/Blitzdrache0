# 命名検査報告: 区画032

## 区画名と範囲

- 区画: `crates/blitz_asset_compiler/src/runtime_compilation/world`(区画番号032、37ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の `db815090`。このツリーは編集していない)
- ファイル一覧の出所: `scratchpad/roller/block_032.txt`

## 直読したファイル数とgrepだけで済ませたファイル数

- 直読: 37/37(全ファイル)
- grepだけで済ませたファイル: 0件

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `argument_name.rs:60` | `解析する`(自由関数、ASCIIモジュール`argument_name`直下) | 条件5(裸の動詞の禁止)、条件2 | ASCIIモジュール名は補完力ゼロのため、呼び出し側`argument_name::解析する(引数名)`では「何を解析するか」が単独で答えられない。`create::生成する`の却下例と同型 | `引数の綴りから対象世界を解析する` |
| `argument_name.rs:95` | `骨格の並びの世界`(自由関数、`fn 骨格の並びの世界(種類) -> 対象世界`) | 条件4(品詞の規律)、条件1 | 処理(構築処理)なのに動詞を持たない名詞句で、「これは何?」への答えにしかならず「〜する処理です」で成立しない | `骨格の並びの世界を種類から作る` |
| `assembly_rule_choice/frame_bay_choice.rs:79` | `名前`(自由関数、`pub(super) fn 名前(綴り: &str) -> Result<接合点名, String>`) | 条件5、条件1、条件4 | 生の文字列を検証済みの`接合点名`へ変換する処理だが、名前が単独の裸名詞で、呼び出し側で`名前("正面のはめ口")`と裸で使われ、何を検証しているのか単独で答えられない | `接合点名を確かめる` |
| `assembly_rule_choice/frame_bay_choice.rs:83` | `識別子`(自由関数、`pub(super) fn 識別子(綴り: &str) -> Result<部品ID, String>`) | 条件5、条件1、条件4 | 同上。`部品ID`への変換処理を裸の名詞で名付けている | `部品識別子を確かめる` |
| `assembly_rule_choice/house_rule.rs:92` | `名前`(自由関数) | 条件5、条件1、条件4 | `frame_bay_choice.rs:79`と同型の違反。この型のモジュールに同名の私有関数が複数箇所で独立に定義されており、いずれも裸名詞のまま呼ばれている | `接合点名を確かめる` |
| `assembly_rule_choice/house_rule.rs:96` | `識別子`(自由関数) | 条件5、条件1、条件4 | 同上 | `部品識別子を確かめる` |
| `assembly_rule_choice/tree_rule.rs:62` | `名前`(自由関数) | 条件5、条件1、条件4 | 同上 | `接合点名を確かめる` |
| `assembly_rule_choice/tree_rule.rs:66` | `識別子`(自由関数) | 条件5、条件1、条件4 | 同上 | `部品識別子を確かめる` |
| `vegetation_declaration.rs:29` | `一覧`(自由関数、`pub(super) fn 一覧() -> Vec<アセット定義>`) | 条件1、条件3、条件2 | ASCIIモジュール`vegetation_declaration`直下の自由関数であり補完力ゼロ。「一覧です」は「何の一覧か」に答えていない | `植生の世界のアセット定義一覧` |
| `village_declaration.rs:36` | `一覧` | 条件1、条件3、条件2 | 同上のパターン | `見本の集落の世界のアセット定義一覧` |
| `stone_hut_declaration.rs:38` | `一覧` | 条件1、条件3、条件2 | 同上 | `屋内の多光源の世界のアセット定義一覧` |
| `night_lights_declaration.rs:55` | `一覧` | 条件1、条件3、条件2 | 同上 | `夜の多光源の世界のアセット定義一覧` |
| `part_house_row_declaration.rs:31` | `一覧` | 条件1、条件3、条件2 | 同上 | `部品で組んだ家の並びの世界のアセット定義一覧` |
| `part_tree_row_declaration.rs:20` | `一覧` | 条件1、条件3、条件2 | 同上 | `部品で組んだ木の並びの世界のアセット定義一覧` |
| `part_frame_row_declaration.rs:64` | `一覧`(引数`種類`あり) | 条件1、条件3、条件2 | 引数はあるが名前自体は依然「一覧」のみで、返す一覧が何の一覧かを名前が言わない | `部品で組んだ一間四方の骨格の並びの世界のアセット定義一覧` |
| `visual_sample_declaration.rs:44` | `一覧` | 条件1、条件3、条件2 | 同上 | `目視見本の世界のアセット定義一覧` |
| `fox_tour_declaration.rs:58` | `一覧` | 条件1、条件3、条件2 | 同上 | `場所巡りの世界のアセット定義一覧` |
| `vertex_diagnostic_declaration.rs:33` | `一覧`(引数`原型`あり) | 条件1、条件3、条件2 | 同上 | `頂点診断の世界のアセット定義一覧` |

上の`一覧`9件は同一パターンの繰り返しであり、いずれも`asset_definition_list.rs`から`vegetation_declaration::一覧()`のようにASCIIモジュール名を介して呼ばれる。対照として同じファイル群の中で`asset_declaration.rs`の`板の世界の一覧()`・`地形の世界の一覧()`は主語を名前へ含めており受理条件を満たす。この対比が、`一覧`単独が条件1・3に落ちることの根拠である。

## 違反の疑い

- `fox_declaration.rs:8` の定数`キツネの寸法の倍率`、および各所の`小物の木箱シーン`・`材質境界の二材質シーン`・`材質差し替えのシーン`等(`asset_declaration.rs`)は、実体が`(&str, ソースアセットの相対パス)`という生タプルであるにもかかわらず「シーン」と名付けており、名前が指す実体(タプル)と語(シーンという完成品を思わせる語)がやや食い違う可能性がある。ただし呼び出し側は`.0`・`.1`で分解して使っており、意味は「そのシーンを表す(名前, ソース)の対」として一貫して読めるため、条件1(受理テスト)は通過すると判断し、確定違反にはしなかった。生タプルを型に包むかどうかはプリミティブ執着禁止の構造上の論点であり、命名の受理条件そのものへの違反ではないと切り分けた
- `target_world.rs:47` のコメント中の「クソゲー1本目「キツネの場所巡り」を遊ぶ世界」という表現は俗語だが、識別子ではなくコメントであるため本検査(識別子検査)の対象外として扱った(呼称と語の選択・文章規範の観点では別途是正の余地がある)

## 違反ゼロの範囲

- ASCII識別子への英字・ギリシャ文字・数学記号の混入、漢字1文字への圧縮(順4・段3型)、英語の直訳調(record→記録、lookup→引く等)、1語1義の衝突は、37ファイルの全識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)を通じて検出しなかった
- `一覧`・`名前`・`識別子`・`解析する`・`骨格の並びの世界`の17件を除く、残りの識別子(構造体・列挙・値オブジェクトの`生成する()`・レシーバ付きメソッド・完全修飾された定数群)は受理条件8つを満たしていると判定した
