# 命名検査報告 区画091

- 区画名: crates/blitz_render/src/renderer/draw_dispatch (実際は draw_dispatch・draw_execute・draw_issue_breakdown・draw_stage_resources・frame_progress・generate の6モジュールを束ねる区画091、対象33ファイル)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 33 / 33(全数直読。grepだけで済ませたファイルは無い)
- 識別子数: 型・関数・メソッド・フィールド・変数・定数・列挙の枝を合わせて約230件(全件を受理条件8つで判定)

## 違反の一覧

該当なし(違反0件)。

## 違反の疑い

1. `crates/blitz_render/src/renderer/generate/core_setup.rs:18` | `タイムスタンプ周期ns` | 条件2(ASCII混入の禁止)に対して疑い | フィールド名の末尾へ単位の略号`ns`(ナノ秒)をASCIIのまま連結している。オーナーが不合格にした実例の「識別子へASCIIの英字を入れたものは不合格」に文字どおり触れる。ただし`ms`・`ns`等の単位略号はこのクレート全体(`gpu_timing`配下・`xpbd_bench`等、この区画外を含め8ファイル)で一貫して使われている既存の命名慣習であり、この区画だけの新規逸脱ではない。断定はせず、リポジトリ全体の単位表記方針(px/rem等と同様の扱いにするか)をオーナーへ確認したうえでの一括是正が妥当と考える | 改名の素案(参考): `タイムスタンプ周期ナノ秒`、または単位を型(`ナノ秒`のnewtype)で表し数値から名前へ単位を追い出す

## 違反ゼロの範囲

- draw_dispatch(4ファイル: draw_mode.rs, frame_record.rs, target_input.rs, work_area_fill.rs): 違反ゼロ
- draw_execute(6ファイル: acquire.rs, current_color_readback.rs, depth_readback.rs, hdr_readback.rs, local_visibility_readback.rs, motion_vector_readback.rs, prepare.rs, submit.rs のうち prepare.rs と submit.rs を含む計8ファイル): 違反ゼロ
- draw_issue_breakdown(3ファイル: pass_issue.rs, point_light_shadow.rs, stage_counts.rs): 違反ゼロ
- draw_stage_resources(9ファイル: atmosphere.rs, cluster_light_assignment.rs, create.rs, indirect_lighting.rs, point_light_shadow.rs, sky.rs, synthetic_depth.rs, create/optional_stages.rs, create/optional_stages/distant_environment.rs, create/optional_stages/sky_stage.rs): 違反ゼロ
- frame_progress(3ファイル: create.rs, slot_cycle.rs, slot_cycle/slot_cycle_tests.rs): 違反ゼロ
- generate(5ファイル: assemble.rs, core_setup.rs, frame_resources.rs, generate_resources.rs, generate_tests.rs): `core_setup.rs`の疑い1件を除き違反ゼロ

## 検査所見(補足)

この区画の命名は総じて受理条件8つを満たしている。特に良好だった点:
- 動詞句名(`〜する`)と名詞句名(値)の品詞の使い分けが徹底されている(例: `資源表世代を最新にして束縛を借りる`、`点光源の影の記録内訳`)
- 目的語・着点の格が省略されていない(例: `一フレーム描画して圧縮前のhdrを読み戻す`、`資源表世代のフェンス通過を記録する`)
- 同型の複数ファクトリーが材料・対象の格で区別されている(`読み戻しバッファから今のフレームの色を作る`/`読み戻しバッファから深度画像を作る`/`読み戻しバッファから局所可視度画像を作る`等)
- 「記録」「生成する」等、過去に不合格となった語が単独名で現れる箇所は無かった(いずれも型名・レシーバによる補完、または目的語付きの句になっている)
- LUT→大気のベイク済み画像、lookup→参照する、といったオーナー確定訳語がこの区画でも一貫して使われている
