# 命名検査報告: 区画159(xtask/src/ow4_bench, xtask/src/part_row_draw)

- 区画番号: 159
- 区画名: `xtask/src/ow4_bench`・`xtask/src/part_row_draw`(2ディレクトリ、計39ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(時点: main の `db815090`。このツリーは編集していない)
- 直読したファイル数: 39/39(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 併読した必読ファイル: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(「部品と接合の語彙」「ベイ格子の語彙」「オープンワールドの語彙」を中心に照合)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `xtask/src/ow4_bench/point.rs:34` | `候補数を小数へ` | 条件1(受理テスト)・条件4(品詞の規律) | 述語(動詞)を欠いており「〜する処理です」への答えとして文が完結しない。「候補数を小数へ」だけでは「どうする」が無く、変換する処理であることが名前から読み取れない(実体は`u64`の候補数を`f64`へ変換する処理)。 | `候補数を小数へ変換する` |
| `xtask/src/part_row_draw/frame_chain.rs:76` | `撮影してコードへ変換する` | 条件1(受理テスト)・条件7(直訳/紛れの検出) | 「コード」が単独では「終了コード(ExitCode)」と「ソースコード」のどちらを指すか読み手が復元できない。戻り値型`ExitCode`は英語(ASCII)識別子であり受理条件2により補完に使えないため、日本語の名前だけで判断すると「撮影した結果をプログラムのコードへ変換する」とも読める。 | `撮影して終了コードへ変換する` |
| `xtask/src/part_row_draw/tree_row.rs:35` | `検収してコードへ変換する` | 条件1・条件7 | 上と同一の理由(「コード」の指す実体が終了コードかソースコードか名前だけでは決まらない)。 | `検収して終了コードへ変換する` |
| `xtask/src/part_row_draw/scale_pair.rs:63` | `検収してコードへ変換する`(`件数を変えた並びの対`のメソッド) | 条件1・条件7 | 同上。この識別子は`house_row.rs:55`・`frame_row.rs:48`から`家の並びの対.検収してコードへ変換する()`/`一間四方の骨格の並びの対.検収してコードへ変換する()`という形で呼ばれており、日本語のレシーバ(`家の並びの対`等)で意味を補っても「コード」の指す実体は変わらず読めない。 | `検収して終了コードへ変換する` |

## 違反の疑い

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `xtask/src/ow4_bench/condition.rs:79`(関数)、対応する`計測条件`の構造体フィールド`一日内時刻の秒`(`condition.rs:28`) | `一日内秒を読む` | 条件6(1語1義)・条件3(必要格) | フィールド名は`一日内時刻の秒`だが、それを読み込む関数名は`時刻`が抜けた`一日内秒`になっている。同じファイル内で完結し呼び出し元も1箇所(`引数を読む`)だけであるため実害は小さいが、フィールド名と関数名で語形が食い違っており、フィールド名だけを見て対応する読み取り関数を探す読み手には気づきにくい。 |

## 違反ゼロの範囲

上記の1件の違反(`候補数を小数へ`)と3件の違反(`撮影してコードへ変換する`系)・1件の違反の疑いを除く、39ファイルの残り全部の日本語識別子(型名・enum名・enumの枝・関数名・メソッド名・フィールド名・定数名・変数名)は受理条件8つ・命名の質・呼称と語の選択のいずれにも違反しなかった。ASCII識別子・ギリシャ文字・数学記号を日本語識別子へ混入した例は0件だった(`CPU`・`GPU`・`Vulkan`は「専門用語の表記」節の一般に通用している語の基準で許容される範囲であり、いずれも受理条件2が言う「補完に使う」用法ではなく型名・定数名自体の一部として使われているため対象外とした)。

検査した範囲: `xtask/src/ow4_bench`直下12ファイル+`budget/median_tests.rs`・`condition/argument_tests.rs`・`error/display.rs`・`gpu_table/parse_tests.rs`・`run/launch_arguments.rs`・`shadow_condition/condition_tests.rs`・`table/comparison.rs`・`table/counts.rs`・`table/run_display.rs`・`validation/run.rs`の10ファイル(計13ファイル ※`table.rs`は`table/`のmodファイルを兼ねる)、`xtask/src/part_row_draw`直下11ファイル+`scale_pair/error.rs`・`scale_pair/judgment.rs`・`scale_pair/measurement.rs`・`scale_pair/run.rs`・`scale_pair/single_measurement.rs`・`scale_pair/error/display.rs`の6ファイル(計17ファイル)。ブロック一覧(`block_159.txt`)の39件全部をこの中に含む。省略した範囲は無い。
