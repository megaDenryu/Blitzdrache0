# 命名検査報告: 区画111 crates/blitz_render/src/vulkan/temporal_reconstruction/pipeline ほか

- 区画番号: 111
- 対象: `crates/blitz_render/src/vulkan/temporal_reconstruction/pipeline` を起点とする、一覧記載の38ファイル(texture・tonemap・transfer・ui配下を含む)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, db815090時点。編集していない)
- 検査日: 2026-09-12
- 直読したファイル数: 38 / 38(全ファイル)。grepだけで済ませたファイルは0件
- 必読: グローバルCLAUDE.mdの「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(該当語のみ照合。HDR・操作サービス・セッション型・資源型・GPU資源の確保係・ステージング経由の転送係の定義箇所を確認した)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_render/src/vulkan/ui/mod.rs:65 | `setを取得する` | 日本語命名の質(英語の直訳調・単語混入禁止)、受理条件7(直訳の検出質問) | 「set」が未翻訳の英単語のまま名前に入っており、英語を知らない読み手が復元できない。しかも同じ概念(ディスクリプタセット)を同モジュール内の他の識別子(`セットレイアウト`・`セットのハンドル`・`テクスチャのセットを割り当てて書き込む`・`セットを解放する`)はすべてカタカナ「セット」で書いており、この1箇所だけ生の英語に戻っている(1語1義の逸脱でもある) | `セットを取得する` |
| crates/blitz_render/src/vulkan/ui/registry.rs:69 | `setを取得する` | 同上 | 同上(定義本体。mod.rsは委譲) | `セットを取得する` |
| crates/blitz_render/src/vulkan/ui/pipeline/assemble.rs:86 | `premultiplied_alphaブレンド状態` | 日本語命名の質(英語の直訳調・省略語混入禁止)、受理条件7 | 「premultiplied_alpha」がスネークケースの生の英語のまま名前へ埋め込まれており、専門用語の初出補足も無い。英語を知らない読み手は「事前に何をどう掛けた透明度なのか」を復元できない | `事前乗算アルファのブレンド状態` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑われる条件 | 理由 |
|---|---|---|---|
| crates/blitz_render/src/vulkan/temporal_reconstruction/pipeline/fixed_function.rs:37-49、crates/blitz_render/src/vulkan/ui/pipeline/assemble.rs:36-52 | `頂点入力state`・`入力アセンブリstate`・`ビューポートstate`・`ラスタライズstate`・`マルチサンプルstate`・`カラーブレンドstate`・`動的state`・`動的state一覧` | 日本語命名の質(英語混入) | 未翻訳の英単語「state」を日本語名詞へ直接連結している。ただし`grep`で確認した限り、この綴りは本区画の2ファイルだけでなく`blitz_render`のパイプライン構築コード全体(`fullscreen_pipeline`・`particles`・`pipeline/*_pipeline`など少なくとも9ファイル)で一貫して使われており、代入元のVulkan構造体名(`vk::Pipeline*StateCreateInfo`)をそのまま踏襲する既存の項目間で確立した書式に見える。区画111だけを直す一括是正は「一括リネームはしない」の運用にも反するため、違反と断定せず疑いとして報告する。是正するなら`頂点入力ステート`のようにカタカナへ統一し、リポジトリ全体のパイプライン構築コードを対象にする判断がいる |
| crates/blitz_render/src/vulkan/ui/mod.rs:80-86 | `pipeline_handle`・`pipeline_layout`(および対応する構造体の`pipeline`フィールド) | 受理条件4(品詞の規律・命名一貫性)、プロジェクトCLAUDE.md「命名」節(ドメイン層は日本語命名を積極使用) | 同じ型`UIリソース一式`の他の全メソッド(`テクスチャを反映する`・`テクスチャを削除する`・`ジオメトリを書き込む`・`破棄する`)は日本語で書かれているのに、この2つだけ全部ASCIIの英語(スネークケース)で書かれており、型の中で命名規則が割れている。フルASCIIの識別子自体は「日本語識別子」の検査対象そのものではないため違反と断定しないが、1つの型の中の一貫性という観点で疑いとして残す | `パイプラインのハンドル`・`パイプラインのレイアウト` |

## 違反ゼロの範囲

上記の3件の確定違反と2件の疑いを除く、残り35ファイルの全識別子(型名・関連関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は受理条件8つ・命名の質・呼称と語の選択のいずれにも抵触しなかった。特に、資源型・操作サービス・セッション型の使い分け(`テクスチャ`・`材質テクスチャ表のサンプラー`・`転送実行環境`・`転送コマンドを積む一時コマンドバッファ`・`ステージング経由の転送係`等)は定義済みの用語(ユビキタス言語「操作サービス」「セッション型」「資源型」節)と実装が一致しており、目的語・着点を欠いた動詞(`生成する`単独名など)もすべて型名または引数名で補完されている。安全性コメント・注意コメントの位置と内容もコード側の設計判断と整合していた。
