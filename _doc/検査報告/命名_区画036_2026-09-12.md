# 命名検査報告: 区画036

- 区画名と範囲: crates/blitz_collision/src (27ファイル、capsule.rs・contact_set.rs・continuous.rs・dynamic_index.rs・height_field.rs・lib.rs・oriented_box.rs・pseudo_random_fixture.rs・shape.rs・solver.rs・triangle.rs・triangle_mesh.rs、および capsule/ 配下15ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller (main の db815090。編集せず走査のみ)
- 直読したファイル数: 27/27(全数)。grepだけで済ませたファイルは0件(全ファイルを直読した。省略なし)

## 違反の一覧

なし(0件)。

## 違反の疑い

なし。

検査時に特に注視した観点(オーナーが不合格にした3つの形、および記号混入)への該当は無かった。

- **実体と合わない語**: 型・フィールドの名前と、冒頭の`//!`が説明する実体を1件ずつ突き合わせた。`静止したカプセルで触れた場所`・`軸と特徴の最近接`・`触れ始める候補`・`接触点集合`・`接触解法の候補点`・`接触生成の余白`等、ユビキタス言語.md(1035行以降「剛体の語彙」、874行以降「接触点集合」節)に登録済みの語と一致する名前を使っており、独自の言い換えは見当たらなかった
- **名詞1語への圧縮**: `候補`・`場所`・`最近接`のような短い名詞は単独では出現せず、すべて`静止したカプセルで触れた場所`・`軸と特徴の最近接`のように、何についての値かを名詞句で書いていた
- **詰め込みすぎ**: 列挙の枝(`静止した軸の端と掃引する軸の端`等)は語数が多いが、`sweep_candidate.rs`の冒頭コメントが4種類9通りの数え上げの根拠を説明しており、枝の名前がその実体(2つの参加者それぞれの部位の組)とそのまま対応していた。本質でない条件の混入は無い
- **ASCII・ギリシャ文字・記号の混入**: 日本語識別子の中にアルファベット・ギリシャ文字・数式記号を混ぜた例は無い。モジュール名(ファイル名。`sweep_solve_axis_end`等)はASCIIだが、Rustのパス制約によるものであり、CLAUDE.mdが定める許容(クレート名・モジュール名はASCII)の範囲内である
- **裸の動詞**: `生成する()`はすべて型の関連関数として現れ(`カプセルどうしの接触の求解::生成する`等)、レシーバとなる型名が目的語を担う値オブジェクト様式に一致するため受理条件5に違反しない
- **英語直訳調**: `record`→記録、`resolve`→解決のような直訳語は見当たらなかった。`求解`・`最近接`・`掃引`はいずれもユビキタス言語.md・既存規約で定着した訳語である

## 違反ゼロの範囲

区画036(crates/blitz_collision/src、27ファイル全数)は、直読による検査の結果、日本語命名の受理条件8つ・命名の質・呼称と語の選択のいずれにも違反が無かった。
