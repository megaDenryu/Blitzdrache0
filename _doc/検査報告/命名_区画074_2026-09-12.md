# 命名検査報告 区画074

- 区画名: crates/blitz_math/src/units
- 範囲: 区画番号074、26ファイル(block_074.txtの一覧全部)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main, db815090時点。このツリーは編集していない)
- 直読したファイル数: 26 / 26(全ファイルを直読した。grepだけで済ませたファイルは無い)

対象ファイル:
global_meter.rs, global_second.rs, inverse_kilogram.rs, inverse_kilogram_square_meter.rs,
inverse_meter.rs, kilogram.rs, kilogram_meter.rs, kilogram_square_meter.rs, mass_error.rs,
meter.rs, meter_per_second.rs, meter_per_second_squared.rs, meter_tests.rs, mod.rs,
narrowing.rs, newton.rs, newton_meter.rs, newton_meter_second.rs, newton_second.rs,
radian.rs, radian_per_second.rs, second.rs, signed_inverse_kilogram.rs, square_meter.rs,
unit_arithmetic_tests.rs, unit_conversion_error.rs

## 違反の一覧

- `crates/blitz_math/src/units/unit_conversion_error.rs:8` | `単位変換エラー::F32範囲外` | 条件2/条件7に相当する識別子への英字混入の禁止(「日本語命名の質」の頭字語混入禁止と同型、識別子へのASCII混入の禁止事例) | 列挙の枝名にASCIIの技術略語「F32」がそのまま混ざっている。このクレートの他の全ファイル(global_second.rs・narrowing.rs等)はf32を「単精度」、f64を「倍精度」と訳して識別子に使っており、この枝だけが訳語を経由せず生の型名を持ち込んでいるため、1語1義(条件6)の観点でも訳語選択が割れている | `単位変換エラー::単精度範囲外`

## 違反の疑い(断定しない)

- `crates/blitz_math/src/units/radian.rs:17` | `度から`(`ラジアン::度から(度: f32) -> Self`) | 条件4(品詞の規律: 処理は動詞句「〜する」) | この関連関数は度からラジアンへ変換する処理だが、名前が格助詞止め(「〜から」)で終わり動詞を持たない。型名`ラジアン`が対象を補うため受理テストは通るように見えるが、「度から」だけを見ると値を指す名詞句(例: 度から求めた値のフィールド名)とも読め、処理であることが名前の品詞から自明でない。ユビキタス言語には「材料から生成する」の形(画素から生成する)が正例として載っており、この関数はその形の`生成する`を省略している
  - 改名素案: `度から生成する`
- `crates/blitz_math/src/units/radian_per_second.rs:21` | `度毎秒から`(`ラジアン毎秒::度毎秒から(度毎秒: f32) -> Self`) | 条件4(品詞の規律) | 上と同じ構造の疑い。「度毎秒から」も格助詞止めで処理の動詞を持たない
  - 改名素案: `度毎秒から生成する`

## 違反ゼロの範囲

上記2件の疑いと1件の違反を除く、26ファイル中の残り全識別子(型名24個、関連関数`生成する`・`値`等のアクセサ、`大域秒::秒へ狭める`・`narrowing::倍精度の無次元量を単精度へ狭める`・`ラジアン::一周の中で最短の弧へ畳む`・`メートル::表現の刻み幅`・`メートル毎秒::比で伸ばす`・`符号付き逆キログラム::逆質量から生成する`等の動詞句、`質量エラー`・`単位変換エラー`の列挙の枝(F32範囲外を除く)、演算子実装(`Add`/`Sub`/`Mul`/`Div`)内の引数名`右辺`・`倍率`・`経過時間`・`半径`・`分母`・`面積`、テストファイル内の関数名・変数名)は受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも抵触しなかった。特に、質量・逆質量・慣性・逆慣性・力・回転力・衝撃・回転衝撃・面積・逆長さ・逆質量(符号付き)の各単位型は、いずれもユビキタス言語の該当定義(「キログラム」「逆キログラム」「キログラムメートルとキログラム平方メートル」「速度・角速度・力・回転力・衝撃・回転衝撃」「面積ベクトルと平方メートル」「符号付き逆キログラム」)と実体・次元が一致していることを照合した。
