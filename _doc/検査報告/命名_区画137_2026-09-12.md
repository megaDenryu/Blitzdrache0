# 命名ローラー検査 区画137

- **区画名と範囲**: crates/editor_server/tests/integration 第2区画（区画番号137、対象1ファイル）
- **時点**: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090`（このツリーは編集していない）
- **直読したファイル数**: 1/1（`crates/editor_server/tests/integration/world_structure_routes.rs`。grepだけで済ませたファイルは0件）
- **参照のため grep のみで覗いたファイル**（違反判定の対象外。区画137の外にある定義箇所の確認のみに使用）: `common/mod.rs`（`一時プロジェクト` 構造体・`ルーターを作る` 関数の定義箇所）、`common/world_layout.rs`（`区画割りJson` 関数・`区画割りを保存する` 関数の定義箇所）。これらのファイルは block_137.txt に含まれず、本区画の検査対象ではない

## 検査した識別子（対象ファイル内で宣言・使用されている日本語識別子）

型・関数・メソッド・フィールド・変数・定数・列挙の枝の候補として、テスト関数名3件、ローカル変数7件（一時・応答・本体・本文・取得データ・不正データ、および参照渡しの再束縛）、および他ファイル定義の関数の呼び出し4件（一時プロジェクト::生成する / ルーターを作る / 区画割りを保存する / 区画割りJson）を確認した。JSON文字列リテラルのキー（"区画割り"・"一辺のメートル"・"軸あたりチャンク数"・"チャンクあたり格子解像度"・"広域道路一覧"・"制御点列"・"全幅メートル"・"細分割数"）は試験データの内容であり、Rustの識別子（型・関数・メソッド・フィールド・変数・定数・列挙の枝）に当たらないため検査対象から除外した。

## 違反の一覧

区画137のファイル自体が宣言した識別子の範囲では、受理条件を落ちる違反は0件だった。

## 違反の疑い

| ファイル:行 | 識別子 | 疑う条件 | なぜ読めない/紛れるか | 改名の素案 |
|---|---|---|---|---|
| world_structure_routes.rs:13,26,41 | `一時` | 条1(受理テスト)・条4(品詞の規律) | 型`一時プロジェクト`（common/mod.rs定義、区画外）から生成した値を、型名の名詞「プロジェクト」を落として`一時`だけへ切り詰めている。「一時」は単独では「一時的な」という形容の語であり、名詞句として「これは何?」に単独回答できない。定義行（`let 一時 = ...::生成する(...)`）を離れて`&一時`（14行目等）だけを読む場面では、プロジェクトを指す変数だと分かりにくい。ただし同一行の型名`一時プロジェクト::生成する`が読み手の一緒に読む識別子であるため、条2の「レシーバ・型名による補完」に近く、断定はしない | `一時プロジェクト`（型名をそのまま変数名に流用）または `検査用一時プロジェクト` |
| world_structure_routes.rs:12 | `大域世界構造の初期状態はnullを返す`中の`null` | 条7(直訳の検出質問)・日本語命名の質(英語の直訳調・頭字語) | 「null」はJSONの空値を指す英語で、UI/CPU/GPU/API/シェーダーのような一般流通語の例には挙がっていない。プログラミング非経験者は「ヌル」が何を指すか復元できない可能性がある | `大域世界構造の初期状態は空値を返す` |
| world_structure_routes.rs:36（定義は共通ヘルパー `common/world_layout.rs:13`、本区画の外） | `区画割りJson` | 条4(品詞の規律)・日本語命名の質(英語の頭字語混入・膠着圧縮) | 「区画割り」（日本語の名詞）と「Json」（英語の頭字語、しかも大文字小文字が不統一な独自表記）を助詞・訳語なしで直結している。「Json」はUI/CPU/GPU/API/シェーダーの例に挙がる一般流通語ではなく、非専門家には復元できない。関数(処理)なのに動詞が無く名詞の直結だけになっている点も品詞の規律に照らして紛れる。この識別子の定義ファイルは区画137に含まれないため断定はせず、当該ファイルを担当する区画での確定を推奨する | `区画割りの期待JSON()`（JSONをカタカナ語として定着させるなら用語定義ファイルへの登録も要る）または `区画割りの期待値をJson形式で返す` |

## 違反ゼロの範囲

`world_structure_routes.rs` が自ら宣言するテスト関数名3件（`大域世界構造の初期状態はnullを返す`の`null`部分を除く）、および変数`応答`・`本体`・`本文`・`取得データ`・`不正データ`は、受理条件8つ・呼称と語の選択・日本語命名の質のいずれにも違反しない。使用中の登録済み用語「大域世界構造」（ユビキタス言語734行）・「世界の区画割り」（719行）・「広域道路」（720行）・「正本」（618行等）はいずれも定義と一致した用法だった。
