# 命名検査報告: 区画135 (crates/editor_server/src/routes ほか)

- 区画名・範囲: crates/editor_server/src/routes 配下25ファイル + crates/editor_server/src/storage 配下10ファイル(合計35ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(編集せず読み取りのみ)
- 時点: main の db815090
- 直読したファイル数: 35 / 35(全ファイル直読。grepだけで済ませたファイルは無い)
- 検査軸: 日本語命名の受理条件8条 + 日本語命名の質(頭字語・直訳調・膠着圧縮の禁止) + 呼称と語の選択 + ユビキタス言語との照合

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| routes/chunk_heightmap_get.rs:7 | `チャンク高さ格子を返す` | 日本語命名の質(助詞省略の膠着圧縮禁止) | 同じファイルの14行目で同じ概念を`状態.チャンクの高さ格子を読み未保存ならマザーから切り出す`と「の」ありで書いている。公開ハンドラ名だけ「チャンク」「高さ」「格子」を助詞なしで詰めており、同一ファイル内で同じ資源に2通りの表記が同居する | `チャンクの高さ格子を返す` |
| routes/chunk_heightmap_put.rs:20 | `チャンク高さ格子を保存する` | 同上 | 39行目で呼ぶ保管庫側のメソッドは`チャンクの高さ格子を検証して保存する`(「の」あり)。ハンドラ名だけ圧縮されている | `チャンクの高さ格子を保存する` |
| routes/chunk_splat_weights_get.rs:12 | `チャンク材質重みを返す` | 同上(+語の脱落) | 13行目で呼ぶ`状態.保管庫().チャンクの材質重みを読む`は「の」ありで書いている。加えて登録語(ユビキタス言語)は「地表材質の重み」であり、ハンドラ名は「地表」も落として「材質重み」まで詰めている | `チャンクの地表材質の重みを返す` |
| routes/chunk_splat_weights_put.rs:19 | `チャンク材質重みを保存する` | 同上 | 38行目の呼び出し先`チャンクの材質重みを検証して保存する`と表記が食い違う | `チャンクの地表材質の重みを保存する` |
| routes/world_heightmap_get.rs:12 | `大域世界高さ格子を返す` | 同上 | 13行目で呼ぶ`状態.保管庫().大域世界の高さ格子を読む`は「の」ありで書いている | `大域世界の高さ格子を返す` |
| routes/world_heightmap_put.rs:12 | `大域世界高さ格子を保存する` | 同上 | 29行目の呼び出し先`大域世界の高さ格子を検証して保存する`と表記が食い違う | `大域世界の高さ格子を保存する` |
| routes/source_asset_export_post.rs:77 | `fn ベイク失敗の応答(誤り: ...) -> Box<Response>` | 条件4(品詞の規律) | エラー値を`Box<Response>`へ変換する処理(`.map_err(ベイク失敗の応答)`で渡す関数)なのに、名前が「ベイク失敗の応答」という値の名詞句になっている。名前だけ見ると応答という値そのものに見え、変換処理であることが読めない | `ベイク失敗を応答へ写す` |
| routes/world_layout_lookup.rs:18 | `fn 区画割り未設定応答() -> Response` | 条件4 | `.ok_or_else(|| Box::new(区画割り未設定応答()))`で呼ばれる、応答を組み立てる処理なのに名詞句のまま。同じファイルの`保存済み区画割りを読む`は正しく動詞句である | `区画割り未設定の応答を組み立てる` |
| routes/static_serve.rs:19 | `async fn 未ビルド応答() -> impl IntoResponse` | 条件4 | axumのfallbackハンドラ(`ルーター.fallback(未ビルド応答)`)であり、他の全ハンドラ(`生存確認を返す`・`楽曲を返す`等)が動詞句であるのに、これだけ名詞句で登録されている | `未ビルド応答を返す` |

## 違反の疑い(条件6・ユビキタス言語の1語1義に対して)

- `routes/building_grid_put.rs:25` の私有関数`検査して保存する`と、`chunk_heightmap_put.rs`・`chunk_splat_weights_put.rs`・`world_heightmap_put.rs`・`music_put.rs`が使う`検証して保存する`は同じ「受け取った内容を確かめてから保存する」という工程を指すが、動詞が「検査」と「検証」で割れている。断定はしない。用語をユビキタス言語へ登録して一本化するかどうかは判断が要る
- `storage/file_repository/music.rs:38`の`読み込んだ楽曲の版::現在の形へ変換する`と、`chunk.rs:20`の`読み込んだチャンク構造の版::最新の形へ変換する`、`world.rs:14`の`読み込んだ大域世界構造の版::最新の形へ変換する`は、同じ「旧版を最新形式へ変換する」工程を指すが、`現在の形`と`最新の形`で語が割れている。読み手は両者が同じ操作か別の操作か迷いうる

## 違反ゼロの範囲

上記10件(違反8件+疑い2件)を除く識別子群は受理条件8条・命名の質・呼称と語の選択のいずれにも抵触しなかった。特にエラー型の枝(`読み込みエラー`・`保存要求エラー`の全枝)、値オブジェクト様式の`生成する()`系関連関数、`検証して保存する`系の統一命名、`世界の区画割り`・`マザーハイトマップ`・`チャンク座標`・`建物の格子`等のユビキタス言語登録済み型名、`〜か`で終わる真偽メソッド(`エディターの世界か`)は全件合格。ASCII英字・ギリシャ文字・数学記号の識別子への混入は0件だった。

## 検査対象ファイル一覧(35件、全件直読)

routes/building_grid_get.rs, routes/building_grid_put.rs, routes/building_list_get.rs, routes/building_outline_catalog_get.rs, routes/chunk_heightmap_get.rs, routes/chunk_heightmap_put.rs, routes/chunk_splat_weights_get.rs, routes/chunk_splat_weights_put.rs, routes/chunk_structure_get.rs, routes/chunk_structure_put.rs, routes/health_get.rs, routes/material_board_get.rs, routes/material_board_put.rs, routes/mod.rs, routes/music_get.rs, routes/music_list_get.rs, routes/music_put.rs, routes/project_info_get.rs, routes/source_asset_export_post.rs, routes/static_serve.rs, routes/world_heightmap_get.rs, routes/world_heightmap_put.rs, routes/world_layout_lookup.rs, routes/world_structure_get.rs, routes/world_structure_put.rs, storage/file_repository/chunk.rs, storage/file_repository/data_directory.rs, storage/file_repository/file_names.rs, storage/file_repository/json_io.rs, storage/file_repository/material_board.rs, storage/file_repository/mod.rs, storage/file_repository/music.rs, storage/file_repository/world.rs, storage/mod.rs, storage/response.rs
