# 命名検査報告: 区画078 crates/blitz_render/src/atmosphere/bake

- 区画名と範囲: crates/blitz_render/src/atmosphere/bake（region 078。実際にはbake/geometry/integration/mapping/mediumの5サブディレクトリ、計40ファイルが対象一覧に含まれる）
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090
- 直読したファイル数: 40 / 40（grepだけで済ませたファイルは0件。全ファイルをcat -nで全文読んだ）
- 検査対象: 型名・関数名・メソッド名・フィールド名・変数名・定数名・構造体名のうち日本語識別子
- 参照した用語定義: `_doc/計画/ユビキタス言語.md`のベイク済み画像・光学距離・透過率RGB・消散係数RGB・消散媒体・大気散乱媒体・密度分布・規格化密度・大気内観測点・天頂余弦・散乱角余弦・位相関数値・遠方環境・天空放射輝度供給・太陽相対フレーム等の定義

## 違反の一覧

該当なし(0件)。

## 違反の疑い

- crates/blitz_render/src/atmosphere/bake/multiscatter_lut.rs:60 | `合成する` | 条件1(受理テスト)・条件3(5W1Hの必要格)に対する疑い | 私有の自由関数(`fn 合成する(放射輝度: [f64; 3], 残存率: [f64; 3]) -> Result<多重散乱RGB, 大気数学エラー>`)であり、名前だけでは「何を合成して何を得るか」が読めない(呼び出し側の引数名`放射輝度`・`残存率`を一緒に読んで初めて復元できる)。呼び出し箇所(38行目)が同じ小さいファイル内にあり実害は小さいが、条件1の受理テスト「〜する処理です」に単独で答えると「合成する処理です」としか言えず、何と何をどう合成するかが抜ける。断定はしない(呼び出し元の直前の変数名で読めるという反論があり得るため) | 改名の素案: `放射輝度と残存率から多重散乱を合成する`、または`等比級数を閉じて多重散乱を求める`(実体によりリネーム員が選定)

## 違反ゼロの範囲

- crates/blitz_render/src/atmosphere/bake/(aerial_condition.rs, aerial_lut.rs, distant_environment_lut.rs, multiscatter_lut.rs[上記1件の疑いを除く], skyview_condition.rs, skyview_lut.rs, transmittance_lut.rs)の7ファイル
- crates/blitz_render/src/atmosphere/geometry/(frustum_rays.rs, intersect.rs, sphere_directions.rs, vector3.rs)の4ファイル
- crates/blitz_render/src/atmosphere/integration/(aerial_march.rs, multiscatter_march.rs, multiscatter_series.rs, multiscatter_step.rs, optical_length.rs, phase.rs, skyview_march.rs, skyview_step.rs, sun_visibility.rs, transmittance.rs)の10ファイル
- crates/blitz_render/src/atmosphere/mapping/(aerial_mapping.rs, distant_environment_resolution.rs, lut_resolution.rs, multiscatter_mapping.rs, skyview_lookup.rs, skyview_mapping.rs, sun_relative_frame.rs, transmittance_mapping.rs, unit_texel.rs, lut_resolution/accessors.rs, lut_resolution/default_resolution.rs, lut_resolution/validate.rs)の12ファイル
- crates/blitz_render/src/atmosphere/medium/(density.rs, density_layer.rs, density_profile.rs, extinction_medium.rs, medium_sample.rs, observation_point.rs, scattering_medium.rs)の7ファイル

上記40ファイル中39ファイルは違反0件、1ファイル(multiscatter_lut.rs)に疑い1件のみを検出した。この区画は物理量(消散係数・光学距離・透過率・位相関数・密度分布等)を扱う数学の正本であり、識別子はユビキタス言語の登録語句とほぼ1対1に対応していた。値オブジェクト様式の`生成する()`・`検証済みの値から()`は型名が目的語を担うため全件合格。ASCII識別子(crate名・ファイル名・shaders/*.slangへの参照)は補完力ゼロとして扱い、それらへ依存する日本語名は検出しなかった。識別子への英字・ギリシャ文字・数学記号の混入(A2・Σλ_n等の形)は0件。実体との不一致(オーナー不合格実例の3形: 実体と合わない語・名詞1語の圧縮・詰め込みすぎ)も検出しなかった。
