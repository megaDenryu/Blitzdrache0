# ローラー検査(命名の軸) 区画155: xtask/src/gen_game_map ほか

- 区画番号: 155
- 対象一覧: `C:/Users/POKR30~1/AppData/Local/Temp/claude/C--devs-Blitzdrache0/612e37aa-a38d-4d29-a7a3-b0e9508d9f15/scratchpad/roller/block_155.txt`(40ファイル。実体は`xtask/src/gen_game_map`2件、`xtask/src/gen_source_assets/supply`14件、`xtask/src/hdr_luminance`7件、`xtask/src/ibl_step`17件)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, db815090時点。編集していない)
- 検査した規約: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「専門用語の表記」「呼称と語の選択」「日本語命名の質」「日本語命名の受理条件」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`
- 直読したファイル数: 40/40(全ファイルを直読。grepだけで済ませたファイルは0件)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| xtask/src/hdr_luminance/statistics.rs:22-26 | `輝度統計`構造体のフィールド`最小のev`・`p1のev`・`p50のev`・`p99のev`・`最大のev` | 識別子への英字混入の禁止(受理条件の前提。オーナー実例の「A2」型と同型) | 「の」で日本語の格を付けているのに、値の核が`ev`という小文字ASCII2文字と`p1`/`p50`/`p99`という英字+数字の組であり、非専門家がこの識別子だけから「EV(露出値)の百分位」を復元できない。ユビキタス言語には「EV」は単位としての説明文中の記述はあるが、識別子の一部として登録された語ではない | `最小のev`→`最小の露出段`、`p1のev`→`露出段のp1`ではなく`露出段の下側1パーセンタイル`のように、露出段(EV)とパーセンタイルを別の日本語句へ開く |
| xtask/src/hdr_luminance/judgment.rs:14-20 | `統計.最小のev`等5箇所での参照 | 同上(参照であり定義は上と同じ) | 同上のフィールド名をそのまま読んでいるため、定義を直せば連動する | 定義側の改名に追従する |
| xtask/src/hdr_luminance/table.rs:9-22, 27-37 | `統計.最小のev`等の参照(表示・要約の組み立て) | 同上 | 同上 | 定義側の改名に追従する |
| xtask/src/ibl_step/difference.rs:17-19 | `領域の段差`構造体のフィールド`明るさのev一覧`・`色のev一覧` | 識別子への英字混入の禁止 | `ev`が小文字ASCIIのまま日本語の格助詞に挟まっており、「明るさの露出段の一覧」であることを非専門家がこの綴りだけから読めない | `明るさのev一覧`→`明るさの露出段一覧`、`色のev一覧`→`色の露出段一覧` |
| xtask/src/ibl_step/summary.rs:36-44, 59-63 | `領域の集計`構造体のフィールド`明るさのp95の最大`・`色のp95の最大`、および`取り込む`メソッド内の同名アクセス | 識別子への英字・数字混入の禁止 | `p95`が英字+数字のまま名詞句の中に挟まり、「95パーセンタイル」であることを式の綴りからしか読めない(専門用語の表記の初出補足も無い) | `明るさのp95の最大`→`明るさの95パーセンタイル値の最大`、`色のp95の最大`→`色の95パーセンタイル値の最大` |
| xtask/src/ibl_step/summary/distribution.rs:13-16 | `分布`構造体のフィールド`p95` | 識別子への英字・数字混入の禁止(この40ファイル中もっとも重い形。フィールド名が日本語の格助詞すら伴わない裸のASCII+数字) | 型定義を単独で見ても「p95」がここでは「95パーセンタイル値」であることが日本語として一切読めない | `p95`→`九十五パーセンタイル値`または`分布`型のもう一方のフィールド`最大`に倣い`パーセンタイル95`ではなく`95パーセンタイル値` |
| xtask/src/ibl_step/measure.rs:31, 48 | ローカル変数`明るさのp95一覧` | 識別子への英字・数字混入の禁止 | 同上のp95問題がローカル変数にも伝播している | `明るさの95パーセンタイル値一覧` |
| xtask/src/ibl_step/scan.rs:63, 73 | ローカル変数`領域ごとのp95`・`地面のp95` | 識別子への英字・数字混入の禁止 | 同上 | `領域ごとの95パーセンタイル値`・`地面の95パーセンタイル値` |
| xtask/src/gen_source_assets/supply/generator_launch.rs:24-25(参照: supply.rs:45, 57) | 型`生成器のexampleをcargoから起こす係` | 日本語命名の質(英語の直訳調・単語らしく見せるための助詞省略は無いが、英単語をそのまま名詞として日本語の格助詞に挟んでいる)/受理条件2(英語識別子は読まれないものとして扱う対象そのものが、ここでは自分自身の識別子の中に埋め込まれている) | `example`は`cargo run --example`というCargoの機能名の直訳借用であり、Rustのビルドツールを知らない読み手には「起こす対象がなぜexampleという英単語なのか」が復元できない。`cargo`は固有名詞(ビルドツール名)として許容できるが、`example`は普通名詞であり訳し漏れである | `生成器のexampleをcargoから起こす係`→`生成器の実行例をcargoから起こす係`(「実行例」で置き換え、cargoは固有名詞として維持) |

## 違反の疑い

- xtask/src/ibl_step/control.rs:17-21 `対照の対象<'呼び名>`のライフタイム引数`'呼び名`が、実際に束縛しているのは`方向: &'呼び名 str`フィールドであり、「呼び名」という語の実体(点のニックネーム、picture.rs側の`代表点.呼び名`で使われる意味)とここでの用途(方向を表す文字列の生存期間)が食い違って見える。ただし本条の受理条件が対象に挙げるのは型・関数・メソッド・フィールド・変数・定数・列挙の枝であり、ライフタイム引数がここに含まれるかは明確でないため、断定はしない(条件7に対しての疑い)

## 違反ゼロの範囲

- `xtask/src/gen_game_map/error.rs`・`xtask/src/gen_game_map/source_write.rs`の2件は全識別子が受理条件8つを満たす
- `xtask/src/gen_source_assets/supply.rs`本体および`supply/buffer_group.rs`・`buffer_presence.rs`・`document_path.rs`・`document_snapshot.rs`・`error.rs`・`fingerprint.rs`・`fingerprint_ledger.rs`・`counting_launch.rs`・`document_agreement_tests.rs`・`invariant_tests.rs`・`mismatch_tests.rs`・`test_scene.rs`の12件は、上表の`generator_launch.rs`定義への参照2箇所(supply.rs内)を除き違反なし
- `xtask/src/hdr_luminance/error.rs`・`judgment.rs`(判定名の組み立て部分)・`record.rs`・`run.rs`・`error/display.rs`の5件、および`ibl_step`配下の`control.rs`・`crossing.rs`・`error.rs`・`judgment.rs`・`picture.rs`・`record.rs`・`run.rs`・`scan.rs`・`crossing/parse.rs`・`crossing/parse_tests.rs`・`error/display.rs`・`picture/representative_point.rs`・`scan/bundle_shot.rs`の13件は、上表で挙げた`p95`/`ev`系の参照を除き違反なし
