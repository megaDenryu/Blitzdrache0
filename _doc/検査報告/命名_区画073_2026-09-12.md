# 命名検査報告 区画073

- 区画名: crates/blitz_math/src/frame 第2区画(区画番号073、10ファイル)
- 対象ツリー: C:/devs/Blitzdrache0_roller(main の db815090。編集せず走査のみ)
- 検査日: 2026-09-12
- 直読したファイル数: 10 / 10(全ファイルを直読。grepだけで済ませたファイルは0件)
- 対象ファイル:
  1. crates/blitz_math/src/frame/transform_construct_tests.rs
  2. crates/blitz_math/src/frame/trs.rs
  3. crates/blitz_math/src/frame/trs_tests.rs
  4. crates/blitz_math/src/frame/two_tier_position.rs
  5. crates/blitz_math/src/frame/two_tier_position_accumulation_tests.rs
  6. crates/blitz_math/src/frame/two_tier_position_algebra.rs
  7. crates/blitz_math/src/frame/two_tier_position_error.rs
  8. crates/blitz_math/src/frame/two_tier_position_tests.rs
  9. crates/blitz_math/src/frame/vector_quantity.rs
  10. crates/blitz_math/src/frame/velocity.rs

## 検査手順

`.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、および`_doc/計画/ユビキタス言語.md`を読んだ上で、上記10ファイルの型・関数・メソッド・フィールド・変数・定数・列挙の枝を全件検査した。特に「二段の位置」「粗い位置」「細かい位置」「境界用に畳んだ位置」「基準原点を選ぶための粗い位置」はユビキタス言語.md(202〜205行、1065行)の定義文と1語ずつ照合した。

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_math/src/frame/transform_construct_tests.rs:18 | `期待ndc_zを計算する` | 日本語命名の質(英語の頭字語・省略語を日本語へ混ぜない)+識別子へのASCII混入の即不合格基準 | `ndc_z`はNormalized Device Coordinates(正規化デバイス座標)のzという英語のsnake_case断片がそのまま日本語識別子へ埋め込まれている。用語定義ファイルにNDCの登録・訳語が無く、ファイル内にも初出補足が無いため、英語を知らない読み手は「ndc」が何かを一切復元できない。「段階A2」型の即不合格基準(識別子へASCII英字を混ぜたもの)に該当する | `期待する正規化デバイス座標のzを計算する`(初出時にNDCの訳語をユビキタス言語.mdへ登録する) |
| crates/blitz_math/src/frame/transform_construct_tests.rs:19-23 | ローカル変数 `a`・`b`・`z_eye`・`clip_z`・`clip_w` | 識別子へのASCII混入の即不合格基準+受理テスト(「これは何?」に単独で答えられない) | `a`・`b`は数式の係数を1文字の英字へ切り詰めており単独で何の量か答えられない。`z_eye`・`clip_z`・`clip_w`は英語のsnake_caseがそのまま日本語ファイルの中で使われ、周囲が全て日本語識別子であるため一貫性も崩れている | `係数甲`のような記号化ではなく`視点空間のz`・`クリップ空間のz`・`クリップ空間のw`等、量の意味を書いた日本語名にする |
| crates/blitz_math/src/frame/trs_tests.rs:8 | `slerpは端点で一致する` | 日本語命名の質(英語の頭字語・省略語を日本語へ混ぜない)+専門用語の表記(初出時の補足が無い) | `slerp`は球面線形補間(Spherical Linear Interpolation)の略であり、ユビキタス言語.mdに登録が無く、ファイル内にも初出補足が無い。英語を知らない読み手はこの試験が何を検証しているか名前から復元できない | `球面線形補間は端点で一致する` |

## 違反の疑い(条件7・日本語命名の質に対して。断定しない)

| ファイル:行 | 識別子 | 疑いの条件 | 理由 |
|---|---|---|---|
| crates/blitz_math/src/frame/trs.rs:13 | `trsから生成する` | 日本語命名の質(頭字語を日本語へ混ぜない) | `TRS`はユビキタス言語.md 489行に「平行移動(Translation)・回転(Rotation)・スケール(Scale)」と英語展開込みで登録済みの用語であり、`trs.rs`冒頭のモジュールコメントでも同じ補足がある。用語定義ファイルへの登録と初出補足はあるため「専門用語の表記」自体は満たすが、識別子そのものには展開形が現れず、頭字語(LUT等と同型)をそのまま識別子へ混ぜている点は「日本語命名の質」の条文に照らすと未解決である。プロジェクト全体(スケルタルアニメーション層)で既に定着した表記であり、一括改名は「一括リネームはしない」の運用にも反するため、断定せず疑いとして報告する |
| crates/blitz_math/src/frame/trs_tests.rs:17 | `恒等クォータニオンのtrs合成は単位行列になる` | 同上 | 同上の`trs`混入。テスト関数名であり公開APIではないが、識別子である点は同じ |
| crates/blitz_math/src/frame/trs_tests.rs:29 | `trs合成は既知の点を正しく写す` | 同上 | 同上 |

## 違反ゼロの範囲

- `two_tier_position.rs`・`two_tier_position_algebra.rs`・`two_tier_position_error.rs`・`two_tier_position_tests.rs`・`two_tier_position_accumulation_tests.rs`の5ファイル(型`二段の位置`とその演算・エラー・試験一式)は、識別子(`粗い位置`・`細かい位置`・`境界用に畳んだ位置`・`基準原点を選ぶための粗い位置`・`倍精度で評価した位置`・`繰り上げて生成する`・`変位を足して繰り上げる`等)をユビキタス言語.md 202〜205行・1065行の定義文と1語ずつ照合し、実体(不変条件・演算の対象)と語が一致することを確認した。品詞規律(値は名詞句、処理は動詞句)、5W1H(目的語・対象格)、1語1義のいずれも違反なし
- `vector_quantity.rs`のマクロ`三成分の量を定義する`とその生成する識別子(`成分から生成する`・`方向へ生成する`・`方向に沿う成分`・`内部から生成する`・`内部ベクトル`)は違反なし
- `velocity.rs`の型`速度`とメソッド(`単位方向`・`方向の成分を除く`)、次元合成の演算子実装は違反なし
- `trs.rs`・`trs_tests.rs`・`transform_construct_tests.rs`のうち、上記で指摘した`trs`・`slerp`・`ndc_z`・数式変数を除く識別子(`ほぼ等しいか`・`合成する`・`適用する`・`逆変換する`・`注視`・`逆向き深度の透視投影`等)は違反なし

## 省略の有無

省略はしていない。10ファイル全件、全識別子を直読で検査した。
