# 命名検査報告: 区画060(crates/blitz_engine/src/sky)

- 区画名: crates/blitz_engine/src/sky
- 範囲: block_060.txt に列挙された38ファイル(sky直下。sky/atmosphere配下のサブモジュール17ファイルは本区画に含まれないため対象外)
- 作業ツリー: C:/devs/Blitzdrache0_roller
- 時点: main の db815090
- 直読したファイル数: 38/38(全ファイルを直読。grepだけで済ませたファイルは0件)

必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`。

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_engine/src/sky/indirect_lighting_policy.rs:30 | `遠方環境LambertGGX`(照明問い合わせ契約の枝。定義はblitz_render側だが本ファイルで消費・分岐している) | 日本語命名の質(英語頭字語の混入禁止)/受理条件7(直訳・専門語の未訳) | `LambertGGX`はランバート拡散反射モデルとGGXマイクロファセット鏡面モデルという2つの専門アルゴリズム名をそのままASCIIで連結したものであり、非専門家はおろかCGの専門用語を知らない読み手には意味が全く復元できない。ユビキタス言語.md本文にも同じ綴りで登録されているが、Vulkan仕様書・validation layerの指摘文と一致させる必要がある「第3群語彙」(BC1・スワップチェーン等)には該当しない社内造語であり、翻訳を免除される理由が無い | `遠方環境の乱反射と鏡面反射の合成`、または初出時に括弧で「LambertGGX(ランバート拡散反射とGGX鏡面反射を合成する近似)」と補足する |
| crates/blitz_engine/src/sky/sun_zenith_crossing.rs:76 | `収める(一覧, 上側と方向, 下側と上側の秒, 方針と刻み)`(モジュール直下の自由関数) | 受理条件2(ASCIIモジュール直下の自由関数は名前単独で答える)/受理条件5(裸の動詞の禁止) | `sun_zenith_crossing`はASCIIモジュールであり補完力ゼロ。`収める`は「何を・どこへ収めるか」を名前が一切示さず、単独では「これは何?」に答えられない。第一引数`一覧`が対象であることは呼び出し側の実引数を読まないと分からない | `一覧へ跨ぎを追加する`、または`未収録の跨ぎだけ一覧へ足す` |
| crates/blitz_engine/src/sky/continuity_tests.rs:17 | `導出する(秒数: f64) -> 天空状態`(テストモジュール直下の自由関数) | 受理条件2/受理条件5 | `continuity_tests`はASCIIモジュール名であり補完力ゼロ。`導出する`は何を導出するのかを名前に持たず、戻り値の型(`天空状態`)を見て初めて分かる | `秒数から天空状態を導出する` |
| crates/blitz_engine/src/sky/derive_tests.rs:14 | `導出する(時: f64) -> 天空状態`(テストモジュール直下の自由関数) | 受理条件2/受理条件5 | 上と同じ理由。`derive_tests`もASCIIモジュール | `時から天空状態を導出する` |
| crates/blitz_engine/src/sky/sun_symmetry_tests.rs:19 | `導出する(時: f64) -> 天空状態`(テストモジュール直下の自由関数) | 受理条件2/受理条件5 | 上2件と同じ理由。3ファイルで同型の裸動詞が繰り返されている | `時から天空状態を導出する` |
| crates/blitz_engine/src/sky/policy_tests.rs:70 | `段(値: f32) -> 露出補正段`(テストモジュール直下の自由関数) | 受理条件4(品詞の規律。処理なのに名詞形)/呼称と語の選択(漢字圧縮に近い切り詰め) | `露出補正段`という完全な語を1文字`段`へ切り詰めた形であり、`着手順4`を`順4`と呼んだ実例と同型。呼び出し側`段(0.0)`だけを読んでも何の段か分からない | `露出補正段を作る` |
| crates/blitz_engine/src/sky/policy_tests.rs:74 | `強度(値: f32) -> 太陽光強度`(テストモジュール直下の自由関数) | 受理条件4/受理条件1 | `太陽光強度`から`太陽光`を落とした切り詰め。単独では何の強度か特定できない(環境光強度・露出補正段も同じ文脈に登場する) | `太陽光強度を作る` |
| crates/blitz_engine/src/sky/policy_tests.rs:78 | `白() -> 太陽光色`(テストモジュール直下の自由関数) | 受理条件1/受理条件4 | 引数無しの名詞1語で、何の白か(太陽光色の白であること)を名前が示さない。品詞は名詞だが実体は値を組み立てる処理である | `白い太陽光色を作る` |
| crates/blitz_engine/src/sky/sun_zenith_crossing_tests.rs:15 | `刻み(区間の数: u16) -> 間引きの刻みの設定`(テストモジュール直下の自由関数) | 受理条件4/呼称と語の選択(切り詰め) | `間引きの刻みの設定`を`刻み`へ切り詰めた形。処理(値オブジェクトの生成)なのに名詞形になっている | `間引きの刻みの設定を作る` |
| crates/blitz_engine/src/sky/sun_zenith_crossing_tests.rs:19 | `天頂余弦(一日内秒: f64, 方針: &空と太陽の方針) -> f32`(テストモジュール直下の自由関数) | 受理条件4 | 引数から天頂余弦を計算する処理なのに名詞1語で、動作(「一日内秒における太陽の天頂余弦を求める」)が名前に無い | `一日内秒における太陽の天頂余弦を求める` |
| crates/blitz_engine/src/sky/sun_zenith_crossing_tests.rs:24 | `識別(一日内秒: f64, 方針: &空と太陽の方針, 設定: 間引きの刻みの設定) -> u16`(テストモジュール直下の自由関数) | 受理条件4/呼称と語の選択(切り詰め) | `区間識別`から`区間`を落とした切り詰め。処理なのに名詞形 | `区間識別を求める` |
| crates/blitz_engine/src/sky/sun_zenith_interval_tests.rs:11 | `刻み(区間の数: u16) -> 間引きの刻みの設定`(テストモジュール直下の自由関数) | 受理条件4/呼称と語の選択(切り詰め) | `sun_zenith_crossing_tests.rs:15`と同型・同名の重複違反 | `間引きの刻みの設定を作る` |
| crates/blitz_engine/src/sky/sun_zenith_interval_tests.rs:15 | `識別(天頂余弦: f32, 区間の数: u16) -> u16`(テストモジュール直下の自由関数) | 受理条件4/呼称と語の選択(切り詰め) | `sun_zenith_crossing_tests.rs:24`と同型の重複違反 | `区間識別を求める` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| crates/blitz_engine/src/sky/default_constant.rs:7 | `確定値<T>(結果: Result<T, 天空状態エラー>) -> T` | 受理条件4 | パニックを内包する処理だが、戻り値の型がそのまま返る通常経路が主であるため名詞形自体は成立しうる。ただし「確定」という語だけでは失敗時にパニックすることが読み取れない |
| crates/blitz_engine/src/sky/derive_tests.rs:19 | `秒で導出する(秒数: f64) -> 天空状態` | 受理条件3・5 | 「秒で」により時刻の単位を区別する格は補っているが、導出対象(天空状態)を名前が持たない。同ファイルの`導出する`(時間表記版)と対で読めば区別できるが、単独では確定しにくい |

## 違反ゼロの範囲

上記以外の識別子(型名・値オブジェクトの`生成する()`/`導く()`などの関連関数・アクセサ・enumの枝・フィールド・定数)は受理条件8条を全て満たしていた。特に確認した点:

- 値オブジェクト様式の`生成する()`・`導く()`は型名(`環境光強度::生成する`・`空中遠近キー::導く`等)が目的語を担うため合格(受理条件5の例外に一致)
- `地表反射率RGB`・`消散係数RGB`・`透過率RGB`等のRGBを含む識別子はユビキタス言語.md本文で1語1義として既に登録・多数の型で一貫使用されており、RGBは非専門家にも通用する一般語のため違反としない
- `スカイビュー`はユビキタス言語.md「遠方環境用スカイビュー画像」等で登録済みのカタカナ語であり、日本語命名の質の直訳調禁止には当たらない
- `跨ぎの方向::呼び名()`のASCII文字列("up"/"down")はファイル名・報告語彙という明示された用途があり、識別子自体は日本語(`呼び名`)であるため違反としない
- ギリシャ文字・数式記号(Σλ_n等)や`A2`のような段階番号の混入は本区画では確認されなかった
