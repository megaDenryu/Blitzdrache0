# 命名検査報告(区画052)

- 区画名: crates/blitz_engine/src/asset/instance ほか(一覧ファイルの実際の範囲は asset/instance・asset/material_data/surface_layer_blend・asset/pbr_material_data・asset/runtime_file・asset/runtime_format 配下)
- 範囲: block_052.txt に列挙された40ファイル(区画番号052)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main の db815090。このツリーは編集していない)
- 時点: 2026-09-12
- 直読したファイル数: 40 / 40(grepだけで済ませたファイルは0件。全ファイルを全文直読した)

## 違反の一覧

該当なし。

## 違反の疑い

該当なし。判断が割れそうな候補として以下を検討したが、いずれも受理条件を満たすと判定した。

- `error.rs` 内の `地形LOD段なし`・`地形LODメッシュ群不正`(runtime_format/error.rs)、`原型`のコメント中の「メッシュLOD列」等: `LOD`は `_doc/計画/ユビキタス言語.md`(215行目)に定義済みの登録語であり、「専門用語の表記」の受理基準(非専門家が読んで意味が取れるか)を満たす定義済み語として扱った。英字だが専門用語の表記節が許す一般に通用している語に準じる登録済み語のため違反としない
- `カタログV3`・`カタログ項目V3`・`メタデータV1`・`メタデータV2`(catalog/v1.rs, v2.rs, v3.rs内部の版限定の私有型): 型名に`V1`等のASCII版番号が付くが、これらは版付き実行時形式の版番号(`アセット形式版::V1`等)と1対1で対応する私有のモジュール内部型であり、ファイル名自体が`v1.rs`等の版番号を示すASCIIモジュールである。読み手はコメント冒頭で「版1の〜」と必ず一緒に読む配置であり、漢字1文字への圧縮(順4・段3型の禁止)には当たらない。版番号は列挙的な識別子であり自然言語の語ではないため、「呼称と語の選択」条1(漢字1文字圧縮の禁止)の対象外と判定した

## 違反ゼロの範囲

block_052.txt に列挙された40ファイル全域(crates/blitz_engine/src/asset/instance、asset/material_data/surface_layer_blend、asset/pbr_material_data、asset/runtime_file、asset/runtime_format およびそのサブモジュール catalog/v1〜v4 とその conversion_tests・write)で、日本語命名の受理条件8条・命名の質・呼称と語の選択のいずれの違反も検出しなかった。識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)は、値=名詞句/処理=動詞句/真偽=「〜か」の品詞規律を保ち、動詞名は「何を+どうする」の格を持ち(例: `被覆を検査する`・`原型を球が覆うことを検査する`・`個体を包囲領域が覆うことを検査する`)、構造体の関連関数`生成する()`はすべて型名が目的語を担う値オブジェクト様式であった。ASCII識別子(crate名・モジュールファイル名・版番号ラベル)への意味の補完依存や、直訳調・膠着圧縮・漢字1文字圧縮・英字ギリシャ文字記号の識別子混入も検出しなかった。
