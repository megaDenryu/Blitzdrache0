# 命名検査報告: 区画067 (crates/blitz_engine/src/visibility_tests + world_shape_query)

## 範囲・時点

- 区画番号: 067
- 区画名: `crates/blitz_engine/src/visibility_tests`(10ファイル) と `crates/blitz_engine/src/world_shape_query`(20ファイル)。合計30ファイル
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の db815090時点。このツリーは編集していない)
- 検査日: 2026-09-12
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`(「世界の形と衝突の語彙」「オープンワールドの語彙」節を中心に照合)

## 検査した件数

- 直読したファイル数: 30/30(全ファイル)。grepだけで済ませたファイルは0件
- 検査した識別子: 型・enum・関数・メソッド・フィールド・列挙の枝・定数・主要なローカル変数を全ファイルで目視。概数は約260個(構造体・enum定義20件強、関数・メソッド約80件、フィールド・枝約90件、定数・主要変数約70件)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_engine/src/visibility_tests/group_bounds_degenerate.rs:23` | `全距離区分定数がnanの直方体` | 受理条件2(ASCII識別子は補完力ゼロ)・日本語命名の質(英語の頭字語・省略語を日本語へ混ぜない) | 識別子の中に小文字の英単語`nan`(NaNの略記)がそのまま埋め込まれている。プロジェクトの用語定義(`_doc/計画/ユビキタス言語.md`)はこの概念を「非有限」「非数」という確立した日本語で扱っており(157行・186行・187行など)、同じファイル内の姉妹関数`距離区分0だけ壊れた直方体`(light_box_degenerate.rs)は英語を使わずに書けている。英語を知らない読み手は`nan`が何を指すか単独で復元できない | `全距離区分の定数が非数の直方体` または `全距離区分の定数が非有限な直方体` |

違反は1件。

## 違反の疑い

| ファイル:行 | 識別子 | 落ちた条件 | 疑いの内容 |
|---|---|---|---|
| `crates/blitz_engine/src/visibility_tests/fixture.rs:80` | `球一覧から材料` | 条件4(品詞の規律。処理=動詞句) | 個体境界球の列から群可視材料を組み立てる処理だが、名前が「〜から材料」という体言止めで動詞を持たない。同じファイルの`材料(...)`・`球(...)`・`基準原点(...)`も同型の体言止めであり、これらは「値そのものを表す名前」として一貫した書式(値=名詞句)であるため、この一貫性を根拠に不合格までは断定しない。ただし「〜から」という格が入り、単なる値の呼称より変換の処理を強く感じさせるため、疑いとして残す。改名案(採用する場合): `球一覧から材料を組み立てる` |

疑いは1件。上記以外にも`fixture.rs`・`pass_interval.rs`・`world_shape_query/world_query_fixture.rs`にある体言止めの私有ファクトリ関数(`単位視錐台`・`透視視錐台`・`基準原点`・`四体の材料`・`大域位置`・`地形を持つ読み口`・`地形を持たない読み口`・`箱を1つ置いた保管`・`建物を置くチャンク`等)は、いずれも関数名がそのまま返す値の呼び名になっており、受理テスト(「これは何?」への単独回答)を満たすため違反扱いにしなかった。

## 違反ゼロの範囲

- `world_shape_query`配下の20ファイル全体(`building_capsule.rs`・`building_contact.rs`・`building_hit.rs`・`building_place.rs`・`building_segment.rs`・`capsule_hit.rs`・`capsule_query.rs`・`capsule_world_tests.rs`・`composition.rs`・`error.rs`・`local_frame.rs`・`query.rs`・`segment_hit.rs`・`segment_query.rs`・`segment_world_tests.rs`・`truncated_completeness_tests.rs`・`truncated_confirmation.rs`・`world_query_fixture.rs`)。型名・フィールド名(`カプセルと建物の接触`の`動けた割合`・`接触の大域位置`・`接触の単位法線`・`所在`・`触れた特徴`等)が`_doc/計画/ユビキタス言語.md`「世界の形と衝突の語彙」の登録句とほぼ逐語で一致しており、実体と名前の食い違い・名詞1語への圧縮・条件の詰め込みすぎのいずれも検出しなかった
- `crates/blitz_engine/src/world_query/`配下2ファイル(`completeness.rs`・`result.rs`)。`問い合わせ完全性`・`世界問い合わせ結果`とも登録語彙と一致し、列挙の枝`対象範囲を完全に評価できた`・`評価できない領域を含む`も実体(2軸のうちの完全性軸)と一致
- `visibility_tests`配下10ファイルのうち、上記1件の違反と1件の疑いを除く全識別子(`可視個体と段の選別係`・`可視判定計数`・`距離区分別の可視判定`・`個体LOD選択設定`等)。`LOD`は`_doc/計画/ユビキタス言語.md`215行で明示的に定義済みの登録語であり、頭字語混入の違反に当たらない。`id`(`区間のid`・`id列`等)も同文書257行の登録語「可視ID列」に対応する略記として扱い、違反に当たらないと判定した
