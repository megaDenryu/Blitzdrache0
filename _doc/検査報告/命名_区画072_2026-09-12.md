# 命名検査報告: 区画072「crates/blitz_math/src/frame 第1区画」

- 区画番号: 072
- 対象範囲: `crates/blitz_math/src/frame/` 配下の40ファイル(一覧は `block_072.txt` のとおり)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の `db815090` 時点)。このツリーは編集していない
- 直読したファイル数: 40 / 40(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 直読した識別子: 型・関数・メソッド・フィールド・変数・定数・列挙の枝を全数、日本語命名の受理条件8条・日本語命名の質・呼称と語の選択・ユビキタス言語との照合で検査した

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `inertia_tensor.rs:42-47` | ローカル変数 `ω`・`h`(`ジャイロ項を陰的に一段解く`の本体) | 識別子へのギリシャ文字・ASCII英字の混入禁止(オーナー実例「Σλ_n」と同型) | `ω`はギリシャ文字1文字、`h`はASCII1文字であり、どちらも小学生は言うに及ばず日本語の非専門家が読んでも量の種類(角速度・刻み幅)を復元できない。コメントの数式表記としてなら許されるが、コードの識別子として使っている | `ω`→`角速度`、`h`→`刻み幅`(関数冒頭で`刻み幅.値()`を束ねた既存の命名と揃える) |
| `inertia_tensor.rs:52-54` | ローカル変数 `ω`(`ジャイロ項を陽的に一段足す`の本体) | 同上 | 同上 | `ω`→`角速度` |
| `rotation_dynamics.rs:38,41` | ローカル変数 `q`(`正規化しない微小回転の更新`) | 同上(ASCII英字1文字) | `q`はクォータニオンを指す英語の頭字1文字であり、読み手はレシーバの型名からしか復元できない(補完源が英語識別子頼みで受理条件2の限定に反する) | `q`→`元の内部クォータニオン`または`回転前` |
| `rotation_dynamics.rs:61-62` | ローカル変数 `q`(`短い弧の側`) | 同上 | 同上 | `q`→`内部クォータニオン` |
| `rotation_dynamics.rs:46,52` | ローカル変数 `q`(`微小回転として回転ベクトルへ`・`対数写像で回転ベクトルへ`) | 同上 | 同上 | `q`→`短い弧側のクォータニオン` |
| `rotation_dynamics_tests.rs:63-64` | ローカル変数 `q` | 同上 | 試験コードでも同じ理由で読めない | `q`→`四元数`または`求めた回転` |
| `rotation.rs:50` | メソッド `slerp` | 直訳・頭字語の禁止(条件7、日本語命名の質「英語の頭字語・省略語を日本語の中に混ぜない」) | `slerp`は"Spherical Linear Interpolation"の省略語で、英語を知らない読み手はこの綴りから「球面線形補間」を復元できない。同じ型の他のメソッドはすべて日本語である | `球面線形補間する` |
| `rotation.rs:50` | 仮引数 `t`(`slerp`の第2引数) | 識別子へのASCII英字の混入禁止 | `t`は補間係数を表す慣習記号だが、非専門家はこの1文字から「0で自分・1で相手」という意味を復元できない | `t`→`補間係数` |
| `rotation.rs:56` | メソッド `境界用xyzw配列` | 日本語命名の質「単語らしく見せるために助詞を省かない」(膠着圧縮)、識別子へのASCII混入 | `xyzw`という4文字の英字ラベルを日本語へ助詞なしで貼り付けており、「境界用の配列で中身がxyzwの並び」という関係が一目で読めない | `境界用のxyzw順配列`(または`境界用の実数4つ組(xyzw順)`のように格を明示) |
| `transform.rs:36` | メソッド `gpu境界用列優先配列` | 同上(膠着圧縮+ASCII混入。GPUは専門用語の表記の節で補足不要語だが、小文字の`gpu`を助詞なしで直結している点が問題) | 「gpu」+「境界用」+「列優先配列」が助詞なしで一続きになっており、どれがどこに掛かるか(GPU境界のための配列なのか、GPU用の列優先なのか)が一読で決まらない | `GPU境界用の列優先配列` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `transform_construct.rs:32,70`、`transform_construct_cube_face.rs:53` | ローカル変数 `y反転` | 日本語命名の質(膠着圧縮の疑い) | 「y」は座標軸のラベルとして本ファイル群全体で`x()`・`y()`・`z()`のアクセサ名に既に定着しており、非専門家にも軸の意味は伝わる。ただし「反転」との間に助詞が無く「y軸の反転」と読めるかは境界例である。型全体の慣習(x/y/z/wをアクセサ名に使う)との整合を優先し、違反と断定はしなかった |
| `direction.rs`・`position.rs`・`displacement.rs`ほか全体 | メソッド `x()`・`y()`・`z()`・`w()` | 識別子へのASCII英字混入 | 受理条件を機械的に当てると全違反になるが、x/y/z/wは3次元座標・四元数成分の世界共通表記であり、小学校の座標学習で使う軸名と同じ1文字であるため、オーナー実例が禁じた「段階A2」「Σλ_n」のような専門的圧縮とは性質が異なると判断した。40ファイルのほぼ全てで一貫して使われている確立済みの規約でもあり、本区画だけの個別違反として指摘するのは水増しに当たると考え、違反ではなく検討事項として記載するに留める |

## 違反ゼロの範囲

- 型定義とユビキタス言語(`_doc/計画/ユビキタス言語.md`)の突き合わせ: `角速度`・`回転衝撃`・`力`・`衝撃`・`回転力`・`慣性テンソル`・`逆慣性テンソル`・`回転ベクトル`・`面積ベクトル`・`カメラ相対位置`・`大域ワールド位置`はいずれも定義済みの語句と実装(単位・次元の合成・保持するメソッド)が一致しており、1語1義(条件6)の違反は無かった
- 値オブジェクト様式の関連関数`生成する()`・`内部から生成する()`はすべて型名またはレシーバが目的語を担っており、条件5(裸の動詞の禁止)の違反は無かった
- フィールド名・enumの枝(`符号付きの座標軸`の6枝、`座標変換エラー`・`方向エラー`・`クォータニオンエラー`・`画素内ずらしエラー`の各枝)はいずれも「これは何?」に単独で答えられ、条件1・3・4の違反は無かった
- 上記「違反の一覧」に挙げた10件・「違反の疑い」に挙げた2種類を除く、40ファイルの残り全識別子(型名・構造体フィールド・関連関数・メソッド・モジュール内自由関数・列挙の枝)には受理条件8条・日本語命名の質・呼称と語の選択のいずれの違反も見つからなかった
