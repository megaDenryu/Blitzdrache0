# ローラー検査(命名の軸) 区画158

- 区画名: xtask/src/material_check/multi_material_draw ほか(区画番号158)
- 範囲: block_158.txt に列挙された38ファイル(material_check配下25件、memory_sampling配下2件、motion_vector配下6件、multi_light_world配下2件、origin_invariance配下3件、ow3_dod配下2件)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 38 / 38(全ファイルを直読した。grepだけで済ませたファイルは無い)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」/ `_doc/計画/ユビキタス言語.md`

## 違反の一覧

1. `xtask/src/memory_sampling/sample.rs:9` | `バイト毎MIB` | 日本語命名の質(英語の頭字語・省略語を日本語の中に混ぜない) | `MIB`はメビバイトの略語であり、単位の分野を知らない非専門家には読めない。日本語(バイト毎)とASCII大文字の頭字語(MIB)が同じ識別子の中で衝突している | 改名素案: `バイト毎メビバイト`

2. `xtask/src/memory_sampling/sample.rs:13,14,15` | `ワーキングセットmib` / `プライベートmib` / `専用vrammib`(`メモリ標本`のフィールド) | 日本語命名の質(頭字語・省略語混入)/受理条件8(短さのために語を削っている) | 英語の単位略語`mib`(メビバイト)を日本語複合語の末尾へ助詞なしで膠着させており、単独では「ミブ」という未知の文字列にしか見えない。単位を示したいなら日本語で書く必要がある | 改名素案: `ワーキングセットのメビバイト数` / `プライベートメモリのメビバイト数` / `専用VRAMのメビバイト数`

3. `xtask/src/memory_sampling/sample.rs:31-35` | `ワーキングセットmib` / `プライベートmib` / `専用vramバイト` / `gpu標本数`(`解析する`内のローカル変数) | 日本語命名の質 | 2と同じ理由。`gpu標本数`は小文字ASCII`gpu`が日本語の中に混入し、「じーぴーゆー標本数」と読めるが単語境界が不明瞭 | 改名素案: `ワーキングセットのメビバイト数` / `プライベートメモリのメビバイト数` / `専用VRAMのバイト数` / `GPU標本の件数`

4. `xtask/src/memory_sampling/sample.rs:53-56` | `ワーキングセットmib` / `プライベートmib` / `専用vrammib`(`メモリ最大`のフィールド) | 日本語命名の質 | 1・2と同じ略語混入がpub(crate)フィールドにも及んでいる | 改名素案: 2と同じ

5. `xtask/src/ow3_dod/metrics.rs:9` | `validation件数`(`計測値`のフィールド) | 日本語命名の質(英語をそのまま日本語の中に混ぜない) | 略語ではなく英単語`validation`をそのまま日本語の`件数`へ接着している。英語を知らない読み手は「バリデーション」の意味(Vulkanの検証層の指摘)を復元できない | 改名素案: `検証層の指摘件数`(同じ意味を持つ`display`側の文言「実行で出た検証層の指摘」と合わせる)

## 違反の疑い

1. `xtask/src/material_check/multi_material_draw/count_judgment.rs:26` と `xtask/src/material_check/prop_multi_material_draw/judgment.rs:14,18` の `計数.validation件数` / `検証層の指摘: 判定の名前` 周辺 | 使っている`validation件数`という識別子の定義は本区画の外(`report_parse`モジュール、block_158に含まれない)にあり、本区画からは参照のみである。違反5と同一の識別子であるため、定義側の是正と合わせて直す必要がある。本区画内だけでは改名できない
2. `xtask/src/ow3_dod/metrics.rs:23-30` の `self.要約.gpu解除件数` / `self.要約.lod変更フレーム数` / `self.要約.lod変更時読込開始件数` / `self.要約.最大ramバイト数` / `self.要約.最大vramバイト数` | いずれも`ストリーミング要約報告`(`streaming_report`モジュール、block_158に含まれない)が定義するフィールドの参照であり、小文字ASCII(`gpu`・`lod`・`ram`・`vram`)が日本語へ混入している。違反1〜4と同型の頭字語混入だが、定義が本区画の外にあるため確定の違反としては挙げず疑いに留める
3. `xtask/src/motion_vector/run.rs:20` の `探査刻み`(`--lod-probe-step`に渡すローカル定数) | コメントでは「カメラを動かす条件の1フレームぶんの奥行き移動量(メートル)」と説明されているが、識別子単独では「何を探査するのか」「何の刻みか」が定まらず、受理テスト(「これは何?」への単独回答)にやや弱い。ただしモジュール私有の小さな定数であり、紛れの実害は小さいため断定はしない | 改名素案(参考): `カメラ前進の1フレーム移動量メートル`

## 違反ゼロの範囲

- `xtask/src/material_check/multi_material_draw/`(count_judgment.rs・error.rs・judgment.rs・run.rs・switch_judgment.rs・count_judgment/resource_table.rs・error/display.rs)の7ファイルは、`正準フォールバック`を含め全識別子が受理条件8つと日本語命名の質を満たす。`正準フォールバック`はユビキタス言語に登録済みの既存語(`正準フォールバックスロット`等)であり、「呼称と語の選択」条4の一括改稿の対象外である
- `xtask/src/material_check/prop_multi_material_draw/`(count_judgment.rs・error.rs・judgment.rs・material_pixels.rs・run.rs・error/display.rs・judgment/color_share.rs)の7ファイルは違反ゼロ
- `xtask/src/material_check/surface_layer_draw/`(error.rs・judgment.rs・layer_color.rs・run.rs・weight_pattern.rs・world_source.rs・error/display.rs・world_source/copy_from_assets.rs・world_source/write_chunk.rs)の9ファイルは違反ゼロ
- `xtask/src/motion_vector/`(condition.rs・error.rs・judgment.rs・observation.rs・run.rs・error/display.rs)の6ファイルは、上記「違反の疑い」3の1件を除き違反ゼロ
- `xtask/src/multi_light_world/`(run.rs・world.rs)の2ファイルは違反ゼロ
- `xtask/src/origin_invariance/`(centroid.rs・compare.rs・run.rs)の3ファイルは違反ゼロ(`画素重心`のフィールド`x`/`y`はASCIIだが数学座標の慣用表記であり日本語命名の対象外と判断した)
- `xtask/src/ow3_dod/run.rs`は違反ゼロ。`xtask/src/ow3_dod/metrics.rs`は違反5(`validation件数`)のみで、他の識別子(`計測値`・`読み取る`・`要約文`等)は違反ゼロ
