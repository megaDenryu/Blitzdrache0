# ローラー検査(命名) 区画109

- 区画: `crates/blitz_render/src/vulkan/pipeline_ledger` を含む39ファイル(pipeline_ledger・point_light_shadow_map・post_process・present_timing・readback・scene_draw_constants・shader_struct/read_error.rs)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の db815090。編集していない)
- 直読したファイル数: 39/39(grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

1. `crates/blitz_render/src/vulkan/pipeline_ledger/entry_table.rs:52` | `引く`(`パイプライン記載表::引く`) | 条7(直訳の検出) | `_doc/計画/ユビキタス言語.md`には登録が無いが、CLAUDE.mdが明記する「オーナー確定の訳語: lookup/sampleの『引く』→『参照する』」に該当する語そのもの。英語のlookupを知らないと「何を引く道具なのか」が復元できず、辞書や紐を引く意味と誤読しうる。呼び出し側(`table_tests/build_tests.rs`・`table_tests/reload_tests.rs`など)にも同じ名前が伝播している | `キーから実体を参照する`

2. `crates/blitz_render/src/vulkan/point_light_shadow_map/create.rs:45` | `続きを生成する` | 条1(受理テスト)・条3(5W1Hの必要格) | 「続き」が何の続きかを名前が答えられない。単独で読むと「(何かの)後に続くものを作る処理です」としか言えず、実体(ビュー群とサンプラーを作ってハンドルを集める工程)を復元できない。呼び出し元の`生成する`と役割が紛らわしい | `ビュー群とサンプラーを生成して集める`

3. `crates/blitz_render/src/vulkan/pipeline_ledger/device_supplier/family_dispatch.rs:16` | `生成する`(`family_dispatch::生成する`) | 条5(裸の動詞の禁止) | `family_dispatch`はASCIIモジュールであり、この位置の自由関数は名前単独で答える必要があるが、`生成する`は目的語を持たない。CLAUDE.mdが挙げる不合格の典型例「`create::生成する`」と同型(モジュール名がASCIIで、関連関数でもなく供給元を第1引数で受け取る自由関数であるため型名による目的語の補完も働かない) | `族とキーからパイプラインを生成する`

4. `crates/blitz_render/src/vulkan/post_process/create.rs:47` | `束を生成する` | 条1(受理テスト)・条3(5W1Hの必要格) | 「束」が何の束か名前だけでは分からない(実体は光のにじみ一式・明るさの圧縮一式・自動露出一式の3つ)。オーナー実例「連立」と同型の、動作対象を省いた名詞1語の圧縮 | `光のにじみと明るさの圧縮と自動露出の一式を生成する`

5. `crates/blitz_render/src/vulkan/post_process/inputs.rs:22-34` | `前処理pipeline`・`前処理layout`・`縮小pipeline`・`縮小layout`・`拡大pipeline`・`拡大layout`・`前処理set`・`縮小set一覧`・`拡大set一覧`・`pipeline`・`layout`(`光のにじみ描画入力`・`明るさの圧縮描画入力`の構造体リテラルのキー) | 条2(ASCII識別子は補完力ゼロ・英字混入の禁止) | 日本語の語幹に生の英単語`pipeline`・`layout`・`set`をそのまま貼り付けている。プロジェクトは同じ概念を`パイプライン`・`レイアウト`・`セット`という訳語で持っており(同ファイルの`ディスクリプタセット`等)、この3語だけ英語のまま残っているのは一貫性を欠く。定義側の構造体はこのブロック外(`crate::vulkan::frame`)にあるが、この行がその識別子を書き下ろしている | `前処理パイプライン`・`前処理レイアウト`・`縮小パイプライン`・`縮小レイアウト`・`拡大パイプライン`・`拡大レイアウト`・`前処理セット`・`縮小セット一覧`・`拡大セット一覧`・`パイプライン`・`レイアウト`

6. `crates/blitz_render/src/vulkan/scene_draw_constants/mod.rs:49` | 仮引数`layout`(`プッシュ定数として積む(self, 積み先, layout: vk::PipelineLayout)`) | 条2(ASCII識別子の禁止) | 同じファイルの`layouts.rs`は同じ概念を`レイアウト`と書いており、ここだけ生の英単語のままである | `レイアウト: vk::PipelineLayout`

7. `crates/blitz_render/src/vulkan/present_timing/support.rs:37` | `含むか`(`fn 含むか(一覧: &[...], 名前: &CStr) -> bool`) | 条1(受理テスト)・条3(5W1Hの必要格) | `support`はASCIIモジュールで自由関数は名前単独で答える必要があるが、「含むか」だけでは主語(何が)と目的語(何を)の両方が抜けている。呼び出し元の`一覧.iter().all(|名前| 含むか(&一覧, 名前))`を読んで初めて意味が確定する | `一覧に名前が含まれるか`

8. `crates/blitz_render/src/vulkan/present_timing/wait.rs:76` | `判定する`(`fn 判定する(結果: ash::prelude::VkResult<()>) -> Result<待機判定, ...>`) | 条5(裸の動詞に準じる。CLAUDE.mdの「生成する・作成する・実行する等」の「等」に該当する汎用動詞の単独使用) | `wait.rs`はASCIIモジュールで自由関数は名前単独で答える必要があるが、「判定する」だけでは何を何に判定するのかが分からない | `提示結果を待機判定へ判定する`

9. `crates/blitz_render/src/vulkan/readback/read/host_bytes.rs:15` | `読む`(`pub(super) fn 読む(device, バッファ, 寸法, 対象) -> Result<Vec<u8>, ...>`) | 条1(受理テスト)・条3(5W1Hの必要格) | `host_bytes`はASCIIモジュールで、呼び出し側も`host_bytes::読む(...)`とASCII名前空間越しに呼ぶため補完が働かない。「読む」単独では読む対象(読み戻しバッファから対象ぶんのバイト列)が分からない | `対象のバイト列を読み取る`

## 違反の疑い

- `crates/blitz_render/src/vulkan/pipeline_ledger/table_tests.rs:53`・`table_tests/reload_tests.rs:13` | `crate::error::生成要求不一致エラー::粒子入力不一致`をパイプライン台帳の試験の汎用失敗として流用している(条6・1語1義に対して) | 「粒子入力不一致」という具体的な意味を持つ既存の枝を、パイプライン生成やGPU待機の失敗を模擬するためだけに使っており、この試験だけを読む人は粒子に関する不具合かと誤読しうる。ただし定義自体はこのブロック外にあり、既存のエラー型を再利用しただけであるため断定はしない
- `crates/blitz_render/src/vulkan/readback/read/pixel_order.rs:16` | `bgra形式か`の大文字小文字 | 条2・条6に対する疑い | 同じファイル・同じクレート内では`RGBA`のように大文字で書く慣習があるのに、ここだけ小文字`bgra`である。読解を阻むほどではないが表記の不統一
- `crates/blitz_render/src/vulkan/post_process/mod.rs:28`・`resize.rs`の`新hdr`/`hdrターゲット` | 条2・条6に対する疑い | 型名は`HDRターゲット`(大文字)なのに、フィールド名・変数名は`hdrターゲット`/`新hdr`と小文字にしている。同じ語が2通りの大文字小文字で書かれている
- `crates/blitz_render/src/vulkan/post_process/inputs.rs:38` | `自動か: u32`(`明るさの圧縮描画入力`のフィールド) | 条4(品詞の規律)に対する疑い | 「〜か」は真偽値の命名慣習だが型は`u32`である。同じ行に「GPU側の整列のためu32で持つ」という理由のコメントがあり、GPU境界での正当な制約という説明も成り立つため断定はしない

## 違反ゼロの範囲

上記以外の識別子(`key.rs`・`key_build.rs`・`layouts.rs`・`lighting_binding_layout.rs`・`reload.rs`・`render_target.rs`・`required_keys.rs`・`supplier.rs`・`table_tests/key_tests.rs`・`point_light_shadow_map/create/image.rs`・`initial_layout.rs`・`sampler.rs`・`post_process/resize.rs`・`sized_images.rs`・`present_timing/mod.rs`・`record.rs`・`readback/buffer.rs`・`mod.rs`・`read.rs`・`target.rs`・`scene_draw_constants/layout_tests.rs`・`shader_struct/read_error.rs`)は受理条件8つを満たし、実体との照合でも語と定義の不一致は見つからなかった。
