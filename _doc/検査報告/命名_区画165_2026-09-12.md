# 命名検査報告 区画165: xtask/src/texture_compression

- 区画名と範囲: 区画番号165、`xtask/src/texture_compression` 配下12ファイルと `xtask/src/type_metrics` 配下28ファイル(一覧は `block_165.txt`)。合計40ファイル
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090`(このツリーは編集していない)
- 直読したファイル数: 40/40(grepだけで済ませたファイルは無い)
- 参照した規約: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

該当なし(0件)。

## 違反の疑い

該当なし(0件)。

## 検査所見

40ファイルすべてを通読し、型・関数・メソッド・フィールド・変数・定数・列挙の枝を受理条件8つに当てた。

- `texture_compression`(12ファイル: `baked_scene.rs`・`contrast_run.rs`・`difference.rs`・`draw.rs`・`error.rs`・`helmet_crop.rs`・`helmet_run.rs`・`judgment.rs`・`plate_area.rs`・`summary.rs`・`world_bake.rs`・`error/display.rs`)は、ブロック圧縮の検収という1つの主題を、焼き付け(`world_bake`)・読み取り(`baked_scene`)・撮影(`draw`)・切り取り(`helmet_crop`)・統計(`difference`)・判定(`judgment`)・報告文(`summary`)・エラー(`error`)へ責務ごとに割った構成である。関数名は「対照の板の世界を方針違いで焼く」「領域内の成分差の統計を求める」「区画が絵に収まるか確かめる」のように動詞句が目的語・着点・条件を伴って書かれており、受理テスト(条件1)・5W1Hの必要格(条件3)を満たす。列挙の枝(`ブロック圧縮の検収エラー`の各枝)も「世界を方針違いで焼けなかった」「区画を拡大する道具が失敗して終わった」のように何が破れたかを名乗っている
- `type_metrics`(28ファイル)は、implブロックの帰属判定・走査・計測・報告という工程ごとに分かれ、`引き当ての材料`・`実装ブロックの引き当て`・`型の所在`・`自己型の経路`のような値オブジェクトが一貫した語で使われている。「経路の指す先と重なる唯一の候補」「自己型も取り込みも場所を決めていないか」のような長い名前も、格・条件を削らずに書かれており条件8(長さは結果であって制約ではない)に沿う
- オーナー不合格実例の3形(実体と合わない語・名詞1語への圧縮・詰め込みすぎ)を型定義とコメントを突き合わせて個別に確認したが、該当する識別子は無かった。フィールドの実体が名前と食い違う例(「試験の許容差の当て方」型の事故)は見当たらない
- 識別子へのASCII英字・ギリシャ文字・数学記号の混入は無かった。`RGBA8`・`BC1`・`ImageMagick`・`DamagedHelmet`は識別子ではなく文字列リテラル・外部ツール名・アセット名としてコメントと`&str`定数の中に現れるのみであり、日本語識別子への混入ではない
- 1語1義の観点で、「判定」「検収」「所在」「観測」等の語がユビキタス言語や他区画と衝突する用法で使われていないかを確認したが、いずれも既存の用法(判定=上限・下限を課す判断、検収=絵を撮って合否を確かめる工程)と一致していた

## 違反ゼロの範囲

区画165の40ファイル全体(`xtask/src/texture_compression`配下12ファイルと`xtask/src/type_metrics`配下28ファイル)で違反0件、疑い0件。省略した検査対象は無い。
