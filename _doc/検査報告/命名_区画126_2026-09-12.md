# ローラー検査(命名の軸) 区画126

- 区画名と範囲: `crates/blitz_sim/src/contact/symmetric_system`、`crates/blitz_sim/src/contact/velocity_stage`、`crates/blitz_sim/src/gpu_layout`(直下・`rigid/`・`xpbd/`)、`crates/blitz_sim/src/intervention`、`crates/blitz_sim/src/lib.rs`(区画番号126、39ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, db815090時点。このツリーは編集していない)
- 直読したファイル数: 39/39(全ファイル直読。grepだけで済ませたファイルは0件)
- 参照した必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(該当語: 対称な連立の固有分解と擬似逆、捨てる向きの判定、解けたと見なす許容差、接触の射影に参加する点、速度段階の参加点、滑走中の印、予測の前の法線相対速度、剛体のGPUレイアウトの契約(判断22)、介入、SPH 等)

## 直読したファイルの一覧(39件)

- contact/symmetric_system: discard_reason.rs, factorization.rs, jacobi_decomposition.rs, mod.rs, pseudo_inverse.rs, pseudo_inverse_solution.rs
- contact/velocity_stage: condition.rs, dynamic_friction.rs, impulse_fixture.rs, impulse_tests.rs, mod.rs, participant.rs, restitution_tests.rs, result.rs, stage.rs
- gpu_layout(直下): gpu_layout_tests.rs, mod.rs, particle_bytes.rs, surface_bytes.rs, surface_bytes_tests.rs, xpbd.rs, xpbd_tests.rs
- gpu_layout/rigid: error.rs, execution_state_bytes.rs, mass_properties_axis_tests.rs, mass_properties_bytes.rs, mod.rs, motion_state_bytes.rs, previous_state_bytes.rs, rigid_tests.rs, word_reader.rs
- gpu_layout/xpbd: bending.rs, target.rs, target_tests.rs
- intervention: encode.rs, encode_tests.rs, kind.rs, mod.rs
- lib.rs

## 違反の一覧

違反0件。以下の理由による。

この区画は、剛体接触の対称連立の求解(判断12・13)、速度段階の反発と動摩擦(判断14)、GPUバイト列のレイアウト契約(判断22・判断9・判断53・判断55)という、`_doc/設計/剛体の状態と接触.md`および`_doc/計画/ユビキタス言語.md`に既に精密な定義がある領域である。読んだ39ファイルの識別子は、次の3点でいずれも該当する定義と一致していた。

1. **名前が実体を表すか**: 例えば`対称な連立の固有分解と擬似逆`の実装(`jacobi_decomposition.rs`・`pseudo_inverse.rs`)は、ユビキタス言語の同名の定義(「法線と接線の連立をヤコビ法で固有分解し、捨てる向きを決めて解く」)と、フィールド・メソッド(`固有値`・`固有ベクトルの列`・`擬似逆で解く`・`第k番の向きを捨てた理由`)が1対1で対応する。`解けたと見なす許容差`・`許容差の内側として捨てた右辺`・`元の行空間の残差`のような長い名前も、ユビキタス言語の同名の定義の条件をそのまま名詞句にしたものであり、実体との乖離が無い。
2. **詰め込みすぎ・圧縮が無い**: 漢字1文字への圧縮(`順4`型)、名詞1語だけの圧縮(`連立`だけで終わる名前等)は無かった。`速度段階の接触点の条件`・`接触の射影に参加する点`・`擬似逆が固有の向きを捨てた理由`のように、格を保った長い名前が一貫して使われている。
3. **ASCII・ギリシャ文字・記号の識別子への混入が無い**: 変数・関数・型・フィールド名にA2やΣλ_nのような記号は無かった。コメント中の数式表記(`v_n⁻`・`λ_k`・`β_k`等)は識別子ではなくコメントであり、受理条件の対象外である。

## 違反の疑い

- `lib.rs:49` | `Sph仕様`・`Sph仕様エラー`・`sphを一ステップ進める`・`sph密度を計算する` | 条件1(日本語命名の質の頭字語混入)に対して | `SPH`(Smoothed Particle Hydrodynamics)はユビキタス言語469行に定義済みの専門用語だが、「一般に通用している語(UI・CPU・GPU・API等)」には当たらず、非専門家がこの綴りだけから意味を復元できない。ただし(a)この区画が直接定義するファイルは`crates/blitz_sim/src/sph/`(区画外)であり`lib.rs`は再エクスポートのみである、(b)ユビキタス言語が既に用語として登録済みで「専門用語の表記」の運用要件(定義してから使う)は満たしている、という2点から断定の違反とはせず疑いに留める。改名の素案(参考): `粒子法流体仕様`・`粒子法流体を一ステップ進める`のように意訳するか、日本語命名の質の許容例外(定着した専門語としてユビキタス言語に登録済み)として現状維持するかは、`sph/`モジュール本体を検査する回で確定するのが妥当である

## 違反ゼロの範囲

`crates/blitz_sim/src/contact/symmetric_system/`(6ファイル)、`crates/blitz_sim/src/contact/velocity_stage/`(9ファイル)、`crates/blitz_sim/src/gpu_layout/`直下・`rigid/`・`xpbd/`(19ファイル)、`crates/blitz_sim/src/intervention/`(4ファイル)、`crates/blitz_sim/src/lib.rs`(1ファイル)の全39ファイルを直読し、上記1件の疑い(区画外定義への再エクスポート起因)を除いて違反ゼロである。
