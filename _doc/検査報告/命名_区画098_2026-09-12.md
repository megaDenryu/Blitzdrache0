# ローラー検査(命名の軸) 区画098

- 区画名: crates/blitz_render/src/vulkan/derived_environment
- 範囲: block_098.txt に列挙された28ファイル(derived_environment直下と、copy_pass/cube_image/descriptor/inputs/probe/table_imageの子モジュールを含む)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 28/28(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、プロジェクトCLAUDE.mdの「命名」節、`_doc/計画/ユビキタス言語.md`(区画に該当する登録語を照合)

## 違反の一覧

1. `crates/blitz_render/src/vulkan/derived_environment/probe/plan.rs:16` | `材料の材料<'a>`(構造体名) | 条件1(受理テスト) | 「材料の材料」は同じ語を二重に重ねただけで、単独で読んでも実体(遠方環境画像・派生表現一式・書き込み元バッファ・受け皿を束ねて`積む材料`を組み立てるための元手)を指せない。日本語として意味を成さない同語反復であり、`材料`や`積む材料`と紛れる | `材料を組む元手` または `積む材料の元手`
2. `crates/blitz_render/src/vulkan/derived_environment/probe/environment_bytes.rs:27` | `狭める`(関数名) | 条件3・条件5 | 何を狭めるのか(倍精度→単精度への型の縮小であること)が名前に無い。同じファイル内の`単精度を半精度へ`という完全な「何から何へ」の型に対し、この関数だけ着点と材料の両方が欠けている | `倍精度を単精度へ狭める`
3. `crates/blitz_render/src/vulkan/derived_environment/probe/readback_set.rs:32` | `作った`(変数名) | 条件4(品詞の規律) | 値(3本ぶんの読み戻しバッファを集めるVec)を動詞の過去形のまま名指ししており、名詞句になっていない。読み手は「作った」だけでは何を作った一覧なのか分からず、直後の`ベイク済み画像の読み戻しバッファ::生成する`まで読んで初めて分かる | `作った読み戻しバッファ一覧`
4. `crates/blitz_render/src/vulkan/derived_environment/descriptor/binding.rs:34` | `書き込む`(関数、`pub(super) fn 書き込む(device, セット, 遠方環境の配列ビュー, 書き込み先)`) | 条件2・条件5 | ASCIIモジュール`binding`直下の自由関数であり、呼び出し元は`binding::書き込む(device, set, ...)`とモジュール名越しに読む。何を書き込むのか(束縛の現物)が名前に無く、`create::生成する`型の裸の動詞に当たる | `束縛を書き込む` または `セットへ束縛を結ぶ`
5. `crates/blitz_render/src/vulkan/derived_environment/table_descriptor.rs:75` | `書き込む`(関数、`fn 書き込む(device, セット, 書き込み先)`) | 条件5 | 4と同型の裸の動詞。ファイル内呼び出し(`書き込む(device, &set, 書き込み先)`)だけで完結し、対象を示す語が名前に無い | `画像を束縛へ書き込む`
6. `crates/blitz_render/src/vulkan/derived_environment/pass.rs:34`、`copy_pass/mip_zero_copy.rs:29`、`copy_pass/readback_pass.rs:27,57`、`copy_pass/upload_pass.rs:62`(いずれもクロージャ引数名`文脈`) | 条件7(直訳の検出) | 実際の型は`GPU命令の積み先と宣言済み資源の取り出し口`(`vulkan/graph/context.rs`で定義、日本語として実体を表す良い名前)だが、この区画の全呼び出し箇所は英語`context`の直訳である`文脈`という別名で受けている。受理条件の失敗例に名指しで挙がっている語そのもの。この別名は`vulkan/graph`配下を含め少なくとも32ファイルに広がる区画外の既存慣習であり、この区画だけの是正では直せない | `資源窓口` または `積み先と取り出し口`(型名の一部を写す)

## 違反の疑い

1. `crates/blitz_render/src/vulkan/derived_environment/probe/resources.rs:37` `受けバッファの口` / `probe/readback_set.rs:56` メソッド`口()` | 条件1に対しての疑い | 「口」はこのファイルの直上コメント(「記録が書き込み先に取るバッファのハンドルだけの組」)が意味を補っており、単独では比喩(受け口=ハンドルの窓口)であることが分かりにくい。ただし同型の比喩的表現(受け皿等)がこの区画・他区画で既に定着しており、コメントで実体が明示されているため断定はしない
2. `crates/blitz_render/src/vulkan/derived_environment/probe.rs:49` `派生表現をgpuで焼いて読み戻す`(関数名) | 条件8に対しての疑い | 識別子中の`gpu`が小文字であり、この区画・他区画の慣習(`GPU資源の確保係`等、常に大文字)と表記が食い違う。ASCIIの大小文字は受理条件そのものの合否基準ではないが、一貫性の観点で記載する

## 違反ゼロの範囲

上記以外の識別子(型名: `派生表現一式`・`派生の立方体画像`・`反射率積分表の画像`・`派生表現ディスクリプタ`・`反射率積分表ディスクリプタ`・`派生表現のディスクリプタ三点`・`派生表現の画像三点`・`派生表現のパイプライン三点`・`派生表現の即時定数`・`派生表現の生成入力`・`受けバッファ四点`・`読み取った四点`・`登録する画像`・`積む材料`・`受けバッファのテクセル数`等、メソッド名・フィールド名・定数名を含む)は受理条件8つ・命名の質・呼称と語の選択のいずれにも違反しないことを確認した。特に、資源型の`生成する()`/`破棄する()`は値オブジェクト様式(型名が目的語を担う)として合格、`拡散照度`・`鏡面畳込み`・`反射率積分表`の3点セットを束ねる型は各フィールド名が実体(用語定義ファイルにある概念)と一致することを確認済み。ASCII英字・ギリシャ文字・数学記号を識別子へ混入した例は無い(「gpu」小文字は上の疑いで別掲)。
