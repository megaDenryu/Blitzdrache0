# 命名検査報告 区画071

- 区画名: crates/blitz_math/src
- 範囲: block_071.txt に列挙された4ファイル
- 作業ツリー: C:/devs/Blitzdrache0_roller
- 時点: main の db815090
- 直読したファイル数: 4/4(全ファイル直読。grepだけで済ませたファイルは0件)
- 検査した日本語識別子の数: 約29件(型3・関連関数/メソッド6・自由関数2・試験関数5・フィールド/変数/引数 約13)

## 違反の一覧

1. `crates/blitz_math/src/deterministic_value.rs:43` | `混ぜる` (自由関数) | 条件2・条件5(裸の動詞の禁止) | ASCIIモジュール `deterministic_value` 直下の非公開自由関数で、レシーバも型名も無く名前単独で答える必要がある。「混ぜる」は動詞だけで目的語(何を混ぜるか)が欠けており、「これは何?」に単独で答えられない。呼び出し箇所を見ると64ビット整数のビットを撹拌するsplitmix64の混合段である。 | `六十四ビットの値を混ぜ合わせる` または `splitmix64の混合段を適用する`(和訳語で言うなら`混合段を適用する`)
2. `crates/blitz_math/src/deterministic_value.rs:32` | `零以上一未満` (`決定的な値の生成器` のメソッド) | 条件1・条件4(品詞の規律) | 名前が名詞句でも動詞句でもなく、値の範囲だけを述べた断片になっている。戻り値がf32であるにもかかわらず、「〜か」を伴わない条件文めいた語形のため、初見では真偽値判定メソッドとも読める(「0以上1未満か」の「か」が欠落した形に見える)。実体は添字と用途から値を計算する処理であり、「これは何?」に単独で答えられない。 | `零以上一未満の値を求める` または `添字と用途から零以上一未満の値を求める`

## 違反の疑い

1. `crates/blitz_math/src/deterministic_value/deterministic_value_tests.rs:8` | `生成器` (試験専用のヘルパー関数、戻り値`決定的な値の生成器`) | 条件4に対して疑い | 関数は`決定的な値の生成器::種の値から生成する(種)`を呼ぶ処理(生成)であるにもかかわらず、名詞形で命名されている。呼び出し箇所`生成器(42)`は「種42から生成器を作る」という短縮コンストラクタとして読めなくはないが、品詞の規律(処理は動詞句)には厳密には反する。試験専用の非公開ヘルパーであり実害は小さいと判断し、断定はしない。 | `種から生成器を作る`
2. `crates/blitz_math/src/deterministic_value/deterministic_value_tests.rs:12` | `用途` (試験専用のヘルパー関数、戻り値`用途番号`) | 条件4に対して疑い | 同上。`用途番号::番号から生成する(番号)`を呼ぶ処理を名詞「用途」で命名している。呼び出し箇所`用途(3)`は読めるが、品詞規律の観点では動詞句が望ましい。 | `番号から用途番号を作る`

## 違反ゼロと判定した範囲

- `crates/blitz_math/src/lib.rs`: このファイルは他モジュール(frame・units等、区画外)の識別子を再エクスポートしているだけで、区画071で新規に定義された識別子は無い。区画内で定義された`決定的な値の生成器`・`用途番号`の再エクスポート記述自体に受理条件違反は無い
- `crates/blitz_math/src/deterministic_value.rs`: 型`決定的な値の生成器`、関連関数`種の値から生成する`、フィールド`種`、引数`添字`・`用途`、変数`上位`・`十六ビット`は実体と一致し、レシーバ・型名による補完も働くため合格。上記の`混ぜる`と`零以上一未満`を除き違反ゼロ
- `crates/blitz_math/src/deterministic_value/use_number.rs`: 型`用途番号`、関連関数`番号から生成する`、引数`番号`、メソッド`値`(レシーバ`用途番号`による目的語の補完が働くため合格)は全て受理条件を満たす。違反ゼロ
- `crates/blitz_math/src/deterministic_value/deterministic_value_tests.rs`: 試験関数名(`同じ入力からは常に同じ値が出る`・`値は0以上1未満に収まる`・`種を変えると値が変わる`・`添字を変えると値が変わる`・`用途を変えると値が変わる`)は文として振る舞いを一意に述べており合格。長さは条件8により制約にならない。局所変数`一回目`・`二回目`・`値`・ループ変数`添字`も文脈で一意に読める。違反ゼロ(上記「違反の疑い」2件を除く)

## 識別子への英字・ギリシャ文字・記号の混入

4ファイルとも識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)にASCII英字・ギリシャ文字・数学記号を混入したものは無い。ファイル名(`deterministic_value.rs`・`use_number.rs`等)はCargoの制約によるASCIIモジュール名であり、識別子そのものではないため対象外とした。

## 省略について

省略した検査は無い。4ファイル全件を直読し、全ての日本語識別子を受理条件8つ・「日本語命名の質」・「呼称と語の選択」・ユビキタス言語(該当語の登録なし)と照合した。
