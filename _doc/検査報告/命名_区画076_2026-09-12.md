# 命名検査報告: 区画076

区画名: crates/blitz_render/src 第2区画（区画番号 076、13ファイル）
作業ツリー: C:/devs/Blitzdrache0_roller（main の db815090 時点。ツリーは編集していない）
検査軸: 命名（グローバルCLAUDE.md「日本語命名の受理条件」8条件、「日本語命名の質」、「呼称と語の選択」、「専門用語の表記」）
用語定義ファイル: _doc/計画/ユビキタス言語.md（該当語のみ照合。ファイル全体は404.8KBのため全読みではなくgrep照合）

## 直読したファイル数と省略の有無

対象13ファイルを全件直読した（grepだけで済ませたファイルは0件）。

1. crates/blitz_render/src/temporal_reconstruction.rs
2. crates/blitz_render/src/terrain_detail.rs
3. crates/blitz_render/src/ui_draw_data.rs
4. crates/blitz_render/src/ui_mesh.rs
5. crates/blitz_render/src/ui_scissor.rs
6. crates/blitz_render/src/ui_texture_id.rs
7. crates/blitz_render/src/ui_texture_material.rs
8. crates/blitz_render/src/ui_vertex.rs
9. crates/blitz_render/src/validation_counter.rs
10. crates/blitz_render/src/vertex.rs
11. crates/blitz_render/src/view_input.rs
12. crates/blitz_render/src/visible_instance_selection.rs
13. crates/blitz_render/src/vulkan_failure.rs

注意: いずれも `mod.rs`相当（親モジュールファイル）またはリーフ実装ファイルであり、`terrain_detail.rs`の子モジュール（`level.rs`・`selection.rs`）と`visible_instance_selection.rs`の子モジュール（`error.rs`・`interval_layout.rs`・`list.rs`・`pass_range.rs`・`selection.rs`・`stage_range.rs`・`visible_pass.rs`）、`temporal_reconstruction/synthetic_input.rs`、`view_input/view_projections.rs`・`view_input_tests.rs`は、区画一覧(block_076.txt)に含まれないため本報告の対象外である（別区画で検査される前提）。

## 違反の一覧

- crates/blitz_render/src/ui_mesh.rs:13 | `テクスチャid`（フィールド名） | 条件6(1語1義)・条件2(補完してよい文脈の限定は綴りの一致を含意) | ユビキタス言語.mdの登録語は「テクスチャID」(大文字)であり、同ファイルが実際に使う型名も`UIテクスチャID`(大文字)である。フィールド名だけ英単語部分を小文字「id」で日本語複合語へ直接接着しており、読み手は「テクスチャID」と同じ概念か、大文字小文字を区別する別概念かを名前だけから判別できない | `テクスチャID`（型名と綴りを揃える）
- crates/blitz_render/src/ui_texture_material.rs:24 | `期待バイト数u64`（ローカル変数名） | 識別子へのASCII混入の禁止(定義の実例が挙げる「識別子にASCIIを入れたものは不合格」に該当)・条件1(受理テスト) | Rustの型接尾辞「u64」を日本語の複合語へ直接接着した識別子であり、単独で読むと「バイト数64」のような数量の意味と紛れる。実体は「u64型で計算した期待バイト数の中間値」だが、u64が型情報の漏出であって名前の格を担っていない | `期待バイト数の64ビット中間値` または単に `期待バイト数64`を避けて`期待バイト数`のシャドーイングをやめ、コメントで型の理由を書く

## 違反の疑い（断定しない）

- crates/blitz_render/src/ui_vertex.rs:9-11 の `位置px`・`色rgba8`、crates/blitz_render/src/ui_scissor.rs:6 の型名`UIシザー矩形px` | 条件2に関連する疑い | 単位・形式を表す小文字ASCII接尾辞(`px`・`rgba8`)を日本語複合語へ直接接着している。ただしこの接尾辞パターンは`UIシザー矩形px`（既に公開型名として広く使われている）を含め、GPU境界型（「フレーム型・単位型を外した生の数値のみを持つ」と各ファイル冒頭コメントが明言する層）でファイルをまたいで一貫使用されており、意図的に確立された表記である可能性が高い。ユビキタス言語.mdに`px`・`rgba8`の単位接尾辞としての登録があるかは今回未確認（大容量ファイルのため該当語のgrep照合に留めた）であり、登録が無ければ条件6・条件2の観点で正式な是正候補になりうる
- crates/blitz_render/src/vertex.rs:8, ui_vertex.rs:10 の `uv` | 条件1に関連する疑い | 頂点のUV座標を表す業界慣用の数学記号だが、非専門家が読んで意味を復元できるかは微妙である。ただしコードベース全体で一貫使用される確立済み数学記法であり、`x`・`y`と同様に単独のASCII短縮というより数学的な標準記法に近いため、違反ではなく疑いに留める

## 違反ゼロの範囲

上記2件の違反と2件の疑いを除き、以下は受理条件8条・命名の質・呼称と語の選択のいずれにも抵触しなかった。

- temporal_reconstruction.rs: `時間再構成方式`・`履歴混合`・`履歴画像を読むパスが積まれるか`・`時間再構成の描画設定`・`出現領域の相対許容`・`時間再構成のシェーダー一式`はいずれも実体（型定義冒頭コメント）と一致し、動詞句・名詞句・真偽値の品詞規律も守られている
- terrain_detail.rs: `地形詳細段`・`地形詳細段選択`・`段を参照する`は目的語・着点が明確
- ui_draw_data.rs・ui_mesh.rs（フィールド`頂点一覧`・`インデックス一覧`・`シザー矩形px`）・ui_scissor.rs・ui_texture_id.rs・ui_texture_material.rs（`バイト長不一致`・`期待バイト数`・`実バイト数`は問題無し）は、上記1件を除き実体と一致
- validation_counter.rs: `検証層の状況`・`有効`・`環境に無い`・`呼び名`・`検証カウンタ`・`内部参照を複製する`・`件数を読む`はいずれも動作・値の格を過不足なく含む
- vertex.rs: `頂点`・`位置`・`法線`・`接線`
- view_input.rs: `描画視点`・`描画視点一覧`・`カメラ相対ビュー変換`・`カメラ大域位置`・`カメラ大域原点`・`射影の復元`・`視点数`・`先頭`・`残り`はいずれも型定義コメントの実体と一致し、5W1Hの必要格を満たす
- visible_instance_selection.rs: `可視ID列エラー`・`可視個体選択一覧`・`パス別描画範囲`・`可視個体選択`・`段別描画範囲`・`可視ID列を読むパス数`・`可視パス`・`個体描画計画`はいずれも対象・単位・目的語が名前に現れている
- vulkan_failure.rs: `Vulkan失敗コード`・`コード番号`・`コード名`・`生成する`（型名が目的語を担う値オブジェクト様式のため合格）
