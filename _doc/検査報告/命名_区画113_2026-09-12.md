# 命名検査報告 区画113: crates/blitz_sim/src/cloth

- 区画番号: 113
- 範囲: `crates/blitz_sim/src/cloth`(29ファイル。`cloth/` 直下25ファイル + `cloth/reference/` 配下4ファイル)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090`(このツリーは編集していない)
- 直読したファイル数: 29 / 29(全ファイルを直読。grepだけで済ませたファイルは0件)
- 参照した定義: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(布・拘束・彩色・XPBD関連の登録語)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_sim/src/cloth/spec.rs:90 | `一辺粒子数u16` (メソッド名) | 識別子へのASCII混入禁止(オーナー実例`段階A2の候補`と同型) | 日本語の識別子「一辺粒子数」に英字+数字「u16」を境界なく直結している。中国語的圧縮ではないが同種の「単語らしく見せるための助詞省略+ASCII混入」であり、日本語の名として読めない | `一辺粒子数を十六ビットで得る` または `一辺粒子数の十六ビット表現` |
| crates/blitz_sim/src/cloth/grid_particles.rs:33 | `一辺粒子数f32` (局所変数) | 識別子へのASCII混入禁止 | 「一辺粒子数」に型接尾辞「f32」を直結。局所変数とはいえ受理条件は変数も対象に含む | `一辺粒子数の実数` または単に型注釈へ譲り `一辺粒子数の値`(後続の掛け算で使うだけなら型接尾辞は不要) |
| crates/blitz_sim/src/cloth/grid_particles.rs:34 | `粒子数f32` (局所変数) | 識別子へのASCII混入禁止 | 同上。「粒子数」への型接尾辞の直結 | `粒子数の実数` |
| crates/blitz_sim/src/cloth/grid_index.rs:3 | `線形添字` (自由関数名) | 品詞の規律(条件4)。受理テスト(条件1) | `一辺粒子数, 行, 列` から添字を計算する処理(自由関数)なのに、名前が値の名詞のままで動詞句になっていない。ファイル内の他の自由関数(`拘束を作る`・`構造を作る`・`せん断を作る`)は全部「〜を作る」の動詞句であり、この関数だけが名詞1語で処理を偽装している(実例(a)「処理なのに名詞形」と同型) | `線形添字を求める` |
| crates/blitz_sim/src/cloth/reference_test_fixtures.rs:67 | `下端の行の平均y` (関数名) | 識別子へのASCII混入禁止 | 「平均」に軸名「y」を直結した合成語。近くのコメント(`reference_bending_tests.rs:34`)は同じ量を`下端の高さ`という完全な日本語で呼んでおり、この関数だけが軸名をASCIIのまま貼り付けている | `下端の行の高さの平均` |
| crates/blitz_sim/src/cloth/bending_constraint.rs:7-10 | `辺a添字`・`辺b添字`・`翼c添字`・`翼d添字` (構造体フィールド) | 識別子へのASCII混入禁止 | 「辺」「翼」というカタカナでない日本語名詞にアルファベット1文字a/b/c/dを直結している。`_doc/計画/ユビキタス言語.md`の「二面角」の定義が「共有する辺(a・b)と翼(c・d)」という同じa/b/c/dの記法を用語として既に登録しているため意味は復元できるが、オーナーの実例節は「識別子にASCIIの英字・ギリシャ文字・数学記号を入れたものはclippyが落ちるより前に不合格」と条件を付けずに明言しており、用語集への登録は識別子内のASCII文字そのものを免除しない | `辺の一端添字`・`辺の他端添字`・`翼の一端添字`・`翼の他端添字`、または`辺始点添字`・`辺終点添字`・`翼片側添字`・`翼反対側添字`(4点の役割が対称なため命名は要再設計) |
| crates/blitz_sim/src/cloth/distance_constraint.rs:14-15 | `粒子a添字`・`粒子b添字` (構造体フィールド) | 識別子へのASCII混入禁止 | 上と同型。「粒子」にa/bを直結 | `粒子一端添字`・`粒子他端添字` |
| crates/blitz_sim/src/cloth/grid_bending_constraints.rs:33-36,39-42、crates/blitz_sim/src/cloth/grid_constraints.rs:42,45-46 | `行a`・`列a`・`行b`・`列b`・`行c`・`列c`・`行d`・`列d` (関数の引数・局所束縛) | 識別子へのASCII混入禁止 | 上記2件のフィールド名をそのまま4点の座標へ伝播させた結果、局所の引数名にもa/b/c/dが直結している。同じ設計変更(4点の役割名の再設計)が波及する範囲であるため1件にまとめて報告する | フィールド名の改名と同時に`(行一端, 列一端)`のような対応する名前へ改める |

## 違反の疑い

| ファイル:行 | 識別子 | 落ちた条件(疑い) | 理由 |
|---|---|---|---|
| crates/blitz_sim/src/cloth/index_convert.rs:5,12 | `usizeへ`・`u32へ` (自由関数名) | 受理テスト(条件1)・品詞の規律(条件4) | 「〜へ」という格助詞で終わり動詞が無いため、単独では「これは何」に「〜する処理です」の形で答えられない(例: `usizeへ変換する`なら合格)。ただし同じ命名の型(型名+「へ」で動詞を省く自由関数)は`blitz_render`・`blitz_math`・`blitz_game`・`blitz_asset_compiler`・`blitz_sim::surface_flow`など少なくとも十数箇所に既に存在し、プロジェクト全体で確立した慣行になっている可能性がある。この区画だけの是正では一貫性を欠くため、断定せず疑いとして報告する |
| crates/blitz_sim/src/cloth/material.rs:45 | `曲げを差し替える` (メソッド名) | 5W1Hの必要格(条件3) | 実際に差し替えるのは「曲げのコンプライアンス」だけであり、`布の物性`が持つ他の量(構造・せん断のコンプライアンス)と紛れる余地がわずかにある。直後のコメント「曲げの硬さの記録が距離の物性を保ったまま曲げを変えるときに使う」で復元できるため疑い止まりとする | `曲げのコンプライアンスを差し替える` |

## 違反ゼロの範囲

`colored_constraints.rs`・`colored_constraints_tests.rs`・`data.rs`・`error.rs`・`generate.rs`・`generate_tests.rs`・`bending_mapping.rs`・`graph_mapping.rs`・`material.rs`(上記1件の疑いを除く)・`mod.rs`・`particle.rs`・`reference.rs`・`reference/bending.rs`・`reference/error.rs`・`reference/step.rs`・`reference/target.rs`・`reference_bending_tests.rs`・`reference_floor_tests.rs`・`reference_tests.rs` の識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)は受理条件8つを満たす。特に`布データ`・`布の彩色済み拘束`・`布の物性`・`布の参照計算`・`布の敷き方`・`距離拘束`・`曲げ拘束`は`_doc/計画/ユビキタス言語.md`の登録定義(フィールド構成まで含む)と実装が一致しており、1語1義の照合で問題を検出しなかった。
