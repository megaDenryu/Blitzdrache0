# 命名検査報告 区画123: crates/blitz_sim/src/contact/scene

- 区画番号: 123
- 対象パス: `crates/blitz_sim/src/contact/scene`(40ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の `db815090` 時点。このツリーは編集していない)
- 検査軸: 命名(日本語命名の受理条件・日本語命名の質・呼称と語の選択)
- 必読リストの参照先: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(接触点集合・接触点集合の法線と接線の連立・試験の許容差の当て方・静止摩擦・二段の位置ほかの登録済み語を照合に使った)
- 直読したファイル数: 40 / 全40(grepだけで済ませたファイルは無い。全ファイルを冒頭から末尾まで通読した)

## 違反の一覧

1. `crates/blitz_sim/src/contact/scene/substep_velocity_stage.rs:42-43` | `a`, `b`(`let a = 拘束.速度段階の剛体の参加点(...)`・`let b = 拘束.速度段階の静的世界の参加点()`) | 条2・条7(識別子への英字の混入。呼称と語の選択の「漢字1文字への圧縮」と同型のASCII単独文字への圧縮) | `a`・`b`は初出の`剛体の参加点`・`静的世界の接触相手`の意味を一切運ばず、代数の変数名をそのまま流用しており、日本語話者がこの2文字だけを見て「これは何か」に単独で答えられない。直後の`速度段階.一点へ適用する(a, b, 条件)`まで読んでも、どちらが剛体側でどちらが静的世界側かは呼び出し元の記憶に依存する | `剛体の参加点`・`静的世界の参加点`(呼び出し元の`拘束.速度段階の剛体の参加点`・`拘束.速度段階の静的世界の参加点`のメソッド名をそのまま変数名に採る)
2. `crates/blitz_sim/src/contact/scene/substep_velocity_stage.rs:51` | `適用後のaの運動状態`(`接触の速度段階の結果::衝撃を適用した`のフィールドを分解束縛した名前) | 条2・条7 | 「aの」という所有格がこのファイル内のどの実体を指すか分からない(このファイルには`a`という名の値が存在しない。宣言元は`crates/blitz_sim/src/contact/velocity_stage.rs`でこのブロックの40ファイルには含まれないが、この識別子はこのファイルで読み・使われている)。ASCII1文字を格助詞で日本語へ接続する形は「段3」と同型の圧縮であり、対象を名指しできていない | `適用後の剛体の運動状態`(宣言元ファイルでのリネームが必要。このファイルはその識別子を分解束縛して使っているだけである)

## 違反の疑い(断定しない)

1. `crates/blitz_sim/src/contact/scene/scene_settings.rs:28` | フィールド`解く品質`(型は`接触を解く品質の設定`) | 条3(5W1Hの必要格。目的語「接触を」の欠落) | 型名は「接触を解く品質の設定」だが、フィールド名は「接触を」を落として「解く品質」だけにしている。同じ構造体の中に「静止摩擦の解き方」のように目的語を保った命名が並ぶため、この1箇所だけ省略が目立つ。ただし読み手は同じ行の型注釈を必ず一緒に見るため、条2が言う「型名による補完」に近い形で意味は復元できるとも読める(フィールド名そのものへの型名の補完を認めるかは受理条件が明文で答えていない) | `接触を解く品質`(または`解く品質`のまま据え置き、型注釈での補完を認める)

## 違反ゼロの範囲

上記2件(および疑い1件)を除く、残り38ファイルの識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は受理条件8条・命名の質・呼称と語の選択のいずれにも抵触しなかった。特に以下を確認した。

- 直訳調(record→記録、resolve→解決、lookup→引く等)や膠着圧縮(助詞抜き複合語)は検出しなかった
- 「試験の許容差の当て方」「接触点集合の法線と接線の連立」「静止摩擦」等はユビキタス言語の登録語と一致しており1語1義を保っていた
- 漢字1文字への圧縮(順4・段3型)は検出しなかった。列挙の枝`保持`・`滑走`・`八本`・`十六本`は完全な2文字語であり圧縮には当たらない
- 裸の動詞の禁止については、ファイル内の自由関数はすべて日本語のモジュール内(型に属する`impl`または`super`経由で読まれる私有関数)にあり、目的語・材料格を伴っていた(`一様な立方体の質量特性`・`世界に置いた直方体を組む`等)
- 品詞の規律(値=名詞句、処理=動詞句)は一貫していた
