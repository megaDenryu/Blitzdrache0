# 命名検査報告 区画065

- 区画名: crates/blitz_engine/src/temporal_reconstruction ならびに crates/blitz_engine/src/terrain_lod・terrain_lod_tests
- 範囲: block_065.txt に列挙された37ファイル(temporal_reconstruction 配下26ファイル、terrain_lod 配下6ファイル、terrain_lod_tests 配下3ファイル、terrain_lod/error.rs含む)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main の db815090。このツリーは編集していない)
- 時点: 2026-09-12
- 直読したファイル数: 37 / 37(全ファイルを直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

- crates/blitz_engine/src/terrain_lod/distance.rs:10 | `x差` | 識別子へのASCII混入(受理条件1「これは何?」への単独回答不成立。実例が挙げるASCII混入禁止に該当) | 日本語のみの中で「差」の対象が数式のx軸を指すと分かるのは`x`という数学記号を読める前提のときだけであり、「これは何?」に日本語だけで答えられない | `X軸方向の距離` または `横方向の距離`
- crates/blitz_engine/src/terrain_lod/distance.rs:11 | `z差` | 同上 | 同上(Z軸) | `Z軸方向の距離` または `奥行き方向の距離`
- crates/blitz_engine/src/terrain_lod_tests/distance.rs:12 | `視点x`(ループ変数。15行目・16行目でも参照) | 識別子へのASCII混入(受理条件1・実例のASCII禁止) | 「視点」という日本語の直後に英字`x`を継ぎ足しており、軸を指す記号を読めない人はこの値が何の成分かを名前だけから復元できない | `視点のX座標` または `視点の横位置`

## 違反の疑い

- crates/blitz_engine/src/terrain_lod/error.rs:6 ほか(`地形LODエラー`・`地形LOD選択設定`・`地形LOD選択器`。selection_settings.rs・selector.rs・fixture.rs・selection.rs 全体に反復) | 条件に対して: 日本語命名の質「英語の頭字語・省略語を日本語の中に混ぜない」 | 「LOD」はUI・CPU・API等の一般的に通用する語の例示に無く、非専門家が単独で読める語ではない。ただし`_doc/計画/ユビキタス言語.md`の215行目に`LOD`が定義・登録済みであり、`地形詳細段`・`個体詳細段`など同一クレート全域で確立した表記として既に広く使われている(このファイル群固有の新規導入ではない)。一括での是正はしない方針(「一括リネームはしない」)とも整合するため、断定はしない | 改名するなら`地形詳細段選択エラー`・`地形詳細段選択設定`・`地形詳細段選択器`だが、コードベース全域の同型識別子(地形詳細段選択に限らず可視個体の選別等LOD関連語彙全体)との整合を取ってから行うべき事項であり、この区画だけの単独リネームは不整合を招く

## 違反ゼロの範囲

- temporal_reconstruction配下26ファイル(admission_tests.rs・blend_tests.rs・clamp_tests.rs・constants.rs・current_frame_ratio.rs・disocclusion_tolerance.rs・error.rs・exponential_moving_average.rs・history_admission.rs・jitter_sequence.rs・jitter_sequence_tests.rs・linear_color.rs・luminance_weight_tests.rs・luminance_weighted_blend.rs・method.rs・mod.rs・motion_vector.rs・motion_vector_tests.rs・neighborhood_range.rs・pixel_offset.rs・pixel_reconstruction.rs・reconstruction_tests.rs・relative_luminance.rs・screen_position.rs・sequence_index.rs・value_object_tests.rs・view_depth.rs)は、型・関数・メソッド・フィールド・列挙の枝のいずれにも受理条件1〜8の違反、直訳調、膠着圧縮、漢字1文字圧縮、詰め込みすぎの型名を確認しなかった。値オブジェクト様式(`生成する()`が型名を目的語として担う)・処理を保持するフィールド名の動詞句化・列挙の枝への理由付きコメントがいずれも規約どおりである。用語は`_doc/計画/ユビキタス言語.md`の「時間再構成」「動きベクトル」「履歴画像」「出現領域」「ビュー空間の奥行き」等の登録済み定義と一致した
- terrain_lod配下(distance.rs・error.rs・hysteresis.rs・relaxation.rs・relaxation_tests.rs・selection_settings.rs・selector.rs)およびterrain_lod_tests配下(distance.rs・fixture.rs・selection.rs)は、上記のASCII混入2件・LOD表記の疑い1件を除き違反を確認しなかった。「地形の語彙へ写す」のようなエラー変換関数、「隣接差を緩和する」「段を決める」「チャンクとの最短距離」等の動詞句はいずれも目的語と動作が揃っている
