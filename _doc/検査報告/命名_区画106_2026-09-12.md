# 命名検査報告: 区画106 crates/blitz_render/src/vulkan/material_table

- 区画名と範囲: `crates/blitz_render/src/vulkan/material_table` 配下(親ディレクトリ直下31ファイル + サブディレクトリ7ファイルの計38ファイル。block_106.txtに列挙された一覧のみを対象とする)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`(main, db815090)。このツリーは編集していない
- 直読したファイル数: 38/38(grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(材質登録簿・資源表世代・テクスチャ台帳・世代内材質レコード・材質GPU梱包工程・常駐テクスチャ供給元・テクスチャ表レイアウト容量・画像ID・テクスチャID・大域材質ID・世代内材質解決・材質GPU参照・世代束縛資源・世代の常駐枚数・正準フォールバック用途 等の登録済み語と照合)

## 直読したファイル一覧(38件)

capacity.rs / device_supplier.rs / dry_run.rs / fallback_usage.rs / feature_set.rs /
frame_hold.rs / generation.rs / generation_build.rs / generation_id.rs / generation_record.rs /
generation_resolution.rs / image_id.rs / image_identity.rs / ledger.rs / material_gpu_reference.rs /
material_id.rs / pack_input.rs / packer.rs / record_index.rs / registration.rs /
registration_state.rs / residency_count.rs / resource_table.rs / stage_reserve.rs / supplier.rs /
tests.rs / texture_id.rs / texture_registry.rs / texture_role.rs / texture_slot.rs /
texture_spec.rs / device_supplier/generation_binding.rs / generation/lookup.rs /
generation_build/completion_check.rs / generation_build/residency.rs / ledger/retirement.rs /
packer/fallback_slots.rs / packer/validate.rs

## 違反の一覧

なし(0件)。

## 違反の疑い

- `crates/blitz_render/src/vulkan/material_table/packer.rs:61` | `一件を梱包する` | 条件3(5W1Hの必要格) | 名前の「一件」が何の一件かを名前単独で担っていない。呼び出し元では`材質`という変数を渡しているが、関数名自体は目的語(材質)を持たず、モジュール内の文脈(周囲のコメント・呼び出しの並び)を読んで初めて「材質1件を梱包する」と分かる。ただし本関数は`packer.rs`内のprivate関数であり、直後に呼ばれる形で使用箇所が極めて限定される(受理テストの「これは何?」に対し、単独では答えがやや弱いが、読み手が同一ファイル内の呼び出し元をすぐ確認できる範囲に収まる) | 改名の素案: `材質1件を梱包する`

- `crates/blitz_render/src/vulkan/material_table/device_supplier/generation_binding.rs:21,54` | `pool`(フィールド)・`セットを割り当てて結ぶ`のASCII識別子`pool`ローカル変数 | 条件2に対する疑い | 他の全識別子が日本語であるのに`pool: vk::DescriptorPool`だけが英語のままである。ただし`CLAUDE.md`「サービスの依存保持とコールバックの限定」条4が資源型の`破棄する(&self, device)`という英語仮引数を明示的な例外として認めており、同ファイルの`破棄する(&self, device: &GPUデバイス)`はこの例外に一致する。`pool`はash型`vk::DescriptorPool`をそのまま保持するフィールド名であり、値そのものを指す一語であるため直訳調や膠着圧縮には当たらない。日本語命名の徹底という観点では改善の余地があるが、受理条件8つに明確に違反するとは言い切れないため疑いに留める | 改名の素案(採るなら): `プール`

## 違反ゼロの範囲

上記2件の疑いを除く、区画106の直読38ファイル全体(型名・関数名・メソッド名・フィールド名・変数名・列挙の枝・定数)で、受理条件8つ・日本語命名の質(頭字語混入/直訳調/膠着圧縮)・呼称と語の選択(漢字1文字圧縮)・ASCII文字/ギリシャ文字/数学記号の識別子への混入、のいずれの違反も検出しなかった。ユビキタス言語に登録済みの語(材質登録簿・資源表世代・テクスチャ台帳・世代内材質レコード・材質GPU参照・大域材質ID・テクスチャID・画像ID・正準フォールバック用途 等)と実装の型名・フィールドは1対1で一致しており、「オーナーが不合格にした実例」型の(実体と合わない語・名詞1語への圧縮・詰め込みすぎ)も見当たらなかった。
