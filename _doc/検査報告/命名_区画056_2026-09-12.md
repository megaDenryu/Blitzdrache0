# 命名検査報告: 区画056 auto_exposure・camera

## 範囲・時点

- 区画名: crates/blitz_engine/src/auto_exposure (区画番号056)。一覧ファイルは block_056.txt。
- 作業ツリー: C:/devs/Blitzdrache0_roller、時点は main の db815090。このツリーは編集していない。
- 一覧に含まれるファイルは crates/blitz_engine/src/auto_exposure 配下32件と crates/blitz_engine/src/camera 配下7件の合計39件である。

## 検査した数

- 直読したファイル数: 39/39(一覧の全ファイル)。grepだけで済ませたファイルは0件。全ファイルを冒頭から末尾まで読み、型・関数・メソッド・フィールド・変数・定数・列挙の枝の識別子を検査した。

## 違反の一覧

該当なし。

## 違反の疑い

該当なし。

## 違反ゼロの範囲

crates/blitz_engine/src/auto_exposure 配下32件(adaptation.rs・adaptation_speed.rs・adaptation_speed_pair.rs・adaptation_state.rs・adaptation_tests.rs・aggregation.rs・clamp.rs・derivation.rs・derivation_result.rs・derivation_tests.rs・error.rs・exposure_method.rs・final_compensation.rs・final_compensation_tests.rs・hdr_pixel.rs・histogram.rs・histogram_boundary.rs・histogram_scale.rs・histogram_scale_tests.rs・histogram_tests.rs・index_weight_sum.rs・log_luminance.rs・luminance.rs・middle_gray.rs・mod.rs・pixel_count.rs・pixel_ratio.rs・positive_luminance.rs・probe_pixel_tests.rs・quantity_tests.rs・temporal_luminance_agreement_tests.rs・value_object_tests.rs)と、crates/blitz_engine/src/camera 配下7件(clamp.rs・display_distance.rs・display_distance_tests.rs・intent.rs・mod.rs・position.rs・projection.rs)の全39ファイルで、日本語識別子の受理条件違反を検出しなかった。

## 検査の所見(参考。違反ではない)

- 個々の識別子(`目標補正段を導く`・`次の適応状態を求める`・`最終補正段を合成する`・`ビンの添字を求める`・`表示距離を据える`・`視点ワールド位置`等)は、いずれも「何を+どうする」の格を持ち、単独で「これは何?」に答えられる。`生成する()`はすべて型に属する関連関数であり、型名が目的語を担う値オブジェクト様式に沿う。
- `補正段毎秒`はメートル毎秒等と同型の単位複合語(1語)であり、助詞省略の膠着圧縮(「大気LUT生成指示」型)には当たらない。`_doc/計画/ユビキタス言語.md`に単位表記の前例(メートル・ラジアン等)があり、単独の名詞1語圧縮(「当て方」「連立」型)の実例とも性質が異なる(補正段毎秒は単位の名であり、名詞1語へ処理や条件を圧縮した実体ではない)。
- `明るさの圧縮前のHDR画素`・`明るさの圧縮前の相対輝度`の「HDR」は`_doc/計画/ユビキタス言語.md`に登録済みの語であり、専門用語の表記の節が許す一般に通用する語(写真・スマートフォンのHDRモード等で非専門家にも意味が取れる)に当たる。
- `カメラインテント`のカタカナ語「インテント」は`_doc/計画/ユビキタス言語.md`の「入力インテント」「ゲームインテント」で定義済みの登録語であり、`camera/intent.rs`冒頭コメントも定義箇所を明示して参照している。
- ASCII・ギリシャ文字・数式記号を識別子へ混ぜた例は0件だった。漢字1文字への圧縮(「順4」「段3」型)も0件だった。
- 型が担う実体との照合: `露出適応状態`(未初期化/追従中の2枝)・`自動補正段の上下限`(下限・上限の帯)・`目標中間灰`・`輝度ヒストグラム`(ビン件数・零以下件数・非有限件数)は、いずれも`_doc/計画/ユビキタス言語.md`の該当項目の定義と、フィールド・メソッドの実装が一致していた。「試験の許容差の当て方」のように名前が実体の一部しか表さない例は見つからなかった。
