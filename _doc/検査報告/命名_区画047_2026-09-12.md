# 命名検査報告: 区画047(crates/blitz_collision/src/triangle)

## 範囲・時点

- 区画名: crates/blitz_collision/src/triangle(区画番号047)
- 対象ファイル数: 33
- 作業ツリー: C:/devs/Blitzdrache0_roller
- 時点: main の db815090(このツリーを編集していない)
- 直読したファイル数: 33 / 全33(grepだけで済ませたファイルは0)

対象ファイルの内訳(すべて直読):
error.rs, feature.rs, query_fixture.rs, segment_crossing_hit.rs, segment_feature.rs,
segment_feature_tests.rs, segment_hit.rs, segment_in_plane.rs, segment_in_plane_tests.rs,
segment_plane_relation.rs, segment_query.rs, segment_shear_frame.rs, segment_tests.rs,
segment_tilted_plane_tests.rs, segment_zero_length_tests.rs, sheared_triangle.rs,
sheared_vertex.rs, sweep_candidate.rs, sweep_contact.rs, sweep_direction.rs,
sweep_dispatch.rs, sweep_fixture.rs, sweep_geometry.rs, sweep_place.rs, sweep_query.rs,
sweep_solve_edge.rs, sweep_solve_edge_axis.rs, sweep_solve_face.rs, sweep_solve_vertex.rs,
sweep_solver.rs, sweep_start_overlap.rs, sweep_start_tests.rs, sweep_tests.rs

必読(全プロジェクト共通CLAUDE.mdの複製): 「日本語命名の受理条件」「日本語命名の質」
「呼称と語の選択」「専門用語の表記」。用語定義ファイル: `_doc/計画/ユビキタス言語.md`
(掃引・接触・特徴・候補・場所・当たり等の関連語をこの区画の識別子と照合した)。

## 違反の一覧

該当なし。

## 違反の疑い

「オーナーが不合格にした実例」の3形(実体と合わない語・名詞1語の圧縮・詰め込みすぎ)、
および識別子へのASCII・ギリシャ文字・記号の混入は、この区画のどの識別子にも見当たらな
かった。以下の2件だけ、受理条件4(品詞の規律: 処理=動詞句)に対して疑いを残す。

1. `crates/blitz_collision/src/triangle/query_fixture.rs:16` | `局所の点` | 条件4に対する疑い |
   この関数は成分の配列から`形の局所座標の位置::生成する`を呼び、失敗したら`panic!`する
   処理である。同じファイルの`三角形を作る`・`線分を作る`が動詞句で処理を表しているのに対し、
   `局所の点`だけが名詞句であり、値なのか処理なのかが名前から一意に決まらない。
   改名の素案(参考): `成分から局所の点を組む`
2. `crates/blitz_collision/src/triangle/sweep_fixture.rs:54` | `局所の変位` | 条件4に対する疑い |
   1と同型。成分の配列から`形の局所座標の変位::生成する`を呼び、失敗したら`panic!`する処理を
   名詞句で名指している。同じファイルの`カプセルを作る`は動詞句であり、命名の品詞が揃っていない。
   改名の素案(参考): `成分から局所の変位を組む`

いずれも`pub(super)`の試験専用ヘルパーであり、公開APIには現れない。対照的に`位置の成分`・
`方向の成分`(`query_fixture.rs`)は失敗しない純粋な射影(値の取り出し)であるため、名詞句のままで
受理テストに答えられると判断し、違反にも疑いにも数えていない。

## 違反ゼロの範囲

上記2件の疑いを除き、この区画33ファイルの型名・関数名・メソッド名・フィールド名・変数名・
定数名・列挙の枝はすべて受理条件8つを満たした。特筆点:

- `三角形で当たった特徴`・`三角形との接触の特徴の候補`・`掃引で動けた割合`・`接触の単位法線`
  等の中核語は`_doc/計画/ユビキタス言語.md`の登録句とそのまま一致し、1語1義を保っている
- `型名::生成する()`の形はすべて値オブジェクト様式(型名が目的語を担う)であり、条件5の
  裸の動詞禁止に抵触しない
- 各ファイルの冒頭コメントが「受け取るものと返すもの」を明記しており、名前が実体(型のフィールド
  ・枝)を表しているかを定義と照合しても食い違いは見つからなかった
- 識別子へのASCII文字・ギリシャ文字・数学記号の混入、および漢字1文字への切り詰め(「順4」型の
  圧縮)は0件だった
