# 命名検査報告 区画053

- 区画名: crates/blitz_engine/src/asset/runtime_format/chunk_directory_v2 ほか(一覧は block_053.txt が指す25ファイル)
- 範囲: block_053.txt に列挙された25ファイルのみ(chunk_directory_v2/read.rs・write.rs、height_field_v1/read.rs・write.rs、scene/直下14ファイル、scene/bytes配下2ファイル、scene/multi_material_body配下4ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main, db815090時点。編集していない)
- 直読したファイル数: 25/25(全ファイルを直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`(grepで関連語のみ照合。ファイルが404KBあり全文は読めないため)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_engine/src/asset/runtime_format/scene/v1.rs:26 | `シーンV1` | 条件2(ASCII識別子は補完力ゼロという以前に、識別子自体にASCII文字が混入している)・条件6(1語1義) | 「版1」という完全な日本語の語がユビキタス言語に登録され、同じ概念をv4.rs〜v7.rsが`版4()`・`版5()`のように漢字+算用数字で一貫して呼んでいるのに、v1〜v3だけがアルファベットの`V`+数字を識別子へ埋め込んでいる。オーナーが不合格にした実例の「識別子にASCIIの英字・記号を入れたものは不合格」に該当する形であり、同じ「版番号」という概念が`版1`と`V1`の2つの綴りを持つ | `シーン版1` |
| 同ファイル:27 | `描画対象V1` | 条件2・条件6 | 同上(型`シーンV1`が保持するフィールドの型名) | `描画対象版1` |
| crates/blitz_engine/src/asset/runtime_format/scene/v2.rs:27 | `シーンV2` | 条件2・条件6 | 同上 | `シーン版2` |
| 同ファイル:34 | `描画対象V2` | 条件2・条件6 | 同上 | `描画対象版2` |
| 同ファイル:42 | `形状V2`(enum) | 条件2・条件6 | 同上。この enum は`通常メッシュ`と`地形LODメッシュ群`の2枝を持ち、版2固有の形状判別を表すが、名前のアルファベット部分だけが版を示しており版4以降の`版ごとの要素の読み方::版4()`と綴りが揃わない | `形状版2` |
| crates/blitz_engine/src/asset/runtime_format/scene/v3.rs:26 | `シーンV3` | 条件2・条件6 | 同上 | `シーン版3` |
| 同ファイル:33 | `描画対象V3` | 条件2・条件6 | 同上 | `描画対象版3` |
| crates/blitz_engine/src/asset/runtime_format/scene/mesh_layout.rs:23 | `個体別置換検査記録長` | 条件1(受理テスト)・条件6(1語1義)・条件7(直訳/語の不一致の検出質問と同型) | 定義本体を開いて照合すると、この定数が数える実体は`crates/blitz_render/.../seen_record.rs`の`既出記録`(同一パス区間内で同じ個体IDが2回現れないかを、個体ごとの「最後に見た検査の世代番号」で見る検査)である。コメント自体も「置換」に触れておらず、重複検出であって置き換え(置換)ではない。読み手は「置換」という語から何を置き換えるのか実体を復元できず、既存の`既出記録`という正規の語と食い違う1語2義になっている | `個体別既出検査記録長` |

## 違反の疑い

| ファイル:行 | 識別子 | 落ちた条件(疑い) | 理由 |
|---|---|---|---|
| crates/blitz_engine/src/asset/runtime_format/scene/v7.rs:17 | `アセット形式版::V7` | 条件2(ASCII混入) | `V7`という枝は本区画外(`asset_version.rs`、区画053のファイル一覧に含まれない)で定義された`アセット形式版`列挙の枝であり、本区画のv1.rs〜v7.rsが軒並みこの綴りへ揃えて呼んでいる。grepで確認したところ同じ`V1`〜`V7`の綴りが`catalog/v1.rs`〜`catalog/v3.rs`・`asset_version.rs`など区画外の広い範囲で一貫して使われており、区画053だけの問題ではなく`runtime_format`モジュール全体の設計判断である可能性が高い。定義元を検査していないため違反と断定せず疑いとして報告する |

## 違反ゼロの範囲

- `chunk_directory_v2/read.rs`・`write.rs`: `チャンク目録内容を読む`・`チャンク目録内容を書く`・`項目最小長`など、動詞句/名詞句の品詞規律を守り、5W1Hの必要格(何を+どうする)を満たす。違反なし
- `height_field_v1/read.rs`・`write.rs`: `高さ場内容を読む`・`高さ場内容を書く`・`標本のバイト数`など。違反なし
- `scene/bytes.rs`・`scene/bytes/read.rs`・`scene/bytes/write.rs`: `読取位置`・`書込先`とそのメソッド(`新規`・`完了する`・`件数`・`宣言件数を確保前に検査する`等)。`u8`/`u16`/`u32`/`u64`/`f32`/`f64`/`usize`はRustのプリミティブ型名そのものであり、値オブジェクト様式の型名がレシーバとして目的語を担うため合格。違反なし
- `scene/mesh_layout.rs`: `個体別置換検査記録長`以外の定数(`頂点長`・`インデックス長`・`スキン頂点属性長`・`個体配置長`・`個体変換長`・`個体別可視ID列長`・`個体別選択ID列長`)はコメントとの照合で実体と一致する。違反なし
- `scene/multi_material_body.rs`・`multi_material_body/read.rs`・`shape_read.rs`・`slot_check.rs`・`write.rs`: `マルチマテリアル本体を読む/書く`・`描画対象を読む/書く`・`形状を読む/書く`・`検査する`など。`検査する`は同一ファイル内で完結する私有関数であり、ファイル冒頭コメント「受け取るのは形状と材質集合、返すのは可否」が実体を示すため裸の動詞の禁止には当たらない。違反なし
- `scene/read_element.rs`・`write_element.rs`: モジュール宣言と re-export の一覧、`メッシュ最小長`・`版4以降のメッシュ最小長`・`形状以外の描画対象長`・`版4以降の形状以外の描画対象長`。すべてコメントの数値根拠と一致する。違反なし
- `scene/shape_tag.rs`: `通常メッシュの判別値`・`地形LODメッシュ群の判別値`・`インスタンス群の判別値`。`LOD`はユビキタス言語に登録済みの頭字語(Level of Detail)であり違反にしない。違反なし
- `scene/surface_layer_fixture.rs`: `非対称な重みの画素列`・`層ごとのタイル倍率`・`地表の層の重ね合わせの材質を作る`・`地表の層の重ね合わせの静的シーンを作る`。助詞を保った自然な句であり違反なし
- `scene/texture_format_tag.rs`: `RGBA8`・`BC1`はユビキタス言語586・595行に登録済みの、Vulkanの画像形式名・validation layerの指摘文と綴りを合わせるため意図的に英語のまま維持する第3群語彙であり違反にしない。`判別値から格納形式を読む`・`格納形式の判別値を返す`は5W1Hを満たす。違反なし
- `scene/v4.rs`〜`v7.rs`: `シーン内容を読む`・`シーン内容を書く`・`シーン内容を格納する`と、`版ごとの要素の読み方::版4()`のように漢字+算用数字で統一された版番号の呼び方。v1〜v3で見つかった`V1`〜`V3`のASCII混入がここには無く、この4ファイル単体では違反なし(ただし`アセット形式版::V7`の使用は上の疑いを参照)
