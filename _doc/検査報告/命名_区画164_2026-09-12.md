# 命名検査報告 区画164: xtask/src/smoke ほか(30ファイル)

- 区画名・範囲: 区画番号164。対象は `block_164.txt` に列挙された30ファイル(`xtask/src/smoke/`5件・`copy_setup/error.rs`・`xtask/src/streaming_bench/`4件・`xtask/src/streaming_report/reading_tests.rs`・`xtask/src/temporal_reconstruction/`6件・`xtask/src/temporal_visual/`5件・`xtask/src/terrain_visual/`4件)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、時点 db815090。このツリーは編集していない。本報告ファイルの追加のみ)
- 直読したファイル数: 30 / 全30(grepだけで済ませたファイルは0件。全ファイルを冒頭から末尾まで直読した)
- 必読リストの参照範囲: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「専門用語の表記」「呼称と語の選択」「日本語命名の質」「日本語命名の受理条件」、`CLAUDE.md`「命名」節。`_doc/計画/ユビキタス言語.md`は404.8KBのため全読みはせず、疑義の生じた語(LOD/RAM/VRAM/GPU等)についてのみgrepで照合した

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| xtask/src/smoke/launch_setting.rs:15, 35, 50-53 | `開発uiあり`(フィールド・ビルダーメソッド) | 日本語命名の質「英語の頭字語・省略語を日本語の中に混ぜない」+ 呼称と語の選択(圧縮の禁止に準じる字種混入) | 小文字ASCIIの`ui`が分かち書きも訳語も無いまま日本語へ直結している。「UI」を一般語として使うなら大文字で独立させるべきで、小文字で融着させると「ゆーあい」と読むのか英字のまま読むのか非専門家が判断できない | `開発UIあり`(UIを一般語として独立させる)、または`開発用画面あり` |
| xtask/src/smoke/stages.rs:18, 51, 55, 57 | `開発ui`(関数名) | 同上 | 同上。関数名単独でも「開発ui」という融着表記は非専門家が読めない | `開発UI` または `開発向け画面` を用いた関数名(例: `開発UIステージ`) |
| xtask/src/streaming_report/reading_tests.rs:29-30(使用箇所。定義は区画外の`xtask/src/streaming_report.rs`) | `要約.lod変更フレーム数`・`要約.lod変更時読込開始件数` | 日本語命名の質「英語の頭字語・省略語を日本語の中に混ぜない」+ 専門用語の表記(初出時の補足が無い) | `LOD`(Level of Detail、詳細度)は日本の非専門家が読める語ではなく、グローバルCLAUDE.mdが名指す`LUT`と同種の要意訳語である。定義ファイルに初出の補足も無い。本ファイルは使用箇所のみで定義は区画外だが、この区画を読んで初めて識別子が見えたため報告する | `詳細度変更フレーム数`・`詳細度変更時読込開始件数` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| streaming_bench/condition_a.rs:11-12、condition_b.rs:13-14、streaming_report/reading_tests.rs:25-26(使用箇所) | `RAM上限`・`VRAM上限`・`要約.最大ramバイト数`・`要約.最大vramバイト数` | 専門用語の表記(非専門家が読めるか)・1語1義(大文字と小文字の不統一) | `RAM`・`VRAM`が`CPU`・`GPU`・`API`と同格の一般語かは判断が割れる(オーナー裁定の明文の白リストには入っていない)。加えて同じ語が定数では大文字`RAM`、フィールドでは小文字`ram`と表記が割れており、1語1義の観点でも気になる。断定はオーナー裁定を要すると考え疑いに留める |
| xtask/src/smoke/launch_setting.rs全体 | `照明なし`・`粒子あり`・`開発uiあり`・`ポストなし`・`布あり`・`ウィンドウ再構築検証あり`(真偽値のフィールド・メソッド群) | 品詞の規律(条件4: 真偽は「〜か」) | 受理条件の文言は真偽値の名前を「〜か」の形にするよう定めるが、本ファイル群は「〜あり/〜なし」という既存の慣用形で統一されている。曖昧さは無く単独で意味は取れるため実害は薄いと考えるが、規約の文言とは字面が違う。プロジェクト全体でこの形が定着しているかはこの区画だけでは判断できないため、リネームでなく親の判断を仰ぐ疑いとして記録する |

## 違反ゼロの範囲

上記2件の定義済み違反(開発ui系)と1件の使用箇所の違反(lod系)、および2件の疑いを除く、区画164の残り全識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝。具体的には`copy_setup.rs`・`copy_setup/error.rs`・`mod.rs`・`run_stage.rs`・`stage_catalog.rs`の全識別子、`streaming_bench`の`order_compare.rs`・`order_sequence.rs`の全識別子、`temporal_reconstruction`6ファイルの全識別子、`temporal_visual`5ファイルの全識別子、`terrain_visual`4ファイルの全識別子)は受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも抵触しないことを確認した。特にエラー型(`スモークのアセットの一時コピーの破れ`・`時間再構成の検収エラー`・`時間再構成の目視材料の撮影エラー`・`目視見本の絵の撮影エラー`)は枝ごとに何が破れたかを名指しており、判定名・報告の見出し・観測構造体の各フィールドも目的語・時点・材料の格を保って書かれている。
