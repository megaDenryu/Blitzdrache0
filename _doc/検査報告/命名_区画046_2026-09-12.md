# 命名検査報告 区画046

- 区画名: crates/blitz_collision/src/solver
- 範囲: block_046.txt に列挙された10ファイル(approach.rs / axis_end.rs / displacement.rs / error.rs / line_pair.rs / quadratic.rs / quadratic_from_geometry.rs / ratio.rs / segment_parameter.rs / smallest.rs)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 10/10(全ファイルを全文直読した。grepだけで済ませたファイルは0件)
- 検査した識別子: 型8、関連関数・メソッド約35、フィールド・定数約20、ローカル変数約25(いずれも日本語識別子)

## 違反の一覧

該当なし。

## 違反の疑い(断定しない)

1. `crates/blitz_collision/src/solver/quadratic.rs:57` | `和の側` | 条1(受理テスト)に対する疑い | ローカル変数名だが、「何の和の側か」(2根の和の公式で先に求めた根であること)が名前単独では読めず、直前の関数冒頭コメント(「和の側の根を先に求めて」)を読んで初めて意味が定まる。ローカル変数のスコープが短く実害は小さいが、名前だけでは「根」であることが分からない。改名の素案: `和の公式で求めた根`
2. `crates/blitz_collision/src/solver/ratio.rs:46` の `一歩も動けなかった()` と `crates/blitz_collision/src/solver/ratio.rs:52` の `変位の全体を動けた()` | 条4(品詞の規律)に対する疑い | この2つは`掃引で動けた割合`という値(名詞句であるべき)を返す関連関数だが、名前が「〜なかった」「〜た」という完了形の節になっており、名詞句ではない。ただし型に属する関連関数であり、`掃引で動けた割合::一歩も動けなかった()`のように型名とレシーバレスの呼び出し文脈で読めば「動く前から接触していた場合の値」だと復元でき、かつコードベース全体でこの一線(境界値を状況節で命名する)の慣習が定着している可能性がある。断定はせず疑いとして記録する。改名の素案(参考): `動く前から接触していた場合の割合()` / `全域を動けた場合の割合()`

## 違反ゼロと判定した範囲

- 型名: `掃引が面へ向かうかの判定`、`軸の端`、`無次元の媒介変数の生成エラー`、`二つの直線の最近接の媒介変数`、`掃引の2次方程式`、`掃引で動けた割合`、`線分の媒介変数`、`最も小さい量の候補だけを覚える器` — いずれも実体(冒頭の`//!`と照合済み)を表し、名詞1語への圧縮も無く、直訳調も無い
- メソッド・関連関数: `掃引の変位から生成する`・`接触として持つか`・`並べる`・`並びの位置`・`両方の範囲に収まる組を求める`・`範囲へ寄せた組を求める`・`直線の組の解を求める`・`第1の直線の比`・`第2の直線の比`・`生成する`(型名で目的語を担う関連関数)・`根を小さい順に並べる`・`根の対を求める`・`平面と球の隔たりから組む`・`点と球の隔たりから組む`・`直線と球の隔たりから組む`・`二つの直線の隔たりから組む`・`根を端へ寄せて受理する`・`値`・`端へ寄せて生成する`・`まだ何も無い状態から始める`・`候補を1つ数える`・`覚えている候補を見る`・`覚えている候補を取り出す` — いずれも「何を+どうする」の格を満たし、裸の動詞は型名・レシーバで補完されている
- フィールド・定数: `平行とみなす余弦の幅`・`掃引の変位`・`変位の長さの二乗`・`第1の直線の比`・`第2の直線の比`・`二次の係数`・`一次の係数`・`定数項`・`根を端へ寄せられる幅`・`現在の最小`・`現在の量` — いずれも実体と一致し、単語化のための助詞省略も無い
- ローカル変数(quadratic_from_geometry.rs一式): `始まりの隔たり`・`隔たりの速度`・`半径の二乗`・`始まりの変位`・`掃引の変位`・`直線の変位`・`直線の長さの二乗`・`始まりの外積`・`速度の外積`・`両方に垂直な向き`・`向きの長さの二乗` — いずれも格が明示され紛れが無い
- ASCII英字・ギリシャ文字・数学記号の混入: 0件(`第1`・`第2`のような算用数字は日本語の序数表記であり、規約が禁じる英字・ギリシャ文字・数式記号の圧縮(`A2`・`Σλ_n`)には当たらない)
- 「実体と合わない語」「名詞1語への圧縮」「詰め込みすぎ」の3形: いずれも該当なし。`根を端へ寄せて受理する`・`範囲へ寄せた組を求める`のように2動作を含む名前も、両方が実際の処理(移動してから受理する/採用する)と一致しており、不要な条件の詰め込みではない
