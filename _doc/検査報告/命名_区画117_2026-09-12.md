# 命名検査報告 — 区画117 crates/blitz_sim/src/contact/non_penetration ほか

## 範囲・時点

- 区画名: crates/blitz_sim/src/contact/non_penetration(区画番号117)
- ファイル数: 25(うち非貫通(non_penetration)配下14、normal_tangential_system/acceptance配下11)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main、db815090時点。このツリーは編集していない)
- 読んだ規約: 全プロジェクト共通CLAUDE.mdの「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、CLAUDE.mdの「命名」節。用語定義ファイルは `_doc/計画/ユビキタス言語.md`(407KBのため関連語のみgrepで参照)

## 検査件数

- 直読したファイル数: 25/25(全件直読、grepだけで済ませたファイルは無い)
- 検査した識別子: 型・列挙・メソッド・フィールド・定数・関数・変数を含め約230件(全ファイルの宣言と主要な変数を読解)
- 違反: 0件
- 違反の疑い: 1件

## 違反の一覧

該当なし。

## 違反の疑い

1. `crates/blitz_sim/src/contact/non_penetration/fixture.rs:97` | `z軸まわりの四分の一回転の姿勢`(関数名。`correction_tests.rs:17,90`で使用) | 識別子へのASCII混入の禁則に対する疑い | 「z」はASCIIの英字であり、指示にある禁止例(`段階A2の候補`)と同型の「英字を識別子へ混ぜた」形に見える。ただし`段階A2`が場当たりの内部コード名であるのに対し、x/y/z軸は座標系の軸を指す万人共通の数学記法であり、非専門家にも通用する(グラフのx軸・y軸と同じ)。さらにこの綴りは本区画に限らずリポジトリ全体(66ファイル)で一貫して使われている確立済みの命名慣習であり、この区画だけの逸脱ではない。断定はせず、方針(慣習として容認するか、全域一括で「Z軸」等への言い換えを検討するか)の判断をリネーム員/親へ委ねる
- 改名の素案(もし是正するなら): `Z軸まわりの四分の一回転の姿勢` (大文字化しても英字混入自体は解消しないため、この案は暫定。全域の軸命名規約が定まっていない状態でこのファイルだけ変えると1語1義の観点で不整合が生じる)

## 違反ゼロの範囲

上記1件の疑いを除き、25ファイル全件で受理条件8つ・日本語命名の質(頭字語・直訳調・膠着圧縮の禁止)・呼称と語の選択(漢字1文字への圧縮禁止)のいずれにも抵触する識別子は見つからなかった。

検査して確認した点:

- 品詞の規律: 値は名詞句(`非貫通の一刻みの係数`・`残差と受理の許容差`)、処理は動詞句「〜する」(`刻み幅で解く係数を導く`・`貫通と乗数から始める`)、真偽は「〜か」(`貫通しているか`・`許容差の内側か`・`動かせない組か`)で一貫している
- 5W1Hの必要格: `乗数の増分を第1の参加者の補正へ分配する`・`捨てた成分を戻した残差を記す`のように、何を・どこへ・どうするかが名前に現れている。「オーナーが不合格にした実例」で指摘された「実体と語が合わない名詞1語」「詰め込みすぎ」の形は見当たらない(例: `混合連立の受理の材料`は定義(5つの契約の測定値を束ねる)と語が一致している)
- 裸の動詞の禁止: `生成する()`はすべて型に属する関連関数であり型名が目的語を担う(`非貫通の解き方::生成する`等)。ASCIIモジュール直下の裸の動詞は無い
- 1語1義: 「非貫通」「受理」「相補条件」「円錐」「連立」等の語は本区画内で一貫した意味を保っている。「記録」「文脈」のような衝突は無い
- 直訳調: `record`→`記録`、`context`→`文脈`、`lookup`→`引く`のような直訳は無い。「射影」「連立」「有効集合」「相補条件」は数学用語としてユビキタス言語(判断12・判断13の設計文書)と対応している
- ASCII/ギリシャ文字/数学記号の混入: 数式の説明はコメント内(`α̃`・`Σ_j`等)にのみ現れ、識別子には現れない。唯一の例外が上記の疑い1件(`z軸`)である
