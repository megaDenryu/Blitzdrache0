# 命名検査報告 区画041

- 区画名と範囲: `crates/blitz_collision/src/height_field` 第2区画(区画番号041、2ファイル)
- 時点: main の db815090(作業ツリー `C:/devs/Blitzdrache0_roller`。このツリーは編集していない)
- 直読したファイル数: 2/2(全ファイルを直読。grepだけで済ませたファイルは0)
  - `crates/blitz_collision/src/height_field/triangle.rs`
  - `crates/blitz_collision/src/height_field/unit_direction.rs`

## 検査対象の識別子

- `triangle.rs`: 列挙 `升目の三角形`、枝 `奥側`・`手前側`
- `unit_direction.rs`: 構造体 `地表の単位向き<数>`、フィールド `東成分`・`天頂成分`・`南成分`、関連関数 `正して生成する`、メソッド `逆を向く向き`・`東成分()`・`天頂成分()`・`南成分()`・`方向へ狭める`

## 違反の一覧

該当なし(0件)。

## 違反の疑い

1. `unit_direction.rs:20` | `正して生成する(東成分, 天頂成分, 南成分) -> Self` | 条件3(5W1Hの必要格)に対して疑い | 「正す」の目的語(「長さを1に」)が名前に無く、引数名の `東成分` は正規化前の生の値であるにもかかわらず正規化後のフィールド名と同じ字面である。関連関数であり型名 `地表の単位向き` が対象を補うため単独では不合格にしないが、`長さを1に正して生成する` のように対象を明示する方が読み手が引数の意味(正規化前の生の成分)を取り違えにくい。断定はしない
2. `unit_direction.rs:54` | `方向へ狭める` | 条件7(直訳の検出)に対して疑い | 「狭める」は英語を知らない読み手にも「範囲を狭める」の比喩として通じるが、実体は「倍精度から単精度へ変換する(情報を落とす)」処理であり、ファイル冒頭のコメントで意味を補足して初めて分かる。コメント無しで単独に読むと「向きの範囲を絞り込む」等に誤読されうる。ただし本ファイル自身が冒頭コメントで意味を明示しており、`_doc/計画/ユビキタス言語.md`にも「単精度へ狭める」という同じ比喩で説明されているため、プロジェクト内で定着した語として扱い違反とはしない。今後この語を他ファイルで補足無しに使うなら条件7へ抵触しうる

## 違反ゼロの範囲

`crates/blitz_collision/src/height_field/triangle.rs` と `unit_direction.rs` の全識別子(型・列挙の枝・フィールド・関連関数・メソッド)は受理条件8つをいずれも満たし、`升目の三角形`・`地表の単位向き`は `_doc/計画/ユビキタス言語.md`(790行目・794行目)の定義と実体が一致することを確認した。ASCII・ギリシャ文字・数学記号の識別子混入は無い。省略した検査は無い(2ファイルとも全文を読んだ)。
