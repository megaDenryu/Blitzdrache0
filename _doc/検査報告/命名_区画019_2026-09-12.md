# 命名検査報告 区画019: crates/blitz_assembly/src/mating

- 区画: crates/blitz_assembly/src/mating(区画番号019、31ファイル。mating配下20ファイル + part配下11ファイル)
- 対象ツリー: C:/devs/Blitzdrache0_roller(main、db815090時点。編集していない)
- 直読したファイル数: 31/31(全件直読。grepだけで済ませたファイルは0件)
- 参照した必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`「部品と接合の語彙」節、`_doc/設計/部品カタログと接合点.md`(正本)

## 直読したファイル一覧

mating: center_position_check.rs, child_origin.rs, child_scale.rs, chimney_placement_tests.rs, containment_check.rs, derived_scale_tests.rs, error.rs, facing_frame.rs, fit_check.rs, floor_placement_tests.rs, joint_fit.rs, mating_fixture.rs, mating_rejection_tests.rs, mating_tests.rs, normal_offset_tests.rs, oriel_placement_tests.rs, parent_transform.rs, rotated_root_tests.rs, scaled_face.rs, size_match_check.rs

part: bounding_box.rs, catalog.rs, catalog_builder.rs, corner_transform.rs, definition.rs, error.rs, oriented_box_tests.rs, part_fixture.rs, part_id.rs, part_tests.rs, stored_part.rs

省略は無い。31ファイル全件の型・関数・メソッド・フィールド・変数・定数・列挙の枝を直読で検査した。

## 総評

この区画は、`_doc/設計/部品カタログと接合点.md`(正本)が定義する語彙(接合点・接合種別・接合の様式・一致・包含・子の倍率・法線方向のずらし・接合面の実寸 等)をコードの識別子がそのまま使っており、型・関数名から実体への対応が正本と一致していることを1件ずつ照合した。値オブジェクト様式(private フィールド + 検証付き `生成する()`)、動詞句の処理名、日本語のレシーバによる文脈補完もほぼ全ファイルで規約どおりに保たれている。ASCII・ギリシャ文字・数学記号を識別子へ混ぜた例は0件だった。

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
| --- | --- | --- | --- | --- |
| crates/blitz_assembly/src/mating/scaled_face.rs:72 | `軸に沿った伸び`(自由関数) | 条件4(品詞の規律) | この関数は「その軸に沿って接合面の長さが何倍に伸びるか」を計算する処理(倍率を掛けた成分の長さを元の長さで割る)であり、値ではなく処理である。にもかかわらず動詞を持たない名詞句のまま定義されている。同じファイル内の同種の計算関数`比を求める`・`長さを求める`(いずれも動詞句)と並べると、この関数だけが「計算結果を保持する値のフィールド」であるかのように読める(品詞が処理を予告しない)。 | `軸に沿った伸びを求める` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
| --- | --- | --- | --- |
| crates/blitz_assembly/src/mating/floor_placement_tests.rs:27, 30 | `床のはめ口の高さメートル`・`階高メートル`(定数) | 条件8/日本語命名の質(助詞を省いた膠着圧縮) | 「〜の高さ」という完成した名詞句に「メートル」を助詞なしで直接連結している。意味は復元できる(単位を表す接尾のみで語の圧縮ではない)ため、他の定数`壁を骨格の面へ食い込ませる量`のように完全な句で書く慣行との不整合の疑いに留める。 |
| crates/blitz_assembly/src/mating/joint_fit.rs:57 | `成分へ写す`(私有の自由関数) | 条件3(5W1Hの必要格) | 「何を」成分へ写すのかが名前に無い(引数の型`位置<ローカル>`でしか分からない。条件2は引数の型による補完を認めていない)。ただし同じファイル内でしか呼ばれない private ヘルパーであり、紛れの実害は小さいため断定しない。 |

## 違反ゼロと確認した範囲

上記2件の疑いおよび1件の違反を除く、31ファイル中の残り全識別子(接合点・接合種別・接合の様式・接合・子の倍率・接合面の実寸・部品カタログ・部品カタログ組み立て器・収蔵部品・部品ID・部品定義・部品の境界箱・各種試験関数名等、300を超える識別子)は受理条件8項目・日本語命名の質・呼称と語の選択のいずれにも違反しない。特に、値オブジェクト様式の`生成する()`はすべて型名が目的語を担う形で使われており(条件5)、`_doc/設計/部品カタログと接合点.md`が定義する語(接合面の姿勢・接合面上の位置・法線方向のずらし・接合の様式の一致/包含/未定 等)とコードの識別子の対応は全件一致を確認した。ASCII・ギリシャ文字・数学記号の識別子混入は0件、1語1義の衝突(用語定義ファイルとの照合)も0件だった。
