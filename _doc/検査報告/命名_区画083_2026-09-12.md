# 命名検査報告 区画083

- 区画名と範囲: crates/blitz_render/src/clustered_lighting 第2区画(区画番号083、4ファイル)
- 対象ファイル:
  - crates/blitz_render/src/clustered_lighting/sphere_boundary_tests.rs
  - crates/blitz_render/src/clustered_lighting/sphere_intersection.rs
  - crates/blitz_render/src/clustered_lighting/sphere_intersection_tests.rs
  - crates/blitz_render/src/clustered_lighting/test_fixture.rs
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 4/4(全ファイルを直読。grepだけで済ませたファイルは0件)
- 用語照合: `_doc/計画/ユビキタス言語.md` へ grep で以下の登録語を照合した: クラスタ格子・クラスタ格子のセルの錐台・局所光の影響範囲の球・影響半径・クラスタ写像・クラスタ格子のセル添字・区間。いずれも定義と実装の使い方が一致していることを確認した

## 違反の一覧

1. `crates/blitz_render/src/clustered_lighting/sphere_intersection.rs:30` | `z成分`(パラメータ名) | 落ちた条件: 識別子へのASCII文字混入の禁止(受理条件の運用上の付帯規律。実例「段階A2の候補」と同型) | ASCIIの英字`z`が日本語識別子の中に直接混ざっており、日本語の名として完結していない。同じ関数の他方の引数`成分`と並べたときに、どちらが横軸相当でどちらが奥行き相当かを英字1文字の意味(座標軸のz)を知らないと復元できない | `奥行き成分`(もう一方の引数`成分`は呼び出し文脈により横または縦の成分を指すため`横または縦の成分`のように呼び出し側で読み替えられる名にするか、関数を横用・縦用へ分けて各成分に`奥行き成分`と対の成分名を与える)

2. `crates/blitz_render/src/clustered_lighting/sphere_intersection.rs:30` | `側面の対から半径以内か` | 条件3(5W1Hの必要格) | 「側面の対から半径以内か」という述語には主語(何が半径以内にあるかを判定するのか)が無い。実際は光の中心点の座標成分(`成分`・`z成分`)を渡しているが、名前は「対象が何であるか」を一切含まない。単独で読むと「側面の対」自体が何かに含まれるかのように読め、点の位置を判定していることが読み取れない | `点が側面の対から半径以内か`

3. `crates/blitz_render/src/clustered_lighting/test_fixture.rs:65-68` | `検査用のセルの錐台`(関数) | 条件4(品詞の規律。処理なのに名詞形) | `写像`と`添字`の2引数を受けて内部で`写像.セルの錐台を求める(添字)`を呼び出す処理(計算・失敗しうる値の取り出しを含む)であるにもかかわらず、名前が動詞を持たない名詞句である。同じファイルの`検査用の球を作る`・`検査用のセル添字を作る`は動詞「作る」を持ち、この関数だけ動詞が欠けており1つのファイル内で品詞の運用が割れている | `検査用のセルの錐台を求める`

4. `crates/blitz_render/src/clustered_lighting/test_fixture.rs:72` | `セル中央のビュー空間の位置`(関数) | 条件4(品詞の規律。処理なのに名詞形) | `写像`と`添字`を受け取り、区間の下限・上限から中央値を計算して位置を構成する処理であるにもかかわらず、名詞句のみで書かれている。呼び出し側(`sphere_boundary_tests.rs`・`sphere_intersection_tests.rs`)では`セル中央のビュー空間の位置(写像, 添字)`という形で計算処理として呼ばれており、値そのものを保持するフィールドではない | `セル中央のビュー空間の位置を求める`

## 違反の疑い(断定しない)

1. `crates/blitz_render/src/clustered_lighting/sphere_boundary_tests.rs:81` | `角から中心への向きの成分`(関数) | 条件3に対する疑い | 名前は「角から中心への向き」という1本のベクトルの成分を指すように読めるが、実際は第3引数`向き`という任意の方向ベクトルへの射影(内積)を計算しており、どの方向へ射影した成分なのかが名前に出ていない。関数名の「向き」と第3引数名「向き」が指すものが違う(前者は角→中心の変位ベクトル、後者は射影先の単位ベクトル)ため、名前だけで2つの「向き」を混同しうる。ただし、この関数はモジュール私有(pub指定なし)であり、呼び出し側1箇所(直後の`for`ループ)でしか使われず、呼び出し文脈(`面の外向き`という変数を渡している)から復元できる余地はある。断定はリネーム員の判断に委ねる | `角から中心への変位の指定方向への成分`

2. `crates/blitz_render/src/clustered_lighting/test_fixture.rs:40` | `検査用のクラスタ写像`(関数) | 条件4に対する疑い | 引数を取らずクラスタ写像の値を組み立てて返す関数であり、上の2件(`検査用のセルの錐台`・`セル中央のビュー空間の位置`)と同型の品詞の揺れだが、引数が無く実質的には固定のテスト用フィクスチャ値を返すだけであるため、「値の名」として名詞句のまま読める余地がある(値オブジェクトのgetter的な性格)。上の2件ほど明確な処理性(入力に応じた計算)を持たないため、疑いに留める | `検査用のクラスタ写像を作る`

3. `crates/blitz_render/src/clustered_lighting/sphere_intersection_tests.rs:68-70` | `奥の刻み`(変数) | 1語1義に対する疑い | 同じ概念(奥行き軸)を、このファイルの他の識別子(`奥行きの区間`・`奥行きの向き`)は「奥行き」とフルスペルで呼ぶ一方、ここだけ「奥」と省略している。「横」「縦」と3軸をそろえる目的の省略とも読めるため、圧縮語の禁止(順4・段3型)には該当しないが、同一区画内での用語の不統一ではある | `奥行きの刻み`

## 違反ゼロの範囲

- `sphere_intersection.rs`の型に属するメソッド`球と交わりうるか`と、その中のローカル変数(`中心`・`半径`・`奥行き`)は受理条件をすべて満たす
- `sphere_boundary_tests.rs`と`sphere_intersection_tests.rs`のテスト関数名(`側面から半径の内側にある球は交わると判定される`等の4件)は動作の主語・対象・結果を文として明示しており、受理条件1〜8のいずれにも抵触しない
- `test_fixture.rs`の`検査用の固定値から作った値を取り出す`・`検査用の球を作る`・`検査用のセル添字を作る`・`検査用の一次元の添字から戻したセル添字`と、公開定数`画面の幅`・`画面の高さ`・`近い奥行き`・`遠い奥行き`・`横の半角の正接`・`縦の半角の正接`は受理条件をすべて満たす
- 4ファイルを通じて、実体(型のフィールド・分岐の中身)と名前の不一致(「試験の許容差の当て方」型の違反)、名詞1語だけの圧縮(「連立」型の違反)、条件を詰め込みすぎた枝名(「許容差の内側の点の行を積まず右辺を零にする」型の違反)は検出しなかった
