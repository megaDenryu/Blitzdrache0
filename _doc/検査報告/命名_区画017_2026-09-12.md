# ローラー検査(命名の軸) 区画017: crates/blitz_assembly/src/expansion

- 区画番号: 017
- 範囲: `crates/blitz_assembly/src/expansion` 配下の40ファイル(`block_017.txt`のとおり)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の `db815090` 時点。このツリーは編集していない)
- 直読したファイル数: 40 / 全40(grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」、`_doc/計画/ユビキタス言語.md`(「部品と接合の語彙」節を中心に照合)

## 違反の一覧

なし(0件)。

## 違反の疑い

なし(0件)。検討したが合格側に倒した項目を記録する。

- `crates/blitz_assembly/src/expansion/frame_extension.rs:47-49`(`骨格へ継ぎ足すもの::屋根を載せる { 部品, 下面 }`)の`下面`は2文字と短いが、`上面・下面`はユビキタス言語「一間四方の骨格」節が持つ登録済みの対概念であり、変数だけで単独に読ませる位置でもない(定義側は`屋根を載せる`という枝の中の宣言、使用側の`crates/blitz_assembly/src/expansion/frame_step_builder.rs:62`は同じ枝名で分解しており、枝名がレシーバ相当の文脈を与える)。受理条件2の「型名::関連関数の型名」に準じる補完として合格と判定した
- `crates/blitz_assembly/src/expansion/instruction.rs:28-35`の`据えた部品の番号`が`添字()`(private)と`番号を返す()`(public)という2つの近い意味のアクセサを持つ。用途が違う(`添字`は配列の添字への変換専用、`番号を返す`はエラー文面へ載せる利用者向けの値)ため1語1義違反ではないと判定したが、読み手が両者の使い分けに迷う余地はわずかに残る

## 違反ゼロの範囲

`crates/blitz_assembly/src/expansion`配下の40ファイル全部(型・関数・メソッド・フィールド・変数・定数・列挙の枝を含む)。

## 検査所感

- この区画は型・enum・メソッドのほぼ全てに、実体を説明する`//!`または`///`が付いており、名前とドキュメントを突き合わせて「名前の語が実体を表すか」を照合できた。owner不合格実例の3形(実体と合わない語・名詞1語の圧縮・詰め込みすぎ)に該当する識別子は見つからなかった
- 候補選択肢を表す列挙(`骨格へ継ぎ足すもの`の`骨格を継ぐ`・`屋根を載せる`、`はめ口へ入れるもの`の`壁をはめる`、`壁の外面の飾り`の`差し込む`、`骨格へ張る床`の`床を張る`等)は動詞句の枝名を持つが、これは「選んだ場合に何が起きるかを表す候補」という一貫した設計であり(候補の数が選ばれる割合を決める、という同一の設計理由がコメントに繰り返し書かれている)、品詞の規律(値=名詞句)への違反ではなく「選択肢としての行為の宣言」という妥当な用法と判定した
- ASCII文字・ギリシャ文字・数式記号を識別子に混入した例は無い。モジュール/ファイル名はASCII(規約どおり)で、クレート内の型・関数は全て日本語
- `_doc/計画/ユビキタス言語.md`の「部品と接合の語彙」節に登録済みの語(部品・接合点・接合種別・展開器・部品ごとの配置表・部品ごとの配置の集約・木の規則・幹の節数等)と、この区画の型名が綴りまで一致していることを確認した
