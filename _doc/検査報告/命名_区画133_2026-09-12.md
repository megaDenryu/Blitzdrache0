# 命名検査報告 区画133

- 区画名と範囲: `crates/editor_server/src/resource/building_grid`・`building_outline_catalog`・`chunk_structure_version`(legacy_building_placement.rsのみ)・`command`・`grid` の26ファイル
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, db815090時点。このツリーは編集していない)
- 検査対象の一覧: `C:/Users/POKR30~1/AppData/Local/Temp/claude/C--devs-Blitzdrache0/612e37aa-a38d-4d29-a7a3-b0e9508d9f15/scratchpad/roller/block_133.txt` に記載の26ファイル
- 直読したファイル数: 26 / 26(全ファイルを直読した。grepだけで済ませたファイルは無い)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`(該当語のみ grep で照合。マザーハイトマップ・ソース・ベイクの3語を照合した)

## 違反の一覧

違反0件。

## 違反の疑い

1. `crates/editor_server/src/resource/building_outline_catalog/validation.rs:25` | `定義を検証する`(私有の自由関数) | 条3(5W1Hの必要格)に対する疑い | この関数はASCIIファイル(`validation.rs`)直下の自由関数であり、呼び出し側は `定義を検証する(定義, &mut 識別子一覧)?;` という変数名だけの呼び出しで、型名やレシーバによる補完が効かない(受理条件2)。名前単独では「どの定義か」(建物外形定義か、升目の宣言か等)を復元できない。ただし同じファイル内でしか呼ばれず、直前の文脈(`for 定義 in &self.建物定義一覧`)で読める範囲は狭いため、断定はしない。改名の素案: `建物外形定義を検証する`
2. `crates/editor_server/src/resource/building_grid/source_conversion.rs:39` | `座標を写す`(私有の自由関数) | 条3(5W1Hの必要格)に対する疑い | 同じくASCIIファイル直下の自由関数で、呼び出しは `座標を写す(self.格子.根の升目)` のように変数名のみ。このリポジトリには`チャンク座標`・`升目の座標`・`平面の位置`など複数の座標系の型があり、「座標」という名前だけでは升目の座標を写す処理だと復元できない。ただし本体の引数型は`升目の座標`固定で、ファイル冒頭コメントが「型契約の建物の格子を...写す工程」と明示しており、同ファイル内の他の`〜を写す`群(升目・はめ口・壁の種類・飾り)と並んで読めば意味は取れる。改名の素案: `升目の座標を写す`

## 違反ゼロの範囲

上記2件の疑い以外、26ファイル全域(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)で受理条件8つ・命名の質・呼称と語の選択への違反、および識別子へのASCII英字・ギリシャ文字・記号の混入は検出しなかった。「実体と合わない語」「名詞1語の圧縮」「詰め込みすぎ」の3形も検出しなかった。`マザーハイトマップ`・`ソース`(建物の格子ソース等)・`ベイクする`はいずれもユビキタス言語に登録済みの語であることを照合し、合格とした。
