# 命名検査報告: 区画069(crates/blitz_game/src 第2区画)

## 範囲・時点

- 作業ツリー: C:/devs/Blitzdrache0_roller (main の db815090。このツリーは編集していない)
- 区画番号: 069(6ファイル)
- 対象ファイル:
  1. crates/blitz_game/src/sweep_hit.rs
  2. crates/blitz_game/src/tour_progress.rs
  3. crates/blitz_game/src/tour_progress_tests.rs
  4. crates/blitz_game/src/tour_route.rs
  5. crates/blitz_game/src/walk_only_state.rs
  6. crates/blitz_game/src/world_shape_port.rs
- 直読したファイル数: 6/6(全ファイルを直読。grepだけで済ませたファイルは無い)
- 検査した識別子数: 約50件(型・trait 4、enum の枝 2、struct のフィールド 3、メソッド・関連関数 21、テスト関数 6、テスト内のローカル変数・ヘルパー関数 十数件)
- 読んだ必読リスト: `.claude/global-reference/全プロジェクト共通CLAUDE.md`(「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」)、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

該当なし。

## 違反の疑い

該当なし(疑いに分類するほどの紛れも見当たらなかった)。

判断に迷い、用語定義ファイルと照合して合格と判定したもの:

- `crates/blitz_game/src/walk_only_state.rs:24` の型パラメータ `口`(`一刻み進めた状態を返す<口: 世界の形を尋ねる口>(self, 口: &mut 口, ...)`): 漢字1文字だが、「着手順4→順4」のような長い語の切り詰めではなく、trait名`世界の形を尋ねる口`が既に使っている独立した1語(「口」=窓口・受け口の意)をそのまま型パラメータ名に転用したもの。ユビキタス言語1112行台の「世界の形を尋ねる口」の定義と一致し、レシーバ`変数.メソッド()`と同じ枠(型パラメータ名がその後の値パラメータ名と揃う)で読めるため、呼称と語の選択の条1(圧縮禁止)には当たらないと判定した
- `crates/blitz_game/src/walk_only_state.rs` の `足元の地面の高さ::高さ場から参照した高さ(高さ)`: 「参照した」はオーナー確定の訳語(lookup/sampleの「引く」→「参照する」、`CLAUDE.md`「命名」節に明記)であり直訳調ではないと判定した
- `crates/blitz_game/src/tour_progress_tests.rs` の `検査用の道順()`・`東の位置(東メートル: f64)`: 値を返す処理を名詞句で命名しているが、値オブジェクト様式のファクトリ命名(型名が目的語を担う `生成する()`)と同じ位置づけのテスト用フィクスチャ生成であり、返す値の実体をそのまま言い当てているため受理テスト(条1)に合格すると判定した

## 違反ゼロの範囲

上記6ファイルの全識別子(型・trait・enum・フィールド・メソッド・関連関数・テスト関数・ローカル変数)を受理条件8つ・命名の質・呼称と語の選択・ユビキタス言語との照合で検査し、違反は0件だった。特に「掃引が最初に触れる面」「場所巡りの進行」「目的地番号」「場所巡りの道順」「歩くだけのゲームの状態」「世界の形を尋ねる口」はいずれもユビキタス言語の登録句と語まで一致しており、実体との食い違いは見つからなかった。
