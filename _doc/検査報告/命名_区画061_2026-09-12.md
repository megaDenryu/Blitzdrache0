# 命名検査報告: 区画061 crates/blitz_engine/src/sky/atmosphere

## 範囲・時点

- 区画番号: 061
- 区画名: crates/blitz_engine/src/sky/atmosphere (および同区画リストに含まれる crates/blitz_engine/src/sky/sky_draw_policy/default.rs、crates/blitz_engine/src/static_shape 配下)
- 作業ツリー: C:/devs/Blitzdrache0_roller (main の db815090、このツリーは編集していない)
- 一覧ファイル: block_061.txt に記載の29ファイル

## 直読数・省略

- 直読したファイル数: 29 / 29(全数)
- grepだけで済ませたファイル数: 0
- 省略: 無し。一覧の全ファイルを冒頭から末尾まで直読した
- 補足: `static_shape/index.rs` は `mod` 宣言のみのファイルであり、そこが宣言する子モジュール群(`candidate.rs`・`chunk_span.rs`・`error.rs`(index配下の別ファイル)・`query.rs`・`query_gather.rs`・`truncated_cover.rs`・`index_*_tests.rs`)は block_061.txt の一覧に含まれていないため、本検査の対象外とした(別区画の担当と判断する)

## 違反の一覧

該当なし(違反0件)。

## 違反の疑い

1. `crates/blitz_engine/src/sky/atmosphere/key_digest.rs:34,43,47,51` | `実数` / `長さ` / `逆長さ` / `三成分`(`キー畳み込み`のメソッド) | 条件4(品詞の規律)に対する疑い | これらは `&mut self` を取り値をビット列へ書き込んで内部状態を進める処理(副作用を持つ工程)だが、名前は「実数」「長さ」のように処理の対象の型名をそのまま名詞で置いただけで、動詞句になっていない。同じ型の`完成する`・`新規`が動詞句/名詞(生成)の使い分けを守っているのに対し、この4つだけが「値を取り込む」という動作を名詞1語に圧縮している。レシーバ`畳み込み`(名詞)からは「取り込む」という動作を補完しづらく、`畳み込み.実数(値)`は初見で「実数を返す」get相当のようにも読める。改名の素案: `実数を取り込む` / `長さを取り込む` / `逆長さを取り込む` / `三成分を取り込む`
2. `crates/blitz_engine/src/sky/atmosphere/key_digest.rs:17-18` | `FNVの初期値` / `FNVの乗数` | 専門用語の表記(必読節)に対する疑い | `FNV`はハッシュアルゴリズム名(Fowler-Noll-Vo)の略で、識別子自体は受理条件8つには抵触しない(アルゴリズムの固有名を訳さず保つのは`_doc/計画/ユビキタス言語.md`の第3群語彙の扱いに準ずる)が、モジュール冒頭の`//!`にも識別子の傍にも「FNVが何の略か」の初出補足が無く、非専門家が読んでも由来が復元できない。識別子の改名は不要と考えるが、コメントへの初出補足(例: 「FNV-1a(Fowler-Noll-Vo。バイト列を掛け算とXORで畳み込むハッシュ関数)」)を推奨する

## 違反ゼロの範囲

`crates/blitz_engine/src/sky/atmosphere` 配下の型契約ファイル13件(absorption_component.rs・albedo_rgb.rs・asymmetry.rs・density_layer.rs・density_profile.rs・extinction_rgb.rs・key_digest.rs・medium_policy.rs・mie_component.rs・planet_shape.rs・rayleigh_component.rs・static_key.rs・atmosphere_tests.rs)、その子モジュールのテスト4件(atmosphere_tests/component_variants.rs・policy_value_tests.rs・policy_variants.rs・static_key_tests.rs)、`sky_draw_policy/default.rs`、および`crates/blitz_engine/src/static_shape`配下の11件(child_shape.rs・child_shape_identifier.rs・chunk_shapes.rs・collision_object.rs・error.rs・index.rs・object_identifier.rs・shape_fixture.rs・shape_tests.rs・storage.rs・storage_tests.rs)は、型名・関連関数・メソッド・フィールド・変数・列挙の枝のいずれも受理条件8つ、日本語命名の質、呼称と語の選択に照らして違反ゼロである。特に`_doc/計画/ユビキタス言語.md`の「大気媒体方針」「惑星形状」「レイリー成分」「ミー成分」「吸収成分」「地表反射率RGB」「大気静的キー」「複合形状」「静的な衝突対象」「衝突対象の安定識別子」「チャンクの静的物理形状」「子形状識別子」の定義と実装のフィールド・メソッドが1対1で一致しており、5W1Hの格・品詞の規律・目的語の明示のいずれも保たれている。
