# 命名検査 区画035: crates/blitz_asset_compiler/src/visual_sample

- 区画番号: 035
- 範囲: crates/blitz_asset_compiler/src/visual_sample 配下2ファイルおよび crates/blitz_asset_compiler/tests/integration 配下20ファイル(小フォルダheight_field_roundtrip/・multi_material_fixture/を含む)、計22ファイル
- 作業ツリー: C:/devs/Blitzdrache0_roller(main, db815090時点。このツリーは編集していない)
- 直読したファイル数: 22/22(全ファイルを直読した。grepだけで済ませたファイルは無い)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_asset_compiler/tests/integration/multi_material_fixture/mod.rs:52 | `長さをu32へ`(関数) | 条1(受理テスト)・条4(品詞の規律) | 目的格「長さを」と着点格「u32へ」だけが並び、動詞が無い。処理(関数)なのに完結した動詞句でなく、「これは何?」に単独で答えられない(「u32へ…変換する」のか「u32へ詰める」のか「u32へ書く」のか名前だけでは決まらない) | `長さをu32へ変換する` |

## 違反の疑い

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| crates/blitz_asset_compiler/tests/integration/multi_material_fixture/document_json.rs:6 | `文書jsonを作る`(関数) | 条2(日本語命名の質・頭字語の混在) | 小文字の英語略語`json`を日本語動詞句の中へそのまま混ぜている。JSONはUI/CPU程度に一般に通用しているとまでは言い切れず、非専門家が読んで意味が取れるかは境界例である。ただし同型の書き方(`walkクリップ`・`id`等)がこの区画とこの周辺の試験コード全体に既に定着しており、この1件だけの改名は表記の一貫性を崩す。一括是正の要否は親の判断に委ねる | (改名するなら)`文書JSONを作る`または`文書の構造化文字列を作る` |
| crates/blitz_asset_compiler/tests/integration/multi_material_fixture/mod.rs:11-12 | `JSONチャンク種別`・`BINチャンク種別`(定数) | 条2 | 頭字語`JSON`・`BIN`を含むが、これらはglTFバイナリ形式(.glb)仕様が定義するチャンク種別の正式な呼び名そのものであり、外部形式の語彙をそのまま指す用途である。翻訳すると仕様書と照合できなくなるため、違反と断定しない |

## 違反ゼロの範囲

上記1件の違反と2件の疑いを除き、次の観点はすべて合格だった。

- `crates/blitz_asset_compiler/src/visual_sample/fixed_objects.rs`・`scene.rs`: 型・関連関数・フィールド・定数(`庭の中心メートル`・`材質見本の立体の描画対象番号`等)は、単独あるいは日本語のレシーバ・型名による補完で「これは何?」に答えられる。処理は動詞句、値は名詞句で書かれている
- `tests/integration/`直下および`height_field_roundtrip/`・`multi_material_fixture/`配下の試験関数名(例: `安定idで登録した高さ場を読み口が見つけて答える`・`貼り合わせた高さ場の各チャンクの角の格子点が…`)は、何を・どう検証するかの格が明示されており、詰め込みすぎ・目的語欠落・名詞1語への圧縮は見られなかった
- 識別子へのASCII英字単独記号・ギリシャ文字・数式記号(`A2`・`Σλ_n`風の混入)は1件も無かった。ASCIIが現れるのは`id`・`u32`・`json`・`JSON`・`BIN`・`walk`のような、既存の慣用または外部形式・外部標準サンプル(`Fox.glb`のクリップ名等)の固有名詞に限られ、いずれも独自の抽象概念を英語のまま名指ししたものではない
- 用語定義ファイル(`_doc/計画/ユビキタス言語.md`)に照らして、この区画の識別子は既存の登録語(チャンク座標・高さ格子・カタログ・実行時形式等)と矛盾なく使われており、1語2義の衝突は検出されなかった

