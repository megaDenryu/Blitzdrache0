# 命名検査報告 区画020

- 区画名: crates/blitz_asset_compiler/examples/check_glb ほか(実体は generate_source_assets / compile_assets / check_glb の3examples配下)
- 範囲: block_020.txt に列挙された40ファイル
- 作業ツリー: C:/devs/Blitzdrache0_roller(main, db815090時点。編集していない)
- 直読したファイル数: 40 / 40(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 検査軸: グローバルCLAUDE.md「日本語命名の受理条件」(8条件)・「日本語命名の質」・「呼称と語の選択」・「専門用語の表記」・`_doc/計画/ユビキタス言語.md`

## 違反の一覧

### 裸の動詞(条件2・条件3・条件5): ASCIIモジュール直下の`書き出す`/`保存する`/`実行する`

以下はいずれも、ファイル名がASCII(英語)のモジュール直下に置かれた`pub(crate)`または`pub(super)`の自由関数であり、名前が動詞1語のみで目的語を持たない。受理条件2は「ASCII識別子(モジュール名)は読まれないものとして扱い、補完力ゼロ」と定めており、モジュール名(`chunk_world`・`night_lights_world`等の英語)が実体を補ってはならない。呼び出し側は`モジュール名::書き出す(...)`の形で呼ぶため、日本語の名前だけを見た読み手は「何を書き出すのか」を復元できない。オーナー実例の`create::生成する`と同型の違反である。

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_asset_compiler/examples/generate_source_assets/chunk_world.rs:25 | `書き出す` | 2,3,5 | ASCIIモジュール名`chunk_world`は補完力ゼロ。単独では板の共有バッファ・glTF文書・目録一式のどれを書き出すか不明 | `チャンク世界のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/night_lights_world.rs:40 | `書き出す` | 2,3,5 | 同上。夜の多光源世界のソース一式であることが名前に無い | `夜の多光源世界のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/part_house_row_world.rs:36 | `書き出す` | 2,3,5 | 同上。家並みの地面という対象語が無い | `家の並びの地面のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/part_frame_row_world.rs:39 | `書き出す` | 2,3,5 | 同上。骨格の並びの地面という対象語が無い | `骨格の並びの地面のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/part_tree_row_world.rs:36 | `書き出す` | 2,3,5 | 同上。木の並びの地面という対象語が無い | `木の並びの地面のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/stone_hut_world.rs:34 | `書き出す` | 2,3,5 | 同上。屋内検収世界のソース一式であることが名前に無い | `屋内検収世界のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/surface_layer_tiles.rs:22 | `書き出す` | 2,3,5 | 同上。地表材質のタイル画像であることが名前に無い | `地表材質のタイル画像一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/terrain_visual_world.rs:46 | `書き出す` | 2,3,5 | 同上。目視見本世界のソース一式であることが名前に無い | `目視見本世界のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/terrain_world.rs:28 | `書き出す` | 2,3,5 | 同上。地形検証用世界のソース一式であることが名前に無い | `地形検証用世界のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/village_world.rs:34 | `書き出す` | 2,3,5 | 同上。見本の集落の地面であることが名前に無い | `見本の集落の地面のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/vegetation_world.rs:42 | `書き出す` | 2,3,5 | 同上。植生検証世界の原型glTF一式であることが名前に無い | `植生検証世界のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/fox_tour_world.rs:18 | `書き出す` | 2,3,5 | 同上。場所巡りの世界のソース一式であることが名前に無い(モジュール冒頭コメントには書いてあるが識別子には現れない) | `場所巡りの世界のソース一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/smoke_assets.rs:11 | `書き出す` | 2,3,5 | 同上。スモーク検証用アセット一式(quad・シャドウ・材質境界・遠方環境)であることが名前に無い | `スモーク検証アセット一式を書き出す` |
| crates/blitz_asset_compiler/examples/generate_source_assets/textures.rs:8 | `保存する` | 2,3,5 | 何を保存するかが無い。実体はquad用の2枚の単色PNG | `quad用の単色画像2枚を保存する` |
| crates/blitz_asset_compiler/examples/generate_source_assets/shadow_scene_texture.rs:9 | `保存する` | 2,3,5 | 何を保存するかが無い。実体はシャドウ検証用の4x4白PNG1枚 | `シャドウ検証用の白画像を保存する` |
| crates/blitz_asset_compiler/examples/generate_source_assets/main.rs:68 | `実行する` | 2,3,5 | main.rs直下の自由関数で目的語が無い。実体は起動引数を読んで書き出す対象を選び分岐実行する処理 | `起動引数から書き出しを実行する` |
| crates/blitz_asset_compiler/examples/compile_assets/main.rs:17 | `実行する` | 2,3,5 | 同上。実体は起動引数を読んでアセットのコンパイルを実行する処理 | `起動引数からコンパイルを実行する` |

### 助詞を省いた膠着圧縮(日本語命名の質)

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_asset_compiler/examples/generate_source_assets/gltf_json.rs:5 | `文書JSON` | 日本語命名の質(膠着圧縮の禁止) | 「文書」と「JSON」を助詞なしで直結している。「文書のJSON表現」のような句を1語へ圧縮した形 | `文書のJSON文字列` または `JSON形式の文書` |
| crates/blitz_asset_compiler/examples/generate_source_assets/shadow_scene_gltf_json.rs:6 | `文書JSON` | 日本語命名の質(膠着圧縮の禁止) | 同上 | 同上 |
| crates/blitz_asset_compiler/examples/generate_source_assets/vegetation_world.rs:35 | `LOD共有バッファファイル名` | 日本語命名の質(膠着圧縮の禁止) | 「LOD」「共有バッファ」「ファイル名」の3つの名詞を助詞なしで連結している。「LOD原型用の共有バッファのファイル名」のような句が圧縮されている | `LOD原型用の共有バッファのファイル名` |
| crates/blitz_asset_compiler/examples/generate_source_assets/vegetation_world.rs:36 | `LOD原型文書ファイル名` | 日本語命名の質(膠着圧縮の禁止) | 同上。「LOD原型のglTF文書のファイル名」の圧縮 | `LOD原型の文書のファイル名` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑いのある条件 | 理由 |
|---|---|---|---|
| crates/blitz_asset_compiler/examples/generate_source_assets/verification_worlds.rs:14 | `一式を書き出す` | 条件3 | 「一式」が何の一式かは呼び出し元(`main.rs`の`検証用の世界一式`)を読まないと分からない。同ファイル内の他の関数(`材質境界アセットを書き出す`等smoke_assets.rs側)は対象語を持つのに対し、この関数は「一式」のみで曖昧さが残る。ただし全世界を束ねて書き出す最上位の入口という文脈上、致命的な誤読には至らないと判断し疑いに留める |
| crates/blitz_asset_compiler/examples/generate_source_assets/indirect_probe_gltf_json.rs:11 | `文書()` | 条件1,3 | 値を返す関数で品詞は正しいが、「文書」だけでは遠方環境検収世界の文書であることが名前だけからは分からない(モジュール名はASCIIで補完力ゼロ)。同型の`marker_gltf_json.rs:10`の`文書()`も同じ疑い |
| crates/blitz_asset_compiler/examples/generate_source_assets/fox_tour_world/marker_gltf_json.rs:10 | `文書(バイナリファイル名: &str)` | 条件1,3 | 同上。「目印の柱の文書」であることが名前に無い |
| crates/blitz_asset_compiler/examples/generate_source_assets/texture_compression_world.rs:52 | `対照の板のgltf文書を作る` | 日本語命名の質 | 対象語(対照の板)を持ち条件3は満たすが、`gltf`が小文字のまま日本語複合語へ埋め込まれている。ユビキタス言語では`glTF`(大文字T小文字tf)の表記で登録されており、実装の綴りが登録表記と食い違う可能性がある(表記統一の疑いであり、命名の受理条件そのものの違反ではない) |

## 違反ゼロと判定した範囲

- `check_glb/main.rs`、`compile_assets/arguments.rs`、`compile_assets/texture_policy_argument.rs`、`generate_source_assets/directory_source.rs`、`generate_source_assets/generation_arguments.rs`、`geometry.rs`、`indirect_probe_geometry.rs`、`indirect_probe_plates.rs`、`multi_material_geometry.rs`、`multi_material_gltf_json.rs`、`part_frame_row_world.rs`・`part_house_row_world.rs`・`part_tree_row_world.rs`・`stone_hut_world.rs`・`village_world.rs`の各`書き出す`以外の内部private関数群(`平らな地面の高さを求める`・`書き込む`等)、`shadow_scene_geometry.rs`、`shadow_scene_gltf_json.rs`(定数`文書JSON`を除く)、`terrain_visual_world.rs`・`terrain_world.rs`・`vegetation_world.rs`・`village_world.rs`の内部private関数群、`texture_compression_world.rs`(条件を満たさない`gltf`の表記1件を除き対象語を明示した命名で合格)、`chunk_world/geometry.rs`、`chunk_world/gltf_json.rs`、`fox_tour_world/height.rs`、`fox_tour_world/marker_geometry.rs`、`fox_tour_world/source_writing.rs`(型`場所巡りの世界のソース書き出し`のメソッド群は型名・レシーバによる補完が効いており合格)、`fox_tour_world/world_definition.rs` は、識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)を全数確認し違反を検出しなかった。`RGBA8`・`BC1`・`LOD`・`glTF`・`JSON`・`UV`はいずれもユビキタス言語または一般通用語として登録・許容されている綴りであり、頭字語混入の違反としては扱っていない。
