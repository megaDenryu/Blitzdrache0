# 命名検査報告: 区画122 `crates/blitz_sim/src/contact/query_origin` + `crates/blitz_sim/src/contact/rest`

- 区画番号: 122
- 対象: `crates/blitz_sim/src/contact/query_origin`(8ファイル)・`crates/blitz_sim/src/contact/rest`(6ファイル)、計14ファイル
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, `db815090` 時点。このツリーは編集していない)
- 直読したファイル数: 14/14(全ファイルを直読。grepだけで済ませたファイルは無い)
- 必読リスト: グローバルCLAUDE.mdの複製(`.claude/global-reference/全プロジェクト共通CLAUDE.md`)の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(1064〜1065行の「接触に参加する剛体」「衝突の問い合わせの基準原点」を含む)

## 違反

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `contact/rest/quiet_check.rs:45` | `剛体a`, `剛体b` | 受理条件2(英字は補完力ゼロ)/Blitzdrache0固有事項(識別子へのASCII英字混入の禁止) | 直前で束縛元は `鍵.小さい方の剛体()` / `鍵.大きい方の剛体()` であり、a/bという英字の接尾辞だけではどちらが小さい方でどちらが大きい方かを名前から復元できない。英字は読まれない前提のため、a/bの対応は日本語の中に存在しない | `小さい方の剛体`, `大きい方の剛体` |
| `contact/rest/quiet_check.rs:34` | `点a` | 受理条件2/ASCII混入の禁止 | 直後で「静的世界側の接触点」との差を取っており、この点は「剛体側の点」であるが、英字aだけでは何の点か分からない | `剛体側の点` |
| `contact/rest/quiet_check.rs:49-51` | `点a`, `点b` | 受理条件2/ASCII混入の禁止 | 45行目の`剛体a`/`剛体b`から求めた点であり、実体は「小さい方の剛体の点」「大きい方の剛体の点」だが、英字だけでは対応が分からない | `小さい方の点`, `大きい方の点` |
| `contact/rest/quiet_check.rs:49` および `contact/query_origin/mapping_tests.rs:76` | `局所接触点a`(フィールド、`拘束.引数().局所接触点a`として参照) | 受理条件2/ASCII混入の禁止 | 参加する剛体のどちらの局所接触点かをASCII添字a/bだけで表しており、対応する剛体名(小さい方/大きい方、または上/下)との関連が名前から分からない。**このフィールドの定義は区画外(`body_body_contact_parameters.rs`)にあり、この2ファイルは参照しているだけである**ため、改名にはその定義ファイルの是正も要る | `剛体側の局所接触点`(小さい方) |
| `contact/rest/quiet_check.rs:50` および `contact/query_origin/mapping_tests.rs:77` | `局所接触点b`(フィールド) | 同上 | 同上(相手側) | `相手側の局所接触点`(大きい方) |
| `contact/rest/rest_tests.rs:20-22` | `位置`(自由関数。`fn 位置(x: f32, y: f32, z: f32) -> 位置<ワールド>`) | 受理条件4(品詞の規律)/受理条件6(1語1義) | `blitz_math`の型`位置<T>`と全く同じ綴りの自由関数であり、同じファイルの中で型と関数の2つの意味を持つ。この関数は値を組み立てる処理であるにもかかわらず裸の名詞で命名されており、`位置(0.0, 0.0, 0.0)`という呼び出しは型`位置`のコンストラクタと見分けが付かない | `ワールド位置を組む` |

## 違反の疑い

| ファイル:行 | 識別子 | 疑われる条件 | 理由 |
|---|---|---|---|
| `contact/rest/rest_tests.rs:24-32` | `休止の閾値の本数`(自由関数) | 受理条件4(品詞の規律)への疑い | 内部で`刻み幅::生成する`と`休止と判定する連続静穏の細分の本数`の2段階の変換を行い、どちらかが失敗すると`panic!`する処理を持つ。単なる値の読み出しではなく手順を含む処理でありながら、裸の名詞句で命名されている。ただし同じ区画・同じクレートの他のテスト補助関数(`先頭の点の貫通量`・`二十メートルの帯の刻み`等)も同型の名詞句命名を広く採用しており、この区画で確立した慣用と見ることもできるため、断定はしない |
| `contact/query_origin/test_fixtures.rs:57-58,61-62,72-73,78-79,85-86,89` | `第1`, `第2`(仮引数名) | 受理条件3(5W1Hの必要格)への疑い | `衝突の問い合わせの基準原点::剛体の粗い位置から選ぶ(第1)`という使い方から分かる通り、`第1`は「基準原点として選ぶ側」という役割上の意味を持つが、仮引数名の`第1`/`第2`は列挙順を示すだけで、どちらが原点になるかという役割を名前だけからは復元できない。関数のdocコメントには明記されているため実害は限定的であり、疑いにとどめる |

## 違反ゼロと確認した範囲

上記の表に挙げた識別子を除き、次はすべて受理条件8つ・日本語命名の質・呼称と語の選択に照らして違反を検出しなかった。

- `query_origin/mod.rs`: 型`衝突の問い合わせの基準原点`とそのメソッド群(`剛体の粗い位置から選ぶ`・`世界の原点`・`剛体の相対の中心を求める`・`剛体の相対の直方体を組む`・`静的な直方体を相対の形へ写す`・`衝突が返した点を世界の二段の位置へ戻す`・`相対の位置を世界の位置として読む`・`大域の原点として読む`)は、いずれもユビキタス言語1065行の定義と実装が一致し、動詞句が目的語・着点を伴っている
- `query_origin/relative_box.rs`: 型`大域原点に相対な直方体`とメソッド`生成する`・`大域の外接箱を求める`・`始点と終点を包む大域の箱を求める`
- `query_origin/shared_frame.rs`: 自由関数`接触点を基準原点からの変位へ写す`・`接触点を渡した相対の中心からの変位へ写す`・`接触法線をワールドへ写す`・`符号付き貫通量を単精度へ狭める`・`メートルへ狭める`(冒頭コメントが自由関数である理由「入力が`blitz_collision`の型でこちらのメソッドにできない」を明記しており、受理条件2の判定に足る根拠がある)
- `query_origin/carry_tests.rs`・`pair_tests.rs`・`static_tests.rs`・`mapping_tests.rs`・`test_fixtures.rs`: 定数・テスト関数名・ローカル変数は上表の`局所接触点a/b`を除きすべて役割を反映した日本語の名詞句・動詞句であり、`粗い`/`細かい`+軸名(x/y/z)の組み合わせは`位置.x()`等の既存の軸表現と一致する数学記法として許容した
- `rest/mod.rs`・`body_rest.rs`・`island_rest.rs`・`new_contact_wake.rs`・`quiet_check.rs`(上表の箇所を除く): `運動が静穏か`・`剛体を起こす`・`休止状態を更新する`・`起こす`・`全部へ変更を配る`・`全部が休止しているか`・`全部の静穏が続いた細分の本数が閾値に達したか`・`新しい接触の契機を島へ伝える`・`休止している側を島ごと起こす`・`静穏か`はいずれも動詞句または「〜か」の真偽形であり、5W1Hの必要格(何を・誰が・どうする)を備える
- `rest/rest_tests.rs`(上表の`位置`・`休止の閾値の本数`を除く): テスト関数名・ローカル変数・定数はすべて意味の取れる日本語である

