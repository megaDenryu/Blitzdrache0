# 命名検査報告: 区画045

- 区画名: crates/blitz_collision/src/shape 第2区画
- 区画番号: 045
- ファイル数: 3
- 作業ツリー: C:/devs/Blitzdrache0_roller(main, db815090時点。このツリーは編集していない)
- 検査日: 2026-09-12

## 検査対象ファイル

1. crates/blitz_collision/src/shape/triangle_tests.rs
2. crates/blitz_collision/src/shape/triangle_vertex_number.rs
3. crates/blitz_collision/src/shape/triangle_with_orientation.rs

直読したファイル数: 3/3(全数)。grepだけで済ませたファイルは0件。3ファイルとも全文を読み、型名・enumの枝・関数名・メソッド名・フィールド名・変数名・定数を全数検査した(検査した識別子はおよそ45個。内訳: 型3、enumの枝3、関連関数/メソッド計21、フィールド3、テスト関数7、テスト内ヘルパー関数4、ローカル変数複数)。

参照した用語定義: `_doc/計画/ユビキタス言語.md`の「局所座標の三角形」「三角形の頂点の番号」「面に垂直な変位」「頂点の番号で表した三角形」「三角形で当たった特徴」の各項。いずれの実体もこの区画の型・メソッドの語と一致している(「三角形の頂点の番号」「面の向きを備えた三角形」は用語定義の登録句と完全一致)。

## 違反の一覧

該当なし(受理条件1〜8のいずれにも明確に落ちる識別子は見つからなかった)。

## 違反の疑い

1. `crates/blitz_collision/src/shape/triangle_vertex_number.rs:21` | `並べる`(`三角形の頂点の番号::並べる() -> [Self; 3]`) | 条件4に対して(疑い) | この関連関数は引数を取らず、3つの頂点を固定の順序(求解が候補を数える順)で並べた配列という「値」を返す。受理条件4は「値=名詞句/処理=動詞句」と定めており、値を返すだけの関連関数が裸の動詞「並べる」になっている。条件5の「型に属する関連関数の生成する()は型名が目的語を担うため合格」という例外は`Self`を組み立てる生成系ファクトリに対するものであり、この関数は`Self`ではなく固定の列挙を返すため同じ理由を援用できるか不確か。加えて、名前だけでは「求解が候補を数える順」という順序の意味(なぜこの並びなのか)が読み取れない。改名の素案(参考): `求解順の全頂点` または `求解の順に並んだ全頂点`。なお`次の頂点(self) -> Self`・`並びの位置(self) -> usize`は同じ型の中でも名詞句になっており、この関数だけが動詞句である点も一貫性の観点で気になる
2. `crates/blitz_collision/src/shape/triangle_tests.rs:27` | `変位.xのメートル()` `変位.yのメートル()` `変位.zのメートル()` | 条件2に対して(疑い、区画外) | 呼び出しはこのファイルにあるが、メソッドの定義は`local_displacement.rs`(本区画の3ファイルに含まれない)にあり、識別子に英字`x`・`y`・`z`が混入している。指示にある「識別子への英字・ギリシャ文字・記号の混入を必ず当てる」に該当する形だが、定義側のファイルが本区画の対象外のため、本報告では違反として確定せず疑いとして記録する。他区画の命名検査でこの識別子の定義箇所(`local_displacement.rs`)が検査対象になっているかを確認し、未検査なら別区画で当てることを勧める

## 違反ゼロの範囲

上記2件の疑いを除き、本区画3ファイルの型名(`三角形の頂点の番号`・`面の向きを備えた三角形`)、enumの枝(`第1の頂点`・`第2の頂点`・`第3の頂点`)、関連関数・メソッド(`三角形から生成する`・`面の向きを添えて生成する`・`三角形`・`面の向き`・`頂点の位置`・`辺の変位を求める`・`面に垂直な変位`・`単位法線の変位`・`次の頂点`・`並びの位置`)、フィールド(`三角形`・`面の向き`)、テスト関数7件(`辺の変位は番号の頂点から次の頂点へ向かう`等)、テストヘルパー(`局所の点`・`水平な直角三角形`・`変位の成分`・`面の向きを備えた水平な直角三角形`)はいずれも受理条件8つを満たし、実体(ユビキタス言語の定義)と名前が一致していることを確認した。第1の頂点・第2の頂点・第3の頂点は漢字1文字への圧縮(例: 「1頂」)を行わずフルスペルで書かれている点も確認した。識別子への英字・ギリシャ文字・数学記号の混入は、本区画が定義する識別子そのものには見つからなかった(区画外定義への参照1件を上記で疑いとして記録)。
