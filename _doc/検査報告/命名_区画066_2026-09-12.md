# 命名検査報告 区画066

- 区画名と範囲: `crates/blitz_engine/src/time`(9ファイル)と`crates/blitz_engine/src/visibility`(29ファイル、サブモジュール`band_light_box/`・`band_light_box/ortho_box/`・`caster_distance/`・`stage_bucket/`・`visible_pass_set/`を含む)。合計38ファイル
- 時点: 作業ツリー`C:/devs/Blitzdrache0_roller`、mainのdb815090
- 直読したファイル数: 38/38。grepだけで済ませたファイルは0件(全ファイルを直読した)
- 参照した用語定義: `_doc/計画/ユビキタス言語.md`から「時刻の型」節(世界時刻・ゲーム時計・時間倍率・一日の長さ・一日内時刻)と、可視判定・距離区分・キャスター距離分布・可視ID列まわりの登録句をgrepで照合した

## 違反の一覧

1. `crates/blitz_engine/src/visibility/combined_select.rs:24,29` | `個体LOD選択設定` | 日本語命名の質(頭字語混入禁止)、受理条件2(ASCII識別子は補完力ゼロ) | `LOD`(Level of Detail)はUI/API/GPUのような非専門家にも通用する語ではなく、LUT・UBO等と同格の業界頭字語である。同じ関心事を指す既存の日本語置き換え`個体詳細段`(同ファイル71行目で使用)が既にあるのに、設定型の名前だけ英語の頭字語のまま日本語識別子へ混ぜている。定義自体はこの区画の外(クレート直下)にあるが、識別子はこの区画内(`combined_select.rs`)で読まれる | `個体詳細段選択設定`(または`LOD`を使わない同義の日本語)。定義側のリネームが必要なため、リネーム員は定義ファイルも合わせて特定すること
2. `crates/blitz_engine/src/visibility/band_light_box/ortho_box.rs:92` | `組み立てる` | 受理条件3(5W1Hの必要格の欠落)、受理条件5(裸の動詞の禁止) | プライベートな自由関数(`band_light_box::ortho_box`モジュール直下)であり、型のレシーバも関連関数の型名も持たない。名前だけでは「何を組み立てるか」が答えられず、単独では「範囲内直方体を組み立てる」処理だと復元できない。呼び出し元`組み立てる([0, 1, 2].map(...))`を読んでも、戻り値の型(`Option<範囲内直方体>`)まで見ないと分からない | `範囲内直方体を組み立てる`
3. `crates/blitz_engine/src/visibility/stage_bucket/tally.rs:90` | `枠` | 受理条件4(品詞の規律。処理が名詞形になっている) | プライベートな自由関数(`stage_bucket::tally`モジュール直下)で、`段番号`の範囲外アクセスをpanicで守りながら可変参照を返す処理を持つ。名前が「枠」という単独の名詞のみであり、「これは何?」への答えが「(段の)枠」という値の呼称にしかならず、実際に行っている「取得する・境界を検査する」という処理を予告しない。同じファイル内の`一件足す`が動詞句+目的語で書かれているのと対照的である | `段の枠を取得する`

## 違反の疑い

1. `crates/blitz_engine/src/visibility/instance_sphere.rs:62` | `局所位置`(自由関数、`成分: [f32; 3] -> 位置<ローカル>`) | 受理条件4に対する疑い | 生の配列から値オブジェクトへ変換する処理でありながら、値オブジェクト様式の`生成する()`でも`にする`でもない裸の名詞形になっている。ただし同ファイル内の`中心成分`・`半径値`など「生値を返す」系の命名と対称的な「生値から作る」ヘルパーであり、局所的な私有関数として読めなくはない。断定はしない

## 違反ゼロの範囲

- `crates/blitz_engine/src/time`配下9ファイル全部(`day_length.rs`・`day_time.rs`・`game_clock.rs`・`game_clock_tests.rs`・`mod.rs`・`time_error.rs`・`time_scale.rs`・`world_time.rs`・`world_time_tests.rs`)は、型名・メソッド名・フィールド名がすべて受理条件を満たし、違反も違反の疑いも検出しなかった
- `crates/blitz_engine/src/visibility`配下のうち、上記3件・疑い1件を除く全ファイル(`band_light_box.rs`・`band_pass_test.rs`・`band_visibility_mode.rs`・`bounds_transform.rs`・`camera_distance.rs`・`caster_distance.rs`・`caster_distance_scan.rs`・`count.rs`・`error.rs`・`frustum.rs`・`group_material.rs`・`light_space_interval.rs`・`pass_test.rs`・`plane.rs`・`selector.rs`・`shadow_range.rs`・`stage_bucket.rs`・`stage_tray.rs`・`visible_pass_set.rs`・`volume_test.rs`・`band_light_box/rebase.rs`・`band_light_box/ortho_box/range_box.rs`・`caster_distance/band_tests.rs`・`stage_bucket/write_tests.rs`・`visible_pass_set/mask_tests.rs`)は違反を検出しなかった
- 「id列」「idへ変換する」等のASCII頭字語「id」の使用は、UI・API等と同格の非専門家にも通用する一般語として扱い違反にしなかった
