# 命名検査報告: 区画102 crates/blitz_render/src/vulkan/frame/record/graph_build/stages ほか

- 対象区画: 区画番号102、`crates/blitz_render/src/vulkan/frame/record/graph_build/stages`配下および同一区画に含まれる`fullscreen_pipeline`・`geometry`・`gpu_environment`・`gpu_timing`の計31ファイル
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, db815090時点。編集していない)
- 直読したファイル数: 31/31(全ファイルを直読。grepだけで済ませたファイルは無い)
- 検査した識別子: 上記31ファイルに現れる型・関数・メソッド・フィールド・変数・定数・列挙の枝の日本語識別子全件(概数120件超。個々の行番号は違反・疑いの一覧に記す。違反ゼロの範囲は末尾に記す)
- 必読: グローバルCLAUDE.mdの「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(索引・インデックスの使い分けの照合に使用)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_render/src/vulkan/frame/record/graph_build/stages/cluster_light_assignment_stage.rs:32 | `登録する`(private fn) | 条件2・3 | ASCIIモジュール直下の自由関数であり名前単独で答える必要があるが、「登録する」だけでは何を登録するのか(バッファ)が無い。呼び出しは`登録する(グラフ, 入力.格子)`という裸呼び出しで、レシーバによる目的語補完も働かない | `バッファを前フレーム状態で登録する` |
| crates/blitz_render/src/vulkan/frame/record/graph_build/stages/local_visibility_stage.rs:38 | `登録する`(private fn) | 条件2・3 | 同上。画像とビューを登録する処理だが名前に対象が無い | `画像とビューを登録する` |
| crates/blitz_render/src/vulkan/frame/record/graph_build/stages/atmosphere_lut_stage/register.rs:49 | `登録する`(private fn) | 条件2・3 | 同ファイル内の`焼く画像を登録する`・`参照する画像を登録する`から呼ばれる共通処理だが、それ自体は裸呼び出し(`登録する(グラフ, 画像, 初期状態)`)であり、名前単独では初期状態を外から与えて画像を登録する処理だとは分からない | `画像を初期状態付きで登録する` |
| crates/blitz_render/src/vulkan/frame/record/graph_build/stages/indirect_lighting_stage/graph_images.rs:65 | `登録する`(private fn) | 条件2・3 | 同上のパターン。`消費する画像を登録する`・`焼く中間画像を登録する`から裸呼び出しされる共通処理で、名前に対象と条件(焼き直すか)が無い | `画像を焼き直すか指定して登録する` |
| crates/blitz_render/src/vulkan/geometry/mod.rs:18 | フィールド`索引`(型`ジオメトリバッファ`) | 条件6・7の疑い(1語1義の逆: 同一概念に2語) | 同じ型の中で、メソッド名は`インデックスバッファ()`・冒頭コメントは「インデックスの総数」、`bytes.rs`側の関数名も`インデックスをバイト列にする`と、この概念を一貫して「インデックス」(ユビキタス言語の「投入インデックス数」と同語)と呼んでいるのに、フィールドだけ漢字の「索引」を使っている。読み手は同じ型の中で語が割れているように読める | フィールド名を`インデックス`へ統一する |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| crates/blitz_render/src/vulkan/fullscreen_pipeline/fixed_function.rs:30-41, 54-62 | `頂点入力state`・`入力アセンブリstate`・`ビューポートstate`・`ラスタライズstate`・`マルチサンプルstate`・`カラーブレンドstate`・`動的state一覧`・`動的state`・`layout一覧`・`layout_info`・`rendering情報`・`create_info`・`ディスクリプタlayout` | 日本語命名の質(英語の頭字語・素語混在) | 日本語の語幹へ英語の生語`state`・`layout`・`info`を翻訳せずに直結している(例: 「頂点入力の状態」ではなく「頂点入力state」)。ただしこれらはVulkanの`vk::Pipeline*StateCreateInfo`という外部APIの語彙をそのまま踏襲した変数名であり、CLAUDE.mdは「ashや外部クレートの型・関数はそのままの英語名で扱う」ことを認めているため、外部API語彙の反映として合格側に立つ余地がある。断定はしない |

## 検査した範囲・違反ゼロの範囲

- 直読31/31ファイル。grepだけで済ませたファイルは無い(0件)
- 上記の違反4件・疑い1件を除く残り全ファイル・全識別子(`atmosphere_lut_stage.rs`本体、`depth_prepass_stage.rs`、`indirect_lighting_stage.rs`本体・`bake_upload.rs`・`injection_upload.rs`、`point_light_shadow_stage.rs`、`scene_stage.rs`、`shadow_stage.rs`、`sky_stage.rs`、`temporal_reconstruction_stage.rs`、`atmosphere_lut_stage/three_planes.rs`、`gpu_environment/create.rs`・`destroy.rs`・`mod.rs`・`physical_query.rs`、`gpu_timing/composite_interval.rs`・`create.rs`・`frame_samples.rs`・`mod.rs`・`pass_time_window.rs`・`query_pool.rs`・`readback.rs`・`readback/readback_tests.rs`)は、受理条件8つ・オーナー不合格の3形(実体と合わない語・名詞1語の圧縮・詰め込みすぎ)・ASCII/ギリシャ文字/記号混入のいずれにも該当しなかった。特にこの区画は値オブジェクト様式の`生成する()`(型名が目的語を担う合格例)、材料・条件で枝分けした複数ファクトリー(`焼く画像を登録する`/`参照する画像を登録する`、`三枚とも焼く`/`三枚とも参照するだけ`/`スカイビューだけ焼く`)、処理を保持するフィールド名の動詞句維持など、規約が求める形が徹底されている
