# 命名検査報告 区画044

- 区画名: crates/blitz_collision/src/shape 第1区画
- 対象範囲: `C:/Users/POKR30~1/AppData/Local/Temp/claude/C--devs-Blitzdrache0/612e37aa-a38d-4d29-a7a3-b0e9508d9f15/scratchpad/roller/block_044.txt` に列挙された40ファイル
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`(main, db815090)。このツリーは編集していない
- 直読したファイル数: 40 / 40（全ファイルを直読。grepだけで済ませたファイルは0件）
- 参照した必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`

## スコープの補足（省略ではなく範囲の確定）

`block_044.txt` が指す40ファイルのほかに、実際のディレクトリには `triangle_tests.rs`・`triangle_vertex_number.rs`・`triangle_with_orientation.rs` の3ファイルが存在する（差分検査で確認済み）。これらは指定の一覧に含まれないため直読の対象外とした。ただし `triangle_face_orientation.rs`・`triangle_inside.rs` がこれらの型（`三角形の頂点の番号`・`面の向きを備えた三角形`）を使っており、使用箇所からは実体と名前の対応におかしな点は見えなかった。この3ファイル自体の識別子は未検査であり、別区画または追検査が必要である。

## 違反の一覧

1件。

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_collision/src/shape/axis_aligned_box.rs:100` | `成分を並べる`（private関数） | 条件3（5W1Hの必要格。着点・順序の格が欠落） | 同じ関心事を扱う兄弟ファイルは全て「軸の順に並べる」または「端の成分を並べる」のように、何を・どの順で並べるかを名前へ書いている（`local_axis_aligned_box.rs`の`形の局所座標の位置の成分を軸の順に並べる`、`global_box_components.rs`の`大域の位置の成分を軸の順に並べる`・`局所の位置の成分を軸の順に並べる`、`dynamic_index/box_edge_check.rs`の`箱の両端の6成分を軸の順に並べる`、`oriented_box_bounds_fixture.rs`の`端の成分を並べる`）。このファイルだけ「軸の順に」も「端の」も無い`成分を並べる`であり、名前だけでは「大小で並べ替える」のか「x・y・zの順に配列へ写す」のか区別が付かない。実体は後者(xyzを配列へ写すだけ)である | `端の成分を軸の順に並べる` |

## 違反の疑い

| ファイル:行 | 識別子 | 対象条件 | 疑いの内容 |
|---|---|---|---|
| `crates/blitz_collision/src/shape/oriented_box_double.rs:69` | `軸ごとの広がりを求める`（pub(super)） | 条件1・3 | 「広がり」は文脈（型の冒頭コメントと`半分の長さの総和を求める`という隣接メソッド）を読めば「回転後に局所座標系へ投影した半分の長さの和」だと分かるが、メソッド名単独では「三角形の面積の広がり」等とも読めてしまう。ただしpub(super)で呼び出し側が同じファイル群に限られ、型のドキュメントコメントが同じ行に説明を持つため断定はしない |

## 違反ゼロの範囲

上記1件と1件の疑いを除く、40ファイル中の残り全識別子（型名・enum・フィールド・メソッド・定数・テスト関数名を含む）は受理条件8つ・命名の質・呼称と語の選択のいずれにも抵触しなかった。特に次の点を個別に確認して合格とした。

- ASCII・ギリシャ文字・数学記号の識別子混入: `X軸`・`Y軸`・`Z軸`の3件のみ該当するが、これは`_doc/計画/ユビキタス言語.md`(625〜628行)に既に登録された確立語であり、オーナー不合格実例が指した「段階A2」「Σλ_n」のような臨時の圧縮ラベルとは性質が異なるため違反としない
- 名詞1語への圧縮: 該当なし。列挙の枝(`半分の長さが2軸以上で零`等)はいずれも主語・述語を保った完全な句である
- 詰め込みすぎの枝: 該当なし
- 実体と名前の不一致: `三角形の面の向き`(面に垂直な変位+単位法線の変位を持つ型)を含め、型定義を開いて照合したが、名前が指す語と実際のフィールドの対応にずれは無かった
- 裸の動詞: `create::生成する`型の違反は無し。`直方体を作る`のように目的語を伴うか、`軸平行の直方体::二つの端から生成する`のように型名が目的語を担う形になっている
- 内積・外積・長さの二乗といった数学の名詞そのままのメソッド名は、blitz_math・blitz_render・blitz_sim・blitz_engine等リポジトリ全体で確立された数学用語の慣用であり、この区画だけの逸脱ではないため違反としない
