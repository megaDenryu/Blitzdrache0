# 命名検査報告 区画140(xtask/src 第2区画)

- 区画名: xtask/src 第2区画(区画番号140、40ファイル)
- 対象範囲: `xtask/src/large_world_bench.rs` 〜 `xtask/src/streaming_report.rs`(一覧は `block_140.txt` のとおり)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090`(このツリーは編集していない)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`
- 直読したファイル数: 40/40(全ファイルを直読。grepだけで済ませたファイルは0件)

## 違反の一覧

1. `xtask/src/lod_crack.rs:29,37-38` | `総roi画素数`(局所変数)・`roi画素数`(フィールド。定義は同モジュールの`image_check`だがこのファイルで読み書きされる) | 日本語命名の質 条1(頭字語・省略語の混入禁止) | `roi`(Region Of Interest)はGPU・CPU・API・UIのような一般に通用する語ではなく、`_doc/計画/ユビキタス言語.md`にも登録が無い。カメラ・画像処理を知らない読み手は`roi`が何の略か復元できない。 | 改名案: `総着目領域画素数`・`着目領域画素数`

2. `xtask/src/pixel_region.rs:10-13,18-19,25,30-31,38` | `x開始`・`x終端`・`y開始`・`y終端`(構造体`画面領域`のフィールドと同名の局所変数・引数) | 受理条件の付随規律(ASCII英字1文字を日本語複合語へ直接くっつける圧縮の禁止。オーナー不合格実例「段階A2」と同型) | 単一のASCII文字`x`/`y`を漢字へ直接くっつけた圧縮であり、日本語として読めない。しかも同じファイルが`画素の横位置`・`画素の縦位置`という日本語の型をすでに持っており、フィールドだけ英字の軸名へ逃げているため1語1義が崩れている(横位置/縦位置と x/y の2系統の呼び方が同じ概念に併存する)。 | 改名案: `横開始`・`横終端`・`縦開始`・`縦終端`

3. `xtask/src/plate_region.rs:52-55,61-63` | `x開始`・`x終端`・`y開始`・`y終端`(局所変数) | 受理条件の付随規律(同上) | 同上とまったく同じ形の混入。このファイルも`画素の横位置`・`画素の縦位置`という日本語型を関数の引数・戻り値として使っているのに、内部計算だけがx/yへ戻っている。 | 改名案: `横開始`・`横終端`・`縦開始`・`縦終端`

4. `xtask/src/sky_draw.rs:47,48,67,68`(呼び出し先の定義は同モジュールの`gpu_time`) | `空パスms`(局所変数)・`空パスが枠を下回るかの判定名`の引数`空パスms`・呼び出し先`gpu_time::空パスの平均msを読む` | 日本語命名の質 条1(頭字語・省略語の混入禁止)+条6相当(1語1義) | 同じファイルの24行目で「空パスの予算ミリ秒」とすでに正しく訳しているのに、実測値のほうだけ未翻訳の`ms`を使っており、同じ概念(空パスの所要時間)が「ミリ秒」と`ms`の2通りの綴りで現れる。`ms`はCPU/GPU/APIのような一般に通用する語ではなく、英語の単位記号を知らない読み手には復元できない。 | 改名案: `空パスミリ秒`・`空パスの平均ミリ秒を読む`

## 違反の疑い

- `xtask/src/release_build.rs:35` | `構築の道具cargoを起こせなかった`(enum枝) | 受理条件2・受理テスト | 「cargo」はRustのビルドツールの固有名で、`crate名やファイル名が英語なのは問題視しない`という既存の運用と同種の例外に当たりうる。ただし単独の識別子の中へ小文字のまま直接くっついており、`Command::new("cargo")`という文字列とは別に、名前として読んだときに借用語だと分からない読み手が「コウグ」の後に続く英単語だと気づけるかは断定できない。一括リネームは不要だが、次にこのenumへ触るときに`構築の道具コマンドを起こせなかった`等へ寄せることを検討する余地がある。

## 違反ゼロの範囲

上記4件・疑い1件を除く36ファイル(large_world_bench.rs、local_visibility.rs、m10_bench.rs、m11_soak.rs、main.rs、material_check.rs、memory_sampling.rs、motion_vector.rs、multi_light_world.rs、object_bench.rs、origin_invariance.rs、ow3_dod.rs、ow4_bench.rs、part_row_draw.rs、part_tools.rs、play_fox_tour.rs、point_light_shadow.rs、prop_draw.rs、raw_png.rs、relative_luminance.rs、release_build.rs(疑い1件を除く)、report_heading.rs、report_line_key.rs、report_parse.rs、reverse_depth.rs、sample_world_region.rs、shader_copy.rs、shader_reload_draw.rs、shadow_diagnostic_image.rs、shadow_loss.rs、shadow_probe.rs、sky_lut.rs、sky_state.rs、sky_time.rs、streaming_bench.rs、streaming_report.rs)は、日本語識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)のいずれも受理条件8つを満たし、直訳調・膠着圧縮・裸の動詞・1語1義の衝突・ASCII/ギリシャ文字/記号の混入は検出しなかった。

なお、`gpu`・`lod`・`ram`・`vram`・`rgba8`・`png`・`validation`をJapanese識別子へ小文字のまま組み込む例(例: `xtask/src/streaming_report.rs`の`ストリーミング要約報告`構造体の`gpu解除件数`・`最大ramバイト数`・`最大vramバイト数`・`lod変更フレーム数`・`lod変更時読込開始件数`、`xtask/src/report_parse.rs`の`validation件数`、`xtask/src/shadow_diagnostic_image.rs`の`rgba8から読み取る`)は複数ファイルに見られるが、これらは`_doc/計画/ユビキタス言語.md`に登録済みの語(LOD・validation layer)か、`CLAUDE.md`「専門用語の表記」が明示的に許す一般語(GPU相当)、またはRustのsnake_case規約が大文字略語を自動で小文字化した結果であり、違反として扱わなかった。判断根拠は`_doc/計画/ユビキタス言語.md`215行目(LOD)・30行目ほか(validation layer)・94行目(逆Z、`reverse_depth.rs`の`逆Z検収エラー`はこの登録語に一致するため合格)である。
