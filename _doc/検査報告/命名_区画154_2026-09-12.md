# 命名検査報告: 区画154(xtask/src/editor ほか)

- 区画名と範囲: 区画154。対象は `xtask/src/editor`(10ファイル)、`xtask/src/file_scan/error.rs`(1ファイル)、`xtask/src/game_fox_tour` 配下(24ファイル)、`xtask/src/gen_atmosphere_reference` 配下(5ファイル)の計40ファイル。一覧は `scratchpad/roller/block_154.txt` による
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090`(このツリーは編集していない)
- 直読したファイル数: 40 / 40(全ファイルを直読。grepだけで済ませたファイルは無い)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(該当語の有無をgrepで確認)

## 違反の一覧

1. `xtask/src/editor/web_root.rs:31` | `開発サーバーを起動する試み`(`エディター画面の置き場`のメソッド) | 条件4(品詞の規律) | この関数はnpmプロセスをspawnし、失敗時に案内を`println!`する副作用を持つ処理であるにもかかわらず、名前が「試み」という名詞で終わっている。「オーナーが不合格にした実例」の(a)「処理なのに名詞形(品詞の偽装)」に該当し、`記録`がログと誤読された経緯と同型である。読み手は戻り値`Option<子プロセスの木>`を見るまで、これが値を作るだけの関数か、副作用を伴う起動処理かを名前から判別できない | `開発サーバーの起動を試みる`
2. `xtask/src/game_fox_tour/run.rs:36` | `描画する`(自由関数、`run`モジュール直下) | 条件1・条件3(受理テスト・5W1Hの必要格) | `run`はASCIIモジュールであり、命名規範上このモジュール名からの意味の補完は無効(受理条件2)。名前単独で読むと「描画する」は画面へ絵を出す処理にしか見えないが、実体はアプリを起動して実行時の進行報告(`実行の進行`: ゲーム更新回数・進行段階・到達済み目的地・大域位置)を読み取って返す関数である。戻り値の主要な情報である「進行を読み取る」が名前に一切現れておらず、「これは何?」に単独で答えられない | `実行して進行を読み取る` または `台本を実行し進行を撮影する`

## 違反の疑い

1. `xtask/src/gen_atmosphere_reference/error.rs:19,27` および `error/display.rs` の対応する枝 | `作業コピーの照合の標準出力がUTF8でない`・`焼き出しの出力がUTF8でない`(列挙の枝) | 日本語命名の質(頭字語の禁止)に対する疑い | `UTF8`は英語の略語(Unicode Transformation Format 8-bit)であり、グローバルCLAUDE.mdが例示する許容語(UI・CPU・GPU・API・シェーダー等)の一覧に含まれない。エンコーディングの知識が無い読み手が意味を復元できるか(専門用語の表記の判断基準)は微妙であり、断定はしない。`_doc/計画/ユビキタス言語.md`に`UTF8`または関連する訳語(例: Unicode変換形式)の登録は無かった(grep 0件)
2. `xtask/src/gen_atmosphere_reference/revision.rs` の複数箇所 | `gitの出力を読む`・`作業コピーでgitを起こせなかった`・`作業コピーでgitが失敗して終わった`(関数名・列挙の枝) | 日本語命名の質(頭字語・外来語混入)に対する疑い | `git`はバージョン管理ツールの固有名詞であり、`npm`・`vite`のようにこのリポジトリで既に固有名詞のまま使われている前例がある(`xtask/src/editor/web_root.rs`の`npmプログラム名`)。固有名詞をそのまま使う慣行が既にあるため違反とまでは判定しないが、`_doc/計画/ユビキタス言語.md`に`git`の項目登録は無く(grep 0件)、判定材料が不足するため疑いに留める

## 違反ゼロの範囲

上記2件の違反と2件の疑いを除く、40ファイル中の残りすべての日本語識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は受理条件8つを満たした。特に次の点を確認した。

- 品詞の規律: 値は名詞句、処理は「〜する」の動詞句、真偽は「〜か」で一貫していた(例外は上記1件のみ)
- 裸の動詞の禁止: `生成する`・`確かめる`等の単独名は、型に属する関連関数(値オブジェクト様式、型名が目的語を担う)か、モジュール私有の小さな委譲(受け手の型・引数が目的語を明示)のいずれかであり、ASCIIモジュール直下の裸の動詞は見つからなかった
- 1語1義: 「畳む」(内容ハッシュを畳み込む)、「焼く」(アセットを生成する)、「検収」(生成物の機械検査)は本区画内および用語定義ファイルとの照合で単一の意味に定まっていた
- 直訳調: `lookup→引く`のような英語直訳の痕跡は見つからなかった。`毒された錠`(poisoned mutex)は英語の直訳ではなくRustの標準的な日本語表現として定着した訳であり違反としなかった
- 圧縮・詰め込み: 漢字1文字への切り詰め(「順4」「段3」型)、ギリシャ文字・数式記号・単独英字の識別子混入は本区画に見つからなかった
- 実体との照合: `建物外形カタログを書き出す`・`場所巡りの通しの検収エラー`・`検収用のルート`等、名前が指す実体を型定義とコメントで照合し、フィールド・枝の意味が名前と一致することを確認した
