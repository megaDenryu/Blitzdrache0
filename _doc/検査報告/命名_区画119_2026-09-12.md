# 命名検査報告 区画119

- 区画: `crates/blitz_sim/src/contact/normal_tangential_system/double_reference`(30ファイル)および同階層の`system/`配下4ファイル・`tolerance_application/spelling.rs`(合計35ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(編集していない)。時点: main の `db815090`
- 直読したファイル数: 35/35(全ファイルを直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」、`_doc/計画/ユビキタス言語.md`(該当語を照合)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_sim/src/contact/normal_tangential_system/system/tolerance_scale_entry.rs:15,21,27,32,33` / `system/row_admission.rs:24,36` / `tolerance_application/spelling.rs:1,6,7,9,12` | `試験の許容差の当て方`(型名・フィールド名・メソッド名・関連関数の引数名として繰り返し使用) | 受理条件1(受理テスト)・6(1語1義) | `_doc/計画/ユビキタス言語.md`の該当項目は2026-09-12に「試験の許容差の当て方」から`試験の許容差の適用規則`へ改称済みである。理由として文書自身が「実体が適用先・倍率・判定条件を束ねた規則であるため」と明記しているが、コード側の型名・フィールド名・メソッド名はいずれも改称前の`当て方`のままであり、用語定義と実装が指す名前が一致していない。「当て方」は会話では「どう当てるか」としか読めず、実体(受理の倍率・行への当て方・届く先・閾値の倍率の4つを束ねた規則)を予告しない。オーナーが2026-09-12に不合格とした実例そのものである | `試験の許容差の適用規則`(型名・フィールド名・メソッド名・引数名を用語定義と揃えて改称。宣言そのものは本区画外の`normal_tangential_system/tolerance_application.rs`にあり、そちらとセットでのリネームが要る) |
| `crates/blitz_sim/src/contact/normal_tangential_system/tolerance_application/spelling.rs:15` | `接線の行への許容差の当て方::許容差の内側の点の行を積まず右辺を零にする`(列挙の枝。使用箇所) | 受理条件1(受理テスト)・8(詰め込みすぎではないが実体とのずれ) | `_doc/計画/ユビキタス言語.md`「試験の許容差の適用規則」の項目は、この枝が表す本番の形を「許容差以下の接線変位の拘束行を追加しない」という語で説明しており、この語がグローバルCLAUDE.mdの「オーナーが不合格にした実例」に挙げられた枝名と文字どおり同一である(「言葉を詰め込みすぎて意味が壊れている。実体は『接線変位が許容差以下の点の拘束行を追加しない』であり、右辺の扱いは本質でない」)。「右辺を零にする」は行を積まない結果の言い換えに過ぎず、本質(何を判定して積まないか)を「積まず」の一語に埋没させている | `許容差以下の接線変位の拘束行を追加しない`(枝の宣言は本区画外の`tolerance_application.rs:29`にあり、そちらとセットでのリネームが要る) |

## 違反の疑い

- 特になし。上記2件は用語定義ファイルへ照合した結果、改称済みの語との不一致が明確であり断定した

## 検査した識別子と結果の概況

- `double_reference/`配下30ファイルは、`倍精度の連立の有効集合`・`倍精度の接触点集合の連立`・`倍精度の法線の一行`・`倍精度の一行の二つの符号付き勾配`・`倍精度の姿勢自由度の参加者`など、単精度側の登録済み用語(`法線と接線の連立の有効集合`・`接触点集合の法線と接線の連立`・`非貫通の接触点集合の一行`・`一行の二つの符号付き勾配`・`姿勢自由度の参加者`)に「倍精度の」を冠しただけの対応する型名であり、ユビキタス言語の「接触の倍精度の参照計算」の記述と実体が一致する。フィールド・メソッド名は「〜を読む」「〜を組む」「〜を測る」「〜か」の品詞規律を守り、目的語(法線・接線・隔たり・残差等)を欠かさない
- `倍精度の擬似逆で解いた増分と捨てた右辺`・`倍精度の解いた行の残差と補正の大きさ`のように複数語を束ねた型名があるが、いずれも束ねた各フィールドが常に同時に生成・消費される値であることをコード冒頭コメントで確認しており、「名詞1語への圧縮」や「詰め込みすぎ」には当たらない
- ASCIIモジュール直下の自由関数(`participant_point.rs`の`一成分の許容差`・`倍精度の表現の刻み幅`、`single_precision_bridge.rs`の`単精度の接線の行の許容差`等)はいずれも、レシーバを持たない理由をコード自身が「対等な計算であるため」「どちらも主語にできないため」と明記しており、Rustの書き方の選択の条2(a)(b)を満たす
- 識別子への英字・ギリシャ文字・記号の混入(`A2`・`Σλ_n`型)は本区画では検出しなかった。数式(`I⁻¹ = Σ_k ...`等)はすべてコメント中の数式表記であり、識別子そのものには現れない

## 違反ゼロの範囲

- 上記の違反2件が現れた3ファイル(`system/row_admission.rs`・`system/tolerance_scale_entry.rs`・`tolerance_application/spelling.rs`)を除く、残り32ファイル全部(`double_reference/`配下30ファイル全部および`system/reduction_reading.rs`・`system/row_admission_tests.rs`)は、直読の結果、日本語命名の受理条件8つに照らして違反を検出しなかった
- 「パターンが一貫しているので省略した」ファイルは無い。35ファイル全部を直読した
