# ローラー検査(命名) 区画089

- 区画名: `crates/blitz_render/src/readback_image` 系(区画番号089、9ファイル)
- 対象作業ツリー: `C:/devs/Blitzdrache0_roller`(main の `db815090` 時点。編集はしていない)
- 対象ファイル一覧:
  1. `crates/blitz_render/src/readback_image/depth.rs`
  2. `crates/blitz_render/src/readback_image/local_visibility.rs`
  3. `crates/blitz_render/src/readback_image/motion_vector.rs`
  4. `crates/blitz_render/src/readback_image/result.rs`
  5. `crates/blitz_render/src/render_object_material/instance_transform_list.rs`
  6. `crates/blitz_render/src/render_object_material/material_slot_entry.rs`
  7. `crates/blitz_render/src/render_object_material/material_slot_list.rs`
  8. `crates/blitz_render/src/render_scene_material/mod.rs`
  9. `crates/blitz_render/src/render_scene_material/moving_instance_declaration.rs`
- 直読したファイル数: 9/9。grepだけで済ませたファイルは0件(全件を全文直読した)
- 読んだ規約: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(全1153行を対象語でgrep)

## 違反の一覧

該当なし(0件)。8条件のいずれかへ明確に落ちる識別子は見つからなかった。

## 違反の疑い

- `crates/blitz_render/src/render_object_material/material_slot_entry.rs:8` | `材質スロット素材`(構造体名) | 条件6(1語1義)に対する疑い | `_doc/計画/ユビキタス言語.md`(538行)には「材質スロット割当」という語が既に登録されており、定義は「1つの材質スロットIDへ実際の材質を対応させた組」である。本ファイルの`材質スロット素材`の型冒頭コメントは「1つの材質スロットへ与える材質と、そのスロットの番号」であり、フィールド構成(`スロット番号` + `マテリアル`)も含めて指している実体が「材質スロットID→材質の対応の組」で一致する。同じ形の概念に「割当」と「素材」という別の語が使われており、読み手は両者が同じ概念か別の概念(層が違うだけの類似構造)かを名前からは判別できない。`_doc/設計/マルチマテリアルと材質境界.md`を`材質スロット素材`で検索しても言及が無く、この型がいつ・なぜ既存の「材質スロット割当」と別概念になったかを名前からも設計文書からも復元できなかった。断定はしない(bounded contextを跨いだ意図的な別概念である可能性が残るため)。改名の素案(別概念だと確定した場合は不要): 同一概念なら`材質スロット素材`→`材質スロット割当`か、既存語と衝突しない`材質スロットの束縛入力`等への改名、または`_doc/計画/ユビキタス言語.md`へ両語の関係を追記する
- `crates/blitz_render/src/render_object_material/material_slot_list.rs:8` | `材質スロット素材一覧`(構造体名) | 条件6に対する疑い | 上と同じ理由の派生(`材質スロット素材`一覧の名前を引き継ぐため)。上の項の結論に従属する

## 違反ゼロの範囲

上記2件の疑いを除き、9ファイル・識別子(型8個・associated関数`生成する`7個・getterメソッド約20個・フィールド約15個・enum2種5枝相当・ローカル変数数個)を全数直読して受理条件8つを当てた結果、明確な違反は0件だった。具体的に確認した観点: (a)品詞の実体一致(`個体変換列`・`材質スロット素材`等の値は名詞句、`動く個体を宣言する`等の処理は動詞句)、(b)裸の動詞の禁止(`生成する`はすべて型に属する関連関数で型名が目的語を担う)、(c)ASCIIモジュール直下の自由関数の裸動詞は無し、(d)直訳調(`正規化装置座標`・`局所可視度`・`読み戻し`はいずれもユビキタス言語に登録済みの定着語であり、lookup→引く等の未加工直訳は無い)、(e)識別子へのASCII文字・ギリシャ文字・記号の混入は無し(標準ライブラリ境界の`u32`・`f32`・`Vec`のみで、これは境界の生値であり日本語識別子ではない)、(f)実体と合わない語・名詞1語への圧縮・詰め込みすぎの3形はいずれも検出しなかった。
