# 命名検査報告: 区画015

- 区画名: crates/blitz_app/src/reports/sky_pixel ほか(区画番号015)
- 範囲: block_015.txt に列挙された40ファイル(reports/sky_pixel・reports/sky_state・reports/sun_zenith_crossing・reports/temporal_reconstruction・reports/xpbd_solver_bench・runtime_assets・smoke 配下)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main の db815090。このツリーは編集していない)
- 直読したファイル数: 40/40(grepだけで済ませたファイルは0件)
- 参照した必読リスト: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(全文は404KBのため見出し一覧とsky_pixel/sky_state/xpbd関連語の grep で照合)

## 違反の一覧

1. `crates/blitz_app/src/smoke/pixel_judgment/srgb_convert.rs:3` | `linearをsrgb8bit相当のf32へ変換する` | 条件6(1語1義)・条件7(直訳の検出) | 同じ「線形色成分をsRGB相当へ変換する」処理が `crates/blitz_app/src/reports/sky_pixel/srgb_encode.rs:5` では確立済みの日本語「線形」で `線形をsrgb8bit相当へ変換する` と命名されているのに、こちらは英単語"linear"を生のまま使っている。英語のlinearを知らない読み手はこの識別子から「線形」を復元できず、同じクレート内の同じ概念が2つの語(linear/線形)で分裂している。ファイル冒頭の`//!`コメント自身は「リニア色成分」と訳しているのに、関数名だけ未翻訳のままである。改名の素案: `線形をsrgb8bit相当のf32へ変換する`
2. `crates/blitz_app/src/smoke/plan.rs:17` | `現在フレームからquadシーンの動作を導出する` | 条件2(ASCII識別子は補完力ゼロ)・条件7(直訳の検出) | 関数名の中に英単語"quad"が翻訳されずそのまま埋め込まれている。同じファイルの兄弟関数は`ヘルメット`・`シャドウ`・`フォックス`とすべて日本語(カタカナ)化しているのに、この1つだけ生の英字が残り、英語を知らない読み手は"quad"が何を指すか復元できない。しかも呼び出し元`world_plan.rs`ではこの計画を選ぶ世界の種別が`平面板`(登録済みの日本語の列挙枝)であり、識別子の"quad"はその語と一致しない(同じ対象を指す語が識別子ごとに食い違っている)。改名の素案: `現在フレームから平面板シーンの動作を導出する`

## 違反の疑い(断定しない)

1. `crates/blitz_app/src/smoke/execution.rs:32`(`開発uiを有効にするか`)・`crates/blitz_app/src/reports/xpbd_solver_bench/comparison.rs:16`(`gpuの拘束違反の二乗平均平方根`)・`crates/blitz_app/src/reports/xpbd_solver_bench/mod.rs:27`(`xpbd並列方式の計測表を出す`) | 条件6に対する疑い | いずれも「UI」「GPU」「XPBD」という既に確立された頭字語(専門用語の表記の一般語)を小文字(ui/gpu/xpbd)で識別子へ混ぜている。同じ区画・同じクレート内では`XPBD計測の条件`・`XPBD並列方式`・`開発UI判定`(smoke/mod.rs内の列挙の枝)のように大文字表記が主流であり、大文字と小文字が同じ語を指して混在している。意味の復元自体は妨げないため条件1〜5・7は通るが、1語1義の観点で表記の分裂が起きている疑いがある。改名するなら大文字表記へ揃える(`開発UIを有効にするか`・`GPUの拘束違反の二乗平均平方根`・`XPBD並列方式の計測表を出す`)。

## 違反ゼロの範囲

上記2件の違反と3件の疑いを除く、40ファイル中の残り全識別子(型名・エラー列挙の枝・フィールド・関数・定数・変数)は受理条件8つを満たしていることを直読で確認した。特に以下は良好な実例として確認した。

- `reports/sky_pixel/*`・`reports/sky_state/*`・`reports/sun_zenith_crossing/report_error.rs`・`reports/temporal_reconstruction/*`: エラー列挙の枝がすべて「何が壊れたか」を動詞句または状態句で明示し(`遠平面の点が無限遠へ写った`・`視線の長さが零`等)、記録・生成する等の裸の動詞は無い
- `reports/xpbd_solver_bench/sha256*`: `十六進の指紋を計算する`・`塊を圧縮する`・`詰め物を足す`のように、FIPS 180-4のアルゴリズムの各段が動詞句+目的語で命名されており、定数表(`constants.rs`)も型名`初期ハッシュ`・`丸め定数`が実体と一致する
- `smoke/*`: `求められたウィンドウ操作`・`求められた書き換え`の2つの限定列挙で17枝のアクションを絞り込む設計そのものが、条件3(5W1Hの必要格)を型で強制しており、`現在フレームから○○の動作を導出する`という関数名の型がモジュール間で統一されている(quadの1件を除く)
