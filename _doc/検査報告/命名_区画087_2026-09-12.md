# 命名検査報告 区画087: crates/blitz_render/src/local_visibility

- 区画番号: 087（全域ローラー検査・命名の軸）
- 対象: `crates/blitz_render/src/local_visibility/` 直下36ファイル（block_087.txt の一覧どおり）
- 作業ツリー: `C:/devs/Blitzdrache0_roller`（main の db815090 時点。このツリーは編集していない）
- 直読したファイル数: 36/36（全ファイルを直読した。grepだけで済ませたファイルは0件）
- 参照した必読: グローバルCLAUDE.md「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、プロジェクトCLAUDE.md「命名」節、`_doc/計画/ユビキタス言語.md`（局所可視度・局所可視度画像・遮蔽半径・遮蔽の深度バイアス・遮蔽の距離減衰・遮蔽の標本数・深度画像・法線の再構成・合成深度の場面・合成深度の注入・局所可視度の上書き・凹面・拡散間接方式の各項目）

## 違反の一覧

該当なし（0件）。

## 違反の疑い

1. `crates/blitz_render/src/local_visibility/draw_settings.rs:18` | `局所可視性の描画設定.方式`（フィールド名`方式`、型は`拡散間接方式`） | 条件1(受理テスト)に対する疑い | 兄弟フィールドの`補正の設定`・`検収の上書き`はそれぞれ役割を名指しした専用の語だが、`方式`は「どの方式か」を一切含まない一般語であり、フィールド型`拡散間接方式`が持つ具体性(拡散間接の)をフィールド名の側でだけ落としている。`局所可視性の描画設定.方式`という読み方だけでは何の方式かを復元できない(条件2のレシーバ補完はフィールドの型ではなく変数`描画設定`側にしか働かない)。ただし同じ値オブジェクト圏内で型名を字面通りフィールド名に採る慣習(例: `局所可視度の上書き`型を持つ`検収の上書き`は型名から具体化して命名している一方、本件は逆に抽象化しているため、他の慣習との整合を見ての最終判断は改名員に委ねる。改名の素案: フィールド名を型名と同じ`拡散間接方式`にする(値オブジェクト様式では型名をそのままフィールド名に使う運用が本モジュール内の他の型でも見られる)

## 違反ゼロの範囲

`acceptance_override.rs`・`bake.rs`・`bilateral_blur.rs`・`blur_settings.rs`・`code_value.rs`・`depth_bias.rs`・`depth_image.rs`・`diffuse_indirect_method.rs`・`distance_falloff.rs`・`error.rs`・`hemisphere_sample.rs`・`image_size.rs`・`local_visibility_tests.rs`・`narrowing.rs`・`neighbor_selection.rs`・`normal_reconstruction.rs`・`occlusion_radius.rs`・`occlusion_sampling.rs`・`occlusion_settings.rs`・`pixel_hash.rs`・`projection.rs`・`rotation.rs`・`same_surface_tolerance.rs`・`sample_count.rs`・`scene_intersection.rs`・`scene_shape.rs`・`settings.rs`・`shader_set.rs`・`synthetic_depth.rs`・`synthetic_scene.rs`・`view_direction.rs`・`view_displacement.rs`・`view_position.rs`・`visibility.rs`・`visibility_image.rs`の35ファイルは、型・関数・メソッド・フィールド・変数・定数・列挙の枝のいずれにも受理条件8条・命名の質・呼称と語の選択への違反を検出しなかった。`draw_settings.rs`も違反の疑い1件を除けば違反は無い。

検査した観点の補足:
- 値オブジェクト様式の単一フィールド(`値`・`幅`・`横`・`縦`・`形`・`軸`等)は、いずれもゲッターがレシーバ経由(`可視度.値()`等)で読まれるため受理条件2の補完対象に当たり、圧縮ではなく既定の様式として合格とした
- ASCII直下の自由関数(`narrowing.rs`の`単精度へ狭める`・`整数を単精度へ狭める`、`bake.rs`の`生の局所可視度を焼く`等)は名前単独で目的語・動作が読めることを確認した
- 「焼く」「均す」「狭める」はいずれも既存の定着比喩または直訳でない固有の動作語であり、直訳調(record→記録等)には当たらない
- コメント中の`SSAO`は既存の設計文書の見出し引用であり識別子ではないため対象外とした。識別子へのASCII・ギリシャ文字・記号の混入は1件も検出しなかった
