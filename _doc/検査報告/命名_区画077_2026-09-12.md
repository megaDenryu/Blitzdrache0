# 命名検査報告 区画077

- 区画名: `crates/blitz_render/src/atmosphere`(区画番号077、37ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の `db815090` 時点。このツリーは編集していない)
- 検査日: 2026-09-12
- 直読したファイル数: 37/37(block_077.txtに列挙された全ファイルを直読した。grepだけで済ませたファイルは0件)

## 違反の一覧

すべて「識別子へのASCII英字・ギリシャ文字の混入」(受理条件2の完全な違反、かつオーナーが2026-09-12に不合格にした実例「識別子にASCIIの英字・ギリシャ文字・数学記号(A2・Σλ_n・n)を入れたものは不合格」に該当する形)である。

1. `crates/blitz_render/src/atmosphere/atmosphere_tests.rs:50` | `m`(関数名。`fn m(値: f32) -> メートル`) | 落ちた条件: 1・2 | ASCII1文字の関数名であり「これは何?」に単独で答えられない。メートル値オブジェクトへ包むだけの処理を1文字の英字へ圧縮しており、オーナーが不合格にした`n`と同型。この関数は`atmosphere_tests`配下の少なくとも8ファイル(`intersect_tests.rs`・`density_tests.rs`・`optical_length_tests.rs`・`multiscatter_series_tests.rs`・`transmittance_tests.rs`・`scalar_field_tests.rs`ほか)で`use super::{m, ...}`により再利用され、違反が伝播している | 改名の素案: `メートルに包む`
2. `crates/blitz_render/src/atmosphere/atmosphere_tests/intersect_tests.rs:41,43-45,56-59` | `mu`(ループ変数・仮引数) | 落ちた条件: 2 | ギリシャ文字μのローマ字表記をそのままASCII識別子として使っている。天頂余弦の値を表すことは`余弦(mu)`という呼び出しからしか分からず、名前単独では数式変数にしか見えない | 改名の素案: `天頂余弦候補`または`余弦値`
3. `crates/blitz_render/src/atmosphere/atmosphere_tests/intersect_tests.rs:42,44` | `r`(ローカル変数。半径のf64値) | 落ちた条件: 1・2 | ASCII1文字。同じ関数内に日本語の`高度`・`媒体`があるのに、この変数だけ数式記号として書かれている | 改名の素案: `半径`
4. `crates/blitz_render/src/atmosphere/atmosphere_tests/intersect_tests.rs:43-44` | `d`(ローカル変数。距離のf64値) | 落ちた条件: 1・2 | ASCII1文字。閉形式の検算をそのままRustへ写した結果、数式の記号がコード識別子へ漏れている | 改名の素案: `距離`
5. `crates/blitz_render/src/atmosphere/atmosphere_tests/optical_length_tests.rs:55-56,59,72-73,75` | `mu`(ループ変数・ローカル変数) | 落ちた条件: 2 | 2と同型。同ファイル内で`余弦`という日本語名も既に使われており、`mu`だけが英字のまま残っている | 改名の素案: `天頂余弦候補`
6. `crates/blitz_render/src/atmosphere/atmosphere_tests/transmittance_tests.rs:73-74` | `mu`(ループ変数) | 落ちた条件: 2 | 2・5と同型 | 改名の素案: `天頂余弦候補`
7. `crates/blitz_render/src/atmosphere/atmosphere_tests/transmittance_tests.rs:15-16,18,34` | `mu段`(ループ変数。ASCIIの`mu`と日本語の`段`を連結した複合識別子) | 落ちた条件: 2 | 日本語の複合語に見せかけてASCII語根`mu`を埋め込んでおり、オーナーが挙げた「段A2」型の圧縮と同じ構造の違反である。同じファイルの他のループでは`段`のみで足りている(例: 47行目) | 改名の素案: `余弦段`
8. `crates/blitz_render/src/atmosphere/atmosphere_tests/phase_tests.rs:12,14,29,33,43-46,52-55` | `nu`(ローカル変数・クロージャ仮引数) | 落ちた条件: 2 | ギリシャ文字νのローマ字表記。散乱角余弦を表す値だが、名前だけでは何のギリシャ文字を指しているか非専門家に伝わらない | 改名の素案: `散乱角余弦値`
9. `crates/blitz_render/src/atmosphere/atmosphere_tests/phase_tests.rs:31` | `g`(ループ変数。非対称係数の値) | 落ちた条件: 1・2 | ASCII1文字。他の箇所では`係数`・`非対称係数`という日本語名を使っており、この変数だけ物理記号gのまま書かれている | 改名の素案: `非対称係数値`
10. `crates/blitz_render/src/atmosphere/atmosphere_tests/skyview_lookup_tests.rs:20,48,82` | `y`(ループ変数。視線のワールドY成分=天頂余弦相当の値) | 落ちた条件: 1・2 | ASCII1文字。数式の軸名をそのまま識別子にしており、周辺の`水平`・`視線`という日本語名と並ぶと単独で意味を持たない | 改名の素案: `天頂成分`

## 違反の疑い(断定しない)

1. `crates/blitz_render/src/atmosphere/narrowing.rs:17` | `実数へ狭める`(fn) | 条件7に対する疑い | ASCIIモジュール`narrowing`直下の自由関数であり、モジュール名からの補完は使えない(受理条件2)。関数はf64をf32へ狭める処理だが、名前の「実数」がf64・f32どちらを指すか一意に定まらず、f64もまた実数だという二重性が残る。ただし他に日本語で「単精度の実数」を短く言い表す定着語が無く、同モジュールの`長さへ狭める`(f64→メートル)との対比で読めば意図は概ね復元できるため、確定の違反にはしない | 改名の素案: `単精度の実数へ狭める`

## 違反ゼロの範囲

上記以外の識別子(型名・関数名・メソッド名・フィールド名・定数名)はすべて受理条件8つを満たしていた。具体的には次の点を確認した。

- `mod.rs`相当の10ファイル(`bake.rs`・`error.rs`・`geometry.rs`・`integration.rs`・`mapping.rs`・`medium.rs`・`narrowing.rs`・`quantity.rs`・`supply.rs`・`table.rs`)は`pub(in crate::atmosphere) mod ...`の宣言列とモジュール冒頭の`//!`だけで構成され、宣言される識別子はASCIIのモジュール名(言語制約で日本語不可、対象外)のみであり、日本語識別子の違反は無い
- `atmosphere_tests.rs`の定数群(`下端半径`・`上端半径`・`レイリー散乱係数`・`レイリー尺度高度`・`ミー散乱係数`・`ミー消散係数`・`ミー尺度高度`・`吸収消散係数`・`吸収下端高度`・`吸収頂点高度`・`吸収上端高度`・`位相非対称係数`)と関数`地球標準の媒体()`は、ユビキタス言語に登録された「消散媒体」「減衰成分」「密度分布」「消散係数RGB」の語彙とそのまま対応しており、実体との照合が取れた。`地球標準の媒体()`は名詞句だが、同型の`大気のベイク済み画像の解像度::既定値()`と同じ「固定の値を返す関連関数」の慣用に沿うため違反としなかった
- `reference_record.rs`の列挙`参照レコード`とその枝(`パラメータ`・`密度`・`地平線`・`上端距離`・`地表距離`・`光学距離`・`透過率`・`位相`)は、型冒頭の`///`が各枝の実体(何に対する期待値か)を明記しており、名前と実体の照合が取れた
- `reference_parse.rs`・`reference_record.rs`・`reference_tests.rs`とその子3ファイル(`geometry_tests.rs`・`integral_tests.rs`・`scalar_field_tests.rs`)の関数(`読み解く`・`レコードを読む`・`一致するか`・`相対誤差`・`表現下限`・`視線`)は、5W1Hの主要格(何を・どう)を満たし、直訳調も無い
- 空中遠近ボリュームの検査群(`aerial_lut_tests.rs`とその子3ファイル)、多重散乱・スカイビュー・透過率の検査群、太陽円盤・太陽相対フレーム・太陽可視性の検査群のテスト関数名(例:`地平線をまたぐ円盤は上側だけが残る`・`正規直交であり往復が恒等である`・`高度が上がると遮蔽の境界が下がる`)は、実体(何を検査しているか)を関数名だけで復元できる文になっており、「詰め込みすぎ」「名詞1語への圧縮」のいずれにも該当しない
- `uv`という識別子(`テクセル中心のuv`・`単位位置からテクセル中心のuv`等)はユビキタス言語で「UV」がテクスチャ座標の定着語として既に登録されており(例:「UVのU方向に沿った表面ベクトル」「地形のUVで標本できる形」)、専門用語の表記の基準(非専門家が読んで意味が取れる一般語)を満たすため違反としなかった
