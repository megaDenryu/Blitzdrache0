# 命名検査報告 区画040

## 区画名と範囲

区画: `crates/blitz_collision/src/height_field` 第1区画(区画番号040、40ファイル)。
一覧は `scratchpad/roller/block_040.txt` の40ファイルであり、`height_field/` 直下の `.rs` ファイル全部(サブディレクトリ `capsule_sweep/` の中身は含まない。`capsule_sweep.rs` はその親モジュールの `mod` 宣言ファイルのみが対象)。

## 時点

作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090` 時点。このツリーは編集していない。

## 検査対象と読み方

40ファイル全部を直読した(直読40/40、grepだけで済ませたファイルは0)。読んだのは各ファイルの全行であり、型・関数・メソッド・フィールド・変数・定数・列挙の枝の識別子を、`.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」8条件・「日本語命名の質」・「呼称と語の選択」・「専門用語の表記」と、`_doc/計画/ユビキタス言語.md`の高さ場・升目・掃引まわりの登録語(高さ場・升目の格子添字・高さ場の格子原点からの位置・高さ場の中の変位・高さ場を貫く線分・線分の走査が調べた範囲 等)を照合して判定した。

## 直読したファイル(40件)

axis_traversal.rs / canonical_end_tests.rs / capsule_sweep.rs / cell_corner.rs / cell_corner_heights.rs / cell_edge.rs / cell_hit.rs / cell_index.rs / cell_interval_end.rs / cell_ratio.rs / cell_segment_intersection.rs / cell_traversal.rs / corner_source.rs / displacement_solving.rs / error.rs / grid_origin_displacement.rs / grid_origin_position.rs / ground_plane.rs / ground_plane_tests.rs / length_limit.rs / length_limit_tests.rs / linear_in_parameter.rs / position_error.rs / real.rs / real_cell_index.rs / scanned_range.rs / segment.rs / segment_boundary_tests.rs / segment_direction_tests.rs / segment_error.rs / segment_hit.rs / segment_query.rs / segment_query_fixture.rs / segment_query_result.rs / segment_query_tests.rs / segment_traversal_tests.rs / shared_edge_tests.rs / slope.rs / slope_direction.rs / slope_direction_tests.rs

## 違反の一覧

該当なし。40ファイルの全識別子(型名・トレイト名・関連関数・メソッド・フィールド・列挙の枝・定数・ローカル変数)を受理条件の1〜8で当てたが、不合格になったものは無かった。ASCII・ギリシャ文字・数学記号の混入も、実体と合わない語も、名詞1語への圧縮も、詰め込みすぎの複合名も見当たらなかった。

具体的に検討した観点:

- **実体との照合**: `升目に居る区間の端`・`高さ場の中の変位`・`高さ場の格子原点からの位置`・`線分の走査が調べた範囲` 等はいずれも `_doc/計画/ユビキタス言語.md` の登録語と一致し、フィールド構成(東・天頂・南の3成分、時刻・比の組等)も定義と一致した
- **裸の動詞の禁止**: `生成する` は全箇所で `型名::生成する(...)` の関連関数として現れ、型名が目的語を担っている(条件5に適合)。ASCIIモジュール直下の裸の自由関数(`real_cell_index.rs`の`実数の床を升目の添字へ写す`等)も名前単独で「何をするか」に答えている
- **品詞の規律**: 値は名詞句(`升目の隅`・`地表の傾き`・`区間の端での軸の比`)、処理は「〜する」(`生成する`・`求める`・`写す`・`直す`)、真偽は`〜か`(`境界の上に静止しているか`)で一貫していた
- **1語1義**: `位置`・`変位`・`比」・`面」・「傾き」等の語がファイルをまたいで同じ意味で使われ続けており、衝突は見つからなかった
- **直訳の検出**: `参照する`(lookupの訳。ユビキタス言語の既定訳と一致)、`写す`(写像の意で使用、直訳調ではなく確立した数学語彙)等、英語の直訳に見える訳語は無かった

## 違反の疑い(断定しない、低確度)

1. `crates/blitz_collision/src/height_field/axis_traversal.rs:25` | ローカル変数 `境界の上から手前へ進む`(bool) | 条件4(品詞の規律。真偽は「〜か」) | 非過去の動詞終止形("進む")で書かれており、真偽値であることが名前だけでは読み取りにくい(同ファイル内の公開メソッド`境界の上に静止しているか`は正しく「〜か」で終わっている)。ただし宣言直後に`if`で使われ、スコープが数行に閉じるため実害は小さい | 改名の素案: `境界の上から手前へ進むか`
2. `crates/blitz_collision/src/height_field/cell_interval_end.rs` の列挙 `区間の端での軸の比` の3枝 `添字の側の縁にあり零` / `次の添字の側の縁にあり一` / `升目の内側にあり1次式で求める` | 条件1・8に対する疑い(詰め込みすぎの実例パターンとの類似) | 「どこにあるか」と「値または求め方」の2つの事実を1つの枝名へ連結しており、「オーナーが不合格にした実例」の詰め込みすぎの形に構造が似ている。ただしこの型自体が「境界を横断した軸は縁の上にあり正準値0/1を持ち、横断していない軸は1次式で求める」という1つの不変条件を表す列挙であり、2つの事実は型の定義そのもの(枝の存在理由)であって、無関係な条件を付け足したものではない。ファイル冒頭の`//!`コメントがこの対応を詳しく説明しており、実体と名前は一致している。そのため確定の違反にはせず疑いに留める | 改善案があるとすれば `添字の側の縁の値零` のような名詞化だが、現状より明確になるとは言い切れない

## 違反ゼロの範囲

上記2件の疑い(いずれも低確度・実害が小さいか実体と一致すると判定)を除き、`crates/blitz_collision/src/height_field/` 直下の40ファイル全部(axis_traversal.rs から slope_direction_tests.rs まで、テストファイル・値オブジェクト・トレイト・列挙・エラー型・操作サービスを含む全種別)で、日本語命名の受理条件8条件・命名の質・呼称と語の選択のいずれにも違反する識別子は見つからなかった。ASCII・ギリシャ文字・数学記号を識別子へ混ぜた例も無かった。省略した検査対象は無い(全40ファイルを直読)。
