# 命名検査報告 — 区画148「xtask/src/conform/declaration_comment_line」ほか

- 区画番号: 148
- 区画名(定義書の表記): `xtask/src/conform/declaration_comment_line`(区画に含まれるのは、この配下だけでなく `block_148.txt` に列挙された40ファイル全体)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の `db815090` 時点。このツリーは編集していない)
- 一覧の出典: `C:/Users/POKR30~1/AppData/Local/Temp/claude/C--devs-Blitzdrache0/612e37aa-a38d-4d29-a7a3-b0e9508d9f15/scratchpad/roller/block_148.txt`
- 直読したファイル数: 40 / 40(全ファイルを直読した。grepだけで済ませたファイルは0件。ただし識別子の網羅的な突き合わせのため、grepを補助として全40ファイルへ横断的に使った)
- 必読とした規約: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

1. `xtask/src/conform/depth_contract/table.rs:10` | `近面ndc`(構造体`深度契約`のフィールド) | 条2(日本語命名の質: 英語の頭字語・省略語を日本語の中に混ぜない) | NDC(Normalized Device Coordinates)は「専門用語の表記」節が補足不要とする一般語(UI・CPU・GPU・API等)に含まれず、非専門家が読んでも意味が取れない。しかも助詞・区切りが無いまま「近面」に直接くっつく膠着圧縮になっている | `近面の正規化デバイス座標` または既に用語として定着していれば `近面の正規化済み奥行き値`(ユビキタス言語への登録要)
2. `xtask/src/conform/depth_contract/table.rs:11` | `遠面ndc`(同上) | 条2 | 同上(1と対) | `遠面の正規化デバイス座標`
3. `xtask/src/conform/depth_contract/table.rs:26,27,35,36` | `近面ndc` / `遠面ndc`(定数`カメラ深度`・`光源影深度`の初期化での再出現) | 条2 | フィールド名自体の違反がそのまま初期化式にも伝播している | 1・2の改名に追従

## 違反の疑い(条2に対して。定義元がこの区画の外にあるため断定しない)

以下は本区画の台帳ファイル(`free_function_whole_type/ledger/table/*.rs`)が、既存の別ファイルにある関数名・型名を文字列データとして引用している箇所である。台帳自体の識別子(定数名・構造体名・関数名)は全部合格だが、引用されている名前そのものが日本語の中へ英語の頭字語を混ぜており、条2に照らすと疑いがある。定義元ファイルは今回の区画に含まれないため、断定せず「疑い」として報告する。

- `xtask/src/conform/free_function_whole_type/ledger/table/blitz_app_app.rs:17` | `圧縮前のhdrを読み戻す`(引用されている関数名。定義元は `crates/blitz_app/src/app/draw_dispatch/temporal_reconstruction_check/readback.rs`) | 条2の疑い | HDR(High Dynamic Range)は一般に通用する語(UI/CPU/GPU/API等)に含まれず、小文字のまま日本語の助詞に直結している | `圧縮前の高輝度画像を読み戻す`(または用語登録の上で「圧縮前のHDR画像」)
- `xtask/src/conform/free_function_whole_type/ledger/table/blitz_render_other.rs:17` | `xpbdの並列方式をgpuで走らせて読み戻す`(引用されている関数名。定義元は `crates/blitz_render/src/xpbd_solver_bench_probe/mod.rs`) | 条2の疑い | XPBD(Extended Position Based Dynamics)は非専門家に通じない頭字語であり、小文字のまま「〜の」に直結している(gpuは一般語として許容範囲内) | `位置基準動力学の並列方式をGPUで走らせて読み戻す`(XPBDの訳語をユビキタス言語へ先に登録してから使う)
- `xtask/src/conform/free_function_whole_type/ledger/table/blitz_render_other.rs:18` | `XPBD計測の条件`(引用されている型名。同上ファイル) | 条2の疑い | 同上 | `位置基準動力学の計測条件`
- `xtask/src/conform/free_function_whole_type/ledger/table/other_crates.rs:11` | `原型の文書jsonを作る`(引用されている関数名。定義元は `crates/blitz_asset_compiler/src/loader/contract/archetype_fixture.rs`) | 条2の疑い | JSON(JavaScript Object Notation)は非専門家に通じない略語であり、小文字のまま名詞へ直結している | `原型の文書をJSONで作る`(用語登録の上で)、または `原型のjson文書を作る` ではなく `原型の設定文書を作る` のような意訳
- `xtask/src/conform/free_function_whole_type/ledger/table/other_crates.rs:14` | `文書jsonを作る`(引用されている関数名。定義元は `crates/blitz_asset_compiler/src/loader/contract/fixture_json.rs`) | 条2の疑い | 同上 | `文書をJSONで作る`

## 違反ゼロの範囲

上記を除く36ファイルの識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名)は受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも抵触しなかった。具体的には次の領域である。

- `declaration_comment_line/tests.rs`、`dependency_whitelist/ledger.rs`、`depth_contract/tests.rs`・`table/camera.rs`・`table/camera_compare.rs`・`table/shadow.rs`(NDCフィールドの引用箇所を除く)
- `duplicate_file_literal/` 配下の全13ファイル(`allowance.rs`・`builtin_include_bytes.rs`・`builtin_include_bytes_tests.rs`・`extract.rs`・`include_bytes_argument.rs`・`scan_scope.rs`・`self_reference.rs`・`tally.rs`・`test_item_skip.rs`・`tests.rs`・`allowance/build_output.rs`・`allowance/table.rs`・`allowance/table/other_files.rs`・`allowance/table/shader_files.rs`・`include_bytes_argument/code_sequence.rs`)
- `error/display.rs`
- `free_function_whole_type/` 配下の識別子(定数名・構造体名・関数名そのもの。`index.rs`・`ledger.rs`・`parameter.rs`・`signature.rs`・`tests.rs`・`ledger/entry.rs`・`ledger/table.rs`・各`ledger/table/*.rs`)。ただし各`table/*.rs`が引用する既存の関数名・型名は上表「違反の疑い」を除き問題なし
- `lighting_query_declaration/table.rs`、`module_import_boundary/table.rs`・`module_import_boundary/tests.rs`

命名以外の観点(構造・行数・コメント配置等)は本検査の対象外であり、検査していない。
