# 命名検査報告: 区画003(crates/blitz_app/src/app/cloth_setup 他)

- 区画: `crates/blitz_app/src/app/cloth_setup`(区画番号003)。実際の対象は一覧ファイル(`block_003.txt`)が指す34ファイルであり、`cloth_setup`配下6件・`create`配下2件・`draw_dispatch`配下13件・`frame`配下13件から成る
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、時点db815090)。このツリーは編集していない
- 時点: 2026-09-12
- 直読したファイル数: 34/34(全件を直読した。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(関連語のみ照合。全文は404KBのため受理条件に照らして必要な語だけをgrepで参照した)

## 違反の一覧

該当なし。34ファイルの全識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)を受理条件8つで検査し、実例が示す3形(実体と合わない語・名詞1語の圧縮・詰め込みすぎ)にも当てたが、条件を落とす識別子は見つからなかった。

## 違反の疑い

1. `crates/blitz_app/src/app/draw_dispatch/temporal_reconstruction_check/readback.rs:13` | `圧縮前のhdrを読み戻す`(および`temporal_reconstruction_injection.rs:79`・`execute.rs`系列からの呼び出し) | 条件6(1語1義)・条件7(直訳・表記統一)に対する疑い | `_doc/計画/ユビキタス言語.md`は「HDR」を大文字の確立語として登録し(「明るさの圧縮前のHDR画素」)、同じ意味の型名も`HDR読み戻し画像`と大文字で書かれている。この関数名だけ小文字`hdr`になっており、読み手が「別の何か(hdrという小文字の略号)」と誤読する余地がある。Rustの関数はsnake_caseであるという言語規約からの小文字化である可能性が高く、実体の取り違えではないと判断したため違反でなく疑いに留めた。改名の素案: `圧縮前のHDRを読み戻す`(型名・登録語の大文字表記に揃える)
2. `crates/blitz_app/src/app/frame/borrowed_draw.rs:23,41` および `crates/blitz_app/src/app/frame/draw_input.rs:76` | `ui描画`(フィールド名・変数名。型は`UI描画データ`) | 条件6・条件7に対する同型の疑い | 型名は`UI`と大文字だが、フィールド・変数では小文字`ui`になっている。上と同じくRustの命名規約(型はUpperCamel、変数はsnake_case)をASCII部分にも機械的に適用した結果である可能性が高く、意味の取り違えは起きにくいため違反でなく疑いに留めた。一括での是正は求めないが、触るときに`UI描画`(またはひらがな化した「ユーザー画面」等ユビキタス言語に既にあれば其方)へ揃える余地がある

いずれも「実体を取り違える」という受理条件の核心には触れておらず、大文字小文字の表記ゆれの水準である。これ以上の追及(一括リネーム)はこの検査の範囲外とし、断定はしない。

## 違反ゼロの範囲

検査した34ファイル全域(cloth_setup配下6ファイル、create配下2ファイル、draw_dispatch配下13ファイル、frame配下13ファイル)で、受理条件8つに対する明確な違反(名詞形での処理の偽装、目的語欠落、直訳調、膠着圧縮、裸の動詞、漢字1文字への圧縮、ASCII/ギリシャ文字/数学記号の混入)は検出しなかった。各ファイルは冒頭コメントで「受け取るもの・返すもの・いつ呼ばれるか」を明示し、識別子(`アタッチ先の対応`・`布の目標拘束`・`目標の更新対応`・`描画の到達`・`世界の描画構成`・`フレーム視点`・`視点の履歴`等)はいずれも用語定義ファイルの登録語または登録語から一意に導ける複合語で、実体(構造体のフィールド・関数の入出力)と名前が一致していることを定義を開いて確認した。
