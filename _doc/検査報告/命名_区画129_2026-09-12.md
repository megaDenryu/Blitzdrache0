# 命名検査報告 区画129: crates/blitz_sim/src/sph, crates/blitz_sim/src/surface_flow

- 区画番号: 129
- 対象ディレクトリ: `crates/blitz_sim/src/sph`(8ファイル)、`crates/blitz_sim/src/surface_flow`(8ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, db815090時点。このツリーは編集していない)
- 直読したファイル数: 16/16(全ファイルをRead toolで直読。grepだけで済ませたファイルは0件)
- 検査軸: 日本語命名の受理条件(8条件)、日本語命名の質、呼称と語の選択、専門用語の表記。参照した用語定義: `_doc/計画/ユビキタス言語.md`(grep検索でSPH等の登録語を確認。同ファイルは404.8KBで全文は読めないため関連語のgrep結果で照合した)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_sim/src/sph/spec.rs:6 | `Sph仕様`(構造体名) | 日本語命名の質・条1(頭字語混入禁止)、受理条件・条2(ASCII識別子は補完力ゼロ) | "Sph"は英語の頭字語(Smoothed Particle Hydrodynamics)がそのままASCIIで日本語の名詞「仕様」へ膠着している。ユビキタス言語.mdは「SPH」を専門用語として定義し初出補足を与えているが、それは文章中の表記であり、識別子へ英字を直接混ぜてよい根拠にはならない。UI・CPU・GPU・API等の「一般に通用している語」の枠には入らない | `弱圧縮性流体仕様`または`粒子法流体仕様`(ユビキタス言語.mdへ訳語を登録してから使う) |
| crates/blitz_sim/src/sph/error.rs:6 | `Sph仕様エラー`(列挙名) | 同上 | 同上(`Sph仕様`のエラー型でも同じ頭字語混入) | `粒子法流体仕様エラー` |
| crates/blitz_sim/src/sph/density.rs:7 | `sph密度を計算する`(自由関数) | 日本語命名の質・条1、受理条件・条2 | 小文字の"sph"が動詞句の直前に直接連結し、日本語の膠着圧縮になっている。呼び出し側は`super::density::sph密度を計算する`とASCIIモジュール名越しに読むため、条2により英字の意味は補完されない | `粒子法流体の密度を計算する` |
| crates/blitz_sim/src/sph/step.rs:8 | `sphを一ステップ進める`(自由関数) | 同上 | 同上。"sph"を目的語の格助詞「を」の直前へ裸のASCIIとして置いている | `粒子法流体を一ステップ進める` |
| crates/blitz_sim/src/surface_flow/state.rs:78 | `usizeへ`(自由関数) | 受理条件・条2、日本語命名の質・条1に準じる英字混入 | Rustのプリミティブ型名"usize"がそのままASCIIで格助詞「へ」の直前に置かれている。呼び出し側`state::usizeへ(値)`もASCIIモジュール越しであり、英字部分は補完されない読み手に「何をする関数か」を型名の綴りでしか伝えられない | `符号無し32ビット整数を添字型へ変換する`または`添字数へ変換する` |
| crates/blitz_sim/src/surface_flow/step.rs:12 | `列数u32`・`行数u32`(局所変数) | 受理条件・条2に準じる英字混入 | ローカル変数名の末尾に型名"u32"をASCIIのまま連結しており、同じ関数内に型変換前後の値(`列数u32`と`列数`)が並ぶため紛れも生む | `列数指定値`・`行数指定値`、または変換前後を`列数(u32型)`のようにコメントで示し変数名自体は`列数`一本にする |
| crates/blitz_sim/src/surface_flow/step.rs:52 | `流す`(自由関数、モジュール私有) | 受理条件・条3(5W1Hの必要格)、条1(受理テスト) | 関数は「セルからセルへ量を移す」処理だが、名前の「流す」だけでは何を(量)・どこから どこへ(元セルから先セルへ)が一意に読めない。ASCIIモジュール(`step.rs`)直下の自由関数であり条2によりモジュール名からの補完も無い。実引数(厚さ配列・元・先・量)を読まないと動作が確定しない点はオーナー不合格実例の「目的語・着点の欠落で一意に読めない」と同型 | `セル間で液量を移す`または`元セルから先セルへ量を移す` |
| crates/blitz_sim/src/sph/kernel.rs:5 | `密度核`(自由関数) | 受理条件・条4(品詞の規律。処理なのに名詞形) | 関数は距離と半径から核関数の値を計算する処理(pub(crate) fn)であり、副作用の無い計算だが計算処理である以上「〜する」の動詞句であるべきところ、値のような名詞句になっている。呼び出し元でも`密度核(距離二乗, 半径)`と読め、「密度核という値を取得する」のか「密度核を計算する処理」なのか型だけでは判別できない | `密度核を計算する` |
| crates/blitz_sim/src/sph/kernel.rs:14 | `圧力勾配係数`(自由関数) | 同上(条4) | 同上のパターン。圧力勾配係数を計算する処理が名詞形のまま | `圧力勾配係数を計算する` |
| crates/blitz_sim/src/sph/kernel.rs:21 | `粘性ラプラシアン`(自由関数) | 同上(条4) | 同上のパターン | `粘性ラプラシアンを計算する` |
| crates/blitz_sim/src/sph/density.rs:22 | `距離二乗`(自由関数、pub(crate)) | 同上(条4) | 2点間の距離の二乗を計算する処理であり、`density.rs`と`step.rs`から呼ばれる計算処理だが、名前は結果の値と同じ名詞形で処理であることが読み取れない | `距離二乗を計算する` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| crates/blitz_sim/src/sph/particle.rs:36 | `更新する`(`流体粒子`のメソッド) | 条3(5W1Hの必要格) | レシーバ`流体粒子`が目的語を担うため受理条件2の補完(型名・レシーバ)は成立し、値オブジェクト様式の枠内ではある。ただし名前自体は「何を更新するか」(位置と速度)を言わず、`流体粒子`が将来フィールドを増やしたときに紛れる余地が残る。現時点のシグネチャ(引数が位置・速度そのもの)では読み手が引数を見れば分かるため、断定的な不合格にはしなかった |
| crates/blitz_sim/src/surface_flow/flux.rs:3 | `流出割合を計算する`が返す`[f32; 4]`の各要素 | 条3 | 関数名自体は「何を(流出割合)+どうする(計算する)」を満たし合格。ただし戻り値配列の4要素(下端・上端・左端・右端の順)の対応が、呼び出し側`step.rs:30-33`のインデックス番号(`割合[0]`〜`割合[3]`)でしか読めず、命名だけでは方向の対応が一意に取れない。これは配列要素への添字アクセスの問題であり識別子の命名条件そのものへの違反とは言い切れないため疑いに留めた |

## 違反ゼロの範囲

- `crates/blitz_sim/src/sph/mod.rs`・`spec.rs`(構造体`Sph仕様`自体は上表で指摘済みだが、フィールド名`平滑化半径`・`静止密度`・`圧力剛性`・`粘性係数`・`時間刻み`・`重力加速度`・`境界`・`反発係数`とそのアクセサ名は全て日本語の名詞句であり、値=名詞句の品詞規律に適合。違反なし)
- `crates/blitz_sim/src/sph/error.rs`・`crates/blitz_sim/src/surface_flow/error.rs`の列挙の枝(`〜が不正`・`〜が小さすぎる`・`〜が範囲外`・`〜が不一致`)は真偽・状態を表す語尾が一貫しており違反なし
- `crates/blitz_sim/src/surface_flow/cell.rs`・`spec.rs`・`state.rs`のうち`Sph`混入と`usizeへ`以外の識別子(`表面セル`・`液膜厚さ`・`接線速度`・`表面流仕様`・`格子寸法`・`セル幅`・`重力接線成分`・`速度減衰`・`表面流状態`・`セル一覧`・`空で生成する`・`液膜を設定する`・`液膜分布で生成する`・`構築する`・`添字を得る`・`セル`・`総液膜厚さ`)は名詞句/動詞句の品詞規律・5W1Hの必要格・1語1義のいずれにも適合
- `crates/blitz_sim/src/surface_flow/step.rs`のうち`流す`以外の識別子(`一ステップ進める`・`速度を進める`)は受理条件に適合
- 識別子へのギリシャ文字・数学記号(Σ・λ・n等)の混入は本区画16ファイル全体で0件

## 検査の網羅性についての注記

- ユビキタス言語.md(404.8KB)は一括読み込みができないため、SPH・粒子法・密度・カーネル等の関連語をgrepで検索して照合した。sph/surface_flowモジュール固有の用語(密度核・圧力勾配係数・粘性ラプラシアン・表面セル等)はユビキタス言語.mdに未登録であり、1語1義の判定はファイル内の一貫性のみで行った(登録の要否は別途是正側の判断)
