# 命名検査報告 区画114: crates/blitz_sim/src/constraint_graph

- 区画番号: 114
- 対象: `crates/blitz_sim/src/constraint_graph`(32ファイル、`mod.rs`直下26ファイル + `irregular/`配下2ファイル + `reference/`配下4ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`、時点: main の `db815090`(このツリーは編集していない)
- 直読したファイル数: 32/32(全数を直読した。grepだけで済ませたファイルは0件)
- 必読リストは全プロジェクト共通CLAUDE.mdの「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`の「XPBD共通拘束基盤の語彙」節を読み、`拘束グラフ`・`点添字`・`拘束添字`・`目標拘束添字`・`曲げ拘束添字`・`グラフ彩色`・`二面角`(共有する辺a・bと翼c・d)等の登録済み語と実装の型・フィールドを照合した

## 違反の一覧

なし(確定できる違反は検出しなかった)。

## 違反の疑い

強い確信を持って不合格と断定するには至らないが、規約の「識別子への英字・ギリシャ文字・記号の混入」条項に照らして検討記録を残す。

1. `crates/blitz_sim/src/constraint_graph/bending_batch.rs:206-209` | `辺a`・`辺b`・`翼c`・`翼d`(`添字付き曲げ拘束`のフィールド) | 条件8(ASCII混入の禁止)に対する疑い | 「辺」「翼」という日本語の名詞へASCII1文字を直接融合した綴りであり、単独では「段階A2」型の圧縮に見える。ただし`_doc/計画/ユビキタス言語.md`「二面角」の項が「共有する辺(a・b)と翼(c・d)の4点」とすでに a/b/c/d の記法を定義済みであり、この命名はその定義済み記法をフィールド名へそのまま持ち込んだものである。ユビキタス言語の定義と実装が一致しているため、規約違反というより「定義済み記法の使い回し」と読める。改名するなら定義側の記法自体(ユビキタス言語の該当項)を見直すところから始めるべきで、この型だけを孤立して改名すると定義との対応が崩れる
2. `crates/blitz_sim/src/constraint_graph/reference/schedule.rs:30` | `接続数a`・`接続数b`(`同時の緩和係数`関数の引数) | 条件8に対する疑い | 1と同型の融合(日本語名詞+ASCII1文字)。対応する`添字付き距離拘束`の既存フィールド`a`・`b`(この区画の外、`indexed_constraint.rs`で定義)を指す引数名であり、既存の型が持つ`a`/`b`という側の呼び方をそのまま接頭辞化しただけで、新規の圧縮ではない
3. `crates/blitz_sim/src/constraint_graph/adjacency.rs:67-70` | `隣接の側`の枝 `A`・`B` | 条件8に対する疑い | 枝名がASCII1文字のみ。ただし`添字付き距離拘束`(区画外で定義済み)がすでに`a`・`b`という2つの参加点をフィールド名に持ち、この列挙はその「どちら側か」を表す対応物である。枝名を日本語の「点A」「点B」等へ言い換えても、由来の`a`/`b`という綴り自体がASCIIのままである以上、真の是正は`添字付き距離拘束`の`a`/`b`フィールドまで遡って検討する必要がある(この区画だけでは閉じない)
4. `crates/blitz_sim/src/constraint_graph/acceleration.rs:11-13` | `一様な加速度`のフィールド `x`・`y`・`z` | 条件8に対する疑い | ASCII1文字。ただしワールド座標の3成分を指す慣用記法であり、`blitz_math`の位置・変位・速度等の既存の値オブジェクト群と同じ記法に揃えたものと見られる(この区画の外の型と同じ流儀)。この区画だけを直して他が`x`/`y`/`z`のままなら一貫性が崩れるため、直すなら`blitz_math`側からの一括判断が要る
5. `crates/blitz_sim/src/constraint_graph/irregular/neighbors.rs:16-19` | `点の対`のフィールド `a`・`b` | 条件8に対する疑い | 3と同型。`添字付き距離拘束`の`a`/`b`と同じ記法をこの区画内の別の型にも広げたものであり、区画内では一貫している
6. `crates/blitz_sim/src/constraint_graph/test_fixtures.rs:15,22,29` | 自由関数 `引数`・`点`・`拘束`(`pub(super)`のテスト用ビルダー) | 条件1・条件4に対する疑い | 「これは何?」への答えが単独では弱い。`点(x, y, 逆質量)`は返す型が`点自由度の初期状態`であり、名前の`点`だけでは初期状態・逆質量を運ぶことが読めない。`拘束(a, b, 静止長)`も同様に返す型`添字付き距離拘束`の「距離拘束」であることが名前に出ていない。ただし3つとも`pub(super)`でこのファイル1本の中でだけ使われ、呼び出し箇所(`三角形と尾のグラフ()`内の`vec![点(0.0, 0.0, 0.0), ...]`)は宣言的なデータ列として読め、同じファイル内の`検査の格子の仕様`・`検査の不規則の仕様`・`三角形と尾のグラフ`という、返す値をそのまま言い切る命名の並びに挟まれた短縮形である。単独の受理テストには弱いが、実害(誤読)が生じている箇所は無いと判断し、疑いに留める

## 違反ゼロの範囲

上記の「違反の疑い」6件を除く、32ファイル全体の型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝(具体的には `一様な加速度`・`点ごとの拘束の隣接表`・`隣接の項目`・`曲げ拘束のバッチ`・`添字付き曲げ拘束`・`曲げ拘束の彩色`・`曲げ拘束添字`・`曲げの色の区間`・`グラフ彩色`・`色の区間`・`拘束添字`・`拘束グラフエラー`の全枝・`拘束グラフ`・`規則格子の仕様`・`規則格子の拘束グラフを作る`・`添字付き距離拘束`・`添字付き目標拘束`・`不規則グラフの仕様`・`不規則な拘束グラフを作る`・`点添字`・`点自由度の初期状態`・`拘束グラフの参照計算`・`反復の更新の順序`・`同時の緩和係数`・`拘束違反の二乗平均平方根`・`拘束違反の平均`・`目標拘束のバッチ`・`目標拘束添字`・`目標拘束の有効`・`目標拘束の目標`、および各ファイルのメソッド群)は、受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも違反しない。特に、動詞句のメソッド名(`成分から生成する`・`一刻みの変位`・`一刻みの速度の変化`・`点の項目`・`接続数`・`拘束へ貪欲に色を割り当てる`・`拘束を並べ替えた`・`一刻み進める`・`一回反復する`等)はいずれも「何を+どうする」の格を満たし、値オブジェクト様式の関連関数`生成する()`はすべて型名またはレシーバが目的語を担っている。ユビキタス言語に登録済みの語(拘束グラフ・拘束違反・コンプライアンス・ラグランジュ乗数・目標拘束・曲げ拘束・二面角・静止角・グラフ彩色・接続数・色の区間 等)と実装の型・フィールドの実体は一致しており、1語1義の逸脱・直訳調・膠着圧縮・実体と合わない語・名詞1語の圧縮・詰め込みすぎの事例は見つからなかった
