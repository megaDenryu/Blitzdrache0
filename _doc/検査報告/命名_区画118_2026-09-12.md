# 命名検査報告: 区画118

- 区画名: `crates/blitz_sim/src/contact/normal_tangential_system`
- 範囲: block_118.txt に列挙された30ファイル
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`(main の `db815090`。このツリーは編集していない)
- 直読したファイル数: 30/30(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 参照した定義: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節
- ユビキタス言語: `_doc/計画/ユビキタス言語.md` は404.8KBでファイル全体を1回で読めなかったため、本区画の識別子(接触点集合・法線・接線・連立・許容差・有効集合等)に絞って個別に照合できる範囲で使った。全文の一括読み込みはしていない。

## 違反の一覧

このオーナー実例(2026-09-12)が挙げた3つの不合格の形が、この区画のソースにそのまま現存している。実例の文面と現在のソースの識別子が一字一句一致しており、まだ是正されていない。

1. `crates/blitz_sim/src/contact/normal_tangential_system/system.rs:31` | `接触点集合の法線と接線の連立`(構造体名。`mod.rs:72`で`pub use`され、`active_set.rs`・`candidate.rs`・`complementarity.rs`・`full_scan_reference.rs`・`participant_correction.rs`・`reduced_system.rs`・`reduced_system_residual.rs`・`reduced_system_tolerance_scale.rs`・`row_order.rs`・`single_point_fixture.rs`・`solve_outcome.rs`(間接)・`subset_search.rs`・`system_fixture.rs`・`tangential_row.rs`(間接)・`tolerance_application.rs`・`translation_tests.rs`ほぼ全ファイルで型として使われる) | 条件1・条件3 | 「連立」という名詞だけでは、法線と接線の行を束ねたものが方程式なのか行列なのか、初見で復元できない。オーナーが2026-09-12に不合格とした実例と一字一句同じ識別子である | `接触点集合の法線と接線の連立方程式`
2. `crates/blitz_sim/src/contact/normal_tangential_system/tolerance_application.rs:34` | `試験の許容差の当て方`(構造体名。フィールドは受理の倍率・行への当て方・届く先・閾値の倍率の4つを束ねる。`mod.rs:75`で`pub(in crate::contact) use`され、`system.rs:27,37,50`・`reduced_system.rs:46`・`reduced_system_residual.rs:94`・`reduced_system_tolerance_scale.rs:22,27`で使われる) | 条件1・条件3 | 「当て方」は会話では「どう適用するか」としか読めないが、実体は適用先・受理の倍率・行を積む閾値の倍率という3種の設定を束ねた指定であり、名前だけから内容を復元できない。オーナーが2026-09-12に不合格とした実例と一字一句同じ識別子である | `試験の許容差の適用規則`
3. `crates/blitz_sim/src/contact/normal_tangential_system/tolerance_application.rs:29` | `許容差の内側の点の行を積まず右辺を零にする`(enum `接線の行への許容差の当て方` の枝) | 条件1・条件3 | 言葉を詰め込みすぎて意味が壊れている。実体は「接線変位が許容差以下の点の拘束行を追加しない」ことであり、右辺を零にするという扱いは本質でない付随情報である。オーナーが2026-09-12に不合格とした実例と一字一句同じ識別子である | `許容差以下の接線変位の拘束行を追加しない`

## 違反の疑い(断定しない)

- `candidate.rs:28` | `質: 解いた行の残差と補正の大きさ,`(フィールド名) | 条件1に対して | 「質」は単独では何の質か読めない。フィールド宣言と同じ行に型`解いた行の残差と補正の大きさ`があり読み手はそこから補える一方、受理条件2が許す補完文脈(型名::関連関数の型名、レシーバ)には該当しないため、疑いとして残す | 例: `解いた質`
- `row_order.rs:22` / `reduced_system.rs:25` | `並び: 抜き出した行の並び,`(フィールド名) | 条件1に対して | 「並び」は単独では何の並びか読めない。同じ行の型`抜き出した行の並び`が補うが、上と同じ理由で受理条件2の許容文脈に厳密には該当しない | 例: `行の並び`
- `row_order.rs:31` | `有効集合から組む(連立: &接触点集合の法線と接線の連立, 有効集合: &法線と接線の連立の有効集合)`(関連関数名) | 条件3に対して | 名前は「有効集合から」とだけ言うが、実引数は連立と有効集合の2つであり、連立が材料から抜けている。呼び出し箇所(`reduced_system.rs:33`)は`連立`を読めば分かるため実害は小さく、疑いに留める | 例: `連立と有効集合から組む`

## 違反ゼロの範囲

上の3件の既知の不合格実例(すべて`tolerance_application.rs`と`system.rs`に定義され、他ファイルは型・値としてimportして使うだけ)を除き、30ファイル全域で受理条件8つ・命名の質(頭字語混入・直訳調・膠着圧縮)・呼称と語の選択(漢字1文字への圧縮・カタカナ語)・識別子へのASCII/ギリシャ文字/数学記号の混入は検出しなかった。特にASCII・ギリシャ文字・記号の識別子混入は0件(数式記号C_n・α̃・λ_n等はすべてコメント内のみで、識別子には現れない)。
