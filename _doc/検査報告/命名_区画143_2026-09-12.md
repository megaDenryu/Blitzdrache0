# 命名検査報告 区画143「xtask/src/acceptance/judgment_value」

- 区画番号: 143
- 区画名: xtask/src/acceptance/judgment_value(実際の一覧はacceptance配下7ディレクトリ+asset_generator配下2ディレクトリの合計39ファイル)
- 対象の一覧: `scratchpad/roller/block_143.txt`
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、db815090時点。このツリーは編集していない)
- 直読したファイル数: 39/39(全ファイルを直読した。grepだけで済ませたファイルは無い)
- 補助として、識別子へのASCII混入の網羅確認のために`xtask/src/acceptance`と`xtask/src/asset_generator`へ正規表現grepを1回ずつ実行した(直読の裏取りであり、直読の代わりではない)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`(該当語を照合)

## 違反の一覧

該当なし。39ファイルを直読し、受理条件8つ・オーナー不合格の3形(実体不一致・名詞1語の圧縮・詰め込みすぎ)・ASCII/ギリシャ文字/記号の混入の観点をすべて当てたが、断定できる違反は無かった。

## 違反の疑い

1. `xtask/src/asset_generator/launch.rs:22`・`xtask/src/asset_generator/error.rs`(全枝)・`xtask/src/asset_generator/launch_command.rs` | フィールド`生成器: &'static str`(構造体`アセット生成器の起動`)、およびエラー型`生成器エラー`の各枝が持つ`生成器: &'static str` | 条件7(直訳/実体不一致の検出質問) | `生成器`という名前は「その生成器という実体(プロセス・生成器そのもの)」を予告するが、実際に保持するのは`指定.生成器の名前()`が返す`&'static str`(`"compile_assets"`等の例名)であり、生成器の実体ではない。読み手が定義だけを見ると生成器のハンドルだと誤読しうる。ただし呼び出し元の`生成器の名前()`という命名や、周辺のエラー文面(`{生成器}を起動できなかった`のように名前として自然に埋め込まれる用途)を踏まえると、プロジェクト内で「識別に使う名前」という意味に一貫して倒しているとも読め、断定はしない | 改名の素案: `生成器: &'static str` → `生成器の名前: &'static str`(型全体で一貫して付け替える)
2. `xtask/src/asset_generator/specification.rs:36,50`・`xtask/src/asset_generator/arguments/bake.rs:24,34` | フィールド名`同居植生個体数`(型`同居植生の個体数`を保持) | 条件6(1語1義) | 同じ概念を、型名は`同居植生の個体数`(助詞「の」あり)、フィールド名は`同居植生個体数`(助詞なし)と綴り分けている。`word_list`へ渡す役割文字列も`"同居植生の個体数"`(助詞あり)であり、同一ファイル群の中で1つの概念に2通りの綴りが存在する。単独では「同居-植生-個体数」という複合名詞として読めるため受理テストは通るが、型名と食い違う綴りが同居する点は1語1義の観点で気になる | 改名の素案: フィールド名を`同居植生の個体数`へ揃える(型名・役割文字列と一致させる)
3. `xtask/src/acceptance/judgment_value/comparable.rs:9,20` | トレイト`判定で比べられる値`(受動態)とその変換先の列挙`判定が比べる値`(能動態、`use super::判定が比べる値;`で導入)が、助詞と態だけが違う酷似した名前で並んでいる | 条件6(1語1義)の隣接懸念 | それぞれ単独では「判定で比べられる値」「判定が比べる値」として受理テストを通るが、名前の差が「で/が」という1助詞と受動/能動の違いだけであるため、コードを読み進める際に取り違えるリスクがある。実体(トレイト vs 具体の列挙)は明確に違うため、断定の違反とはしない

## 違反ゼロと判定した範囲

- `xtask/src/acceptance/launch_specification/`(option_stack.rs・reserved_tests.rs)
- `xtask/src/acceptance/readback_hdr_image/`(invariant_tests.rs・load.rs・pixel_access.rs・test_support.rs)
- `xtask/src/acceptance/readback_image/`(invariant_tests.rs・load.rs・pixel_access.rs・test_support.rs)
- `xtask/src/acceptance/readback_size/parse_tests.rs`
- `xtask/src/acceptance/report_line/`(indexed_values.rs・value_by_key.rs)
- `xtask/src/acceptance/report_only_environment/reserved_tests.rs`
- `xtask/src/acceptance/run_environment/`(existing_dump.rs・single_run.rs)
- `xtask/src/acceptance/run_name/`(names_in_use_tests.rs・offending_character_tests.rs・validation.rs・validation_tests.rs)
- `xtask/src/acceptance/world_report_environment/reserved_integration_tests.rs`
- `xtask/src/asset_generator/`(arguments.rs・error.rs・inspection_files.rs・launch.rs・launch_command.rs・planting_count.rs・specification.rs・world_name.rs)
- `xtask/src/asset_generator/arguments/`(bake.rs・part_tools.rs・source_assets.rs・spelling.rs・word_list.rs)
- `xtask/src/asset_generator/launch/`(drop_guard.rs・terminate.rs)
- `xtask/src/asset_generator/world_name/`(round_trip_tests.rs・table.rs)

上記のうち「疑い」に挙げた3件を除き、識別子は受理条件8つ・名詞/動詞の品詞規律・目的語や着点の欠落・直訳調・膠着圧縮のいずれにも該当する明確な違反を確認しなかった。`cargo`・`glTF`・`RGBA8`・`HDR`のようなASCII語は、ツール名・ファイル形式名・広く通用する画像規格の略称として日本語識別子中に一貫して使われており(例: `毎回cargoに構築させて起動する`、`入力するglTFの契約を検査する`、`全てRGBA8の綴り`)、ギリシャ文字・数式変数風の記号(`A2`・`Σλ_n`・`n`)の混入は無かった。`cargo`はRustのビルドツールの固有名詞であり、非専門家にも一般名として通用する語(グローバルCLAUDE.mdの「専門用語の表記」が許す一般語彙に準じる)として扱い、違反とはしていない。
