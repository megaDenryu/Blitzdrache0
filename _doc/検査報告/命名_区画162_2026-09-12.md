# 命名検査報告: 区画162「xtask/src/shadow_loss/error」

- 区画番号: 162
- 区画名: xtask/src/shadow_loss/error(一覧の実体はshadow_loss・shadow_probe・sky_draw・sky_lutの4モジュール、39ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main、db815090時点。このツリーは編集していない)
- 直読したファイル数: 39/39(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 必読リスト: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(全文は404.8KBのため関連語をgrepで照合)

## 検査対象ファイル一覧(39件、すべて直読)

xtask/src/shadow_loss/error/display.rs、xtask/src/shadow_loss/guard/judgment_tests.rs、xtask/src/shadow_loss/guard/negative_control.rs、xtask/src/shadow_loss/range_world/distance_range_tests.rs、xtask/src/shadow_loss/region_count/counting_tests.rs、xtask/src/shadow_probe/assets.rs、xtask/src/shadow_probe/error.rs、xtask/src/shadow_probe/parse.rs、xtask/src/shadow_probe/plan.rs、xtask/src/shadow_probe/record.rs、xtask/src/shadow_probe/run.rs、xtask/src/shadow_probe/schedule.rs、xtask/src/shadow_probe/table.rs、xtask/src/shadow_probe/error/display.rs、xtask/src/shadow_probe/plan/axis.rs、xtask/src/shadow_probe/plan/axis_cases.rs、xtask/src/shadow_probe/plan/case.rs、xtask/src/shadow_probe/plan/measurement_world.rs、xtask/src/shadow_probe/plan/round_count.rs、xtask/src/sky_draw/error.rs、xtask/src/sky_draw/gpu_time.rs、xtask/src/sky_draw/gradient.rs、xtask/src/sky_draw/judgment.rs、xtask/src/sky_draw/run.rs、xtask/src/sky_draw/error/display.rs、xtask/src/sky_lut/error.rs、xtask/src/sky_lut/farthest_distance.rs、xtask/src/sky_lut/gpu_time.rs、xtask/src/sky_lut/indirect_pass_count.rs、xtask/src/sky_lut/judgment.rs、xtask/src/sky_lut/pass_count.rs、xtask/src/sky_lut/readback_size_match.rs、xtask/src/sky_lut/run.rs、xtask/src/sky_lut/series_integrity.rs、xtask/src/sky_lut/stop_point.rs、xtask/src/sky_lut/time_of_day_argument.rs、xtask/src/sky_lut/error/display.rs、xtask/src/sky_lut/gpu_time/interval_tests.rs、xtask/src/sky_lut/gpu_time/table.rs

## 総評

この区画は全体として命名の質が高い。型・関数・フィールドの大半が「これは何?」の受理テストへ単独で答えられ、判定名(`判定の名前`)は破れた前提を文で説明する形に統一されている。明確な受理条件違反は1件で、識別子へのASCII混入である。他に2件、断定を避けて疑いとして報告する。

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| xtask/src/sky_draw/run.rs:40, xtask/src/sky_lut/run.rs:43 | `アプリの起こし方::毎回cargoに構築させて起動する` | 識別子へのASCII混入禁止(オーナー実例の禁則。受理条件2の「英語識別子は読まれない」を裏から破る形) | 列挙子名の中に英字`cargo`がそのまま埋め込まれている。定義自体は本区画の外(`xtask/src/acceptance/app_executable.rs`、未検査領域)にあるが、本区画の2ファイルがこの列挙子を`アプリの起こし方::毎回cargoに構築させて起動する`という形でそのまま書いており、日本語の文の途中に英字の product 名が挟まる構造になっている | `毎回ビルドツールに構築させて起動する`(ツール名を出す必要があるなら「毎回Cargoで構築して起動する」ではなく意味で言い換える) |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 | 改名の素案 |
|---|---|---|---|---|
| xtask/src/shadow_probe/record.rs:11 | `高度度`(構造体`太陽の角度`のフィールド) | 受理条件1・7 | 「高度」という語自体が既に「度」で終わるため、単位を表す`度`をさらに続けると`高度度`という文字の連なりになり、初見では誤字(二重の度)に見える。同じ構造体の`方位度`は`方位`+`度`で衝突が無く問題にならないため、`高度度`だけが特異に読みにくい | `高度の度数`または`高度_度`ではなく`高度(度)`のような区切りが無理なら、単位を型で持たせて`高度`のみにする(角度の値オブジェクトが既にあるなら回付を検討) |
| xtask/src/sky_draw/run.rs:33-34 | `空ありポストなし`・`空なしポストなし`(列挙`条件`の枝) | 命名の質(略語・フルスペル) | 同じファイルのコメントは「ポスト処理」とフルスペルで説明しているのに、識別子側は`処理`を落として`ポスト`だけにしている。読み手が英語の"post"由来と知らなければ、なぜ`ポスト`だけで意味が通るのか単独では復元しにくい | `空ありポスト処理なし`・`空なしポスト処理なし` |

## 違反ゼロと確認した範囲

上記2件の疑いと1件の違反を除く、shadow_loss(5ファイル)・shadow_probe(14ファイル)・sky_draw(6ファイル)・sky_lut(14ファイル)の全識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)は受理条件8つ・命名の質・呼称と語の選択のいずれにも抵触しない。特に、この区画は判定名(`判定の名前::定数から生成する`/`組み立てた綴りから生成する`)に「何が・なぜ破れたか」を文で埋め込む統一様式を徹底しており、オーナーが不合格にした3類型(実体と合わない語・名詞1語の圧縮・詰め込みすぎ)に該当する識別子は見つからなかった。漢字1文字への圧縮(呼称と語の選択)も無い。
