# ローラー検査(命名の軸) 区画013

- 区画: `crates/blitz_app/src/reports`(区画番号013、対象38ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、`db815090`時点。編集していない)
- 直読したファイル数: 38 / 38(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

1. `crates/blitz_app/src/reports/gpu_time_table.rs:9` | `表示する`(`pub(crate) fn 表示する(表: &[...])`) | 条件2・条件3・条件5 | 呼び出し側は`crate::reports::gpu_time_table::表示する(&self.パス別gpu時間を取得する())`(`app/exit_report.rs:16`)というASCIIモジュール直下の完全修飾で呼ぶため、モジュール名は補完力ゼロであり「表示する」単独では「何を表示するか」が読めない。`create::生成する`型の裸の動詞と同型 | `パス別gpu時間の表を表示する`
2. `crates/blitz_app/src/reports/gpu_frame_samples.rs:12` | `表示する`(`pub(crate) fn 表示する(標本一覧: &[フレーム別の標本])`) | 条件2・条件3・条件5 | 呼び出し側は`crate::reports::gpu_frame_samples::表示する(...)`(`app/exit_report.rs:19`)。ASCIIモジュール名からの補完不可、「何を」が欠落 | `パス別gpu時間のフレーム別生値を表示する`
3. `crates/blitz_app/src/reports/streaming_summary.rs:6` | `表示する`(`pub(crate) fn 表示する(要約: &ストリーミング要約)`) | 条件2・条件3・条件5 | 呼び出し側は`streaming_summary::表示する(&要約)`(`app/exit_report.rs:91`)。ASCIIモジュール名からの補完不可 | `ストリーミング要約を表示する`
4. `crates/blitz_app/src/reports/auto_exposure.rs:20` | `報告する`(`pub(crate) fn 報告する(画像: &HDR読み戻し画像, 観測: &自動露出の観測, 探り色: Option<[f32; 3]>)`) | 条件2・条件3・条件5 | 呼び出し側は`crate::reports::auto_exposure::報告する(画像, &観測, 探り色)`(`app/frame_dump/hdr_dump.rs:49`)。ASCIIモジュール名からの補完不可、「何を」報告するのかが名前に無い | `自動露出の照合行を出す`
5. `crates/blitz_app/src/reports/local_visibility.rs:24` | `報告する`(`pub(crate) fn 報告する(形: 合成深度の形, 正本: &局所可視度画像, 一枚目: &局所可視度読み戻し画像, 二枚目: &局所可視度読み戻し画像)`) | 条件2・条件3・条件5 | 呼び出し側は`local_visibility::報告する(形, &正本, &一枚目, &二枚目)`(`app/draw_dispatch/local_visibility_check.rs:61`)。同型の裸の動詞 | `局所可視性の照合行を出す`
6. `crates/blitz_app/src/reports/cluster_assignment.rs:12` | `報告する`(`pub(crate) fn 報告する(統計: クラスタ選別の割り当て統計)`) | 条件2・条件3・条件5 | 呼び出し側は`crate::reports::cluster_assignment::報告する(統計)`(`app/frame_dump/cluster_assignment_check.rs:21`)。ASCIIモジュール名からの補完不可 | `クラスタ割り当て統計を出す`
7. `crates/blitz_app/src/reports/sky_pixel.rs:18` | `線形をsrgb8bit相当へ変換する`(`pub(crate) use srgb_encode::線形をsrgb8bit相当へ変換する;`) | 識別子への英字・記号の混入(受理条件の対象外形状) | 日本語の動詞句の中に`srgb8bit`という英字と数字の連結が助詞なしで埋め込まれ、単語境界が読み取れない。他ファイル(`reports/indirect_probe/row.rs:12`)からも同名でuseされ、この綴りのまま外部境界を越える | `線形色をsRGBの8ビット相当へ変換する`(定義本体は`srgb_encode.rs`にあり本区画の対象外だが、この綴りが`sky_pixel.rs`から再エクスポートされ他モジュールへ渡っている)
8. `crates/blitz_app/src/reports/display_timing.rs:9` | `実表示とみなす停止時間MS`(`const 実表示とみなす停止時間MS: f64 = 1.0;`) | 識別子への英字の混入 | 日本語の名詞句の末尾へ単位の英字略号`MS`がそのまま連結されており、日本語の中に英語の単位記号が挟まる形になっている。同ファイル内の`統計.p50ms`等は他クレート(`crate::app::フレーム時間統計`)の外部フィールドで本区画の定義対象外 | `実表示とみなす停止時間ミリ秒`

## 違反の疑い(断定しない)

1. `crates/blitz_app/src/reports/atmosphere_lut.rs:35`・`derived_environment.rs:39`・`distant_environment.rs:41`・`sky_state.rs:31`・`sun_zenith_crossing.rs:32` | 私有の`fn 報告する() -> Result<(), ...報告エラー>` | 条件3への疑い | いずれも引数ゼロでファイル内限定のprivate関数であり、直前直後で公開関数(`○○表を出す()`)から1回だけ呼ばれる。ASCIIモジュール名の補完は使えないが、呼び出しが同一ファイル内の隣接1箇所に閉じており、型名レシーバによる補完に相当する読み方が成立するとも読めるため断定を避ける。5ファイルにまったく同じ形で繰り返されている
2. `crates/blitz_app/src/reports/auto_exposure.rs:92` | `fn 添字(位置: usize) -> blitz_engine::auto_exposure::ビンの添字`(私有関数) | 条件4への疑い | 位置からビンの添字型を生成し失敗時はpanicする処理だが、名前が名詞「添字」のみで動詞句になっていない。値オブジェクト様式の関連関数(型名が目的語を担う)には該当しない自由関数であるため、品詞の規律に照らすと動詞形が必要な可能性がある | 改名案(参考): `位置から添字を作る`
3. `crates/blitz_app/src/reports/cloth_xpbd_reference.rs:31` | `comparison::突き合わせる(&読み戻し, &参照)` | 条件2・条件3への疑い | `突き合わせる`は動詞のみで「何と何を」が名前に無く、ASCIIモジュール`comparison`直下の呼び出しであるため補完力もゼロ。ただし定義本体は`reports/cloth_xpbd_reference/comparison.rs`にあり本区画(013)のファイル一覧には含まれないため、断定は次区画の直読へ委ねる
4. `crates/blitz_app/src/reports/temporal_reconstruction.rs:19,25,36` | `difference::求める(...)`・`digest::求める(...)` | 条件2・条件3への疑い | いずれも動詞のみで対象が名前に無く、ASCIIモジュール直下。定義本体(`difference.rs`・`digest.rs`)は`temporal_reconstruction/`配下のサブモジュールで本区画の一覧に含まれないため、断定は次区画の直読へ委ねる

## 違反ゼロの範囲

上記の指摘対象以外の識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は、目的語・着点・材料の格が揃い、値=名詞句/処理=動詞句の品詞規律を満たし、直訳調・膠着圧縮・実体との不一致・詰め込みすぎのいずれにも該当しなかった。特に`atmosphere_lut/`配下9ファイルの代表テクセル・統計・再現性まわりの命名(`代表行を出す`・`不一致数`・`数え上げる`・`有限な成分の最大値を求める`等)は目的語と動作が一貫して揃っており違反ゼロと判定した。
