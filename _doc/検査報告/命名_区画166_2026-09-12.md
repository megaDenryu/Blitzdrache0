# ローラー検査(命名) 区画166: xtask/src/type_metrics/rust_module ほか

- 区画番号: 166
- 区画名: xtask/src/type_metrics/rust_module (対象ファイル一覧は `xtask/src/verify/clean_build_cache`・`xtask/src/verify`・`xtask/src/verify/output`・`xtask/src/vertex_diag`・`xtask/src/village_draw`・`xtask/src/world_setup/error.rs` の36ファイルにまたがる)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、時点 db815090。このツリーは編集していない)
- 直読したファイル数: 36 / 36(全数直読。grepだけで済ませたファイルは無い)
- 検査観点: 日本語命名の受理条件8つ、日本語命名の質(頭字語・直訳調・膠着圧縮の禁止)、呼称と語の選択(漢字1文字への切り詰め禁止・カタカナ語の基準)、専門用語の表記(初出補足)。参照した用語定義: `_doc/計画/ユビキタス言語.md`の「検証の出力ルート」「検証の出力の置き場名」「検証の出力のファイル名」「型計測の破れ」「規約検査の破れ」「宣言の分量」「ローラー検査」の各項目。

## 直読したファイルの一覧

1. xtask/src/type_metrics/rust_module/module_directory.rs
2. xtask/src/type_metrics/rust_module/module_position.rs
3. xtask/src/verify/clean_build_cache/candidate.rs
4. xtask/src/verify/clean_build_cache/collect.rs
5. xtask/src/verify/clean_build_cache/error.rs
6. xtask/src/verify/clean_build_cache/mod.rs
7. xtask/src/verify/clean_build_cache/mode.rs
8. xtask/src/verify/clean_build_cache/occupied_size.rs
9. xtask/src/verify/clean_build_cache/role.rs
10. xtask/src/verify/clean_build_cache/subject.rs
11. xtask/src/verify/clean_build_cache/candidate/tests.rs
12. xtask/src/verify/log_error.rs
13. xtask/src/verify/log_file_name.rs
14. xtask/src/verify/log_place.rs
15. xtask/src/verify/sequence.rs
16. xtask/src/verify/tee.rs
17. xtask/src/verify/utc_moment.rs
18. xtask/src/verify/output/file_name.rs
19. xtask/src/verify/output/mod.rs
20. xtask/src/verify/output/name.rs
21. xtask/src/verify/output/root.rs
22. xtask/src/verify/output/spelling.rs
23. xtask/src/verify/sequence/closing_tests.rs
24. xtask/src/verify/tee/terminal_copy.rs
25. xtask/src/verify/tee/terminal_failure_tests.rs
26. xtask/src/verify/tee/terminal_failure_tests/test_doubles.rs
27. xtask/src/vertex_diag/count_check.rs
28. xtask/src/vertex_diag/error.rs
29. xtask/src/vertex_diag/pair.rs
30. xtask/src/vertex_diag/pixel_check.rs
31. xtask/src/vertex_diag/run.rs
32. xtask/src/vertex_diag/error/display.rs
33. xtask/src/village_draw/error.rs
34. xtask/src/village_draw/run.rs
35. xtask/src/village_draw/error/display.rs
36. xtask/src/world_setup/error.rs

## 違反の一覧

無し。36ファイルの全識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)を受理条件8つで当てたが、受理テスト・5W1Hの必要格・品詞の規律・裸の動詞の禁止・1語1義・直訳の検出のいずれにも落ちるものが無かった。オーナーが不合格にした実例の3形(実体と合わない語・名詞1語への圧縮・詰め込みすぎ)、および識別子への英字・ギリシャ文字・記号の混入も個別に照合したが、該当は無い(理由は次節)。

## 違反の疑い

1. `xtask/src/verify/output/root.rs:21` | `CODEXレビューのビルドの出力先の場所`(定数名) | 条件6(1語1義)に対する疑い | 同じ語「Codex」の綴りが本区画の中で3通りに揺れている(`role.rs`の列挙の枝は`Codexレビューのビルドの出力先`、`collect.rs`・`mod.rs`・`root.rs`のメソッド名は`codexレビューのビルドの出力先`、この定数だけ`CODEX`と全て大文字)。意味の衝突ではなく綴りの不統一であり、受理条件が問う「同じ語が2つの意味を持つ」には直接当たらないため違反でなく疑いとする。読み手が「大文字のCODEXは何か別のものを指すのか」と一瞬迷う余地はある | 改名の素案: 他の箇所と同じく`codexレビューのビルドの出力先の場所`へ小文字を揃える

## 識別子への英字・ギリシャ文字・記号の混入の当て方

以下はすべて識別子に現れる英字だが、いずれも固有名詞(ツール名・製品名)であり、「専門用語の表記」の節が許す「一般に通用している語」と同じ扱いとして合格と判定した(GPU・APIが日本語文中で許容されるのと同型)。実例の禁止対象(`A2`・`Σλ_n`のような、意味を持たない段階記号・ギリシャ文字の変数名)には該当しない。

- `cargo`(`xtask/src/verify/sequence.rs`の`cargo引数`、`xtask/src/type_metrics/rust_module/module_position.rs`のコメント中の`Cargo`等): Rustのビルドツールの固有名詞
- `codex`/`Codex`/`CODEX`(`xtask/src/verify/clean_build_cache/`配下、`xtask/src/verify/output/root.rs`): レビューに使う外部ツールの固有名詞。ユビキタス言語の「検証の出力ルート」の項目が「codexレビューのビルドの出力先」という綴りで既に登録している
- `git`(`xtask/src/verify/log_file_name.rs`の`gitから読む`等): バージョン管理ツールの固有名詞

これらは意味を持つ日本語識別子の中に埋め込まれているが、固有名詞であるため意訳できず、意訳の必要も無いと判断した。数式記号・段階記号の類はどのファイルにも見つからなかった。

## 検討したが違反と判定しなかった項目(参考記録)

- `xtask/src/vertex_diag/run.rs:23`と`xtask/src/village_draw/run.rs:23`の定数`一日内秒`: 「一日」「内」「秒」の3語を助詞なしで繋いだ複合語だが、「圏内」「時間内」と同じ「内」の用法で自然に読め、両ファイルで綴りが揃っている(1語1義を保っている)ため違反としなかった。ただし`crates/blitz_app/src/cli/time_args.rs`の登録語は「一日内時刻」であり、単位が違う別概念(時刻 vs 秒)として別の語を宛てること自体は妥当と判断した
- `xtask/src/vertex_diag/run.rs:22`の定数`カメラ俯角差分度`: `crates/blitz_app/src/cli/placement_args.rs`で確立済みの語「カメラ俯角差分」に単位を表す「度」を足した形であり、実体(`--camera-pitch`へ渡す度数の文字列)と語が一致するため違反としなかった

## 違反ゼロの範囲

区画166の全36ファイル(`xtask/src/type_metrics/rust_module/`・`xtask/src/verify/`(`clean_build_cache/`・`output/`・`sequence/`・`tee/`を含む)・`xtask/src/vertex_diag/`・`xtask/src/village_draw/`・`xtask/src/world_setup/error.rs`)について、日本語識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)の受理条件8つ、日本語命名の質、呼称と語の選択のいずれにも違反が無いことを確認した。省略した範囲は無い(全36ファイルを直読)。
