# 命名検査報告: 区画095

- 区画名: `crates/blitz_render/src/vulkan/atmosphere_lut`(大気のベイク済み画像の資源一式)
- 対象範囲: `block_095.txt`に列挙された39ファイル(base_resources・binding_set・composite_descriptor・descriptor_common・distant_skyview・draw_input・image・inputs・lut_extent・march_descriptor・medium_bytes・medium_uniform・mod・multiscatter_descriptor・pass・pipeline・probe・readback_buffer・sample_descriptor・transmittance_descriptorの各モジュールと、その子モジュール群)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(時点: mainのdb815090。このツリーは編集していない)
- 直読したファイル数: 39/39(全ファイルを直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(登録語との照合に使用)

## 違反の一覧

1. `crates/blitz_render/src/vulkan/atmosphere_lut/binding_set/create/descriptors.rs:40` | `作る`(関数名) | 条件2・条件5 | ASCIIモジュール`descriptors`直下の自由関数が裸の動詞のみで、名前単独で「何を作るか」に答えられない。受理条件が挙げる founding 例(`create::生成する`→`共有資源を確保する`)と全く同型の違反である。呼び出し元は`descriptors::作る(確保係, 基盤)`であり、モジュール名`descriptors`はASCIIのため読まれない前提に立つと「作る」だけが残る | `ディスクリプタ五点を作る`
2. `crates/blitz_render/src/vulkan/atmosphere_lut/binding_set/create/pipelines.rs:20` | `作る`(関数名) | 条件2・条件5 | 同上。ASCIIモジュール`pipelines`直下の自由関数が裸の「作る」のみで、5本のパイプラインを作ることが名前に無い | `5本の生成パイプラインを作る`
3. `crates/blitz_render/src/vulkan/atmosphere_lut/base_resources/images.rs:12` | `順に作る`(関数名) | 条件3 | 「順に」は副詞句であり目的語ではない。ASCIIモジュール`images`直下の自由関数だが、名前だけでは「ベイク済み画像を」作ることが分からない | `ベイク済み画像を順に作る`
4. `crates/blitz_render/src/vulkan/atmosphere_lut/probe/record.rs:103` | `登録する`(関数名) | 条件3・条件5 | ASCIIモジュール`record`直下の自由関数が裸の動詞のみ。引数`画像`があるにもかかわらず名前に目的語が無く、「何を登録するか」(グラフへの画像登録)が名前単独で読めない | `画像をグラフへ登録する`

## 違反の疑い

1. `crates/blitz_render/src/vulkan/atmosphere_lut/probe.rs:52` | `環境で焼く` | 条件3に対して疑い | 「環境で」は手段格であり目的語(何を焼くか=大気のベイク済み画像)が名前に無い。ASCIIモジュール`probe`直下の自由関数のため文脈補完も効かない。ただし同一ファイル内で完結する2行程度の私有分解関数であり、直後の呼び出し元`大気のベイク済み画像をgpuで焼いて読み戻す`が全体の意味を担っているため、断定はしない | `環境で大気のベイク済み画像を焼く`
2. `crates/blitz_render/src/vulkan/atmosphere_lut/probe.rs:64` | `一式で焼く` | 条件3に対して疑い | 同上の理由。「一式で」は手段格で目的語が無い | `一式で大気のベイク済み画像を焼く`
3. `crates/blitz_render/src/vulkan/atmosphere_lut/probe/record.rs:19` | `焼いて読み戻す` | 条件3に対して疑い | 2つの動詞のみで目的語が無く、「何を」焼いて読み戻すのかが名前単独では分からない(引数の`入力`・`受け皿`から読み取るしかない)。ただし`pub(super)`でモジュール内部の1呼び出し元しか持たない工程分割であることは酌量点 | `4枚のベイク済み画像を焼いて読み戻す`
4. `crates/blitz_render/src/vulkan/atmosphere_lut/composite_descriptor/binding.rs:64` | `書き込む` | 条件3に対して疑い | ASCIIモジュール`binding`直下の自由関数で目的語が無い。ただし深度用とボリューム用の2つの公開関数(`深度を書き込む`・`ボリュームを書き込む`)が共有する下請けであり、対象を特定の1つに決められない(意図的な汎化)という酌量がある | `descriptor_common`的な汎用名として維持するか、`位置指定で書き込む`
5. `crates/blitz_render/src/vulkan/atmosphere_lut/march_descriptor/binding.rs:46` | `書き込む` | 条件3に対して疑い | 同上のパターン。`束縛先`の4項目すべてを一括で書く関数で、対象は「束縛先」全体だが名前に出ていない | `束縛先を書き込む`
6. `crates/blitz_render/src/vulkan/atmosphere_lut/multiscatter_descriptor/binding.rs:41` | `書き込む` | 条件3に対して疑い | 同上のパターン | `束縛先を書き込む`
7. `crates/blitz_render/src/vulkan/atmosphere_lut/base_resources.rs:97` | `平面`(関数名) | 条件4に対して疑い | 処理(値を組み立てる自由関数)なのに名詞1語(「平面」)で品詞が動詞句になっていない。ただし返す値がまさに`大気のベイク済み画像の形::平面`であり、値コンストラクタとして値の名でそのまま呼ぶ書き方は本コードベースの他所(`.値()`等のアクセサ)にも見られ、断定はしない | `平面の形を作る`
8. `crates/blitz_render/src/vulkan/atmosphere_lut/medium_bytes.rs:47` | `係数の生値`(関数名) | 条件4に対して疑い | 変換処理(GPU境界で単位を剥がす関数)なのに名詞句のみで動詞句になっていない。同じファイル内の`バイト列にする`が動詞句(「〜にする」)で書かれているのと対称性が崩れている | `係数を生値へ開く`

## 違反ゼロと確認した範囲

上記に挙げた8件の疑いと4件の違反を除く、39ファイル中のその他すべての型名・関連関数(`生成する`・`破棄する`・`書き込む`のうち型のimplブロック内にあるもの)・フィールド名・変数名・定数・enumの枝・bool値(「〜か」)は、受理条件8条・日本語命名の質・呼称と語の選択のいずれにも違反を確認しなかった。特に次の点を確認した。

- `生成する()`・`破棄する()`は全箇所が型のimplブロック内(レシーバまたは型名`Self`が目的語を担う値オブジェクト様式)であり、条件5の例外に当たる
- ユビキタス言語に登録済みの語(計算の班・計算の発行・即時定数・受け皿・材料・大気のベイク済み画像等)をそのまま使っている箇所は1語1義を満たす
- 頭字語・直訳調(LUT・lookup等)・助詞省略の膠着圧縮は検出しなかった
- ASCII識別子(英字・ギリシャ文字・数学記号)を識別子へ混入した例は検出しなかった
- 「試験の許容差の当て方」型の実体との不一致(名前が実装の一部の条件しか表さない)は検出しなかった。`描画入力の材料`・`焼く条件`はいずれも定義側のコメントと実体(フィールド構成)が一致することを確認済み
