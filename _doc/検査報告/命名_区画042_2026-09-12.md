# 命名ローラー検査: 区画042 crates/blitz_collision/src/height_field/capsule_sweep

- 区画番号: 042
- 範囲: `crates/blitz_collision/src/height_field/capsule_sweep`(33ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`、時点: main の `db815090`(このツリーは編集していない)
- 直読したファイル数: 33/33(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 参照した必読リスト: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、および`_doc/計画/ユビキタス言語.md`(掃引・高さ場を掃引するカプセル・接触の特徴の候補・軸と特徴の最近接・掃引で動けた割合・掃引の水平の覆い・掃引の走査が調べた範囲 等の登録語と照合した)

## 直読したファイルの一覧

candidate.rs, candidate_dispatch.rs, capsule.rs, contact.rs, contact_candidate.rs, contact_direction.rs, contact_place.rs, contact_solver.rs, covering.rs, crossing.rs, error.rs, feature.rs, length_limit_arithmetic_tests.rs, radius_lower_limit.rs, radius_lower_limit_tests.rs, scanned_range.rs, solve_edge.rs, solve_edge_axis.rs, solve_face.rs, solve_vertex.rs, solver_geometry.rs, surface_triangle.rs, surface_triangle_inside.rs, sweep_end_tests.rs, sweep_feature_tests.rs, sweep_hit.rs, sweep_query.rs, sweep_query_cell.rs, sweep_query_fixture.rs, sweep_query_tests.rs, sweep_result.rs, sweep_start_tests.rs, triangle_answer.rs

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| sweep_query_fixture.rs:17 | `位置`(自由関数、`fn 位置(東のメートル: f64, 天頂のメートル: f64, 南のメートル: f64) -> 高さ場の格子原点からの位置`) | 条件4(品詞の規律)、条件5(裸の動詞/裸の名詞の禁止の趣旨) | この関数は処理(値を組み立てて返すファクトリー)であり動詞句であるべきだが、名前が名詞`位置`のみである。呼び出し箇所`位置(1.0, 3.0, 1.0)`は、フィールド参照や値そのものの略記に見え、処理呼び出しであることが名前だけから分からない。`型名::生成する()`の値オブジェクト様式(条件5の例外)にも当たらない。ASCIIモジュール`sweep_query_fixture`直下の自由関数であるため名前単独で答える必要があるが、答えられていない | `位置を組む` または `高さ場の格子原点からの位置を組む` |
| sweep_query_fixture.rs:21 | `変位`(自由関数、`fn 変位(東のメートル: f64, 天頂のメートル: f64, 南のメートル: f64) -> 高さ場の中の変位`) | 条件4(品詞の規律)、条件5 | 上と同じ理由。処理なのに名詞1語であり、値と処理の区別が名前から取れない | `変位を組む` または `高さ場の中の変位を組む` |

## 違反の疑い

なし。上記2件以外に、品詞の逆転・目的語の欠落・直訳調・1語1義違反・記号混入は見つからなかった。

## 違反ゼロの範囲

上記2件を除く全ファイル・全識別子(型名31種・関連関数`生成する`群・メソッド群・フィールド・列挙の枝・テスト関数名・ローカル変数を含む)は受理条件8つを満たした。特に以下を確認した。

- `接触の特徴の候補`・`軸と特徴の最近接`・`触れ始める候補`・`触れた場所`・`地表に触れた特徴`・`掃引の水平の覆い`・`掃引の走査が調べた範囲`・`三角形の中の接触`・`カプセルの掃引と高さ場の接触の結果`・`カプセルが地表に最初に触れる点`・`高さ場を掃引するカプセル`・`升目の地表の三角形`・`カプセルと三角形の接触の求解`・`高さ場のカプセルの掃引の問い合わせ`は、いずれも`_doc/計画/ユビキタス言語.md`の登録語(掃引・高さ場を掃引するカプセル・接触点集合・掃引で動けた割合 等の周辺語彙)と実体が一致しており、名詞1語への圧縮や実体と合わない語は無かった
- 関連関数`生成する()`はすべて型名前置(`型名::生成する`)であり、値オブジェクト様式の例外に該当するため合格(条件5)
- 真偽を返すメソッドはすべて`〜か`で終わる(`面へ向かう接触か`・`面の上の点が内側にあるか`)
- 識別子へのASCII英字・ギリシャ文字・数学記号の混入は無かった(`length_limit_arithmetic_tests.rs`の説明コメント中の「B」はコード識別子ではなく文章内の数式記号であり、識別子の検査対象外)
- 直訳調(record→記録、lookup→引く 等)の語は使われていなかった

省略した範囲: 無し。33ファイル全件を直読した。
