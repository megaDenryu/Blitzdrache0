# 命名検査報告 区画031

- 区画: `crates/blitz_asset_compiler/src/runtime_compilation/building_outline_catalog` を中心に27ファイル(実際は `building_outline_catalog`・`chunk_ledger`・`compilation`・`compile_target`・`source_kind`・`vegetation_definition` の各配下を含む)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(時点: main の `db815090`。このツリーは編集していない。本報告ファイルの新規作成のみ行った)
- 直読したファイル数: 27 / 27(全ファイル)。grepだけで済ませたファイル: 0件(全ファイルを直読した)

## 違反の一覧

該当なし(0件)。

## 違反の疑い

1. `crates/blitz_asset_compiler/src/runtime_compilation/source_kind/ground_grid_ledger.rs:22-23` | `ソース種別::Gltfシーン` / `ソース種別::寸法を合わせるGltfシーン` | 条件2(識別子へのASCII混入) | 「Gltf」というASCIIの略称が日本語識別子の中に混ざっている。LUT等と同様、日本の非専門家がそのまま読める語ではない可能性がある。ただし本区画の27ファイルにはこの列挙型 `ソース種別` 自体の定義が無く(定義は区画外の `runtime_compilation/source_kind.rs` にある)、ここでは列挙のパターンマッチとして参照されているだけである。定義そのものの検査と改名の要否判断は、`source_kind.rs` を含む別区画の検査に委ねる。改名の素案(参考、要再検討): `Gltfシーン`→`glTFシーン`程度の表記統一止まりか、`glTF読み込みシーン`のような意訳が要るかは定義側の文脈次第
2. `crates/blitz_asset_compiler/src/runtime_compilation/chunk_ledger/bake_options.rs:13` | `焼き方の指定<'指定>` | 条件3(必要格の充足) | 「焼き方」は「試験の許容差の当て方」の失格例と同じ「〜方」の形であり、単独では抽象的に見える。ただし定義を開くと、フィールド(世界名・同居植生個体数・テクスチャ格納方針・遠景地形・散布一覧・地表層テクスチャ集)がすべて「材料が同じでも生成物を変える設定値」という冒頭コメントの説明と一致しており、「当て方」の実例のように実体(適用先・倍率・判定条件のような雑多な束)と名前が乖離してはいない。このため違反とまでは断定せず疑いに留める

## 違反ゼロの範囲

上記2件の疑い以外、区画031の27ファイル全体(`building_outline_catalog`本体11ファイル・`outline_builder`配下6ファイル・`chunk_ledger`配下4ファイル・`compilation`配下3ファイル・`compile_target`配下1ファイル・`source_kind`配下1ファイル・`vegetation_definition`配下1ファイル)で、型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝を受理条件8つに当てた結果、明確な違反は検出しなかった。

検査の要点:
- 「実体と合わない語」の形(`試験の許容差の当て方`型): `建物の組み立ての由来`・`差し替えの候補`・`対象1件の仕上げ係`・`対象1件の仕上がり`・`焼き方の指定`はいずれも型定義・フィールドを開いて名前と実体の一致を確認した。乖離は見つからなかった
- 「名詞1語の圧縮」の形(`接触点集合の法線と接線の連立`型): 該当する裸の名詞1語での圧縮は見つからなかった。`焼き上がり`・`差し替えの候補`等はいずれも修飾語を伴い、何が焼き上がった/差し替えたかが型・フィールドの文脈から一意に読める
- 「詰め込みすぎ」の形(`許容差の内側の点の行を積まず右辺を零にする`型): 長い関数名(`建物を1件差し替えた組み直しを試す`・`外接箱が据えた全部品を包むことを確かめる`等)はいずれも実装と1対1で対応し、本質でない条件の混入は見つからなかった
- 裸の動詞の禁止(条件5): ASCIIモジュール直下の自由関数(`outline_builder.rs`・`bay_structure.rs`・`combined_box.rs`・`identity_expansion.rs`・`part_outlines.rs`・`acceptance.rs`・`declared_expectation.rs`)はすべて目的語を伴う完全な動詞句であり、`生成する`・`作成する`単独の名前は無かった。型に属する`生成する()`(`建物定義ID::生成する`・`植生定義ID::生成する`・`差し替えの候補::組み上がった材料から生成する`等)は型名または修飾語が目的語を担っており合格とした
- 1語1義(条件6): `許容メートル`という同名の定数が`outline_builder/acceptance.rs`と`outline_builder/bay_structure.rs`の2箇所に private スコープで別々に定義されているが、どちらも「単精度の丸め誤差を吸収する許容量(メートル)」という同じ意味で使われており、モジュール private のため衝突・混同の実害は無いと判断した(違反としては数えていない)
- 直訳調(条件7): `record`→`記録`、`context`→`文脈`、`resolve`→`解決`、`lookup`→`引く`に相当する直訳語は見つからなかった。`台帳へ記録する内容ハッシュ`の「記録する」は実際にその値が台帳へ記録される値であり、実装と一致している
