# ローラー検査(命名)区画021: fox_tour_world/source_writing 他 generate_source_assets 配下

- 区画: `crates/blitz_asset_compiler/examples/generate_source_assets/fox_tour_world/source_writing`(区画番号021、対象一覧は26ファイル、実体は`fox_tour_world`配下1件+周辺の姉妹ワールド25件)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, db815090時点)。ツリーは編集していない
- 時点: 2026-09-12
- 直読したファイル数: 26/26(全ファイルをRead toolで直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`

## 対象ファイル一覧(26件、全件直読)

1. fox_tour_world/source_writing/ground.rs
2. night_lights_world/height.rs
3. night_lights_world/obstacle.rs
4. night_lights_world/obstacle_gltf_json.rs
5. night_lights_world/obstacle_shape.rs
6. terrain_visual_world/distant_ground.rs
7. terrain_visual_world/geometry.rs
8. terrain_visual_world/gltf_json.rs
9. terrain_visual_world/height.rs
10. terrain_visual_world/pedestal_mesh.rs
11. terrain_visual_world/sample_bodies.rs
12. terrain_visual_world/solid_grid.rs
13. terrain_visual_world/sphere_mesh.rs
14. terrain_world/height.rs
15. texture_compression_world/geometry.rs
16. texture_compression_world/gltf_json.rs
17. texture_compression_world/gradient_texture.rs
18. texture_compression_world/noise_texture.rs
19. vegetation_world/geometry.rs
20. vegetation_world/gltf_json.rs
21. vegetation_world/stage_amount.rs
22. vegetation_world/subdivided.rs
23. vegetation_world/gltf_json/sections.rs
24. vegetation_world/subdivided/faces.rs
25. vegetation_world/subdivided/geometry.rs
26. village_world/height.rs

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| terrain_visual_world/gltf_json.rs:12 | `文書`(関数名。`fn 文書(連結: &連結した立体, バイナリファイル名: &str) -> String`) | 受理条件1(受理テスト)・4(品詞の規律) | 呼び出し先のモジュール`gltf_json`はASCIIで補完力ゼロ。「文書」は名詞1語であり、値を保持するフィールドのように読めてしまい、実体である「材質見本の立体のglTF文書を組み立てる処理」だと単独で答えられない。同じ区画内の`obstacle_gltf_json.rs`の`遮蔽物のgltf文書を組み立てる`、`texture_compression_world/gltf_json.rs`の`対照の板のgltf文書を作る`、`vegetation_world/gltf_json.rs`の`文書を作る`はいずれも動詞句であり、この2件だけが名詞1語で取り残されている | `材質見本の立体のgltf文書を組み立てる` |
| terrain_visual_world/distant_ground.rs:54 | `文書`(関数名。`fn 文書(バイナリファイル名: &str) -> String`) | 受理条件1・4 | 同上。モジュール`distant_ground`はASCII。「文書」だけでは何のglTF文書かも、値か処理かも単独で答えられない | `遠景地面のgltf文書を組み立てる` |
| terrain_visual_world/geometry.rs:26 | `組み立てる`(自由関数。`pub(super) fn 組み立てる() -> 連結した立体`) | 受理条件1・2・5(裸の動詞の禁止) | 呼び出し元(`terrain_visual_world.rs`、区画外)では`geometry::組み立てる()`と書かれる。`geometry`はASCIIモジュール名で補完力ゼロのため、この自由関数は名前単独で「これは何をする処理か」に答える必要があるが、「組み立てる」だけでは目的語(球と台座をどう連結した何を組み立てるのか)が無く、`create::生成する`型の裸の動詞と同型の違反である | `材質見本の立体を組み立てる` |
| vegetation_world/gltf_json.rs:12-16, 19-23 | `bufferview一覧`・`accessor一覧`・`mesh一覧`・`node一覧`・`node番号一覧`(ローカル変数) | 日本語命名の質(英語の技術用語を日本語へ未翻訳のまま混ぜる)・受理条件7(直訳の検出) | `bufferview`・`accessor`・`mesh`・`node`はglTF仕様固有の英語語であり、UI/CPU/APIのような一般に通用する語ではない。英語のglTF仕様を知らない読み手はこれらの単語だけから中身を復元できない。同じ区画内の`terrain_visual_world/gltf_json.rs`は同じ概念を`インデックスアクセサ一覧`のようにカタカナへ訳しており、この区画だけ未翻訳の生英語が残っている | `バッファビュー宣言一覧`・`アクセサ宣言一覧`・`メッシュ宣言一覧`・`ノード一覧`・`ノード番号一覧` |
| vegetation_world/gltf_json/sections.rs:13-14 | `bufferview宣言`・`accessor宣言`(`段の区間`のフィールド) | 日本語命名の質・受理条件7 | 同上。型のフィールド名にも未翻訳の生英語が使われている | `バッファビュー宣言`・`アクセサ宣言` |
| vegetation_world/gltf_json/sections.rs:52 | `mesh宣言を作る`(関数名) | 日本語命名の質・受理条件7 | 同上。`mesh`が未翻訳のまま残る | `メッシュ宣言を作る` |
| vegetation_world/gltf_json/sections.rs:24 | `uv区間`(ローカル変数。`let uv区間 = 接線区間 + 接線区間長;`) | 受理条件6(1語1義)・7(直訳の検出) | 同じ関数の同じ処理内で、同一概念(テクスチャ座標の区間)を19行目では`テクスチャ座標区間長`(翻訳済み)、24行目では`uv区間`(未翻訳の略語)と2つの語で呼んでおり、1つの概念に2つの名前が付いている。`uv`だけでは英語のglTF属性名(TEXCOORD)を知らないと復元できない | `テクスチャ座標区間の開始` |

## 違反の疑い

なし。上の7件はいずれも実体(定義・呼び出し規約・区画内の対比例)を照合したうえで違反と判定した。断定を避けるべき境界事例は見当たらなかった。

## 違反ゼロの範囲

上記7件を除く19ファイル(ground.rs、night_lights_world/height.rs・obstacle.rs・obstacle_gltf_json.rs・obstacle_shape.rs、terrain_visual_world/height.rs・pedestal_mesh.rs・sample_bodies.rs・solid_grid.rs・sphere_mesh.rs、terrain_world/height.rs、texture_compression_world/geometry.rs・gltf_json.rs・gradient_texture.rs・noise_texture.rs、vegetation_world/stage_amount.rs・subdivided.rs・subdivided/faces.rs・subdivided/geometry.rs、village_world/height.rs)は、型・関数・メソッド・フィールド・変数・定数の識別子を全数当たり、受理条件8つ・命名の質・呼称と語の選択のいずれにも抵触しなかった。

なお`添字x`・`添字z`・`u方向`・`v方向`・`u比率`・`v比率`のような単一のASCII文字を座標軸・テクスチャ座標の軸として使う識別子は、区画内の他ファイル(`night_lights_world/height.rs`の`添字x`・`添字z`等)で既に一貫して使われている数学記号としての慣習であり、違反としなかった。
