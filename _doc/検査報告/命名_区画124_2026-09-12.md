# 命名検査報告: 区画124(cone_breach_record / numeric_contract_record)

- 区画名: crates/blitz_sim/src/contact/scene/cone_breach_record および crates/blitz_sim/src/contact/scene/numeric_contract_record(子モジュール tolerance_dependence を含む)
- 範囲: block_124.txt に列挙された40ファイル(区画番号124)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 40 / 40(全数直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`

## 直読したファイルの一覧(40件)

cone_breach_record/{eigen_direction_readers,instrumented_substep,manifold_observation,mod,point_observation,record_line,recorded_case,tangential_system_observation}.rs、
numeric_contract_record/{breakdown_line,candidate_reading,case_construction,comparison_line,comparison_substep,diagnostic_case,divergence_pair,divergence_scan,double_precision_reading,holding_class,mod,oscillation_scan,single_precision_reading,sliding_normal_budget_record,sliding_normal_budget_sample,slope_boundary_table,substep_oscillation,tolerance_table,translation_table}.rs、
numeric_contract_record/tolerance_dependence/{active_set_agreement,anchor_displacement,case,direction_line,entry_reading,fidelity_tests,instrumented_substep,iteration_reading,mod,record_line,scene_variation,substep_record,summary}.rs

## 違反の一覧

### 1. 識別子へのASCII英字+数字の混入(受理条件5・呼称と語の選択条1に相当する圧縮)

- ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案
- `crates/blitz_sim/src/contact/scene/numeric_contract_record/tolerance_dependence/scene_variation.rs:29` | `段階A1の場面からの一つの変更`(enum型名) | 呼称と語の選択条1(漢字1文字圧縮と同型のアルファベット+数字による中国語的圧縮の禁止)、日本語命名の受理条件7(直訳・略記の禁止に準ずる符丁化) | 「A1」はIssue #59の作業上の通し番号(段階Aの1番目)であり、この型を単独で読む人には「段階Aとは何か」「1とは何番目の何を指すか」が復元できない。オーナーが不合格にした実例で名指しされた`段階A2の候補_…`と同じ「アルファベット+数字」の符丁パターンである。ユビキタス言語.mdには`段階A1の場面からの一つの変更`としてこの名のまま登録されているが、登録は現状の綴りの記録であって受理条件への適合を保証しない | `許容差依存診断の基準場面からの一つの変更`、または`第4段階の基準場面からの一つの変更`(段階の実体である「Issue #59第4段階(許容差依存の解消)の基準場面」を語で書く)
- 同一識別子の同一区画内での再出現(定義以外の参照。改名は上の1箇所を直せばLSPリネームで追従する): `case.rs:12,31,46,68`、`fidelity_tests.rs:9,21`、`mod.rs:31,45,51`

### 2. 実体と合わない名詞1語の圧縮(受理条件1・3。オーナー不合格実例と同一の綴り)

- `crates/blitz_sim/src/contact/scene/numeric_contract_record/case_construction.rs:13,22,34,50`、`diagnostic_case.rs:16,32,37`、`single_precision_reading.rs:10,24,33`、`tolerance_dependence/case.rs:15,52,61,62`、`tolerance_dependence/active_set_agreement.rs:24,37`、`tolerance_dependence/entry_reading.rs:45,48,61,84`、`tolerance_dependence/scene_variation.rs:16,42,45` | `試験の許容差の当て方`(型・フィールド・引数名) | 受理条件1(受理テスト)・3(5W1Hの必要格) | 「当て方」は会話では「どう適用するか」としか読めないが、実体は受理の倍率と接線の行への当て先(旧の形/候補(i)/本番の形)を束ねた設定であり、初見で「何を当てるのか・どこへ効くのか」を復元できない。ユビキタス言語.md 1104行が定義するとおり、この語は`_doc/検査報告`の対象外文書ではなくCLAUDE.mdの「オーナーが不合格にした実例」に名指しで挙げられた綴りそのものである。定義側の型(`normal_tangential_system`モジュール)は本区画の40ファイルに含まれないため改名は当該モジュールで行う必要があるが、本区画内の20箇所超がこの名を読み込み・使用しており、区画の識別子検査としてここに記録する | `試験の許容差の適用規則`(オーナー指定の改名先)

### 3. 列挙の枝への詰め込みすぎ(受理条件3・8。オーナー不合格実例と同一の綴り)

- `crates/blitz_sim/src/contact/scene/numeric_contract_record/tolerance_dependence/case.rs:73`、`fidelity_tests.rs:20` | `接線の行への許容差の当て方::許容差の内側の点の行を積まず右辺を零にする`(enum枝) | 受理条件3(5W1Hの必要格)・8(長さは結果であって圧縮の言い訳にならない) | 名前に「右辺を零にする」という実装の帳簿上の扱いまで詰め込んでいるが、この枝の本質は「接線変位が許容差以下の点については拘束行を追加しない」ことであり、右辺の扱いは本質でない付随事項である。CLAUDE.mdの「オーナーが不合格にした実例」に完全一致で挙げられている綴りである。定義側のenum(`normal_tangential_system`モジュール)は本区画外だが、本区画内で5箇所以上参照・分岐している | `許容差以下の接線変位の拘束行を追加しない`(オーナー指定の改名先)

## 違反の疑い(条件1・3に対して、断定しない)

- `crates/blitz_sim/src/contact/scene/numeric_contract_record/candidate_reading.rs:20` | `六つの量から組む`(関連関数名) | 条件1(受理テスト)・3(5W1Hの必要格) | 「六つの量」は個数だけを述べ、何の量かを名前が答えない。読み手は型`粘着の候補の読み取り`のフィールド定義まで遡って初めて中身(結末の綴り・法線の拘束違反・接線変位・法線の乗数の合計・接線の合力の大きさ・円錐の内側か)を知る。型名`粘着の候補の読み取り::六つの量から組む`という形で型名がある程度目的語を補うため確定の不合格とはしないが、個数詞だけの命名は再発しやすいパターンであり注記する | 例: `読み取った六量から組む`ではなく`結末と乗数と拘束違反から組む`のように主要な格を1つでも名前へ出す案が考えられる(確定はリネーム員判断)

## 違反ゼロと確認した範囲

- 上記2件の定義箇所(`試験の許容差の当て方`本体、`接線の行への許容差の当て方`本体)を除く、40ファイル中の残り大部分の識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名)は受理条件8項目を通過した。特に、Greek文字(λ, Σ, Δ, β, δ 等)は本区画内で一貫してコメント・println!書式文字列の中だけに現れ、Rustの実識別子(変数・フィールド・関数名)としては1件も使われていないことをgrepで確認した(識別子のASCII/ギリシャ文字混入は上記1件を除きゼロ)。
- 「記録」という語は本区画内で終始「Issue #59の計器・診断が細分ごとに読み取って綴る計測記録」という単一の意味だけに使われており、他クレート(blitz_render)で過去に問題となった「GPU命令の積み込み」の意味との衝突は本区画には存在しない(1語1義は充足)。
- 直訳調(record→記録、context→文脈、resolve→解決、lookup→引く 等)に該当する識別子は本区画に見つからなかった。
- 助詞を省いた膠着圧縮(「大気LUT生成指示」型)は見つからなかった。長い名前(`単精度の許容差を据え置いた倍精度`等)はいずれも格・助詞を保持したままの自然な句であり、条件8(長さは結果)の違反ではない。

## 直読数の内訳(省略の理由)

40ファイルすべてを全文直読した。省略した範囲は無い(パターンが一貫しているという理由での省略はしていない)。cargoは実行していない(grep・catのみで検査した)。
