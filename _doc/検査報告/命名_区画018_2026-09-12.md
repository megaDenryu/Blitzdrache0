# 命名検査報告 区画018

- 区画名: crates/blitz_assembly/src/expansion/frame_tests および crates/blitz_assembly/src/joint（区画番号018）
- 範囲: block_018.txt に列挙された34ファイル
- 作業ツリー: C:/devs/Blitzdrache0_roller（main の db815090。このツリーは編集していない）
- 時点: 2026-09-12
- 直読したファイル数: 34/34（grepだけで済ませたファイルは0件。全ファイルをReadツールで全文読んだ）
- 必読リストの参照箇所: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`「部品と接合の語彙」節（404行以降）

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_assembly/src/expansion/frame_tests/frame_placement_tests.rs:74 | `四分の一`（`let 四分の一 = std::f32::consts::FRAC_PI_4.sin();`） | 条件1（受理テスト）、実体と合わない語（オーナー不合格実例(a)と同型） | 「四分の一」は日本語話者が読めば分数1/4すなわち数値0.25を意味する語である。しかしこの変数が保持するのは`sin(45°)≈0.7071`であり、90度回転（全周の4分の1回転）を表すクォータニオンの実部・虚部の大きさである。「四分の一」という名だけを読んだ読み手は値を0.25だと誤解する。同じ関数内のテスト名`四面の壁が四分の一回転ずつ違う向きへ立つ`は「回転」を伴っているため正しく「4分の1回転」を指すが、変数名だけは「回転」の語を落としており、名前が実体（回転成分の大きさ）とずれている | `四分の一回転の成分`または`四分の一回転のsin成分` |
| crates/blitz_assembly/src/expansion/frame_tests/frame_rule_tests.rs:18 | `件数を数える`（`fn 件数を数える(配置表: &部品ごとの配置表, 綴り: &str) -> usize`） | 条件1（受理テスト）、条件3（5W1Hの必要格） | ASCIIモジュール直下の自由関数であるため名前単独で答える必要があるが、「件数を数える」は「件数という名の何かを数える」という同語反復であり、「何の件数か」（配置表の中で指定した綴りに一致する部品の件数）を全く含まない。単独で読むと対象不明の汎用カウンタにしか見えない | `綴りに一致する配置の件数を数える` |

## 違反の疑い

なし（上記2件はいずれも「これは何?」に単独で答えられない実体不一致であり、断定して違反へ計上した）

## 違反ゼロの範囲

上記2件を除く32ファイルの識別子（型・関数・メソッド・フィールド・変数・定数・列挙の枝。数百件規模、`接合点`・`接合種別`・`接合面の姿勢`・`法線方向のずらし`・`正で有限な長さ`・`一間四方の骨格の規則`・`骨格へ継ぎ足すもの`・`はめ口へ入れるもの`・`壁の外面の飾り`・`煙突の接合点一覧`等）は受理条件8つを満たす。特に確認した点:

- ASCII識別子（頭字語・略語）の混入は0件。ギリシャ文字・数学記号・裸の英字変数の混入も0件（`joint_kind.rs`の`接合種別`列挙21枝を含め全数を目視）
- `接合点::生成する()`・`接合面の姿勢::生成する()`・`正で有限な長さ::生成する()`等の関連関数`生成する`はすべて型名が目的語を担う値オブジェクト様式であり合格
- `骨格`は単独識別子として使わない、というユビキタス言語412行の禁止（スケルタルアニメーションの「ジョイント」との1語1義の衝突回避）に反する裸の`骨格`識別子は0件。すべて`骨格の部品`・`骨格へ張る床`・`骨格を継ぐ`のように修飾された形で使われている
- `幹頂部`・`幹基部`・`枝の生え口`・`枝の根元`・`頂芽`・`葉房の根元`はユビキタス言語410行・427行・429行に登録済みの木の部品語彙と一致し、直訳調・膠着圧縮も無い
- `一致`・`包含`・`受け入れる側`・`差し込む側`・`未定`はユビキタス言語421・423行の接合の様式の語彙と一致
- 詰め込みすぎ（オーナー不合格実例(c)）に該当する枝は無し。`骨格へ継ぎ足すもの::骨格を継ぐ { 自分の接合点, 床, はめ口一覧 }`等の複数フィールド枝も、各フィールド名が実体（接合点・床の有無・はめ口の指定一覧）とそのまま対応しており圧縮による意味破壊は無い
- 名詞1語への圧縮（オーナー不合格実例(b)）に該当する識別子は上記「四分の一」を除き検出せず

