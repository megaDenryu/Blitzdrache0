# 命名検査報告 区画006

## 区画名と範囲
- 区画: `crates/blitz_app/src/app/time_of_day`(区画番号006、37ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(このツリーは編集していない)
- 時点: main の `db815090`

## 直読したファイル数とgrepだけで済ませたファイル数
- 直読: 37 / 37(全ファイルを直読した。grepだけで済ませたファイルは0件)

## 検査の前提
`.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」(受理条件8つ)・「日本語命名の質」・「呼称と語の選択」・「専門用語の表記」と、`CLAUDE.md`の「命名」節、`_doc/計画/ユビキタス言語.md`を読んでから検査した。ユビキタス言語には本区画固有の語(空中遠近・遠方環境・間引き・段差走査等)の定義が「オープンワールドの語彙」「時間の規律の語彙」節に分散して登録されている。

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_app/src/app/time_of_day/wiring/create.rs:29` | `生成する`(モジュール直下の自由関数) | 条件5(裸の動詞の禁止) | ASCIIモジュール`create`直下の自由関数であり、呼び出し側`create::生成する(材料)`のモジュール名は英語で読まれない前提のため、名前単独では「何を生成するか」が答えられない。受理条件の例示`create::生成する`とまったく同じ形である | `天空配線を生成する` |
| `crates/blitz_app/src/app/time_of_day/aerial_input.rs:20` | `組む`(モジュール直下の自由関数) | 条件5 | ASCIIモジュール`aerial_input`直下の自由関数。呼び出し側は`aerial_input::組む(...)`であり、モジュール名は補完力を持たないため「何を組むか」が名前に無い | `空中遠近の入力を組む` |
| `crates/blitz_app/src/app/time_of_day/atmosphere_input.rs:31` | `組む`(モジュール直下の自由関数) | 条件5 | 同上。ASCIIモジュール`atmosphere_input`直下で、呼び出し側`atmosphere_input::組む(...)`のモジュール名は補完力ゼロ | `大気入力を組む` |
| `crates/blitz_app/src/app/time_of_day/distant_environment_input.rs:31` | `組む`(モジュール直下の自由関数) | 条件5 | 同上。ASCIIモジュール`distant_environment_input`直下で、呼び出し側`distant_environment_input::組む(...)`のモジュール名は補完力ゼロ | `遠方環境入力を組む` |
| `crates/blitz_app/src/app/time_of_day/step_scan.rs:66` | `積む`(モジュール直下の自由関数) | 条件5・条件3 | ASCIIモジュール`step_scan`直下の自由関数であり、何を(撮影を)どこへ(撮影一覧へ)積むかが名前に無い。引数を読んで初めて分かる | `撮影を撮影一覧へ積む` |
| `crates/blitz_app/src/app/time_of_day/wiring.rs:40` | `空を描く`(struct `天空配線`のbool型フィールド) | 条件4(品詞の規律) | 真偽値のフィールドなのに動詞句のままで「か」が無く、「(これから)空を描け」という命令・宣言文に読める。同じ型が公開する読み出しメソッドは`空を描くか`であり、フィールドと公開APIで語尾が食い違っている | `空を描くか` |
| `crates/blitz_app/src/app/time_of_day/wiring.rs:52` と `wiring/create.rs:26` | `明示境界を使わない`(bool型フィールド、2箇所) | 条件4 | 真偽値なのに「〜ない」という否定の宣言文で終わり「か」が無い。「使うな」という指示文にも読める | `明示境界を使わないか` |
| `crates/blitz_app/src/app/time_of_day/sky_input.rs:32` | `確定値`(ジェネリックな内部関数) | 条件4 | 名詞形だが、実体はResultをunwrapし、失敗時にpanicで停止する処理(副作用を持つ判定処理)である。値を運ぶだけの名詞に見えて、実際は「不変条件を検査して確定させる」という動作を隠している | `不変条件を確認して取り出す` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `crates/blitz_app/src/app/time_of_day/wiring/band_decision.rs:18` | `世界級の遠景がある`(bool引数) | 条件4 | 「〜がある」は述語として真偽が読み取れるため実害は小さいが、この区画の他のbool述語(`空パスを積むか`等)はすべて「か」で終わっており、語尾の様式が1箇所だけ違う |
| `crates/blitz_app/src/app/time_of_day/sun_disk_override.rs:32` | `円盤を消した空描画方針を作る`内の`円盤` | 条件7(直訳・省略検出) | ファイル内の文脈(太陽円盤指定)からは「太陽円盤」だと分かるが、関数名だけを単独で読むと「円盤」が何の円盤か(点光源の影の立方体等、他の文脈の「円」概念と)取り違えうる。実害は同一ファイル内で完結するため小さい |

## 違反ゼロの範囲

上記8件の違反と2件の疑いを除く、区画内の残り全識別子(型名・enum・struct全フィールド・受理条件を満たすメソッド名・変数名。具体的には`aerial_distance_record.rs`・`aerial_frustum.rs`・`atmosphere_update.rs`・`atmosphere_update_tests.rs`・`bake_commit_tests.rs`・`clock.rs`・`distant_environment_key_record.rs`・`distant_environment_update.rs`・`distant_environment_update_tests.rs`・`exposure_elapsed.rs`・`indirect_lighting_chain_tests.rs`・`interval_override.rs`・`reproduction_condition.rs`・`scene_policy.rs`・`scene_policy_tests.rs`・`sun_zenith_interval_record.rs`・`thinning.rs`・`thinning_commit_tests.rs`・`scene_policy/sky_pass_choice.rs`・`scene_policy/world_class.rs`・`scene_policy_tests/supply_tests.rs`・`step_scan/range.rs`・`step_scan/shot.rs`・`wiring/bake_input.rs`・`wiring/band_decision.rs`(件名の対象を除く)・`wiring/clock_advance.rs`・`wiring/exposure.rs`・`wiring/scan_override.rs`の全識別子)は受理条件8つに照らして違反を検出しなかった。ASCII文字・ギリシャ文字・数学記号の識別子への混入は本区画のどのファイルにも無かった。省略した検査対象は無い(全37ファイルを直読で検査した)。
