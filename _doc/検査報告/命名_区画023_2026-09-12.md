# 命名検査報告: 区画023

- 区画名と範囲: `crates/blitz_asset_compiler/src`(区画番号023、一覧ファイル`block_023.txt`が指す38ファイル)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の db815090 時点。このツリーは編集していない)
- 直読したファイル数: 38 / 38(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 参照した必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`(該当語の照合。ファイルサイズが大きいため`アセットコンパイラ`関連の見出しをgrepで抽出して照合した)

## 直読したファイル一覧(38件)

assembled_scatter.rs / assembled_scene.rs / assembly_parts.rs / assembly_reference.rs /
asset_layout.rs / atomic_file_write.rs / building_grid_source.rs / chunk_directory_source.rs /
compile.rs / distant_terrain.rs / distant_terrain_bake.rs / edge_agreement.rs / editor_chunk.rs /
error.rs / fixed_placement.rs / generation_ledger.rs / height_field.rs / height_grid.rs /
lattice_noise.rs / lib.rs / nonnegative_floor.rs / part_row.rs / placed_instance_count.rs /
placement.rs / scatter_bake.rs / scene_compiler.rs / source_asset_paths.rs / static_physics.rs /
surface_material.rs / terrain.rs / texture_storage.rs / uniform_scale.rs / vegetation.rs /
village.rs / visual_sample.rs /
assembled_scatter/error.rs / assembled_scatter/expansion_run.rs / assembled_scatter/groups.rs

## 違反の一覧

該当なし(0件)。

## 違反の疑い

該当なし(0件)。検査対象の識別子はいずれも次を満たしていた。

- 型・関数・メソッド・フィールド・変数・定数・列挙の枝の名前が単独で「これは何?」に答えられる
  (例: `置いた個体の数`・`固定物の据え付け`・`部品で組んだ散布のコンパイルエラー`・`ソースアセットのコンパイル係`)
- 処理は動詞句、値は名詞句、真偽は「〜か」の品詞規律を守っている(`焼くか`・`足す`・`引く`は
  すべて型に属する`impl`のメソッドで、レシーバが目的語を担うため裸の動詞の禁止に抵触しない)
- ASCIIモジュール直下の自由関数(`lattice_noise.rs`の`整数を混ぜる`・`格子雑音の値`、
  `nonnegative_floor.rs`の`負でない床をu16にする`等)も、名前単独で「何を・どうする」が読める
- 英語の頭字語・直訳調・助詞省略の膠着圧縮は見られない。LUT等の頭字語は本区画に出現しない
- 識別子へのASCII英字・ギリシャ文字・数学記号の混入は無い(型名・定数名はすべて日本語で完結し、
  ASCIIが現れるのは`u16`・`u64`・`PathBuf`等の型・外部クレート由来の語のみで、識別子の中への混入ではない)
- 「実体と合わない語」「名詞1語の圧縮」「詰め込みすぎ」の3形も検出しなかった。例えば
  `寸法を合わせる一様倍率`・`部品で組んだ並びのコンパイルエラー`のように長い名前が複数あるが、
  いずれも定義(冒頭の`//!`コメントおよび構造体のフィールド)と語の対応が取れている

## 違反ゼロの範囲

`crates/blitz_asset_compiler/src`配下の38ファイル全域(直読38/38)で、日本語命名の受理条件8条・
命名の質・呼称と語の選択のいずれにも違反する識別子を検出しなかった。省略した範囲は無い。
