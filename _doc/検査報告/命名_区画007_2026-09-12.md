# ローラー検査(命名の軸) 区画007

- 区画名と範囲: `crates/blitz_app/src/app/time_step`・`crates/blitz_app/src/app/visibility`・`crates/blitz_app/src/app/window_setup`・`crates/blitz_app/src` 直下の一部(atmosphere_medium・cli・embedded_*・error・game・main・reports・runtime_assets)
- 対象の作業ツリー: `C:/devs/Blitzdrache0_roller`(main の `db815090` 時点。このツリーは編集していない)
- 依頼書が「35ファイル」と述べていたのに対し、実際に受け取った `block_007.txt` は34件だった(改行の数え違いの可能性がある)。検査は受け取った34件全部を対象にし、この件数差そのものを疑いとして記録する(「検査器は解析できなかった入力を黙って対象から外してはならない」に倣い、差分を無言で握り潰さない)
- 直読したファイル数: 34/34(全件を`Read`で直読。grepだけで済ませたファイルは0件)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_app/src/app/window_setup/window_create.rs:12` (呼び出し側 `crates/blitz_app/src/app/window_setup.rs:32`) | `window_create::生成する` | 受理条件2(ASCIIモジュール直下の自由関数は名前単独で答える)・条件5(裸の動詞の禁止) | `window_create`はASCII名のモジュールで、中身は`生成する()`という関数1つだけである。定義そのものが挙げる不合格実例`create::生成する`と同型であり、呼び出し側`window_create::生成する(event_loop)`を読んでも「何を生成するか」が分からない(型名やレシーバによる日本語の補完が無い) | `ウィンドウを生成する`(モジュール名はASCIIのままでよいが、関数名に目的語を持たせる) |
| `crates/blitz_app/src/main.rs:50` | `実行する`(`fn 実行する() -> Result<ExitCode, 起動エラー>`、`main.rs`はクレート起点でASCII名の位置づけ) | 受理条件2・3(5W1Hの必要格)・5(裸の動詞の禁止。定義が挙げる禁止語のリストに`実行する`が名指しされている) | クレート起点の`main()`から`実行する()`と呼ぶだけで、CLI引数を解析して起動要求へ分岐する処理だと復元できない。「これは何?」への答えが「実行する処理です」以上に進まない | `起動要求を解析して実行する` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `crates/blitz_app/src/main.rs:63` | `描画する`(`fn 描画する(起動設定: cli::起動設定) -> Result<ExitCode, 起動エラー>`) | 条件2・3 | クレート起点の私有関数で、名前だけでは「起動設定を使ってウィンドウとイベントループとアプリを起こし描画ループへ入る」ことまで復元できない。ただし引数の型`cli::起動設定`が対象を示しており、`実行する`ほど空虚ではないため断定しない |
| `crates/blitz_app/src/app/visibility/judgment_range.rs:47` | `配列にする`(`fn 配列にする(一覧: Vec<...>) -> [...; 距離区分数]`) | 条件3(5W1Hの必要格。「何を」配列にするかが名前に無い) | 呼び出し側`配列にする(距離区分ごとの行列)`の引数名は補完できる文脈(型名の関連関数・レシーバ)に当たらないため、名前だけでは材料が復元できない。1箇所だけで使う短い私有ヘルパーであり実害は小さい |
| `crates/blitz_app/src/app/visibility/settings.rs:12` | `ヒステリシス幅メートル`(定数) | 専門用語の表記(初出補足)・呼称と語の選択(カタカナ語は小学生が分かるか) | 「ヒステリシス」は物理・工学の専門語であり、`_doc/計画/ユビキタス言語.md`に登録が見当たらない。ただしリポジトリ全体で37ファイルが同じ語を使っており(`terrain_lod`・`instance_lod`系等)、このファイル固有の新規造語ではなく既存の確立した語彙であるため、登録漏れの疑いとして留め、改名までは求めない |
| `crates/blitz_app/src/reports.rs:54-59` | `統計.フェンス待機ms`・`統計.フレームデータ準備ms`等(フィールドアクセス。定義側は本区画外の`blitz_render::CPU区間時間`・`crate::app::フレーム時間統計`) | 命名の質・「略語禁止、フルスペル」 | `ms`は`ミリ秒`の略記であり、プロジェクトの命名規約は略語を禁止している。ただし該当フィールドの定義は本区画に含まれる34ファイルの外にあり(`app/frame_timing.rs`等)、この区画からは使用側しか見えない。同型の`ms`接尾辞はリポジトリ全体で14ファイル33箇所に及ぶ既存の広範な慣行であり、この区画だけの是正では閉じないため、定義側を含む区画の検査へ申し送る |

## 違反ゼロの範囲

- `crates/blitz_app/src/app/time_step/`(9ファイル: `accumulator.rs`・`accumulator_tests.rs`・`advance.rs`・`advance_tests.rs`・`draw_advance.rs`・`interpolation_ratio.rs`・`interpolation_ratio_tests.rs`・`policy.rs`・`tick_count.rs`)は、型・メソッド・フィールドの名前がすべて「何を+どうする」を満たし、値オブジェクト様式の`生成する()`もレシーバ型が目的語を担っているため受理条件を満たす。違反ゼロ
- `crates/blitz_app/src/app/visibility/`の`frame_select.rs`・`registration.rs`・`registry.rs`・`tray.rs`は、`window_create.rs`と同型の指摘対象は無かった。違反ゼロ(`judgment_range.rs`・`settings.rs`の2ファイルにのみ上表の疑いがある)
- `crates/blitz_app/src/atmosphere_medium.rs`・`atmosphere_medium_tests.rs`・`cli.rs`・`error.rs`・`game.rs`・`runtime_assets.rs`・`embedded_*.rs`(9ファイル)は、確認した全識別子が受理条件を満たした。`cli.rs`が再輸出する`XPBD`・`LOD`はユビキタス言語に登録済みの確立語であり質の違反に当たらない。違反ゼロ
