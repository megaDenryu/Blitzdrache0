# 命名検査報告 区画012

- 区画: `crates/blitz_app/src/hot_reload`(区画番号012、39ファイル)。範囲は入力層(`input/`)と開発用UI重畳層(`overlay_ui/`)も含む(block_012.txtの列挙どおり)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、mainのdb815090
- 直読したファイル数: 39/39(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 検査基準: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

1. `crates/blitz_app/src/hot_reload/mtime.rs:6` | `取得する(パス: &Path) -> std::io::Result<SystemTime>` | 条件1・2・3・5 | ASCIIモジュール`mtime`直下の自由関数であり補完力ゼロ。「取得する」だけでは何を取得するのか(最終更新時刻)が名前に無く、`create::生成する`と同型の裸の動詞である | `最終更新時刻を取得する`
2. `crates/blitz_app/src/hot_reload/slangc.rs:24` | `発見する() -> Result<スランガー位置, ...>` | 条件1・2・3・5 | ASCIIモジュール`slangc`直下の自由関数で補完力ゼロ。「発見する」単独では何を発見するのか(slangcの実行位置)が復元できない | `スランガーの実行位置を発見する`
3. `crates/blitz_app/src/input/confirm.rs:18` | `確定する(状態: &mut 入力状態) -> カメラインテント` | 条件1・2・3・5 | ASCIIモジュール`confirm`直下の自由関数で補完力ゼロ。`状態`は第一引数でありレシーバではないため型による補完も働かない。「確定する」単独では何を確定するのか(蓄積からカメラインテントを確定する)が分からない | `蓄積からカメラインテントを確定する`
4. `crates/blitz_app/src/input/ingest.rs:13` | `取り込む(状態: &mut 入力状態, event: &WindowEvent)` | 条件1・2・3・5 | ASCIIモジュール`ingest`直下の自由関数で補完力ゼロ。「取り込む」単独では何を取り込むのか(winitのウィンドウ事象)が分からない | `winit事象を入力状態へ反映する`
5. `crates/blitz_app/src/overlay_ui/mesh_convert.rs:7` | `変換する(一覧: &[egui::ClippedPrimitive], pixels_per_point: f32) -> blitz_render::UI描画データ` | 条件1・2・3・5 | ASCIIモジュール`mesh_convert`直下の自由関数で補完力ゼロ。「変換する」単独では何から何への変換か(egui図形一覧からUI描画データ)が分からない。同ファイル内の私有関数`メッシュへ変換する`は着点を持ち合格だが、この公開の入口だけ裸である | `図形一覧をUI描画データへ変換する`
6. `crates/blitz_app/src/overlay_ui/scissor_convert.rs:6` | `変換する(clip_rect: egui::Rect, pixels_per_point: f32) -> blitz_render::UIシザー矩形px` | 条件1・2・3・5 | ASCIIモジュール`scissor_convert`直下の自由関数で補完力ゼロ。「変換する」単独では何を変換するのか(eguiのクリップ矩形)が分からない | `クリップ矩形をUIシザー矩形pxへ変換する`
7. `crates/blitz_app/src/overlay_ui/texture_id_map.rs:4` | `変換する(id: egui::TextureId) -> blitz_render::UIテクスチャID` | 条件1・2・3・5 | ASCIIモジュール`texture_id_map`直下の自由関数で補完力ゼロ。「変換する」単独では何を変換するのか(eguiのテクスチャID)が分からない | `eguiのテクスチャIDをUIテクスチャIDへ変換する`
8. `crates/blitz_app/src/overlay_ui/texture_sync.rs:7` | `反映する(レンダラー: &mut ..., デルタ: &egui::TexturesDelta, ミラー: &mut テクスチャミラー) -> Result<(), 起動エラー>` | 条件1・2・3・5 | ASCIIモジュール`texture_sync`直下の自由関数で補完力ゼロ。「反映する」単独では何をどこへ反映するのか(eguiのテクスチャ差分をレンダラーへ)が分からない | `テクスチャ差分をレンダラーへ反映する`
9. `crates/blitz_app/src/overlay_ui/stats.rs:6` | フィールド`validation件数: u64` | 日本語命名の質(頭字語・省略語の非翻訳) | 英語「validation」がそのまま日本語識別子へ混入している。Vulkanの検証層に関する専門語であり、UI/CPU/GPU/APIのような一般周知語ではないため、非専門家が読めない | `検証エラー警告件数`(または`検証件数`。用語定義ファイルへ登録のうえで使う)

## 違反の疑い

- `crates/blitz_app/src/hot_reload/compile/fragment_contract.rs:20-51` | 列挙の枝`定数近似の標準金属粗さpbr`・`遠方環境の標準金属粗さpbr`(条件6・1語1義) | 同じ枝の`名前()`メソッドは"標準金属粗さPBR"(大文字)を返すが、識別子自体は小文字"pbr"であり、`_doc/計画/ユビキタス言語.md`の登録語(標準金属粗さPBR)と綴りが食い違う。用語自体は登録済みで意味は一意に読めるため不合格とまでは断定しないが、綴りの不一致は是正が望ましい

## 違反ゼロの範囲

- 上記の指摘を除く残り(hot_reloadの`asset_reload_error.rs`・`asset_watch.rs`・`compile.rs`・`compile_error.rs`・`dir_mtime.rs`・`mod.rs`・`reload_result.rs`・`shader_watch.rs`・`watched_shader.rs`・`asset_watch_tests.rs`・`fixture.rs`、inputの`camera_distance_tests.rs`・`camera_intent_policy.rs`・`camera_intent_tests.rs`・`game_input_tests.rs`・`game_intent_policy.rs`・`game_key_state.rs`・`mod.rs`・`tick_confirm_order_tests.rs`・`tick_distribution.rs`・`tick_distribution_tests.rs`、overlay_uiの`font_setup.rs`・`frame_time.rs`・`game_instrument_panel.rs`・`game_screen.rs`・`mod.rs`・`overlay_content.rs`・`panel.rs`・`texture_mirror.rs`)は、型名・レシーバによる補完、または名前自体が持つ「何を+どうする」の格により受理条件を満たし、違反を検出しなかった
