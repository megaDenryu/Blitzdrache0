# 命名検査報告: 区画094 crates/blitz_render/src/view_input ほか

- 区画番号: 094
- 対象: `crates/blitz_render/src/view_input`・`crates/blitz_render/src/visible_instance_selection`・`crates/blitz_render/src/vulkan/allocator`(25ファイル、block_094.txt の一覧どおり)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main の db815090 時点。編集していない)
- 検査観点: 命名(全プロジェクト共通CLAUDE.mdの「日本語命名の受理条件」8条件・「日本語命名の質」・「呼称と語の選択」・「専門用語の表記」)
- 直読したファイル数: 25/25(全ファイルを直読した。grepだけで済ませたファイルは0)

## 違反の一覧

1. `crates/blitz_render/src/visible_instance_selection/list.rs:44` | `引く`(`可視個体選択一覧::引く`) | 落ちた条件: 7(直訳の検出質問) | 実装は「選択一覧を線形走査して束idと描画対象添字が一致する要素を探し、無ければNoneを返す」検索処理であり、英語の"lookup"の直訳である。プロジェクトのCLAUDE.md「命名」節はオーナー確定の訳語として「lookup/sampleの「引く」→「参照する」」を明記しており、この識別子はその禁止形そのものに当たる。読み手は「引く」だけでは「何を引く算段なのか」(在庫から引く? 差し引く?)を復元できず、英語のlookupを知って初めて「検索して取り出す」の意味だと分かる | `束idと描画対象添字から個体描画計画を探す` または `束idと描画対象添字で参照する`

## 違反の疑い

1. `crates/blitz_render/src/vulkan/allocator/memory_type.rs:35` | `選ぶ`(モジュール直下の`fn 選ぶ(プロパティ一覧, 要求メモリ型ビット, 要求プロパティ) -> Option<u32>`) | 疑いの条件: 3・5(5W1Hの必要格・裸の動詞の禁止) | 名前単独では「何を選ぶのか」が無く、返り値がメモリ型の添字であることは型シグネチャを読んで初めて分かる。ただし本関数は`pub(super)`より狭い完全private・呼び出し元は同じファイル内の`適合する型の添字を選ぶ`1箇所だけであり、その1箇所の名前が「適合する型の添字を選ぶ」で目的語をすでに担っている。読み手が実際に一緒に読む文脈(同一ファイル・数行上の呼び出し元)で意味が復元できるため、断定はしない。改名するなら`適合する型の添字を選ぶ内側`のような入れ子にするか、`ビットとプロパティに適合する添字を選ぶ`のように単独でも読める形にする

## 違反ゼロの範囲

上記2件(1件の違反・1件の疑い)を除く23ファイルの識別子(型名・関連関数・メソッド・フィールド・enum枝・定数・テスト関数名を含む)は受理条件8条件・命名の質・呼称と語の選択のいずれにも抵触しなかった。特に次の点を個別に確認した。

- `視点を作る`・`全個体を描く`・`パス区間が列を隙間なく埋めるかを検査する`等、目的語と動作を含む動詞句はいずれも単独で読める(条件1・3・4)
- `GPU資源の確保係`・`専用メモリ付きバッファ`・`巻き戻せる確保の台帳`・`メモリの置き場`はユビキタス言語.md「GPU資源の確保係」「専用メモリ付きバッファ」「資源型」の定義と実体が一致していた(実例の(a)「実体と合わない語」には該当しない)
- `id列`・`可視ID`・`GPU`等のASCII頭字語混在は、既にユビキタス言語.md全体で確立された表記であり新規導入ではない。「専門用語の表記」の初出補足義務は既存プロジェクト語彙の継続使用には及ばない
- ASCIIモジュール(`allocator`・`view_input`・`visible_instance_selection`)直下の公開関数(`バッファを作って専用メモリを結び付ける`・`画像の作り方から画像を確保する`・`標本の取り方からサンプラーを確保する`等)はいずれも単独で目的語と動作を持ち、裸の動詞(条件5)に当たらなかった
- 識別子へのASCII英字単独・ギリシャ文字・数学記号の混入は見つからなかった(型・フィールド名はすべて完全な日本語句かVulkan/ash由来の生型で、後者は日本語識別子として扱っていない)
- 名詞1語への圧縮(「試験の許容差の当て方」型の実例)・詰め込みすぎ(「許容差の内側の点の行を積まず右辺を零にする」型の実例)に該当する識別子は見つからなかった
