# 命名検査報告: 区画153「xtask/src/distant_view/judgment」

## 範囲・時点

- 区画番号: 153
- 区画名: `xtask/src/distant_view/judgment`(一覧ファイルは`judgment`配下26件に加え、同一検査回で隣接する`distant_view/plan`3件・`distant_view/provenance`3件・`distant_view/run`1件の計36件を含む。一覧は`block_153.txt`のとおり)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(このツリーは編集していない)
- 時点: main の `db815090`
- 必読: グローバルCLAUDE.mdの「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`(該当語をgrepで照合)

## 直読数・省略の有無

- 一覧36ファイルの全部を直読した(直読36/全数36、grepだけで済ませたファイルは0件)。
- 省略した範囲は無い。ただし`実行の別`列挙(識別子`Ssaoなし対照を採る`等)の定義本体は`xtask/src/distant_view/plan.rs`にあり、この区画の一覧には含まれない。定義側の是正は別区画の検査対象になる。本報告ではこの区画内の**参照側**(`plan/argument_name.rs`)にASCII混入の使用が現れることだけを指摘する。
- 同様に`診断画像::rgba8から読み取る`は`crate::shadow_diagnostic_image`が定義元であり一覧外である。使用側(`judgment/shadow_visibility.rs`)にASCII混入の呼び出しが現れることだけを疑いとして記録する。

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `judgment/bloom_footprint.rs:33` | `最小辺PX`(定数) | 日本語命名の質(頭字語・記号の非日本語混入)、受理条件8(長さでなく格の完全性の問題ではないが英字混入自体が不可) | 「PX」という英語の単位略号を日本語の複合語の末尾へ直接連結している。ピクセルを指すと分かるのは英語の慣習を知っている読み手だけであり、非専門家は読めない。同じ意味を持つ既存の`最上段`計算では`画素`という日本語が一貫して使われており、この定数だけ英字が混入して1語1義が崩れている | `最小辺画素数` |
| `plan/capture_condition.rs:30` | `ssaoを使わない`(フィールド) | 日本語命名の質(頭字語混入)、受理条件6(1語1義) | 英語の略語「ssao」をそのまま複合語の頭へ置いている。この区画の`provenance/capture_flags.rs`は同じ概念を`局所可視性を使うか`という日本語で持っており、同じドメイン概念(SSAO=局所遮蔽の可視性判定)に2つの綴りが並立している。読み手は「ssao」と「局所可視性」が同じものだと文書を読まないと分からない | `局所可視性を使わない` |
| `plan/capture_condition.rs:53` | `ssaoを切る`(メソッド) | 同上 | 同上。動詞句としての品詞は正しいが英字混入は変わらない | `局所可視性を切る` |
| `plan/capture_condition.rs`(30,43行)と`provenance/capture_flags.rs`(40行) | `ssaoを使わない` / `局所可視性を使うか` | 受理条件6(1語1義) | 同一区画内の2つのファイルが、SSAO(局所遮蔽の可視性)という同じ概念を別の綴りで持つ。片方は英語の略語のまま、片方は日本語訳語であり、具体側(`plan/capture_condition.rs`)を改名すべき組み合わせである | `capture_condition.rs`側を`局所可視性を使わない`へ統一 |
| `judgment/scatter_stage.rs:26` | `判定する`(pub(super)関数) | 受理条件2(ASCII識別子の補完力ゼロ)、受理条件5(裸の動詞の禁止) | `scatter_stage`はASCIIのモジュール名であり読まれない前提であるため、`scatter_stage::判定する`という呼び出しは型名・レシーバによる目的語の補完が働かない。「判定する」だけでは何を判定するのか(散布の構図であること)が名前に無い。オーナー実例の`create::生成する`と同型の違反である | `散布の構図を判定する` |
| `judgment/shadow_stage.rs:44` | `判定する`(pub(in crate::distant_view)関数) | 同上 | `shadow_stage::判定する`も同型。影の距離区分の再配分を判定する処理だが、名前に対象が無い | `影の構図を判定する` |
| `judgment/geometry_contract.rs:20` | `遠景勝利`(構造体`幾何の実測`のフィールド) | 受理条件1(受理テスト)、受理条件3(5W1Hの必要格) | 実体は「遠景が近傍を隠した画素の数」(同じ型の`課す()`メソッド内で実際にこの語で説明されている)である。フィールド名の「勝利」はゲーム的な比喩であり、単独で読んでも「画素数を数える対象が何か」を復元できない。オーナー実例の「試験の許容差の当て方」と同型(実体と合わない語) | `遠景が近傍を隠した画素数` |
| `plan/argument_name.rs:19-20` | `Ssaoなし対照を採る` / `Ssaoなし候補を採る`(列挙`実行の別`の分岐の使用) | 日本語命名の質(頭字語混入) | 英語の略語「Ssao」を日本語の複合語の先頭へそのまま連結している。定義自体は区画外の`plan.rs`にあるが、この区画の`argument_name.rs`が同じ綴りを使用している。是正は定義元の`plan.rs`(別区画)で行う必要がある | 定義側で`局所可視性なし対照を採る`等へ統一 |

## 違反の疑い

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `judgment/contour.rs:37,21` | `広げる`(private自由関数) | 受理条件1・3 | 呼び出し元(`輪郭を二画素広げる`・`印を二画素広げる`)は目的語を持つ完全な名前だが、内部で共有する`広げる(&mut 結果, 幅, 高さ, x, y)`は動詞のみで「何を・どれだけ広げるか」を名前が持たない。同一ファイル内の直近呼び出し元から意味を復元できるため確定違反とはしないが、条件2が定める補完可能な文脈(型名・レシーバ)には該当しない |
| `provenance/capture_flags/on_off.rs:6` | `入切`(自由関数、値はbool→文字列変換) | 受理条件4(品詞の規律) | 処理(bool を "on"/"off" の文字列へ変換する動作)なのに名前が名詞のままである。同じ役割を持つ`shadow_stage/verdict_names.rs`の`旗の綴り`は動詞的な名詞句(「綴り」を導く処理)として一貫した命名だが、`入切`は状態名をそのまま関数名に流用しており、品詞から処理であることが予告されない |
| `judgment/geometry_contract.rs:19,21` / `judgment/scatter_geometry.rs:27` | `近傍の深度差` / `非有限`(構造体`幾何の実測`・`散布の幾何の実測`のフィールド) | 受理条件3 | 型が「実測」(計測結果の集計)であることは分かるが、フィールド自体は「差」「非有限」という状態や量を表す語であり、それが画素数(件数)であることは同じ型内の他のメソッド(`.零件であることを課す(self.非有限)`)を読まないと確定できない。同じ区画の`非有限深度数`(`image_pair.rs`)は明示的に「数」を付けており、命名の一貫性が崩れている |
| `judgment/shadow_visibility.rs:62` | `rgba8から読み取る`(呼び出し。定義は区画外`crate::shadow_diagnostic_image`) | 日本語命名の質(頭字語混入) | 「rgba8」という画素形式の英語表記がそのまま複合語へ混入している。定義自体は本区画に含まれないため確定は保留する |

## 違反ゼロの範囲

上記に挙げた項目を除く残り全識別子(構造体・列挙・関数・メソッド・フィールド・変数・定数)は受理条件8つを満たしていた。特に以下は積極的に合格と判定した根拠を記す。

- `にじみの足跡`・`許可域外の色差`・`画素の区分`・`散布の画素の区分`・`検収画像`・`色差の帰属の集計`・`影可視度の対`・`色の動き`など、型名が目的語を担う関連関数・メソッド(`求める`・`測る`・`集める`・`課す`系)は受理条件5の例外(型に属する関連関数)に該当し合格
- 真偽を返す関数・メソッドは全て「〜か」で終わり品詞規律(条件4)を満たす(`近傍か`・`許可域の外か`・`記録が動いたか`等)
- 判定名の組み立て関数群(`verdict_names.rs`系、3ファイル)は測る対象と実測値を丸ごと引数に取り、名前が「何の判定か」を明示しており条件3を満たす
- テスト関数名(`gap_tests.rs`・`bloom_footprint/footprint_tests.rs`)は日本語の完文形式であり、受理テスト(条件1)に単独で答えられる

検査した識別子の総数は概算で230件程度(型定義・フィールド・メソッド・関数・定数・列挙枝の合計)であり、このうち確定違反8件・疑い4件を報告した。
