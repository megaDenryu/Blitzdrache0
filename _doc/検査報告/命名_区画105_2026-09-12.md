# 命名検査報告: 区画105 lighting_query/local_visibility/material_record

## 区画名と範囲
区画番号105。対象は次の3ディレクトリ配下の全ファイル(39ファイル)。

- `crates/blitz_render/src/vulkan/lighting_query/`(直下23ファイル + `header_bytes/header_layout_tests.rs` + `pack_tests/count_tests.rs` + `pack_tests/visibility_tests.rs` の計26ファイル)
- `crates/blitz_render/src/vulkan/local_visibility/`(直下8ファイル + `images/create.rs` + `setting/layout_tests.rs` の計10ファイル)
- `crates/blitz_render/src/vulkan/material_record/`(3ファイル)

## 時点
作業ツリー `C:/devs/Blitzdrache0_roller`、mainブランチのコミット `db815090`。このツリーは編集していない。

## 直読したファイル数とgrepだけで済ませたファイル数
39ファイル全件をReadツールで直読した。grepだけで済ませたファイルは0件。

## 必読と照合した資料
- `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」
- `CLAUDE.md`「命名」節
- `_doc/計画/ユビキタス言語.md`(該当語を`クラスタ`・`ライティング`・`局所可視性`・`遮蔽`・`両側ぼかし`・`即時定数`等でgrepし照合)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_render/src/vulkan/local_visibility/input.rs:21,35` | `遮蔽の標本化pipeline`(フィールド) | 日本語命名の質(英語語彙の未翻訳混入)、受理条件2・7 | 前半「遮蔽の標本化」はユビキタス言語に登録された正しい訳語(128行・143行)だが、後半に英単語`pipeline`がそのままアルファベットで接尾している。同じクレート内の対応する型(`local_visibility/pipelines.rs`の`局所可視性のパイプライン一式`のフィールド`遮蔽の標本化`)は接尾辞を持たず、`パイプラインレイアウト`等ではカタカナに訳されている。この1箇所だけが未翻訳の英語を地続きでくっつけており、英語を知らない読み手はこの識別子の後半が何を意味するか単独で復元できない。同じ概念(`遮蔽の標本化`というVkPipeline)が「pipeline」ありとカタカナなしの2通りの表記で存在する不統一でもある | `遮蔽の標本化パイプライン` |
| `crates/blitz_render/src/vulkan/local_visibility/input.rs:22,36` | `両側ぼかしpipeline`(フィールド) | 同上 | 同上と同型。「両側ぼかし」はユビキタス言語140行に登録された正しい訳語だが、`pipeline`が未翻訳のまま接尾している | `両側ぼかしパイプライン` |

## 違反の疑い

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `crates/blitz_render/src/vulkan/lighting_query/directional_bytes.rs:8,21-23` 他(`local_bytes.rs`・`material_record/bytes.rs`) | `vec4を書き込む`・`uvec4を書き込む`・`u32を書き込む`・`可視性をuvec4にする` 等 | 日本語命名の質(英語のシェーダー型名の混入) | `vec4`・`uvec4`・`u32`はGPUのシェーダー言語の型名であり、UI/CPU/GPU/APIのように非専門家が読んで意味が取れる一般語とまでは言い切れない。ただしこの命名は`crate::vulkan::uniform::write_bytes`という区画外の共有モジュールが定めた既存の広域規約であり、本区画のファイルはそれを呼び出しているだけである。区画外の既存規約の是非は本区画の検査の対象外と考え、断定せず疑いに留める |
| `crates/blitz_render/src/vulkan/local_visibility/input.rs:23` `layout: vk::PipelineLayout` / `descriptor.rs:26` `pool: vk::DescriptorPool` / `images.rs:26` `memory: vk::DeviceMemory` | 受理条件2(ASCII識別子は読まれない) | 日本語主体のファイル群の中で`layout`・`pool`・`memory`・`device`のみ裸の英単語がフィールド名・引数名になっている。ただしこれはVulkanハンドルを保持する低レベル資源型の全域にわたる既存の一貫した慣習(`専用メモリ付きバッファ`等でも同型の命名が多数ある)であり、本区画に限った新規の逸脱ではないため、指摘は疑いに留め改名は提案しない |

## 違反ゼロの範囲
上記2件(および疑い2件)を除く、39ファイル中のその他すべての型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝(`照明問い合わせ資源束`・`クラスタ格子の資源`・`局所光レコードの並び`・`局所可視性一式`・`材質レコードバッファ`等、日本語識別子の大半)は受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも抵触しなかった。特に「実体と合わない語」「名詞1語の圧縮」「詰め込みすぎ」の3形と、英字・ギリシャ文字・記号の識別子への混入は、上記2件を除き検出しなかった(`セル1つのバイト数`のような定数名も実体と一致し、`遮蔽の標本化`・`両側ぼかし`・`即時定数`・`局所可視性補正`等の専門語はすべてユビキタス言語に登録済みの訳語と一致する)。
