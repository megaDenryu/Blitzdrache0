# 命名検査報告: 区画051

- 区画名と範囲: crates/blitz_engine/src/asset 第2区画（区画番号051、8ファイル）
- 作業ツリー: C:/devs/Blitzdrache0_roller（編集していない）
- 時点: main の db815090
- 直読したファイル数: 8/8（skin_data.rs, skin_vertex_attribute.rs, static_trs.rs, terrain_lod_meshes.rs, texture_data.rs, texture_storage.rs, vertex_attribute.rs, world_provenance.rs）
- grepだけで済ませたファイル数: 0
- 備考: texture_storage.rs は再エクスポートだけを持つ集約ファイルであり、`ブロックの一辺の画素数`・`ブロック格子`・`テクスチャ格納バイト数エラー`・`テクスチャ格納形式`・`縮小段の寸法`・`格納済みテクスチャ`・`格納済みテクスチャエラー`の実体（定義）は本区画のファイル一覧に含まれない子モジュール（block_grid.rs等）にあるため、この報告は再エクスポート名の見え方だけを検査した。定義側の全識別子検査は対象外（別区画の担当）。

## 違反の一覧

1. `crates/blitz_engine/src/asset/static_trs.rs:8` | `静的TRS` | 条件2(日本語命名の質: 頭字語禁止) | 「TRS」はTranslation/Rotation/Scaleの略で、UI・CPU・APIのような非専門家に通用する語ではなく、ASCII頭字語が日本語識別子へ混入している。オーナーが不合格にした実例と同型（ASCII英字の識別子混入）。 | 改名の素案: `静的な平行移動回転拡大縮小` または `バインド時静的姿勢`
2. `crates/blitz_engine/src/asset/terrain_lod_meshes.rs:9` | `地形LODメッシュ群エラー` | 条件2(日本語命名の質: 頭字語禁止) | 「LOD」(Level of Detail)は非専門家に通用しない頭字語であり、ASCII英字が日本語識別子に混入している。 | 改名の素案: `地形詳細段メッシュ群エラー`
3. `crates/blitz_engine/src/asset/terrain_lod_meshes.rs:15` | `地形LODメッシュ群` | 条件2(日本語命名の質: 頭字語禁止) | 同上。ファイル冒頭の説明文自身が「地形1チャンクの詳細段ごとのメッシュ列」と日本語で言い換えられており、型名だけがLODという頭字語を残している。 | 改名の素案: `地形詳細段メッシュ群`

## 違反の疑い

1. `crates/blitz_engine/src/asset/skin_vertex_attribute.rs:8` | フィールド`ジョイント: [u16; 4]` | 条件3(5W1Hの必要格)の疑い | 直上のコメントで「`ジョイント`はスキンの`ジョイント一覧`内の添字」と説明されており、実体は関節そのものでなく関節を指す添字（インデックス）の組である。フィールド名が名詞1語「ジョイント」だけだと、関節そのもの(`ジョイント`型の値)を保持しているように読め、実際の型`[u16; 4]`とも一致した読み方にならない。オーナー実例の「実体と名前の不一致」に近い形だが、コメントが直近にあり復元可能なため疑いに留める。 | 改名の素案: `影響ジョイント添字`
2. `crates/blitz_engine/src/asset/texture_data.rs:8` | フィールド`rgba8` | 条件2(日本語命名の質)の疑い | 構造体`テクスチャデータ`は他フィールドが「幅」「高さ」と日本語なのに、この1フィールドだけ英語の略記("rgba8")になっている。RGBA8は画像処理分野でよく使われる符号化名だが、UI/CPU/APIほど一般に通用する語ではなく、非専門家には復元できない可能性がある。 | 改名の素案: `画素データ`（型コメントで「RGBA8」の符号化を注記）
3. `crates/blitz_engine/src/asset/vertex_attribute.rs:13` | フィールド`uv` | 条件2(日本語命名の質)の疑い | 「uv」はテクスチャ座標の慣用略記であり、他フィールド(位置・法線・接線)が全て日本語であるのに対しこの1語だけ英語のままである。定着した専門用語として用語定義ファイルへ登録済みなら合格するため、登録の有無を要確認。 | 改名の素案: `テクスチャ座標`

## 違反ゼロの範囲

- `skin_data.rs`: 型名`スキンデータ`とフィールド`ジョイント一覧`は実体（`Vec<ジョイント>`、トポロジカル順）と一致しており、受理条件8項目すべてに違反なし
- `world_provenance.rs`: `世界の乱数の種`・`世界の生成器の版`・`世界の由来`とその関連関数`生成する()`・`値()`はいずれも値オブジェクト様式（型名が目的語を担う）に沿っており違反なし。エラー枝`世界の由来が不正`も実体と一致
- `terrain_lod_meshes.rs`のLOD以外の識別子（`段一覧`・`段数`・`最詳細メッシュ`・`段番号`・`段にスキン頂点属性がある`・`生成する`・`最詳細段`・`より粗い段一覧`）は品詞・格ともに受理条件を満たす
- `static_trs.rs`のTRS以外のフィールド（`平行移動`・`回転`・`スケール`）は違反なし
- `skin_vertex_attribute.rs`の型名`スキン頂点属性`とフィールド`ウェイト`は違反なし
- `texture_storage.rs`の再エクスポート名（`ブロックの一辺の画素数`・`ブロック格子`・`テクスチャ格納バイト数エラー`・`テクスチャ格納形式`・`縮小段の寸法`・`格納済みテクスチャ`・`格納済みテクスチャエラー`）は名前単独で「これは何?」に答えられ、違反なし（ただし実体は定義側ファイルにあり本検査の対象外）
