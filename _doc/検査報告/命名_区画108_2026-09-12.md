# 命名検査報告 区画108

- 区画名: crates/blitz_render/src/vulkan/particles/draw_pipeline ほか(pipeline・physical_device)
- 範囲: block_108.txt に列挙された34ファイル(vulkan/particles/draw_pipeline 3件、vulkan/physical_device 7件、vulkan/pipeline 直下16件、vulkan/pipeline のサブディレクトリ8件)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main, db815090時点)。このツリーは編集していない
- 直読したファイル数: 34/34(全件を直読した。grepだけで済ませたファイルは無い)
- 検査した識別子: 上記34ファイルに現れる型・関数・メソッド・フィールド・変数・定数・列挙の枝の日本語識別子全件
- 参照した必読文書: `.claude/global-reference/全プロジェクト共通CLAUDE.md`(日本語命名の受理条件・日本語命名の質・呼称と語の選択・専門用語の表記)、`_doc/計画/ユビキタス言語.md`(該当箇所を検索して照合)

## 違反の一覧

1. `crates/blitz_render/src/vulkan/pipeline/graphics_pipeline.rs:25` | `組み立てる`(`pub(super) fn 組み立てる(...)`) | 条件5(裸の動詞の禁止) | 呼び出し側は `graphics_pipeline::組み立てる(device, ...)`(`crates/blitz_render/src/vulkan/pipeline/create.rs:34`)であり、`graphics_pipeline`はASCIIモジュール名で補完力ゼロ。「何を組み立てるか」が名前に無く、単独で「これは何?」に答えられない | `色パスの固定機能を組み立てる`
2. `crates/blitz_render/src/vulkan/pipeline/aerial_composite_pipeline/assemble.rs:19` | `組み立てる` | 条件5 | 呼び出し側は `assemble::組み立てる(...)`(`crates/blitz_render/src/vulkan/pipeline/aerial_composite_pipeline/create.rs:29`)。`assemble`はASCIIで補完力ゼロ。対象(空中遠近合成パイプラインの固定機能)が名前に無い | `空中遠近合成の固定機能を組み立てる`
3. `crates/blitz_render/src/vulkan/pipeline/depth_prepass_pipeline/assemble.rs:18` | `組み立てる` | 条件5 | 呼び出し側は `assemble::組み立てる(...)`(`crates/blitz_render/src/vulkan/pipeline/depth_prepass_pipeline.rs:25`)。同上の欠落 | `深度プリパスの固定機能を組み立てる`
4. `crates/blitz_render/src/vulkan/pipeline/point_light_shadow_pipeline/assemble.rs:19` | `組み立てる` | 条件5 | 呼び出し側は `assemble::組み立てる(...)`(`crates/blitz_render/src/vulkan/pipeline/point_light_shadow_pipeline.rs:75`)。同上の欠落 | `点光源の影の固定機能を組み立てる`
5. `crates/blitz_render/src/vulkan/pipeline/shadow_pipeline/assemble.rs:18` | `組み立てる` | 条件5 | 呼び出し側は `assemble::組み立てる(...)`(`crates/blitz_render/src/vulkan/pipeline/shadow_pipeline/create.rs:29`)。同上の欠落 | `シャドウの固定機能を組み立てる`
6. `crates/blitz_render/src/vulkan/pipeline/sky_pipeline/assemble.rs:18` | `組み立てる` | 条件5 | 呼び出し側は `assemble::組み立てる(...)`(`crates/blitz_render/src/vulkan/pipeline/sky_pipeline/create.rs:30`)。同上の欠落 | `空パイプラインの固定機能を組み立てる`
7. `crates/blitz_render/src/vulkan/pipeline/graphics_pipeline/vertex_input.rs:7` | `記述する`(`pub(super) fn 記述する() -> (...)`) | 条件3(5W1Hの必要格) | 引数を持たない裸の関数で、何を記述するか(頂点入力のバインド記述と属性記述)が名前に無い。呼び出し側 `vertex_input::記述する()`(同ファイル56行目)の`vertex_input`はASCIIで補完力ゼロ | `頂点入力を記述する`
8. `crates/blitz_render/src/vulkan/pipeline/graphics_pipeline/vertex_input.rs:44` | `布用記述する`(`pub(super) fn 布用記述する() -> (...)`) | 条件3・日本語命名の質(助詞を省いた膠着圧縮の禁止) | (a)「布用」と「記述する」の間の助詞(に)が無く、同ファイル `pipeline/mod.rs:43` の `布用に生成する` と表記が食い違う。(b)何を記述するかの対象語(頂点入力)も無い | `布用に頂点入力を記述する`
9. `crates/blitz_render/src/vulkan/pipeline/shadow_pipeline/vertex_input.rs:9` | `記述する`(`pub(in crate::vulkan::pipeline) fn 記述する() -> (...)`) | 条件3 | 上記7と同型。位置(location0)だけの頂点入力記述だが、対象語が名前に無い | `位置だけの頂点入力を記述する`

## 違反の疑い

1. `crates/blitz_render/src/vulkan/pipeline/graphics_pipeline/vertex_input.rs:51` | `選択して記述する`(引数`選択: super::頂点属性選択`) | 条件3に対して | 「選択して」で選ぶ対象が引数の型名(頂点属性選択)から辛うじて読めるが、「記述する」の対象語(頂点入力)は無いまま。引数の型が同じ宣言行にあるため条件2の「一緒に読む識別子」に含めてよいかどうかで判定が割れる。断定せず疑いとして報告する | `属性選択に応じて頂点入力を記述する`
2. `crates/blitz_render/src/vulkan/pipeline/*` 全体・`vulkan/particles/draw_pipeline/*` に広がる「日本語+ASCII語」の複合識別子(例: `頂点入力state`・`入力アセンブリstate`・`ビューポートstate`・`ラスタライズstate`・`マルチサンプルstate`・`カラーブレンドstate`・`深度state`・`動的state`・`ディスクリプタlayout`・`頂点spirv`・`画素段spirv`・`rendering情報`) | 「オーナーが不合格にした実例」の「識別子への英字混入は不合格」の条項に対して | この34ファイル内だけで日本語語+ASCII語の複合が約150箇所ある(`頂点入力state`のような`state`系が約100件、`ディスクリプタlayout`系が約30件、`spirv`系が4件)。ルールの文面は英字混入を無条件で不合格としているが、いずれも対応するVulkanの構造体名(`VkPipelineViewportStateCreateInfo`等)の末尾語をそのまま借りた命名であり、`A2`・`Σλ_n`のような数式記号の混入とは性質が異なる。件数が非常に多く、file:line単位で全件を列挙すると本報告が読めなくなるため、ここでは代表例と概算件数だけを報告し、全件の列挙は行っていない。是正するかどうかは一括リネームの是非に関わる判断であり、本検査では断定せず疑いとして起票する

## 違反ゼロの範囲

- 上記の「違反の一覧」8件と「違反の疑い」2件(後者は複合パターンにつき代表例のみ)を除く、34ファイル中の残り全識別子(型名・構造体・列挙・フィールド・引数名・ローカル変数・定数)は受理条件8条を通過した。特に次の点を個別に確認した。
  - `粒子描画パイプライン`・`空中遠近合成パイプライン`・`点光源の影のパイプライン`・`シャドウパイプライン`・`空パイプライン`・`パイプラインレイアウト`・`選定候補`・`色パスの深度状態`はいずれも受理テスト(「これは何?」への単独回答)に合格し、フィールド・メソッド名も対象語を含む
  - `選ぶ`(physical_device/choose.rs)・`満たすキューファミリを探す`・`機能要件を満たすか`・`適合キューファミリを探す`(physical_device/base_requirement.rs)はいずれも主語・目的語を含み一意に読める
  - 値オブジェクト様式の関連関数`生成する()`(`選定候補::生成する`・`粒子描画パイプライン::生成する`等)は型名が目的語を担うため受理条件5の例外として合格
  - 用語定義ファイル(`_doc/計画/ユビキタス言語.md`)との照合では、「ディスクリプタ索引機能項目」「ウィンドウなし実行」等の既登録語と矛盾する使い方は見つからなかった
