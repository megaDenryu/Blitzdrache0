# 命名検査報告 区画103

- 区画名: crates/blitz_render/src/vulkan/graph（区画番号103、対象ファイル一覧は `block_103.txt`）
- 作業ツリー: C:/devs/Blitzdrache0_roller（main の db815090時点。このツリーは編集していない）
- 対象ファイル数: 37
- 直読したファイル数: 37/37（grepだけで済ませたファイルは0件）
- 検査に用いた規約: グローバルCLAUDE.mdの「日本語命名の受理条件」8条・「日本語命名の質」・「呼称と語の選択」・「専門用語の表記」、および `_doc/計画/ユビキタス言語.md`（該当語をgrepで照合。件数が多いため全文は読まず、登場した固有名だけを個別に検索した）

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| barrier_derivation.rs:18-22（画像バリア記述の`前`/`今`フィールド。使用箇所: barrier_apply.rs:41-46, barrier_derivation.rs:79-91, 各テストファイル） | `前` / `今` | 条3（5W1Hの必要格の欠落） | フィールドの型は`画像状態`だが、名前は時間的前後関係だけを表す語で「状態」という対象語が落ちている。呼び出し側では`バリア.前.stage`のように読み手変数`バリア`を介して補完するしかなく、「前のバリア」「前の何か」とも読める。同じファイルの内部ロジックはローカル変数を`前状態`/`今状態`（対象語つき）と命名しており、フィールド名だけが対象語を落としている ― 実装自身が「本来は状態という語が要る」ことを示している | `前状態` / `今状態` |
| buffer_barrier_derivation.rs:19-23（バッファバリア記述の`前`/`今`フィールド） | `前` / `今` | 条3 | 上と同型。バッファ版でも`差分を計算する`内のローカル変数は`前状態`/`今状態`であり、フィールドだけが圧縮されている | `前状態` / `今状態` |
| initial_state.rs:63（`crates/blitz_render/src/vulkan/graph/initial_state.rs`。ディレクトリ`initial_state/`の親モジュールファイル） | `前フレームhdr読み直後状態` | 条2（ASCII識別子は補完力ゼロ）＋日本語命名の質「頭字語を日本語へ混ぜない」 | 小文字ASCIIの`hdr`が日本語の複合語の中に無区切りで埋め込まれており、視覚的に単語境界が壊れている。同じ関数群の兄弟（`前フレーム深度書き込み直後状態`等）はすべて漢字のみで統一されているのに、この1件だけ英字が混入している | `前フレームHDR中間画像読み直後状態`（同ファイル内のHDR用語表記に合わせるなら大文字化。より踏み込むなら「HDR中間画像」を用語集に登録した上で使う） |
| hdr_target/create.rs:35, 12（`hdr中間画像を作る`） | `hdr中間画像を作る` | 条2＋日本語命名の質「頭字語混入」 | 同ファイル内で型名`HDRターゲット`・定数`HDR形式`は大文字で書かれているのに、この自由関数だけ小文字`hdr`を使っており、大文字/小文字の使い分けに一貫性が無い。ASCIIモジュール直下(`create.rs`)の自由関数は名前単独で読ませる規律のところへ、英字混入で単語境界も崩れている | `HDR中間画像を作る` |
| pass.rs:34, 46, 58（構造体`パス宣言`のフィールド、および関連する関数引数） | `gpu命令をコマンドバッファへ積む` | 条2＋日本語命名の質「頭字語混入」 | 小文字`gpu`が複合語の先頭に無区切りで混入している。同じ概念を指す型名は`GPU命令の積み先`（大文字）であり、フィールド名だけ表記が割れている | `GPU命令をコマンドバッファへ積む` |
| rendering_setup.rs:29, 46（ローカル変数） | `カラーload_op` / `深度load_op` | 条2＋日本語命名の質「頭字語・膠着圧縮」 | 日本語のカタカナ・漢字表記とASCIIのスネークケース`load_op`が無区切りで接続されており、単語境界が視覚的に崩れている。同じ値を渡す先のパラメータ名は`ロード操作`（日本語）であり、渡す側と受ける側で表記方式が食い違う | `カラーのロード操作` / `深度のロード操作` |
| rendering_setup.rs:68（自由関数、モジュール`rendering_setup`直下） | `ロードオペレーションとカラークリア値` | 条4（品詞の規律）＋条6（1語1義）＋呼称と語の選択「カタカナ語は小学生が分かるものだけ」 | (a) この関数は`クリア指定`から値を導出する処理だが、名前が動詞を持たない名詞句のみで「〜する」処理として読めない。(b) 同じ概念（Vulkanの`AttachmentLoadOp`）を`color_attachments.rs`側は`ロード操作`と呼んでいるのに、この関数だけ`ロードオペレーション`という別のカタカナ語を使っており、1つの語に2つの表記が生まれている。(c)「ロードオペレーション」は専門知識が無いと理解できないカタカナ語で、「小学生が分かるか」の基準を満たさない | `クリア指定からロード操作とクリア値を導出する`（表記を`ロード操作`へ統一し、動詞句にする） |
| initial_state/temporal_reconstruction.rs:31（`前フレーム今のフレームの色読み直後状態`） | `前フレーム今のフレームの色読み直後状態` | 条3（紛れの発生） | 「前フレーム」（過去の時点を指す語）と「今のフレーム」（現在の時点を指す語）が隣接しており、単独で読むと「前なのか今なのか」が矛盾して見える。実体は「“今のフレームの色”という名前を持つ画像の、前フレームでの読み直後の状態」であり、内側の対象名（今のフレームの色画像）に既に時制語を含むため外側の時制語と衝突する。兄弟関数（`前フレーム動きベクトル書き込み直後状態`等）と同じ語順パターンを機械的に当てはめたために起きた事故 | `今のフレームの色画像の前フレーム読み直後状態`（対象名を先に置き、時制語の衝突を切り離す） |

## 違反の疑い（断定しない）

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| builder.rs:27, registry.rs:22, buffer_registry.rs:18 | `新規()` | 条4（品詞の規律。処理は動詞句のはずが名詞） | 構築という処理を表すのに動詞を持たない名詞「新規」を使っている。ただし`fn 新規()`という形はこのリポジトリ全体で22ファイルに渡って使われている確立した慣用（引数無しコンストラクタの既定名）であり、この区画だけの問題ではない。単独での断定は避け、リポジトリ全体の方針確認を推奨する |
| color_attachments.rs:80（自由関数`記述を作る`） | `記述を作る` | 条3（目的語の具体性不足） | 「記述」が汎用的すぎ、このファイルの中には`画像バリア記述`・`バッファバリア記述`・`パス宣言`等、他にも「記述」を含む型が複数あるため、単独で読むと何の記述を作るのか一意に決まらない。ただし呼び出し元では`色の記述 = |色| 記述を作る(...)`という束縛を介して読まれ、戻り値の型`vk::RenderingAttachmentInfo`から実体は明確なので、断定は避ける |
| rendering_setup.rs:73（`カラー.rgba配列()`の呼び出し） | `rgba配列` | 条2（ASCII混入の疑い） | 呼び出し箇所はこの区画内にあるが、定義（`crate::clear_color::クリアカラー`）は区画103の対象ファイル一覧に含まれないため、定義側の全文を確認していない。呼び出し名だけを見ると小文字ASCII`rgba`が日本語複合語へ混入しており、他の違反と同型に見える |
| executor.rs（メソッド名`グラフを積む`）と builder.rs:2（冒頭コメント「実際のGPU命令発行は`executor::実行する`が...」） | `グラフを積む` / コメントが指す`実行する` | 識別子とコメントの整合の疑い | builder.rsの冒頭コメントは`executor::実行する`という名前を参照しているが、executor.rs内の実際のメソッド名は`グラフを積む`であり、この名前の関数は存在しない。読み手がコメントを頼りに実装を辿ると見つからない。命名規約の直接の違反ではなくコメントの陳腐化の疑いだが、識別子の受理性（コメントとの整合を含む「これは何？」への回答可能性）に関わるため記録する |

## 違反ゼロと確認した範囲

- ハンドル型（`handle/buffer_handle.rs`・`handle/image_handle.rs`・`handle/mod.rs`）: フィールド・メソッドとも`添字`・`世代`・`生成する`で一貫し、値オブジェクト様式に沿っている
- `context.rs`（`GPU命令の積み先と宣言済み資源の取り出し口`）: 英語直訳の「文脈」を避け、実体（積み先の保持＋宣言済み資源の参照口）をそのまま名前にしている。ユビキタス言語の登録語`GPU命令の積み先`と整合
- `usage/`配下4ファイルと`buffer_state.rs`・`state.rs`・`aspect.rs`・`depth_attachment.rs`・`clear_spec.rs`・`pass_resource_usage.rs`: 列挙の枝・フィールド・メソッド名はいずれも動作・対象が明確で、5W1Hの必要格を満たしている
- `barrier_derivation/barrier_derivation_tests/*`・`buffer_barrier_derivation/buffer_barrier_derivation_tests.rs`のテスト関数名・ローカル変数名: 「〜のはず」を含む断定文形式で書かれており、テスト対象と期待の対応が単独で読める
- `executor/pass_record.rs`・`registry.rs`・`buffer_registry.rs`の`ハンドルの指すエントリを参照する`等の非公開ヘルパー: 5W1Hの必要格を満たす
