# ローラー検査(命名の軸) 区画008: crates/blitz_app/src/cli 第1区画

- 区画番号: 008(40ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main, db815090時点。編集していない)
- 直読したファイル数: 40 / 40(全ファイルを直読した。grepだけで済ませたファイルは0)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

CLI引数の解析関数の名前が、フラグの綴り(ASCII)をそのまま識別子へ連結する形になっている。この形は2026-08-10に`game引数を処理する`が同じ理由(ASCII語の混入)で`遊ぶゲームの指定の引数を処理する`へ改名された前例があり(`_doc/開発スレッド/端末ログ/2026-08-10デンリュウ0810.txt`)、今回の区画には同型の未是正が10件残っている。

1. `crates/blitz_app/src/cli/path_args.rs:13` | `shader_source引数を処理する` | 条件2 | ASCII識別子(`shader_source`)は読まれないものとして扱う規約に反し、しかも一緒に読む日本語の型名・レシーバが無いためASCIIの部分が補完されない。読み手はcrate外の`--shader-source`という綴りを知らないと関数名の前半を読めない | `シェーダー入口の指定の引数を処理する`
2. `crates/blitz_app/src/cli/path_args.rs:20` | `asset_root引数を処理する` | 条件2 | 同上(`asset_root`がASCIIのまま識別子の一部になっている) | `アセットルートの指定の引数を処理する`
3. `crates/blitz_app/src/cli/placement_args.rs:36` | `global_offset引数を処理する` | 条件2 | 同上(`global_offset`) | `大域ずらし量の引数を処理する`
4. `crates/blitz_app/src/cli/placement_args.rs:51` | `camera_pitch引数を処理する` | 条件2 | 同上(`camera_pitch`) | `カメラ俯角の引数を処理する`
5. `crates/blitz_app/src/cli/placement_args.rs:63` | `camera_yaw引数を処理する` | 条件2 | 同上(`camera_yaw`) | `カメラ方位の引数を処理する`
6. `crates/blitz_app/src/cli/placement_args.rs:75` | `camera_nudge引数を処理する` | 条件2 | 同上(`camera_nudge`) | `カメラずれの引数を処理する`
7. `crates/blitz_app/src/cli/instance_lod_args.rs:13` | `lod_probe_step引数を処理する` | 条件2 | 同上(`lod_probe_step`) | `個体別lod探査刻みの引数を処理する`
8. `crates/blitz_app/src/cli/cloth_reference_args.rs:14` | `cloth_xpbd_reference引数を処理する` | 条件2 | 同上(`cloth_xpbd_reference`) | `布のxpbd参照比較の引数を処理する`
9. `crates/blitz_app/src/cli/cloth_reference_args.rs:30` | `cloth_xpbd_reference_bending引数を処理する` | 条件2 | 同上(`cloth_xpbd_reference_bending`) | `布の曲げのコンプライアンスの引数を処理する`
10. `crates/blitz_app/src/cli/cloth_reference_args.rs:55` | `cloth_xpbd_reference_shape引数を処理する` | 条件2 | 同上(`cloth_xpbd_reference_shape`) | `参照比較の題材の形の引数を処理する`

同じ理由でASCIIの型名・関数名を裸で識別子へ連結した例が、`引数を処理する`以外にもある。

11. `crates/blitz_app/src/cli/lod_crack_args.rs:33` | `i32を読む` | 条件2 | ASCIIの型名`i32`が識別子の先頭に来ており、Rustの型を知らない読み手には「何を読むか」が伝わらない。同ファイルには型を伴わない`数を読む`(ibl_step_scan_args.rs)という書き方が既にあり、こちらだけ型名を裸で出している | `一方の成分を読む`(呼び出し位置に応じて命名。汎用にするなら`整数を読む`)
12. `crates/blitz_app/src/cli/lod_crack_args.rs:38` | `u8を読む` | 条件2 | 同上(`u8`) | `詳細段の番号を読む`
13. `crates/blitz_app/src/cli/object_count.rs:24` | `usize値` | 条件2 | ASCIIの型名`usize`をそのまま名詞へ連結しており、Rustの型システムを知らない読み手には「値」が何の値かの手掛かりが型名のASCIIに依存する | `件数として取り出す`

列挙の枝がASCIIの略号+数字だけで、名詞句としての意味を持たない例。

14. `crates/blitz_app/src/cli/modes.rs:35-37` | `Sph512` `Sph1024` `Sph2048` | 条件1・条件3 | 「これは何?」に単独で答えられない(512/1024/2048が粒子数なのか解像度なのか格が無い)。ユビキタス言語に登録された表記は`SPH`(全大文字)であり、この3枝は`Sph`(先頭のみ大文字)と綴りが違う。オーナー実例の「段階A2の候補」と同型(略号+数字の圧縮) | `SPH粒子512個` `SPH粒子1024個` `SPH粒子2048個`(粒子数であることを格で示す。オーナー確認要)

同一機能内で同じ略号の大文字・小文字表記が割れている例(1語1義に対する疑い、下記「違反の疑い」参照)。

## 違反の疑い

1. `crates/blitz_app/src/cli/argument_error.rs:58`(`Lod継ぎ目検査不正`)と `crates/blitz_app/src/cli/lod_crack_args.rs:9,34,35,39,40,42` (`起動引数エラー::Lod継ぎ目検査不正`および`LOD継ぎ目検査設定`という別型への参照) | 条件6の疑い | 同じ機能(LOD継ぎ目検査)を指す略号が、型`LOD継ぎ目検査設定`では全大文字`LOD`、エラー枝`Lod継ぎ目検査不正`では先頭大文字`Lod`と表記が割れている。同じ概念に2通りの綴りがあると、grepや検索で片方が漏れる実害がある。断定しないのは、これが1語1義(意味の衝突)ではなく単純な綴りの不統一であり、受理条件のどの項が直接対応するか一意に決まらないためである。改名素案: `LOD継ぎ目検査不正`へ統一
2. `crates/blitz_app/src/cli/scene_selection.rs:25,31-34,44`(`安定id`というフィールド名・メソッド名) | 条件6の疑い | `_doc/計画/ユビキタス言語.md`に登録された正式表記は「安定ID」(全大文字)であり、他の全域(カタログ・アセットの語彙等)でも「安定ID」で統一されているのに対し、この型だけ小文字の`安定id`を使っている。同一の登録語の綴り違いであり、断定しないのは実害(意味の混同)までは生じていないと考えられるためである。改名素案: `安定ID`
3. `crates/blitz_app/src/cli/modes.rs:15`・`launch_request.rs:4,14,28`・`argument_error.rs:68` ほか多数(`XPBD参照比較`・`XPBD並列方式計測`等、識別子中の`XPBD`)、および`argument_error.rs:30`・`instance_lod_args.rs:1-19`ほか多数(識別子中の`LOD`) | 条件7(日本語命名の質「頭字語」)の疑い | `XPBD`と`LOD`はどちらもユビキタス言語に日本語の意味を添えて登録された略号(「XPBD: 拡張位置ベース動力学」「LOD: 詳細度に応じた技法」)だが、識別子の中では終始ASCIIの略号のまま使われ、日本語へ意訳されていない。日本語命名の質は「LUT・UBO・SSBO・PSO」を意訳すべき例に挙げ、「一般に通用している語(UI・CPU・GPU・API等)」だけを許容している。`XPBD`・`LOD`は本エンジン内で広く定着した用語であり、一般に通用する語(GPU/CPU/API)と同じ扱いにできるかは、`UI・CPU・GPU・API`ほど社会一般に膾炙していない専門略号である点で判断が割れる。プロジェクト全体で一貫して(綴りの統一を除き)この形を使っており、この区画だけの是正では済まない全域判断のため、個別の行を数十件列挙せず1件の疑いとしてまとめて報告する。断定は親またはオーナーの裁定に委ねる

## 違反ゼロの範囲

上記に挙げた識別子(ASCII混入・略号圧縮・綴り不統一)を除き、40ファイル全体の型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝は、受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも違反を確認しなかった。特に、値オブジェクト様式の`生成する()`(型名が目的語を担う形)、`露出倍率`・`アニメーションのブレンド係数`・`布のコンプライアンス指定`のような単位を持つ値の型、`起動引数エラー`の各枝(引数名を`--`付きでdocコメントに明記し、枝名は日本語の名詞句で読める)、`描画対象の走査順`・`描画対象の並べ方`のような概念をまとめた型は、いずれも受理テスト(「これは何?」に単独で答えられる)を満たしていた。
