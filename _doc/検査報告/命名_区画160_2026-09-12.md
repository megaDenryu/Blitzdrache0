# 命名検査報告 区画160「xtask/src/part_row_draw/tree_row」ほか

- 区画番号: 160
- 対象: xtask/src/part_row_draw/tree_row、xtask/src/part_tools、xtask/src/point_light_shadow、xtask/src/prop_draw、xtask/src/raw_png、xtask/src/release_build、xtask/src/report_line_key、xtask/src/report_parse(一覧は block_160.txt、38ファイル)
- 時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 38/38(全ファイル直読。grepだけで済ませたファイルは0件)
- 参照した規約: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`_doc/計画/ユビキタス言語.md`

## 違反

1. `xtask/src/prop_draw/background_color.rs:19` | `決める`(`pub(super) fn 決める(画像: &読み戻し画像) -> Result<[u8; 3], 判定の破れ>`) | 条件1・条件3・条件5 | ASCIIモジュール`background_color`直下の自由関数であり、モジュール名は読まれないものとして扱う規約により補完力がゼロである。「決める」単独では何を決めるのかが無く(何を+どうするの「何を」が欠落)、`create::生成する`型の裸の動詞禁止にそのまま該当する。呼び出し側は`background_color::決める(画像)`で、日本語の側だけを読むと「(画像から)決める」としか分からない | `背景色を決める`
2. `xtask/src/prop_draw/object_pixels.rs:25` | `要約する`(`pub(super) fn 要約する(画像: &読み戻し画像, 背景色: [u8; 3]) -> 物体画素の要約`) | 条件1・条件3・条件5 | ASCIIモジュール`object_pixels`直下の自由関数。「要約する」に対象語が無く、画像全体を要約するのか物体画素だけを要約するのか(実体は背景でない画素の分布の要約)が名前から復元できない | `背景でない画素を要約する`
3. `xtask/src/report_parse/extract.rs:8` | `取り出す`(`pub fn 取り出す(報告: &終了時報告) -> Result<計数報告, 検収エラー>`) | 条件1・条件3・条件5 | ASCIIモジュール`extract`直下の自由関数。冒頭コメントは「アプリの終了時報告を1つの計数報告へ写す工程」と実体を語っているが、識別子自体は「(報告から)取り出す」としか読めず、終了時報告のうち何を取り出すのかが欠落している | `終了時報告から計数報告を取り出す`

## 違反の疑い

1. `xtask/src/release_build/provenance/capture.rs:42` | `行ごとのパス`(`pub(super) fn 行ごとのパス(出力: Option<&String>) -> Vec<String>`) | 条件4に対して | gitの標準出力の文字列をパスの並びへ変換する処理(副作用は無いが変換処理)であるのに名詞句のまま置かれている。値オブジェクトの導出メソッド的な読み方(「これは行ごとのパスです」)でも成立はするため、断定はしない
2. `xtask/src/release_build/provenance/capture.rs:29` | `実行ファイルの状態`(`pub(super) fn 実行ファイルの状態(実行ファイル: &Path) -> (String, u64)`) | 条件4に対して | `std::fs::metadata`という外部I/Oを伴う処理でありながら名詞句で置かれている。ただし返り値そのものを指す名前として単独で読めるため(「実行ファイルの状態です」)、上と同様に断定はしない

## 違反ゼロと判定した範囲

- `xtask/src/part_row_draw/tree_row/`(error.rs・judgment.rs・run.rs・error/display.rs)の4ファイル
- `xtask/src/part_tools/`(assembly.rs・catalog.rs)の2ファイル
- `xtask/src/point_light_shadow/`の全12ファイル(error.rs・instrument.rs・instrument_judgment.rs・interior_judgment.rs・interior_measure.rs・interior_region.rs・night_judgment.rs・night_measure.rs・night_region.rs・region.rs・shadow_count_scale.rs・summary.rs・error/display.rs)
- `xtask/src/prop_draw/`のうち上記2件を除く5ファイル(bounding_box_judgment.rs・error.rs・judgment.rs・run.rs・error/display.rs)
- `xtask/src/raw_png/error.rs`
- `xtask/src/release_build/`のうち上記2件の疑いを除く6ファイル(error.rs・provenance.rs・raw_value_file.rs・tsv_prefix.rs・provenance/diff_presence.rs・provenance/notes_tests.rs)
- `xtask/src/report_line_key/`の全2ファイル(binding_tests.rs・value_lookup.rs)
- `xtask/src/report_parse/`のうち上記1件を除く1ファイル(line_read.rs)

なお`gitの出力`(capture.rs・diff_presence.rs)、`構築の道具cargoを起こせなかった`(release_build/error.rs)は、ASCII語が固有のツール名(git・cargo)であり訳語を要する専門用語ではないため違反として扱わなかった。`gpu時間のp50ms`等のGPU・p50は専門用語の表記の一般流通語およびユビキタス言語登録済みの分位点表記であるため違反として扱わなかった。
