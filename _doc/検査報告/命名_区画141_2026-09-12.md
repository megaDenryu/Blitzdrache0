# ローラー検査(命名)区画141: xtask/src 第3区画

- 区画番号: 141(13ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main の db815090。このツリーは編集していない)
- 対象ファイル一覧: `xtask/src/temporal_reconstruction.rs` `xtask/src/temporal_visual.rs` `xtask/src/terrain_visual.rs` `xtask/src/texture_compression.rs` `xtask/src/validation_count.rs` `xtask/src/vegetation_run.rs` `xtask/src/verify.rs` `xtask/src/vertex_diag.rs` `xtask/src/village_draw.rs` `xtask/src/visual_sample_world.rs` `xtask/src/watch_assets.rs` `xtask/src/world_setup.rs` `xtask/src/xpbd_solver_bench.rs`
- 直読したファイル数: 13/13(全ファイルを全文Readで直読。grepだけで済ませたファイルは0件)
- 必読(定義に指定された節): グローバルCLAUDE.mdの複製(`.claude/global-reference/全プロジェクト共通CLAUDE.md`)の「専門用語の表記」「呼称と語の選択」「日本語命名の質」「日本語命名の受理条件」を読んだ。用語照合は `_doc/計画/ユビキタス言語.md` を該当語(XPBD・目視見本の世界・見本の集落等)でgrepして参照した(全文404.8KBのため一括Readは不可、offset/grepで代替)

## 違反の一覧

1. `xtask/src/visual_sample_world.rs:29` | `pub fn 用意する() -> Result<(), 検収世界の用意の破れ>` | 条件2・条件5 | ASCIIモジュール`visual_sample_world`直下の自由関数が裸の動詞「用意する」であり、何を用意するか(目視見本世界の実行時形式)が名前に無い。呼び出し側は`crate::visual_sample_world::用意する()`としか読めず、モジュール名(ASCII)は補完力ゼロなので「何の用意か」が単独で読み取れない。定義に書かれた不合格実例`create::生成する`と同型 | `目視見本世界を用意する`
2. `xtask/src/temporal_reconstruction.rs:40` | `crate::visual_sample_world::用意する()`(呼び出し) | 条件2・条件5 | 上記1の呼び出し箇所。同じ理由で単独で読めない | 定義1の改名に追従
3. `xtask/src/temporal_visual.rs:46` | `crate::visual_sample_world::用意する()`(呼び出し) | 条件2・条件5 | 同上 | 同上
4. `xtask/src/terrain_visual.rs:38` | `crate::visual_sample_world::用意する()`(呼び出し) | 条件2・条件5 | 同上 | 同上
5. `xtask/src/temporal_reconstruction.rs:45` | `judgment::判定する(&一回目, &二回目)` | 条件2・条件5 | ASCIIモジュール`judgment`直下の自由関数が裸の動詞「判定する」。何を判定するか(一回目と二回目の観測が一致すること)が名前から読めず、モジュール名はASCIIで補完しない。同ファイルの直前の行`injection::取り出して判定する(...)`も目的語が薄いが、こちらは動詞の複合で幾らか救われている一方、`判定する`単独はさらに弱い | `一回目と二回目の観測が一致することを判定する`
6. `xtask/src/vertex_diag.rs:76,84` | `pixel_check::数える(&影なしの粗い.画像, &影なしの細かい.画像)` / `pixel_check::数える(&粗い.画像, &細かい.画像)` | 条件2・条件3・条件5 | ASCIIモジュール`pixel_check`直下の自由関数が裸の動詞「数える」。何を数えるか(食い違う画素)が名前に無い。姉妹の処理である`xtask/src/temporal_visual.rs`内の`pixel_diff::食い違う画素を数える`は同じ種類の処理に目的語付きの名前を与えており、同種処理の呼称が割れている(条件6 1語1義の観点でも紛れる) | `食い違う画素を数える`
7. `xtask/src/xpbd_solver_bench.rs:39,42,46,52,54,56,64,73`(関数定義`xpbd並列方式を計測する`とその呼び出し・ログ文字列) | `xpbd並列方式を計測する` | 条件2・日本語命名の質(頭字語の直接連結・膠着圧縮の禁止) | 小文字ASCIIの`xpbd`が助詞を挟まず日本語へ直接膠着している。同じファイル内で型名は`XPBDの並列方式の計測エラー`と大文字+「の」で書かれており綴りが割れている。さらに兄弟コマンド(`dispatch/measurement.rs`が束ねる`ow4_bench::植生密度別に性能を計測する`・`shadow_probe::影の律速切り分けを計測する`等)はいずれも純粋な日本語の関数名であり、この関数だけがASCII頭字語を生のまま名前へ混ぜている | `XPBD並列方式を計測する`(最小修正)。将来的には日本語だけの言い回しへの言い換えも検討可
8. `xtask/src/village_draw.rs:30` | `pub fn 見本集落の描画を確認する() -> ExitCode` | 条件6(1語1義)・日本語命名の質(助詞省略の膠着圧縮の禁止) | 同じファイルの24〜27行目で世界の呼び名を`"見本の集落"`と助詞「の」を入れて定義しているのに、関数名だけ「見本集落」と助詞を落として圧縮しており、同一ファイル内で同じ語が2つの表記を持つ | `見本の集落の描画を確認する`

## 違反の疑い(断定しない)

- `xtask/src/temporal_visual.rs:62-63` | `時間再構成の条件::効かせる` / `時間再構成の条件::切る`(列挙の定義は`temporal_visual/run.rs`にあり区画外だが、本ファイルで使用) | 条件4(品詞の規律)に対する疑い | 列挙の枝は値であり本来は名詞句(例: `有効`/`無効`)であるべきだが、動詞の辞書形をそのまま枝名にしている。`時間再構成の条件::効かせる`は「条件、効かせる」と読め、値なのか指示なのかが名前だけでは紛れうる。ただし直前の変数`効かせた`/`切った`という命名が対で使われており、書き手が意図的に「その条件を適用した状態」を指す略記として選んだ可能性がある。定義側(区画外)を見ないと断定できない
- `xtask/src/visual_sample_world.rs`ほか多数(`目視見本世界のシーン名`・`目視見本世界のアセットルートを作る`等) | 条件6(1語1義)に対する疑い | `_doc/計画/ユビキタス言語.md`の登録語は「目視見本の世界」(助詞あり)だが、識別子は「目視見本世界」(助詞なし)で統一されている。単体で見れば助詞省略の膠着圧縮に見えるが、コードベース全体で30箇所以上に渡り一貫して使われており、複合名詞として定着済みの可能性が高い(本区画だけの問題ではなく全域の慣習)。一括是正の要否は親の判断に委ねる

## 違反ゼロの範囲

- `xtask/src/validation_count.rs`・`xtask/src/vegetation_run.rs`・`xtask/src/verify.rs`・`xtask/src/watch_assets.rs`・`xtask/src/world_setup.rs`は全文を直読し、識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)に受理条件8条・命名の質・呼称と語の選択のいずれの違反も見つからなかった
- 上記5ファイル以外の8ファイルも全文を直読したが、違反一覧に挙げた箇所を除く識別子(`検収世界の用意`・`検証の出力の置き場名`・`地面を照らす光`・`太陽が地平線より上か`・`計測の実行環境`等)はいずれも受理条件を満たしていた
