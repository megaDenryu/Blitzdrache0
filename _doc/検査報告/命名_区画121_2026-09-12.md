# 命名検査報告 区画121

- 区画名と範囲: crates/blitz_sim/src/contact/pipeline 第2区画(区画番号121、17ファイル)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090`(このツリーは編集していない)
- 直読したファイル数: 17 / 17(grepだけで済ませたファイルは無い)
- 対象ファイル一覧:
  1. substep_predict.rs
  2. substep_rest_judgment.rs
  3. tentative_multipliers.rs
  4. tower_fixture.rs
  5. tower_gap_record.rs
  6. tower_invariance_tests.rs
  7. tower_manifold_size_record.rs
  8. tower_margin_point_record.rs
  9. tower_rest_condition_record.rs
  10. tower_rest_probe.rs
  11. tower_rest_tests.rs
  12. tower_row_admission_record.rs
  13. tower_run_tally.rs
  14. tower_standing_tests.rs
  15. tower_substep_count_tests.rs
  16. transition_discard_tests.rs
  17. transition_history_probe.rs

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| tower_rest_probe.rs:11,45,100,102-103 / tower_row_admission_record.rs:17,40 | `試験の許容差の当て方` | 受理条件1(受理テスト) | この型は「受理の許容差に掛ける倍率」と「接線の行への当て先(3枝の列挙)」を束ねた設定である(`_doc/計画/ユビキタス言語.md`1104行で確認)。「当て方」という名詞だけでは、これが2つの設定を束ねた1つの値であることが復元できず、「どう適用するか」という手順の説明としてしか読めない。グローバルCLAUDE.mdの「オーナーが不合格にした実例」がこの識別子そのものを不合格例として挙げ、改名先まで示している | `試験の許容差の適用規則`(定義の改名案どおり) |
| tower_row_admission_record.rs:26(および同型を宣言する `contact/normal_tangential_system/tolerance_application.rs` の枝定義。本区画では利用箇所のみ検出) | `接線の行への許容差の当て方::許容差の内側の点の行を積まず右辺を零にする` | 受理条件1・3 | 枝の実体は「接線変位が許容差以下の点について拘束行を追加しない」という1つの判断である。名前は「行を積まない」ことと「右辺を零にする」ことの2つを並べており、後者は同じ枝の中の本質でない付随条件(右辺の扱いは行を積まないことの結果であって主題ではない)である。グローバルCLAUDE.mdの「オーナーが不合格にした実例」がこの文字列そのものを不合格例として挙げている | `許容差以下の接線変位の拘束行を追加しない`(定義の改名案どおり) |

## 違反の疑い

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| tower_run_tally.rs:15(構造体名) | `塔の走行の数え` | 受理条件4(品詞の規律) | 「数え」は動詞「数える」の連用形を名詞化した略式の語で、値(蓄積した計数の集まり)を指す名詞句としてはやや据わりが悪い。ただし「数え年」等の定着した用法もあり、文脈(構造体が複数のカウンタを保持し`一刻みを数える`で更新する)からは実体を復元できるため、断定はしない |
| tower_row_admission_record.rs:25 / tolerance_application.rs(区画外) | `接線の行への許容差の当て方::右辺を零にする` | 受理条件7(直訳・実体一致) | `_doc/計画/ユビキタス言語.md`1104行の説明では、この枝が対応する候補(i)の右辺は「−C_t」であり、必ずしも0ではない可能性がある(枝名は「零にする」と断定している)。ただし本枝の宣言ファイルは本区画の外(`normal_tangential_system/tolerance_application.rs`)にあり、実装の数式まで確認していないため確定できない。宣言側の検査で実体と数式を照合することを推奨する |

## 違反ゼロの範囲

上記2件の外来識別子(区画外で宣言され本区画で利用のみ)を除き、本区画17ファイルの型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝はすべて受理条件8つを満たした。特に、綴り用の計器群(tower_gap_record.rs・tower_manifold_size_record.rs・tower_margin_point_record.rs・tower_rest_condition_record.rs・tower_rest_probe.rs・tower_run_tally.rs・transition_history_probe.rs)は「計器」「綴る」という一貫した語彙を使い、何を数え何を出力するかを関数名・変数名がそのまま述べており、直訳調・膠着圧縮・裸の動詞・1語1義違反のいずれも検出しなかった。塔の場面の組み立て(tower_fixture.rs)、休止・静穏まわりの判定(substep_rest_judgment.rs)、仮の乗数の集計(tentative_multipliers.rs)、細分の予測(substep_predict.rs)、休止・遷移・不変性・段数の各検査群(tower_invariance_tests.rs・tower_rest_tests.rs・tower_standing_tests.rs・tower_substep_count_tests.rs・transition_discard_tests.rs)も同様に違反ゼロである。
