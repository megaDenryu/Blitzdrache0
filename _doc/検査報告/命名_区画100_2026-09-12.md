# 命名検査報告 区画100

- 区画名と範囲: `crates/blitz_render/src/vulkan/distant_environment`(14ファイル)と`crates/blitz_render/src/vulkan/frame`(26ファイル)。合計40ファイル
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、mainのコミット `db815090`(このツリーは編集していない)
- 直読したファイル数: 40/40(全ファイルを直読。grepだけで済ませたファイルは0件)
- 検査の観点: グローバルCLAUDE.md「日本語命名の受理条件」8条件、「日本語命名の質」、「呼称と語の選択」、`_doc/計画/ユビキタス言語.md`との照合

## 違反の一覧

該当なし(0件)。

## 違反の疑い

1. **識別子内でのASCII語と日本語の連結について(条件2・「日本語命名の質」に対する疑い、断定しない)**
   - 対象箇所(代表例。同型が多数あるため一部のみ挙げる): `crates/blitz_render/src/vulkan/frame/cloth_types.rs:26-38`(`介入pipeline`・`積分pipeline`・`目標の確定pipeline`・`乗数零化pipeline`・`拘束pipeline`・`曲げ拘束pipeline`・`目標拘束pipeline`・`ハッシュ消去pipeline`・`ハッシュ格納pipeline`・`分離pipeline`・`床とカプセルの押し出しpipeline`・`仕上げpipeline`・`頂点生成pipeline`)、`crates/blitz_render/src/vulkan/frame/post_types.rs:9-14`(`前処理pipeline`・`縮小pipeline`・`拡大pipeline`)、`crates/blitz_render/src/vulkan/frame/types.rs:49`(`深度プリパスpipeline`)
   - なぜ疑いに留めるか: 「日本語の名詞+英語小文字の`pipeline`」という接続は、助詞を省いた膠着圧縮(「大気LUT生成指示」型)そのものではなく、Vulkanの生ハンドルを指すフィールド名に`pipeline`・`layout`・`device`・`memory`・`pool`・`sampler`のような裸の英語名を使う慣行がこのクレート全体(本区画の40ファイル全部を含む)で一貫して行われている。これは「生値に戻すのは境界1箇所のみ」の例外的境界表記として設計側が選んだ様式と読める。一方で同じファイル群の中に`コンピュートパイプライン`・`描画パイプライン`のように全部を片仮名で書く箇所(`types.rs:69-71`)も同居しており、書き分けの基準が明文化されていない。実体を誤読させる例ではなく、意味の復元自体は型・文脈から可能なため違反とまでは判定しないが、表記の一貫性については実装側の判断を仰ぐ価値がある
   - 改名の素案: 出さない(是正するかどうかも含めてリネーム員・親の判断に委ねる)

## 違反ゼロの範囲

`distant_environment`配下14ファイルと`frame`配下26ファイルの全識別子(型名・関連関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)について、受理条件8つ・日本語命名の質・呼称と語の選択との照合を行い、確定した違反は0件だった。実体と合わない語・名詞1語への圧縮・詰め込みすぎ・識別子への英字ASCII単語以外の記号(ギリシャ文字・数式記号)の混入は検出しなかった。省略した領域は無い(全40ファイルを直読した)。
