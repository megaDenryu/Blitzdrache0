# 命名検査報告: 区画115 (crates/blitz_sim/src/contact)

- 区画名と範囲: `crates/blitz_sim/src/contact` 直下の39ファイル(サブモジュール `pipeline/`・`non_penetration/`・`normal_tangential_system/`・`history/`・`island/`・`rest/`・`broad_phase/`・`query_origin/`・`body_body_contact/`・`body_static_contact*`・`scene/`・`velocity_stage/`・`static_friction/`・`symmetric_system/`・`contact_projection_row.rs`・`generation_margin.rs` は本区画の対象外であり別区画の担当と見なす)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の db815090(このツリーは編集していない)
- 直読したファイル数: 39/39(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 補足の網羅確認: ASCII混入の識別子を洗い出すため、本区画39ファイルに対して `剛体a|剛体b|第1の形|第2の形` と `を引く` のgrepを別途実行し、ヒットした箇所を直読済みの本文と突き合わせて判定した(この2件のgrepは、直読で読んだ本文の中の該当箇所を特定するための補助であり、検査そのものはファイル全文の直読で行っている)

## 違反の一覧

1. `crates/blitz_sim/src/contact/mixing_rule.rs:47`(および呼び出し側 `mixing_rule.rs:38-39`、`mixing_rule_builder_tests.rs:67`) | `表面物性を引く` | 条件7(直訳の検出質問) | `引く` は英語 lookup の直訳調であり、英語を知らない読み手には「取り出す・調べる」のどちらの動作かが名前から復元できない。プロジェクトCLAUDE.mdの「命名」節はこの語について訳語を既に確定しており(「lookup/sampleの『引く』→『参照する』」)、この確定済みの規約に反したまま実装が残っている | `表面物性を参照する`

2. `crates/blitz_sim/src/contact/body_body_contact_parameters.rs:18-19` | フィールド `剛体a` / `剛体b`(および同ファイル20-21行目の `局所接触点a` / `局所接触点b`) | 識別子へのASCII文字混入の禁止(オーナーが不合格にした実例の「識別子への英字・ギリシャ文字・記号の混入」) | 日本語の名詞へ英字1文字 `a`・`b` を直接連結しており、定義が挙げた禁止例(`段階A2の候補`・単独の`n`)と同型の混入である。同じファイル内で「第1の剛体」「第2の剛体」という完全な日本語の言い回しも使われており(エラーenumのフィールド名`第1の剛体の識別子`等)、英字1文字表記に切り詰める必然性が無い | `剛体第一` / `剛体第二`、`局所接触点第一` / `局所接触点第二`(または既存の「第1の」「第2の」という言い回しへ揃える)

3. `crates/blitz_sim/src/contact/batch_builder_push.rs:31-32,35,37,40,42-43,46,48-49,53-54,58-61`(冒頭コメント5-6行目も同様) | 引数名・ローカル変数名 `剛体a` / `剛体b` | 識別子へのASCII文字混入の禁止 | 上記2と同型。この工程全体で剛体を指す一時変数・引数がすべて英字1文字接尾辞で書かれており、同じファイルの中でも `第1の剛体の識別子`・`第2の剛体の識別子`(エラーenumのフィールド名)という完全な日本語表記が並行して使われているため、English字1文字表記へ切り詰める理由が無い | `剛体第一` / `剛体第二`(呼び出し元 `batch_builder_push.rs` 内の統一が必要なため、上の2とあわせて1回のリネームで揃えるべき)

## 違反の疑い

なし。上記3件はいずれも定義の文言(直訳の禁止・ASCII混入の禁止)に直接該当すると判断した。

## 違反ゼロの範囲

上記の3箇所(1つの直訳調・a/b接尾辞のASCII混入がまたがる2ファイル)を除く、残り36ファイルの全識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は受理条件8条・命名の質・呼称と語の選択のいずれにも違反しなかった。具体的には次の観点で確認した。

- 品詞の規律: 値は名詞句(`接触物性`・`摩擦係数`・`形の最小の厚み`等)、処理は動詞句「〜する」(`生成する`・`検査済みの係数から組む`・`薄い方を選ぶ`等)、真偽は「〜か」(`滑走中か`)で一貫していた
- 裸の動詞の禁止: `生成する()`はすべて型に属する関連関数であり型名が目的語を担っていた。ASCIIモジュール直下の自由関数(`閉じる`)も、そのモジュール内で完結する私有関数であり、文脈の識別子(引数名)だけで用途が読める形だった
- 1語1義: 「接触物性」と「表面物性」は別の型として明確に区別され、ユビキタス言語.mdの定義とも一致していた。「材質の識別子」と「静的世界の接触相手の識別子」も別の型として区別され、取り違えを型で防ぐ設計意図がコメントに明記されていた
- 直訳・膠着圧縮・漢字1文字圧縮: `表面物性を引く`以外に該当する識別子は無かった。助詞を省いた圧縮(例:「大気LUT生成指示」型)や、着手順・段階を1文字へ切り詰める中国語的圧縮も無かった
- 実体との照合: `接触を解く品質の設定`・`混合則の組み立て`・`接触点集合の占める範囲`等、複合的な概念を持つ型名についてユビキタス言語.mdの「剛体の語彙」節の定義と照合し、フィールドと定義の記述が一致することを確認した
