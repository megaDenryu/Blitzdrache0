# 命名検査報告 — 区画145「xtask/src/cluster_lights」+「xtask/src/command_ui/command_catalog」

- 区画番号: 145
- 対象: `xtask/src/cluster_lights`(9ファイル、うち`error/`配下1ファイル)、`xtask/src/command_ui/command_catalog`(29ファイル、うち`argument/`・`asset/`・`benchmark/`・`entry/`・`measurement/`の子ディレクトリを含む)
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、db815090時点。編集していない)
- 検査日: 2026-09-12
- 直読したファイル数: 38 / 38(全件)。grepだけで済ませたファイルは0件(省略なし)
- 読んだ必読リスト: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`の「命名」節、`_doc/計画/ユビキタス言語.md`(cluster_lights関連語・XPBD関連語を照合)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `xtask/src/cluster_lights/assignment.rs:36` | `取り出す`(fn) | 条件2・条件3・条件5 | `assignment`はASCIIモジュールであり補完力ゼロ。呼び出しは`assignment::取り出す(結果.報告())`(`scale.rs:63`)で、名前に目的語が無く「何を取り出すか」を単独で答えられない。同じファイルの`行一覧を取り出す`は目的語を持ち合格しているのに対し、この関数だけ目的語が落ちている(実体はクラスタ割り当ての統計を組み立てて返す処理) | `割り当ての統計を取り出す` |
| `xtask/src/cluster_lights/gpu_time.rs:31` | `取り出す`(fn) | 条件2・条件3・条件5 | `gpu_time`はASCIIモジュールで補完力ゼロ。呼び出しは`gpu_time::取り出す(結果.報告())`(`scale.rs:60`、`gpu_time_tests.rs:9`)で、「GPU時間の表から何を取り出すか」が名前に無い(実体は選別とシーン描画の2区間の中央値) | `区間の中央値を取り出す` |
| `xtask/src/cluster_lights/determinism.rs:30` | `確かめる`(fn) | 条件2・条件3・条件5 | `determinism`はASCIIモジュールで補完力ゼロ。呼び出しは`determinism::確かめる(&夜の実行環境)`(`cluster_lights.rs:52`)で、「何を確かめるか」が名前に無い(実体は同一起動内で撮った2枚の絵と割り当ての報告行の一致の確認) | `同一起動の2枚の一致を確かめる` |
| `xtask/src/cluster_lights/interior.rs:29` | `測る`(fn) | 条件2・条件3・条件5 | `interior`はASCIIモジュールで補完力ゼロ。呼び出しは`interior::測る(&屋内の実行環境)`(`cluster_lights.rs:54`)で、「何を測るか」が名前に無い(実体は屋内の灯ありと灯なしの平均輝度) | `屋内の明るさを測る` |
| `xtask/src/cluster_lights/scale.rs:33` | `測る`(fn) | 条件2・条件3・条件5 | `scale`はASCIIモジュールで補完力ゼロ。呼び出しは`scale::測る(&夜の実行環境)`(`cluster_lights.rs:53`)で、「何を測るか」が名前に無い(実体は光の件数を振って各件数のGPU時間と割り当てを測る処理) | `光の件数を振って測る` |

5件とも同じ形(`ASCIIモジュール::裸動詞`)であり、受理条件が不合格例として挙げる`create::生成する`と同型である。5件のうち`取り出す`は同ファイル内の`行一覧を取り出す`(目的語あり、合格)と対になっており、目的語の有無だけが違いになっている。

## 違反の疑い

| ファイル:行 | 識別子 | 疑いの条件 | 理由 |
|---|---|---|---|
| `xtask/src/command_ui/command_catalog/entry.rs:55` | `ascii名`(メソッド) | 条件「日本語命名の質」の頭字語混入 / 専門用語の表記 | `ASCII`は非専門家に一般に通用する語(UI・CPU・GPU・API等)の並びには挙げられておらず、初出の補足も無い。ただし「日本語のコマンド名」と対で使う値オブジェクト様式の識別子として`command_catalog`全体で一貫使用されており(`entry.rs`が正本)、意味は文脈から復元できる。断定はせず疑いとして残す |
| `xtask/src/cluster_lights/assignment.rs:15-25`ほか(`割り当ての統計`型) | `割り当ての統計` | 条件3(5W1Hの必要格) | ユビキタス言語の登録句は「クラスタ選別の割り当て統計」だが、型名は「クラスタ」の格が落ちている。`pub(super)`で`cluster_lights`モジュール配下だけに閉じ、フィールド名(`セル総数`・`宣言光数`等)が光とセルの文脈を補っているため実害は小さいと判断し、断定はしない |

## 違反ゼロの範囲

上記5件の違反と2件の疑いを除く、38ファイルの残り全識別子(型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝)は受理条件8つ・日本語命名の質・呼称と語の選択・専門用語の表記のいずれにも抵触しなかった。特に`command_catalog`配下29ファイルはコマンド一覧の値オブジェクト様式(`コマンド項目`・`引数定義`・`選択肢`・`省略したときの扱い`・`引数の構文`)で統一されており、動詞名はすべて目的語を伴っていた(`表を表示する`・`要約を組む`・`明るさの比を判定する`・`起動指定を組み立てる`等)。ASCII頭字語の使用は`GPU`・`XPBD`・`CPU`のみで、いずれも一般に通用する語(GPU/CPU)か、ユビキタス言語ファイルに初出の補足付きで登録済みの語(XPBD、`_doc/計画/ユビキタス言語.md:469`「拡張位置ベース動力学」)であり、条件違反ではない。識別子へのASCII文字・ギリシャ文字・数学記号の混入(段階A2・Σλ_n型)は1件も無かった。
