# 命名検査報告 — 区画127

- 区画名と範囲: `crates/blitz_sim/src/rigid_body`(32ファイル。`rigid_body/` 直下30ファイル + `rigid_body/body/` 配下2ファイル)
- 作業ツリーと時点: `C:/devs/Blitzdrache0_roller`(main の `db815090`。このツリーは編集していない)
- 直読したファイル数: 32/32(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`「剛体の語彙」節

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `rigid_body/ledger.rs:63`(使用箇所: `rigid_body/transition_tests.rs:51,78,89`) | `剛体一覧_mut` | 日本語命名の質「英語の頭字語・省略語を日本語の中に混ぜない」 | `_mut`はRustの`mutable`の省略語であり、Rustの`get`/`get_mut`慣習をそのまま持ち込んでいる。日本語しか読めない読み手はこの部分だけ復元できず、隣の`剛体一覧()`(不変参照を返す)との違いも名前からは分からない | `剛体一覧を可変で参照する` |
| `rigid_body/transition_tests.rs:82` | `剛体_静的` | 日本語命名の質「単語らしく見せるために助詞を省かない」 | アンダースコアで名詞と修飾語を接続し、語順も入れ替えている(自然な日本語は「静的な剛体」)。英語のsnake_case命名習慣を日本語識別子へそのまま持ち込んだ結合であり、自然な句になっていない | `静的な剛体` |
| `rigid_body/transition_tests.rs:93` | `剛体_動的` | 日本語命名の質「単語らしく見せるために助詞を省かない」 | 同上(「動的な剛体」の圧縮) | `動的な剛体` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑いの条件 | 理由 |
|---|---|---|---|
| `rigid_body/orientation.rs:73`(`_doc/計画/ユビキタス言語.md`「剛体の語彙」の`姿勢`の項にも同じ語で登録済み) | `境界用xyzw配列` | 識別子へのASCII文字混入の検出項目 | `xyzw`という英字4文字がそのまま日本語識別子の中へ埋め込まれている。ただし同じ型の`生成する(x, y, z, w)`の引数名と対応が取れ、GPU境界向けの成分順を表す一般的な表記(クォータニオンのxyzw)であり、ユビキタス言語ファイルへ既に登録済みの語でもある。実害(意味が復元できないか)は小さいと判断し、断定せず疑いとして報告する。改名するなら`境界用のxyzw成分配列`だが、登録語の変更は親の裁定を要する |

## 違反ゼロの範囲

上記2件(3箇所)を除く残り29ファイルの識別子(型名・列挙の枝・メソッド名・フィールド名・関数名・定数・テスト内のローカル変数を含む)は、受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも違反しなかった。特に次の点を個別に確認した。

- `action_accumulator.rs`・`action_point.rs`・`body_actions.rs`・`ledger_actions.rs`の力・衝撃・回転力・回転衝撃を扱う一連のメソッド名(`重心へ力を加える`・`点へ衝撃を加える`・`回転力を加える`等)は、動作(動詞句)と対象格(重心へ/点へ)を明示しており、5W1Hの必要格を満たす
- `body_id.rs`の`配列添字`は名前単独ではやや一般的だが、レシーバ`剛体の識別子`による補完で「この識別子の配列添字」と一意に読める(条件2の日本語レシーバ補完に該当)
- `mass_properties.rs`・`mass_properties_shapes.rs`・`principal_inertia.rs`の`第一`・`第二`・`第三`は、型`主慣性`のコメントで軸との対応が明記されており、実体(主慣性の3値のうちどれか)を予告する
- `wake_reason.rs`の6つの枝(`作用の投入`・`新しい接触`・`拘束の着脱`・`運動種別の遷移`・`構造変更`・`区域の読込の完了`)は、いずれも`_doc/計画/ユビキタス言語.md`の`休止から起きた理由`の定義文にある6条件の語と完全に一致し、1語1義を保っている
- `mod.rs`のモジュール宣言・`pub use`の並びはASCII(ファイル名・モジュール名)と日本語識別子(型名)の対応であり、ASCII側は補完力ゼロとして扱った上で、re-exportされる日本語名自体は単独で読めることを確認した

省略した範囲は無い(32ファイル全部を直読した)。
