# blitz_app まとまりとつながりの検査(第1区画)

- 検査員: 溶けた概念の分解員(Opus 5・effort medium)
- 対象: `crates/blitz_app` の `アプリ` 型と `app/` 配下54ファイル
- 検査日: 2026-09-04
- 時点のコミット: `78bb61cc`

## この報告は要約である(全文は失われた)

**検査員の報告の全文は残っていない。** 報告は親のセッションの文脈にしか置かれず、そのセッションが要約されたときに失われた。以下は `_doc/計画/検査の網羅台帳.md` へ記録した要約から復元したものである。

失われたのは、各概念が持つもの・見せるもの・見せないものの内訳と、工程20とフィールドの対応表と、規約への提案4件それぞれの根拠である。**是正の工事(Issue #45)はその内訳を必要とするため、着手前に分解員を走らせ直す。** 正本は分解員の定義(`.claude/agents/溶けた概念の分解員.md`)であって報告ではないため、同じかそれ以上の結果が得られる。

この事故を受けて、報告の全文をこのディレクトリへ保存することを `.claude/skills/ローラー検査/SKILL.md` の手順7へ入れた。

## 残っている結果

**網羅**: フィールド46と実数75メソッドを全件分類した。ツールの報告値は64であり、その差は下記の欠陥による。

**命名した概念(10件)**: 束の常駐台帳 / この描画の検収経路 / 実行の計器と終了時の報告 / 布の実演 / アニメーションの再生 / 据え付けを待つ起動指定 / 世界の素材と描画入力への写し方 / 実行の進み具合 / この世界の絵の作り方 / 開発用の重ね画面とそれが書き換えるつまみ

**分解の案**: 5階層(受け口8・描画の一巡13・検収6・計器4・据え付け5)。停止条件2つの充足を示した。第2階層をそれ以上細かく割らなかった根拠は、割ると3つの工程が互いを読み合って相互依存になることである。

**向きが書けないこと**: 下位モジュールが `&mut アプリ` を丸ごと受ける自由関数が21本ある(親のgrepでも19本を確認した)。ファイルは26へ分かれているが依存の向きは無い。

**46まで育った経路**: 検収の道具が1つ増えるたびに `アプリ` のフィールドが1つ増える形である。`9d13b947`・`f2c596af`・`93c332bb`・`77c4e399`・`df9ff728` の5コミットがすべて同じ形をしている。第3階層(検収の道具)を先に切ると、フィールド6とメソッド16が一度に出る。

**逃げ場の実績**: 独立と判断したのはフィールド9(20%)・メソッド35(47%)、名前が付かなかったのは `視点の履歴` 1件(2.2%)である。

**副産物**: `xtask/src/type_metrics/keyword.rs` の `修飾子一覧` が `pub(in crate::…)` を剥がせず、メソッド数を15%過少に数える欠陥を検出した(親が現物で確認した)。

**他の検査員へ回した2件**: `アニメ時刻` の置き場と、`画面へ重ねるui` の `Option`。どちらも既存規約の違反であり、この役の担当ではない。

## 規約への提案4件(すべて採用済み)

1. 切り出しの根拠から「親を丸ごと受け取る形」を外す → `CLAUDE.md`「切り出しの根拠義務」へ条文を追加(`87c6d8ba`)
2. `type-metrics` を「現状の値を上限として台帳登録し、増えたら落とす」形にする → `CLAUDE.md`「機械的強制」の但し書きを撤回(`87c6d8ba`)。機械検査の実装は別便
3. `type-metrics` の可視性の取りこぼしを直す → `xtask` の実装で対応
4. 「束ねてよい条件」を積極形で書く → グローバルCLAUDE.mdとその複製へ追加(`87c6d8ba`)
