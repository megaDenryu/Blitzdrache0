# 命名検査報告 区画034

- 区画名と範囲: `crates/blitz_asset_compiler/src/texture_storage`(サブディレクトリ`block_compression`・`mip_chain`を含む)、`crates/blitz_asset_compiler/src/vegetation`(サブディレクトリ`vegetation_tests`を含む)、`crates/blitz_asset_compiler/src/village/scene.rs`
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main、時点はdb815090。このツリーは編集していない)
- 直読したファイル数: 40/40(全ファイル)。grepだけで済ませたファイルは0件
- 検査手順: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「専門用語の表記」「呼称と語の選択」「日本語命名の質」「日本語命名の受理条件」と`CLAUDE.md`の「命名」節を読み、`_doc/計画/ユビキタス言語.md`のテクスチャ・植生関連の登録語(縮小段・テクスチャ格納方針・材質テクスチャ役割・BC1・RGBA8等)と照合した

## 違反の一覧

1. `crates/blitz_asset_compiler/src/vegetation/bounds.rs:70` | `距離`(関数名) | 条件1(受理テスト)・条件4(品詞の規律) | この関数は2点間の距離を計算する処理(処理=動詞句であるべき)だが、名前が裸の名詞「距離」だけである。「距離です」は処理の答えとして成立せず、「距離を求める処理です」と読めない。同じファイル内の`頂点の包囲を求める`・`群の包囲領域を求める`は同種の計算にすべて「〜を求める」を付けており、この関数だけが裸名詞になっている | `距離を求める`
2. `crates/blitz_asset_compiler/src/texture_storage/block_compression/rgb565.rs:8-10,43` | `赤の段数`・`緑の段数`・`青の段数`・`成分を段へ量子化する`の`段`/`段数` | 条件6(1語1義) | 「段」はこのクレート全体で「縮小段(mip level)」の訳語として確立している(`_doc/計画/ユビキタス言語.md`「縮小段」、`texture_storage`直下・`mip_chain`配下の全ファイルで「段: &テクスチャデータ」がmip levelを指す)。ところがこのファイルでは同じ「段」をRGB565の量子化ステップ値(0〜31/0〜63の整数)という別概念に使っている。同じディレクトリ木の中で読み手が「段=mip level」の類推を持ち込むと誤読する | `量子化段`(例: `赤の量子化段数`・`成分を量子化段へ量子化する`)
3. `crates/blitz_asset_compiler/src/texture_storage/block_compression/reference_decode.rs:38,44,48` | `段を取り出す`・`五ビットの段を8ビットへ広げる`・`六ビットの段を8ビットへ広げる`の`段` | 条件6(1語1義) | 上と同じ「段」のオーバーロード。RGB565の16ビット詰め値から取り出した5/6ビットの成分値(量子化ステップ)を「段」と呼んでおり、mip levelの「段」と衝突する | `量子化段`(例: `量子化段を取り出す`・`5ビットの量子化段を8ビットへ広げる`)
4. `crates/blitz_asset_compiler/src/vegetation/terrain_group.rs:17` | `原型id`(フィールド名) | 条件6(1語1義)/表記の統一 | フィールドの型は`アセットID`であり、同じ`_doc/計画/ユビキタス言語.md`・同クレート内の確立語(`アセットID`・`描画対象ID`・`テクスチャID`・`画像ID`)はすべて大文字の「ID」で終わる。このフィールドだけ小文字の「id」を名詞へ直接くっつけており、読み手は同じ概念の表記違いか別概念かを区別できない | `原型ID`

## 違反の疑い

1. `crates/blitz_asset_compiler/src/vegetation/placement.rs:55`、`crates/blitz_asset_compiler/src/vegetation/terrain_placement.rs:65` | `y軸回転を作る`(関数名) | ASCII混入の禁止(オーナーが不合格にした実例の「識別子への英字混入」) | 日本語識別子の中にASCIIの`y`が混ざっている。座標軸のX/Y/Zは`_doc/計画/ユビキタス言語.md`の外(`crate::placement::deterministic_value`の`用途::X方向の揺らぎ`等、本区画の外のファイル)で同種の表記が既に使われており、断定は避けるが、軸名の英字1文字を許容してよいかは方針判断が要る。改名案(採る場合): `縦軸回転を作る`または`鉛直軸回転を作る`
2. `crates/blitz_asset_compiler/src/texture_storage/bake_by_policy.rs:14` | `不透明の不透明度`(定数名) | 条件1(受理テスト) | 「不透明の不透明度」は「不透明度の不透明度」のように読め、単独では「不透明とみなす不透明度の閾値(255)」という実体が伝わりにくい。ただしコメント(直上の`///`)が実体を補っており、致命的な誤読には至らない可能性がある | `不透明とみなす不透明度の値`または`完全不透明の不透明度`

## 違反ゼロの範囲

上記4件の違反と2件の疑いを除く、区画034の40ファイルすべての型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝(BC1の符号化工程一式、縮小段の生成工程一式、植生のインスタンス群・配置・境界算出・各検収シーンの組み立て工程、見本の集落のシーン組み立て)は受理条件8つ・日本語命名の質・呼称と語の選択のいずれにも抵触しなかった。ユビキタス言語に登録済みの語(縮小段・テクスチャ格納形式・テクスチャ格納方針・材質テクスチャ役割・BC1・アセットID等)との実体照合でも食い違いは見つからなかった。BC1・RGBA8・sRGBのような小文字/大文字混在のASCII語幹は、ユビキタス言語が「Vulkan仕様書の綴りのまま維持する第3群語彙」と定めた方針に沿っており、助詞「の」を伴う自然な日本語文へ組み込まれているため違反としなかった。
