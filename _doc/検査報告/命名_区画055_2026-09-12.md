# 命名検査報告: 区画055

区画名: `crates/blitz_engine/src/asset/runtime_format/scene/write_element`(全域ローラー検査の一部)
範囲: `block_055.txt`に列挙された40ファイル(`write_element`配下17ファイル、`surface_layer_texture_set_v1`の読み書き2ファイル、`runtime_scene_instance_tests`3ファイル、`runtime_scene_multi_material_tests`7ファイル、`scene_file/content.rs`1ファイル、`texture_storage`配下10ファイル)
時点: 作業ツリー`C:/devs/Blitzdrache0_roller`のmain、コミット`db815090`(このツリーは編集していない)
直読したファイル数: 40/40(全数直読。grepだけで済ませたファイルは0)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| `crates/blitz_engine/src/asset/runtime_format/scene/write_element/material.rs:18` | `スロット0を指す` | 条件4(品詞の規律) | `.is_some()`の結果を受けるbool値であり、受理条件が真偽値に定める「〜か」の形になっていない。宣言だけを読むと「スロット0を指す」という動作の説明文に見え、真偽の判定結果であることが名前から分からない | `スロット0を指すか` |
| `crates/blitz_engine/src/asset/runtime_format/scene/write_element/mesh_primitive.rs:28` | `全体を塗る単一プリミティブ` | 条件4(品詞の規律) | 複数条件の論理積の結果を受けるbool値だが、「〜か」を持たない体言止めの名詞句になっている。「全体を塗る単一プリミティブ(という物)」という値の名前に読め、真偽値だと単独では判定できない | `全体を単一プリミティブで塗るか` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| `crates/blitz_engine/src/asset/runtime_format/scene/write_element/version_codec.rs:16-18`(構造体`版ごとの要素の書き方`のフィールド`テクスチャ1件`・`材質1件`・`静的物理形状の節`)、および同ファイル60・64・68行の同名アクセサ | 条件4(処理を保持するフィールド=動詞句のまま) | 3つとも値の型は`fn(...)`を包んだ「〜を書く工程」型(`テクスチャを書く工程`等)であり、実体はコールバックとして後で呼び出す処理である。フィールド名・アクセサ名は動詞を落とした体言(「テクスチャ1件」=データの単位のように読める)になっており、「記録」がVulkanコマンド積み込み処理を名詞形で持って計器と誤読された経緯と同型に見える。ただし本件は隣接する型注釈が同じ日本語で「〜を書く工程」と明示しており(`テクスチャ1件: テクスチャを書く工程`)、フィールド宣言を型注釈込みで読む読み手には処理であることが伝わる余地があるため、断定でなく疑いとして報告する。改名するなら`テクスチャを書く工程`・`材質を書く工程`・`静的物理形状の節を書く工程`のように型名と揃える案がある |

## 違反ゼロの範囲

上記2件の違反と1件の疑いを除く、40ファイルの残り全識別子(型名・関連関数・メソッド・フィールド・変数・定数・列挙の枝)は受理条件8つ、日本語命名の質(頭字語・直訳調・膠着圧縮の禁止)、呼称と語の選択(漢字1文字への切り詰めの禁止)のいずれにも抵触しなかった。ASCII英字・ギリシャ文字・数学記号を識別子へ混入した例も無かった(u32・f32・usize等のプリミティブ型名は日本語識別子ではないため対象外として扱った)。

`版4`・`版5`・`版6`・`版7`のような「版+数字」の名は、`_doc/計画/ユビキタス言語.md`で「版」がバージョン管理の完全な語として繰り返し登録・使用されていることを確認した上で、「着手順4→順4」のような単漢字への切り詰めとは別物(「版」自体が完結した1語)と判定し、違反に含めなかった。

`書込先`・`読取位置`・`格納済みテクスチャ`・`縮小段の寸法`・`ブロック格子`等の値オブジェクト、および型に属する`生成する()`系の関連関数群(`材質スロットID::生成する()`等)は、型名が目的語を担う値オブジェクト様式として受理条件5に照らして合格と判定した。
