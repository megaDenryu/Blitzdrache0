# 命名検査報告 区画132

- 区画名: crates/editor_server/src/export/one_building 他(mother_height_cutout・normalized_app・resource)
- 範囲: 区画番号132、対象37ファイル(block_132.txt に列挙された全ファイル)
- 対象リポジトリ・時点: 作業ツリー C:/devs/Blitzdrache0_roller、main の db815090(このツリーは編集していない)
- 直読したファイル数: 37/37(全ファイルを直読。grepだけで済ませたファイルは0件)
- 使用した必読リスト: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」節、`_doc/計画/ユビキタス言語.md`

## 違反の一覧

該当なし(0件)。

## 違反の疑い

該当なし(0件)。検討したが違反としなかった候補を記録する。

- `crates/editor_server/src/normalized_app/mod.rs:31` の型名`経路正規化アプリ`と、`chunk_height_sample.rs`他が使う`高さ関数材料`: いずれも「呼称と語の選択」のカタカナ語基準・実体一致の観点で吟味したが、`アプリ`は`_doc/計画/ユビキタス言語.md`に多数の既登録用例があり(例: 10行目「エディタ」の定義文、481行目・932行目等)、小学生にも意味の通るカタカナ語として扱ってよい既定語であるため違反としなかった。`高さ関数材料`は`Fn(i16, i16) -> f32`の実体を保持しつつ`高さを求める`メソッドを持つ構造体であり、ファイル冒頭コメントの説明(「大域高さ関数の材料」)と実装が一致しているため名詞形でも合格とした
- `chunk_height_sample.rs`の`高さ関数材料`のフィールド`チャンクx`・`チャンクz`、`chunk_coordinate.rs`の`チャンク座標`のフィールド`x`・`z`: x/y/zをASCII文字として識別子に混ぜている。座標軸のx/y/zはユビキタス言語内の`位置3次元`・`チャンク座標`など既存の多数の型が同じ規約で使っており、区画132が新規に持ち込んだ用法ではないため、本区画の違反としては報告しない(座標軸表記そのものの是非は用語定義側の既定路線であり、この検査の対象を超える)

## 違反ゼロの範囲

区画132の全37ファイル(`export/one_building/`3件、`mother_height_cutout/`5件、`normalized_app/`4件、`resource/`25件)を直読し、型名・関数名・メソッド名・フィールド名・変数名・定数名・列挙の枝のすべてについて受理条件8条・命名の質・呼称と語の選択・ユビキタス言語との照合を行った結果、違反は0件だった。特に「オーナーが不合格にした実例」の3形式(実体と合わない語の圧縮・名詞1語の圧縮・詰め込みすぎ)、およびASCII英字・ギリシャ文字・記号の識別子混入は、上記の疑い2件(座標軸x/y/zの慣用表記)を除いて検出しなかった。各型の冒頭コメント・フィールド名・検証メソッドの名前(`検証する`)・エラー枝の名前は、いずれもユビキタス言語の定義(チャンク構造・見下ろし図の下書き・粗マスの塗り・等高線・建物の配置・建物定義ID・散布の設定・散布の個体・広域道路・チャンクの道路・世界の区画割り・大域世界構造・マザーハイトマップ等)と一致した。
