# 命名検査報告: 区画142(xtask/src/acceptance)

- 区画名と範囲: `xtask/src/acceptance` 配下40ファイル(トップレベル27ファイル+`app_launch/`3ファイル+`error/`5ファイル+`error/judgment_break/`1ファイル+`exit_report/`1ファイル+`judgment_name/`2ファイル+`judgment_name/order_imposition/`1ファイル)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、main の `db815090`
- 直読したファイル数: 40/40(全ファイルを`cat -n`で全文直読。grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`

## 総評

この区画は検収(acceptance)の共通語彙を型に閉じた設計であり、命名は総じて受理条件をよく満たしている。値オブジェクト・セッション型・操作サービス・型付きエラーのいずれも、フィールド名と識別子が実装の実体と一致しており、「オーナーが不合格にした実例」の3形(実体と合わない語・名詞1語の圧縮・詰め込みすぎ)に該当する明確な違反は検出しなかった。ASCII・ギリシャ文字・記号の混入も、外部ツール名(cargo)・登録済み専門用語(HDR)・モジュールファイル名(ASCII規約に従うもの)を除き見当たらない。

検出したのは、対になる2つのメソッドの一方だけが対象を明示し、もう一方が無qualifierで読み手に「どちらの対象か」を型定義まで見ないと復元させない、という条件1・3の軽微な違反1件と、疑いの水準の2件である。

## 違反の一覧

1. `xtask/src/acceptance/exit_report.rs:370` | `本文(&self) -> &str`(戻り値は`self.標準出力`) | 落ちた条件: 1(受理テスト)・3(5W1Hの必要格) | なぜ読めないか: `終了時報告`型は`標準出力`と`標準エラー`の2つの本文を持つ。同じ型に`標準エラーの本文(&self)`という対になるメソッドが存在するため、無qualifierの`本文`は「標準出力の」を暗黙の既定として頼っており、この名前だけを見た読み手は「どちらの本文か」を復元できない(コメントを読んで初めて標準出力だと分かる)。5W1Hのうち対象格(何の本文か)が欠落している | 改名の素案: `標準出力の本文`(対になる`標準エラーの本文`と対称にする)

## 違反の疑い

1. `xtask/src/acceptance/exit_report.rs:360` | `画面へ流す(&self)`(中身は`print!("{}", self.標準出力)`) | 条件1・3に対する疑い | 同じ型に`標準エラーを画面へ流す(&self)`という対になるメソッドがあり、無qualifierの`画面へ流す`が標準出力だけを指すのか両方を指すのか、名前だけからは決まらない。上の`本文`と同型の非対称だが、こちらは副作用(印字)であり実害(誤読による誤ったコードの実行)がやや小さいため、断定でなく疑いとして分ける | 改名の素案: `標準出力を画面へ流す`(対称化)
2. `xtask/src/acceptance/readback_image.rs:38` | フィールド `rgba8: Vec<u8>` | 条件2に対する疑い(ASCII識別子の私有フィールドとしての妥当性) | 兄弟の型`圧縮前のHDR画像`(readback_hdr_image.rs)は同じ役割の画素データ列を`成分列: Vec<f32>`という日本語名で持つのに対し、こちらは`rgba8`という英数字混じりの生の識別子をそのまま使っている。RGBA8はグラフィックスの画素形式の固定名称であり、LUTのような抽象頭字語とは性質が違うため直ちに違反とは言えないが、同じ区画内で同種の概念に日本語名と英数字名が混在している点は1語1義の観点で気になる。私有フィールドであり公開APIには出ないため実害は小さい | 改名の素案: `画素バイト列`または`RGBA8バイト列`(型内で完結する私有フィールドのため必須ではない)

## 違反ゼロの範囲

上記2件(1違反+2疑い)を除く、区画142の残り37ファイルの識別子(型名・enum枝・メソッド名・フィールド名・変数名・定数名)はすべて受理条件8つを満たしていることを確認した。特に以下の設計は明示的に条件を満たす良い例として確認した。

- `アプリの起こし方`・`描き終わりの決め方`・`書き出しの形式`・`判定の破れの種類`など、enumの枝がすべて動詞句または名詞句で状態・処理の実体と一致している
- `世界を描く起動から作る`・`報告の求めから作る`・`定数から生成する`・`組み立てた綴りから生成する`など、同型の複数ファクトリーが材料の格で正しく区別されている(条件5の模範)
- `検収エラー`・`判定の破れ`・`読み戻しの破れ`・`割合の破れ`・`報告の読み取りの破れ`の枝が、フィールド名も含めて「何が破れたか」を単独で答えられる
- `画像の幅`・`画像の高さ`・`画素の横位置`・`画素の縦位置`・`画素の番号`・`画素の占める割合`など、数学DDDに沿った次元別のnewtypeが取り違えを型で防いでいる

## 検査の限定事項

- 本検査は識別子の命名だけを見た。設計の切り出しの妥当性(切り出しの根拠義務)・1ファイル100行原則・コメント規約は対象外である
- `mod comparable`(judgment_value.rs内)・`mod parse_tests`(readback_size.rs内)等、区画142のファイル一覧に含まれないサブモジュールの中身は検査していない(ブロック一覧の40ファイルのみが対象)
