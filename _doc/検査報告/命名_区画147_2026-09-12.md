# 命名検査報告 区画147: xtask/src/conform

- 区画名と範囲: xtask/src/conform 配下の40ファイル(block_147.txtに列挙された一覧そのもの。サブディレクトリのtable.rs・ledger.rs等の子モジュールは区画の対象外)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main、時点 db815090。このツリーは編集していない)
- 直読したファイル数: 40/40(grepだけで済ませたファイルは0件)
- 必読: `.claude/global-reference/全プロジェクト共通CLAUDE.md`「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`「命名」、`_doc/計画/ユビキタス言語.md`(「口」の用例をgrepで確認)

## 違反の一覧

### 系統的違反: ASCIIモジュール直下の裸の動詞`検査する`(条2・条3・条5)

このクレートの各検査ファイルはASCIIファイル名のモジュールであり、呼び出し側は`allow_lint::検査する(...)`のように英語モジュール名越しに呼ぶ。受理条件2は「英語(ASCII)識別子は読まれないものとして扱い、ASCIIモジュール直下の自由関数は名前単独で答える」と定めるが、次の12件は`検査する`という名前だけが単独で立っており、何を検査するかという目的語(条3「何を+どうする」)を持たない。受理条件5が挙げる`create::生成する`型の不合格例と同型である。

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| xtask/src/conform/allow_lint.rs:30 | `検査する`(pub fn) | 2・3・5 | `allow_lint::検査する(パス, 内容)`と読むとき、モジュール名は読まれない前提のため「何を検査するか」が名前に無い | `不正なallowの緩和を検査する` |
| xtask/src/conform/declaration_comment_line.rs:15 | `検査する`(pub fn) | 2・3・5 | 同上。宣言の間のコメント行という対象が名前に無い | `宣言の間のコメント行を検査する` |
| xtask/src/conform/doc_reference.rs:23 | `検査する`(pub fn) | 2・3・5 | 同上。参照パスの実在という対象が名前に無い | `参照パスの実在を検査する` |
| xtask/src/conform/drop_impl.rs:34 | `検査する`(pub fn) | 2・3・5 | 同上。Drop実装の配置という対象が名前に無い | `Drop実装の配置を検査する` |
| xtask/src/conform/forbidden_strings.rs:32 | `検査する`(pub fn) | 2・3・5 | 同上。禁止語・経緯語・絵文字という対象が名前に無い | `禁止語と絵文字の混入を検査する` |
| xtask/src/conform/line_count.rs:27 | `検査する`(pub fn) | 2・3・5 | 同上。行数の上限という対象が名前に無い | `行数の上限超過を検査する` |
| xtask/src/conform/module_import_boundary.rs:19 | `検査する`(pub fn) | 2・3・5 | 同上。取り込みの境界という対象が名前に無い | `取り込みの境界を検査する` |
| xtask/src/conform/reload_without_device_wait.rs:19 | `検査する`(pub fn、引数0) | 2・3・5 | 引数も無いため対象を推測する手がかりがさらに少ない。GPU全作業完了待ちという対象が名前に無い | `シーン差し替えのGPU全作業完了待ちを検査する` |
| xtask/src/conform/rigid_raw_triplet.rs:21 | `検査する`(pub fn) | 2・3・5 | 同上。単精度3つ組の宣言という対象が名前に無い | `剛体モジュール群の単精度3つ組宣言を検査する` |
| xtask/src/conform/single_lighting_slot_write.rs:24 | `検査する`(pub fn、引数0) | 2・3・5 | 引数も無い。照明問い合わせスロットへの書き込み元という対象が名前に無い | `照明問い合わせスロットへの書き込み元を検査する` |
| xtask/src/conform/test_directory_layout.rs:22 | `検査する`(pub fn) | 2・3・5 | 同上。testsの直置きという対象が名前に無い | `試験ファイルの直置きを検査する` |
| xtask/src/conform/two_tier_fold_boundary.rs:67 | `検査する`(pub fn) | 2・3・5 | 同上。二段の位置の口の呼び出し境界という対象が名前に無い(同ファイル内の`限定する口`型のメソッド版`self.検査する`はレシーバが型名で補完されるため合格。この67行目のモジュール直下版だけが不合格) | `二段の位置の口の呼び出し境界を検査する` |

### 裸の動詞`集める`(条2・条3・条5)

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| xtask/src/conform/whole_repository.rs:16 | `集める`(pub fn、引数0) | 2・3・5 | `whole_repository::集める()`と読むとき、モジュール名は読まれない前提。「複数ファイルを横断する検査の違反」という対象が名前に無く、単独では「何を集めるのか」が不明 | `複数ファイルを横断する検査の違反一覧を集める` |

### 真偽値なのに「か」を欠く(条4)

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| xtask/src/conform/allow_lint.rs:20 | `ディレクトリで許容`(let、bool) | 4 | 名前が「ディレクトリによる許容(という値)」という名詞句に読め、真偽値であることが品詞から予告されない | `ディレクトリで許容されるか` |
| xtask/src/conform/allow_lint.rs:23 | `ファイル名で許容`(let、bool) | 4 | 同上 | `ファイル名で許容されるか` |
| xtask/src/conform/allow_lint.rs:31 | `パス許容`(let、bool) | 4 | 「パスの許容(状態)」という名詞句に読め、真偽値であることが品詞から予告されない | `パスが許容されるか` |

## 違反の疑い(断定しない)

| ファイル:行 | 識別子 | 疑う条件 | 理由 |
|---|---|---|---|
| xtask/src/conform/allow_lint.rs:15 | `不正allowを含むか` -> `Option<&'static str>` | 4 | 名前は「〜か」で真偽値を予告するが、戻り値は一致した語そのものを運ぶ`Option`である。`.filter(|_| ...)`という使い方に慣れた読み手には自然だが、品詞の規律(真偽=「〜か」)を厳密に当てると値を運ぶ関数を真偽の形で名乗っている |
| xtask/src/conform/forbidden_strings.rs:16 | `行に禁止語を含むか` -> `Option<&'static str>` | 4 | 同上の理由 |
| xtask/src/conform/forbidden_strings.rs:20 | `行に経緯語を含むか` -> `Option<&'static str>` | 4 | 同上の理由 |
| xtask/src/conform/allow_lint.rs:32 | `cfg_testを見た`(mut bool) | 4 | 真偽値のフィールドだが「か」を欠く。完了形の動詞のため文脈では読み取れるが、品詞の規律を厳密に適用すると要修正 |
| xtask/src/conform/forbidden_strings.rs:35 | `経緯語を見つけた`(mut bool) | 4 | 同上の理由 |
| xtask/src/conform/type_metrics_ledger.rs:38 | `照合する`(private fn) | 3 | 同一ファイル内の呼び出しのみで、`計測: &走査範囲の型計測`という引数名から対象を推測できるが、名前自体は「何と何を照合するか」を持たない。同ファイル内の`計測1件を照合する`は対象を持ち合格している対比がある |
| xtask/src/conform/type_metrics_ledger.rs:77 | `消えた型の違反`(fn) | 4 | 関数(処理)なのに名詞句で命名されている。`.map(消えた型の違反)`という値変換の用法のため許容の余地はあるが、品詞の規律(処理=動詞句)を厳密に当てると疑いが残る |
| xtask/src/conform/type_metrics_ledger.rs:86 | `引き当てられなかった実装ブロックの違反`(fn) | 4 | 同上の理由 |

## 違反ゼロの範囲

上記に挙げた識別子を除く、区画147の40ファイルの残り全識別子(型名・フィールド名・列挙の枝・大多数のメソッド名・変数名・定数名)は受理条件8つ・日本語命名の質・呼称と語の選択・識別子へのASCII文字/ギリシャ文字/記号の混入のいずれにも抵触しない。特に「型名::関連関数()」「変数.メソッド()」の形の日本語補完は随所で機能しており(例: `違反::行単位`・`違反::ファイル単位`・`限定する口`型のメソッド`検査する`・`許可するファイルか`)、値オブジェクト様式の`生成する`系はこの区画に無い。「口」という語はユビキタス言語で「読み口」「書き口」等として登録済みの比喩であり、`限定する口`・`畳む口`・`読み口`は定着語の用法内である。
