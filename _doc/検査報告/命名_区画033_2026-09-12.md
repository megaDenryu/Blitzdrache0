# 命名検査報告: 区画033 crates/blitz_asset_compiler/src/source_asset_paths ほか

- 区画番号: 033
- 区画名: crates/blitz_asset_compiler/src/source_asset_paths(一覧ファイルには static_physics・surface_material・terrain も含む、計39ファイル)
- 作業ツリー: C:/devs/Blitzdrache0_roller(main、db815090時点。このツリーは編集していない)
- 検査日: 2026-09-12
- 直読したファイル数: 39/39(全ファイルを直読した。grepだけで済ませたファイルは0件)
- 読んだ必読リスト: `.claude/global-reference/全プロジェクト共通CLAUDE.md`の「日本語命名の受理条件」「日本語命名の質」「呼称と語の選択」「専門用語の表記」、`CLAUDE.md`の「命名」節、`_doc/計画/ユビキタス言語.md`(地表材質・地表層テクスチャ集・スカート・LOD・ソースアセットの相対パス/ファイル名等の登録語を照合に使った)

## 直読したファイル一覧(39件)

source_asset_paths/{file_name,known_paths,relative_path,spelling,tests}.rs、
static_physics/{builder_tests,building_builder,error,part_box_table}.rs、
surface_material/{layer_texture_set,weight_texture,weight_texture_tests,weights}.rs、
surface_material/layer_texture_set/{bake_choice,compile,error,layer_assignment,tile_directory}.rs、
surface_material/weights/{error,format_tests,grid,read,specification,write}.rs、
terrain/{ground_height,ground_object,lod_bake,lod_level,skirt,skirt_depth,surface,terrain_tests,vertex}.rs、
terrain/terrain_tests/{boundary,fixture,ground_height,ground_height_surface,skirt,structure}.rs

## 違反の一覧

1. `crates/blitz_asset_compiler/src/terrain/skirt_depth.rs:67` | `f32から求める` | 条件1(受理テスト)・条件3(5W1Hの必要格) | 関数は`値: i32`を受け取り`f32`を返す(`i16`経由で狭めてから`f32::from`する)。名前の格助詞「から」は「f32が起点(入力)である」と読ませるが、実際は逆でf32は出力側であり入力はi32である。直上の`///`コメント自身が「格子添字を実数へ変換する」と書いており、名前とコメントが矛盾している。同じファイル内の`vertex.rs`の類例`格子添字を実数へ変換する`(を+へで方向が明確)と比べても、この名前だけは方向が逆転して読める。名前だけを見た読み手は「f32型の値から何かを求める」と誤読し、実際の入力型・変換方向を復元できない。改名の素案: `格子添字をf32へ変換する`(または`vertex.rs`の命名に揃えて`値をf32へ狭めて変換する`)

## 違反の疑い

- `blitz_engine::地形LODメッシュ群`(`terrain/ground_object.rs`・`terrain/lod_bake.rs`等が使う型名)は日本語の中にASCIIの頭字語「LOD」を混ぜている。ただし`_doc/計画/ユビキタス言語.md`に`LOD (Level of Detail)`として明示的に登録済みであり、この区画のファイルはこの型を定義しておらず`blitz_engine`から輸入して使っているだけである。区画033の識別子として新規に導入されたものではないため違反とは断定せず、疑いとして記録するにとどめる(是正が要るなら`blitz_engine`側の型定義を触る作業のついでに検討する)

## 違反ゼロの範囲

上記1件(および関連する疑い1件)を除く39ファイル全域で、型名・関数名・メソッド名・フィールド名・変数名・定数名・enumの枝のいずれにも、実体と合わない語・名詞1語への圧縮・助詞を省いた膠着圧縮・裸の動詞・1語2義・直訳調・ASCII/ギリシャ文字/数学記号の混入は見つからなかった。値オブジェクト様式(`ソースアセットのファイル名`・`ソースアセットの相対パス`・`地表材質の重み格子`・`地表材質の重み格子諸元`等)の`生成する()`は型名が目的語を担うため受理条件5に適合し、真偽を返す関数(`spelling.rs`の`空か`・`斜線を含むか`等)はすべて「〜か」の形を守っていた。エラー型の枝(`静的物理形状のコンパイルエラー`・`地表材質の重み格子エラー`・`地表層テクスチャ集コンパイルエラー`)は、破られた不変条件を日本語の文としてそのまま書いており、実体との不一致は見当たらなかった。
