# 命名検査報告: 区画011(crates/blitz_app/src/game)

- 区画名と範囲: crates/blitz_app/src/game 配下、`block_011.txt` に列挙された39ファイル(直下24ファイル + entity_ledger/5ファイル + fox_tour/5ファイル + walk_only/1ファイル + world_shape_port/3ファイル)
- 時点: 作業ツリー `C:/devs/Blitzdrache0_roller`、`main` の `db815090`(このツリーは編集していない)
- 直読したファイル数: 39/39(全ファイルを直読。grepだけで済ませたファイルは無い)
- 検査した識別子: 型・enumの枝・関数・メソッド・フィールド・変数・定数を全ファイルにわたって走査した(概算で400識別子超)

## 違反の一覧

1. `crates/blitz_app/src/game/instrument.rs:21` | `完全性で動かなかった刻みの数`(フィールド) | 条件1(受理テスト)・条件7(直訳/意味復元の失敗) | 「AでBした」という日本語の「で」は原因・手段を表す通常の用法であり(例: 「事故で電車が遅れた」)、この名前は素直に読むと「完全性という状態が原因でその刻みが動かなかった」と読める。ところが実体は逆で、同じファイルの行末コメント「問い合わせが評価できない領域を含み、位置も移動状態も動かさなかった刻みの累計」が示すとおり、動かなかった原因は**不完全**な評価(`掃引の完全性::評価できない領域を含む`)である。名前の語(完全性)が指す方向と実体の条件(不完全性)が逆転しており、コメント無しでは正反対の意味に復元されうる | `評価未完で動かなかった刻みの数` または `評価できない領域を含み動かなかった刻みの数`
2. `crates/blitz_app/src/game/instrument.rs:35` | `完全性で保った描画の数`(フィールド) | 条件1・条件7 | 同型の逆転。実体は「掃引の答えが評価できない領域を含み、前の表示距離を保った描画の累計」(行末コメント)であり、保った原因は不完全な評価である。名前は「完全性で(＝完全性ゆえに)保った」と読め、実体の不完全性と逆の印象を与える | `評価未完で保った描画の数`
3. `crates/blitz_app/src/game/camera_wiring.rs:33`(フィールド定義)、`44`(初期化)、`77`(更新)、`102`(計器への引き渡し) | `完全性で保った描画の数` | 条件1・条件7 | 上記2と同じ実体(`プレイヤーカメラの配線`が持つ累計)。ここでの意味は「`遮蔽の判定::評価できず前の表示距離を保った`」場合に加算される値であり、原因はやはり不完全な評価であって「完全性」そのものではない | 同上
4. `crates/blitz_app/src/game/movement_record.rs:22`(フィールド定義)、`32`(初期化)、`42`(更新、2箇所)、`60`(計器への引き渡し) | `完全性で動かなかった刻みの数` | 条件1・条件7 | 上記1と同じ実体(`移動の観測の記録`が持つ累計)。原因は`観測.完全性で動かなかったか`(blitz_game側の真偽フィールド。本区画の外にあるため直接の改名対象ではないが、この区画側の累計フィールド名がその意味を引き継いで同じ逆転を持つ) | `評価未完で動かなかった刻みの数`

上記1〜4は同一の言い回し(「完全性で」)が2つの独立した概念(移動の刻み・カメラの描画)へ同型で使われており、いずれも「完全性」という語が実際には「不完全性」を指す逆転構造を共有している。1箇所の誤りでなく、命名パターンとして複製された誤りであるため、是正は4箇所まとめて行うのが妥当である。

## 違反の疑い

- なし。上記の「完全性で」問題は行末コメントとの照合で実体との不一致が明確なため、疑いでなく違反として計上した。

## 違反ゼロの範囲

上記の「完全性で」パターン(instrument.rs・camera_wiring.rs・movement_record.rsの計3ファイル、フィールド定義+使用箇所)を除く、区画011の残り36ファイル、および該当3ファイル内の他のすべての識別子(型・enumの枝・関数・メソッド・変数・定数)は、受理条件8項目・日本語命名の質・呼称と語の選択のいずれにも違反しなかった。ASCII・ギリシャ文字・数学記号を識別子へ混入した箇所も無かった(`エンティティid`・`束id`の小文字`id`はASCIIだが、ユビキタス言語で既に確立された「ID」の慣用であり全プロジェクト共通CLAUDE.mdの「専門用語の表記」の許容範囲内と判断した)。「実体と合わない語」「名詞1語の圧縮」「詰め込みすぎ」の3形について個別に確認したが、上記の「完全性で」以外に該当する識別子は見つからなかった。
