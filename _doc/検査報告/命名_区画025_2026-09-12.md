# 命名検査報告 区画025

- 区画: `crates/blitz_asset_compiler/src/compile` 他(一覧は `block_025.txt` の30ファイル。実際の配置は `distant_terrain/`・`edge_agreement/`・`editor_chunk/`・`compile/` の4サブディレクトリにまたがる)
- 範囲: 30ファイル
- 作業ツリー: `C:/devs/Blitzdrache0_roller`(main, db815090時点。ツリーは編集していない)
- 時点: 2026-09-12
- 直読したファイル数: 30/30(grepだけで済ませたファイルは0件。全ファイルを冒頭から末尾まで通読した)

## 違反の一覧

1. `crates/blitz_asset_compiler/src/distant_terrain/statistics.rs:14-15,26,35-36,48-59,63` | `p95`・`p99`(構造体`遠景の沈み統計`のフィールド) | 受理条件2(ASCII識別子は補完力ゼロ)・条件7(直訳/専門記号の検出) | 「p95」「p99」はASCIIアルファベットと数字を組み合わせた統計記号であり、日本語の文脈中に英字と算用数字がそのまま混ざる。この2文字だけでは「パーセンタイル」という語も「95」が何の単位かも日本語として読めず、英語の統計慣習を知らないと復元できない。同じファイルの`百分位`という関数名・`中央値`というフィールド名は正しく日本語化されているのに、この2つだけ生の記号が残っている(1語1義でも表記の統一が破れている) | `第九十五百分位`・`第九十九百分位`(または`パーセンタイル95`のように少なくとも一部を仮名にする折衷ではなく、既存の`中央値`に揃えて完全な日本語表記にする) — 注: 同型の`p50`・`p95`・`p99`は`blitz_app`側(`frame_timing.rs`・`reports.rs`等)にも広く存在し、本区画固有の新規違反ではなく既存の全域的な表記慣習である。是正は本区画だけでなく該当箇所を横断する作業になる
2. `crates/blitz_asset_compiler/src/distant_terrain/vertex.rs:39,62,77` | `面上のuv`(構造体`頂点の素材`のフィールド) | 受理条件2・条件7 | 同じ構造体の他のフィールド(`東座標`・`高さ`・`南座標`・`東方向の高低差`等)はすべて完全な日本語であるのに、このフィールドだけ「面上の」という日本語の助詞句にASCIIの`uv`を直接連結している。英語のテクスチャ座標の慣習(u,v)を知らない読み手は`uv`が何を指すか単独では復元できず、同じ構造体内で命名の一貫性も破れている | `面上のテクスチャ座標`(値は`[f32; 2]`のまま。境界(GPUへ送る`メッシュ頂点属性.uv`)への変換は`頂点属性へ書き出す`の中で1回だけ生のフィールド名`uv`へ戻せばよく、この構造体自身は完全な日本語で持てる)
3. `crates/blitz_asset_compiler/src/editor_chunk/scatter_groups/archetype_choice.rs:15` | `原型の安定id`(構造体`焼く原型`のフィールド) | 受理条件2・条件6(1語1義) | ユビキタス言語に登録された正式表記は「安定ID」(大文字)であり、`アセットID`型名も大文字を使う。このフィールドだけ小文字の`id`をアルファベットのまま日本語へ連結しており、同じ概念に大文字表記と小文字表記の2通りの綴りが生まれている(1語1義の破れ) | `原型の安定識別子`(型`アセットID`が既にIDであることを示すため、フィールド名側は漢字の「識別子」で受ける)
4. `crates/blitz_asset_compiler/src/editor_chunk/scatter_groups/archetype_choice.rs:20,24,34` | `植生定義id`(関数`植生定義の識別子から焼く原型を引く`の引数名) | 受理条件2・条件6 | 引数の型は`植生定義ID`(大文字)なのに、引数名だけ小文字の`id`を使っている。型名と変数名で綴りが食い違い、同じ画面を読む人が同じ概念に2つの表記を見ることになる | `植生定義識別子`または型名に合わせて`植生定義ID`(変数名として大文字IDを使う既存の広い慣習に合わせる)
5. `crates/blitz_asset_compiler/src/editor_chunk/scatter_groups/archetype_choice.rs:61` | `引いた.原型の安定id`(テストコード中の参照) | 受理条件2・条件6 | 上の3と同一フィールドの参照であり、同じ理由で不合格 | 3の改名に追従

## 違反の疑い(断定しない)

1. `crates/blitz_asset_compiler/src/edge_agreement/error.rs:28` および `crates/blitz_asset_compiler/src/edge_agreement/source_check.rs:38-39` の `x: i32, z: i32`(`縁の一致エラー::チャンク一辺不一致`のフィールド) | 条件2に対する疑い | 単独のアルファベット1文字`x`・`z`は他の受理条件8条件の「漢字1文字への圧縮」に近い形だが、`チャンク座標`型が既に公開メソッド`.x()`・`.z()`を持ち、東西南北でなく数学の座標軸として扱う既存の広い慣習と一致している。ドメイン全体で座標軸をx/zと呼ぶ設計判断がユビキタス言語側にあるなら合格、単なる書き手の省略なら条件1(名詞1語では東西南北のどちらを指すか本来復元できない)に抵触する。本区画の他のエラー型(`東`・`南`・`隣の東`・`隣の南`)は完全な日本語(方角)を使っており、このエラー型だけ`x`・`z`へ戻っている点で内部一貫性が無い
2. `crates/blitz_asset_compiler/src/editor_chunk/ground_material.rs`(コメント中の`UV`は識別子でないため対象外だが)や `crates/blitz_asset_compiler/src/loader` 配下に見られる`金属粗さPBRデータ`・`金属粗さPBR`(本区画では`scene.rs:6,21`が参照するのみで定義はしていない) | 条件2に対する疑い | 「PBR」はユビキタス言語に初出補足つきで登録済みの語であり、既存の広い慣習として定着している。ただし識別子中への英字混入という形自体は受理条件2の原則(ASCII識別子は補完力ゼロ)には反しており、断定はしない
3. `crates/blitz_asset_compiler/src/editor_chunk/building_definitions.rs:52` の `部品.安定id()`(呼び出し先メソッド)、`archetype_choice.rs:29` の `識別.安定id()`(呼び出し先メソッド) | 条件2・条件6に対する疑い | いずれも本区画の外(おそらく`blitz_assembly`または`runtime_compilation`のいずれか)で定義されたメソッドの呼び出しであり、定義自体は本区画の対象ファイルに無い。呼び出し側の綴りとして`.安定id()`が現れている事実は記録するが、定義箇所が区画外のため改名の要否は定義側の検査に委ねる

## 違反ゼロの範囲

- `distant_terrain/cell_corner.rs`・`cell_scan.rs`・`deviation.rs`・`error.rs`・`gap.rs`・`grid.rs`・`grid_measure.rs`・`mesh.rs`・`scene.rs`・`sink.rs`・`subdivision_point.rs`・`tests.rs`
- `edge_agreement/band.rs`・`edge_agreement_tests.rs`・`error.rs`(縁の一致エラー本体の`x`・`z`以外の枝)・`pairwise.rs`・`source_check.rs`(`目録と高さ格子の一辺が一致することを確かめる`本体)・`source_check_tests.rs`
- `editor_chunk/building_bake_result.rs`・`building_groups.rs`・`ground_material.rs`・`identifier_seed.rs`・`scatter_groups.rs`(`原型の安定id`の参照箇所を除く本体)・`scene.rs`・`source.rs`
- `compile/source_scene.rs`

上記の範囲では、型名・関数名・メソッド名・フィールド名・変数名・定数・列挙の枝のいずれも受理条件8条件・日本語命名の質・呼称と語の選択のいずれにも抵触する識別子を検出しなかった。長い複合語(`寸法を合わせる倍率を与えてソースシーンをコンパイルする`等)は長さそのものを理由に不合格とはしていない(条件8「長さは結果であって制約ではない」)。
