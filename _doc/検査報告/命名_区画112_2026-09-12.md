# 命名検査報告 区画112

- 区画名: crates/blitz_render/src/vulkan/uniform 他(uniform・unsent_command_buffers・visible_id・xpbd_bench・xpbd_solver_bench_probe)
- 対象ファイル数(通知): 35
- 作業ツリー: C:/devs/Blitzdrache0_roller
- 時点: main の db815090(このツリーは編集していない)
- 直読したファイル数: 35 / 35(全ファイルを直読。grepだけで済ませたファイルは0件)

## 違反の一覧

| ファイル:行 | 識別子 | 落ちた条件 | なぜ読めないか | 改名の素案 |
|---|---|---|---|---|
| crates/blitz_render/src/vulkan/uniform/create.rs:10 (呼び出し側: mod.rs:46 `create::生成する(確保係)`) | `生成する`(モジュール`create`直下の自由関数) | 条件5(裸の動詞の禁止) | `create`はASCIIモジュール名であり読まれない前提のため、`生成する`単独では何を生成するかが名前から一意に決まらない。CLAUDE.mdの受理条件が挙げる不合格実例`create::生成する`とまったく同じ形である | `フレームシェーダー定数一式を生成する`(型に属する関連関数へ寄せるか、動詞へ目的語を足す) |
| crates/blitz_render/src/vulkan/uniform/mod.rs:49 | `ビュー定数のbuffer` | 条件2・6・7 | 同じ概念をこのファイルの他所は「バッファ」と日本語(カタカナ)で呼んでいる(`定数バッファ一式`・`フレームスロットのバッファ`)のに、ここだけ生の英単語`buffer`を使い、1つの語に2つの綴りが生まれている。英語を知らない読み手は`buffer`をそのまま読めない | `ビュー定数のバッファ` |
| crates/blitz_render/src/vulkan/uniform/mod.rs:53 | `多段影のbuffer` | 条件2・6・7 | 同上 | `多段影のバッファ` |
| crates/blitz_render/src/vulkan/uniform/mod.rs:57 | `空パスのbuffer` | 条件2・6・7 | 同上 | `空パスのバッファ` |
| crates/blitz_render/src/vulkan/visible_id/reference.rs:21 | `buffer`(公開メソッド名) | 条件2・6・7 | 同じ型の他フィールドは「スロット別バッファ」と日本語で書かれているのに、公開メソッド名だけが裸の英単語。呼び出し側からは`参照.buffer(添字)`としか読めず、英語を知らない読み手には何を返すか名前から取れない | `バッファ` |
| crates/blitz_render/src/vulkan/xpbd_bench/descriptor.rs:40 | `layout`(構造体フィールド) | 条件2・6・7 | 同じファイル・同じ型が扱う概念を`セットレイアウトを確保する`・`レイアウトのハンドル`と日本語(カタカナ)で呼んでいるのに、内部フィールドだけ生の英単語`layout`。1つの概念に2つの綴りがある | `レイアウト` |
| crates/blitz_render/src/vulkan/xpbd_bench/descriptor.rs:41 | `pool`(構造体フィールド) | 条件2・6・7 | 同様に「プール」という日本語(カタカナ)化した語を使わず生の英単語を使っている | `プール` |
| crates/blitz_render/src/vulkan/xpbd_bench/descriptor.rs:47,49-56,82,83 | `layout`/`pool`/`pool_info`(局所変数) | 条件2・6・7 | フィールド名と同じ生英語がそのまま局所変数へも伝播している | `レイアウト`/`プール`/`プールの生成情報` |
| crates/blitz_render/src/vulkan/xpbd_bench/pipelines.rs:22 | `layout`(構造体フィールド, `XPBD計測パイプライン群`) | 条件2・6・7 | 同じ型が持つ他フィールド(`積分`・`乗数零化`・`工程`)は日本語なのに、パイプラインレイアウトだけ生英語。型名自体は「パイプライン群」と日本語なのに中のレイアウトだけ英語で1語1義が破れる | `レイアウト` |
| crates/blitz_render/src/vulkan/xpbd_bench/pipelines.rs:31 | `ディスクリプタlayout`(仮引数) | 条件2・6・7 | 「ディスクリプタ」は日本語(カタカナ)化しているのに直後の`layout`だけ英語のままで、1つの識別子の中で表記が割れている(膠着圧縮に近い症状) | `ディスクリプタレイアウト` |
| crates/blitz_render/src/vulkan/xpbd_bench/pipelines.rs:36,41,45,69,82 | `layout_info`/`layout`(局所変数・戻り値経路) | 条件2・6・7 | 同上の伝播 | `レイアウトの生成情報`/`レイアウト` |
| crates/blitz_render/src/vulkan/xpbd_bench/passes.rs:80 | `layout: self.パイプライン群.layout` | 条件2・6・7 | `発行の束縛`型のフィールド名が生英語であり、代入元の`パイプライン群.layout`も同じ生英語を運んでいる | `レイアウト` |
| crates/blitz_render/src/vulkan/xpbd_bench/passes/dispatch.rs:13 | `layout`(構造体`発行の束縛`のフィールド) | 条件2・6・7 | 同じ構造体の隣のフィールド`セット`は日本語なのに、`layout`だけ生英語で1つの構造体の中で表記の基準が割れている | `レイアウト` |
| crates/blitz_render/src/vulkan/xpbd_bench/passes/dispatch.rs:22 | `pipeline`(構造体`発行の指定`のフィールド) | 条件2・6・7 | 同じ構造体の他フィールド(`名前`・`読み`・`書き`・`スレッド数`・`色の区間`)は日本語なのに、`pipeline`だけ生英語。このコードベースは他所で「パイプライン」という日本語(カタカナ)語を積極的に使っており(`パイプライン群`・`パイプラインを生成する`)、1語1義が破れている | `パイプライン` |
| crates/blitz_render/src/vulkan/xpbd_bench/passes/dispatch.rs:29,46,51 | `pipeline`/`束縛.layout`(局所変数・参照) | 条件2・6・7 | フィールド名の生英語がそのまま局所変数・参照経路へ伝播している | `パイプライン`/`束縛.レイアウト` |

## 違反の疑い

| ファイル:行 | 識別子 | 疑いの条件 | 理由 |
|---|---|---|---|
| crates/blitz_render/src/vulkan/uniform/write_bytes.rs:13,21,28 | `vec4を書き込む`・`uvec4を書き込む`・`ベクトル3をvec4化` | 条件7(直訳・頭字語) | `vec4`/`uvec4`はシェーダー側(slang)の実際の型名をそのまま指しており、`CascadeShadowUniform`等の実在シンボル名をテスト内で保持する慣行と同型の可能性がある。技術用語の引用として許容できるとも読めるため断定しない |
| crates/blitz_render/src/vulkan/visible_id/write.rs:16 | `可視id列`(仮引数。`id`が小文字) | 条件6(1語1義) | 同じ概念を型名・他ファイルでは`可視ID列`と大文字で統一しているのに、この仮引数だけ小文字`id`になっている。Rustの命名規約(非ASCII識別子への静的解析の振る舞い)が理由の可能性があり、意図的な表記揺れか機械的制約かを本検査では切り分けられない |
| crates/blitz_render/src/vulkan/xpbd_bench/run.rs:20,21,70,71 | `刻み別のgpu時間`・`gpu時間の分布一覧` | 条件6(1語1義) | 同じ略語をこのコードベースの型名では`GPU`と大文字で統一している(`GPU資源の確保係`・`刻みごとのGPU計測`)のに、フィールド名では小文字`gpu`になっている。Rustの構造体フィールドはsnake_caseが要求されるため機械的制約の可能性があり、設計上の誤読とまでは断定しない |
| crates/blitz_render/src/xpbd_solver_bench_probe/mod.rs:40,41,48 | `刻み別のgpu時間`・`gpu時間の分布一覧`・`xpbdの並列方式をgpuで走らせて読み戻す` | 条件6(1語1義) | 上と同じ理由。特に関数名`xpbdの並列方式をgpuで走らせて読み戻す`は型名の`XPBD`(大文字)と綴りが割れている |

## 違反ゼロの範囲

上記の指摘を除く残り(cascade_bytes.rs、cascade_content.rs、layout_tests.rs、shader_struct_tests.rs、sky_bytes.rs、sky_content.rs、sky_write_order.rs、sky_write_order_tests.rs、view_bytes.rs、view_content.rs、buffer_set.rs、unsent_command_buffers_tests.rs、visible_id/capacity.rs、visible_id/mod.rs、xpbd_bench/buffers.rs、xpbd_bench/mod.rs、xpbd_bench/params.rs、xpbd_bench/pass_names.rs、xpbd_bench/readback.rs、xpbd_bench/timing.rs、xpbd_bench/buffers/create.rs、xpbd_bench/passes/iteration.rs、xpbd_solver_bench_probe/material.rs、xpbd_solver_bench_probe/shader_set.rs)は、日本語識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)を受理条件8つ・命名の質・呼称と語の選択の3観点で検査し、違反を検出しなかった。特に`XPBD計測バッファ`・`可視ID列バッファ`・`読み戻しの受け皿`・`未送信のコマンドバッファ数`等の型は、ユビキタス言語.mdの登録語(可視ID列・XPBD等)と一致し、実体(フィールド・分岐)とも照合して食い違いを検出しなかった。省略した領域は無い(全35ファイルを直読した)。
