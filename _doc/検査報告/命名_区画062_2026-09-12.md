# 命名検査報告 — 区画062: crates/blitz_engine/src/static_shape/index

- 区画番号: 062
- 対象範囲: crates/blitz_engine/src/static_shape/index 配下 13ファイル
- 作業ツリー: C:/devs/Blitzdrache0_roller(main, db815090時点。このツリーは編集していない)
- 検査軸: 命名(日本語命名の受理条件8条件・日本語命名の質・呼称と語の選択)
- 直読したファイル数: 13/13(全ファイル直読。grepだけで済ませたファイルは0件)
- 用語定義ファイル: `_doc/計画/ユビキタス言語.md` の該当語(静的な子形状の候補・静的世界の候補の選別・問い合わせ完全性等)を開いて照合した

## 直読したファイル一覧

1. candidate.rs
2. chunk_span.rs
3. error.rs
4. index_boundary_tests.rs
5. index_fixture.rs
6. index_overhang_tests.rs
7. index_probe_fixture.rs
8. index_receptacle_tests.rs
9. index_selection_tests.rs
10. index_truncation_rounding_tests.rs
11. query.rs
12. query_gather.rs
13. truncated_cover.rs

## 違反の一覧

違反0件。

## 違反の疑い

無し。

## 検査所見(合格側の根拠)

- `candidate.rs` の型`静的な子形状の候補`とフィールド(チャンク座標・チャンクの大域原点・衝突対象の安定識別子・子形状)は、ユビキタス言語「静的な子形状の候補」の定義(「粗い選別が『この子形状には触れうる』と答えた1件」「どのチャンクのどの衝突対象のどの子形状かと、その形を答える」)と1対1で対応する。実体と名前の食い違いは無い
- `chunk_span.rs` の型`覆いが跨ぐチャンク座標の矩形`は、名前が「覆い」「跨ぐ」「チャンク座標」「矩形」の4つの語で実体(東西南北の始まり終わりを持つ矩形)を過不足なく表す。名詞1語への圧縮も詰め込みすぎも無い。メソッド`読込済みチャンクで埋まるかを求める`・`座標を含むか`・`チャンク件数`も、動詞句・真偽句・値の名詞句の品詞規律を守る
- `error.rs` の列挙`静的世界の候補の選別エラー`の各枝(`覆いを格子へ写せない`・`問い合わせの位置が有限でない`・`カプセルの半径が0以上の有限値でない`・`掃引の変位が有限でない`)は、いずれも「何が」「どういう条件を満たさない」かを枝名だけで復元できる。目的語・主語の欠落は無い
- `query.rs` の型`静的世界の候補の選別`、関連関数`保管とチャンクの一辺から生成する`(材料の格で複数ファクトリーを区別)、メソッド`線分に触れうる子形状の候補を集める`・`掃引したカプセルに触れうる子形状の候補を集める`は、動詞句に「何を(子形状の候補を)」「何によって(線分に/掃引したカプセルに)」「どうする(集める)」の格をすべて含み、5W1Hの必要格を満たす。裸の`集める`単独名は無い
- `query_gather.rs`・`truncated_cover.rs` のプライベートメソッド(`覆いに触れる子形状の候補を集める`・`チャンク1つの候補を集める`・`線分を当たりの時刻まで切り詰めた覆いの完全性を求める`・`掃引を接触の割合まで切り詰めた覆いの完全性を求める`・`覆いの完全性を求める`)も同様に、対象・条件・動作の格が揃っており、オーナー不合格実例(「試験の許容差の当て方」のような実体と合わない圧縮、「連立」のような目的語欠落の名詞1語、詰め込みすぎ)のいずれにも該当しない
- テストファイル群(`index_*_tests.rs`)の試験名(`読み込まれていないチャンクを覆いが含むと完全性が落ちる`・`東の境界へ張り出した形は隣のチャンクからの問い合わせで候補に挙がる`等)は文単位の完全な日本語文であり、5W1Hを備え、単独で読んで検査内容が復元できる
- `index_fixture.rs`・`index_probe_fixture.rs` のヘルパー(`立方体の子形状を作る`・`衝突対象を作る`・`東へ進む変位`・`候補を組へ落とす`)も、材料・対象・変換先の格を持ち、ASCIIモジュール直下の裸の動詞にも当たらない
- 識別子へのASCII英字・ギリシャ文字・数学記号の混入は無い(定数`試験のチャンクの一辺メートル`は日本語の名詞句に単位を表す「メートル」を合成したものであり、記号混入ではない)

## 違反ゼロの範囲

区画062の13ファイル・識別子(型・関数・メソッド・フィールド・変数・定数・列挙の枝)を全数直読し、日本語命名の受理条件8条件・質・呼称と語の選択のいずれにも違反は見つからなかった。この区画は2026-08-28〜2026-08-29の実装(判断8・判断9・判断11関連)で命名規律が保たれたまま書かれている。
