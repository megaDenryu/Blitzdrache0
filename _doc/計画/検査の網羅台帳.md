# 検査の網羅台帳

命名と構造の検査が、このリポジトリのどこまでを検査済みかを記録する生存型文書である。2026-08-11のオーナー指示(「触るときに直すだけでは、誰も触らないコードが永遠に未検査で残る。日常的にローラー的に監査を派遣すべきだ」)で制定した。正本の規約はグローバルCLAUDE.mdが持つ(該当箇所は「日本語命名の受理条件」にある運用の箇条書きと、「プリミティブ執着禁止はパス・テキスト・名前にも適用する」)。

## 運用

- **記録の無い領域は未検査として扱う。** この台帳がローラー検査の完了を定義する
- 検査は2系統で進む。**差分検査**は委譲成果の検収時に命名チェック員+構造チェック員が常時行う(2026-08-09以後の全変更が対象)。**ローラー検査**はセッションの待ち時間に未検査領域へ検査員を派遣して行う
- 記録の単位は「領域(クレートまたはディレクトリ)×検査の軸(命名・構造)」であり、検査した日付・時点のコミット・結果の要約を書く
- 検査で見つかった違反の是正は、機能変更と別コミットで行う(リネーム員・実装員)。是正が済んだら結果の欄を更新する
- 検査の軸の定義: **命名**はグローバルCLAUDE.md「日本語命名の受理条件」の8条件。**構造**は同「サービスの依存保持とコールバックの限定」「デザインパターンの既定」「プリミティブ執着禁止はパス・テキスト・名前にも適用する」(自由関数の許容2条件・プリミティブ執着・値オブジェクト様式・&mut連鎖・暗黙のグローバル依存・凝集を失った定数)

## 命名検査の網羅

| 領域 | 検査日 | 時点 | 結果 |
| --- | --- | --- | --- |
| blitz_engine(25ファイル分) | 2026-08-14 | 8690a3c0 | 着手順1・着手順5.5の全数検査が完了した。規範制定時の違反24件と、生のタプル6件を含む是正差分の違反は全件是正済みであり、再検査では違反0件である |
| blitz_app(全域) | 2026-08-14 | 8690a3c0 | 着手順3で変更した99ファイルを含む全数検査が完了した。シーン名の判別共用体化、起動構築の型付け、誤りの型付け、数学DDDの是正後は違反0件である |
| blitz_render vulkan層(全域) | 2026-08-14 | 8690a3c0 | 着手順4・着手順5.5の全数検査が完了した。vulkan/frame配下で検出した27件を含む命名違反は全件是正済みであり、再検査では違反0件である |
| 2026-08-09以後の全変更差分 | 2026-08-14 | 8690a3c0 | 着手順0から着手順5.5までの各検収で識別子を全数検査し、検出した違反を各工程で是正した。着手順5.5の二重検査では約190個の識別子を再検査し、違反0件である |
| blitz_render vulkan/frame配下(直下+record) | 2026-08-14 | 8690a3c0 | 73ファイル・340個超の識別子から検出した違反27件を58bf2091で是正した。着手順4でGPU依存を受ける自由関数を構造ごと収容した後の全数再検査でも違反0件である |

## 構造検査の網羅

構造の規約軸は2026-08-11制定のため、それ以前のコードは全域が未検査だった。同日に全リポジトリ監査を実施し、2026-08-14に着手順5.5の二重検査まで完了した。是正の完了記録は `_doc/計画/構造是正計画.md` が持つ。

| 領域 | 検査日 | 時点 | 結果 |
| --- | --- | --- | --- |
| blitz_app | 2026-08-14 | 8690a3c0 | 是正と再検査が完了した。シーン名の文字列比較26箇所を判別共用体の網羅分岐へ、17引数の起動構築を2つの材料型へ移し、非テストの`Result<_, String>`22署名を0にした。構造違反は0件である |
| blitz_render + blitz_engine | 2026-08-14 | 8690a3c0 | 是正と再検査が完了した。モジュール外で依存を持参させる公開自由関数は実質0本であり、構築局面の9本は操作サービスの違反ではない。GPU資源の生タプルと公開の`&mut`受け皿持参は0件である。構造違反は0件である |
| xtask + blitz_asset_compiler | 2026-08-14 | 8690a3c0 | 是正と再検査が完了した。検収の判定層にあった`Result<_, String>`541箇所を0にし、規約検査・型計測・アセットの置き場に残った45箇所も型付きの破れへ移した。公開15関数と`&mut Vec<契約指摘>`を受ける19関数を型のメソッドへ移し、構造違反は0件である |
| 大規模世界第1段階の新設コード | 2026-08-14 | 8690a3c0 | 構造是正と再検査が完了した。12の型とファイル境界の8メソッドを新設した形を維持し、構造違反は0件である |

着手順5.5の二重検査は、命名では約190個の識別子を調べて違反0件、構造では全対象を調べて違反0件で完了した。出力用のバッファを確保せず再利用する3つの`&mut Vec`受け取りは依存の持参ではなく、公開境界の外側にあるため違反へ数えない。
