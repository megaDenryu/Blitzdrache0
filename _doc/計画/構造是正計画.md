# 構造是正計画

2026-08-11のオーナー裁定「是正先行」を受けた、全リポジトリの構造是正の工事の正本である。発端はオーナーによる構造の規約違反の検出(台帳の本文が裸の&str・出力ルートが裸の&Path・自由関数の乱用・定数の平坦列挙と綴りの複製)であり、同日に全クレートの構造監査3本で違反の地図を確定した。**機能開発(大規模世界の生成の第2段階以降)は本計画の工事が完了するまで保留する。**

判定基準の正本はグローバルCLAUDE.mdが持つ(「サービスの依存保持とコールバックの限定」「デザインパターンの既定」「数学DDD」「プリミティブ執着禁止はパス・テキスト・名前にも適用する」)。検査済みの範囲は `_doc/計画/検査の網羅台帳.md` が持つ。

## 監査が確定した違反の地図(2026-08-11、時点0fd6153f)

| 領域 | 自由関数率 | 中核の違反 |
| --- | --- | --- |
| xtask | 80%(1,025本)。操作サービス型ゼロ | 61コマンド全部が自由関数連鎖・`実行結果`の11重複定義と`画素()`の10重複実装・`Result<_, String>`551箇所・validation見出しの5重綴り |
| blitz_render | 41.9% | GPU依存を引数で運ぶ自由関数348本(モジュール外公開60本が明確な違反)・フレーム記録が13引数自由関数+begin/end括り(転送側は是正済みで手本が既にある)・conform外のコメント同期定数約30箇所・実在しない検査への虚偽参照1件(auto_exposure.rs) |
| blitz_app | 50.8%(reports/smokeに偏在) | シーン名Stringの26箇所分散再解釈・17引数の起動構築・`アプリ`43フィールド・`Result<_, String>`22署名 |
| blitz_engine | 40.2%(大半は合法な純粋計算) | 公開境界のfs直叩き4本・可視判定の&mut受け皿持参・裸f32の6箇所 |
| blitz_asset_compiler | 69% | 公開境界の自由関数15本(カタログとルートを持参させる)・契約検査の`&mut Vec<指摘>`を19関数へ通す連鎖。生成台帳・配置の型群は是正済みで完動の見本 |

全違反はゼロコスト(newtype・借用・repr(transparent))で是正可能と3監査とも結論した。Rc/RefCell/dyn/cloneの追加を要する箇所は無い。**唯一の警告**: blitz_renderの是正を「神クラスへメソッドを寄せる」形でやると借用が衝突する。サブサービス型がフィールドを所有する分解(転送実行環境の様式)で行う。

## 工事の一覧(依存順)

各工事の完了条件は、明記が無ければ「verify全緑」「既存入口の判定値・生成物のバイト不変」「構造チェック員+命名チェック員+codexの検収通過」の3つである。

- **順0: 機械強制の先行(完了)。** conformの定数一致検査へクレート同士の対を登録し、報告の行の綴りの契約の検査と、ファイル名らしい文字列リテラルの重複の検出を新設した。実測は「進行の記録」の同日の項が持つ。登録できなかったものは3種類ある。1つ目は積や和の式で書かれた定数(個体別可視ID列長40=可視IDバイト長×パス数×進行中フレーム数、ヒストグラムの要素数130=ビン数+2)で、検査が行から値を読めない。ヒストグラムの要素数はビンの本数の一致で代わりを果たす。2つ目はシェーダーの束縛番号で、`[[vk::binding(0, 1)]] Sampler2D sceneDepth;`のように番号が識別子より前に来るため、行の前置きで対象の行を一意に選べない。3つ目は即時定数のバイト数と頂点レイアウトの合意で、片側が構造体の宣言であり定数の行を持たない。クリアカラーの濃紺も登録できない(検収側がsRGBへ写した値を持ち、線形の値と等しくない)。**依存ゼロ。これで綴りの複製の悪化が止まる**
- **順1: 走行中の是正の完了。** 大規模世界第1段階の残是正5件(構造チェック員3件+codex2件)と、命名ローラー第1波の27件のリネーム(vulkan/frame配下の裸の動詞)。完了で第1段階が閉塞する
- **順2: xtaskの是正(完了)。** 共通値オブジェクトの新設(読み戻し画像・終了時報告・世界名enum・手書き誤り型enum)→検収実行サービスの新設(cargo起動+validation零確認+読み戻しの束)→61コマンドの順次乗せ換え。**この順序を守らないと61回の個別発明になる。** 3工程すべて完了した。第1工程は器の新設、第2工程は61コマンドの起動の乗せ換え、第3工程は判定層の誤りの型付けと生成器の起動の器化である。`Result<_, String>`は541箇所から0箇所へ、報告の本文を裸の`&str`で受ける署名は71箇所から0箇所へ減った。残る2つの`String`を返す入口(`conform/shader_constant`と`type_metrics`)は検収の判定層でなく規約検査と計測の道具であり、順6の定点で扱う
- **順3: blitz_appの是正(完了)。** シーン名の判別共用体化(起点。26分岐がmatchへ)→smokeの操作サービス化→17引数構築の型付き化→Stringエラーのthiserror化→数学DDDのnewtype(露出・ブレンド・秒・メートル)。5工程すべて完了した。実測は「進行の記録」の同日の項が持つ
- **順4: blitz_renderの是正。** 確保系下請けのサービス化(タプル返し廃止)→フレーム記録セッション型(転送実行環境+セッション型の様式を移植。device+command_bufferを受ける自由関数49本の解消)→descriptor/texture等の生成・束縛の型への収容。規模100〜150ファイル。**絵に触るため、是正前ビルドとの読み戻しバイト突き合わせを完了条件に足す**
- **順5: blitz_asset_compilerの公開境界の是正。** カタログと方針を保持するサービス型の新設(公開15関数をメソッドへ)・契約検査型(指摘一覧を所有、19関数をメソッドへ)。規模約35ファイル
- **順6: 定点。** 網羅台帳の全行更新・監査の再実行による違反ゼロ確認(自由関数の許容2条件での再集計)・グローバルCLAUDE.md「マイルストーン完了時の定点」5項目

## 進行の記録

- 2026-08-11: 計画制定。順1の残是正5件が走行中。命名ローラー第1波(違反27件検出)はリネーム待ち
- 2026-08-11: **順1の残是正5件が完了。** compile側のサービス型化(実行時アセットのコンパイルが置き場と2つのカタログを保持し、4つの自由関数をメソッドへ)・チャンク座標のタプル剥がしの復旧・置き場の型の`&str`と`&Path`の統一(世界のディレクトリ名を検証付き値オブジェクトへ)・汎用の連結の封鎖と綴りの正本の一元化(生成台帳とmap_seedの写しを消し、xtaskがライブラリを参照)・台帳書式の回帰検査の新設。構造チェック員の再検査で前回3件の解消を確認し、新規違反なし
- 2026-08-11: **順0が完了。** 定数一致検査へ16対を足した(個体変換のバイト長のengineとrenderの対1・自動露出のビンの本数2・計算の班の一辺とslangの`numthreads`の12・点光の種別の生値1)。綴りの契約の検査を新設して15件を登録した(validationの件数の見出し1・パス別の描画発行の行9・代表テクセルの見出し4・シェーダー差し替えの成功行1)。ファイル名らしい文字列リテラルの重複の検出を新設し、寄せられる重複3種13箇所(計測用の実行ファイルのパス6→1・計測の生値のファイル名5→1・シェーダーの入口のファイル名2→1)を正本へ寄せ、寄せられない8件を理由つきの台帳へ載せた。反証は8件実施し、片側だけを変えるとconformが落ちること(定数3件・綴り3件・重複1件・台帳の陳腐化1件)を実測した。auto_exposure.rsの虚偽参照は検査の実登録により事実になった
- 2026-08-11: **順0の補遺(codexの検収の4件)を完了。** 検収は「新設した検査自体に穴があり、現在の緑が閉塞根拠にならない」であり、4件とも採用した。(1)引用符の数え上げで文字列リテラルを判定していたため、綴りに触れたコメント1つで出す側の欠落を隠せた。行コメント・ブロックコメント・通常文字列・生文字列・文字リテラルを区別する字句の走査を新設し、2つの検査が同じ判定を使う形にした。(2)ファイル名らしい綴りの重複が、最初に`cfg(test)`を含む行でファイルを打ち切っていた。属性の付いた項目の範囲だけを外す形へ直し、突き合わせの鍵をファイル名の部分にした。新たに見えた重複は、標準サンプルの取得先2件を取得対象の型へ寄せ、ビルド時に焼いて取り込むシェーダー中間表現の55件を規則で許した(綴りが食い違えばコンパイルが失敗するため、この検査が重ねて見る必要がない)。(3)台帳の置き場ごと走査から外していたのを、自分で検査の対象を名指しする14ファイルの明示の一覧へ狭め、陳腐化検査を付けた。(4)重複を寄せた先の3つの裸の公開定数を役割の型へ閉じた(計測の生値のファイル・シェーダーの入口のファイル・報告の見出し)。反証は補遺だけで4件実施した
- 2026-08-11: **順0の補遺2(codexの再検収の3件)を完了。** (1)通常文字列の中身を原文のまま返していたため`"scene\x2eslang"`が点を持たない綴りに見え、また文字リテラルがコードの区分だったため`'{'`が項目の境界の数に入り、試験の範囲がファイル末尾まで伸びていた。逃がしの復号と区分の分離で両方を塞いだ。(2)ビルド時の成果物の名前を許す規則が第3の出現まで巻き添えで許していた。「焼く側が1つで、ほかの出現が全部その成果物の取り込みであること」へ絞り、外れた3件を理由つき台帳へ移した。(3)自己参照の台帳の判定を偶然の衝突から役割へ移し、対象の綴りを列挙する台帳4件を足した。反証は4件実施した
- 2026-08-11: **順0の補遺3(codexの再々検収の3件)を完了。** (1)符号位置の逃がしの桁に書ける下線を読み飛ばせず、`"scene\u{2_e}slang"`の重複を隠せた。(2)ビルド成果物を許す規則が「ほかの出現が全部取り込みか」を証明できていなかった。ファイルごとに最初の出現しか残していなかった点と、同じ行に呼び出しが在るかだけで引数と数えていた点を直し、出現を全部集めて`include_bytes!`の括弧の対応で引数を見分ける形にした。(3)許可の台帳が綴りだけで無条件に許していたため、台帳へ「現れてよい場所」を持たせ、場所以外への出現と場所からの消失の両方向で止める形にした。反証は4件実施した
- 2026-08-11: **順0の補遺4(codexの検収の残り2件)を完了。** (1)`include_bytes!`の一致が名前の境界を見ておらず、`not_include_bytes!`の引数まで取り込みと数えていた。(2)行末の逃がしによる継続が改行だけを見ており、復帰改行のファイルで綴りが継ぎ目で切れた。走査の入口で行末を揃える形にした。反証は2件実施し、どちらも是正を戻すと検査が落ちることを実測した
- 2026-08-11: **順0の補遺5(codexの検収の残り2件)を完了。** (1)取り込みが組み込みを指すことを確かめていなかった。同じ名前のマクロを定義した呼び出しとパスで修飾した呼び出しの2つが取り込み扱いになっていたため、修飾された呼び出しを受けない形にし、同じ名前のマクロの定義と取り込みが走査の対象に現れること自体を違反にした。(2)名前の続きの判定を、ASCIIの英数字と下線とASCII外の全部という上位集合へ広げた(親裁定。Rustの規則そのものの判定は外部のクレートを要し依存ゼロの方針と衝突するため、許可が狭まる側へ倒す)。反証は4件実施した
- 2026-08-11: **順5の一部を前倒しで完了。** 構造チェック員が順1の再検査で見つけた`(チャンク座標, PathBuf)`と`(チャンク座標, 高さ格子)`のタプル露出6箇所を、`チャンクの高さ格子ソース`と`チャンクの高さ格子`の2型で解消した。残る順5は契約検査の`&mut Vec<指摘>`連鎖19関数と、コンパイル系の公開関数群である
- 2026-08-11: **順2の第1工程(器の新設)が完了。** 61のコマンドが共通で使う器を`xtask/src/acceptance`へ置き、コマンド側だけがそこを知る向きにした。新設した器は8つである。**読み戻し画像**(寸法とRGBA8を所有し、座標と添字の画素・画素列・バイト列・寸法の一致をメソッドで持つ。構築は読み込みだけを通し「バイト長が寸法と合う」ことを不変条件にする)、**読み戻しの置き場と読み戻しの書き出し先**(役割を持つパスの型。`.raw`と`.size`の綴りとディレクトリの作成を私有する)、**終了時報告**(標準出力と読み手の名前を所有し、見出しの行の取り出しと検証層の指摘が零件であることの確認をメソッドで持つ)、**検収エラー**(手書きenumと`Display`の様式。xtaskは外部依存を持たないためthiserrorを使えない。`String`へ戻る境界は`From`実装1箇所だけである)、**アプリの起こし方**(cargo経由と構築済みリリース版の2枝。リリース実行ファイルの綴りを私有する)、**実行時アセットルート**、**アプリの起動指定**(シーンと枚数を構築の口で受け、任意の選択肢だけをメソッドの連鎖で足すビルダー)、**描画検収の実行環境**(起こし方と置き場をコンストラクタ注入で保持し、起こす・検証層の指摘が零であることを確かめる・読み戻すの3手をメソッドで持つ操作サービス)。統合の実測は、寸法とRGBA8を持つ構造体11個→1個、画素を読む実装10箇所→1つのメソッド、読み戻しの対の2ファイルを読む工程4箇所→1つである。`世界名`は11個の`pub const &str`から11枝の判別共用体になり、`世界名: &str`の8公開署名が型で受ける形になった。release_buildの`pub const 計測用の実行ファイル`(順0からの送り)は起こし方の中へ閉じ、6ファイルへ流れていた綴りと、メモリ採取条件の`実行ファイル: &str`フィールドまでを型へ置き換えた。代表として2つのコマンド(prop-drawとprop-multi-material-draw)を実行環境へ乗せ換え、判定値が乗せ換えの前後で一致することを実測した(prop-draw: 背景色[63, 75, 97]・物体画素158534・占有率0.1720・囲む矩形x390から860とy172から643・明度74から144。prop-multi-material-draw: 物体画素147505・上位3色(153,121,85)78181と(221,197,118)46957と(129,132,139)22367・シーンパスの発行数3)
- 2026-08-11: **順2の第1工程へcodexの検収の4件を反映した。** 全件採用した。(1)試験用の構築だけが読み戻し画像の不変条件(バイト長=幅×高さ×4)を破れた。読み込みも試験用の合成も`検証して組み立てる`の1つの口を通す形にし、局面は分けても不変条件は分けないようにした。(2)寸法の解析が数として読めない語を捨てており、「1920 壊れた語 1080」を1920×1080として受理していた。旧実装は失敗していたため退行であり、枝の名前とも食い違っていた。語を1つ残らず数として読み、ちょうど2語であることを求める形へ直した。(3)世界名の一覧が手書きの配列であり、枝を足して綴りだけを直すと試験も緑のままコマンド行から新しい世界を解釈できなかった。網羅の`match`である`次の枝`の連なりから一覧を組む形にし、枝の追加がコンパイルを落とすようにした。(4)新設した値オブジェクト自身のプリミティブ執着を3つ是正した。画像の寸法と画素の位置を次元ごとの型(`画像の幅`・`画像の高さ`・`画素の横位置`・`画素の縦位置`・`画素の番号`。すべて`#[repr(transparent)]`)へ分け、走査を位置の並びで回す形にした。書き出しの基準名を`検収の実行名`で閉じ、名前を書き出し先の型が持つ形にして、書き出し先を組んだ名前と失敗の文面に出す名前が食い違う余地を消した。描画フレーム数の0を構築時に拒む形にした。反証は5件実施し、いずれも是正を戻すとコンパイルか試験が落ちることを実測した(枝の追加・区切りを含む実行名・0枚・空白を含むシーン名・横と縦の取り違え)。回帰検査は18件足した。乗せ換え済みの2コマンドの判定値は不変である
- 2026-08-11: **順2の第1工程へcodexの再検収の3件を反映した。** 全件採用した。(1)世界名の枝の連なりは、新しい枝を足すときに直前の枝からの向け直しを忘れると、コンパイルも試験も通ったまま新しい枝が一覧から脱落した。枝と綴りの正本を表1つにし、表から判別共用体・綴り・一覧・逆引きを同時に生む形にした。判別共用体の定義そのものが表の出力であるため、一覧から切り離された枝を構文として書き表せない。(2)型が入口と出口の飾りになっていた。読み戻し画像が寸法を数で保持して読み出しのたびに包み直し、書き出し先と終了時報告が実行名を綴りで保持して再構築していた。読み戻し画像は`画像の幅`と`画像の高さ`をそのまま保持し、実行名は`Cow<'static, str>`を包む所有できる値オブジェクトにして、書き出し先・終了時報告・誤りの型が型のまま持つ形にした。数で受ける境界は構築の口の1箇所だけである。(3)実行名の2つの構築の口の不変条件が食い違っており、原文の口はASCII空白しか見ずNULや非ASCIIの空白を通していた。両方の口が同じ検証を通る形にしたうえで、拒む側でなく許す側を数え上げる形(英数字と下線とハイフンだけ)へ倒した。点も拒む(拡張子の付け替えが最後の点から後ろを置き換えるため、`a.b`は`a.raw`と`a.size`へ化ける)。Windowsの予約された装置の名前22件も大文字小文字を問わず拒む。反証は6件実施した(表への1行が4つ全部へ届くこと・予約された装置の名前と空白を含む定数がコンパイル時に落ちること・横と縦の取り違え・枝の追加)。回帰検査は12件足して合計318件になった。prop-drawの判定値は不変である
- 2026-08-11: **順2の第3工程のうち、判定の破れの器の新設と代表3コマンドの乗せ換えを完了した。** 判定層の失敗が541箇所の`Result<_, String>`へ散っており、同じ「上限を超えた」が入口ごとの文面で書かれていた。破れの形を数え上げる2つの束を検収の共通語彙へ置いた。**判定の破れ**は9つの形(上限を超えた・下限に届かない・許容の範囲を外れた・一致するはずが食い違った・零件のはずが残った・あるはずのものが1件も無い・有限でない・正でない・材料を数として読めない)を数え上げ、何の判定かは`判定の名前`が、比べた数は`判定が比べる値`(件数・実数・綴りの3枝)が持つ。比べそのものを名前のメソッド(`上限を課す`・`下限を課す`・`許容の範囲を課す`・`一致を課す`・`綴りの一致を課す`・`真であることを課す`・`零件であることを課す`・`有限であることを課す`・`正であることを課す`)にしたため、上限と下限を取り違えた形を呼び出し側で書けない。**報告の読み取りの破れ**は8つの形を数え上げ、読む側は`終了時報告`から`報告の行`と`報告の区画`を型のまま受け取り、行の中の値は`報告の行の鍵`と`鍵に値が結ばれる形`(次の語・この綴りで終わる語の次・この綴りに続く残り・等号の後ろの4枝)で引く。`報告の見出し`は実行時に組み立てた綴りも持てるようにした。画素の数どうしの比は`画素の占める割合`が持ち、分母の零をここで落とす。代表はprop-draw(画素の判定)・cluster-lights(報告の行と区画の読み取りと決定性の判定)・auto-exposure(鍵と値の行の読み解きと許容つきの比べ)の3つである。共有の`report_parse`(12入口が使う計数報告の読み取り)と`validation_count`も終了時報告のメソッドへ寄せた。指標は`Result<_, String>`が541から512へ、報告の本文を裸の`&str`で受ける署名が71から50へ減った
- 2026-08-11: **順2の第3工程のうち、生成器の起動の器化を完了した。** 5つの入口がそれぞれ`Command::new("cargo")`から`blitz_asset_compiler`のexampleを書き起こしており、example名の綴りが5箇所、「起動に失敗」「終了コードで失敗」の文面が5組に散っていた。器を`xtask/src/asset_generator`へ置き、入口の側だけがそこを知る向きにした。何を生成させるかは`生成の指定`が4枝(実行時形式を焼く・ソースアセットを生成する・入力するglTFの契約を検査する・アセットの変更を見張る)で数え上げ、枝ごとに受け取る引数を型で固定するため、ある生成器の起こし方へ別の生成器の引数を渡す形は書けない。生成器へ渡す語の並びは`完成した生成引数`が組み立てて検証し、1回ぶんの起動は`アセット生成器の起動`が`#[must_use]`と破棄時の閉じ忘れの検出つきのセッション型で持つ。検証は2つ足した(検査するファイルが1つも無い起動と、値として渡す語が選択肢の綴りに見える起動)。ow4-benchの`Vec<String>`はメモリの周期採取が起動のセッション型の形に収まらないため寄せられず、理由をファイルの冒頭へ書いて`run/launch_arguments`へ切り出した
- 2026-08-12: **順2の第3工程が完了し、順2の全体が閉じた。** 判定層の`Result<_, String>`541箇所と、報告の本文を裸の`&str`で受ける署名71箇所を、どちらも0にした。展開は代表3コマンドで様式を確立してから束ごとに進めた(影の3・空の5・計測の5・時間再構成と環境の8・個体群と材質と布の10・残る共有工程と入口)。**判定の破れ**は9つの形を数え上げ、比べそのものを`判定の名前`のメソッド(上限を課す・下限を課す・許容の範囲を課す・一致を課す・綴りの一致を課す・真であることを課す・零件であることを課す・有限であることを課す・正であることを課す)にしたため、上限と下限を取り違えた`if`を呼び出し側で書けない。**報告の読み取りの破れ**は9つの形を数え上げ、読む側は`終了時報告`から`報告の行`と`報告の区画`を型のまま受け取り、行の中の値は`報告の行の鍵`と`鍵に値が結ばれる形`(次の語・この綴りで終わる語の次・この綴りに続く残り・等号の後ろ・この綴りに続く数字の並びの5枝)で引く。判定でも報告の読み取りでもない破れは、コマンドごとの誤りの型(手書きenum+`Display`を別ファイルへ+`検収エラー`と`判定の破れ`を内包する枝)で表した。共有工程も型付きにした(検収世界の用意・シェーダーの一時コピー・計測用の構築・目視用の絵への変換・ファイル走査・スモークのアセットの一時コピー)。`検収エラー`・`判定の破れ`・`生成器エラー`の`impl From<...> for String`は全入口が型付きへ移ったため削除した。統合の実測は、実行時形式の実在の確認が5箇所→1つの型、シェーダーの一時コピーが9コマンド→1つの型、計測用の構築が4コマンド→1つの型、`report_parse/section_parse`(区画の読み取りの写し)が全廃である。`世界名`は生成器の語彙であるため`asset_generator`へ移した。**判定値は変えていない**が、非有限の実測に対する扱いが厳しくなった箇所が9件ある(旧実装は`NaN > 上限`が偽になり素通りしていた)。うち到達不能を確認したのが6件、到達しうるが落とす側へ変わる安全側が3件である
- 2026-08-12: **順3が完了した。** 5工程を依存順に進め、blitz_appだけを触って99ファイルを直した。第1工程はシーン名の綴りの一元化である。`String`のまま7ファイル26箇所で比較されていた綴りを、`--scene`の入口で1回だけ`起動時シーン`へ解く形にした。この型は安定IDを`アセットID`のまま保持し、どの世界かは13枝の`世界の種別`が持つ。接頭辞で括る3つの群(地形・小物・植生)の内訳は内側の枝で名指しする。26箇所の比較は、初期カメラ・初期ライティング・多段影の最大距離・空の方針・間接照明方針・露出方式・拡散間接方式・時間再構成方式・スモークの計画・ホットリロードの差し替えの対の10箇所の網羅`match`になった。綴りの正本は`cli/scene_selection/parse.rs`1箇所であり、`quad`の複製(起動設定の既定とスモークの書き換え先)も消えた。綴りの解き方を1つに揃えたため、実在しない2つの綴り(`vegetation`と`terrain`の下線なし)の扱いが変わるが、実在する植生の6世界と地形の4世界はすべて下線つきであり判定値の変わる入口は無い。第2工程はスモークの操作サービス化である。役割を持つ2つのパスを`実行時アセットの置き場`と`監視するシェーダーの入口ファイル`で包み、配下のファイル名の綴り(`catalog.blitzcatalog`・`chunk_directory.blitzchunks`・`.blitzasset`)を置き場が私有する形にした。`std::fs`と`SystemTime::now`へ触るのは差し替えの局面1箇所だけになった。`スモーク実行`は入口ファイル・置き場・差し替えの対・計画の材料をコンストラクタ注入で保持し、7引数の`判定する`と3段の前置きが1つの流れになった。第3工程は起動構築の型付けである。17引数を`読み込む世界の材料`と`画面の作り方`の2つへ分け、6要素のタプル別名を`起動時に組み上げた一式`にした。段階を組み立てるビルダーを別に立てなかったのは、部品が揃ったことを検査して完成させる口が`レンダラー::生成する`そのものであり、その口が順4の側にあるためである。第4工程は誤りの型付けであり、非テストの`Result<_, String>`は22署名から0になった。新設は5つ(シェーダー再コンパイル・シェーダー中間表現の読み解き・空代表画素の照合・代表板の照合・跨ぎ報告)と`描画対象数エラー`である。第5工程は数学DDDであり、露出倍率とアニメーションのブレンド係数を`#[repr(transparent)]`の値オブジェクトへ、アニメーション時刻を`秒`へ、段探査の刻みを`メートル`へ移した。開発UIのスライダーはeguiが`&mut f32`しか受けないため生値を貸す境界を値オブジェクト自身が持ち、動かせる範囲も値オブジェクトが持つ形にして、範囲の両端が受理範囲の内側にあることを検査で固定した。`reports`の`[f64; 3]`は包まなかった。視線の向きをblitz_renderの公開APIが`[f64; 3]`で受けており、blitz_mathの単位型はf32のため対応する型が今は無い。ここで包むと同じファイルの中で即座に剥がすことになり、検査を1つも足さずに壁だけが増える。f64の向きの型は順4でblitz_render側に置く。あわせて範囲外と明示されていた`reports/exit.rs`の`終了時報告を出す(アプリ: &アプリ)`を`アプリ`のメソッドへ移した。判定値の不変は工程ごとに確かめた(smokeの全ステージ・prop-draw・material-reload-draw・shader-reload-draw・sky-time・auto-exposure・cluster-lights・multi-material-draw・instance-draw)。`instance-lod`は順3の着手前(0fd6153fの後の796f0bd1)から同じ数(39と42の食い違い)で落ちており、順3の変更とは無関係である
- 2026-08-12: **順4の第1工程(確保系下請けのサービス化)が完了した。** 論理デバイスと物理デバイスのメモリ性質を保持する操作サービス`GPU資源の確保係`を`vulkan::allocator`へ立て、確保をメソッドへ移した。確保系の依存持参の自由関数は9本(バッファ2・メモリ3・サンプラー2・シェーダーモジュール1・コマンドプール1)から0本になり、モジュール7つ(commands・device_buffer・host_buffer・linear_sampler・memory・nearest_sampler・shader_module)を削除した。`(vk::Buffer, vk::DeviceMemory)`のタプル返しは資源型`専用メモリ付きバッファ`が引き受け、メモリのハンドルは確保係の外へ出なくなった(写像と解放の対がこの型の中だけに閉じ、ホストからの読み書きも5つのメソッドになった)。同じ形の重複も寄せた。進行中フレーム数ぶんの確保と巻き戻しを写していた6箇所は`フレームスロットごとのバッファ`1つへ、布とスキニングが同じ内容で持っていた巻き戻しの台帳2つは`巻き戻せる確保の台帳`1つへ、画像へのメモリの結び付けを書いていた12箇所は1つのメソッドへ寄せた。触れたのは238ファイルであり、`メモリプロパティ`を引数で運ぶ署名は`GPU環境`と`ウィンドウなし実行GPU環境`の問い合わせ2箇所を残して0になった。**絵のバイト不変**は代表6入口(prop-draw・cluster-lights・sky-time・atmosphere-lut・instance-draw・multi-material-draw)の読み戻し70ファイルを是正の前後で突き合わせて実測し、全ファイルがバイト一致した。判定値の行も一致している。追加でsmoke・material-reload-draw・local-visibility・instance-cullの4入口が緑であることを確かめた(この4つは自分の中に基準値と決定性の判定を持つため前後の突き合わせは採っていない)。第2工程(フレーム記録セッション型)と第3工程(descriptor等の収容)は未着手である
- 2026-08-13: **順4の第2工程(フレーム記録セッション型)が完了した。** 転送実行環境の様式をフレームの経路へ移した。`フレームの記録の実行環境`(論理デバイスの複製とグラフィックスキューを保持する操作サービス)をフレーム進行が持ち、`フレームのgpu命令を積み始める`が返すセッション型が`フレームのgpu命令を積む`と`記録を閉じて送信し提示する`をメソッドで公開する。閉じ忘れは`#[must_use]`と未送信の勘定と破棄時のpanicで検出する。13引数の`frame::描画する`と、その下の`コマンドを記録する`と`送信して提示する`の自由関数の連鎖は無くなり、begin/endの手続きの括りは積み始めと閉じの2つのメソッドになった。あわせて論理デバイスと記録中のコマンドバッファを組で持つ`GPU命令の積み先`を新設し、この2つを別々に受け取っていた自由関数を1つの組で受ける形へ揃えた。**論理デバイスとコマンドバッファの両方を受ける自由関数は50本から0本になった**(残る1つは`GPU命令の積み先::生成する`であり、組を作る構築の口そのものである)。組を作れるのは積み込みを開始した3つのセッション型とパスの文脈だけである。グラフの実行器は積み先とクエリプールを保持する`グラフ実行器`になり、バリアの発行・パスの記録・dynamic renderingの開始と終了・タイムスタンプの書き込みも組で受ける。記録の材料8つは`フレームの記録の材料`の1つの束にし、`#[allow(too_many_arguments)]`を2つ消した。読み戻しの待機の要否は`bool`から2枝の判別共用体へ移した。第1工程からの送りも2件片付けた。確保係を通らずに論理デバイスを持参して`create_sampler`を呼んでいた5つの自由関数(多段影の比較・点光源の影の比較・材質テクスチャ表・UIテクスチャ・遠方環境)は確保係の`標本の取り方からサンプラーを確保する`を通る形にし、専用メモリ付きバッファの「前提: 長さは確保量以下である」は確保量を持つこの型自身の検査になった(破れたらpanicする)。**絵のバイト不変**は代表5入口(prop-draw・cluster-lights・sky-time・instance-draw・multi-material-draw)の読み戻し72ファイルを是正の前後で突き合わせて実測し、全ファイルがバイト一致した。判定値の行も一致しており、smokeは全ステージ緑である。第3工程(descriptor等の収容)は未着手である
