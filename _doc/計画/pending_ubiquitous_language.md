# 裁定待ちユビキタス言語

裁定待ちユビキタス言語とは、実装の途中でAIが新しく作り、オーナーの裁定をまだ受けていない語のことである。

実装員は、既存の語では表せない概念に出会ったとき、その場で名前を決めてよい。ただし決めた名前は案であり、合意済みの語彙ではない。このファイルはその案を置く場所である。

`_doc/計画/ユビキタス言語.md` は、対話で合意した語彙を記録する正本である。親と実装員は、裁定を受けていない語をそちらへ書いてはならない。
グローバルCLAUDE.md「専門用語の表記」が求める「定義なしで新語を使い始めるのは禁止」は、このファイルへの登録で満たす。

手順の全文はプロジェクトのCLAUDE.md「新しいドメイン概念と語彙の導入」にある。

## 書き方

1件につき次の5つを書く。語を見出しにし、残りの4つを箇条書きにする。

- **区切られたドメイン**: 共通・描画・物理・ゲーム実行・ECS・資産ストリーミング・エディター・開発基盤のどれか。1語1義はこの範囲の中で成り立つ規律であり、区切られたドメインが違えば同じ表記が別の語でありうる。詳細はCLAUDE.md「区切られたドメイン」にある
- **暫定の定義**: 「Xとは〜のことである」の形で書く
- **使っている場所**: その語を識別子または文書で使っているファイルのパス
- **既存語で表せないと考えた理由**: どの既存語を検討し、なぜ足りないと判断したか

裁定が済んだら、親はこの節から項目を消す。親は、採用ならユビキタス言語へ定義を移し、必要なリネームを別のコミットで行う。親は、却下なら既存語へ戻すか、概念そのものを畳む。

## 裁定待ちの語(2026-09-20時点で4件、2026-09-26に「移動可能範囲の指定」を型ごと消して3件。PR #138 の実装員と親が作った語)

### 歩行方向

- **区切られたドメイン**: ゲーム実行
- **暫定の定義**: 歩行方向とは、旅行者が歩く向きを表す、東の比率と北の比率の二乗和が1である値オブジェクトのことである。生成の口はキーボード歩行入力からの導出`キー入力から導く`だけであり、相反するキーが相殺して零になれば向きは無い(`None`)。斜めの入力は長さを1へ正規化する。同型の`f32`を2つ並べる生成の口を置かないのは、東と北を取り違えても型が通るためである
- **使っている場所**: `crates/blitz_esca/src/walking_direction.rs`(定義)・`crates/blitz_esca/src/traveler.rs`・`crates/blitz_esca/src/traveler_movement.rs`・`crates/blitz_esca/src/tests/walking_direction_tests.rs`・`_doc/設計/Esca/設計正本.md` 4.4
- **既存語で表せないと考えた理由**: 「方向」「向き」を検討した。長さが1であることを不変条件に持ち、生成の口をキーボードの入力からの導出だけに限る値オブジェクトであることを、裸の方向では言えなかった

### 経過時間

- **区切られたドメイン**: ゲーム実行
- **暫定の定義**: 経過時間とは、遷移関数へ渡す、有限かつ0以上の時間のことである。`blitz_math::秒`を`経過時間::生成する`で検証して包んだ値オブジェクトであり、負・NaN・無限大は`経過時間の生成の失敗`で拒む。遷移関数が負の時間で逆向きに動いたり、数値でない時間で現在地を汚したりしないために置く。Esca第1段階の遷移パラメータ(`MParameter`)が持つ唯一のプロパティである
- **使っている場所**: `crates/blitz_esca/src/elapsed_time.rs`(定義)・`crates/blitz_esca/src/ontology.rs`・`crates/blitz_esca/src/traveler_movement.rs`・`crates/blitz_esca/src/tests/elapsed_time_tests.rs`・`_doc/設計/設計オントロジー.md` 3.5.1・4.5・`_doc/設計/Esca/設計正本.md` 4.1・4.4
- **既存語で表せないと考えた理由**: 「秒」「刻み幅」を検討した。`blitz_math::秒` は符号も有限性も持たない生の量であり、遷移に渡してよい(有限かつ0以上)ことを型で言う語が要った。刻み幅は固定刻みの語であり、可変の経過を指さない

### 旅行者の現在地の生成の失敗

- **区切られたドメイン**: ゲーム実行
- **暫定の定義**: 旅行者の現在地の生成の失敗とは、旅行者の現在地の東または北が有限な数値でない(NaN・無限大)ときに`旅行者の現在地::生成する`が返す失敗のことである。NaN は比較がすべて偽になり区域の閉包の判定が正しく働かないため、生成の口で閉じる
- **使っている場所**: `crates/blitz_esca/src/traveler_error.rs`(定義)・`crates/blitz_esca/src/traveler.rs`・`crates/blitz_esca/src/traveler_movement.rs`・`crates/blitz_esca/src/tests/traveler_invariant_tests.rs`・`_doc/設計/設計オントロジー.md` 3.5.9・`_doc/設計/Esca/設計正本.md` 4.1・4.4
- **既存語で表せないと考えた理由**: 既存の失敗の型の名前の様式(`移動可能範囲の生成の失敗`・`歩行の規則の生成の失敗`)に揃えた。現在地の座標が有限でないことを表す型付きエラーを指す語が無かった


## 設計の検証の層の語(2026-09-21から2026-09-22。Issue #170・#173・#174。2026-09-22にPR #177のオーナーレビューを受けて是正)

この節は表で書く。既存の「1件につき5つを書く」書き方(区切られたドメイン・暫定の定義・使っている場所・既存語で表せないと考えた理由の4つを箇条書きにする)を使わない理由は、これらがどれも同じ層(設計の検証)に属し、同じ理由(設計の命題を機械で検証する層を新設したため既存語に無い)を共有しており、1件ずつ「既存語で表せないと考えた理由」を書くと同じ文が繰り返し並ぶためである。区切られたドメインはすべて「開発基盤」である。表の列は語・暫定の定義・使っている場所の3つである。

**登録するのは、`_doc/設計/設計オントロジー.md` の本文が語として使っている語だけである。** 文書が使っていない語は2つに分かれる。文書が使うべきなのに書けていない語は、文書へ書いてから登録する。実装の内部の名前は登録せず、モジュールの冒頭コメントが説明を持つ。2つの見分け方は「その語を使わずに設計の判断を説明できるか」である。説明できるなら内部の名前である。2026-09-22のオーナーレビューが、この基準に照らして通らない語(`抽出の成果`・`関係の問い方`・`結合を解く工程`・`探索の上限`・`幅優先の探索`等)がここへ混ざっていたことを指摘し、以下の表はこの基準を当て直した結果である。落とした語とその理由は開発スレッドとPRの記録に残る。重複していた6語(`有限に数え上げられる領域`・`数え上げ`・`遷移の帰結`・`数え上げの網羅性`・`探索の上限`・`探索の結末`)は1つの表へ統合した。

### モデルの層(`crates/blitz_design_verification/src/`。移動の前は `xtask/src/design_model/`)

| 語 | 暫定の定義 | 使っている場所 |
|---|---|---|
| 設計概念 | 設計概念とは、設計関係グラフの節点になる、名前を持つ設計上の概念のことである。型・トレイト・処理の3種を区別する | `crates/blitz_design_verification/src/concept.rs` |
| 設計概念の識別子 | 設計概念の識別子とは、設計概念の同一性を表す、名前空間と定義のモジュールパスと所有者の並びと名前の4つ組のことである。設計概念の種類は同一性に入らない | `crates/blitz_design_verification/src/concept.rs` |
| 設計概念への参照 | 設計概念への参照とは、設計関係の端点と命題の項と量化の割り当てが指し示す、1つの設計概念への指し先のことである。設計概念の識別子と設計概念の種類の組であり、指し先が設計関係グラフに実在するとは限らない。`_doc/設計/設計オントロジー.md` 6.6節が「関係の主語と目的語は、設計概念そのものではなく`設計概念への参照`(設計概念の識別子と設計概念の種類の組)である」とこの語を使う | `crates/blitz_design_verification/src/concept/reference.rs` |
| 設計関係 | 設計関係とは、2つの設計概念の間に成り立つ、設計上の意味を持つ結び付きのことである | `crates/blitz_design_verification/src/relation.rs` |
| 抽出元の構文 | 抽出元の構文とは、定義の式が関係を導いた事実が書かれていたRustの構文を表す列挙のことである。構造体のフィールド・エンティティの識別子の関連型・関数の役割の型引数・関数の役割が固定で持つ入力の4つの選択肢を持ち、定義の式はこの列挙に無い構文の事実から関係を導かない。`_doc/設計/設計オントロジー.md` 6.5節は「導いた1件は、出どころ(表記が在ったソースの行と、関係を導いた事実が書かれていたRustの構文。本書はこの構文を抽出元の構文と呼ぶ)…を持つ」とこの語を使い、規則の表の列の見出しにもこの語を使う | `crates/blitz_design_verification/src/relation.rs` |
| 関数の役割が固定で持つ入力 | 関数の役割が固定で持つ入力とは、抽出元の構文の選択肢のうち、役割の定義が型引数によらず常に受け取る固定の入力から関係を導くもののことである。`M遷移関数`の`遷移パラメータ`がこれであり、`関数の役割の型引数`と分けるのは、表記の在り処が使用箇所の行でなく役割の定義の側に在ることである。`_doc/設計/設計オントロジー.md` 6.5節の規則の表がこの語を使う | `crates/blitz_design_verification/src/relation.rs` |
| 設計関係グラフ | 設計関係グラフとは、設計概念を節点、設計関係と保持する関係を有向の辺、分類を節点のラベルとして持つ、検証の対象になるグラフのことである。設計関係と保持する関係は別の一覧として持つ。設計関係への参加は設計関係の辺だけで数え、保持する関係の辺と分類は数えない | `crates/blitz_design_verification/src/relation_graph.rs`・`crates/blitz_design_verification/src/分類.rs` |
| 同一性の衝突 | 同一性の衝突とは、同じ設計概念の識別子へ種類の違う設計概念が現れたことのことである。衝突した識別子と、先に現れた種類と、後から現れた種類を持つ | `crates/blitz_design_verification/src/relation_graph.rs` |

`設計概念の種類`(型・トレイト・処理を区別する列挙)と`設計関係の種類`(設計の関係の4語を表す列挙)は、`_doc/設計/設計オントロジー.md` の本文がその名前で語っていないため、この表へ入れない。前者は概念の同一性を説明する文の中で触れるだけであり、後者は第6節の6.5節の表の見出し語(識別する・消費する・生成する・参照する)がそのまま実体を説明する。

### 抽出の層(`xtask/src/design_model/extract/` に残る)

| 語 | 暫定の定義 | 使っている場所 |
|---|---|---|
| 抽出できなかった行 | 抽出できなかった行とは、抽出の規則が当たる構文でありながら、抽出器がそこから設計関係を1件も組めなかったソースの行のことである。`_doc/設計/設計オントロジー.md` 第6節がこの語を使う | `xtask/src/design_model/extract/unextracted_line.rs` |

`抽出できなかった理由`・`抽出の成果`・`抽出の結果`・`抽出対象のソース群`・`設計オントロジーの適用範囲`・`役割の使用箇所`は、`_doc/設計/設計オントロジー.md` の本文がその名前で語っていない内部実装名であるため、この表から落とす。`関数の役割`は新語ではなく、`_doc/設計/設計オントロジー.md` 第3節が節の題にしている既存語であるため、この表から削る(裁定待ちへ入れない)。

### 命題と検証の層(`crates/blitz_design_verification/src/`)

| 語 | 暫定の定義 | 使っている場所 |
|---|---|---|
| 命題 | 命題とは、設計について真偽を問う式のことである。原子・否定・連言・選言・含意・全称・存在の7つの形を持つ | `crates/blitz_design_verification/src/proposition.rs` |
| 原子命題 | 原子命題とは、それ以上分解しない最小の主張のことである。`_doc/設計/設計オントロジー.md` 第8節が一覧を定める | `crates/blitz_design_verification/src/atom.rs` |
| 対象集合 | 対象集合とは、量化が走る範囲のことである。全ての設計概念・種類で絞った設計概念・マーカーを持つ設計概念・全ての設計関係の主語・到達可能な状態・到達可能な遷移の6つの選択肢を持つ | `crates/blitz_design_verification/src/quantification.rs` |
| 検証結果 | 検証結果とは、1つの命題について検証器が答える、証明済み・反証済み・未決定の3つのどれかのことである | `crates/blitz_design_verification/src/evidence.rs` |
| 証拠 | 証拠とは、検証結果が証明済みであるときの根拠であり、どの方式で何を全部見たかを持つもののことである | `crates/blitz_design_verification/src/evidence.rs` |
| 反例 | 反例とは、検証結果が反証済みであるときの根拠であり、人が再現できる材料を持つもののことである | `crates/blitz_design_verification/src/evidence.rs` |
| 検証の方式 | 検証の方式とは、証明済みまたは反証済みを示した検証の手立てを表す列挙のことである | `crates/blitz_design_verification/src/evidence.rs` |
| 検証の方式の選択肢 `推論` | 検証の方式の選択肢 `推論` とは、推論規則が前提から結論を導いたことを根拠にした検証結果であることを表す、検証の方式の選択肢のことである。前提を示した方式は証拠の説明が別に並べる。`_doc/設計/設計オントロジー.md` 第9節が「方式として `推論` を名乗る」とこの語を使う | `crates/blitz_design_verification/src/evidence.rs` |
| 命題の集合 | 命題の集合とは、アプリケーションが満たすべき、名前付きの命題の並びのことである | `crates/blitz_design_verification/src/specification.rs` |
| 設計の検証の合否 | 設計の検証の合否とは、`cargo xtask design-verify` が答える、反証済みが1件も無い・反証済みが在る・検証した命題が1件も無いの3つの選択肢を持つ判断のことである。反証済みが1件でもあれば不合格であり、命題を1件も検証しなかった実行も合格にしない。未決定が合否へどう効くかはIssue #175の設計の受理が決める | `xtask/src/design_verify.rs` |
| 普遍命題としての法則 | 普遍命題としての法則とは、検証器へそのまま渡せる1つの命題を持つ法則のことである | `crates/blitz_design_verification/src/law.rs` |
| 推論規則 | 推論規則とは、前提の命題の並びと結論の命題を持ち、前提が全部証明済みのときに結論を導く法則のことである | `crates/blitz_design_verification/src/law.rs` |
| 排他の推論規則 | 排他の推論規則とは、2つのイベントが排他であるという法則から、一方の発生の成立から他方の不発生を導く具体の推論規則のことである | `crates/blitz_design_verification/src/law/exclusive_rule.rs` |

`設計関係の経路が在る`・`関係の問い方`・`項`・`束縛変数`・`束縛の割り当て`・`列挙した対象`・`命題の検証器`・`原子と対象集合の解き手`・`結合を解く工程`・`構造の検証器`・`検証の集計`は、`_doc/設計/設計オントロジー.md` の本文がその名前で語っていない内部実装名であるため、この表から落とす。`未決定の理由`(検証結果が未決定であるときの理由を表す列挙そのもの)も同様に落とすが、その選択肢のうち文書の本文が名指す1件(`関係を落とした抽出の欠落が在る`)は、下の「PR #177 の是正が足した語」に単独で登録する。

### 遷移と有限全数の層(`crates/blitz_design_verification/src/`。重複していた登録をここへ統合した)

| 語 | 暫定の定義 | 使っている場所 |
|---|---|---|
| 遷移モデル | 遷移モデルとは、前状態・入力・適用した規則・後状態・生成イベント・失敗を1件として持つ、遷移の集まりのことである | `crates/blitz_design_verification/src/transition_model.rs` |
| 遷移 | 遷移とは、前状態と入力と適用した規則から後状態と出来事が定まった1回の移りのことである | `crates/blitz_design_verification/src/transition_model/transition.rs` |
| 有限に数え上げられる領域 | 有限に数え上げられる領域とは、状態とコマンドを数え上げられ、実物の遷移関数を呼んで1回の遷移を行える、ドメインの側が実装する契約のことである | `crates/blitz_design_verification/src/domain.rs` |
| 数え上げの網羅性 | 数え上げの網羅性とは、ある対象集合を数え上げた並びが取りうる値の全部であるか、その一部に限るかの区別のことである | `crates/blitz_design_verification/src/coverage.rs` |

`遷移の識別子`・`遷移の材料`・`到達可能な状態一覧`・`有限全数の検証器`・`数え上げ`・`遷移の帰結`・`探索の結末`・`探索の上限`・`幅優先の探索`は、`_doc/設計/設計オントロジー.md` の本文がその名前で語っていない内部実装名であるため、この表から落とす。

ドメイン側の試験が契約を実装するために作った4つ(`旅行者の歩行を数え上げる領域`・`現在地を名指すマスの一辺`・`旅行者の歩行の命題を組む工程`・`名指した遷移の3つ組`。置き場は `crates/blitz_esca/src/tests/`)は、この表へ入れない。実装員がこれらを検証の足場であってEscaのドメイン概念ではないと判断し、親が受理した。**この判断そのものが誤りであるなら、4つを登録し直す必要がある。**

### PR #177 の是正が足した語(2026-09-22。Blocker 2・4・8の是正)

次の2語は、`_doc/設計/設計オントロジー.md` 第9節がこの作業(Blocker 4・8)で新しく足した記述によって、本文で使う語になった。

| 語 | 暫定の定義 | 使っている場所 |
|---|---|---|
| 関係を落とした抽出の欠落 | 関係を落とした抽出の欠落とは、設計関係そのものを1件も組めなかった箇所を、理由の説明ごとの件数で数えたもののことである。同一性の衝突もこの欠落に数える | `crates/blitz_design_verification/src/extraction_loss.rs` |
| 未決定の理由の選択肢 `関係を落とした抽出の欠落が在る` | 未決定の理由の選択肢 `関係を落とした抽出の欠落が在る` とは、抽出器が設計関係そのものを組めなかった箇所が在り、辺の不在をその答えの根拠にできなかったときに検証器が返す未決定の理由のことである | `crates/blitz_design_verification/src/evidence.rs` |

このほか2語が今回の見直しで新しく本文と対応した。`同一性の衝突`は上の第9節への追記(Blocker 4・8)が使う語であり、上の「モデルの層」の表へ加えた。`検証の方式の選択肢 `推論`` は第9節の既存の記述(「方式として `推論` を名乗る」)が使っていた語であり、上の「命題と検証の層」の表へ加えた。

次の3語は `_doc/設計/設計オントロジー.md` 第10.1節(Blocker 2の是正。今回の作業より前に確定済み)が本文で使っており、今回の見直しで初めて裁定待ちへ登録する。

| 語 | 暫定の定義 | 使っている場所 |
|---|---|---|
| 状態の同一性 | 状態の同一性とは、探索が2つの状態を同じ1件として扱ってよいかを決める、有限に数え上げられる領域が選ぶ判断のことである。名指しと違い、異なる状態を同じ値へ写してはならない | `crates/blitz_design_verification/src/domain.rs` |
| 数え上げの網羅性の選択肢 `異なる状態に同じ同一性を与えた` | 数え上げの網羅性の選択肢 `異なる状態に同じ同一性を与えた` とは、領域が答えた状態の同一性が、互いに等しくない2つの状態を同じ値へ写したときに探索が返す数え上げの網羅性のことである。重複と判定された側の状態の名前の一覧を持つ | `crates/blitz_design_verification/src/coverage.rs` |
| 現在地のビット表現 | 現在地のビット表現とは、旅行者の現在地の東と北を単精度のビットの並びのまま写した、状態の同一性のことである | `crates/blitz_esca/src/tests/behavior_identity.rs` |

`辿る状態の台帳`(探索が状態の同一性ごとに代表を控える私有の構造体)・`宣言を囲む波括弧`・`落とした理由ごとの件数`・`抽出できなかった理由の区分`・`設計概念の種類の選択肢「実行時のドメイン概念」`・`設計概念の名前空間`・`Rustの項目の種類`・`グラフの組み立ての結果`・`非空の命題一覧`・`非空の命題一覧の生成の失敗`・`未決定の理由の選択肢「推論規則の前提を証明できない」`・`役割に包まれた関数のパス`は、`_doc/設計/設計オントロジー.md` の本文がその名前で語っていない内部実装名であるため、裁定待ちへ入れない。

次の1語は `_doc/設計/設計オントロジー.md` 第10.2節(今回のPR #177の是正作業で新設)が本文で使っており、既に登録済みの`数え上げの網羅性の選択肢 `異なる状態に同じ同一性を与えた`` と対称であるため、今回の見直しで裁定待ちへ登録する。

| 語 | 暫定の定義 | 使っている場所 |
|---|---|---|
| 数え上げの網羅性の選択肢 `異なる値に同じ名指しを与えた` | 数え上げの網羅性の選択肢 `異なる値に同じ名指しを与えた` とは、領域が答えた名指しが、互いに等しくない2つの値を同じ参照へ写したときに探索が返す数え上げの網羅性のことである。重複と判定された名指しの表記の一覧を持つ | `crates/blitz_design_verification/src/coverage.rs` |

`名指しの台帳`(探索が名指しごとに代表を控える私有の構造体)は、`_doc/設計/設計オントロジー.md` の本文がその名前で語っていない内部実装名であるため、裁定待ちへ入れない。

### PR #177 の型の定義の探索と取り込みの読みが足した語(2026-09-23から2026-09-24)

PR #177 は `M不変データ` の自己変更の禁止の検査を足したが、2026-09-25 のオーナーの裁定でその検査を外した(理由は `_doc/設計/設計オントロジー.md` 2.5.3)。その検査のためだけに登録した語は、指すものが無くなったため裁定待ちから落とした。次の7語は、型の定義の探索(純粋データ規約・型種別・`MParameter` の検査と設計関係の抽出が使う)と `use` の読みに残るため据え置く。

| 語 | 暫定の定義 | 使っている場所 |
|---|---|---|
| 識別子の境界 | 識別子の境界とは、ある名前の現れの直前の文字と直後の文字がどちらも識別子の文字(英数字・下線・非ASCIIの文字)でない位置のことである。実装の見出しの読みが、`for` と `where` をこの位置の現れだけで区切りと読む | `xtask/src/conform/design_ontology/identifier_boundary.rs`・`xtask/src/conform/design_ontology/impl_syntax.rs` |
| 位置のモジュール | 位置のモジュールとは、ソースのある行について、そのファイルから推定したモジュールパスの下へ、その行を囲む波括弧付きのモジュール(`mod 名 { … }`)の名前を外側から繋いだモジュールパスのことである。型の定義の探索が、実装の位置のモジュールの直下の定義と、定義の位置のモジュールをこの単位で数える | `xtask/src/conform/design_ontology/module_path/enclosing_module.rs`・`xtask/src/conform/design_ontology/type_definition.rs` |
| 定義の位置のモジュール | 定義の位置のモジュールとは、型の定義の行についての位置のモジュールのことである。型の同一性は、これと型名の組である | `xtask/src/conform/design_ontology/type_definition/definition_candidate.rs`・`xtask/src/conform/design_ontology/type_definition.rs` |
| モジュールの直下 | モジュールの直下とは、ソースのある行の頭で開いている波括弧がすべてモジュールの本体(ファイルそのものか `mod 名 { … }` の本体)である位置のことである。型の定義の探索は、この位置に書いた定義だけを採る | `xtask/src/conform/design_ontology/module_path/enclosing_module.rs`・`xtask/src/conform/design_ontology/type_definition/definition_candidate.rs` |
| 局所の位置 | 局所の位置とは、関数・実装・トレイト・`struct` の本体や `const` のブロックの波括弧が1つでも開いている位置のことである。モジュールの直下と対になる | `xtask/src/conform/design_ontology/module_path/enclosing_module.rs` |
| 項目の位置 | 項目の位置とは、実装かトレイトの宣言の本体の直下やモジュールの直下で、直前の区切り(`;`・`{`・`}`)からマクロの名前までが空白と外側の属性だけである位置のことである。位置のモジュールの計算が、この位置で波括弧を使って呼んだマクロの本体をマクロの呼び出しの中として数える。Rust の参照が言う item position の訳語である | `xtask/src/conform/design_ontology/module_path/enclosing_module.rs` |
| マクロのメタ変数 | マクロのメタ変数とは、`macro_rules!` の本体の中で、呼び出しの引数の字句に置き換わる `$名` と `$(..)` のことである。`$crate` はメタ変数に数えない。`use` の読みは、別名かパスの最後の名前がこれを含む文を取り込み元の候補にしない | `xtask/src/conform/design_ontology/macro_metavariable.rs`・`xtask/src/conform/design_ontology/use_resolution.rs` |

### 設計解釈マーカーを実装した型の実装の置き場の警告が足した語(2026-09-25。自己変更の禁止の検査の置き換え)

次の3語は、`_doc/設計/設計オントロジー.md` 2.5.3 がこの作業で新しく足した記述と、`cargo xtask conform` の警告の口によって使う語になった。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 |
|---|---|---|---|
| 警告 | 開発基盤 | 警告とは、`cargo xtask conform` が表示するが終了コードを変えない報告のことである。ファイル・行・文言の3つを違反と同じ形で表示し、見出しの行 `[xtask] conform警告:` と行の頭の `警告:` で違反と見分ける | `xtask/src/conform/warning.rs`・`xtask/src/conform/report.rs` |
| 検査の報告 | 開発基盤 | 検査の報告とは、`cargo xtask conform` の違反の一覧と警告の一覧を組で持ち、表示と終了コードを決める値のことである。終了コードを決めるのは違反の一覧だけである | `xtask/src/conform/report.rs`・`xtask/src/conform/mod.rs`・`xtask/src/conform/whole_repository.rs` |
| 全称の実装 | 開発基盤 | 全称の実装とは、実装の対象の型から参照と `Pin` を繰り返し外した型が実装自身の型引数である `impl`(`impl<T: 境界> トレイト for T`・`for &mut T`・`for Pin<&mut T>`)のことである。Rustの blanket implementation の訳語である。対象の型に名前が付いていないため、設計解釈マーカーを実装した型の実装の置き場の警告はこの実装を照らさない | `xtask/src/conform/design_ontology/impl_syntax.rs`・`xtask/src/conform/design_ontology/marker_impl_placement.rs` |

`設計解釈マーカーを実装した型の定義の置き場` と `型の定義のファイルと実装したマーカー` は、`_doc/設計/設計オントロジー.md` の本文がその名前で語っていない内部実装名であるため、裁定待ちへ入れない。

### 非推奨語「綴り」の是正で改めた登録語の見出し(2026-09-25。Issue #164)

次の1語は、ユビキタス言語に登録済みの語「骨格方式の部品の綴り」の見出しを、非推奨語「綴り」の是正で改めた後の名前である。概念そのものは裁定済みであり、裁定を待つのは呼称だけである。既存の語(骨格方式・役割・部品の識別子)の組み合わせで書いたため新しい概念の導入ではないと実装員は判断したが、登録語の呼称を変えたため、オーナーの確認のためにここへ挙げる。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 |
|---|---|---|---|
| 骨格方式の役割ごとの部品の識別子 | 資産・ストリーミング | 骨格方式の役割ごとの部品の識別子とは、ベイ格子が組み立てに使う8つの役割それぞれに部品の識別子が定まった表のことである。旧称は「骨格方式の部品の綴り」である | `crates/blitz_assembly/src/building_grid/part_spelling.rs`・`crates/blitz_assembly/src/building_grid/part_spelling_builder.rs`・`_doc/計画/ユビキタス言語.md` |

### 非推奨語「枝」の是正で改めた登録語の見出し(2026-09-25。Issue #164)

次の2語は、ユビキタス言語に登録済みの語の見出しに入っていた非推奨語「枝」を改めた後の名前である。概念そのものは裁定済みであり、裁定を待つのは呼称だけである。「問い合わせ完全性の選択肢」は判別共用体の取りうる値を指すため、2026-09-04に確定した「選択肢」へ機械的に置き換えた。「退避の枝」は判別共用体の取りうる値ではなく、定義文が「解き直す経路のことである」と書く経路を指すため、「選択肢」でなく「経路」を選んだ。登録語の呼称を変えたため、オーナーの確認のためにここへ挙げる。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 |
|---|---|---|---|
| 問い合わせ完全性の選択肢 | 物理 | 問い合わせ完全性の選択肢とは、世界問い合わせ結果が持つ完全性の2つの値(対象範囲を完全に評価できた・評価できない領域を含む)のことである。旧称は「問い合わせ完全性の枝」である | `_doc/計画/ユビキタス言語.md` |
| 退避の経路 | 物理 | 退避の経路とは、粘着の候補の混合連立方程式を確定できなかった反復を、同じ開始の配置から法線だけの連立方程式で解き直す経路のことである。旧称は「退避の枝」である | `_doc/計画/ユビキタス言語.md`・`_doc/設計/剛体の状態と接触.md` |

### 非推奨語「フォールバック」の是正で改めた登録語の見出し(2026-09-25。Issue #164)

次の2語は、ユビキタス言語に登録済みの語の見出しに入っていた非推奨語「フォールバック」を改めた後の名前である。概念そのものは裁定済みであり、裁定を待つのは呼称だけである。2語が指す実体は、テクスチャを持たない役割の代わりに束縛される、意味ごとに実在する1x1のテクスチャである。旧い定義文とモジュールの冒頭コメントが既にこの実体を「既定テクスチャ」と書いていたため、実装員は「フォールバック」をその語へ置き換え、新しい語を作らなかった。「正準」は旧称から引き継いだ。登録語の呼称を変えたため、オーナーの確認のためにここへ挙げる。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 |
|---|---|---|---|
| 正準の既定テクスチャのスロット | 描画 | 正準の既定テクスチャのスロットとは、「テクスチャ無し」の材質でも束縛される、意味ごとに実在する既定テクスチャ(法線なら平坦な法線、ベースカラーなら白)を指すテクスチャスロットのことである。旧称は「正準フォールバックスロット」である | `_doc/計画/ユビキタス言語.md`・`_doc/設計/GPU資源束縛の分離と索引化.md`・`crates/blitz_render/src/vulkan/material_table/` |
| 正準の既定テクスチャの用途 | 描画 | 正準の既定テクスチャの用途とは、正準の既定テクスチャのスロットが指す既定テクスチャの種類(白・平坦法線・中立金属粗さ・先頭の層だけの重み)のことである。旧称は「正準フォールバック用途」である | `crates/blitz_render/src/vulkan/material_table/fallback_usage.rs`(定義)・`crates/blitz_render/src/vulkan/material_table/texture_role.rs`・`_doc/計画/ユビキタス言語.md` |

### 非推奨語「文脈」の是正で付けた変数の名前(2026-09-25。Issue #164)

次の2語は、英語の context の直訳として「文脈」と名付けていた変数・引数・私有フィールドを、持っている中身を名指す名前へ改めたものである。どちらも局所の名前であり、公開の型名は変えていない。1つ目は登録語「GPU命令の積み先と宣言済み資源の取り出し口」を短く呼んだ形であり、新しい概念の導入ではないと実装員は判断したが、登録語と違う呼び方を使い始めたため、オーナーの確認のためにここへ挙げる。2つ目は既存の語に当たるものが無かったため新しく作った。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| 積み先と取り出し口 | 描画 | 積み先と取り出し口とは、レンダーグラフのパスのクロージャと、その中から呼ぶ工程の関数が受け取る `GPU命令の積み先と宣言済み資源の取り出し口` の値を指す変数の名前のことである | `crates/blitz_render/src/vulkan/` の配下の32ファイル(フレームの記録・派生表現・遠方環境・大気のベイク済み画像・布とXPBD計測の読み戻し・合成深度の注入・時間再構成の合成入力の注入のパス) | 型名そのもの(`gpu命令の積み先と宣言済み資源の取り出し口`)を変数名にすることを検討した。受け取る側のクロージャと関数の署名が型名を既に書いており、変数名で全文を繰り返すと1行が長くなる(この短い名前でも3ファイルで rustfmt が引数を折り返した)ため、型名の「積み先」と「取り出し口」の2語を残して短くした |
| キャンバスへ描く口 | エディター | キャンバスへ描く口とは、canvas要素から取り出した `CanvasRenderingContext2D` の値のことであり、見下ろし図の下地と重ね描きがこれへ線・塗り・文字を描く命令を出す | `editor_web/src/ツール/チャンク編集/画面/見下ろし図/` の6ファイル(キャンバス部品・見下ろし図部品・見下ろし図の下地の管理・重ね描きの配色・等高線/等高線の重ね描き・粗マス/粗マスの重ね描き)・`editor_web/src/ツール/チャンク編集/見下ろし図を配線する.ts` | 「描画コンテキスト」は外来語の直訳であり、小学生が分かるカタカナ語ではない。「キャンバス」はcanvas要素そのものを指す語として同じファイルで使っており、描く命令を受ける側の値と区別できない。このリポジトリが「読み口」「取り出し口」「発行口」で使う「口」を借り、何へ何をする口かを名前に入れた |

### 非推奨語「ヘッドレス」の是正で付けた語(2026-09-25。Issue #164)

次の1語は、ゲーム開発用エディターの文書・コメント・試験の説明が「ヘッドレス」と書いていた入り口を、実体をそのまま書いた名前へ改めたものである。非推奨語の一覧の候補「ウィンドウなし実行」は、ユビキタス言語で描画の実行モード(ウィンドウもスワップチェーンも作らずGPUだけを使う実行)として登録済みであり、エディターのこの入り口はGPUを使わずNodeで動くため、同じ語を当てると1語が2つの実体を指す。そのため実装員は別の名前を作った。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| ブラウザなしの適用 | エディター | ブラウザなしの適用とは、ブラウザの画面を開かずに、Nodeでコマンド列のJSONを受け取り、保管庫から資源を読み、画面と同じTSの適用の実装で適用して検証し保存する入り口のことである。AIはこの入り口とRESTの読み書きの口で、人と同じ操作をする。関連する言い方として「ブラウザなしの操作」(この入り口から積む操作)・「ブラウザなしの経路」「ブラウザなしの側」を使う。入り口のディレクトリの実名 `editor_web/src/ヘッドレス/` と eslint の境界の層の名前 `ヘッドレス` はまだ変えていない | `_doc/設計/ゲーム開発用エディター基盤.md` 判断1・`_doc/設計/楽曲エディター.md`・`_doc/設計/見下ろし図による地形編集.md`・`_doc/計画/ゲーム開発用エディター計画.md`・`_doc/計画/ユビキタス言語.md`「編集コマンド」・`crates/editor_server/src/resource/command/mod.rs`・`crates/editor_server/src/resource/music/command/mod.rs`・`editor_web/src/ヘッドレス/` の3ファイル・`editor_web/src/入り口/ガイド/使い方の節.ts`(定数 `ブラウザなしで適用するコマンド`) | 「ウィンドウなし実行」は上のとおり描画の実行モードの登録語であり、エディターの入り口へ当てると1語1義が破れる。「一括適用」は既に「編集コマンド一括適用」としてファイル名とコマンドに使っているが、コマンドを何件まとめて適用するかを言う語であり、ブラウザの画面を通すかどうかを言わない |

### 非推奨語「インテント」の是正で改めた登録語の見出し(2026-09-25。Issue #164)

次の1語は、ユビキタス言語に登録済みの語「入力インテント」の見出しを、非推奨語「インテント」の是正で改めた後の名前である。概念そのものは裁定済みであり、裁定を待つのは呼称だけである。2026-09-14のオーナー裁定(Issue #106)が具体の2語を「ゲームの操作意図」「カメラの操作意図」と改めており、実装員はその2語に共通する部分「操作意図」を総称の見出しにした。登録語の呼称を変えたため、オーナーの確認のためにここへ挙げる。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 |
|---|---|---|---|
| 操作意図 | ゲーム実行 | 操作意図とは、「移動したい」「決定した」等、どのデバイスから来たかに依らない操作の意図のことである。ゲームの操作意図とカメラの操作意図はこの語の具体である。旧称は「入力インテント」である | `_doc/計画/ユビキタス言語.md`・`_doc/計画/開発計画.md`・`_doc/計画/エンジンフェーズ1完了報告とフェーズ2計画.md`・`_doc/設計/シミュレーション層.md`・`crates/blitz_app/src/input/mod.rs`・`crates/blitz_engine/src/camera/intent.rs`・`crates/blitz_engine/src/lib.rs` |

### 設計関係への参加の計測で付けた語(2026-09-26。Issue #187 の作業D)

次の5語は、`cargo xtask design-verify` が表示する計測のために実装員が作った語と、その計測が母集団を採るために設計オントロジーの文書から識別子へ持ち込んだ語である。どれも、全称の命題「すべてのドメイン概念は、少なくとも1つの設計関係へ参加する」の母集団をオーナーが裁定するための道具の語であり、命題へは接続していない。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| 母集団の案 | 開発基盤 | 母集団の案とは、全称の命題の母集団を設計関係グラフのどの節点から採るかのことである。裁定を待つ案は2つあり、案1はドメインのクレートの型から関数の役割の型と意味型を除いたもの、案2はグラフの全ての節点である。どちらも参加を設計関係の辺だけで数え、保持する関係の辺と分類は数えない。参加の数え方は案ごとに違わないため、案は母集団をどの節点から採るかだけを持つ | `xtask/src/design_verify/設計関係への参加の計測/裁定を待つ母集団の案.rs`(定義)・`xtask/src/design_verify/設計関係への参加の計測/計測の表示.rs`・`xtask/src/design_verify/設計関係への参加の計測/設計関係への参加の計測の試験.rs` | 登録語の「対象集合」(量化が走る範囲)を検討した。対象集合は命題が本番で使う範囲の語であり、裁定の前の候補を対象集合と呼ぶと、命題へ接続済みの範囲と読まれる。候補を指す語として、対象集合と別の語が要った |
| 設計関係への参加の計測 | 開発基盤 | 設計関係への参加の計測とは、母集団の案ごとに、検証器が抽出の欠落によって答えを弱める前に、設計関係へ参加していないと数えた概念の件数と例を、設計関係グラフへ直接尋ねて数えたもののことである。抽出の欠落の件数と保証範囲の外の構文の件数を一緒に持つ。参加していない概念を、参加に数えない保持する関係の辺と分類の事実のどちらに現れるかで4つの理由に分けて数える | `xtask/src/design_verify/設計関係への参加の計測.rs`(定義)・`xtask/src/design_verify.rs` | 「検証結果」「検証の集計」を検討した。どちらも命題を検証した答えの語であり、この計測は命題へ接続せず、弱めを掛ける前の数を数えるため、検証の答えと呼ぶと本番の命題の答えと読み違える |
| 母集団を採った内訳 | 開発基盤 | 母集団を採った内訳とは、母集団の案がグラフから母集団を採ったときに、何を外し何を含めたかの事実のことである。ドメインのクレートから採ったときは母集団から外した関数の役割の型と意味型の一覧を、グラフの全ての節点を採ったときは母集団に含まれる、定義をたどれずモジュールパスが空の節点の件数を持つ | `xtask/src/design_verify/設計関係への参加の計測/裁定を待つ母集団の案.rs`(定義)・`xtask/src/design_verify/設計関係への参加の計測/計測の表示.rs` | 「内訳」単独と「対象集合」を検討した。内訳だけでは何の内訳かを答えず、対象集合は命題が本番で使う範囲の語であるため、母集団を採る工程の副産物を指せなかった |
| 設計語彙のクレート | 開発基盤 | 設計語彙のクレートとは、設計オントロジーの適用範囲のうち、設計解釈マーカーと関数の役割の正本を定義するクレート(`blitz_design`)のことである。適用範囲の残りをドメインのクレートと呼ぶ | `xtask/src/design_model/extract/ontology_scope.rs`(定義)・`xtask/src/design_model/extract/marker_concept.rs`・`xtask/src/design_model/extract/role.rs` | 登録語の「共通設計語彙クレート」(設計解釈マーカーの項)を検討した。同じものを指すが、ドメインのクレートと対にして適用範囲を2つに分ける呼び方が要り、「共通」を冠すると対の相手が読めなかった。裁定で登録語へ寄せるなら識別子を改める |
| 意味型 | 開発基盤 | 意味型とは、型引数に設計解釈マーカーの境界を持ち、関数の役割の戻り値の形を固定する型のことである。汎用の入れ物ではない。`遷移成功結果<状態: M状態, イベント: Mイベント>` と `遷移失敗結果<状態: M状態, 失敗: std::error::Error>`(2026-09-26 の契約変更で意味型になった)がこれに当たる | `_doc/設計/設計オントロジー.md` 第3節(出どころ)・`xtask/src/design_model/extract/role.rs`・`xtask/src/design_verify/設計関係への参加の計測/裁定を待つ母集団の案.rs` | 語そのものは設計オントロジーの文書が第3節で使っている。識別子へ持ち込むのはこの作業が初めてであり、ユビキタス言語に登録が無いためここへ挙げる |

### 分類と公理で付けた語(2026-09-26。Issue #193 の段階1)

次の4語は、設計解釈マーカーの実装と上位トレイトの宣言を設計関係の辺から外し、節点のラベルとして持つために実装員が識別子へ持ち込んだ語である。「分類」と「公理」は Issue #193 の「合意した考え方」と「契約」が使う語であり、ここでは識別子としての定義を挙げる。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| 分類 | 開発基盤 | 分類とは、型が実装した設計解釈マーカーの集合を、設計解釈マーカーどうしの上位トレイトの宣言で閉じたもののことである。`M状態` を実装した型は `M不変データ` も持つ。`Clone` のような標準のトレイトは、上位トレイトの宣言に在っても分類に入らない。分類を持つのは型だけであり、トレイトと処理は持たない。原子命題 `マーカーを持つ` は、型でない主語と設計解釈マーカーでない名前について未決定で答える | `crates/blitz_design_verification/src/分類.rs`(定義)・`crates/blitz_design_verification/src/atom.rs`・`crates/blitz_design_verification/src/quantification.rs` | 登録語の「設計解釈マーカー」を検討した。マーカーは1つの印であり、型が持つマーカーの集合を上位トレイトで閉じたものを指す語が要った。旧来の `下位型である` の辺はオーナーが呼び方ごと退けた |
| 分類の事実 | 開発基盤 | 分類の事実とは、分類を決める2種類の事実、すなわちマーカーの実装の事実(`impl マーカー for 型` の1件)と上位トレイトの宣言の事実(`trait A: B` の上位トレイト1つぶんの1件)を、抽出したまま並べたもののことである。あわせて、分類に入りうるトレイトである設計解釈マーカーの一覧を持つ。この一覧は抽出器が `cargo xtask conform` の持つマーカーの正本の一覧から渡し、検証器は写しを持たない。上位トレイトの宣言の事実が公理の正本であり、手書きの公理の表を持たない | `crates/blitz_design_verification/src/分類/事実.rs`(定義)・`xtask/src/design_model/extract/marker_impl.rs`・`xtask/src/design_model/extract/supertrait.rs`・`xtask/src/design_model/extract/outcome.rs` | 「設計関係」を検討した。マーカーの実装も上位トレイトの宣言も2つの概念のあいだの関係ではなく、設計関係と呼ぶと辺として参加に数えられる |
| 節点の分類 | 開発基盤 | 節点の分類とは、設計関係グラフが持つ、節点ごとの分類を引ける索引のことである。分類の事実から閉包を計算して組み、組んだ事実そのものも持つ。分類は辺でないため、設計関係への参加の問いはこの索引を読まない | `crates/blitz_design_verification/src/分類.rs`(定義)・`crates/blitz_design_verification/src/relation_graph.rs` | 「設計関係グラフ」を検討した。グラフは節点と辺の全体であり、そのうち節点のラベルだけを受け持つ部分に名前が要った |
| 事実の出どころ | 開発基盤 | 事実の出どころとは、分類の事実の1件がどのソースのどの行から出たかを表す、パスと行番号の組のことである | `crates/blitz_design_verification/src/分類/事実.rs`(定義) | 登録語の「抽出元の構文」を持つ `抽出の出どころ` を検討した。分類の事実は事実の種類が構文を決めるため、構文の欄を持つと同じことを2度言い、食い違いうる。Issue #193 の段階2で設計関係も事実から導く形へ移るとき、出どころの型をこれへ寄せる |

### 地点と経路の表現で付けた語(2026-09-26。Issue #187 の作業Gの前提)

次の5語は、`_doc/設計/Esca/設計正本.md` 5節第2段階が、第2段階の設計判断を書くために新しく使った語である。どれもまだ実装が無く、型の名前だけを予定として文書へ書いた。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| 地点 | ゲーム実行 | 地点とは、世界の中の1つの区域を持つ、地点と経路のグラフの節点のことである。旅行者がどの地点にいるかは、旅行者の居場所が現在地と一緒に持つ(境目の上の点は2つの区域のどちらの閉包にも入り、点からは決められないため) | `_doc/設計/Esca/設計正本.md` 5節 | 「旅行者の現在地」を検討した。現在地は東と北の連続な座標を持つ値であり、地点は区域という離散した単位を指す別の概念である |
| 区域 | ゲーム実行 | 区域とは、地点が持つ、地面を上から見た多角形の世界の中の範囲のことである。歩行の遷移は、旅行者の現在地がいる地点の区域の閉包にあることを確かめ、境目で滑るか通り口を抜ける。いる地点を点が範囲に入るかで判定するのは、最初の居場所を決めるとき(`区域の空間制約::点を含む地点を決める`)だけである | `_doc/設計/Esca/設計正本.md` 5節 | 既存語に無かった。第1段階の`移動可能範囲`は世界全体の境界を表し、世界を分割した1つ1つの範囲を指す語ではない |
| 経路 | ゲーム実行 | 経路とは、隣り合う2つの区域のあいだで通り抜けられる箇所（橋・門・峠道など）のことである | `_doc/設計/Esca/設計正本.md` 5節 | 既存語に無かった。ユビキタス言語の「経路」は各所で通り道を指す普通語として使われており、区域どうしをつなぐドメイン概念としての用法はまだ無い |
| 地点と経路のグラフ | ゲーム実行 | 地点と経路のグラフとは、地点と経路のつながりを、ゲームの開始時に組んだ後は変わらない`M不変データ`として持つ、Graphite（Rustの型で検査されるグラフ記述言語）の型付きのグラフのことである | `_doc/設計/Esca/設計正本.md` 5節 | 既存語に無かった。つながりが不変であることと、Graphiteの型付きグラフで表すことを1つの名前で言う語が要った |
| 経路の開閉の状態 | ゲーム実行 | 経路の開閉の状態とは、閉じている経路の識別子の集合を持つ`M状態`のことである。`M遷移関数`が、出来事から作られる開閉の命令と、遷移パラメータの刻みに来た時刻の予定を入力に、その新しい値を返す(2026-09-26 の作業G1で、時刻を出来事として扱う案から遷移パラメータの時刻を規則が判断する形へ改めた)。歩行の側の制約は、この状態が閉じている経路を通さない形で参照する | `_doc/設計/Esca/設計正本.md` 5節 | 既存語に無かった。地点と経路のグラフは不変であり、封鎖と開通という可変の事実を別の型で持つ必要があった |

### 地点と経路の開閉の実装で付けた語(2026-09-26。Issue #187 の作業G1)

次の語は、作業G1(地点と経路のつながりと経路の開閉)の実装員が、上の5語を型にするときに新しく持ち込んだ語である。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| 世界の時刻 | ゲーム実行 | 世界の時刻とは、世界が始まってから経った、有限かつ0以上の時間を表す値オブジェクトのことである。中身を登録語の `大域秒`(倍精度)で持つ。遷移パラメータは、この刻みが始まった世界の時刻(刻みの始まりの時刻)を持ち、刻みの終わり(次の刻みの始まり)を作る口は遷移パラメータの `次の刻みへ進める` の1つだけである | `crates/blitz_esca/src/時刻.rs`(定義)・`crates/blitz_esca/src/transition_parameter.rs` | 登録語の「経過時間」を検討した。経過時間は1回の遷移が進める時間の長さであり、時刻どうしの前後の比較と、時刻に長さを足して次の時刻を得る演算は長さでは表せない |
| 閉じている時間帯 | ゲーム実行 | 閉じている時間帯とは、経路が時間によって閉じている区間のことであり、始まりの時刻に経路が閉じ、終わりの時刻に経路が開く。始まりを含み終わりを含まず、始まりは終わりより前である。検証の前の公開フィールドの入力を閉じている時間帯の指定と呼ぶ | `crates/blitz_esca/src/地点と経路/時間帯.rs`(定義)・`crates/blitz_esca/src/地点と経路/開閉の予定.rs` | 既存語に無かった。設計正本は「経路ごとの閉じている時間帯」と散文で書いており、それを型の名前にした |
| 経路の開閉 | ゲーム実行 | 経路の開閉とは、経路1本が開いているか閉じているかを表す2つの値のことである。予定のはじめの開閉と、予定や命令が経路を変える先の両方をこの型で表す | `crates/blitz_esca/src/地点と経路/開閉の予定.rs`(定義) | 真偽値で「閉じているか」と持つことを検討した。真偽値は予定の変化の向き(閉じる・開く)を名前で読めない |
| 経路の開閉の予定 | ゲーム実行 | 経路の開閉の予定とは、ステージを作る人が経路1本ごとに置く、はじめに開いているか閉じているかと、閉じている時間帯の並びのことである。地点と経路のグラフの辺の積み荷として持つ。時間帯は時刻の順に並び、互いに重ならない | `crates/blitz_esca/src/地点と経路/開閉の予定.rs`(定義)・`crates/blitz_esca/src/地点と経路/図式.rs` | 既存語に無かった。設計正本の「はじめは閉じた経路として置いておく」と「閉じている時間帯」の2つを、経路1本ぶんの1つの値として呼ぶ語が要った |
| 地点と経路の図式 | ゲーム実行 | 地点と経路の図式とは、地点を節点、経路を辺とし、経路が開閉の予定を積み荷に持つことを宣言したGraphiteの図式(schema)と、その生成物のモジュールのことである。完成したグラフを複製できる図式である | `crates/blitz_esca/src/地点と経路/図式.rs`(宣言)・`crates/blitz_esca/src/地点と経路/generated/地点と経路の図式.rs`(生成物) | 登録を待つ「地点と経路のグラフ」を検討した。地点と経路のグラフは地図として読み取りの口だけを持つ `M不変データ` であり、書き換えの口を持つGraphiteの完成したグラフとその図式は、それと別のものである |
| 図式 | ゲーム実行 | 図式とは、Graphiteのschema(節点の種別・辺の種別・積み荷を宣言したもの)の訳語である。モジュール名 `地点と経路::図式` に使う | `crates/blitz_esca/src/地点と経路.rs` | 借用語の「スキーマ」を検討した。小学生が分かるカタカナ語ではない。Graphiteの文書も「図式グラフ」と書いている |
| 時刻 | ゲーム実行 | モジュール名としての時刻とは、世界の時刻とその生成の失敗を持つモジュールのことである | `crates/blitz_esca/src/lib.rs`・`crates/blitz_esca/src/時刻.rs` | モジュール名を型名と同じ `世界の時刻` にすると、クレートの根で型を再公開したときに名前が衝突するため、短い別の名前にした |
| 時間帯 | ゲーム実行 | モジュール名としての時間帯とは、閉じている時間帯とその指定と生成の失敗を持つモジュールのことである | `crates/blitz_esca/src/地点と経路.rs`・`crates/blitz_esca/src/地点と経路/時間帯.rs` | 同上 |
| 開閉の予定 | ゲーム実行 | モジュール名としての開閉の予定とは、経路の開閉と経路の開閉の予定とその生成の失敗を持つモジュールのことである | `crates/blitz_esca/src/地点と経路.rs`・`crates/blitz_esca/src/地点と経路/開閉の予定.rs` | 同上 |
| 地点の識別子・経路の識別子 | ゲーム実行 | 地点の識別子(経路の識別子)とは、地図(地点と経路のグラフ)がその地点(経路)が自分の中に在ることを確かめてから渡す、Graphiteの生成した識別子を包んだ値オブジェクトのことである。地図の外では作れない。経路の識別子は中身の文字列の順で全順序を持つ | `crates/blitz_esca/src/地点と経路/識別子.rs`(定義)・`crates/blitz_esca/src/地点と経路/地図.rs` | Graphiteの生成した `地点Id`・`経路Id` をそのまま使うことを検討した。中身の文字列が公開されており誰でも作れるため、地図に無い地点・経路を指す値が型の上で作れてしまう |
| 経路の両端 | ゲーム実行 | 経路の両端とは、向きの無い経路がつなぐ2つの地点の識別子の組のことである。どちらを一方と呼ぶかに意味は無い | `crates/blitz_esca/src/地点と経路/地図.rs`(定義) | 既存語に無かった。生のタプルで返すことは多重戻り値の生タプル禁止(設計正本 4.4)が禁じる |
| 経路の開閉の規則 | ゲーム実行 | 経路の開閉の規則とは、開閉の遷移へ読み取り専用で与える`M規則`であり、地図を1つ持つ。命令が指す経路が地図に在るかを確かめる口と、遷移パラメータの刻みに来た時刻の予定から開閉の変化を求める口を持つ | `crates/blitz_esca/src/地点と経路/開閉の規則.rs`(定義) | 地図そのものを`M規則`にすることを検討した。地図は歩行の側からも読まれる`M不変データ`であり、開閉の遷移の規則はその読み方の1つである |
| 経路の開閉の命令 | ゲーム実行 | 経路の開閉の命令とは、出来事をきっかけに経路を閉じる・開くことを開閉の遷移へ求める`Mコマンド`のことである。選択肢は経路を閉じる・経路を開く・命令が無いの3つである。命令が無いは、時刻の予定だけを当てる刻みのためにある | `crates/blitz_esca/src/地点と経路/開閉の命令.rs`(定義) | 「意図」(旅行者の意図)を検討した。意図は主体(旅行者)が選ぶものであり、経路の開閉は世界の出来事への反応として作られるため、主体の意図と呼べない |
| 経路の開閉の出来事 | ゲーム実行 | 経路の開閉の出来事とは、開閉の遷移によって経路が実際に閉じた・開いたという過去の事実を表す`Mイベント`のことである。遷移の前後の開閉の差からだけ組み、きっかけ(時刻か命令か)を持たない | `crates/blitz_esca/src/地点と経路/開閉の出来事.rs`(定義) | 既存語に無かった |
| 経路の指定の失敗 | ゲーム実行 | 経路の指定の失敗とは、経路の開閉の状態の生成と開閉の遷移が、地図に無い経路を指した値を拒む理由のことである。開閉の遷移の遷移失敗結果の失敗の理由になる | `crates/blitz_esca/src/地点と経路/指定の失敗.rs`(定義) | 既存語に無かった |
| 経路の開閉の変化 | ゲーム実行 | 経路の開閉の変化とは、経路1本の開閉をどちらへ変えるかの1件のことである。時刻の予定と命令の両方がこの形で変化を出し、開閉の遷移が出た順に当てる。モジュールの外へ出さない | `crates/blitz_esca/src/地点と経路/開閉の規則.rs`(定義)・`crates/blitz_esca/src/地点と経路/開閉の命令.rs`・`crates/blitz_esca/src/地点と経路/開閉の状態.rs` | 既存語に無かった |
| 出来事が触れた経路 | ゲーム実行 | 出来事が触れた経路とは、経路の開閉の状態が閉じている経路の集合と並べて持つ、開閉の命令(出来事から作られる)が一度でも閉じたり開けたりした経路の識別子の集合のことである。時刻の予定はこの集合の経路に当たらず、その経路の開閉を変えられるのは別の命令だけである(2026-09-26 オーナー裁定「時刻の予定と出来事がぶつかったら出来事が勝つ」) | `crates/blitz_esca/src/地点と経路/開閉の状態.rs`(定義)・`crates/blitz_esca/src/地点と経路/開閉の規則.rs`・`crates/blitz_esca/src/tests/経路の開閉の出来事と予定の試験.rs`・`_doc/設計/Esca/設計正本.md` 5節 | 「命令で変えた経路」を検討した。命令は出来事から作られる値であり、裁定の文言が「出来事で一度閉じたり開けたりした経路」と言うため、裁定の語に合わせた |
| 刻みの始まりの時刻 | ゲーム実行 | 刻みの始まりの時刻とは、遷移パラメータが持つ、その刻みが始まった世界の時刻のことである。1回の遷移は、刻みの始まりの時刻を含み、経過時間だけ進んだ時点を含まない区間を覆う | `crates/blitz_esca/src/transition_parameter.rs`(定義)・`crates/blitz_esca/src/地点と経路/開閉の予定.rs` | 「世界時刻」(設計オントロジー 3.5.1 の遷移パラメータの例)を検討した。刻みの始まりと終わりのどちらの時刻かを名前で言う必要があった |
| 経路の開閉を数え上げる領域 | ゲーム実行 | 経路の開閉を数え上げる領域とは、経路の開閉の実物の遷移関数に当てる、有限に数え上げられる領域の試験側の実装のことである。初期状態は地図の経路のあらゆる部分集合、コマンドは命令が無いと地図の経路と地図の外の経路それぞれの開閉であり、どちらも全件である | `crates/blitz_esca/src/tests/経路の開閉の数え上げ.rs`(定義)・`crates/blitz_esca/src/tests/経路の開閉の数え上げの試験.rs` | 既存の「旅行者の歩行を数え上げる領域」と同じ形の名前である |
| 地図・識別子・指定の失敗・開閉の出来事・開閉の命令・開閉の状態・開閉の規則 | ゲーム実行 | モジュール名としてのこれらは、それぞれ地点と経路のグラフ・地点と経路の識別子・経路の指定の失敗・経路の開閉の出来事・経路の開閉の命令・経路の開閉の状態・経路の開閉の規則を持つ、`地点と経路` の子のモジュールのことである。「地図」は地点と経路のグラフを散文で呼ぶ短い名前でもある | `crates/blitz_esca/src/地点と経路.rs` | モジュール名を型名と同じにすると、親のモジュールで型を再公開したときに名前が衝突するため、型名から「経路の」を落とした名前にした |
| 遷移失敗結果の元の状態の食い違い | 開発基盤 | 遷移失敗結果の元の状態の食い違いとは、有限全数の検証の探索が実物の遷移関数を呼んで失敗した遷移のうち、遷移失敗結果に入れて返した元の状態が遷移の受け取った状態と等しくなかったものの並びのことである。探索はこれが1件でもあれば遷移モデルを返さずにこの値を返す。探索が失敗した遷移ごとに比べて控える台帳を元の状態の照合と呼ぶ | `crates/blitz_design_verification/src/domain/exploration/元の状態の食い違い.rs`(定義)・`crates/blitz_design_verification/src/domain/exploration/breadth_first.rs`・`_doc/設計/設計オントロジー.md` 3.5.9 | 「数え上げの網羅性」の選択肢にすることを検討した。網羅性は見た範囲の限りを言う語であり、遷移関数が契約を破ったことは見た範囲の問題ではないため、同じ語へ入れると1語1義が破れる |
| Graphiteの生成物の照合 | 開発基盤 | Graphiteの生成物の照合とは、Graphiteに依存するパッケージごとに `cargo graphite generate --check` を走らせ、生成物の本文が宣言から生成し直した結果と一致することを確かめる、`cargo xtask graphite-check` の工程のことである。検証の標準列の規約検査の次の段である | `xtask/src/graphiteの生成物の照合.rs`(定義)・`xtask/src/verify/sequence.rs` | 登録を待つ「Graphiteの生成物」を検討した。生成物は照合される対象であり、照合する工程の名前が別に要った |
| 地点と経路 | ゲーム実行 | モジュール名としての地点と経路とは、Escaの第2段階の地点と経路のつながりと経路の開閉を持つモジュールのことである | `crates/blitz_esca/src/lib.rs`・`crates/blitz_esca/src/地点と経路.rs` | 登録を待つ「地点」「経路」をつないだ名前であり、新しい概念ではない |

### 作業G1の検収の指摘の是正で付けた語(2026-09-26。Issue #187 の作業G1)

次の語は、作業G1の検収の指摘(graphite-check の生成器の版と対象の漏れ・計測からの生成物の除き方)を直すために実装員が識別子へ持ち込んだ語である。有限全数の検証の側の語「遷移失敗結果の元の状態の食い違い」は上の作業G1の表に置く。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| Graphiteの固定の版 | 開発基盤 | Graphiteの固定の版とは、Cargo.lock が固定した graphite の包みの git のコミット(40桁の16進)のことである。生成器を入れるコマンドの `--rev` と、入れた生成器の出どころと比べる相手になる | `xtask/src/graphiteの生成物の照合/生成器の版.rs`(定義)・`xtask/src/graphiteの生成物の照合/生成器の版のファイル.rs` | 登録語の「生成器の版」(世界を焼く規則の版番号)を検討した。あちらは資産ストリーミングの生成器の版番号であり、Graphiteの生成器のコミットを指すと1語1義が破れる |
| 依存の版を固定したファイル | 開発基盤 | 依存の版を固定したファイルとは、cargo が依存の版を固定して書き出すファイル(Cargo.lock)のことである。識別子へ英字の混じった `Cargo_lock` を書くと Rust の命名の警告に落ちるため、実体を日本語で書いた | `xtask/src/graphiteの生成物の照合/生成器の版.rs`(定義)・`xtask/src/graphiteの生成物の照合/生成器の版のファイル.rs` | 既存語に無かった。借用語の「ロックファイル」は小学生が分かるカタカナ語ではない |
| 生成器の版の食い違い | 開発基盤 | 生成器の版の食い違いとは、cargo が入れた道具の記録にある Graphite の生成器の出どころが、Graphiteの固定の版と一致しなかった理由のことである。選択肢は記録に生成器が無い・コミットが違う・コミットを確かめられない出どころ(パスから入れた生成器)の3つである | `xtask/src/graphiteの生成物の照合/生成器の版.rs`(定義)・`xtask/src/graphiteの生成物の照合/照合の失敗.rs` | 既存語に無かった |
| 入れた生成器の確かめ | 開発基盤 | 入れた生成器の確かめとは、Cargo.lock と cargo が入れた道具の記録の2つのファイルの場所を保持し、それを読んで、入れた生成器がGraphiteの固定の版から作られているかを確かめる係のことである | `xtask/src/graphiteの生成物の照合/生成器の版のファイル.rs`(定義)・`xtask/src/graphiteの生成物の照合.rs` | 既存語に無かった。モジュール名を型名と同じにすると名前が衝突するため、モジュール名は「生成器の版のファイル」とした |
| 生成物を持つパッケージの一覧 | 開発基盤 | 生成物を持つパッケージの一覧とは、conform が見出しで認めたGraphiteの生成物を1件以上持つパッケージのディレクトリの並びのことである。graphite-check はこれが照合の対象に全部入っていることを確かめる | `xtask/src/graphiteの生成物の照合/生成物を持つパッケージ.rs`(定義)・`xtask/src/graphiteの生成物の照合.rs` | 登録を待つ「Graphiteの生成物」を検討した。生成物はファイルであり、照合の単位のパッケージを言う語が要った |
| 生成物を除いた観測 | 開発基盤 | 生成物を除いた観測とは、型の計測が走査したファイルごとの観測から、見出しで認めたGraphiteの生成物を除いたものと、除いた生成物のパスの並びの組のことである。単独の type-metrics と conform の型ごとの分量の台帳の検査と自由関数の検査が、同じこの値から観測を得る | `xtask/src/type_metrics/生成物の除外.rs`(定義)・`xtask/src/type_metrics/mod.rs`・`xtask/src/conform/type_metrics_ledger.rs`・`xtask/src/conform/free_function_whole_type.rs` | 既存の「ファイルの観測」を検討した。ファイル1本の観測であり、生成物を除いたことと除いた生成物の並びを言えなかった。モジュール名は型名との衝突を避けて「生成物の除外」とした |
| 経路の開閉の状態の指定 | ゲーム実行 | 経路の開閉の状態の指定とは、経路の開閉の状態を作るための、閉じている経路の並びと出来事が触れた経路の並びを名前付きの公開フィールドで持つ指定のことである。規則(`経路の開閉の規則::指定を地図で確かめて状態を作る`)が、どちらの並びの経路も自分の地図に在ることを確かめて状態にする | `crates/blitz_esca/src/地点と経路/開閉の状態の指定.rs`(定義)・`crates/blitz_esca/src/地点と経路/開閉の規則.rs`・`crates/blitz_esca/src/地点と経路/開閉の状態.rs`・`_doc/設計/Esca/設計正本.md` 5節 | 「経路の開閉の状態」を検討した。状態は地図で確かめた後の値であり、確かめる前の入力を同じ語で呼ぶと、規則を通らずに作れないという不変条件が名前から読めない。同じ型の2つの並びを2引数で受けると取り違えても型が通るため、名前付きのフィールドを持つ型にした。様式は `閉じている時間帯の指定`・`移動可能範囲の指定` に揃えた |

### Graphiteのコードの検査の扱いで付けた語(2026-09-26。Issue #187 の作業C)

次の5語は、`cargo xtask conform` と設計関係の抽出がGraphite(Rustの型で検査されるグラフの記述言語)のコードをどう扱うかを実装するために実装員が識別子へ持ち込んだ語である。「生成物」と「見出し」はオーナーの裁定の文言に現れた語であり、裁定の文言を識別子へ写したものである。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| Graphiteの生成物 | 開発基盤 | Graphiteの生成物とは、Graphiteの生成器が利用者のschemaの宣言から書き出し、手編集を禁じられたRustのファイルのうち、先頭の見出しが名乗る生成元のファイルがそのファイルを生成先として宣言しているもののことである | `xtask/src/conform/graphiteのコード/生成物の一覧.rs`(定義)・`xtask/src/conform/single_file.rs`・`xtask/src/conform/design_ontology/scan_entry.rs`・`xtask/src/design_model/extract/本番のソース.rs` | 登録語の「生成物」は大規模世界の生成が焼くチャンクやアセットを指しており、Graphiteの生成器が書くRustのファイルを指す用法は無い。区別のためGraphiteを冠した |
| 生成物の見出し | 開発基盤 | 生成物の見出しとは、Graphiteの生成器が生成物の先頭の2行へ書く、手編集を禁じる文と生成元の案内のことである | `xtask/src/conform/graphiteのコード/生成物の見出し.rs`(定義) | 既存語に無かった。「見出し」は生成台帳の見出しの4行を指す登録の用法があるが、別の区切られたドメイン(資産ストリーミング)の語である |
| 生成物の取り込み | 開発基盤 | 生成物の取り込みとは、Graphiteが利用者に書かせる、本体が `include!` の1文だけの波括弧付きのモジュールであり、Graphiteの生成物を1つ取り込むもののことである。設計関係の抽出はこれを辿らない | `xtask/src/conform/graphiteのコード/生成物の取り込み.rs`(定義)・`xtask/src/design_model/extract/unextracted_line.rs` | 既存語に無かった。モジュールの宣言の本体の形(別のファイル・波括弧の中)の語彙はあるが、本体が生成物の取り込みだけであることを言う語が要った |
| Graphiteの宣言 | 開発基盤 | Graphiteの宣言とは、利用者が手で書くGraphiteのコードのうち、`dynamic_graph_schema!`・`static_graph_schema!`・`graph!` の呼び出しと、生成物の置き場を名指す `generated = "..."` の項を持つ静的グラフのinstanceの呼び出しのことである | `xtask/src/conform/graphiteのコード/手書きの宣言.rs`(定義)・`xtask/src/conform/graphiteのコード/マクロの本体.rs` | 既存語に無かった。オーナーの裁定は「利用者が手で書くGraphiteのコード」と呼んだが、生成先の項とマクロの本体を読む工程の名前として短く呼ぶ語が要った |
| Graphiteのマクロの本体の行 | 開発基盤 | Graphiteのマクロの本体の行とは、原文の中のGraphiteのマクロ(`dynamic_graph_schema!`・`static_graph_schema!`・`graph!`・静的グラフのinstanceのマクロ)の呼び出しについて、開きの区切り記号の行と閉じの区切り記号の行に挟まれた行の集まりのことである。行数の検査はこの行を数えから除く | `xtask/src/conform/graphiteのコード/マクロの本体.rs`(定義)・`xtask/src/conform/line_count.rs` | 既存語に無かった。「Graphiteの宣言」は呼び出し全体を指し、開きと閉じの行(呼び出しを書くRustの行)を含むため、数えから除く範囲を言うには足りなかった |

次の2語は、作業Cの検収の指摘(原文の一覧が名前の無いまま持ち回られ、パスで探す処理が2か所で重複している)を直すために実装員が識別子へ持ち込んだ語である。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| 走査した原文の一覧 | 開発基盤 | 走査した原文の一覧とは、検査や抽出が走査して読んだファイルのパスと原文の対を読んだ順に並べ、パスで原文を探す口を持つ値のことである | `xtask/src/conform/走査した原文の一覧.rs`(定義)・`xtask/src/conform/single_file.rs`・`xtask/src/conform/design_ontology/scan_entry.rs`・`xtask/src/conform/graphiteのコード/生成物の一覧.rs`・`xtask/src/design_model/extract/本番のソース.rs` | 既存の `抽出対象のソース群` を検討した。あちらは本番の選別を済ませたコードだけの行を持ち、型の同一性を答える抽出側の値であり、規約の検査が読む原文そのものを持たない。規約の検査と抽出の両方が使う原文の段の値を言う語が要った |
| 斜線で揃えたパス | 開発基盤 | 斜線で揃えたパスとは、パスの `.` と `..` を解き、区切りを斜線へ揃えてつないだ表記のことである。2つのパスが同じファイルを指すかを、実行環境の区切りと宣言の中の表記の書き方に左右されずに比べるために使う | `xtask/src/conform/走査した原文の一覧/斜線の表記.rs`(定義)・`xtask/src/conform/graphiteのコード/生成物の一覧.rs`・`xtask/src/design_model/extract/本番のソース.rs` | 既存語に無かった。行数の台帳が斜線へ揃える処理(`台帳の表記へ揃える`)を持つが、`.` と `..` を解かず、名前の付いた型も無い |

### 事実と定義の式で付けた語(2026-09-26。Issue #193 の段階2)

次の語は、抽出器がプリミティブな事実だけを出し、設計関係と保持するを定義の式で導く形へ変えるために実装員が識別子へ持ち込んだ語である。「プリミティブな事実」と「定義の式」は Issue #193 の契約の文言を識別子へ写したものである。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| プリミティブな事実 | 開発基盤 | プリミティブな事実とは、抽出器がRustのソースから読む、コンパイラが知っている形の事実のことである。型の定義・フィールドの型の表記・エンティティの識別子の関連型・関数の役割の宣言の4種の一覧を持つ。マーカーの実装と上位トレイトの宣言は同じ高さの事実であり、分類の事実が持つ | `crates/blitz_design_verification/src/導出/プリミティブな事実の定義.rs`(定義)・`xtask/src/design_model/extract/outcome.rs` | Issue #193 の契約の語をそのまま型の名前にした。既存の「分類の事実」は分類を決める2種だけを指し、関係を導く事実を含まない |
| 定義の式 | 開発基盤 | 定義の式とは、設計の関係とプリミティブの高さの関係(保持する)を、プリミティブな事実の上の規則として値で並べたもののことである。規則を当てる順に持ち、評価するとプリミティブな事実から関係を導く | `crates/blitz_design_verification/src/導出/定義の式の規則一覧.rs`(定義)・`crates/blitz_design_verification/src/導出/評価.rs`・`xtask/src/design_model/extract/outcome.rs` | Issue #193 の契約の語をそのまま型の名前にした |
| 導出の規則 | 開発基盤 | 導出の規則とは、定義の式の規則1件のことであり、名前・頭の関係・本体の原子の並び(連言)・導いた関係の出どころに書く構文を持つ | `crates/blitz_design_verification/src/導出/定義の式の規則一覧.rs`(定義) | 「規則」は `M規則`(ゲームの規則)と衝突する。定義の式の中の1件であることを「導出の」で言った |
| 頭の関係 | 開発基盤 | 頭の関係とは、導出の規則の本体が成り立つときに導く関係の種類のことである。設計関係(設計関係の種類の1つ)か、保持するかを選ぶ | `crates/blitz_design_verification/src/導出/定義の式の規則一覧.rs`(定義)・`crates/blitz_design_verification/src/導出/評価.rs` | 論理プログラミングの「頭部(head)」の直訳を避け、規則の結論の側の関係であることを言った。案として「導く関係」も在る |
| 本体の原子 | 開発基盤 | 本体の原子とは、導出の規則の本体を成す、それ以上分解しない条件のことである。事実の原子と、型の表記を読む原子(表記が型を名指す)の2つを選ぶ | `crates/blitz_design_verification/src/導出/規則の本体の原子.rs`(定義)・`crates/blitz_design_verification/src/導出/評価.rs` | 登録語の「原子命題」は検証器が解く命題の最小の主張であり、規則の本体の条件とは別物である |
| 事実の原子 | 開発基盤 | 事実の原子とは、プリミティブな事実を1件選び、概念の変数を1つと型の表記を1つ当てる本体の原子のことである | `crates/blitz_design_verification/src/導出/規則の本体の原子.rs`(定義) | 本体の原子のうち事実を選ぶものを区別する語が要った |
| 概念の変数 | 開発基盤 | 概念の変数とは、導出の規則の頭と本体が名指す、主語と目的語の2つの変数のことである | `crates/blitz_design_verification/src/導出/規則の本体の原子.rs`(定義)・`crates/blitz_design_verification/src/導出/束縛.rs` | 登録語の「束縛変数」は量化の命題が当てる変数であり、規則の変数と別物である |
| 束縛(導出の) | 開発基盤 | 束縛とは、規則の本体を左から当てていく途中で、概念の変数と型の表記へ何を当てたかと、使った事実の出どころを持つ値のことである。導出の木の中だけで使う | `crates/blitz_design_verification/src/導出/束縛.rs`(定義)・`crates/blitz_design_verification/src/導出/評価.rs` | 登録語の「束縛の割り当て」は量化の割り当てであり別物である。型は導出の木の外へ出さない |
| 型の表記 | 開発基盤 | 型の表記とは、ソースに書かれた型の表記1件のことであり、包みを剥がす前の綴りと、その表記を書いた場所で有効な型引数の名前の一覧を持つ | `crates/blitz_design_verification/src/導出/プリミティブな事実の定義.rs`(定義)・`xtask/src/design_model/extract/held_declaration.rs` | 既存コードは同じものを裸の文字列で持っていた。型引数の名前を添えないと、型引数と設計概念を表記だけから見分けられない |
| 型の定義の事実 | 開発基盤 | 型の定義の事実とは、`struct`・`enum` の宣言1件がその型の節点を立てるという事実のことである | `crates/blitz_design_verification/src/導出/プリミティブな事実の定義.rs`(定義)・`xtask/src/design_model/extract/held_field.rs` | Issue #193 の契約の「型の定義」を事実の型にした |
| フィールドの型の表記の事実 | 開発基盤 | フィールドの型の表記の事実とは、`struct`・`enum` のフィールド1つについて、持ち主の型と、フィールドの型の表記を包みを剥がす前のまま並べた事実のことである | `crates/blitz_design_verification/src/導出/プリミティブな事実の定義.rs`(定義)・`xtask/src/design_model/extract/held_declaration.rs` | Issue #193 の契約の語を事実の型にした |
| エンティティの識別子の関連型の事実 | 開発基盤 | エンティティの識別子の関連型の事実とは、`impl Mエンティティ for 型 { type 識別子 = 表記; }` の1件について、実装した型と右辺の表記を並べた事実のことである | `crates/blitz_design_verification/src/導出/プリミティブな事実の定義.rs`(定義)・`xtask/src/design_model/extract/entity_identifier.rs` | Issue #193 の契約の語を事実の型にした |
| 関数の役割の宣言の事実 | 開発基盤 | 関数の役割の宣言の事実とは、関数の役割の宣言1件について、役割と、型引数の表記を位置の順に並べた一覧と、包んだ関数の節点を並べた事実のことである | `crates/blitz_design_verification/src/導出/プリミティブな事実の定義.rs`(定義)・`xtask/src/design_model/extract/function_role.rs` | 登録語の「関数の役割の宣言」を事実の型にした |
| 保持する関係 | 開発基盤 | 保持する関係とは、型が別の型をフィールドとして持つという、プリミティブの高さの関係のことである。設計関係と別の型であり、設計関係グラフが別の一覧で持つ。設計の意味を持たないため、設計関係への参加に数えない | `crates/blitz_design_verification/src/relation/保持する関係の定義.rs`(定義)・`crates/blitz_design_verification/src/relation_graph/保持する関係の問い.rs`・`crates/blitz_design_verification/src/atom.rs` | Issue #193 は「保持する」を設計の関係と別の高さに置くと決めた。設計関係の種類の1つのままでは高さを分けられない |
| 導出の根拠 | 開発基盤 | 導出の根拠とは、導出した1件の関係について、それを導いた規則の名前と、使った事実の出どころを並べたもののことである | `crates/blitz_design_verification/src/relation/導出の根拠の定義.rs`(定義)・`crates/blitz_design_verification/src/導出/評価.rs` | 既存の「抽出の出どころ」は1つの行と構文だけを持ち、規則の名前を持たない |
| 導出の結果 | 開発基盤 | 導出の結果とは、定義の式を評価して得た、設計関係と保持する関係と、それらの端点の節点と、導けなかった箇所の一覧のことである | `crates/blitz_design_verification/src/導出/導出の結果の定義.rs`(定義)・`xtask/src/design_model/extract/outcome.rs` | 既存語に無かった |
| 導出の欠け | 開発基盤 | 導出の欠けとは、定義の式が型の表記から型を名指せなかった1件のことであり、表記の出どころと理由(表記を読めない・型引数である・定義をたどれない)を持つ。抽出器はこれを抽出できなかった行へ写す | `crates/blitz_design_verification/src/導出/導出の結果の定義.rs`(定義)・`xtask/src/design_model/extract/outcome.rs` | 登録語の「抽出できなかった行」は抽出器の語彙であり、このクレートは抽出器を知らない |
| 型の表記を読めない理由 | 開発基盤 | 型の表記を読めない理由とは、定義の式が型の表記の綴りを読めなかった理由のことである。Rustのパスでない・剥がす包みでないジェネリクスである・フレーム型の型引数が座標系の名前の並びでない・パスの修飾があるの4つを選ぶ | `crates/blitz_design_verification/src/導出/型の表記を読めない理由の定義.rs`(定義)・`xtask/src/design_model/extract/out_of_range_syntax.rs` | 段階2の前は抽出器の「保証範囲の外の構文」の4つの選択肢であった。表記を読むのが定義の式の側へ移ったため、その語彙を定義の式の側へ移した |
| 型の表記の読み方 | 開発基盤 | 型の表記の読み方とは、型の表記の綴りから、その表記が名指す設計概念の名前を採る定義の式の側の知識のことである。包みの剥がし方・プリミティブの一覧・フレーム型の扱いを持つ | `crates/blitz_design_verification/src/導出/型の表記の読み方.rs`(定義) | 段階2の前は抽出器の `type_notation` が持っていた。モジュールの名前として日本語の名前が要った |
| 型の名前の解決 | 開発基盤 | 型の名前の解決とは、型の表記を書いた場所から見て、その名前の型の定義がどのモジュールに在るかを答える口のことである。抽出器の抽出対象のソース群が実装する | `crates/blitz_design_verification/src/導出/型の名前の解決の定義.rs`(定義)・`xtask/src/design_model/extract/source_group.rs` | 既存語に無かった。英語の直訳の「名前解決」を避け、何の名前を何に解決するかを言った |
| 問う辺 | 開発基盤 | 問う辺とは、構造の検証器が関係の在り方を問うときに、設計関係のどの種類を問うか、保持する関係を問うかの区別のことである | `crates/blitz_design_verification/src/verifier/structural/atom/relation_question.rs`(定義) | 既存語に無かった。問う関係の高さを1つの値で運ぶ語が要った |
| 関数の役割の定義の在り処 | 開発基盤 | 関数の役割の定義の在り処とは、関数の役割の型と、その役割に伴う意味型を定義するモジュールのパスを答える、抽出器の側のトレイトのことである | `xtask/src/design_model/extract/role.rs`(定義) | 定義の場所の知識は設計語彙のクレートの名前に依存し、抽出器を知らない検証のクレートへ置くと名前を持つ場所が2つになるため、抽出器の側にトレイトとして持つ必要があった |
| 関数の役割の型か意味型の見分け | 開発基盤 | 関数の役割の型か意味型の見分けとは、設計概念への参照が、関数の役割の型か、役割に伴う意味型の節点を指すかを、定義のモジュールパスと種類まで一致させて判定する、抽出器の側のトレイトのことである | `xtask/src/design_model/extract/role.rs`(定義)・`xtask/src/design_verify/設計関係への参加の計測/裁定を待つ母集団の案.rs` | 名前だけで判定すると、ドメインのクレートが同じ名前の型を定義したとき設計語彙と取り違えるため、判定そのものを1つの口として持つ必要があった |
| 空いている変数へ当てる | 開発基盤 | 空いている変数へ当てる(束縛のメソッド)とは、まだ何も当てていない概念の変数へ設計概念への参照を1つ当て、新しい束縛を返すことである | `crates/blitz_design_verification/src/導出/束縛.rs`(定義)・`crates/blitz_design_verification/src/導出/名指す型の読み取り.rs` | 既存語に無かった。規則の本体を左から当てていく操作の1つに名前が要った |
| 役割の型引数の数が合わない | 開発基盤 | 役割の型引数の数が合わないとは、導出の欠けの理由の1つであり、関数の役割の宣言の型引数の数が、その役割が取る数と合わないために、位置の規則がその宣言から関係を導けないことである | `crates/blitz_design_verification/src/導出/導出の結果の定義.rs`(定義) | 既存語に無かった。導出の欠けの理由の選択肢の1つに名前が要った |

### 関係の3つの高さと仕様の関係の種類で付けた語(2026-09-26。Issue #193 の段階3)

次の語は、`_doc/設計/設計オントロジー.md` 第6節を関係の3つの高さで書き直すときに、本文で使う語になった。「仕様の関係」「設計の関係」「プリミティブな関係」「並べて決まる関係」「規則で決まる関係」「公理」は Issue #193 の「合意した考え方」と「契約」が使う語であり、ここでは文書の定義を挙げる。「仕様」は非推奨語の一覧に載るが、設計文書の散文でメタの概念として使うことは許されており、ここでの「仕様の関係」はその用法である。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| 関係の高さ | 開発基盤 | 関係の高さとは、その関係が何について語るかの区別のことである。仕様の関係・設計の関係・プリミティブな関係の3つがあり、上の高さの関係は下の高さの関係の組み合わせ(定義の式)で定義する | `_doc/設計/設計オントロジー.md` 6.1節 | 既存語に無かった。「層」はオントロジーの層(2.1節)とアーキテクチャの層に使われており、関係が何について語るかの区別に当てると1語1義が破れる |
| 仕様の関係 | 開発基盤 | 仕様の関係とは、ゲームの世界の値について語る関係のことである(町Aと町Bはつながっている・北の橋は閉じている)。ソースの型を読むだけでは現れず、実行とは別の入口の試験(有限全数の検証)が、プリミティブを使って命題を検証する形で定義する | `_doc/設計/設計オントロジー.md` 6.1節・6.2節 | 既存の「設計関係」はコードの型の役割について語る関係であり、世界の値について語る関係を指せない |
| 設計の関係 | 開発基盤 | 設計の関係とは、コードの型の役割について語る関係のことである。識別する・消費する・生成する・参照するの4語があり、プリミティブな事実の上の定義の式から導く。登録を待つ「設計関係」と同じものを、関係の高さの1つとして呼ぶ言い方である | `_doc/設計/設計オントロジー.md` 6.1節・6.5節 | 「設計関係」をそのまま使うことを検討した。高さの3つを並べて呼ぶとき、仕様の関係・プリミティブな関係と同じ形の句で呼ぶ必要があった。裁定で1つへ寄せるなら「設計関係」へ揃える |
| プリミティブな関係 | 開発基盤 | プリミティブな関係とは、コードの形について語る関係のことである(フィールドに持つ・引数に取る・呼ぶ・マーカーを実装する)。いまの設計関係グラフが持つプリミティブな関係は保持するだけである | `_doc/設計/設計オントロジー.md` 6.1節・6.5節 | 既存語に無かった。登録を待つ「プリミティブな事実」は抽出器が読む事実の値であり、その事実から定義の式で導く関係(保持する)とは別物である |
| 並べて決まる関係 | 開発基盤 | 並べて決まる関係とは、関係に入る組を1つずつ名指して並べることでしか決まらない仕様の関係のことである。書き手はGraphiteのグラフで事実を並べて書く。実例は Esca の地点と経路のグラフである | `_doc/設計/設計オントロジー.md` 6.2節 | 既存語に無かった。「グラフで書く関係」と呼ぶと、どんな関係もグラフで表せるため区別にならない |
| 規則で決まる関係 | 開発基盤 | 規則で決まる関係とは、関係に入る組を少数の規則から導ける仕様の関係のことである(チェスのナイトは縦2横1に動ける)。書き手は規則を `M規則` の型と関数で書き、事実の一覧としてグラフへ並べない | `_doc/設計/設計オントロジー.md` 6.2節 | 登録語の `M規則`(遷移へ読み取り専用で与える規則値)は型の分類であり、規則から決まる関係そのものを指さない |
| 公理 | 開発基盤 | 公理とは、分類どうしの一種であるという関係(`M状態` は `M不変データ` の一種である)のことである。正本は `blitz_design` の上位トレイトの宣言であり、コンパイラが強制するため手書きの表を持たない | `_doc/設計/設計オントロジー.md` 6.3節・`crates/blitz_design_verification/src/分類/事実.rs` | 既存語に無かった。上の「分類と公理で付けた語」は分類だけを登録しており、公理は Issue #193 の文言のままだった |
| 設計関係への参加 | 開発基盤 | ある概念が設計関係へ参加しているとは、その概念が1本以上の設計関係の主語か目的語であることである。保持する関係の辺と分類は数えない | `_doc/設計/設計オントロジー.md` 6.6節・第8節・`crates/blitz_design_verification/src/relation_graph.rs`・`xtask/src/design_verify/設計関係への参加の計測.rs` | 原子命題「設計関係へ1つも参加していない」が使っていた語だが、何を数えるかの定義が文書に無かった |

### 区域と歩行の接続で付けた語(2026-09-26。Issue #187 の作業G2)

次の語は、作業G2(区域と、歩行との接続)の実装員が新しく持ち込んだ語である。区切られたドメインは、Escaの語が「ゲーム実行」、仕組みの層の語が「共通」である。上の表の「地点」「区域」は、この作業で型になった(地点は区域を持つ節点の値、区域は下の定義の多角形)。

| 語 | 区切られたドメイン | 暫定の定義 | 使っている場所 | 既存語で表せないと考えた理由 |
|---|---|---|---|---|
| 区域の頂点・区域の指定 | ゲーム実行 | 区域の頂点とは、区域の多角形の頂点1つの東と北(メートル)のことである。区域の指定とは、区域を作るための頂点の並びを公開フィールドで持つ指定のことであり、`検証する()` が単純多角形であることを確かめて区域にする | `crates/blitz_esca/src/地点と経路/区域の形.rs`(定義)・`crates/blitz_esca/src/tests/地点と経路の試料.rs` | 様式を `閉じている時間帯の指定` に揃えた。検証の前後を同じ語で呼ぶと、検証を通らずに作れないという不変条件が名前から読めない |
| 境目 | ゲーム実行 | 境目とは、区域の多角形の辺のことである。隣の区域と共有する辺も、外周の辺もこれに当たる。境目を越えられるのは通り口だけである(2026-09-26 オーナー裁定の文言) | `crates/blitz_esca/src/地点と経路/区域の中の移動.rs`・`_doc/設計/Esca/設計正本.md` 5節 | 「境界」を検討した。境界は仕組みの層(平面の幾何)で多角形の辺の全体を指す語として使っており、裁定の文言が区域について「境目」と言うため、Escaの側はそれに合わせた |
| 通り口 | ゲーム実行 | 通り口とは、経路が区域の境目を越えられる箇所のことであり、経路の両端の2つの区域が共有する1本の辺で指す。自由な座標では持たない | `crates/blitz_esca/src/地点と経路/通り口.rs`(定義)・`_doc/設計/Esca/設計正本.md` 5節 | 「経路」を検討した。経路は2つの地点のつながり(グラフの辺)であり、そのつながりが境目のどこで越えられるかを指す語が要った |
| 区域の辺の番号・通り口の指定 | ゲーム実行 | 区域の辺の番号とは、区域の辺を、その始まりの頂点の番号で指す値のことである(辺はその頂点から次の頂点へ張る)。通り口の指定とは、経路の両端の区域それぞれの辺の番号を1つずつ持つ値のことであり、どちらの番号がどちらの区域の辺かは地図の検証が決める | `crates/blitz_esca/src/地点と経路/通り口.rs`(定義)・`crates/blitz_esca/src/地点と経路/通り口の照合.rs` | 裸の `usize` の組では、頂点の番号と辺の番号と件数を取り違えても型が通る |
| 区域の辺の所在・確かめた通り口・通り口の開閉の対象 | ゲーム実行 | 区域の辺の所在とは、ある地点の区域のある辺の組のことである。確かめた通り口とは、地図の検証が両端の区域の辺を照らした後の通り口であり、経路・両側の区域の辺の所在・通り口の開閉の対象を持つ。通り口の開閉の対象とは、通り口が封鎖できない(いつも開いている)か、封鎖できる経路の識別子に従って開閉するかの区別のことである | `crates/blitz_esca/src/地点と経路/通り口.rs`(定義)・`crates/blitz_esca/src/地点と経路/地図の検証.rs`・`crates/blitz_esca/src/地点と経路/開閉の状態.rs` | 検証の前の指定と後の値を同じ語で呼ぶと、検証を通らずに作れないという不変条件が名前から読めない |
| 通り口の辺の当て方 | ゲーム実行 | 通り口の辺の当て方とは、通り口の指定の2つの番号を経路の両端の区域へ当てた結果のことである。Graphiteの向きの無い辺は両端の順序を保たないため、地図の検証がそのままの向きと入れ替えた向きを試して1つに決める | `crates/blitz_esca/src/地点と経路/通り口の照合.rs`(定義) | 既存語に無かった |
| 経路の種類 | ゲーム実行 | 経路の種類とは、経路が幹線道路・橋・門・脇道・山道のどれであるかの区別のことである。幹線道路・橋・門は封鎖でき、脇道・山道は封鎖できない(2026-09-26 オーナー裁定) | `crates/blitz_esca/src/地点と経路/種類.rs`(定義)・`crates/blitz_esca/src/地点と経路/積み荷.rs`・`crates/blitz_esca/src/地点と経路/地図の検証.rs` | 5つの値の名前(幹線道路・橋・門・脇道・山道)は裁定の文言である。まとめて呼ぶ語が要った |
| 経路の積み荷 | ゲーム実行 | 経路の積み荷とは、地点と経路のグラフの辺(経路)が持つ値であり、経路の種類・通り口の指定・開閉の予定の3つを持つ。Graphiteの「辺の積み荷」をそのまま型の名前にした | `crates/blitz_esca/src/地点と経路/積み荷.rs`(定義)・`crates/blitz_esca/src/地点と経路/図式.rs` | 3つを別の表で持つと、地図に無い経路を指す食い違いが型の上で起こせるため、辺と一緒に持つ1つの値に名前が要った |
| 封鎖できる経路の識別子 | ゲーム実行 | 封鎖できる経路の識別子とは、地図が、その経路が自分の中に在り、しかも種類が封鎖できることを確かめてから渡す識別子のことである。経路の開閉の命令・状態・出来事はこの識別子で経路を指すため、封鎖できない経路を閉じる命令は型の上で作れない | `crates/blitz_esca/src/地点と経路/封鎖できる経路.rs`(定義)・`crates/blitz_esca/src/地点と経路/開閉の命令.rs` ほか開閉の型 | 既存の「経路の識別子」を検討した。経路の識別子は封鎖できない経路も指せるため、命令の型へ入れると脇道を閉じる命令が作れる |
| 地図の生成の失敗・未検証の地図 | ゲーム実行 | 地図の生成の失敗とは、Graphiteの完成したグラフが地図の不変条件(区域の内部が重ならない・経路の両端が別の地点・封鎖できない経路が閉じる予定を持たない・通り口の辺が共有される・通り口どうしが重ならない)を満たさなかった理由のことである。未検証の地図とは、それを確かめる前のグラフを持つ型のことである | `crates/blitz_esca/src/地点と経路/地図の失敗.rs`・`crates/blitz_esca/src/地点と経路/地図の検証.rs`(定義) | 既存語に無かった |
| 通れる通り口の集合 | ゲーム実行 | 通れる通り口の集合とは、経路の開閉の状態と地図から刻みごとに導く、いま開いている通り口の並びのことである。歩行の規則の空間制約の一部として、読み取り専用で遷移へ渡る(`M観測` ではない) | `crates/blitz_esca/src/地点と経路/通れる通り口.rs`(定義)・`crates/blitz_esca/src/地点と経路/開閉の状態.rs`・`_doc/設計/設計オントロジー.md` 3.5.2 | 既存語に無かった |
| 区域の空間制約 | ゲーム実行 | 区域の空間制約とは、歩行の遷移が読み取り専用で受け取る、地図の区域といま通れる通り口の組のことである(設計正本 4.4 条2 の空間制約)。第1段階の `移動可能範囲` を置き換えた | `crates/blitz_esca/src/地点と経路/空間制約.rs`(定義)・`crates/blitz_esca/src/traveler_movement.rs` | 「空間制約」だけでは第1段階の長方形と区別できない |
| 区域の中の移動・区域の中の移動の指定・区域の中の移動の結果・区域の中の移動の失敗 | ゲーム実行 | 区域の中の移動とは、地点の区域の中の始点から終点へ動こうとしたとき、境目で滑るか通り口を抜けるかを決める工程のことである。指定はいる地点・始点・終点を、結果は着いた地点・着いた点・遮られたかを持ち、失敗は地図に無い地点にいることと始点が区域の外にあることを表す | `crates/blitz_esca/src/地点と経路/区域の中の移動.rs`・`crates/blitz_esca/src/地点と経路/区域の中の移動の型.rs`(定義) | 既存語に無かった |
| 滑る・出口 | ゲーム実行 | 滑るとは、境目に遮られた動きの残りから境目に垂直な成分を除き、境目に沿う成分だけで動くことである(2026-09-26 オーナー裁定の文言)。出口とは、動きが区域から初めて外へ出る点のことである | `crates/blitz_esca/src/地点と経路/区域の中の移動.rs`・`_doc/設計/Esca/設計正本.md` 5節 | 既存語に無かった |
| 世界の外・地点を決められない理由 | ゲーム実行 | 世界の外とは、どの区域の閉包(内側か境目の上)にも入らない点のことである。地点を決められない理由とは、点から地点を1つに決める口が、世界の外の点と2つ以上の区域の境目の上の点を拒む理由のことである | `crates/blitz_esca/src/地点と経路/空間制約.rs`(定義)・`_doc/設計/Esca/設計正本.md` 5節 | 既存語に無かった |
| 旅行者の居場所 | ゲーム実行 | 旅行者の居場所とは、旅行者がいる地点と、その地点の区域の中の現在地の組であり、第2段階の歩行の遷移の状態(`M状態`)である。境目の上の点は2つの区域のどちらの閉包にも入るため、いる地点を点と別に持つ | `crates/blitz_esca/src/居場所.rs`(定義)・`crates/blitz_esca/src/旅の一刻み.rs` | 「旅行者の現在地」を検討した。現在地は東と北の座標だけを持つ値であり、どの区域にいるかを持たない |
| 歩行の失敗 | ゲーム実行 | 歩行の失敗とは、歩行の遷移が次の居場所を作れなかった理由のことであり、次の現在地を作れない(`旅行者の現在地の生成の失敗`)と、区域の中を動けない(`区域の中の移動の失敗`)の2つを持つ。歩行の遷移の遷移失敗結果の失敗の理由になる | `crates/blitz_esca/src/traveler_error.rs`(定義)・`crates/blitz_esca/src/居場所.rs` | 既存の「旅行者の現在地の生成の失敗」は現在地の座標の失敗だけを指し、区域の外にいることを言えない |
| 旅の進行・旅の進行の指定・旅の一刻みの入力・旅の一刻みの結果・旅の一刻みの失敗 | ゲーム実行 | 旅の進行とは、経路の開閉の状態と旅行者の居場所と、開閉の状態から導いた歩行の規則を持ち、1刻みを「開閉を遷移する → 通れる通り口を導く → 歩行を遷移する」の順で進める工程(`M工程`)のことである。指定は進行を始める材料を、入力は1刻みの開閉の命令と旅行者の意図を、結果は次の進行と生じた出来事と失敗を、失敗はどちらの遷移が失敗したかを持つ | `crates/blitz_esca/src/旅の一刻み.rs`(定義)・`crates/blitz_esca/src/tests/旅の一刻みの試験.rs`・`crates/blitz_esca/src/tests/旅の数え上げ.rs` | 登録語の「刻み」「固定刻みの処理位置」はエンジンの側(blitz_app)の語であり、Escaの2つの遷移を組む工程を指さない |
| 平面の幾何・平面の点・平面の線分・単純多角形・単純多角形の生成の失敗・多角形に対する位置 | 共通 | 平面の幾何とは、地面を上から見た平面の点と線分と単純多角形の計算を持つ仕組みの層のことであり、Escaの語彙を知らない。単純多角形とは、自己交差せず、頂点が反時計回りに並ぶ、頂点が3つ以上の多角形のことである。多角形に対する位置とは、点が内側・境界の上・外側のどれにあるかのことである | `crates/blitz_esca/src/平面の幾何.rs` と `crates/blitz_esca/src/平面の幾何/`(定義) | 登録語の「形の局所座標の位置」などは衝突数学層(blitz_collision)の3次元の語であり、地面を上から見た2次元の計算を指さない |
| 閉包・境界の許容幅・着く点の許容幅・境界に沿わせる | 共通 | 閉包とは、多角形の内側と境界の上を合わせた範囲のことである(数学の用語)。境界の許容幅とは、点を境界の上とみなす距離の幅のことであり、座標の大きさの2の20乗分の1(1メートル未満の座標では1メートルの座標と同じ幅)である。着く点の許容幅とは、移動の終点が境界の外にあっても閉包の中に着いたとみなす幅のことであり、境界の許容幅の半分である(着いた点を単精度へ戻して動いても、次の移動の始点が境界の許容幅の中に残るため)。境界に沿わせるとは、境界の上の点を辺へ下ろした足から、終点へ向かう動きのうち辺に沿う成分だけ進めることである | `crates/blitz_esca/src/平面の幾何/点と線分.rs`・`crates/blitz_esca/src/平面の幾何/点の位置.rs`・`crates/blitz_esca/src/平面の幾何/線分の区切り.rs`・`crates/blitz_esca/src/平面の幾何/境界に沿わせる.rs`(定義)・`_doc/設計/Esca/設計正本.md` 5節 | 既存語に無かった |
| 区域の形・種類・積み荷・封鎖できる経路・地図の失敗・地図の検証・通り口の照合・通れる通り口・空間制約・区域の中の移動の型・居場所・旅の一刻み・平面の幾何・多角形・点と線分 | ゲーム実行・共通 | モジュール名としてのこれらは、それぞれ区域とその指定・経路の種類・経路の積み荷・封鎖できる経路の識別子・地図の生成の失敗・未検証の地図・通り口の辺の当て方・通れる通り口の集合・区域の空間制約・区域の中の移動の指定と結果と失敗・旅行者の居場所・旅の進行・平面の幾何・単純多角形・平面の点と線分を持つモジュールのことである | `crates/blitz_esca/src/地点と経路.rs`・`crates/blitz_esca/src/lib.rs`・`crates/blitz_esca/src/平面の幾何.rs` | モジュール名を型名と同じにすると、親のモジュールで型を再公開したときに名前が衝突するため、型名から「経路の」などを落とした名前にした(G1の様式) |

## 楽器の割り当てを音の並びの種類で決める実装が足した語(2026-09-26。Issue #93・PR #94)

### 楽器の音の種類

- **区切られたドメイン**: エディター
- **暫定の定義**: 楽器の音の種類とは、楽器が音高を持つ音源か打楽器の音源かの区別のことである。取りうる値は「音高」と「打楽器」の2つであり、値が音高の楽器はユビキタス言語の「音高の楽器」に、値が打楽器の楽器は「打楽器の楽器」に当たる。トラックの音の並び(音高の行一覧・打楽器の行一覧)もそれぞれがこの2つのどちらかを受け入れ、楽器を割り当ててよいかはこの2つが一致するかだけで決まる(`_doc/設計/楽曲エディター.md`「判断17」)。Rustの列挙もTypeScriptの型も同じ名前と同じ2つの値を持つ。TypeScript側は別の表を持たず、楽器ごとの音色の対応表の鳴らし方(音高を鳴らす・打楽器を鳴らす)から導く
- **使っている場所**: `crates/editor_server/src/resource/music/instrument.rs`(定義)・`crates/editor_server/src/resource/music/note_rows.rs`・`crates/editor_server/src/resource/music/command/track_ops.rs`・`crates/editor_server/src/resource/music/track.rs`・`editor_web/src/ツール/楽曲編集/編集モデル/楽器の音色/index.ts`
- **既存語で表せないと考えた理由**: 「音高の楽器」「打楽器の楽器」を検討した。この2語は楽器の集合を指す語であり、楽器と音の並びが共有する区別そのもの(音の並びの側も「音高」か「打楽器」かを答える)を1つの型として名指す語が無かった。「音の並びの種類」は音の並びの判別(音高の行一覧・打楽器の行一覧)を指す別の語であり、楽器の側を答えない

## 裁定の記録

裁定の結果はこのファイルに残さない。採用した語はユビキタス言語の定義文に、裁定の日付と裁定を行ったissueの番号を添えて書く。
却下した語は、却下を裁定したissueのコメントに残る。
