# 評価軸

この文書は、エンジンを評価するための判断軸・判断方法・判断フローを定義する。実装・計画と常に一致させて維持する。制定の経緯は開発スレッド2026-07-20を参照。

評価は「能力」「品質」「性能」「変更容易性」の4系統に分ける。品質系は機械検証が導入済みのため、この文書は残り3系統の制度化が中心になる。

## 1. 能力(何ができるか・その意義・できないことの重大さ)

能力は一般的な機能一覧に対する充足率ではなく、**対象ゲーム像(原神型オープンワールドRPG)への到達度**で測る。

- 能力の判定には代表シーンテストを使う。**代表シーンテスト**とは、対象ゲームの典型的な1シーンを構成要素に分解し(フィールド地形・植生・キャラクターと衣装物理・空と時間帯・戦闘エフェクト・UI・ストリーミング・オーディオ)、各要素を現在のエンジンで実行できるか判定する試験のことである。
- 実行できない要素の重大さは**出自分類**で判定する。出自分類とは、その要素が計画のどこに由来するかを次の3つに振り分けることである。
  - (a) 実装予定のマイルストーン(M0〜M11)にある → 予定どおりの未実装。
  - (b) 長期フェーズ計画にある → 意図して後回しにした未実装。
  - (c) どちらにも無い → **計画から漏れた真の欠落**。発見したら開発計画の改訂として扱う。
- 能力の意義は、その能力がこのエンジンの差別化の中心(キャラクターシミュレーション・性能・AI駆動開発への適合)に属するか、汎用エンジンを使えば追加コストなしに得られるものかで判定する。差別化の中心に属さない能力は先行して実装せず、計測または仕様が必要性を示してから実装する。
- 2026-07-23のM11分類結果: キャラクターと衣装物理はフェーズ1で実装済みである。地形、植生、空と時間帯、戦闘エフェクト、本番UI、ストリーミング、オーディオは長期フェーズ計画の(b)に存在し、計画から漏れた(c)は検出されなかった。詳細は `_doc/計画/エンジンフェーズ1完了報告とフェーズ2計画.md` を参照する。
- 2026-07-28のOW3完了時点で、代表シーンの構成要素のうち地形が(b)から実装済みへ移った。高さ地形をチャンク単位の詳細段で描き、境界の亀裂をスカートで塞ぎ、km級の大域座標でも描画が原点移動に対して不変であることを画素で判定できる。植生は未実装のままOW4に残り、空と時間帯、戦闘エフェクト、本番UI、オーディオは引き続き(b)である。計画から漏れた(c)はOW3でも現れなかった。
- 2026-07-29のOW4完了時点で、植生が(b)から実装済みへ移った。1つの原型を共有するインスタンス群を地形と同じチャンク束へ載せ、シーンパスとシャドウパスが別々の視錐台で群と個体の2段階の可視判定を行い、個体単位で距離別のメッシュLOD段を選び、群と非空LOD段の組ごとに1回の描画発行で描ける。空と時間帯、戦闘エフェクト、本番UI、オーディオは引き続き(b)である。計画から漏れた(c)はOW4でも現れなかった。

## 2. 品質(正しく動くか) — 機械検証導入済み

- validation layer(同期検証込み)エラーゼロ、`cargo xtask smoke` のピクセル読み戻し判定、`cargo xtask conform` の規約検査、検収5軸(設計遵守・規約遵守・孤立実装の検出・検証の再実行・差分の全件確認)。
- 参照: `_doc/計画/開発計画.md` 自律ループの運用規約。この文書ではこれ以上扱わない。

## 3. 性能

「速いか」という問いは比較先を決めないと判定できない。この文書では比較先を3つ定義する。

### 3.1 フレーム予算との比較(日常の計器)

60fps(16.6ms)をパス群に配分した予算表に対する消費率で判断する。計器はM5で導入したパス別GPU時間計測と開発用UI。

初期予算表(エンジンフェーズ1向け。パス追加時にこの表を改訂する):

| パス | 予算 |
|---|---|
| シーン描画(PBR) | 4.0ms |
| シャドウ(M6。2026-08-03のオーナー裁定で2.0msから改定) | 2.6ms |
| ポストプロセス(M7。順3-IIbの自動露出3パスを内数として含む) | 1.5ms |
| 空(OW5第7段、大気のベイク済み画像。毎フレーム走る4区間の合計) | 1.0ms |
| 間接照明の更新(順3-II。時間平均。更新1回のピーク上限2.5msは毎フレームの配分でないため別に持つ) | 0.15ms |
| 局所可視性補正(順3-IIa。遮蔽の標本化と両側ぼかしと深度プリパスの合計) | 0.3ms |
| 時間再構成(順2。毎フレーム走る1パス) | 0.45ms |
| クラスタの選別(着手順5の第4段階。毎フレーム走る1本のコンピュート) | 0.15ms |
| 点光源の影(着手順5の第5段階。影付きの灯×6面の深度パス群。暫定であり代表物量の計測日に再審査する) | 0.10ms |
| スキニング(M8、コンピュート) | 0.5ms |
| シミュレーション(M9、コンピュート) | 3.0ms |
| UI・その他 | 1.0ms |
| 予備(フェーズ2のストリーミング用に温存。順3-IIの2枠へ0.45msを回して3.0msから改定し、順2の時間再構成の枠へさらに0.45msを回して2.55msから改定し、着手順5のクラスタの選別の枠へ0.15ms・点光源の影の枠へ0.10msを回して2.10msから2段階で改定) | 1.85ms |

M10固定条件のシミュレーション更新時間は、128×128表面流が1.9166msで予算の63.9%、全対全探索SPHが512粒子で3.8503ms（128.3%）、1024粒子で5.2107ms（173.7%）、2048粒子で9.6049ms（320.2%）だった。表面流は予算内であり、SPHは512粒子から超過するため、本実装の前提を空間ハッシュによる近傍探索へ更新する。

2026-07-25のOW2固定ベンチ5回では、シーン描画が0.1487〜0.1605msで予算の3.7〜4.0%、シャドウが0.0303〜0.0436msで1.5〜2.2%、ポストプロセス合計が0.1008〜0.1135msで6.7〜7.6%、粒子更新が0.0115〜0.0130msで0.4%だった。GPU合計は0.3105〜0.3329msで16.6msフレーム予算の1.9〜2.0%である。ただし4回目だけ明るさの圧縮が0.5303msとなり、ポストプロセス合計0.6594ms(44.0%)、GPU合計0.9017ms(5.4%)へ跳ねた。この1回の原因は特定しておらず、GPU側の変動要因として残す。詳細は `_doc/計測/OW2_2026-07-25.md` を参照する。

OW3(2026-07-28完了)はGPUパスを追加していない。地形チャンクは既存のシーンパスとシャドウパスで描かれ、1チャンクが1つの描画対象として、距離で選んだ詳細段の頂点とインデックスだけを積む。したがってパスの区分は変わらず、予算表も改訂しない。ただしOW3完了時点でのパス別GPU時間の再計測は行っていないため、消費率は3.4節に記録したOW2完了時計測が最新である。地形の物量を載せた消費率は、植生と可視判定を含む代表シーンを測るOW4で更新する。

OW4(2026-07-29完了)もGPUパスを追加していない。植生は地形と同じシーンパスとシャドウパスで描かれ、群と非空LOD段の組ごとに1回の描画発行を積むだけである。したがって予算表は改訂しない。OW4の物量計測は`--unlit --no-post`で行っており、ライティングとポスト処理を止めているため、消費率を更新できるのはシーン描画とシャドウの2行だけである。以下の値の一次記録は`_doc/計測/OW4_2026-07-29.md`にある。

シャドウは、可視判定で個体を絞らず全候補を描いていた第5段の実装で、常駐50,026個体のとき3.27〜3.85msとなり2.0ms予算の164〜193%を消費した。1,000,026個体では5.21〜5.31msで261〜266%である。第6段でライト視錐台による個体の可視選別を入れた後は、50,026個体で0.143〜0.155ms(予算の7〜8%)、1,000,026個体で0.0324〜0.0328ms(予算の2%)へ下がり、予算内へ戻った。

シーン描画は1,000,026個体で5.44〜5.46msとなり、4.0ms予算の136〜137%を消費した。第6段の後は同じ物量点で5.90〜5.99ms(148〜150%)である。**これがOW4完了時点で残る唯一の予算超過である。**ただし超過が確定するのはこの物量点だけである。10,026〜100,026個体ではGPUのクロックと電力状態の変動が絶対値を支配し、入力が1件も違わない前後の実行で3倍以上動いた実測があるため、この負荷帯のシーン描画の値は物量の関数として読めない。低・中負荷帯での予算判定には、GPU負荷を固定した別の計測が要る。

OW5第7段(2026-07-30完了)は空(大気のベイク済み画像方式)をGPUパスへ追加した。空パスは予備4.6msから1.0msを切り出した独立枠であり、ポストプロセスの1.5msへは混ぜない。残る予備は3.6msである。GPU実測(地形世界1280x720、時計停止160フレームの移動平均)は、毎フレーム走る定常4区間(スカイビュー生成0.0205ms・空中遠近生成0.0307ms・空中遠近合成0.0188ms・空描画0.0126ms)の合計が0.0826msで、空1.0ms予算の8.3%であり順調である。空中遠近合成は実行ごとに0.012msから0.031msまで揺れるため、この揺れ幅を足しても定常合計は高々0.10ms程度で予算の余裕は動かない。大気パラメータが変わったフレームだけに発生する透過率生成0.126ms・多重散乱生成0.547msは定常予算に含めない更新時の値であり、この2区間の合計0.673msも空1.0ms予算に収まる。ベイク済み画像の生成パス数は条件で変わり、大気パラメータの初回構築が4本(透過率・多重散乱・スカイビュー・空中遠近)、大気パラメータが前フレームと同じときは0本、太陽が進行するフレームはスカイビューと空中遠近の2本である。一次記録は`_doc/設計/空と時間帯と遠距離シャドウ.md`「大気のベイク済み画像方式の設計」節の「予算と計器」「生成パス数の実測」にある。ギャップ判定: 予算超過なし。多段シャドウ4距離区分化に伴うシャドウ2.0ms予算の再計測は第7段のスコープ外であり、OW5の品質・性能定点(段階導入の8・9)で行う。

OW5第8段(2026-08-01、品質・性能定点)で、低い太陽(一日内時刻17時、太陽高度約12度)と本番の描画条件(空・ライティング・ポスト処理あり)で4項目を実測した。代表物量は常駐100,026個体である(この文書は代表物量点を定めていなかったため、第8段が10倍を代表として選んだ根拠を計測記録に明示した)。3回反復の中央値は、空の定常合計0.2598ms(1.0ms予算の26.0%)、シャドウ合計11.0549ms(2.0ms予算の552.7%)、可視個体の選別1.5228ms(2.0ms予算の76.1%)、GPU定常合計16.3491ms(16.6ms予算の98.5%)である。GPU定常合計には毎フレーム走る区間だけを入れ、大気が変わったフレームだけ走る透過率生成と多重散乱生成の合計0.676msは別に記録する。この初回実行は有効表示2画面かつ他の常駐処理がある機材状態で、GPU側だけが2〜4倍に上振れしていた。2026-08-02に1画面・重い常駐処理なしで再計測し(フレーム間隔p50が16.66msの60fps提示へ戻ったことが環境の健全さを示す)、**4項目の判定を確定した**: 空の定常合計0.0854ms(8.5%)・シャドウ合計3.1722ms(**158.6%で予算超過**)・可視個体の選別1.5672ms(78.4%)・GPU定常合計4.4973ms(27.1%)。1倍ではシャドウ1.9936ms(99.7%)で予算に張り付く。シャドウの是正はキャスター側の絞り込み・描画コストの削減という設計判断を要するためOW5の範囲外とし、積み残しとしてフェーズ2の性能課題へ持ち越す。一次記録は`_doc/計測/OW5第8段_2026-08-01.md`(追記: 1画面クリーン環境での確定計測)にある。

ギャップ判定(OW5完了時点): **シャドウ2.0ms予算の超過がOW5完了時点で残る予算超過であった。**4距離区分化と距離区分別の可視個体の選別を入れた後も、低い太陽の代表物量では距離区分1から3に費用が集中する(3つで合計の88%)。太陽が低いと1個体の影が引き伸ばされてシャドウマップ上で覆う画素が増えるため、描く個体が12%少ない条件でシャドウが11%高くなる逆転が実測された。

**シャドウ予算の改定(2026-08-03、オーナー裁定)**: この超過はフェーズ2の性能課題として律速を切り分け(記録は`_doc/計測/シャドウ律速切り分け_2026-08-03.md`)、支配要因が個体キャスターの処理数に紐づく費用(約73%)であること、トポロジー量の削減では解けないことを確定させた。裁定材料として、最大影距離の短縮(α)と個体の影の視距離の切断(β)の3水準ずつを1画面クリーン環境で実測した結果、**単独で2.0msへ届く水準は無く、削減幅は基準3.0〜3.1msに対して最大0.6〜0.7msである。**オーナーはαの225メートルを採り(実測2.4842ms、影の欠落2.3%・余分1.9%はすべて輪郭の揺らぎでまとまった消失なし)、βは「この距離より遠い個体が近くへ届く影も落とさない」という意味の損失を受け入れないとして採らなかった。合わせて**シャドウ予算を2.0msから2.6msへ改定した。**2.6msは採用水準の実測中央値2.4842msへ環境の揺れの余裕を持たせた値であり、増やした0.6msは予備(3.6→3.0ms)から回した。GPU定常合計が16.6ms予算の27%であり、フレーム全体では余裕があるための再配分である。**この改定でシャドウは予算内(2.4842ms=95.5%)になり、フェーズ2の性能課題としての予算超過は解消した。**設計正本の記録は`_doc/設計/空と時間帯と遠距離シャドウ.md`「S2-1の裁定結果」にある。

4距離区分の世界メートル毎テクセル(1テクセルが世界で覆う長さ)は、最大影距離225メートル(2026-08-03の裁定後の既定)で距離区分0から順に0.026411m・0.055101m・0.100279m・0.259011mである(裁定前の300メートルでは0.035081m・0.072876m・0.131614m・0.345348mであった)。カメラの射影と多段設定と解像度2048だけで決まり、太陽にも物量にも依らない。**判定の閾値は未制定であり、この値は実測の記録である。****大規模世界の第5段階(2026-08-16)で多段設定が世界ごとに分かれた。**上の値は最大影距離225メートルの実用分割を採る世界のものであり、世界級の遠景を持つ大規模世界は明示境界25・100・1000・10000メートルで走るため別の値になる。大規模世界の世界メートル毎テクセルは計測していない。

順3-I(IBL、2026-08-04完了)は間接照明の生成パス群(遠方環境生成・最詳細段の複製・拡散照度生成・鏡面畳込み5段・反射率積分表生成)をGPUへ追加した。1画面クリーン環境の公式計測(`_doc/計測/IBL全更新費用_2026-08-04.md`)では、更新なし定常は生成パス0本でシーン描画だけが走り(地形世界でp50 1.4121ms、シーン描画4.0ms予算の35%)、反射率積分表は起動後1回だけの0.0922msである。**ギャップ判定(順3-I完了時点): 時計進行時のIBL全更新費用がどの予算枠にも未配分である。**遠方環境キーが時刻(スカイビューキー)に連動するため時計が進む限り毎フレームが全更新になり、1フレームの生成合計の分位でp50 2.4812ms/p95 3.6844msが掛かる。空1.0ms予算(大気の定常4区間)へは収まらず、予備3.0msをほぼ使い切る規模である。空の変化は緩慢であり毎フレーム焼き直す必然は無いため、対策の第一候補は更新の間引き(鍵の時刻成分の量子化)である。IBL生成の専用予算は焼き直し頻度が決まらないと意味を持たないため、制定は間引きの審査とセットで行う(札#138へ起票済み)。

順3-II(2026-08-06完了)は自動露出3パスと局所可視性補正2パスと深度プリパス1パスをGPUへ追加し、IBLの更新を太陽天頂区間で間引いた。1画面クリーン環境の公式計測(`_doc/計測/順3-II費用_2026-08-06.md`)を読んでオーナーが予算を裁定し、予算表へ3つの変更を入れた。**第一に、間接照明の更新へ時間平均0.15ms/描画フレームの行を新設した**(実測0.05544msで37.0%)。実測は計測できた10回の更新の平均0.8870msを全描画フレーム160で割った値であり、条件は一日の長さ86400秒・時間倍率3600・間引きの刻み200区間である。倍率3600は本番の時間進行より速いため、この値は時間平均の上振れ側の観測である。**更新1回のピーク上限2.5msは別に持つ**(観測p95 1.7613msで70.5%)。ピークは更新フレームだけに掛かる上限であって毎フレームの配分ではないため、予算表の行にはしない。2.5msが観測p95の1.42倍なのは、この機材のGPU時間が二峰であり、上側モードを機材の要因として捨てずに包むためである。**第二に、局所可視性補正へ0.3msの枠を新設した**(実測0.1751msで58.4%。遮蔽の標本化0.0891 + 両側ぼかし0.0635 + 深度プリパス0.0225)。深度プリパスを同じ枠へ入れるのは、補正を宣言した世界では補正の要件として深度プリパスが必ず積まれるためである。**第三に、自動露出は新枠を作らずポストプロセス1.5msの内数とした**(ヒストグラム消去0.0022 + 集計0.0174 + 導出と適応0.0082 = 0.0278msで、枠の1.9%)。新設した0.15msと0.3msの合計0.45msは予備から回し、予備は3.0msから2.55msへ減った。フレーム予算の合計は16.6msのまま変わらない。

**ギャップ判定(順3-II完了時点): 順3-Iで起票した「時計進行時のIBL全更新費用がどの予算枠にも未配分である」というギャップは、この制定で解消した。**間引きにより、時計進行の160フレームで焼いたのは11フレーム(初回の1回と太陽天頂区間が変わった10回)になり、間引きの前に160フレームすべてが焼いていた状態から更新頻度が確定した。更新1回のピークと時間平均の2本が実測とともに定まったため、専用予算が意味を持つ条件が揃っている。**順3-II完了時点で新たな予算超過は検出していない。**残る観察は深度プリパスの既定であり、局所可視性補正を使わない世界の既定は現行のAのまま維持された(裁定の根拠は絵の不変のみである)。同じ計測でB(深度プリパスを積み、色パスは近いか同値で比べて深度へ書く)がAの最良窓より0.0481ms(25.1%)短く、窓の中央値ではAより0.2938ms(67.2%)短いことが3窓すべてで安定して観測されている。**この性能上のB優位は裁定と分離して記録し、将来の再裁定の材料とする。**

順2(時間再構成、2026-08-09に段4完了)は毎フレーム走る1パスをGPUへ追加した。1画面環境の公式計測(`_doc/計測/順2時間再構成費用_2026-08-09.md`)を読んでオーナーが予算を裁定し、予算表へ**時間再構成の枠0.45msの行を新設した**(代表値p50 0.2888msで消費率64.2%)。代表値は同じ条件の3実行の直近60フレーム窓のp50 0.2857 / 0.2888 / 0.2898の中央値であり、3窓の最大差が1.4%であることが採用条件の成立根拠である。**枠の判定は下側モードのp50で行い、上側モードが窓へ入った実行(2本目のp95 1.0568ms)は機材要因として順3-IIで定めた二峰の規律に従う。**枠0.45msは代表値の1.56倍であり、IBL更新1回のピーク上限の1.42倍・局所可視性の枠の1.71倍と同じ帯にある。0.45msは予備から回し、予備は2.55msから2.10msへ減った。**フレーム予算の合計は16.6msのまま変わらない**(4.0 + 2.6 + 1.5 + 1.0 + 0.15 + 0.3 + 0.45 + 0.5 + 3.0 + 1.0 + 2.10 = 16.6)。設計正本が段4の観測項目に挙げた「今のフレームの色」の専用画像を経由する増分は、この実測が時間再構成ありの構成で採ったものであるため区間の値に内包されている。増分が予算枠を圧迫したときという再審査条件は、消費率64.2%で成立しない。

**ギャップ判定(順2完了時点): 予算超過は検出していない。**新設した枠は消費率64.2%であり、既存のどの枠も時間再構成の導入で超過へ転じていない。ただし2つの維持項目を残す。**第一に、実表示ペーシングの時間帯依存の脱落は原因が未特定のままである。**2026-08-09の`cargo xtask bench-display-timing`は深夜と日中の2時間帯で各477観測を採り、実表示間隔の25ms超過が両時間帯とも0回・p50 16.667ms・CPUの実仕事が1フレームあたり約0.4msだった。ここから確定するのは「CPUが重いのではない」ところまでであり、**ペーシングが健全であることは確定していない**。33ms級の遅延が実表示の更新脱落を伴うことは2026-07-25に実測で確定しており、同一コード・同一コマンドで25ms超過が時間帯により0回から156回まで動いた実測もある(下の「3.4 計測の再現性」)。したがってこれは直近2時間帯で脱落なしという観測であって、M11の完了条件が掲げる60fps安定の残債は解消していない。**第二に、順2の計測が同じ実行で採った局所可視性補正の参考値は、局所可視性の枠0.3msを上回る**(2パスと深度プリパスの合計が約0.47から0.49ms)。**この参考値は枠の判定に使えない。**世界が違い(参考値は目視見本の庭`terrain_visual`、枠の根拠は本番の地形世界`terrain_origin`)、日も違うため、順3-IIが定めた「日をまたいだ絶対値の比較はしない」に該当する。枠の判定に使える値は依然として2026-08-06の実測0.1751msであり、決着には`terrain_origin`での同日対照が要る。

第1段階(クソゲー1本目「キツネの場所巡り」の土台10項目、2026-08-10完了)はGPUパスを1本も追加していない。ゲーム層が変えたのは、どの世界を開くかと、動く個体1体の変換を毎フレーム書き換えることだけであり、パスの構成も画素あたりの計算も変えない。したがって予算表の行は1つも動かず、改訂もしない。**ゲームに関わるGPU時間は計測していない。**理由は、フレーム予算に行が立つのはGPUパスを足したときであり、ゲーム層はそれを1本も足していないからである。動く個体の毎フレームの書き込みは、個体レコード1件112バイト(`crates/blitz_render/src/vulkan/instance_transform/bytes.rs`のレコード長)を1体ぶんホスト可視メモリへ写すだけであり、既存のどの計器の分解能でも観測できる量ではない。**ギャップ判定(第1段階完了時点): 予算超過は検出しておらず、能力の出自分類(c)も現れていない。**観察を1つ残す。動く個体を宣言した描画対象は、静的な枝が持たないこの写しを毎フレーム1回行う。動く個体が数千体になる要求が立った時点で、この写しが観測できる量へ育つかを測る。

着手順5の第4段階(夜の多光源の検収、2026-08-10完了)はクラスタの選別1パスをGPUへ追加した(選別の結果を読む照明評価はシーン描画の内数)。夜の多光源の検収の世界での公式計測(60標本の窓のp50、3回の中央。`cargo xtask cluster-lights`)は、選別が1灯0.0061ms・8灯0.0164ms・32灯0.0502ms・64灯0.0952msで件数にほぼ線形だった。親の裁定で**クラスタの選別の枠0.15msの行を新設した**(代表値=宣言上限64灯の0.0952msで消費率63.5%。枠は代表値の1.58倍であり、時間再構成の1.56倍・局所可視性の1.71倍と同じ帯にある)。0.15msは予備から回し、予備は2.10msから1.95msへ減った。**フレーム予算の合計は16.6msのまま変わらない**(4.0 + 2.6 + 1.5 + 1.0 + 0.15 + 0.3 + 0.45 + 0.15 + 0.5 + 3.0 + 1.0 + 1.95 = 16.6)。**64灯のシーン描画は0.7834msを観測上限として記録するに留める**——2回の実行で0.7834/0.5837と34%振れ、光評価の実費用とGPUの周波数・電力状態を現在の証拠では分離できないためである(1灯から32灯は3桁目まで再現する)。シーン描画へ上限の議論を持ち込むときは、64灯の生標本を採って条件順を入れ替え、原因を分離してから行う。一次記録は`_doc/設計/クラスタ多光源と点光源の影.md`の第4段階の実測にある。

着手順5(クラスタ多光源と点光源の影、2026-08-11完了)は第5段階で点光源の影のGPUパスを追加した。影付きの灯1件につき6本、宣言の上限4件で24本であり、24本は同じパス名を持つため計器の表には合計の1行だけが出る。夜の多光源の検収世界(灯64件・描画対象3件)で影を落とす灯を1件から上限の4件まで振った公式計測(単一画面(オーナー確認済み、2026-08-11)、リリースビルド、60標本の窓のp50、満載の4件は3回の中央)は、6面0.0184ms・12面0.0358ms・18面0.0522ms・**24面0.0696ms**であり、灯1件を足すごとの増分は0.0174・0.0164・0.0174msでほぼ線形だった。満載の3回のうち2本目はp95が0.7854msでこの機材の上側モードが窓へ入っており、順3-IIが定めた二峰の規律に従って下側モードのp50で読んでいる。同じ実行のクラスタの選別は0.0891msで、着手順5の第4段階で制定した枠0.15msの59.4%である。一次記録は`_doc/設計/クラスタ多光源と点光源の影.md`の「第6段階の定点」にある。

**ギャップ判定(着手順5完了時点): 予算超過は検出していないが、点光源の影のGPU時間がどの予算枠にも未配分である。**設計正本の判断hが「点光源の影はシャドウの枠2.6msへ載せず、予備から新しい枠を切る」と定めており、制定は実測を読んだ人の裁定に掛かっている。予備は第4段階でクラスタの選別へ0.15msを回した後の1.95msであり、満載の代表値0.0696msに対して既存の作法どおり代表値の1.4倍から1.7倍の帯から採るなら、切る量は予備を動かす規模ではない。**この未配分は順3-Iで起票した「時計進行時のIBL全更新費用がどの予算枠にも未配分である」と同型のギャップであり、値が出そろっている点だけが違う。**あわせて2つの限界を残す。**第一に、この実測は描画対象3件の小さい世界のものであり、代表物量(常駐100,026個体)での点光源の影を測っていない。**面ごとの絞りが描画対象の粒度で働くため、物量が増えたときに費用を決めるのは面の数ではなく面の錐台を通った描画対象の頂点の量である。**第二に、24本のパスのコマンド記録のCPU時間に専用の区間が無い。**判断nは記録の区間と発行数を別に測ると定めたが、実装したのは発行数の側だけであり、記録の費用は`描画記録送信提示ms`へ混ざったまま出る。**能力の出自分類(c)は着手順5でも現れていない。**多光源と点光源の影は`_doc/計画/技術ビジョン.html`第8章の着手順5として計画に在り、この完了で実装済みへ移った。

**点光源の影の枠の制定(2026-08-11、親裁定)**: 上のギャップを受けて**点光源の影の枠0.10msの行を新設した**(満載24面の代表値0.0696msで消費率69.6%。枠は代表値の1.44倍であり、IBL更新1回のピーク上限の1.42倍と同じ帯にある)。0.10msは予備から回し、予備は1.95msから1.85msへ減った。**フレーム予算の合計は16.6msのまま変わらない**(4.0 + 2.6 + 1.5 + 1.0 + 0.15 + 0.3 + 0.45 + 0.15 + 0.10 + 0.5 + 3.0 + 1.0 + 1.85 = 16.6)。**この枠は暫定である。**根拠の実測が描画対象3件の小さい世界のものであり、面ごとの絞りが描画対象の粒度で働く以上、物量が増えたときの費用は面の数でなく面の錐台を通った頂点の量が決めるためである。代表物量(常駐100,026個体)で点光源の影を測った日に再審査し、そのときCPU記録の専用の区間の不在も同時に解消する(設計正本の積み残しの期限の再裁定と同じ日付である)。

大規模世界の第5段階(遠景、2026-08-16完了)は**GPUパスを1本も追加していない**。遠景メッシュは世界に1枚だけの地形メッシュであり、既存のシーンパスと最遠距離区分のシャドウパスが描く定数1件の描画対象である。空中遠近のベイク済み画像は奥行きの分割を32から160へ増やしたが、生成パスも合成パスも本数は変わらない。多段シャドウの距離区分の再配分も、距離区分数4とパスの構成を動かさず境界の距離だけを変える。したがってパスの区分は変わらず、**予算表も改訂しない**(OW3・OW4と同型の判断)。

**ギャップ判定(大規模世界の第5段階完了時点): この段階のGPU時間は1つも計測していない。**公式の計測は第7段階(公式の計測と予算の制定)であり、第5段階の完了条件に性能の実測を含めていない。未計測は3つある。**第一に、遠景メッシュ1枚の描画とシャドウの最遠距離区分への追加が、シーン描画4.0ms枠とシャドウ2.6ms枠のどちらへどれだけ載るかを測っていない。**遠景は25メートル格子の粗い面であり頂点の量は近傍チャンクより桁で少ないが、画面の広い面積を覆うため画素段の費用が支配しうる。**第二に、空中遠近の奥行きを5倍にした生成費用の増分を測っていない。**OW5第7段の実測は奥行き32時点の空中遠近生成0.0307ms・合成0.0188msである。生成の費用はボクセルの数に比例すると見込まれ、空1.0ms枠に対する定常合計が0.0826msだったことから5倍でも枠内と見込むが、これは見込みであって実測ではない。**第三に、距離区分を10000メートルまで広げたことがシャドウのGPU時間へ与える影響を測っていない。**最遠距離区分の正射影が覆う面積が広がり、そこへ出すキャスターの量が変わる。3つとも第7段階の公式の計測で確定する。

容量はVRAM側だけが動いた。空中遠近のベイク済み画像がRGBA16Fで256KiBから1.25MiBへ5倍になる。起動時に一度だけ確保する固定資源の1枚であり、ストリーミングが保持してよい容量(3.5)には入らない(参照: `_doc/設計/性能アーキテクチャ方針.md`「5. GPUメモリ確保」)。

**能力の出自分類(c)は大規模世界の第5段階でも現れていない。**遠景は`_doc/設計/大規模世界の生成と遠景.md`の段割りに在り、この完了で実装済みへ移った。

大規模世界の第7段階(公式の計測と予算の制定、2026-08-18)は**GPUパスを1本も追加していない**。第5段階と第6段階が足したのは遠景メッシュ1枚と散布の個体群であり、どちらも既存のシーンパスとシャドウパスで描かれる。したがって予算表は改訂しない。**この段階が行ったのは、大規模世界の代表条件でのGPU時間を初めて公式に測ることである。**8km×10kmを8キロメートルの直線経路で移動する1000フレームを、単一画面のリリースビルドで3プロセス×3反復=9標本採った(一次記録は`_doc/計測/大規模世界第7段階_2026-08-18.md`)。9標本の中央値は、シーン描画3.2177ms(4.0ms予算の**80.4%**、9標本の範囲79.0〜82.7%)・シャドウ合計7.9527ms(2.6ms予算の**305.9%**、範囲300.2〜311.9%)・空の定常合計0.2086ms(1.0ms予算の**20.9%**)・GPU定常合計22.0915ms(16.6ms予算の**133.1%**、範囲131.1〜135.9%)である。決定的な値(発行数908・距離区分別の発行数[305, 367, 2156, 2286]・候補数78,826・シーン可視数24,236・段別個体数・投入インデックス数)は9標本すべてで一致した。

**ギャップ判定(大規模世界の第7段階完了時点): シャドウ2.6ms予算の305.9%超過と、GPU定常合計16.6ms予算の133.1%超過の2つが確定した。**どちらも9標本の範囲の下端で枠を割らないため、機材の揺れで裏返る判定ではない。**帰属は距離区分2と3へ散布個体が全数入る構造である。**距離区分3の可視数78,533は候補数78,826の99.6パーセントであり、可視の絞り込みがほとんど効かない。距離区分2と3の投入インデックス数の合計59,878,896はシーンの11,064,252の5.41倍である。シーンは距離で段を選んで24,236個体まで絞るのに対し、最遠距離区分は10,000メートルまで覆うため先読み半径8の常駐個体がまるごと入る。**これは実装の欠陥でなく、大規模世界だけが使う明示境界25/100/1000メートルと最大影距離10,000メートルという距離区分の切り方が構造的に招く量である。**是正は距離区分の切り方かキャスターの絞り方の設計判断を要するため、外部台帳の札#138のコメント359へ起票して第7段階の外で扱う。**シーン描画・空・可視個体の選別の3つは予算内であり、第7段階で新たな超過は検出していない**(超過した2つは第7段階が初めて測って確定させたものである)。

**大規模世界の第7段階は投入インデックス数を記録の軸として新設した。**公式値はシーン11,064,252・距離区分別[1,004,484 / 1,206,096 / 27,932,316 / 31,946,580]である。**判定の閾値は未制定であり、この値は実測の記録である。**距離区分の世界メートル毎テクセルと同じ様式であり、発行数や可視数が個体の件数を数えるのに対し、この軸は描く三角形の量の代理変数として読む。個体数が同じでも段の選ばれ方と原型のトポロジー量で動くため、部品カタログのように1個体が複数部品へ展開される路線ではこちらだけが動く。

判定基準: 消費率50%未満は順調。50〜100%は注視し、物量スケーリング(3.3)を確認する。100%超過は最適化タスクとして札に登録する。**最適化は計測に基づくものだけを行う**(開発計画の判断基準4)。

### 3.2 理論性能との比較(判定精度は高いが算出コストも高い。予算超過したパスに絞って適用)

- 対象パスの支配資源(メモリ帯域・ピクセル処理レート・三角形処理レート・演算能力)を特定し、GPUの仕様値から求めた理論下限時間との乖離倍率を出す。
- 乖離が説明できない大きさ(目安: 数倍以上)のときに初めて最適化に着手する。手法はGPUプロファイラ(Nsight等)による支配資源の実測と、ボトルネックへの構造的対処。

### 3.3 物量スケーリング(オープンワールド方針の検証手段)

- 同一シーンの物量(インスタンス数・頂点数・テクスチャ量)を1倍・10倍・100倍に変えて所要時間の伸び方を記録し、線形から外れる点(折れ点)を特定する。
- 折れ点の原因(描画コマンド数・メモリ帯域・CPU律速のいずれか)が、フェーズ2の投資先(バインドレスディスクリプタ・GPU駆動可視選別)の根拠データになる。
- OW0完了時点では、同じメッシュとマテリアルを持つ描画対象を固定カメラ内へ1・10・100個配置し、描画発行数、GPU時間、CPU時間、GPUメモリを`cargo xtask object-bench`で比較できる。2026-07-23の実測では、1→100対象でシーン描画が0.0506→0.0755ms、シャドウが0.0287→0.0353ms、CPUの記録・送信・提示平均が0.1482→0.1784msとなり、描画発行は予算内だった。GPUメモリ確保数は10→604件へ増え、対象ごとに6件増える線形構造を確認した。この固定quad計測は描画対象数だけの基礎特性を測るものであり、OW4では植生、可視判定、LODを含む代表シーンへ差し替えて折れ点を正式に判定する。2026-07-25のOW2時点で同じコマンドを再実行したところ、1→100対象でシーン描画は0.0060→0.0154ms、シャドウは0.0037→0.0090ms、CPUの記録・送信・提示平均は0.2041→0.1761msだった。GPUメモリ確保数は10→64→604件で対象ごとに6件増える線形構造が維持されている。GPU時間の絶対値は2026-07-23の計測より低いが、同一コミットでの反復計測を行っていないため、実行間変動と実装差のどちらが主因かは判定していない。
- OW3完了時点(2026-07-28)で、地形1チャンクの実物量を得た。実行時形式は5段の詳細段を含めて440,882バイトであり、最詳細1段だけを焼いていた時点の319,694バイトの1.379倍である。先読み半径2で25チャンクを常駐させた統合計測(`cargo xtask ow3-dod`)では、ディスク読込が25件・11,022,050バイト、GPU転送が25件・11,017,200バイトだった。地形は1チャンクにつき1つの描画対象であるため、この量は常駐チャンク数に比例する。1倍・10倍・100倍の折れ点判定は、同一メッシュを共有するインスタンス群が入るOW4で行う方針を変えない。
- OW4完了時点(2026-07-29)で、代表シーンによる1倍・10倍・100倍の折れ点判定を正式に行った。計測は地形と植生が同居する25チャンクの検証用地形世界で行い、原型・共有マテリアル・カメラ経路・LOD閾値・配置の種をすべて固定して**変えたのはチャンクあたりの個体数だけ**である。物量点は常駐10,026・20,026・50,026・100,026・1,000,026個体の5点であり、各3回反復した(入口は`cargo xtask ow4-bench`)。**折れ点は5倍の50,026個体であり、そこで最初に予算を超えたのはGPUのシャドウパスである。**1倍と10倍のあいだに折れがあったため、規則に従って2倍と5倍を足して局在化した。折れ点での支配要因はGPUのシャドウパスであり、観測範囲全体(1倍から100倍)の支配区間は可視個体の選別である(増分9.92msで、2番目のシャドウの5.03msの約2倍)。この2つは別の問いへの答えであり混同しない。CPU区間には観測範囲内に折れ点が無く、可視個体の選別の個体あたり費用は5点すべてで9.55〜10.25nsに収まる。**描画発行数は5点すべてでシーン40・シャドウ51、GPUメモリ確保数は687件であり、個体数が100倍になっても変わらない。**フェーズ2の投資先はこの結果で判定した。間接描画・バインドレスディスクリプタ・サブアロケーションはいずれもトリガーが立たず不採用であり、立ったのは個体判定とLOD分類の区間の改善だけである。詳細は `_doc/計測/OW4_2026-07-29.md` を参照する。

### 3.4 計測の再現性

- ベンチマークはリリースビルド、固定シーン、固定カメラ、固定600フレームで行い、`cargo xtask bench` として登録済み(繰り返す検証は使い捨てにせずコマンド化する方針)。GPU時間に加え、先頭120フレームを除外したCPU側フレーム間隔の平均・p50・p95・p99・最大値・25ms超過回数を出力する。
- 結果は追記専用の記録として `_doc/計測/` に残し、推移を見る。CPU側はフレーム時間分布(中央値p50・悪化側p99・突発的な遅延の検出)を併記する(M11の完了条件に接続)。
- 2026-07-23のM11初回計測では、GPU合計0.7276msに対してCPU側フレーム間隔はp50 16.7057ms、p95 33.2550ms、25ms超過率8.75%だった。GPU予算には余裕があるが60fps安定は未達であり、イベントループとFIFO提示待ちを次の調査対象とする。詳細は `_doc/計測/M11_2026-07-23.md` を参照する。
- レンダラー内部の区間計測では、p99 33.5772msの遅延がフェンス待機へ集中し、フレームデータ準備・画像取得・記録送信提示はいずれもp99 1.5ms未満だった。進行中フレーム数変更では改善が再現しなかった。
- 実表示時刻は`VK_KHR_present_id`と`VK_KHR_present_wait`で直接測る。提示に単調増加のIDを付け、次フレームの先頭でそのIDの表示完了を待ち、`--report-display-timing`指定時に実表示間隔と停止時間を出力する。対応可否は物理デバイスの拡張一覧と機能フラグの両方から判定し、非対応環境では拡張を有効化せず計測も行わない。2026-07-25のOW2の5回反復では、CPU側の25ms超過と実表示の25ms超過が最大1回差で一致し、CPU側フレーム開始間隔の33ms級の遅延が実表示の更新脱落を伴うことを確定させた。したがってCPU側間隔はFIFO供給の診断値であると同時に、この環境では更新脱落回数の指標としても使える。詳細は `_doc/計測/OW2_2026-07-25.md` を参照する。
- この計測は専用フラグ`--report-display-timing`で明示したときだけ動く。NVIDIAドライバー596.21では`vkWaitForPresentKHR`がtimeout=0でも表示まで停止するため、指定した実行は毎フレーム約16ms描画ループが止まる条件になる。指定が無ければ拡張自体を有効化しないので、`cargo xtask bench`は従来と同じ素の条件を保つ。実表示計測つきの実行は`cargo xtask bench-display-timing`で採る。
- 計測の有無を比べるときは交互に実行すること。この機材のフレーム間隔は時間帯で大きく変動し、同一コード・同一コマンドで25ms超過が0回から156回まで動いた実測がある。時間帯をまたいだ比較で摂動の有無を判断してはならない。計測なしの条件の脱落回数は、FIFOの提示1件がvblank1回でしか表示されないことを使った算術で求める。
- 単発実行の結果から実行間分散と実在する欠陥を区別できないため、再現性の判定には同一条件の反復実行を用いる。2026-07-25のOW2時点では `cargo xtask bench` を5回反復し、25ms超過が5回中5回(40〜57回 / 480標本)、p99が33.8084〜35.0483msだった。M11の値は再現し、OW1完了時の25ms超過0回は5回中1回も再現しなかった。ただしこれはその時間帯の性質であり、この環境の常態と断定してはならない（同日の後半に0回から156回までの変動を観測した。上の項を参照）。詳細は `_doc/計測/OW2_2026-07-25.md` を参照する。
- **計測は表示構成を記録してから行う（2026-07-27制定）。** デスクトップの有効表示を1画面へ切り替えた条件で同じ5回反復を行うと25ms超過は5回中4回で0回・1回で1回になり、33ms級遅延の主要因はWindowsの複数画面デスクトップ合成であると強く支持された。表示構成が違う計測値を同じ時系列として比較してはならない。単一画面のこの条件でp99は20ms以下に収まり、60fps安定の証明に使える計測環境が初めて成立した。詳細は `_doc/計測/OW2_2026-07-25.md` の追記を参照する。
- 2026-07-29のOW4物量計測も単一画面条件で行い、各物量点を3回ずつ採った。さらに既定の3点は、validation検査を足す前のコード状態でも同じ日に1回採ってある。個体あたり費用の帯と会計の6列は両方の実行で重なり、描画発行数も一致した。折れ点が2倍と5倍のあいだにあることは2つの実行で再現した。一方でGPU時間は、入力が1件も違わない前後の実行で絶対値が3倍以上動く点があり、原因はGPUのクロックと電力状態の変動である。GPU時間の比較は負荷帯をそろえた条件でしか成立しない。詳細は `_doc/計測/OW4_2026-07-29.md` を参照する。
- 2026-07-28のOW2完了時計測(単一画面、リリースビルド、helmet+粒子): GPU合計0.3404msで16.6ms予算の2.1%。シーン描画0.1522ms(4.0ms予算の3.8%)、シャドウ0.0341ms(2.0ms予算の1.7%)、ポスト処理計0.1208ms(1.5ms予算の8.1%)、粒子計0.0333ms。CPU側フレーム間隔はp50 16.6308ms、p99 21.1073ms、25ms超過1回/480標本。OW2はGPUパスを追加しておらず、チャンクは既存のシーンパスで描かれる。能力の出自分類(c)は現れなかった。

### 3.5 容量予算(ストリーミングが保持してよいRAM・VRAM)

3.1がフレーム時間(16.6ms)の配分を扱うのに対し、この節は容量(バイト数)の上限を扱う。ストリーミングが保持してよいRAMとVRAMの上限は別々に持ち、超えた分は中心優先の順序で外周を縮退させる。

| 資源 | 上限(2026-07-26時点の既定値) | 位置づけ |
|---|---|---|
| ストリーミングRAM | 400バイト | 暫定。検証用世界で縮退を発動できる値 |
| ストリーミングVRAM | 4096バイト | 暫定。板の世界ではRAM上限が先に拘束するため、縮退の決め手にならない |

**この2つの値は暫定であり、実運用の値ではない。** 検証用世界は1チャンクが369バイト、25チャンク合計9,225バイトで、開発機のベースライン(専用VRAM 144.64MiB)に対して無視できる量である。現実的な上限を置くと縮退機構が一度も発動せず、機構が動くことを確かめられない。したがって既定値の根拠は「検証用世界で縮退を発動できること」だけに限る。

RAM上限を400バイトにしたのは、1チャンクの読込前見積が216バイト、読込完了後の実測が369バイトだからである。上限をこの2つの間に置くと、中心チャンク1件だけを収容して外周8件を縮退させ、なおかつ読込完了後の実測使用量が上限を超えない。上限を2チャンク分以上へ上げると、見積を根拠に収容した集合が読込完了後に上限を超えて現在使用量超過になる。この見積と実測の乖離(実測が見積の約1.71倍)は、見積がメッシュとテクスチャの生バイト数だけを数え、実測が版付きファイル全体の長さであることに由来する。

OW3で地形1チャンクの実測量が440,882バイトになり(3.3)、暫定既定値との差が確定した。既定のRAM上限400バイトは地形1チャンクの約1,102分の1、VRAM上限4096バイトは約108分の1であり、既定のままでは地形チャンクを1件も収容できない。これがOW3完了時点で検出した予算とのギャップである。地形世界を描く実行はこのギャップを`--streaming-ram-limit`と`--streaming-vram-limit`で16メビバイトを明示して迂回しており(参照: `_doc/設計/チャンクストリーミング.md`「検証用地形世界」)、実測の最大使用量はRAM 11,018,170バイト、VRAM 11,017,200バイトでこの上限に収まる。

このギャップを既定値の改定で埋めることはOW3完了時点では行わない。既定値の根拠が「検証用の板の世界で縮退を発動できること」だけであり、地形の物量へ引き上げると縮退機構が一度も動かず、機構が働くことを確かめられなくなるためである。実運用の値の制定には1チャンクの実測量に加えて、視界距離あたりの常駐チャンク数と対象機のVRAM配分が要る。この2つは植生と可視判定を載せた代表シーンを測るOW4で確定するため、それまでは板の世界と地形世界が別々の上限を使う状態を維持する。

OW2完了時(2026-07-28)に、この暫定予算が実行時に効くことを実測で確認した。縮退・上限超過の安全退避・中心優先の優先度退避がいずれも発動し、退避を無効化した対照実行では現在使用量超過で停止する(予算が判定に使われていることの直接証拠)。30分相当108,000フレームの反復で読込12,156回・解除12,150回を経てRAM・VRAMは横ばいだった。台帳のVRAMは実破棄の報告まで減らないため、解除予約は予算上の空きにならない。

**OW4完了時(2026-07-29)も既定値は400バイトと4096バイトのままであり、実運用の値は制定していない。**OW3完了時に制定をOW4の代表シーン計測へ委ねたが、OW4が確定させたのは検証用地形世界の側の値だけである。植生を同居させた1チャンクは446,212バイトになり、先読み半径2の25チャンクで最大使用量はRAM 11,152,980バイト・VRAM 11,274,000バイトだった(参照: `_doc/設計/チャンクストリーミング.md`「検証用地形世界」)。実運用の視界距離と対象機のVRAM配分は依然として未定であり、この2つが決まらないまま既定値を上げても、検証用の値が別の検証用の値へ変わるだけで縮退機構を確かめられなくなる副作用だけが残る。**したがって制定はOW5以降へ持ち越す。これがOW4完了時点で残るギャップである。**

物量計測は個体1件あたりの会計も確定させた。台帳RAMが44.0バイト(個体配置40 + 置換検査記録4)、台帳VRAMが120.0バイト(個体変換112 + 可視ID列8)であり、見積の定数と1バイトの誤差なく一致した。1,000,026個体では台帳RAMが55,082,000バイト、台帳VRAMが131,082,000バイトである。**ただし台帳が数えるRAMとプロセス実測のprivateの差は未閉塞である。**10,026個体から1,000,026個体でprivateが約150MiB増えたのに対し、台帳RAMの増分は41.5MiBで3.6倍の開きがある。差に含まれるのはホスト可視の可視ID列・群可視材料の個体境界球・個体別の段状態・可視選択の並べ替え作業域・ドライバー側の確保であり、いずれも入場制御が数えていない物量比例の常駐ホスト構造である。棚卸しと算入の判断は札#138へ起票した(参照: `_doc/計測/OW4_2026-07-29.md`「追記: RAM会計の記述の訂正」)。VRAM側は台帳の125.01MiBに対して専用VRAMの実測が213.62MiBだが、差の88.6MiBは10,026個体での差88.3MiBとほぼ同じであり、台帳の追跡対象外の量は物量で増えていない。

**着手順4のアセット形式整備(テクスチャのブロック圧縮と縮小段生成)の完了時(2026-08-09)も、既定値は400バイトと4096バイトのままである。**この完了で暫定値を再制定しないことは設計正本の判断fが先に確定させており、根拠は実アセットが到着した計測の日に一度で行えば暫定値の付け替えを2回やらずに済むことである(参照: `_doc/設計/テクスチャのブロック圧縮と縮小段生成.md`「判断f」)。**この判断を黙って飛ばさずここへ書くのは、上限の値が動かないことと、上限を制定する条件が満たされたかどうかが別の問いだからである。**制定に要る実運用の視界距離と対象機のVRAM配分は依然として未定であり、OW3から続く持ち越しはこの完了時点でも解消していない。

**第1段階の完了時(2026-08-10)も、既定値は400バイトと4096バイトのままである。**場所巡りの世界も検証用地形世界と同じく`--streaming-ram-limit`と`--streaming-vram-limit`で16メビバイトを明示して既定値を迂回する(値を置く1箇所は`xtask/src/fox_tour_launch.rs`であり、遊ぶ入口と検収の入口が同じ指定を読む)。**この世界の容量は計測していない。**アセットコンパイラが焼いたときに出力した9チャンクのファイル長は合計およそ3.97メガバイトだが、これはストリーミング台帳が数えた常駐量ではなく、上限の制定に使える値でもない。制定に要る実運用の視界距離と対象機のVRAM配分は依然として未定であり、OW3から続く持ち越しはこの完了時点でも解消していない。

**実運用の上限を制定した(2026-08-18、大規模世界の第7段階)。**OW3から持ち越してきた「実運用の視界距離と対象機のVRAM配分が未定である」という制定の条件は、8km×10kmの世界を先読み半径8で走らせた公式計測(`_doc/計測/大規模世界第7段階_2026-08-18.md`)で揃った。

| 資源 | 上限(2026-08-18制定) | バイト数 | 実測最大 | 倍率 |
|---|---|---|---|---|
| 大規模世界の台帳RAM | 64MiB | 67,108,864 | 37,573,348バイト | 1.79倍 |
| 大規模世界の台帳VRAM | 256MiB | 268,435,456 | 178,350,008バイト | 1.51倍 |

**上限は2進の丸い値から採る。**ストリーミングの上限はバイト数で受け渡され、既存の指定はどれも2進の丸い値である(検収の実行が渡す512MiB、検証用地形世界と場所巡りの世界が渡す16MiB)。10進のメガバイトで置くと、指定の綴りだけが他と揃わなくなる。

**倍率の根拠に3.1のGPU時間の枠の帯(代表値の1.4倍から1.7倍)を使わない。意味が違うためである。**時間の枠の余裕は機材のGPU時間の揺れを包むものであり、フレームごとに上下する量に対して置く。容量の上限は揺れでなく**成長**——視界距離と散布密度の拡張——を包む天井であり、一度確保したら下がらない量に対して置く。

**成長の検算。**判断jの個体の枠を128体から450体へ上げたとき(`_doc/設計/大規模世界の生成と遠景.md`「判断j: ブレス オブ ザ ワイルド級のアセット規模は手続き的な配置で作る」)、台帳がどれだけ伸びるかを内訳から求める。台帳VRAMの1チャンクあたり617,128バイトの内訳は、原型の複製量122,399バイト(19.8%)・個体32,731バイト(5.3%、個体1件120バイト)・残り461,999バイト(74.9%)である。**残りは地形メッシュであり、OW3が独立に実測した地形1チャンクの440,882バイトと4.8パーセントの差で符合する。**

**密度で動くのは個体の項だけである。**原型の複製量は個体数と無関係に原型の中身だけで決まり、地形メッシュは散布と無関係である。したがって270.0体から450体へ上げても台帳VRAMは178,350,008バイトから184,496,888バイト(175.9MiB、上限の**68.7%**)へ伸びるだけで、**伸びは1.034倍にとどまり上限には当たらない。**

**個体密度の方向では、容量の上限より先に可視個体の選別の予算が必ず当たる。**3.6節のとおり現在の単価19.43ナノ秒では選別の2.0ms予算が1チャンクあたり約356体で拘束し、そのときの台帳VRAMは172.8MiB(上限の67.5%)である。**上限が実際に拘束するのは地形側が伸びたとき**——先読み半径を広げるかチャンクを細かくするか——であり、散布密度ではない。

**台帳RAMの最大値は密度に直接比例しない。**台帳は常駐の記録をVRAMだけで数え、RAMとして数えるのは読込中・準備済み・GPU転送中という途中状態の記録だけである(`crates/blitz_engine/src/streaming/chunk_ledger/usage.rs`)。したがってRAMの最大値を決めるのは先読みが同時に進める件数であり、密度は1件あたりの大きさにしか効かない。1.79倍という余裕は、この同時進行数が増える方向の成長を包む。

**板の世界の既定値400バイトと4096バイトは従来どおり維持する。**上の上限は大規模世界が明示して渡す値であり、既定値を置き換えるものではない。既定値の根拠は「検証用世界で縮退を発動できること」だけであり、実運用の値へ引き上げると縮退機構が一度も動かず、機構が働くことを確かめられなくなる。**2本立ての構造は変えない。**

ブロック圧縮が減らすのはテクスチャが占めるバイト数であって、上限の値ではない。実測は対照の板が5.98分の1、DamagedHelmetが1.37分の1である(後者が1.37にとどまるのはベースカラー1枚だけを圧縮する初版の範囲の帰結であり、符号化の性能ではない。詳細と内訳は`_doc/計測/ブロック圧縮の容量効果_2026-08-09.md`)。**あわせて、この節の見積が読む値の意味が変わった。**カタログの容量メタデータは版3で「展開済みテクスチャの合計バイト数」からテクスチャ格納バイト数(格納形式で数えたバイト数)へ変わり、GPUへ載る実バイト数と一致するようになった。既存の全世界は既定の`全てRGBA8`で焼かれるため、この意味の変更で動く数値は現時点で1つも無い。

### 3.6 CPU区間予算(2026-07-29制定、暫定)

3.1がGPUパスへのフレーム時間の配分を扱い3.5が容量の上限を扱うのに対し、この節はメインスレッドのCPU区間が1フレームで占めてよい時間の上限を扱う。区間の切り方は `_doc/設計/植生インスタンスと物量計測.md`「CPU区間の切り方」の3区間に従う。

| CPU区間 | 予算(2026-07-29時点の暫定値) | 位置づけ |
|---|---|---|
| 可視個体の選別(群と個体の可視判定・個体別LODの段選択・可視ID列の並べ替えを1回に融合した走査) | 2.0ms | 暫定。約10ns/個体の実測に対して常駐200,000個体に相当する |

**この値は暫定であり、実運用の値ではない。**根拠は2つある。1つはOW4の実測であり、可視個体の選別の個体あたり費用は9.55〜10.25ns(シャドウのライト視錐台判定を足した第6段の後は11.14〜11.62ns)の帯に収まって物量で悪化しない。この単価のもとで2.0msは常駐約200,000個体にあたる。もう1つは他の予算との釣り合いであり、可視個体の選別はシーンの主描画そのものではなくそれに従属する区間であるから、主描画の4.0msではなくシャドウGPUと同じ2.0msの枠へそろえた。

この予算に対し、1,000,026個体での実測10.0174ms(第6段の後は11.3722ms)は5.0倍(5.7倍)の超過である。**したがってこの予算は、現在の単一スレッド走査で常駐させてよい個体数の上限を規定する予算として機能する。**同じ物量点では可視個体の選別だけで16.6msフレームの60.3%(第6段の後は68.5%)を1区間が占め、フェンス待機のp50が0.0075msへ落ちて律速がFIFO提示待ちからCPUの処理量へ移る。

**見直し条件**: OW5、または実運用の視界距離と常駐個体数が制定された時点で、この暫定値を実データで置き換える。それまでの200,000個体という含意は、検証用世界の密度と実測単価から逆算した値にすぎない。

OW5第8段(2026-08-01)の実測では、低い太陽と本番の描画条件の代表物量(常駐100,026個体)で可視個体の選別のp50が1.5228msとなり、2.0ms予算の76.1%だった。個体あたりは15.20〜15.26nsであり、1倍(常駐10,026個体)の16.02〜16.34nsと同帯である。この単価のもとで2.0msは常駐約130,000個体にあたる。**暫定値2.0msそのものはこの計測で置き換えていない。**置き換えに要る実運用の視界距離と常駐個体数が依然として未定であり、第8段が確定させたのは検証用世界での単価と代表物量での消費率だけだからである。p99は3回のうち2回が1.67〜1.68msで予算内、1回が2.3779msで超過した。**CPU区間予算の判定はp50で行い、p99は監視値として併記する**(2026-08-01裁定)。p99は機材の常駐負荷で3回中1回だけ超過する振れ方をし、予算判定の基準に使うと機材状態が判定を裏返す。予算の役割は設計判断の物差しでありp50が担い、突発遅延の検出はp99の併記と推移の監視で行う。p99の超過が機材状態と無関係に恒常化したときは、原因をフレーム分布として調べた上でこの裁定を見直す。一次記録は`_doc/計測/OW5第8段_2026-08-01.md`にある。

大規模世界の第7段階(2026-08-18)の実測では、8km×10kmを8キロメートルの直線経路で移動する代表条件で可視個体の選別のp50が1.5315msとなり、**2.0ms予算の76.6%**だった(9標本の中央値。範囲76.2〜77.1%)。常駐候補数は78,826個体であるから個体あたりは19.43ナノ秒であり、OW5第8段の15.20〜15.26ナノ秒より27パーセント高い。**単価が上がったのは、散布の1個体が部品の複数個体へ展開されて個体1件あたりの群の数が増え、粗判定の効きが変わったためと見込まれるが、この計測は単価を分解していない。**同じ実行の作業領域更新のp50は1.8445ms、描画記録・送信・提示のp50は1.0025msである。**この2区間には予算の行が無い。**p99の中央値は2.0335msで予算をわずかに超えるが、2026-08-01の裁定どおり判定はp50で行い、p99は監視値として併記する。**暫定値2.0msそのものはこの計測でも置き換えていない。**置き換えに要る実運用の視界距離と常駐個体数は依然として未定である。一次記録は`_doc/計測/大規模世界第7段階_2026-08-18.md`にある。

## 4. 変更容易性(作り続けられるか)

エンジンは完成品ではなく継続開発される資産のため、変更のしやすさ自体を評価対象にする。

- **開発ループ速度**は、差分ビルド時間と、シェーダー・アセットのホットリロード反映時間で測る。目安は check 数秒・ホットリロード1秒未満である(`_doc/設計/開発ループとコンパイル時間.md` の段階定義に対応)。
- **AI駆動開発への適合**(このプロジェクト固有の軸)は、自律ループにおける検収指摘数の推移で測る。実績はM0で3件、M1で1件、M2で1件、M3で0件、M4で軽微1件である。増加に転じたら、コードではなく規約・分割粒度・機械検証の側の欠陥を疑って直す。
- **依存の隔離の健全度**(外販可能性・移植可能性の裏付け)は、公開APIへのash型の露出ゼロ、blitz_engine のash・winit非依存、外部依存クレート総数の推移で測る。層ごとの直接依存は `cargo xtask conform` の白リストで機械検査し、推移的依存は `cargo tree --workspace --edges normal` で確認する。2026-07-23時点のWindows向け通常依存グラフは85パッケージ版（自前7、外部78）である。
- **信頼性**は、長時間実行でのメモリ・VRAMリークの有無と、デバイスロスト時の型付きエラー伝播で測る。長時間実行の計測はM11で行う。
- M11の3600フレーム連続実行では、初期安定化後のワーキングセット、プライベートメモリ、専用VRAMは横ばいで、フレーム数に比例する増加を検出しなかった。`cargo xtask m11-soak`で同じテキスト計測を再実行できる。

## 判断フロー(いつ・何を判断するか)

1. **毎イテレーション**(自律ループ): 完了定義の充足 + validation + smoke + conform(現行の検証列)。M5以降はパス別GPU時間の確認を追加。
2. **マイルストーン完了時**: 予算表の消費率を更新する。超過があれば最適化タスクとして札に登録する。能力の出自分類で (c) が現れたら開発計画の改訂として扱い、非ゴールの変更に当たる場合はユーザーへ相談する。
3. **M11(エンジンフェーズ1完了検証)**: 4系統の総点検(代表シーンテスト・ベンチマーク総括・物量スケーリング計測・変更容易性の各指標)をレポートにまとめ、フェーズ2詳細計画の入力にする。
4. **対象ゲーム決定後**: 代表シーンを実仕様のシーンへ差し替え、予算表を実データで再配分する。

## 行わないこと

- 他エンジン(Unity・Unreal・Bevy)との相対ベンチマークはフェーズ1では行わない。等価なシーンを両側で用意するコストが高く、理論性能との比較(3.2)の方が同じ労力で得られる情報が多いため。外販の営業材料として必要になった時点で計画する。
- 合成的なマイクロベンチマーク(三角形数百万個の単純描画など)は、代表シーンテストの代替にしない。
