# 2026-09-18 4本のPRのレビュー対応と世界実行 遺言

逐語は経路B(`/export`)で `_doc/開発スレッド/2026-09-18-4本のPRのレビュー対応と世界実行.txt` へ保存する(ユーザー操作)。本ファイルはその後記(遺言)である。セッション: https://claude.ai/code/session_01PyfxsqN46S1QfmKGh6prh5

## 1. 確定した到達点

- PR #140(`blitz_ecs`)・#142(PR-A)・#143(PR-B)・#144(PR-C)のレビュー対応を終え、4本ともオーナーの指示でマージした(main は `0cee56e6`)。Issue #45・#47・#49 は閉じた。起票: Issue #145(`resumed` の契機とゲーム世界の初期化の分離)、Issue #146(常駐するチャンクの集合のゲームへの反映を固定刻みの工程境界へ置き、固定経路の位置を固定刻みの番号から決める)
- Issue #46(世界実行API): 判断12を `_doc/設計/ゲーム制作アーキテクチャ.md` へ書き、[PR #148](https://github.com/megaDenryu/Blitzdrache0/pull/148)(ブランチ `feat/issue-46-world-execution`、先端 `f9d1229d`、6コミット、`Part of #46`)を出した。実装と検収(構造・行数・命名・テスト実効性・設計正本整合・孤立実装)は完了し、是正2便も済んだ。issue #46 へ吟味を投稿済み
- 用語 `世界実行`・`刻み入力`・`刻み結果`・`描画供給`・`ゲーム配線` をユビキタス言語へ登録済み

## 2. 進行中の作業

- **PR #148 の先端 `f9d1229d` の機械検証が未完走。** 最後の2コミット(遊ばない起動の試験・正本の記述)を含む木での `cargo xtask verify` は1度も完走していない。畳む前の `e14a2b95`・`ade2e1a7` は既知の2件だけで通過していた。ビルド員へ `f9d1229d` の検証を依頼済み(新セッションではビルド員を起動し直して依頼し直す)
- 完了報告の下書き: スクラッチパッド `issue46_done.md`(文章検査済み。セッション固有で消えうる。要点は本ファイル4節)

## 3. 次の一手

1. `f9d1229d` の検証を完走させる(既知の失敗は `crates/blitz_sim/src/contact/scene/` の2件)。通ったら PR #148 の本文の検証の表を更新し、`Part of #46` を `Closes #46` へ差し替え、issue #46 へ完了条件6つを引用した完了報告を投稿し、状態ラベルを「裁定待ち」に保つ
2. オーナーの受理後に、Issue #44 の順で #50(世界更新工程)→ #52(描画供給の抽出)。低頻度の周期と、ヨーを読む順序を型で守る形は #50 で扱う(判断12に明記)

## 4. 未決着の事項

- 完了報告の要点: 6条件はすべて満たした(入口の限定・時間意味の分離・ECS内部を触らない(白リストで機械強制)・型付き結果・段階移行・補間値を戻さない(型))。吟味からの変更点は「注視点を `描画供給` に含めず別の口 `カメラの注視点にする混ぜた大域位置` にした(計器が1描画古くなるため)」
- ヨーを読む順序(カメラの更新より前)は工程の並びだけが守り、型も試験も守っていない。判断12に明記済み、#50 の範囲

## 5. 地雷と環境制約

- **ビルド専用チェックアウト `C:/devs/Blitzdrache0_build` を、オーナーの別の作業(agy。作業ツリー `C:/devs/Blitzdrache0_agy`、ブランチ `feat/issue-137-domain-ontology-substep`)が同時に使い、HEADを切り替える。** その最中のビルド員の検証結果は別の木のものになりうる(実際に `030796c7` と名付けた走行が `fb6ad86b` の木で走った)。ビルド員には走行の先頭で `rev-parse HEAD` を依頼の先端と突き合わせるよう指示した。`tasklist` に agy.exe が残っていてもオーナーは「動いていない」と言っており、プロセス表示は当てにならない
- 本体の作業ツリーで `git status` に改行の正規化だけの M が出る(`git diff` は空)。rebase を阻むので `git checkout -- <それらのファイル>` で戻してよい(diff が空であることを先に確かめる)
- `_doc/設計/ゲーム実行基盤.md` のパスをコードのコメントに書くと、main を含まないブランチで conform の参照パス実在検査が落ちる(語で書く)
- 作業ツリーは本体1つ。オーナーの指示で増やさない
- メモリ索引に別セッションが「既存の識別子を勝手に改名しない」を足している(2026-09-18)。実装員への委譲プロンプトに「移設のついでに改名しない」を書く

## 6. 学びと注意

- スレッド引き継ぎの読み飛ばしを最初に叱責された。スキル全文・前スレッド・設計正本・札とAgentRoomの照合を省かない
- 副官の反論は2度とも設計を大きく変えた(#140: 走査しながら予約できる口、Drop での検出 / #46: 借用で縛る案の破棄、共有状態の所有、必要時の口)。判断を固める前に必ず通す
- 代理指標で断定した誤りを2度犯した(検証ログを木の同一性を確かめずに採用、`u32` へ縮める境界を毎描画の経路に置いた)。ログは依頼の先端と `rev-parse` で突き合わせる。境界を置く前にその経路が毎描画で走るかを見る
- テスト実効性チェック員の「同語反復の試験」の指摘は正しかった。不変条件を生産コードの工程順が守っているとき、型の生成関数だけを通す試験は固定にならない

## 7. 参照先

- PR #148、Issue #46・#44・#50・#52・#55・#145・#146。判断12: `_doc/設計/ゲーム制作アーキテクチャ.md`
- 検証のログ: `C:/devs/Blitzdrache0_build/target/verify-logs/`
