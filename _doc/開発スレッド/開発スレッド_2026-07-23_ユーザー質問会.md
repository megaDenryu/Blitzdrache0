# ユーザー質問会スレッド

生成元セッションID: e65a1900-d9c4-400d-80b8-8f454a2315f7
生成日時: 2026-07-23 05:09:24
プロジェクトディレクトリ: C:\devs\Blitzdrache0

## 現状・残タスク

コード実装は行っていない、ユーザーとAIの壁打ち・Q&Aのみのセッション。codexが並行してOW0第5段（複数描画対象のGPU資源・描画発行）を実装している最中に、ユーザーがそのアーキテクチャ・原則・運用を横から問い詰めた記録。

扱った主題（発言順）:
1. codexの並行作業（OW0第4〜5段）の内容確認
2. エンジン全体アーキテクチャの現状の不足点（能力・性能・変更容易性・設計状態）
3. Unityのレンダーパイプライン概念とこのリポジトリのレンダーグラフの対応関係
4. Unity固有の欠陥（GC・ドメインリロード・GameObject/ECS併存・シリアライズ）
5. Unity描画層の制約とオープンワールド必須技法の非対応範囲
6. このリポジトリのワークフロー欠落点（fmt検査・CI・pre-commit・性能退行検知）
7. 「可能性を閉じる決定を早期にしない」原則の正しい解釈・誤った解釈
8. その原則に照らした現状の進行評価（恒久決定は守られているが、視点一覧化とフレーム構成移管が後回し）
9. 頂点データがf32配列のままである理由（あるべき設計・初期判断可能性・現状の合理性）
10. Vulkanの難所（フェンス・同期）とこのリポジトリの現状評価
11. ドキュメントの自己言語化能力の評価（ポジティブ/ネガティブ）
12. 負債トリアージ方針の議論とユーザーによる優先度訂正（CI優先度は過大評価だった）、札#138が2026-07-20から更新されていない事実の発見、5項目目の定点追加提案

**残タスク**: 「負債のトリアージ方針」節で提案した以下は、ユーザー合意待ちで未着手。
- `_doc/計画/開発計画.md`「マイルストーン完了時の定点」への5項目目（札#138棚卸し）の正式追記
- 札#138への新規発見4件（フェンス待機の未解明遅延、OW0のDoD未達2点、頂点データの複合課題の分解、運用面負債の監査範囲の欠落）の転記

セッション内でメモリ（`blitzdrache0-debt-triage-doctrine.md`、`blitzdrache0-external-ledger.md`、`blitzdrache0-checkpoint-duties.md`）に記録済み。

---

### ユーザー
> 今codexが作業中です。どのような目的の作業をしているか教えてほしいです。前４コミットくらいを見るといいと思います。

### AI
直近4コミットと現在のワーキングツリーの状態を確認しました。

**大枠（OW0: 基盤共通化）**: フェーズ2の「後続機能が共有するシーン・描画・座標・メモリ確保の基盤を統一する」作業段階です。

**直近4コミット（第1〜4段）**:
- 第1段（a96dfd6）: GPUメモリ確保（`vk::allocate_memory`呼び出し）を1箇所に一元化
- 第2段（62a44ba）: GPUメモリ確保台帳（`memory_ledger.rs`）を実装し、確保・解放を追跡
- 第3段（dbf5063）: GPUメモリ統計をCLIからテキスト出力できるようにした
- 第4段（5e3f650）: blitz_engineのシーンを「非空の描画対象一覧」に変更。1つのシーンが複数の描画対象（メッシュ+マテリアルの組）を持てる契約を導入

**codexが今作業中の内容（未コミット、第5段）**:
開発スレッドの追記によると、これは「**OW0第5段: 描画対象ごとのGPU資源と複数描画発行**」です。

- blitz_appがengineの全描画対象を「描画シーン素材」（型付き変換・頂点・インデックス・マテリアルを束ねた安全な入力）へ変換する処理を追加（`scene_load/render_input.rs`新設）
- blitz_render側で、対象ごとにローカル→ワールド変換・法線行列・ベースカラー・金属度・粗さを持つユニフォームを新設（`render_object_material.rs`, `render_scene_material.rs`, `object_uniform/`）
- ディスクリプタ（Vulkanの資源バインディング機構）を対象ごとに作らず、1レイアウト・1プールから「対象数×フレームインフライト数」分だけセットを割り当てる設計に変更（`descriptor/`配下の変更）
- シーンパス・シャドウパスの両方で全対象を描画するループ化（`frame/dispatch.rs`, `frame/record/shadow_draw.rs`, `graph_build/stages.rs`新設）
- 旧`mesh_resources.rs`を削除し、`render_object_resources/`ディレクトリへ再編
- シェーダー側も`scene_io.slang`を新設し、`scene.slang`/`shadow.slang`を対応する構造に変更

要するに、第4段で「シーンは複数の描画対象を持てる」というデータ契約を入れたのを受けて、第5段でレンダラー（blitz_render）本体を「1対象前提のハードコード」から「複数対象をループして描画する」実装へ書き換えている最中です。開発スレッドには「検証結果: `cargo xtask verify`と既存1対象のスモーク全7ステージが成功。複数対象の実機実行と物量計測は次段」と記載されているので、単体検証は完了しつつ、複数対象での実機確認はまだこれからという段階のようです。

---

### ユーザー
> 今のゲームエンジンの全体的なアーキテクチャについてどのような不足がありますか？

### AI
直近の自己監査文書（`_doc/計画/エンジンフェーズ1完了報告とフェーズ2計画.md`、2026-07-23付け、codexの現在作業も反映済み）に基づいて、「能力」「性能」「変更容易性」「設計状態」の4系統で整理します。この文書自体がプロジェクトの評価軸（`_doc/計画/評価軸.md`）に沿って不足を追跡する生存型文書なので、そこからの要約が最も正確です。

## 1. 能力面の不足（未実装の構成要素）

対象ゲーム像（原神型オープンワールドRPG）の代表シーンを構成要素に分解した結果、以下が未実装です。いずれも「計画から漏れた欠落」ではなく、フェーズ2の各マイルストーン（OW1〜OW5、CHAR0、UI0）に割り当て済みです。

- 地形と植生
- 空と時間帯
- ストリーミング（安定IDだけ先行実装済み、実体の読込・解除は未実装）
- 戦闘エフェクトと敵集団（粒子トイのみ）
- 本番UI（開発用eguiのみ）
- キャラクタークリエイト
- オーディオと剛体物理（長期計画の要求発生時に着手する系統として保留）

## 2. 性能面の不足

- **物量スケーリングが未計測**: 現行レンダラーは長らく1シーン=1描画対象だったため、インスタンス数を1倍・10倍・100倍に変える計測ができない状態でした。これがOW0第4〜5段（先ほどcodexが作業していた内容）で複数描画対象化された理由です。次段でOW4の計測入口を作る必要があります。
- **実表示時刻での60fps安定が未証明**: GPU処理は16.6ms予算の4.4%（0.7276ms）で余裕がある一方、CPU側フレーム開始間隔はp95で33.255ms、p99で34.843msと悪化し、フェンス待機に遅延が集中しています。原因はまだ未特定で、`VK_EXT_present_timing`（GPU提示時刻を取得するVulkan拡張）による計測導入が次の調査対象ですが、採用中のash 0.38がこの拡張を型として提供していないため着手できていません。
- **シミュレーション（SPH: Smoothed Particle Hydrodynamics、粒子ベースの流体計算方式）が物量に弱い**: 全対全探索SPHは512粒子で予算の128.3%を消費しており、空間ハッシュによる近傍探索への切替が前提となっています（SIM0で対応予定）。

## 3. 変更容易性面の既知の構造的負債

文書が明記する既知の負債は2点で、両方ともOW0で対処中です。

- フレーム構成とライト方針がblitz_render（本来はエンジン層が宣言すべき）側にある
- 単一視点・単一描画対象の制約（これがまさに直前のコミット群とcodexの現在作業で解消しようとしている部分）

## 4. 設計状態（将来拡張への閉じ具合）

- 絵の様式に関する3条件（マテリアル型固定なし、TAA未採用、G-buffer固定なし）は「開いている」= 健全
- **大座標対応は部分的**: カメラ・変換は座標系フレームを型で区別済みだが、アセット頂点・スキニングデータは生のf32配列のままで、f64ワールド座標→f32カメラ相対座標への型境界が未実装（OW0で型定義、OW3で実データ適用予定）
- **VR対応は部分的**: フレーム入力が単一ビュー射影のみで、視点数1が前提。本番UIのワールド空間配置も未実装

## 性能アーキテクチャ9軸のうちフェーズ2で判断待ちの項目

- 描画発行: CPUが1メッシュを直接発行する方式のまま。物量計測後にバインドレスディスクリプタ・間接描画・GPUカリングの要否を決める
- パイプライン管理: 永続キャッシュなし。マテリアル多様化後にp99悪化で判断
- 物量とストリーミング: 安定ID以外は未実装。OW1〜OW4が主要対象

---

要するに、現状の最大の不足は「オープンワールドを構成する地形・植生・ストリーミング等の実体機能そのものがまだ何もない」ことと、「複数描画対象・物量スケーリングという、それら全部の前提になる基盤がついさっき（OW0第4〜5段）まで存在しなかった」ことです。codexが今作業している内容は、まさにこの基盤側の不足を埋める作業に当たります。

---

### ユーザー
> 私はキャッチーなワードしか知らないのですが、Unityにはレンダーパイプラインというのがありますね。レンダーグラフを使ってシーンの配置物とかを描画する戦略を決める層だそうですが、認識があってるかどうかは知りませんが、そう聞きました。このリポジトリではワードが出てきませんが、これは必須ではないからでてこないのでしょうか？Unityは巨大ですが、欠陥の多い言語なのでレンダーパイプラインとかも本当にまともな概念なのか疑っています。

### AI
認識はおおむね合っています。ただし言葉が分かれている点を整理すると理解しやすいと思います。

## 幹: レンダーパイプラインとは何の層か

Unityのレンダーパイプライン（Built-in / URP / HDRP / カスタムSRP）が担っているのは、次の2つに分解できます。

1. **仕組み（mechanism）**: GPUリソースの確保・同期バリア・パスの実行という、絵の内容を知らない汎用能力
2. **方針（policy）**: どのパスをどの順で積むか、光源をどう扱うか、シェーディングモデルは何か、という「絵を決める」決定

Unityがこれを1つの層として括り出しているのは、同じゲーム内でもモバイル向け（URP）と高品質向け（HDRP）のように、**下の仕組みは共有しつつ方針だけを差し替える**必要があるからです。方針が仕組みから分離されていないと、絵の様式を変えるたびに同期処理まで書き直すことになります。

## 葉: このリポジトリでの対応関係

このプロジェクトは同じ2層分解をすでに持っていますが、名前が違うのと、片方がまだ完成していません。

- **仕組み = `レンダーグラフ`**（`_doc/設計/レンダーグラフ.md`）。パス宣言、仮想リソース、バリア自動導出、パス実行という、絵の内容を知らない層です。実はUnityも近年（Unity 6のSRP RenderGraph API）で同じ「レンダーグラフ」という語を採用しており、概念としては同一のものです。用語が偶然一致しています。
- **方針 = フレーム構成とライティング定数**。現状は`blitz_render`内の`graph_build`にハードコードされています（`_doc/設計/機能配置の指針.md`の「現状の既知の逸脱」に明記されている負債）。本来は「何を描くか」を知る`blitz_engine`が宣言し、`blitz_render`はそれを実行するだけにすべきところ、まだ分離できていません。

つまり「レンダーパイプライン」という語自体が出てこない理由は、**必須でないから省かれているのではなく、方針層がまだ独立した名前を持てるほど分離されていないから**です。これはOW0（先ほどcodexが作業していた基盤整備）がまさに解消しようとしている対象で、`_doc/計画/エンジンフェーズ1完了報告とフェーズ2計画.md`に「シーン表現の決定と同時に、エンジンがフレーム構成とライトを宣言し、renderはそれを実行する形へ移行する」と記録済みです。

もう1つの理由として、現状は絵の様式が1種類（forward + dynamic rendering、PBR固定）しかなく、切り替えるべき複数パイプラインが存在しません。設計文書には「絵の様式確定までforward経路を維持する」と明記されており、複数パイプラインを選択する必要が出てから初めて方針層を独立させる、という意図的な先送りです（このプロジェクトの「可能性を閉じる決定を早期にしない」という設計方針どおりです）。

## Unityへの懐疑について

レンダーパイプラインという概念自体は、Unity固有の欠陥とは独立した、一般的に妥当な設計パターンです。フォワードとディファード、PBRとトゥーンなど複数の絵作り戦略を1つのエンジンで扱う実装は、ほぼ例外なくこの「仕組みと方針の分離」に行き着きます（Unreal Engineにも同様の層があります）。Unityの言語・スクリプティング周りの評価とレンダーアーキテクチャの評価は別軸の話で、この概念自体を疑う根拠にはならないと考えます。

---

### ユーザー
> Unityにはどういう固有の欠陥があったりしますか？私は遅いということしか知らないです。

### AI
「遅い」という評判の実体は、単純な処理速度の低さというより、**特定の場面で予測不能に処理落ちする**という性質の問題が中心です。原因は大きく4つの層に分かれます。

## 幹: 4つの原因層

1. ランタイムの一時停止（GC: Garbage Collection、不要になったメモリを自動回収する仕組み）
2. 開発中のイテレーション速度（ビルド・コンパイル）
3. アーキテクチャの過渡期状態（GameObject/MonoBehaviourとECS/DOTSの併存）
4. データ形式・APIの互換性

## 葉: それぞれの具体内容

**1. GCによるフレーム落ち**
UnityのゲームロジックはC#（マネージドヒープ、自動メモリ管理）で書きます。`foreach`でのボックス化（値型を参照型として扱うための隠れたヒープ確保）、LINQ（コレクション操作用の関数型API）の内部確保、文字列結合などが毎フレーム発生すると、GCが走った瞬間に数十ms単位でフレームが止まります。これは「常に遅い」のではなく「時々大きく止まる」問題で、体感の悪さの主因です。IL2CPP（C#をC++経由でネイティブコードへ変換するビルド方式）でもGC自体は残ります。

**2. ドメインリロード（スクリプト変更のたびに発生するC#実行環境の全再構築）**
スクリプトを1行変更するたびに、エディタがC#アセンブリを全て再ロードします。大規模プロジェクトでは数十秒かかることがあり、「動作確認1回に数十秒待つ」状態が開発速度を直接削ります。このプロジェクトが評価軸で「ホットリロード1秒未満」を目安に掲げているのと対照的な弱点です。

**3. GameObject/MonoBehaviourとECS/DOTSの併存**
Unityは長年、GameObjectに`MonoBehaviour`をアタッチする方式（内部的にリフレクション経由の`Update()`呼び出しやメッセージ送信を使い、実行時のオーバーヘッドが大きい）を使ってきました。性能問題への回答として後からECS（Entity Component System、データ指向の設計）とDOTS（並列実行基盤）を追加しましたが、最初から設計に組み込まれた仕組みではないため、旧方式と新方式が長期間並存し、「どちらで書くべきか」がプロジェクトごとに割れる状態が続いています。

**4. シリアライズとバージョン互換の弱さ**
Unity標準のシリアライザは`Dictionary`や多態（ポリモーフィズム）を直接扱えず、回避策が常態化しています。また主要バージョン間（LTS間）やパッケージ（URP/HDRP等）のAPI破壊的変更が頻発し、プロジェクトのアップグレードがそれ自体で作業になります。

これらは「言語（C#）が遅い」というより、**GameObjectモデルがリフレクションと動的ディスパッチに依存する設計**であることと、**性能重視の仕組み（ECS/Job System/Burstコンパイラ）が後付けで積まれた**ことが根本原因です。このリポジトリが採っている、最初から型安全・単一所有・静的ディスパッチで設計する方針は、まさにこの種の「後から性能層を継ぎ足す」負債を避けるための選択と言えます。

---

### ユーザー
> あー、まあそれはドメインレイヤーでのプログラミングテクニックである程度回避できるので別にいいんですが、描画レイヤーはどうでしょうか？最高レベルのパフォーマンスではないと思います。

---

### ユーザー
> あー、まあそれはドメインレイヤーでのプログラミングテクニックである程度回避できるので別にいいんですが、描画レイヤーはどうでしょうか？最高レベルのパフォーマンスではないと思います。あとUnityで公開されてるAPIでできる範囲も限られてると思いますが、何ができないでしょうか？既存のオープンワールドで必須な技法とかで。

### AI
Unityの描画層の性質は「特定のグラフィックスAPIを直接使わせない、抽象化された層」であることに起因します。これはDirectX12/Vulkan/Metal/コンソール向けAPIの共通項だけを公開APIとして提供する設計のため、**各GPU APIが個別に持つ低レベル機能へは、その機能がUnity側で明示的にラップされるまで到達できません**。オープンワールドで必要とされる技法の多くは、まさにこの低レベル制御そのものを要求するため、ここが摩擦点になります。

## 葉: 具体的に届かない、または制約が強い技法

**GPU駆動レンダリング（CPUがドローコールを発行せず、GPU側でカリング・LOD選択・間接描画まで完結させる方式）**
Unity 6で「GPU Resident Drawer」という機能が追加され部分的にカバーされましたが、独自エンジンで組む間接描画（`vkCmdDrawIndexedIndirect`のようなAPI）+コンピュートカリングパイプラインほどの粒度制御はありません。長年、大量オブジェクトの可視判定はCPU側ドローコール数がボトルネックになる構造でした。このリポジトリの計画にあるOW4（バインドレスディスクリプタ・間接描画・GPUカリング）は、まさにここを自前で持とうとする部分です。

**バインドレスリソース（多数のテクスチャ・バッファを添字1つで選ぶ方式。ドローコールごとのバインド切替を減らす）**
HDRPの新しめのバージョンで限定的に対応が始まった段階で、URPや旧バージョンでは前提にできません。

**Nanite相当の仮想化ジオメトリ（メッシュを自動でクラスタ分割し、手動LOD作成なしにほぼ無制限の詳細度を扱う方式）**
Unity側には対応する機能がありません。Unreal Engine 5固有の技術で、Unityでは従来どおり手作業のLODGroup（LOD0/1/2を別メッシュとして用意する方式）が前提です。オープンワールドのアセット制作コストに直結します。

**大座標・カメラ相対描画（ワールド原点から遠い場所での浮動小数点精度崩れを防ぐ仕組み）**
Unityの`Transform`は単精度float前提で、原点から離れるほど座標が暴れる問題（floating point precision jitter）が起きます。エンジン側にfloating origin（ワールド原点を動かす仕組み）は組み込まれておらず、開発側で自作する必要があります。このリポジトリがOW3で計画しているf64ワールド座標→f32カメラ相対座標の変換は、Unityでは標準機能として提供されていない領域です。

**地形システム**
Unity標準のTerrainはハイトフィールド（高さマップ）固定で、洞窟や張り出し地形を同一オブジェクトとして扱えません（別メッシュを併用する必要があり、実はこのリポジトリのOW3方針「洞窟や張り出しは通常メッシュとして同じシーンへ載せ、地形形式へ無理に統一しない」と同じ回避策です）。大規模化にも弱く、出荷されたUnity製オープンワールドの多くはTerrainを使わず完全自作か大幅改造しています。

**大規模動的グローバルイルミネーション**
UE5のLumen（完全動的なライティング解決）に相当する、常時変化する広大な世界向けの成熟したリアルタイムGIはUnityにありません。ベイク済みライトマップとAdaptive Probe Volumesの併用が現実解で、地形が動的にストリーミングされる前提とは相性が悪いです。

**非同期コンピュート・マルチキューの明示制御**
グラフィックスキューとコンピュートキューを分けて依存しない処理を並走させる制御は、HDRPで部分的に触れる程度で、独自Vulkanレンダラーのように「どのパスをどのキューに積むか」を宣言的に決める自由度はありません。

**ストリーミングの精密なメモリ予算制御**
Addressablesは資産のロード管理を提供しますが、C#レベルの抽象化でGCとオブジェクトライフサイクルのオーバーヘッドを伴い、チャンク単位の優先度・退避順序をバイト単位で制御するような精密さはありません。

まとめると、Unityの描画層は「複数GPU APIの最大公約数」という設計上の制約を持つため、オープンワールドが要求する技法の多くは「Unityの外」か「Unity内部の未公開実装」の領域に落ちます。出荷済みのUnity製オープンワールドタイトルの多くが地形・ストリーミング・カリングを自前実装で置き換えているのは、この制約が実際に効いている証拠です。このリポジトリがashでVulkanを直接叩く設計を採っているのは、まさにこの種の制約を最初から避ける選択だと言えます。

---

### ユーザー
> このリポジトリのワークフローにおいて欠けているものはありますかね？

### AI
「ワークフロー」は複数の意味を取りうるので、開発プロセス面（xtaskの検証列・CI・自律ループ運用）とツール配置規約の追従状況の両方を確認します。まずグローバル規約で必須とされている追従チェックを行います。

〔ツール実行あり〕

ワークフロー面（`cargo xtask`の検証列・自律ループ運用・コミット規律）を実際のファイルで確認しました。運用規約自体は`_doc/計画/開発計画.md`にかなり作り込まれていますが、機械的強制が効いていない箇所が4つあります。

## 1. フォーマットチェックが検証列に無い

`xtask/src/verify.rs`を読むと、実行順は`conform → check → clippy -D warnings → test`で、**`cargo fmt --check`が入っていません**。`rustfmt.toml`は存在するので整形ルール自体はあるのに、それを守っているかを機械検査する場所がない状態です。過去に「フォーマット設定を適正化しフォーマット実行」というコミットが1回ありますが、以後の差分で整形崩れが混入しても`cargo xtask verify`は検出しません。conformの100行制限・禁止文字列チェックとは別軸の抜けなので、`verify`の手順一覧に1ステップ足すだけで塞げます。

## 2. CI（継続的インテグレーション）が存在しない

`.github/workflows`が無く、検証は完全にローカル実行任せです。現在ローカルはorigin/mainより12コミット先行していますが、push時点で何かを機械検査する仕組みがありません。今は単独開発で自律ループの規律（検証列を通さずコミットしない）に依存していますが、README「最終成果物は基盤の外販も視野」という方針と照らすと、将来複数セッション・複数人が絡む場面で「ローカルで通したはず」を機械的に裏付ける第二の砦が要ります。

## 3. コミット前の機械的強制（pre-commit/pre-push hook）が無い

`.git/hooks`にサンプル以外のフックがありません。「検証列が通らないコードはコミットしない」は運用規約（CLAUDE.md）止まりで、実行を忘れても止まる仕組みがなく、AIセッションの自己規律だけに依存しています。少なくとも軽量な`conform`だけをpre-commitで強制する、という選択肢はあります。

## 4. 性能計測の退行検知が手動

`_doc/計測/`は「追記専用の記録として推移を見る」設計ですが、現状ファイルは`M11_2026-07-23.md`の1件のみで、閾値超過・退行を自動検出する仕組みはありません。今後ベンチ回数が増えたときに「前回よりp99が悪化していないか」を人（AI）が過去ログと目視比較する前提のままだと、評価軸4系統のうち性能系だけ定量トラッキングが弱くなります。

---

優先度としては、**1（fmt検査追加）が最小コストで即塞げる穴**、次に**3（pre-commit）**が自律ループの規律を機械化する意味で価値が高いと考えます。2（CI）と4（退行検知の自動化）は現状の単独開発フェーズでは緊急度が低く、外販フェーズや計測回数が増えた時点で着手すれば十分です。着手するなら、これらもプロジェクトCLAUDE.mdの「ツールとドキュメントの配置規約」に従い`cargo xtask`へ登録してください。

---

### ユーザー
> 作ろうとしてるものが大規模（多数のレイヤーに渡る）なので最終的なアーキテクチャを見据えて実装してるようにはあまり見えないです。いろいろな後から発生する選択肢を不可能にしないようにするという方針はどう解釈されるべきだと思いますか？逆にどう解釈されるべきではないと思いますか？

### AI
良い指摘です。実際に文書を読み込むと、この原則は「アーキテクチャを決めずに進む」という意味ではなく、**どの種類の決定を早期に固定してよく、どの種類を固定してはいけないか**を区別する原則だと分かります。

## 幹: 原則が対象にしているのは「決定一般」ではなく「特定の性質を持つ決定」

開発計画.mdの文言をよく見ると、実際にこのプロジェクトは多くを早期に固定しています。

- `blitz_math → blitz_render → blitz_engine → blitz_app`という4層のクレート分割と依存方向は、Cargoの依存グラフでコンパイル時に強制済み（M0から不変）
- `blitz_engine`/`blitz_app`の`#![forbid(unsafe_code)]`
- 数学DDD（座標系フレーム・単位を幻影型で区別する）の型設計
- マテリアルを判別共用体にする、という型契約

これらは「将来の選択肢を閉じない」原則の対象ではなく、**day 1で確定させ、以後見直さない決定**です。つまり原則は「一切決めるな」ではありません。

対象になっているのは「対象ゲームの内容が決まらないと正しい形が分からない決定」（絵の様式、大座標の実データ、VRの本採用、シーン表現/ECSの形）だけです。この区別が原則の核心だと解釈しています。

## 正しい解釈: 「決めない」のではなく「安い保険を先に張り、高いコミットを遅らせる」

未確定の対象について、原則は**型レベルの逃げ道**を先に用意することを要求しています。例えば:

- 絵の様式が未確定でも、マテリアルを判別共用体にしておけば後からトゥーン調を「割り込ませる」だけで済む（実装を書き直さなくてよい）
- VR採否が未確定でも、視点数を`Vec<視点>`型にして今は要素数1で運用すれば、後から複数視点を足すのに型の作り直しは要らない
- 大座標が未確定でも、ワールド座標とGPU座標を別の型にしておけば、後からカメラ相対変換を挟む場所が最初から用意されている

これらはいずれも**実行時コストゼロ・実装コスト数行〜数十行**で、後回しにした場合の手戻り（全呼び出し箇所の書き直し）と比べて著しく安い保険です。つまり原則の実態は「今すぐ実装はしないが、後から安く差し込める形にだけ最初からしておく」であり、フルスクラッチの汎用化とは別物です。

## 誤った解釈1: 「とにかく汎用的・柔軟に作れ」という意味ではない

これは文書内で明示的に否定されています。`_doc/計画/開発計画.md`の非ゴール節には、汎用ECS・シェーダーグラフ・RHI（マルチAPI抽象層）が並んでおり、理由は「対象ゲーム決定前の汎用化は負債」です。判断基準の優先順位でも「3. 単純さ: 現マイルストーンに必要な最小の実装を選ぶ。将来のための汎用化はしない」が明記され、原則0（扉を閉じない）のすぐ下に位置しています。

これは矛盾ではありません。**汎用フレームワークを先に作ることは、それ自体が「間違った形を早期に固定する」行為**だからです。要求が分からないまま作った抽象化は、後で要求が判明したときに正しい形へ作り直すコストを発生させます。つまり「先に汎用化する」ことも「将来の選択肢を閉じる」原則違反になり得る、というのがこのプロジェクトの立場です。

## 誤った解釈2: 「決定を先延ばしにしてよい」「未完成のまま進めてよい」という意味ではない

各「扉」はプロジェクト内で名前付きの具体的コミットです（判別共用体にする、視点数を型で持つ、等）。曖昧に保留しているのではなく、「何を今やり、何を後回しにするか」を明文化した上で、後回し部分の逃げ道だけを先に作っています。CLAUDE.mdの「半端な実装も禁止」という規律と両立しており、「決めない」ことの言い訳には使えません。

## 誤った解釈3: レイヤー構造そのものを流動的に保つ、という意味ではない

クレート境界・依存方向・unsafe封じ込めは、ゲーム内容が何であっても変わらない制約（Vulkan採用・型安全方針）から導かれるため、対象ゲーム未確定でも正しい形が分かります。だからこそ最初から固定してよく、実際に固定されています。「将来の選択肢を閉じない」の対象は、あくまで**対象ゲームの内容に依存する決定**に限定されます。

---

総括すると、このプロジェクトが「最終アーキテクチャを見据えていないように見える」のは、意図的な非対称投資の結果だと考えます。**内容に依存しない骨格（レイヤー構造・型安全方針）はすでに恒久決定済みで、内容に依存する部分（絵作り・ECS形状・大座標実装）だけが名前付きの安い逃げ道つきで保留されている**という状態です。大規模プロジェクトにありがちな「後で困らないように何でも柔軟にしておく」という素朴な未来対応とは異なり、保留対象を絞り込んでコストを最小化している点が、この原則の運用として妥当だと判断します。

---

### ユーザー
> その観点において、現状の進行はどうでしょうか？

### AI
OW0の現在の進行を、原則の枠組み（恒久決定は保たれているか／扉は開いたままか／未決定を安全に保留できているか）で評価するため、コミット済みのM11監査結果と、codexが今作業中の内容を照合します。

〔ツール実行あり〕

実際のコードを確認すると、恒久決定側は健全に守られている一方、「扉」側は計画どおり段階的に、しかし一部は本来より後回しになっている、という評価になります。

## 恒久決定は破られていない（良い兆候）

複数描画対象という大きな拡張が入った今回のOW0第4〜5段でも、`crates/blitz_render/src/material.rs`の`マテリアル素材`は依然としてenum（判別共用体）のままです。単一メッシュ前提から複数対象前提へレンダラーの内部構造を大きく書き換える圧力がかかった局面でも、絵の様式の扉を閉じる誘惑（マテリアルを特定シェーディングモデルへ焼き付ける）に屈していません。クレート境界・unsafe封じ込めも維持されています。これは原則が実際に効いている証拠だと評価します。

## 2つの扉はまだ「部分的」のまま、今回の作業でも前進していない

M11監査で「部分的」と自己申告されていた2点を確認しました。

- **大座標の扉**: `描画対象素材`（`render_object_material.rs`）は`変換<ローカル, ワールド>`という型で座標系フレームを区別していますが、頂点データはまだ生のf32配列のままです。f64ワールド座標→f32カメラ相対座標の型境界は今回のOW0第4〜5段でも未着手です
- **VRの扉**: `draw_inputs.rs`を含む描画入力周りに視点（viewpoint）関連の型は一切現れません。単一視点のハードコードは今回の複数描画対象化でも解消されていません

これ自体は「悪化」ではなく、M11監査時点の状態が維持されているだけです。ただし当初の計画（開発計画.mdのOW0節）では「フレーム入力を非空の視点一覧にする」ことがOW0のDoDに含まれており、複数描画対象化と同じマイルストーン内での達成が想定されていました。現在の進捗記述（開発スレッドの第5段追記）を見ると、次段は「複数対象を生成する計測入口と2対象以上の実機判定」であり、視点一覧化やフレーム構成のエンジンへの移管はまだ次タスクとして明言されていません。

## 既知の逸脱（フレーム構成・ライト方針がblitz_renderにある）も未解消

`_doc/設計/機能配置の指針.md`が負債として記録している「フレーム構成がblitz_renderのgraph_buildに固定され、ライティング定数もrender内にある」問題を確認したところ、`graph_build.rs`は今回の変更後も引き続きblitz_render内にあります。これもOW0のDoD（「フレーム構成とライト定数がblitz_renderから除かれ」）の一部ですが、まだ着手されていません。

## 評価

原則の運用は表面的な理念倒れになっておらず、型安全な扉（マテリアルのenum性）は大規模な内部変更を経ても実際に保護されています。一方で、OW0というマイルストーン単位で見ると、DoDの一部（視点一覧化・フレーム構成移管）が複数描画対象化より後回しになっており、これは意図的な段階分割の範囲内ではありますが、1点だけ気になる兆候があります。

今回新設されたディスクリプタ確保ロジック（1レイアウト・1プールから「対象数×フレームインフライト数」の積だけセットを割り当てる設計）は、視点の次元をまだ考慮していません。視点一覧化に着手する段階で、この資源配分の計算式に「視点数」という3つ目の次元を追加する修正が必要になる可能性が高いです。これは「扉を開けたままにする」ことに完全に成功しているとは言い切れない部分で、視点次元を最初からパラメータとして持たせておけば避けられた手戻りかもしれません。ただし規模は小さく（掛け算する項を1つ増やす程度）、原則が要求する「安い逃げ道」の範囲を大きく超える手戻りではないと見ています。

要するに、**原則自体は今回の実装でも守られているが、OW0マイルストーンそのものはまだ道半ば**であり、「複数描画対象」という能力面の前進に対して、「視点一覧化」「フレーム構成の所有権移管」という契約面の前進がやや遅れている、という状態です。次にOW0を完了扱いにする前に、この2点が本当に次段のタスクとして明示的にスコープインされているかを確認する価値があります。

---

### ユーザー
> 頂点データはまだ生のf32配列のままとかは本来何であるべきですか？そしてそれは初期から判断できますか？なぜそうなっているのでしょうか？

### AI
## 本来あるべき形

大座標対応の標準的な設計は「カメラ相対描画（floating origin）」と呼ばれる技法で、座標を3段階のフレームに分けます。

1. **ローカル空間の頂点**: モデル原点を基準にした座標。メッシュ自体は数メートル〜数十メートル程度の範囲に収まるため、**f32のままで精度上問題ありません**。精度崩れの原因は「ワールド原点からの絶対距離」であって、モデル内の相対距離ではないからです
2. **ローカル→ワールド変換**: オブジェクトを世界のどこに置くかという並進成分は、原点からkm単位離れると**f64が必要**になります（回転・スケール成分はf32のままで問題ありません）
3. **ワールド→カメラ相対変換**: 毎フレーム、カメラのワールド位置（f64）をオブジェクトのワールド位置（f64）から引き算し、結果の差分だけをf32へ落とします。カメラ近傍の差分は小さい値になるため、ここで初めてf32へ落としても精度が保たれます。GPUへ渡すのはこの変換を通した後の値だけであるべきです

つまり「頂点データがf32である」こと自体は誤りではありません。**欠けているのは、その手前にあるべき「ワールドの並進成分がf64で持たれていること」と「ワールド→カメラ相対への変換点（型と関数）」の2つ**です。M11監査の言う「型境界は未実装」とは、このカメラ相対フレームという第3の型と、そこへ落とす専用の変換関数がまだ存在しないことを指しています。

## 初期から判断できるか

できます。この技法自体はゲーム内容にもECSの形にも絵の様式にも依存しない、確立した工学的知識です。実際、開発計画.mdでは2026-07-20の壁打ち時点で「大座標の扉（オープンワールド固有の第4の扉）」として名指しされ、「ワールド座標の型とGPUへ渡す座標の型を最初から区別する」ことが明記されています。つまり**必要性の判断自体はプロジェクト開始日にすでに済んでいます**。前回説明した「原則が対象にする、内容依存の未決定」には該当せず、むしろ内容非依存の既知の工学的要求です。

## なぜ今この状態なのか

M2マイルストーンで座標系フレーム型（ローカル/ワールド/ビュー/クリップの幻影型）自体は導入済みですが、内部実装はglamベースのf32のままで統一されています。これは数学DDDの設計方針（glamは「計算基盤としてのみ」採用し、ドメイン層は幻影型の薄い包み）を踏まえると、**型の外形（フレームを区別する殻）だけを先に用意し、中身の精度は後から差し替える**という意図的な順序だと解釈できます。呼び出し側は殻越しにしか触れないため、後でワールド側の内部表現をf64へ差し替えても、呼び出し側の書き換えは最小で済む設計になっています。

加えて、M0〜M11で使ってきた検証シーン（DamagedHelmet、Fox）はいずれも原点近傍の小さいシーンで、km級の距離が一度も発生していません。f64境界を今作っても、それを踏み外して壊れることを検出するテストが書けない（実際に精度崩れを起こすデータが存在しない）状態でした。判断基準3「現マイルストーンに必要な最小の実装を選ぶ」に照らすと、検証しようのない配管を先に作るのは「将来のための汎用化」に当たり、避けるべき対象です。そのため計画上は「OW0で境界型を定義し、OW3でkm級の実データへ適用する」と、実際にkm級データが登場するマイルストーンまで意図的に遅延されています。

一方で、**頂点・スキニングデータが既存のローカル/ワールド幻影型にすら包まれず生配列のままである点**は、上記のf64精度の話とは別問題です。これはkm級データを待たなくても、既存の型機構を適用するだけで今すぐ閉じられる欠落に見えます。ここだけは「安く閉じられる扉をまだ閉じていない」状態であり、次にOW0で境界型を定義する際に、精度をf64にする作業と同時にではなく、先に単独で片付けられる余地があると考えます。

---

### ユーザー
> Vulkanを使うのはとても難しいと聞きます、フェンスとかいう概念とかが難しいと聞きます。また、一方で描画パフォーマンスについてVulkanの使い方はどういう違いがどういう差を生むのでしょうか？そしてこれらの観点においてこのリポジトリは現状Vulkanの使い方はどうでしょうか？そして難所は超えているのでしょうか？まだまだこれからどんどんVulkanの難所はでてくるのでしょうか？

### AI
## 幹: Vulkanが難しい理由は「ドライバがやっていたことをアプリ側が肩代わりする」設計だから

OpenGLやDirectX11では、リソースの依存関係追跡・同期・メモリ確保・パイプライン状態の切り替えをドライバが暗黙に処理していました。Vulkanはこれを全部アプリ側の明示的責任にします。難しさの正体は「概念が難解」というより、**間違えても動いてしまう（validation layerを有効にしないと気づけない）失敗モードが大量にある**ことです。

主な明示化ポイントは4つです。

1. **同期の明示化**: GPU内のパス間依存（バリア）、GPU-GPU間の順序（セマフォ）、CPU-GPU間の完了通知（フェンス）を全部自分で宣言する
2. **メモリの明示化**: `VkBuffer`/`VkImage`（論理的な入れ物）と実メモリ（`vkAllocateMemory`で確保する裏付け）が分離されており、バインドも別操作
3. **リソース束縛の明示化**: ディスクリプタ（シェーダーへのテクスチャ・バッファの結び付け）をプール・レイアウトから自分で設計する
4. **パイプライン状態の明示化**: ブレンド・深度・頂点入力・シェーダー段を1つの不変オブジェクト（`VkPipeline`）として事前に固める

## フェンスが難しい理由

フェンスはCPU↔GPU間の同期に特化した仕組みです。難しいのは「概念」より「**待つタイミングを間違えたときに、動きはするが遅いだけのバグになる**」点です。

- 早すぎる待機（提出直後に待つ）: CPUがGPUの完了を待つだけの状態になり、本来非同期に進められるはずのCPU作業まで止まる。動作は正しいが性能が壊滅する
- 遅すぎる/待たない: GPUがまだ読んでいるリソースをCPUが上書きし、データ破損や未定義動作になる。validation layerでも検出できないことがある
- フレームインフライト（CPU/GPU並行実行のためのリソース多重化）と組み合わせないと成立しない。「何フレーム分バッファを持ち、何フレーム前の完了を待つか」という設計判断が絡む

正しさ（validation エラーなし）とパフォーマンス（GPU稼働率）は別の合格ラインで、フェンスは**両方に同時に効く**ため難所として名指しされやすい部分です。

## 使い方の違いが生む性能差

| 使い方の要素 | 悪い使い方が生む結果 |
|---|---|
| バリアの粒度 | 過剰に広いバリア（全ステージ一括同期）はGPU内部の並列実行を無駄に潰す |
| フェンス待機のタイミング | 待ちすぎるとCPU/GPUの重なりが消え、スループットがシリアル実行並みに落ちる |
| ディスクリプタ設計 | オブジェクトごとに個別セットを毎回束縛すると、対象数に比例してCPU側の発行コストが増える。バインドレス（添字1つで多数の資源を選ぶ方式）ならこの比例増加が消える |
| 描画発行方式 | CPUがオブジェクトごとに`vkCmdDrawIndexed`を個別発行すると対象数に比例してCPUコストが増える。GPU側で描画コマンドを生成する間接描画に切り替えるとCPU律速が消える |
| メモリ確保方式 | `vkAllocateMemory`をリソースごとに個別発行すると、OS・ドライバの確保数上限に当たり、断片化も起きる。大きなブロックからサブアロケーションする方式で回避する |
| キュー使用 | グラフィックスキュー1本に全部積むと、本来独立できる計算処理まで直列化される。非同期コンピュートキューを使えば真に並行実行できる |
| パイプライン生成タイミング | 初出のマテリアル組み合わせをフレーム中に生成すると、シェーダーコンパイルで数msの一時停止（ヒッチ）が起きる |

## このリポジトリの現状

**バリア（最難所の1つ）は構造的に解決済み**です。M0〜M4で手書きバリアを実際に書いた経験から、M5でレンダーグラフを導入し、「パスが宣言したリソース用途からバリアを機械導出する」設計にしました。人手のstage/accessマスク選定ミスという、まさにM1・M3で実際に踏んだ種類のバグを構造的に消しています。これはVulkan難所の中でも最も踏み外しやすい部分を機構で潰した、と評価できます。

**フェンスまわりは「正しいが遅い」状態が今まさに現れています。** M11の計測（先ほどの評価軸文書）で、CPU側フレーム間隔のp99遅延33.5772msが**フェンス待機に集中**していることが分かっています。フレームデータ準備・記録・提示はいずれもp99 1.5ms未満で律速していません。これは概念的な話ではなく、このリポジトリで今まさに発生している未解決事象で、まさにご質問の「フェンスの難しさ」が実測データとして出ている状態です。原因はまだ特定できておらず、フレームインフライト数を変えても再現しなかったと記録されています。上で述べた「待ちすぎ」パターンが疑わしいですが、確定はしていません。

**メモリ確保は集約・計測はできたが、サブアロケーションはまだ**です。OW0第1〜2段で`vkAllocateMemory`の呼び出し窓口を1箇所へ統一し、確保数・上限比・用途別バイト数を追跡する台帳を作りました。ただしこれは「見える化」であり、確保数上限に当たるリスク自体（1リソース1確保のまま）はまだ残っています。物量が増えたときに顕在化する可能性があります。

**ディスクリプタは改善したが、まだバインドレスではありません。** 先ほど確認した通り、対象ごとに個別プールを作る旧方式から「1レイアウト・1プールから対象数×フレームインフライト数のセットを割り当てる」方式へ変わりましたが、依然として対象ごとに個別セットを束縛する設計です。対象数に比例するCPU側コストはまだ残っています。

**描画発行はCPU直接発行のまま、キューはグラフィックス1本のまま、パイプラインは起動時生成で永続キャッシュなし**です。これらは計測前の推測最適化を避けるという判断基準4に従い、意図的に未着手のまま据え置かれています。

## 難所は超えたか

**最も踏み外しやすい「同期の正しさ」は超えています。** レンダーグラフによる機械導出は、Vulkan初学者が最も苦しむ部分（バリアのstage/accessマスク選定）を人間の判断から切り離しました。

**一方で「性能を出すための使い方」の難所はこれからが本番です。** フェンス待機の遅延原因はまだ未解明で、これは今まさに立ちはだかっている壁です。加えて、以下は計画上まだ手つかずで、今後着手時に新しい種類の難所が出てきます。

- **非同期コンピュート（SIM0）**: 異なるキュー間の同期はバリアだけでは足りず、セマフォによる順序付けと、リソースの所有権をキュー間で明示的に受け渡す操作が必要になります。Vulkanの中でも屈指の難所とされる領域です
- **間接描画・GPU駆動カリング（OW4）**: GPU側が生成した描画コマンドバッファを、次のパスが正しく読めるよう同期する新しいバリアパターンが必要になり、レンダーグラフの語彙拡張が要ります
- **サブアロケーション（OW4次第）**: 自作するなら、アラインメント・断片化・バッファ/イメージ間の確保粒度制約という新しい正しさの領域に入ります
- **`VK_EXT_present_timing`**（実表示時刻計測）: 採用中のash 0.38が未対応で、拡張のFFI呼び出しを自分で足すか、ashの更新を待つ必要があります

総括すると、**「動くVulkan」の最難所（同期の正しさ）はレンダーグラフで超えていますが、「速いVulkan」の最難所（フェンス待機・非同期実行・大量描画時のCPU/GPUオーバーヘッド）はまだ手前**というのが現状です。フェンス待機の原因調査は、次に着手すべき最も差し迫った未解決課題だと考えます。

---

### ユーザー
> 私が心配している点は、現在のリポジトリはドキュメントレベルで今抱えている問題を認識して適切に言語化されているかどうかですが、その点どうでしょうか？ポジティブな面とネガティブな面両方教えて。

### AI
実際に文書を読み込んで気づいた点は、**このプロジェクトの言語化能力は総じて高いが、その厳密さが領域によって非対称**という点です。具体例で示します。

## ポジティブな面

**負の結果を数値付きで正直に書いている。** M11報告の結論部は「GPU処理は予算の4.4%で余裕がある」という良い数字のすぐ後に「実表示時刻に基づく60fps安定は証明できていない」と明記し、p50 16.7ms・p95 33.3ms・25ms超過率8.75%という具体的な悪い数字まで併記しています。フレームインフライト数を変えて改善を試みたが再現しなかった、という**失敗した対策**まで記録されているのは、都合の悪い情報を丸める傾向が無いことの証拠です。

**「不明」と「計画漏れ」を区別する機構がある。** 評価軸.mdの出自分類（未実装要素が(a)予定通り(b)意図的後回し(c)計画から漏れた真の欠落、のどれかを毎回判定する）は、単なる進捗リストではなく、**見落としそのものを検出する自己点検の手順**になっています。実際に2026-07-23時点で(c)は検出されなかったと明記されており、判定した形跡が残っています。

**負債が名指しで管理されている。** 機能配置の指針.mdの「現状の既知の逸脱」は、フレーム構成とライト方針がblitz_render内にあるという設計違反を、隠さず節を割いて明記し、外部の追跡札（#138）とも紐付けています。私がコードを読んで確認した限り、この負債は今も実在しており、文書の記述と一致していました。

**判断根拠が残る。** 開発スレッドの判断75のような記述は、結論だけでなく「なぜその設計にしたか」を残しており、後から読む人（次のセッションの私を含む）が意図を再構築できます。

## ネガティブな面

**DoDと進捗記述が構造的に照合されていない。** OW0節はDoD（箇条書き）と「進捗」（自由文）が別々に書かれており、両者を突き合わせるチェックリストがありません。実際に私がコードを直接読むまで、「複数描画対象化は進んだが、フレーム構成のエンジンへの移管と視点一覧化という、同じOW0のDoDに含まれる2項目はまだ手つかず」という事実は文書からは明示的に読み取れませんでした。進捗欄の文章は「次段は複数対象の計測入口と実機判定」としか書いておらず、この2項目が次段のスコープに入っているのかいないのか判別できません。これは今回、私が文書だけでなくコードを直接確認しないと気づけなかった実例です。

**1つの文に異なる性質の課題を混在させている箇所がある。** 「アセット頂点とスキニングデータは生のf32配列であり、f64ワールド座標からf32カメラ相対GPU座標へ変換する型境界は未実装である」という一文は、実際には「型で包まれていない」（今すぐ安く直せる）と「f64精度がない」（OW3まで妥当に遅延できる）という性質の違う2つの課題を1文に圧縮しています。この区別は先ほど私が構造体定義を直接読むまで分かりませんでした。

**運用・ツール面の負債を監査する仕組みが存在しない。** 「既知の逸脱」節はアーキテクチャ負債（フレーム構成の所在等）は監査していますが、CI不在・fmtチェック未実装・pre-commit不在といった開発プロセス側の負債は、どの文書にも記載がありません。前回私が実際にリポジトリ構造を調べて初めて判明した内容で、対応する「既知の逸脱」の枠組みがアーキテクチャ面にしか及んでおらず、運用面には及んでいないという非対称性があります。

**最重要の未解決課題への次の一手が、記述の精度に対して曖昧。** フェンス待機への遅延集中は、60fps安定を妨げる唯一にして最大の未解決要因ですが、次の一手は「イベントループとFIFO提示待ちを次の調査対象とする」という一文だけで、他の記述で見られる仮説の優先順位付けや具体的な実験計画までは踏み込んでいません。発見の重要度に対して、対応計画の具体性が見合っていない箇所だと感じます。

## 総括

自己監査の**仕組み自体**（出自分類・既知の逸脱・数値付き負の結果の明記）は他のプロジェクトではあまり見ない水準で整っています。ただし、その仕組みがカバーする範囲は**アーキテクチャ・能力の負債に偏っており**、DoDと実進捗のクロスチェック、運用・ツール面の負債、および複合的な課題の要素分解には及んでいません。今回私が見つけたネガティブな点は、いずれも「文書を読むだけでは分からず、コードを直接確認して初めて判明した」ものである点自体が、このギャップの性質を物語っていると思います。

---

### ユーザー
> CI不在・fmtチェック未実装・pre-commit不在などはそんなに重要でしょうか？これは実行粒度を考えないとAIの開発速度に悪影響をもたらすと思います。なぜならこれらは細かなヒューマンエラーを防止する形式的な要素だと思いますが、今のAIは形式的な問題はコード規約が極めて忠実に守られているのでほぼ問題ないと思うからです。100てんではないものの95点くらいはあります。と言っても全くやらなくていいというわけではないです。次に、「DoDと実進捗のクロスチェック、運用・ツール面の負債、および複合的な課題の要素分解には及んでいません」は妥当だと思いました。こちらは本質的な問題を認識し解決できる能力がリポジトリにあるかという問だと思います。しかしこちらも高頻度でやればいいというわけではないと思います。結局のところ、大量の開発を進めている中で、「ん？」とにおいを感じ取ったときにとりあえず起票して起き、適切なタイミングで対応されるべきものもしくは並行して別のエージェントに委譲されて解決されるべきものにも見えます。どれも「負債」であるということに変らず負債をどういう単位で認識・対処するかという話だと思います。コードに関しては100行チェックと厳しいddd制約によって常に負債が返されるようになっていると思いますが、どのようなルールを持てば他もすなおに回るでしょうか？

### AI
CI/fmt/pre-commitへの指摘は妥当だと思います。検証列がループの必須手順に組み込まれている以上、CIが主に守るのは「人間のうっかり」であって、そこはAI駆動開発では前提が違います。設計ルールの提案に入る前に、既存のjimbo札（fudaba）が実際どういう粒度で運用されているかを見てから、コード層の100行チェックに相当する規律を他層にどう適用するか答えます。

〔ツール実行あり〕

## CI/fmt/pre-commitの優先度は訂正します

ご指摘の通りです。検証列(`cargo xtask verify`)は自律ループの必須手順（開発計画.md「1イテレーションの手順」の3番目）として組み込まれており、「人間がうっかり実行を忘れる」というCIが本来守るべきリスクは、このループでは構造的に発生しません。CIの限界的価値は、単一ループが順守されている前提が崩れる場面、つまり**並行エージェント運用**（実際に今回、codexとClaudeが同じリポジトリを同時に触っています）に主に残ります。fmtチェックは追加コストがほぼゼロなので入れて損はありませんが、優先度は当初の説明より低く見直します。

## 負債を2種類に分けて考える、という整理は正しいです

コード層の100行制限・DDD制約が「常に負債を返す」ように機能しているのは、次の3条件を同時に満たしているからです。

1. 検証列で**毎イテレーション**機械的に強制される（覚えている必要がない）
2. 違反が**二値判定**で安く検査できる（行数・禁止文字列など、意味理解を要さない）
3. 発生した**その場**で捕まる（後からのまとめて監査ではない）

この3条件を満たせない負債（文書と実装のずれ、DoDと進捗記述の整合、運用面の欠落のような判断が要るもの）に、同じ頻度・同じ強制力を求めるのは筋違いです。ご提案の「気づいた瞬間に起票、適切なタイミングで対応か並行委譲」は、この種の負債に対する正しい粒度だと思います。

## 実はこの仕組み、すでに存在していました

このリポジトリのjimbo札を確認したところ、**札#138「Blitzdrache0 既知の未検証事項・積み残しissue」がまさにこの役割で作られています**。中身は「今は対処しない」と判断した項目を番号付きで並べ、各項目に対処条件（例:「M11の完了検証で棚卸し」「M2の数学DDD値オブジェクト導入時から」）を添える形式で、新規発見・解消は**コメント追記だけ**で済むよう設計されています。まさに「起票コストを最小化し、対処タイミングを後から決める」という、ご提案の形そのものです。

ところが確認すると、**この札は2026-07-20の作成以来、一度もコメント追記されていません**（作成時刻と更新時刻が完全一致）。その間にM7〜M11の5マイルストーンが完了し、M11監査だけでも「CPU側フレーム間隔がフェンス待機に集中する未解明の遅延」「大座標・VR扉が部分開のまま」「フレーム構成がblitz_renderに残る既知の逸脱」という、明らかにこの札に転記されるべき発見が複数出ています。今回の会話で私が見つけた4点（DoDと進捗記述の不一致、頂点データの複合課題、運用面の負債、フェンス待機への対応計画の粒度不足）も同様に未転記です。

つまり**仕組み自体は正しく設計されていたが、それを使う習慣が根付いていない**というのが実態です。これは「マイルストーン完了時の定点」を制定した2026-07-20の動機（「文書に書くだけでは後続セッションが実行する保証にならない」）と同じ失敗パターンが、1つ上の階層（負債台帳そのもの）で再発している状態だと見ています。

## 提案するルール

新しい仕組みは作らず、既存の2つの機構を1本のリンクで繋ぐだけで十分だと考えます。

**起票ルール（低頻度でなく、気づいた都度・低コスト）**: 通常の実装・レビュー・調査の副産物として、当初のタスクと無関係な疑問・矛盾・欠落に気づいたら、その場で1〜2文を札#138へコメント追記する。深掘り・優先度判定・解決はその場でしない。起票コストを上げると先送りされて忘れられるため、コストは常に最小に保つ。

**棚卸しルール（新設サイクルでなく、既存の定点に統合）**: 「マイルストーン完了時の定点」の4項目に5番目を追加する。「5. 札#138の棚卸し: 新規追記項目を、このマイルストーンで解消/次マイルストーンへスコープイン/対処条件付きで保留、のいずれかへ分類する」。高頻度の専用監査は作らず、すでに義務化されている完了報告のタイミングに相乗りさせます。

**委譲ルール**: 棚卸しで見つかった項目のうち、他のタスクに依存せず単独で調査できるものは、Agentツールで並行委譲してよい。

**正本の一本化**: `_doc/設計/機能配置の指針.md`の「既知の逸脱」のように文書内に負債節を作る場合も、可能な限り札番号を紐付けて正本を1つに保つ。現状この逸脱は文書だけで管理され#138とリンクしておらず、負債の記録場所が知らぬ間に2箇所に分裂するリスクがあります。

この提案は開発計画.mdへの正式な追記としてはまだ行っていません。合意いただければ反映します。あわせて、今回の整理と札#138が滞留している事実は、次のセッションでも参照できるようメモリに記録しました。

---

### ユーザー
> とても有意義な会話だったので、/開発スレッドで一言一句残さず「ユーザー質問会スレッド」として残してほしいです。要約ではなく一言一句お互いの言葉をそのまま残してください。

### AI
逐語収録には、この汎用スキルではなく専用の`dev-thread-gen`（セッショントランスクリプトから逐語抽出する）スキルを使う方が適切です。
