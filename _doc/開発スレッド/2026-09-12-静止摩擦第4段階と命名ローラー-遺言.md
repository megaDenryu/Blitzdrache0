# 遺言(後記): 2026-09-12 静止摩擦の第4段階(#59/#103)・改名の裁定・命名ローラー全域

逐語は経路B(オーナーの /export)で `_doc/開発スレッド/2026-09-12-静止摩擦第4段階と命名ローラー.txt` へ保存する(セッション https://claude.ai/code/session_019oWdS6zjkExGAqVg6PLtHa)。本ファイルはその後記であり、次のセッションはこれと GitHub の issue/PR だけで次の1手を打てる。

## 1. 確定した到達点

- **PR #102(#59 第4段階の段階C、候補(i'))はマージ済み**(main `db815090`)。接線の行の右辺から δ を追い出し、|C_t| ≤ δ の点の行を積まず、混合連立方程式の受理の判定を退避の枝の前に置いた。27組・n = 16 の3方向・平行移動の分類が δ の倍率に依らず一致。落ちる試験は反証(d)(`slope_far_translation_tests`、#103 所有)と #98 の2件だけ
- **Issue #101**(坂の上の2段の箱が倍率と右辺の形に依らず滑走。転倒は除外済み、円錐を最初に超えるのは床と下の箱の対の細分2)を起票。**#99** へ B1 の破れの2群と 0.48 の後退の機構を追記
- **命名ローラー(全域、sonnet の命名チェック員)**: 167区画(5364ファイル)のうち167区画すべての報告を受領。違反 672件・疑い 333件。報告の全文は `_doc/検査報告/命名_区画NNN_2026-09-12.md`、台帳は `_doc/検査報告/命名ローラー_台帳_2026-09-12.tsv`、区画の一覧は `命名ローラー_区画一覧_2026-09-12.tsv`。時点は main `db815090`。全区画が台帳に記載済み
- **体制**: 命名チェック員を haiku から sonnet(medium)へ上げ、オーナーが不合格にした3件(当て方・連立・右辺を零にする)を反例として定義へ固定(`bda96de8`、PR #104 に同梱)。~/.claude の CLAUDE.md も追随してコミット済み

## 2. 進行中の作業(最重要)

- **PR #104(リネーム)**: `rename/tolerance-application-rule`(先端 `7361b808`)。「許容差の当て方」→「許容差の適用規則」、列挙の枝3つ(本番の枝は `許容差以下の接線変位の拘束行を追加しない`)、「連立」→「連立方程式」、整形2件、命名チェック員の体制。検証は既知2件の失敗のみ。**オーナーの受理待ち**。マージ後に `C:/devs/Blitzdrache0_tol` の `diag/pseudoinverse-acceptance` を main へ載せ直す(競合は識別子の改名なので LSP の rename で追随)
- **Issue #103 段階A4の計測**(物理実装員 `a984231b696cb9823`、ブランチ `diag/pseudoinverse-acceptance`、作業ツリー `C:/devs/Blitzdrache0_tol`): 計器6本は終了コード0(先端 `ed1bfbe1`→見出し是正 `f3e57022`)。実装員は記録 `_doc/計測/剛体の接触の静止摩擦の許容差依存の診断_2026-09-09.md` の「Issue #103 の段階A4」へ表50〜を書いている途中。**委譲の要点**(再委譲するならこれで足りる): (a) 箱10段の塔の本番の行ごとの δ_i の実測(副官の予測: 接線 6e-8、法線 1.2e-7〜9.5e-7)、(b) 同じ塔での候補(ζ)の式(`contact_projection_row/row_gradient/query_origin_tolerance.rs`)の δ_i、(c) 候補(θ')=(ζ)の式を擬似逆の右辺の判定だけへ届かせる枝の遠方0.58 × 受理0.5/1/2倍(表43と同じ形)と、細分64の破局的な向き(|β| = 5.10e-8)との余裕、(d) 同じ(θ')の塔の休止(表48と同じ形)、(e) 回し45度で(θ')の δ が雑音の床 8.0e-7 の下へ入るか。本番は変えない。識別子に英字・記号を入れない。`[f32;3]` を宣言しない

## 3. 次の一手(依存順)

1. 段階A4の報告を受け取り、副官(`general-purpose`, opus, `.claude/agents/副官.md` を読ませる)へ「δ の式の裁定の案」の反論を求め、第2段の裁定文を #103 へ投稿する(第1段は投稿済み: https://github.com/megaDenryu/Blitzdrache0/issues/103#issuecomment-5645633112。δ を3語へ切り直し倍率は混合連立方程式の受理だけへ・(η)を外す・帯は保留)
2. 命名ローラーの締め: (i) `_doc/検査報告/` の報告167件と台帳は本遺言のコミットで main に入っている、(ii) `_doc/計画/検査の網羅台帳.md` へ命名の軸の表を追記(時点 `db815090`・sonnet・2026-09-12・区画は台帳を参照)、(iii) 親が違反を吟味し、種類ごとに是正の issue を切る。種類: ASCIIモジュール直下の裸の動詞(`create::生成する`型。最多)/ 英字添字(`剛体a`・`x開始`・`p95`)/ 未翻訳の英単語と頭字語(`pipeline`・`layout`・`validation`・`LOD`・`Sph`)/ 処理なのに名詞句・真偽値の「か」抜け / 1語1義(`引く`→`参照する`、「束」「空」「バインド」の衝突)。是正はリネーム員で領域ごとに別コミット。**当て方・連立・右辺を零にするの指摘(区画118〜125・150)は PR #104 で是正済みなので除く**
3. PR #104 のマージ後、`diag/pseudoinverse-acceptance` を main へ載せ直す
4. #101(2段の箱)の一次計測の続き(着手可、独立)。#99/#100/#98 は依存待ち

## 4. 未決着の事項

- #103 第1段の裁定(3件)。第2段(δ の式)は A4 の結果待ち。副官の警告: (θ')の坂の δ_k = 5.16e-8 は破局的な向きの |β| = 5.10e-8 の1.012倍で余裕1%。回し45度の雑音の床 8.0e-7 を(θ')の式は説明できない(姿勢の項が無い)
- 正本 判断13 の矛盾する2文(本文「δ_i はその行の右辺が持つ丸め」と細目「行の右辺の下限は丸めの誤差の上限ではなく解けたと見なす許容差」)のどちらを正本とするか
- 命名ローラーの疑い329件の吟味(親の裁定): 「インテント」「ミリ秒の `ms`」「`x()/y()/z()` の成分アクセサ」「PBR/HDR/UI/GPU の大小文字」は全域の慣行として一括で決める

## 5. 地雷と環境制約

- 作業ツリー: 本体 `C:/devs/Blitzdrache0`(いま `rename/tolerance-application-rule` を checkout 中。main は `db815090`)、`C:/devs/Blitzdrache0_tol`(#103 の診断ブランチ)、`C:/devs/Blitzdrache0_roller`(main の detached、ローラーの読み取り用。本遺言と報告をここから branch `docs/thread-2026-09-12` で main へ載せた)、`C:/devs/Blitzdrache0_build`(ビルド員専用)
- ビルド員 `ab2ebf108dc704794` が常駐(実装員が SendMessage で依頼)。実装員は cargo(fmt 除く)を走らせない
- **サブエージェントのコンテキスト残量は親から見えない。** 完了報告の累計トークンが50万を超えた体へ追加作業を頼まない(リネーム員が84万で詰まりかけた)。段階ごとに新しい体へ
- conform の頻出の落とし方: 識別子への ASCII 英字(`A2`・`Σλ_n`)、`[f32;3]` の宣言、型の分量の台帳(`xtask/src/conform/type_metrics_ledger/`)の未更新、改名で伸びた行の100行超
- 段階Cの計器のログは `C:/devs/Blitzdrache0_build/target/verify-logs/gauge_*.log`
- sonnet のレート制限で Bash/Agent の分類器が一時的に落ちることがある(読み取りは可)。待って再試行

## 6. 学びと注意

- 親の見立てが副官に3回覆された: (1)2段の箱の転倒(幾何は正しかったが原因ではなかった。測ってから断定)、(2)候補(η)は症状の向きに届かない(算術で予測できた)、(3)(θ)の 2.66e-7 は塔の δ_t を「上げて」いた(接触点の座標から δ を積算すれば分かった)。**式で予測できることを測る前に、まず式で予測する**
- 「当て方」「連立」「右辺を零にする」は haiku の命名検査で違反0件で通っていた。判断が要る受理条件は sonnet 以上
- 裁定は「式に依らないもの」と「測ってから決めるもの」に分ける。測っていない候補を推奨として出さない
- 委譲プロンプトに「識別子に英字・記号を入れない」「[f32;3] を宣言しない」「台帳を更新する」を毎回書く。書かないと conform で2往復増える

## 7. 参照先

- Issues: #59(依存待ち)、#103(裁定待ち)、#101(着手可)、#99/#100/#98(依存待ち)。PR #104(受理待ち)
- 記録: `_doc/計測/剛体の接触の静止摩擦の許容差依存の診断_2026-09-09.md`(表1〜49、A4 で表50〜)
- 正本: `_doc/設計/剛体の状態と接触.md` 判断12・13・19
- 工程: `.claude/skills/物理基盤の工程/SKILL.md`、`.claude/skills/ローラー検査/SKILL.md`
- 親の現在地の覚え書き: `_doc/検査報告/命名ローラー_親の現在地_2026-09-12.md`
