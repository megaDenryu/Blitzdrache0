# 開発スレッド 2026-09-22 PR #177 の第1回と第2回のレビューへの是正

会話の逐語は `_doc/開発スレッド/端末ログ/2026-09-22_PR177の第1回と第2回のレビューへの是正.txt` にある。
本ファイルは、その会話が1つのコンテキストに収まらなくなったため、次のセッションへ引き継ぐ事実だけを書く。

前のセッションの引き継ぎは `_doc/開発スレッド/開発スレッド_2026-09-22_設計オントロジーの検証層とPR177.md` にある。**検証の作法と作業ツリーの規律はそちらが正本であり、本ファイルはそれを前提に書く。**

## 現在地

ブランチ `feat/issue-170-design-relation-proposition`、先端 `820350f6`(押し上げ済み)。PR #177 は**第3回のレビューを受け、まだマージ不可**である。

| 回 | レビューのID | 判定 | 親の是正 |
|---|---|---|---|
| 第1回 | 5271407031 | マージ不可。Blocker 9件と追加1件 | すべて是正して統合した(先端 `046cffc6`) |
| 第2回 | 5273016074 | マージ不可。Blocker A〜EとMajor F | すべて是正して統合した(先端 `820350f6`) |
| 第3回 | 5277227884 | **マージ不可。Blocker 1〜5** | **未着手。次のセッションがここから始める** |

レビューの全文は `gh api repos/megaDenryu/Blitzdrache0/pulls/177/reviews/<ID> -q '.body'` で読む。

**次のセッションは会話の逐語を読み返す必要が無い。** 第3回のレビューの本文と、PR #177 の本文と、下の「次のセッションが引き継ぐ判断」を読めば足りる。

## 第3回のレビューが挙げたBlocker 5件

レビューは5件すべてを「偽の証明済みに到達できる」としており、根を1つに集約している。

> 「有限全数」「Relation不在」「Concept identity」を名乗るための閉世界の境界

| # | 要旨 | 触る場所 |
|---|---|---|
| 1 | 量化の母集団の重複を畳む鍵が識別子だけであり、**種類の違う概念を母集団から消せる**。消された概念は原子の評価まで到達しないため、既存の欠落の弱めが届かない | `verifier/structural/target_set.rs` の `重複を畳む` |
| 2 | `遷移の識別子` を構造化したのに、**命題の項へ写す境界で `.表記()` へ潰している**。表記は種類も名前空間も含まないため、別の遷移が同じ参照になりうる | `transition_model/transition_identifier.rs`・失敗の節点を組む工程 |
| 3 | **閉世界の境界が半分だけ開いている。** マーカーの実装と上位トレイトと役割は全クレートを読み、`保持する` だけ2クレートである。`blitz_sim` には既にマーカーの実装が実在するため、`NOT (A 保持する B)` を偽に証明できる | `ontology_scope.rs` と5つの抽出の規則 |
| 4 | 型名の解決が、読めない取り込みを**リポジトリ内で唯一の同名の型へ推測**して偽の辺を作る。glob の取り込みを解決していない | `type_definition.rs`・`use_resolution.rs` |
| 5 | 役割の正規形の条件4が、**トレイトの実装まで受理する**。`impl 所有者Trait for 入力型` が `impl 所有者Trait` で始まると判定される | `wrapped_function_path.rs` の `実装の行が名指す対象か` |

**レビューはBlocker 3について2案を示し、どちらかを選べと裁定している。** (A)2クレートの実験を維持し、scopeを5つか6つの抽出の規則すべてへ同じように掛ける。(B)全クレートを対象にし、その範囲の正規形を `conform` で保証する。**「節点は全クレート、`保持する` だけ2クレート」は却下されている。** あわせて `cfg(test)` を閉世界の境界へ含めるかも決める必要がある(いまは試験の型がグラフへ入っている)。

レビューは追加の回帰試験を9件挙げている(前回の12件に加えて)。

**PR本文の整合も直すよう求められている。** 第1回の後の記述と第2回の是正後の記述が同居し、現在の状態と矛盾する箇所がある(代表例はBlocker 7節の「親は、遷移パラメータと失敗を触っていない」であり、いまは両方実装済みである)。**現在の設計・現在閉じているもの・現在閉じていないものへ整理し直すこと。**

## 第3回のレビューが「閉じた」と判定したもの

次は追加の差し戻しが無い。次のセッションはここへ手を入れない。

空の連言と選言と0前提の推論規則 / 3値 / 抽出の欠落が不在の証拠へ反転する経路 / 役割の右辺を実処理へ結ぶ方針 / `M遷移関数` の6つの写し / 初期状態の数え上げ / 状態の同一性の畳みの検出 / 名指しの衝突の検出 / 波括弧の中の外部のモジュールの宣言 / 実行時のドメイン概念を表せること / 未決定を #175 より先に確定しないこと / #178 の範囲。

`秒`・`メートル` のモジュールパスが空であることと、`欠落.在るか()` が保守的に全体を倒しうることは、**偽の証明ではないためBlockerにしない**と明記されている。

## 次のセッションが引き継ぐ判断

### 親は判断を固める前に必ず副官へ反論を求める

**この作業で、親が最初に立てた判断は2回とも大きく外れた。**

- 第2回のレビューへの対応で、親が立てた判断A・B・D・Eの**4件すべてを副官が覆した**。とくにAの却下の理由は、親がBlocker 4で自分で入れた弱めの仕組みが同じ形の下でも働くことを数えていなかった
- 第1回のレビューへの対応でも、親の判断3件を副官が覆した

副官は現物のコードの行番号まで当たって反論を返す。**親が紙の上で立てた判断は、現物を読むと成り立たないことが多い。**

### 実装員は親の指示の誤りを実測で見つける

第2回では、親が書いた正規形の文言(Blocker D)が**いまの3つの実例のうち2つを落とす**ものであった。副官が指摘して直した。Blocker E でも、親が「全部を欠落へ倒す」と決めていたが、実測すると**5件のうち2件は本物の辺であり倒してはならない**ものであった。

**親は、範囲を測る前に「全部倒す」「全部読む」と決めない。** 着手の前に実測させる。

### 並行させた作業の統合で、親は毎回取りこぼしを出した

第1回は5件、第2回は1件である。形は次の3つである。

1. 改名への未追随(親が改名の名前だけを検索し、**署名の変わった口を検索しなかった**)
2. 戻り値の型の変更への未追随
3. **改名と移動が同時に起きる形**(作業Bが型を改名して別のファイルへ移し、作業Eの試験が旧い名前で期待していた)

3は `check` も `clippy` も検出せず、**実物のリポジトリを走査して期待する辺が在ることを見る回帰試験だけが捕まえた。** 親が第1回の後に導入した「押し上げる前に広く検索する」手順でも捕まえられなかった。

**次のセッションは、統合のたびに全列をビルド員へ回す。** 検索で代替しない。

### 親の文章は5回とも検査で違反を出された

繰り返した違反は次である。

- **非推奨語**: `便`(2026-09-13の裁定で正しくなる場面は無し)・`文脈`・`枝`(判別共用体の取りうる値は `選択肢`)・`綴り`(2026-09-20の裁定)
- **自分で発明した比喩**: `落穂`
- **主語の省略**: 述語ごとに動作の主体を書く。見出しに主体を書いても文には要る
- **主題と述語の対応**: 「〜の理由は、〜していなかった」のように名詞の主題へ動詞の述語を付けない
- **語順が申告を弱める形**: 実害より自分の判断を先に置く。改善の数値を先頭に置いて失敗の申告を後ろへ回す

**投稿の前に必ず文章チェック員へ回す。** 親は自分で書いた文の違反を自分で見つけられていない。

## 実装員が申告した、閉じていない限界(第3回のBlockerとは別)

- 試験5(別の出来事を同じ表示のラベルにしても `発生する` が交差しない)を、`blitz_esca` の実物の値では書けていない。`歩行方向` の取りうる値が8つしかなく、どの2つも表記を共有しない。検証器の題材の側で固定してある
- Major F の是正が実際に効くのは、コマンドを全件で数え上げる領域である。`blitz_esca` はコマンドが元から標本であるため網羅性の選択肢が動かない
- `blitz_esca` の探索の状態の件数は**手計算であって実測ではない**(121件から154件への見積り)
- 辺の端点の種類の取り違えが、**節点が1つだけ在る場合に衝突として数えられない**。型では止まっていない

## 実測の推移(PR本文の第4節と同じもの)

| 先端 | 入った是正 | 証明済み | 反証済み | 未決定 | 消費/生成/参照 | 保持する | 欠落 |
|---|---|---|---|---|---|---|---|
| `d0d389f5` | 基準値 | 2 | 0 | 0 | 0 / 0 / 0 | 36 | 概念が無い |
| `046cffc6` | 第1回の9件 | 1 | 0 | 1 | 4 / 4 / 1 | 36 | 0 |
| `820350f6` | 第2回のA〜EとF | 1 | 0 | 1 | 5 / 5 / 1 | 38 | 0 |

`820350f6` の検査は全段通っている。`blitz_design_verification` が76件・`xtask` が766件・`blitz_esca` が35件である。既知の失敗7件(`blitz_sim` の2件と `editor_server` の5件)は件数も試験名も基準値と一致する。

**証明済みが2件から1件へ減ったのは、レビューが指摘した偽の証明が消えたためである。** 親は件数が動かなかったことを健全化の証拠として出さない。いま問うている法則が2件だけであり、そのどちらも推測で積まれた辺を読まないためである。

## 作業ツリーと検証

`C:/devs/Blitzdrache0_build` がビルド員の置き場である(先端 `820350f6`)。**生成物(`.heightgrid` と `.png`)が汚れているが、これはLFSのオブジェクトがサーバーに無く再生成したものである。破棄しない。**

作業ツリーを新しく切るときは `GIT_LFS_SKIP_SMUDGE=1 git worktree add ...` を使う。**そのままだとLFSのオブジェクトが取れず作成が落ちる。**

是正の作業ツリーはすべて統合して削除済みである。

## 親が使った作業の切り方

第2回では次の段階で進めた。**端点の型を変える作業は関係・項・量化・グラフの問い・検証器・遷移モデル・有限全数・探索・抽出器のすべてと連動するため、単独で先行させる必要がある。**

1. 段階1(単独で先行): `設計概念への参照` を入れ、端点・項・量化・節点の照合を寄せる。**振る舞いを変えず、件数が1つも動かないことを受理の条件にする**
2. 段階2(3つを並行): 種類を答えへ効かせる / 名指しの単射化 / 役割の抽出
3. 段階3: 保証範囲の外を、静かに何も出さない状態から欠落へ倒す
4. 最後: 文書・PR本文・返信

第3回のBlocker 1〜5も同じ形で切れる。1と2は検証器と遷移モデル、3は抽出の範囲、4と5は抽出器の読み取りであり、3が最も広く波及する。
