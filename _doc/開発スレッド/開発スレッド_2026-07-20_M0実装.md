# 開発スレッド 2026-07-20 M0実装（ウィンドウとクリアカラー）

前スレッド: `開発スレッド_2026-07-19_Vulkanエンジン構想壁打ち.md`（構想・計画・規約の確定）

### ユーザー: M0着工の指示

> Blitzdrache0（Rust + Vulkan自作ゲームエンジン）の開発を続けます。前セッションで壁打ちが完了し、方向性・計画・規約はすべてリポジトリ内に文書化済みです。
> 3. タスク: M0を着工する。winitでウィンドウ表示 → blitz_renderがVulkanインスタンス〜スワップチェーンを構築 → クリアカラーを毎フレーム提示。DoD・判断基準・エスカレーション条件はすべて開発計画に従う
> 4. 検証: cargo xtask verify と validation layer（同期検証込み）エラーゼロ。実装は原則実装員サブエージェントへ委譲し、親は設計判断と検収
> 5. 規約は最初の1行から厳守（1ファイル100行・unsafe封じ込め・数学DDD。unwrap等はlintで機械強制済み）

### AI: M0の設計判断（実装委譲前に親が確定）

環境確認: Vulkan SDK 1.4.321.1 導入済み（validation layer・slangc 利用可）。

**判断1: Vulkan 1.3 を最低要件とし、クリアは dynamic rendering で行う**
- レンダーパスオブジェクト・フレームバッファを作らず、`loadOp=CLEAR` の動的レンダリングで
  クリアカラーを提示する
- 理由: M1のグラフィックスパイプライン構築が単純になる（レンダーパス互換性の概念が消える）。
  デスクトップVulkanの現代標準であり、レンダーグラフ（M5）とも整合する。SDK/GPU要件は
  Vulkan 1.3 で満たされる
- 却下案: `vkCmdClearColorImage`（TRANSFER境界が増えるだけでM1に繋がらない）、
  旧来のレンダーパスオブジェクト（M1以降で捨てる資産になる）

**判断2: M0のフレームインフライトは1**
- 単一フェンスで毎フレームGPU完了を待つ最小構成。複数フレームインフライトはM2のDoDで導入
- 理由: 開発計画の判断基準3（現マイルストーンに必要な最小の実装）

**判断3: validation の自動検証をカウンタで機械化する**
- debugビルドで `VK_LAYER_KHRONOS_validation` + 同期検証（VkValidationFeaturesEXT）を常時有効
- デバッグメッセンジャーがエラー・警告件数を共有カウンタ（Arc<AtomicU64>）に記録し、
  blitz_app がレンダラー破棄「後」に読み取って終了コードへ反映する
- 理由: DoDの「破棄時のvalidation警告ゼロ」は破棄後にしか判定できないため、
  カウンタをレンダラーの外に生かす設計が必要

**判断4: スモークモード（自動DoD検証）を blitz_app に持たせる**
- `--frames N` 指定で N フレーム描画後に自動終了。実行中の所定フレームで
  リサイズ → 最小化 → 復帰 を自分自身に対して実行する（winit API経由）
- 入口は `cargo xtask smoke` として登録（ツールの唯一の入口規約）
- 理由: DoDの「リサイズ・最小化で落ちない」を、GUI自動操作（要事前許可）なしで
  毎イテレーション機械検証できる。validation件数は判断3の終了コードで判定

**判断5: blitz_render 公開APIの語彙**
- `レンダラー`（生成する / 一フレーム描画する / サイズ変更を通知する）、`クリアカラー`、
  `ウィンドウ寸法`、`描画結果`（提示した / 見送った(最小化等)）、`レンダラーエラー`、
  `検証カウンタ`
- コンストラクタは raw-window-handle の型を受ける（規格クレートは要石として公開APIに
  許容済み。README参照）。ash型は一切露出しない
- 「最小化中で提示しない」はエラーでも無言スキップでもなく `描画結果` の正常系の値として
  型で表す（無し・未設定の設計規約に従う）

**実行モード**: 委譲規範0.5に従い、指定がないため確実モード（直列1体）。

### AI: 実装員による実装（1体・直列）と、実装中に検出された同期バグ

実装員がblitz_render（Vulkan層、23ファイル）・blitz_app（配線・CLI・スモーク、7ファイル）・
xtask smoke を実装。全ファイル100行以内・関数50行以内で完走。

実装中の初回スモーク実行で、検証カウンタが **WRITE_AFTER_READ ハザード**を検出した:
取得セマフォの待機段は COLOR_ATTACHMENT_OUTPUT なのに、描画前レイアウト遷移バリアの
srcStageMask が TOP_OF_PIPE だったため、バリアが待機の同期スコープ外になり
vkAcquireNextImageKHR の読み出しと衝突していた。srcStageMask を COLOR_ATTACHMENT_OUTPUT に
修正して解消（barrier.rs に `注意:` コメントとして定着）。判断3のカウンタ設計が
初回から機能した実例。

### AI: 親の検収で3件を差し戻し

全37ファイルの実読・行数実測・規約grepによる検収で発見:

1. **ash型の公開API露出（封じ込め違反)**: レンダラーエラーの `#[from] ash::LoadingError` /
   `#[from] ash::vk::Result` は pub enum のバリアントフィールドとして ash 型を公開APIに
   露出していた。修正: 自前値オブジェクト `Vulkan失敗コード`（i32 + コード名String、
   コンストラクタは pub(crate)）を新設し、From実装は「ashを受けて自前表現へ変換する」
   手書き実装に変更。公開APIからash型を取り出せる経路を消した
2. **孤立バリアント**: `必須機能非対応` は定義のみで構築箇所ゼロ（実装は機能非対応デバイスを
   選定時にフィルタする方式）。削除し、`適合物理デバイスなし` のメッセージへ必須機能の
   情報を統合
3. **存在しない文書位置への参照コメント**: `参照: CLAUDE.md「判断3」` 等4箇所が
   CLAUDE.md にない節（委譲プロンプト由来の見出し）を指していた。本スレッドの該当判断へ訂正

教訓: サブエージェントは委譲プロンプトを「文書」として参照コメントに書くことがある。
参照コメントの実在検証は検収の定番項目にする。

### AI: M0完了（DoD充足を親が再検証）

親自身の再実行で確認:
- `cargo xtask verify`: check / clippy -D warnings / test すべて成功
- `cargo xtask smoke`（240フレーム、途中でリサイズ→最小化→復帰の自己操作）:
  validationエラー・警告合計 0 件、終了コード成功

DoD照合: validation（同期検証込み）エラーゼロ = 充足 / リサイズ・最小化で落ちない =
スモーク自己操作で充足 / 破棄時のvalidation警告ゼロ = 破棄後読み取りのカウンタ0件で充足。

未検証として残る事項（実装員の申告どおり）: 手動での連続リサイズ、他ベンダーGPU、
ユニットテスト（Vulkan初期化はGPU環境依存のため実行時validation計測に委ねた）。

### ユーザー: 自律ループの開始指示

> 開発計画に従った作業を自立ループしてください

### AI: M1着工。設計判断6〜10を確定

自律ループ運用規約に従い、最小未完了マイルストーン M1（三角形とシェーダービルド基盤）を
選択。ユビキタス言語へ SPIR-V・パイプライン・ホットリロード の3語を先に定義追加した。

**判断6: シェーダーの配置とビルド時コンパイル**
- リポジトリ直下に `shaders/` を新設し、`triangle.slang` を置く（エントリは
  vertexMain / fragmentMain。エントリごとに個別の .spv へコンパイル）
- blitz_app の build.rs が slangc（VULKAN_SDK/Bin から発見）を呼んで OUT_DIR へ
  SPIR-V を生成し、include_bytes! で初期シェーダーとして埋め込む。
  `cargo:rerun-if-changed` でソース追従。ビルド時に slangc が無い・失敗ならビルドエラー

**判断7: ホットリロードは std のみの mtime ポーリング**
- notify 等の新規依存は追加しない（ライブラリ採用方針: 迷ったら借りない。
  ファイル監視はポーリングで十分であり依存ゼロで書ける）
- 約0.5秒間隔で mtime を確認 → 変更検知で slangc を子プロセス実行 → 成功時のみ
  パイプライン再生成。コンパイル失敗時は旧パイプライン継続 + stderr へ表示（落とさない）
- 監視対象は既定で `shaders/triangle.slang`、`--shader-source <path>` で差し替え可能
  （スモークが一時コピーを使うため）

**判断8: パイプラインは dynamic rendering + 動的ビューポート/シザー**
- VkPipelineRenderingCreateInfo でスワップチェーン形式を指定。頂点バッファなし
  （頂点インデックスから3頂点を生成）。動的ビューポート/シザーにより
  リサイズでのパイプライン再生成を不要にする

**判断9: DoDの機械検証はピクセル読み戻しで行う**
- スワップチェーン画像を TRANSFER_SRC 可能にし、「一フレーム描画して読み戻す」APIを追加
  （GPU同期を伴うためスモーク用途）。読み戻しは BGRA→RGBA 並び替え済みの自前型で返す
- smoke: 中央ピクセル=三角形の色、隅=クリアカラー を sRGB符号化考慮の許容誤差で判定。
  ホットリロードは、一時コピーした .slang の色定数をスモークシナリオが実行中に書き換え、
  反映後の中央ピクセル変化で判定（リポジトリの shaders/ は汚さない）
- ヘッドレス（サーフェス無し）オフスクリーン単体テストは見送り: レンダラー生成経路の
  二重化になるため、M5レンダーグラフ形式化時に再設計する。提示経路そのものの読み戻しの
  方が「表示される」の証明としても強い

**判断10: 公開API追加語彙**
- `シェーダー一式`（頂点・フラグメントのSPIR-Vバイト列。4バイト整列検証付き生成）
- `レンダラー::生成する` にシェーダー一式を追加、`シェーダーを差し替える(シェーダー一式)`
  でホットリロード時の再生成、`一フレーム描画して読み戻す(クリア色)` が
  `読み戻し結果`（読み戻した(読み戻し画像) | 見送った(見送り理由)）を返す

### ユーザー: 実装体制の許可と外部台帳の指示

> vulkanを触るのはSonnet5がサブエージェントでは難易度が高いと思うので。Fableがやるのも許可します。と言っても何を使うかはあなたに任せます。
> またvulkanのドキュメントが必要なら言ってください。
> またループで問題があれば教えてください。巨大なのでたぶん知識グラフとかにはしたほうがいい気はしますが。jimboのAgentRoomの札にissueや計画を書いておいてください

**判断（体制）**: 定型的な実装（M0で実証済みの水準）は引き続きSonnet実装員へ委譲し、
親（Fable）は設計判断・検収に加えて、Vulkan同期設計・アーキテクチャの難所と
実装員が詰まった箇所を直接実装する。委譲規範第0条「親は委譲が原則」よりこの許可が優先。
Vulkanドキュメントは現時点で不要（validation layer + 同期検証がオラクルとして機能）。
必要になったら（M5レンダーグラフの同期章精読など）ユーザーへ依頼する。

**判断（外部台帳）**: jimbo Fudaba に進行台帳（札 #137: M0〜M11のチェックリスト）と
積み残しissue台帳（札 #138: 未検証事項・ループ運用リスク）を作成した。正本はリポジトリ内
文書のままとし、札には「外から見える現在地」だけを写す（二重管理の回避）。
マイルストーン完了ごとに #137 をチェックし完了コミットをコメントする。

### AI: M1完了（実装員実装 → 親検収1件是正）

実装員がM1一式（shaders/triangle.slang、build.rs + build_support、パイプライン、
ホットリロード、ピクセル読み戻し、スモーク拡張）を実装。全ファイル100行以内を維持。

実装員が申告した設計判断の余白の埋め（親検収で承認）:
1. slangc に `-fvk-use-entrypoint-name` を追加（無いとエントリ名が main に正規化され
  vertexMain 指定のパイプライン生成が validation エラーになる。手動検証で発見）
2. 論理デバイスで `shaderDrawParameters`（Vulkan 1.1機能）を追加有効化
  （Slang が SV_VertexID の HLSL 意味論再現に gl_BaseVertex を使い SPIR-V の
  DrawParameters capability を要求するため。物理デバイス選定基準と対にした）
3. `読み戻し結果` は `見送り理由` を `描画結果` と共有（同一意味論のため語彙重複を回避）

親検収での是正1件: smoke/pixel_judgment.rs の `#[allow(clippy::as_conversions)]`
（f32→u8変換）は理由が正当でも「緩和はテストモジュールのみ」の機械的強制規約に反する
前例になるため、親が直接是正した。u8→f32（From実装あり）方向へ揃えて比較をf32領域で行い、
asキャストと allow 自体を消した。教訓: 「f32→整数の安全な変換が標準に無い」問題は
比較の向きを逆にする（整数→f32は常に安全）ことで回避できる。

DoD充足（親の再実行で確認）:
- `cargo xtask verify` 全通過
- `cargo xtask smoke`（480フレーム）: validation 0件 / 三角形の色を中央ピクセルで確認 /
  隅のクリアカラー確認 / 実行中のシェーダー書き換え → 緑三角形への変化を読み戻しで確認
- ホットリロードの検証カウンタ設計（判断3）と読み戻し判定（判断9）が両方機能

READMEを現状に同期（Slang採用済み化・smoke コマンド・shaders/ とホットリロードの説明）。

### ステータス

- 現在: **M1完了**。三角形表示 + Slangビルド基盤（build.rs）+ シェーダーホットリロード、
  ピクセル読み戻しによるDoD機械検証つき
- 次: M2（メッシュ・カメラ・フレームインフライト・数学DDD最小セット）
