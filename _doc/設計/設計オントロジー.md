# 設計オントロジー

この文書は、共通設計語彙クレート `crates/blitz_design` が持つ設計解釈マーカーと関数の役割、およびUpdate型のEventの語彙とコード文法を定める設計正本である。本書の内容は、Issue #137 のオーナーのコメント(2026-09-18)に基づく。マーカーの定義文は `_doc/計画/ユビキタス言語.md`「設計解釈マーカー」「M状態はM不変データである」「Mイベント」と同じ内容であり、本書はそれと矛盾しない。

## 1. 位置づけ

`blitz_design` は、Rust上の型が設計上何として解釈されるか(純粋な所有データ・状態・コマンド・イベント・規則・入力・工程と、存在の分類である値オブジェクト・エンティティ・エンティティ識別子・不変エンティティ・可変エンティティ)を宣言する設計解釈マーカーと、射影・解釈という関数の役割を表す境界付きの newtype だけを持つ。このクレートはドメインの語彙を1つも持たない。

置き場の判定の基準は「その型が何について語るか」である。

- 実装者は、プログラムについて語る(型が設計上どの役割かを言う)ものを `blitz_design` に置く。
- 実装者は、シミュレーションについて語る(時間尺度・物理小刻み等)ものを `blitz_sim` に置く。
- 実装者は、Esca世界について語る(旅行者・移動可能範囲等)ものを `blitz_esca` に置く。

`blitz_design` は何にも依存しない最下層であり、`blitz_sim` と `blitz_esca` がこれを使う。実装は `crates/blitz_design/src/marker.rs`(マーカー)と `crates/blitz_design/src/function_role.rs`(関数の役割)にある。

## 2. 設計解釈マーカーの一覧と共通契約

設計解釈マーカーとは、Rust上の型が設計上何として解釈されるかを、メソッドを持たないトレイトの実装で宣言する印のことである。例外は `Mエンティティ`(2.5節)だけであり、`Mエンティティ` は識別子を返すメソッドを1つ持つ。どの値がそのエンティティの識別子なのかを機械的に取得できるようにするためであり、2026-09-20 のオーナー裁定による。

本節の表はデータの役割の軸である。存在の分類(値オブジェクトとエンティティ)と変更可能性(不変と可変)の軸は 2.5 節が持ち、関数境界の役割(必須入力の束と任意の設定の束と評価の排他的な帰結)の軸は 2.6 節が持つ。

| マーカー | 設計上の役割 | `M不変データ` との関係(Relation) | `M不変データ` から継承する法則 | 役割ごとに追加で課す法則 |
|---|---|---|---|---|
| `M不変データ` | 参照や内部可変性を所有せず、値として複製・受け渡しできる純粋な所有データ。境界を越える転送にも状態の正本にも使う。2026-09-20 のオーナー裁定による意味は次の5つである。内部可変性を持たない。自己変更を行わない。値として複製・受け渡しできる。変更が必要な場合は既存の値を書き換えるのではなく新しい値を構築する。変数への再代入や別の値への置き換えそのものは禁止しない | (基底) | 参照・生ポインタ・内部可変性を持たない。固有の `impl` とトレイトの実装に `&mut self` を持たない(以上2つは `cargo xtask conform` が検査する)。`Clone` を持つ(上位トレイト) | 無し |
| `M状態` | 実在し、保持され、遷移されるゲームまたはシミュレーションの状態 | is-a(`M状態: M不変データ`) | 同上 | `struct` に限定せず `enum` も状態として扱う。遷移は新しい状態を返す工程が担い、状態自身は `&mut self` を持たない(`M不変データ` から継承する法則。2026-09-20 に「`&mut self` は禁じない」から改めた) |
| `Mコマンド` | 主体が選択・発行する命令、意図 | is-a(`Mコマンド: M不変データ`) | 同上 | 必ず `enum` で定義する。固有の `impl` とトレイトの実装に `&mut self` を持たない |
| `Mイベント` | ある状態遷移によって成立した過去の事実 | is-a(`Mイベント: M不変データ`) | 同上 | 固有の `impl` とトレイトの実装に `&mut self` を持たない。`enum` を必須としない |
| `M規則` | 遷移からは読み取り専用で与えられる規則値(内部可変性を持たず、型自身に可変メソッドを持たない純粋な所有データ。値の置き換えを型では禁じない)。規則の「不変」とは、1回の遷移の評価のあいだ読み取り専用の入力であることであり、プログラムの全期間で同じ値であることは意味しない(速度の強化や難易度の変更で規則の値が変わることを妨げない) | is-a(`M規則: M不変データ`) | 同上 | 必ず `struct` で定義する。固有の `impl` とトレイトの実装に `&mut self` を持たない |
| `M入力` | 外界(入力デバイス等)から届く、意味付けの前の生の信号 | is-a(`M入力: M不変データ`) | 同上 | ゲームの意味論を持たない |
| `M観測` | 状態・処理・評価について、正本や因果を構成せず、計器や開発用の画面などが観察するために導出されたデータ(2026-09-20 のオーナー裁定。PR #169 のコメント) | is-a(`M観測: M不変データ`) | 同上 | 正本ではない。観測の対象そのものの因果へ戻さない。`struct` と `enum` のどちらであるかは本質ではない。`Mイベント` とは別の概念である(`Mイベント` はドメイン上で成立した過去の事実であり、`M観測` は対象を外から見るために導出した情報である)。`M結果` の部分型にはしない |
| `M工程` | 状態を読み書きして進める工程 | 無し(工程はデータでない) | 無し | 現時点で共通契約を課さない |

`cargo xtask conform` が機械で保証しているのはここまでであり、設計解釈マーカーの型契約は値の置き換え(`規則.身体 = 別の値`、`&mut 規則` を受ける自由関数)を禁じていない。

本書は、かつて本節の表にあった `M設定`(起動時に決まり、工程からは読み取り専用で与えられる設定値)を、2026-09-20 のオーナー裁定(PR #169 のコメント)で共通のマーカーから外した。「起動時に決まる」「外から与えられる」「長く保持される」はライフサイクルや利用方法の事情であり、型の本質ではないためである。関数境界の役割は `MParameter` と `MOptions` が表す。起動設定やアプリケーション設定が要るなら、実装者はそれを具体のドメインの語彙として定義する。

データの役割(`M状態`・`Mコマンド`・`Mイベント`・`M規則`・`M入力`・`M観測`)はすべて `M不変データ` を上位トレイトに持つ。`Mコマンド: M不変データ` 等の supertrait の Relation はコンパイラが保証し、`cargo xtask conform` は `M不変データ` と役割の実装から型の定義をたどって、その定義に純粋データ規約(参照・生ポインタ・内部可変性を持たない)を当てる。`impl M不変データ for` をどのファイルに書くかは法則ではない。本書は、PR #138 の第5回レビュー(2026-09-20)の必須2を受けて、同じファイルに書く要求を消した。`cargo xtask conform` は、同じ型が複数の役割を持っても違反を1回だけ報告する。

`M状態: M不変データ` は Blitzdrache 全体の共通契約である(2026-09-18 オーナー裁定)。既存コードが適合しない場合、実装者は共通契約を弱めず既存コードの側を直す。本書が `Mコマンド`・`Mイベント`・`M規則` を `M不変データ` の上に積んだのは、PR #138 の第4回レビュー(2026-09-19)の必須3・高優先度6への対応である。`Mコマンド` だけが純粋データ契約から外れていると、`&コマンド` から外部状態を書き換えられ、`M遷移関数` が純粋計算であるという理論を型契約が支えられないためである。

本書が `M不変データ` の役割を「境界を越えて運ばれるデータ」から「純粋な所有データ」へ広げたのは、PR #138 の第3回レビュー(2026-09-19)の必須1に対して、本書が `M状態: M不変データ` の共通契約を保つ側(案A)を採ったためである。`blitz_sim` のソルバ内部の作業状態も `M状態` であり、それは境界を越えないが純粋な所有データではあるので、この定義なら矛盾しない。この役割の名前は、2026-09-20 のオーナー裁定(PR #138 のコメント)で `MDTO` から `M不変データ` に決まった。

## 2.5 存在の分類(同一性・ライフサイクル・不変性)

本節は、PR #138 のオーナーのコメント(2026-09-20)の裁定1から裁定5を正本として記録する。DDD(ドメイン駆動設計)の基礎概念である同一性・ライフサイクル・不変性の軸を、第2節のデータの役割とは別の軸として設計オントロジーへ足す。

### 2.5.1 4つの軸

設計解釈マーカーは、互いに直交する4つの軸を持つ。1つの型は各軸から独立に分類を選ぶ。本節が扱うのは2つ目と3つ目の軸であり、4つ目の軸(関数境界の役割)は 2.6 節が持つ。

| 軸 | 問い | 分類 |
|---|---|---|
| データの役割 | その型は因果モデルのどこに現れるか | `M不変データ`(基底)・状態・コマンド・イベント・規則・入力・観測・工程(第2節) |
| 存在の分類 | その型は同一性を持つか | 値オブジェクト(`M値オブジェクト`)とエンティティ(`Mエンティティ`)と、エンティティの識別子(`Mエンティティ識別子`) |
| 変更可能性 | その型は自己変更を行うか | 不変(`M不変エンティティ`)と可変(`M可変エンティティ`)。この軸のマーカーはエンティティに付け、値オブジェクトは `M不変データ` により常に不変である |
| 関数境界の役割 | その型は関数の境界をどう渡るか | 必須入力の束(`MParameter`)と任意の設定の束(`MOptions`)と評価の排他的な帰結(`M結果`)(2.6節) |

`M不変エンティティ` と `M可変エンティティ` は同じ軸の排他の分類であり、同じ型が両方を名乗ることを `cargo xtask conform` が違反にする(2.5.3)。

値オブジェクトとエンティティの区別と、不変と可変の区別は別の軸である。値オブジェクトは常に不変であるが(`M値オブジェクト: M不変データ`)、エンティティは不変にも可変にもなりうる。`M不変エンティティ` は「不変エンティティ」という新しい哲学ではなく、同一性とライフサイクルを持つこと(`Mエンティティ`)と自己変更を禁じること(`M不変データ`)の2つの既存の概念の合成である。

### 2.5.2 マーカーの一覧と法則

| マーカー | 定義 | 上位トレイト | 課す法則 | コンパイラが強制するもの | `cargo xtask conform` が検査するもの |
|---|---|---|---|---|---|
| `M不変データ` | 参照や内部可変性を所有せず、値として複製・受け渡しできる純粋な所有データ(第2節) | `Clone` | 内部可変性を持たない。自己変更を行わない。値として複製・受け渡しできる。変更は既存の値の書き換えでなく新しい値の構築で表す。変数への再代入や別の値への置き換えは禁止しない | `Clone` | 定義に参照・生ポインタ・内部可変性が無いこと。固有の `impl` とトレイトの実装に `&mut self` が無いこと |
| `M値オブジェクト` | 同一性を持たず、その値そのものによって意味と等価性が決まる型 | `M不変データ + PartialEq` | 自己同一性を持たない。内容が同じなら同じ値として扱う。自己変更せず、変更は新しい値の生成として表す | `Clone`・`PartialEq` | `M不変データ` を通して同上(この印を直接には見ない) |
| `Mエンティティ` | ライフサイクルを通して維持される同一性を持ち、状態が変化しても同じものとして追跡される型 | 無し | 同一性は識別子の一致で決まる。識別子を機械的に取得できる | 関連型 `識別子: Mエンティティ識別子` と `fn 識別子(&self) -> &Self::識別子` の存在 | 無し |
| `Mエンティティ識別子` | エンティティの同一性を担う識別子。値オブジェクトとして扱う | `M値オブジェクト + Eq + Hash` | 完全な同値とハッシュで同定できる | `Clone`・`PartialEq`・`Eq`・`Hash` | `M不変データ` を通して同上 |
| `M不変エンティティ` | 同一性とライフサイクルを持ち、かつ自己変更を行わない型 | `Mエンティティ + M不変データ` | 両方の上位トレイトの法則 | 両方の上位トレイトの要求 | `M不変データ` を通して同上 |
| `M可変エンティティ` | 同一性を維持したまま内部の状態を更新する設計を意図している型 | `Mエンティティ` | 強い追加の法則を持たない。設計の分類の印である | `Mエンティティ` の要求 | 無し |

`M値オブジェクト` の基底を `Eq` でなく `PartialEq` にするのは、`f32` を含む値オブジェクトでは完全な同値(反射律)が成り立たないことがあるためである。`Eq` が成り立つ値オブジェクトと、浮動小数を含み完全な同値が成り立たない値オブジェクトを、将来別の分類として足してよい。

`Mエンティティ` には `PartialEq` を要求しない。エンティティの同一性は全フィールドの一致ではなく識別子の一致で決まるためである。安易な `derive(PartialEq)` とエンティティの同一性を混同してはならない。例として、`旅行者#42, hp=100` と `旅行者#42, hp=50` は値としては異なるが、同じエンティティである。

### 2.5.3 機械検証の範囲

コンパイラが強制するものは、上位トレイトの `Clone`・`PartialEq`・`Eq`・`Hash` と、`Mエンティティ` の関連型と識別子のメソッドの存在である。

`cargo xtask conform` の設計オントロジーの検査が見るものは、`M不変データ` またはデータの役割を実装する型の定義に参照・生ポインタ・内部可変性が無いこと、`Mコマンド` が列挙で `M規則` が構造体であること、`M不変データ` を実装する型(データの役割を実装する型をすべて含む)の固有の `impl` とトレイトの実装に `&mut self` が無いことである。存在の分類のマーカーについても、実装の正規形(`impl マーカー名 for 型` または `impl blitz_design::マーカー名 for 型`)・別名の取り込みの禁止・波括弧付きのモジュールの中の実装の禁止・再公開の禁止は検査する(第5節)。純粋データ規約と `&mut self` の禁止は `M不変データ` を上位トレイトに持つもの(`M値オブジェクト`・`Mエンティティ識別子`・`M不変エンティティ`)だけが `impl M不変データ for` の行を通して対象になり、`Mエンティティ` と `M可変エンティティ` には課さない(`Mエンティティ` は `M不変データ` を要求せず、`M可変エンティティ` は自己変更を意図する分類であるため)。同じ型が `M不変エンティティ` と `M可変エンティティ` の両方を名乗ることは違反にする(型の同一性は推定したモジュールパス + 型名で突き合わせる)。

`M観測` について機械で検査できるのは、`M不変データ` に由来する法則(純粋データ規約と `&mut self` の禁止)だけである。「`M状態` が `M観測` を正本として保持していないか」「遷移の因果の入力へ観測を戻していないか」は、型と型の関係の解析ができるようになってから検査の候補にする(2026-09-20 のオーナー裁定)。

次のものは検査しない。エンティティが本当に同一性を持つか。識別子が本当に一意か。`M可変エンティティ` が同一性を保っているか(識別子を書き換えていないか)。private のフィールドを通した間接の変更(工程が `&mut 型` を受けてフィールドへ代入する形や、値の置き換え)。これらは意味論または具体ドメインの配線であり、構文の検査では判定できない。

### 2.5.4 設計の方針

Rust では束縛が既定で不変だが、それは言語の性質である。本節が扱っているのは設計の上での変更可能性・同一性・ライフサイクルであり、言語の性質とは別の層として明示する。意味のあるドメインの制約はマーカーとして明示し、できる限り法則を構文の検査(`cargo xtask conform`)で確かめる。この方針は 2026-09-20 のオーナー裁定(裁定5)による。

## 2.6 関数境界の役割(4つ目の軸。必須入力の束と任意の設定の束と評価の排他的な帰結)

本節は、PR #169 のオーナーのコメント(2026-09-20)の裁定を正本として記録する。関数の境界を渡る型を、入力側の必須入力の束(`MParameter`)と任意の設定の束(`MOptions`)、出力側の評価の排他的な帰結(`M結果`)に分類する。本書は、この軸をかつて「引数オブジェクトの分類」と呼んでいたが、出力側の `M結果` を同じ軸へ置いた 2026-09-20 の裁定(PR #169 の「設計オントロジーを文法グラフとして構築する方針」の第8節)で「関数境界の役割」へ改めた。

本書はこの分類を、データの役割(第2節)とも存在の分類とも変更可能性(2.5節)とも別の4つ目の軸として置く。データの役割は「その型は因果モデルのどこに現れるか」を問い、存在の分類は「その型は同一性を持つか」を問い、変更可能性は「その型は自己変更を行うか」を問うのに対し、関数境界の役割は「その型は関数の境界をどう渡るか」を問う。どの既存の軸の問いにも答えないため、既存の軸の中へ入れない。`MParameter` と `MOptions` はこの軸の排他の分類であり、同じ型が両方を名乗ることを `cargo xtask conform` が違反にする(2.6.1)。`M結果` は排他の組に入らない。

| マーカー | 定義 | 上位トレイト | 課す法則 | `cargo xtask conform` が検査するもの |
|---|---|---|---|---|
| `MParameter` | 関数の実行に必要な必須入力を、名前付きのプロパティとして1つの引数オブジェクトへ束ねた型 | `M不変データ` | 関数へ渡す必須入力の束である。各プロパティは必要だから存在する値である。`Option<T>` をフィールドとして持たない。不変性は `M不変データ` から継承する | 定義に `Option<` のフィールドが無いこと(2.6.1)。`M不変データ` を通して純粋データ規約と `&mut self` の禁止 |
| `MOptions` | 関数呼び出し時に、呼び出し側が任意に指定できる設定を束ねる引数オブジェクト | `M不変データ` | `Option<T>` をフィールドとして持つことを許す。全フィールドが `Option<T>` である必要は無い。`Option<T>` を持つこと自体は成立の条件でない。指定してもしなくても関数の呼び出しが成立する設定を束ねる | `M不変データ` を通して純粋データ規約と `&mut self` の禁止 |
| `M結果` | ある評価が取り得る排他的な帰結を表す判別型。`Result<T, E>` が代表例であり、3つ以上の選択肢を持つドメイン固有の列挙でもよい | 無し | 本質は `Result` という標準の型そのものではなく、排他的な帰結を持つ判別型であることにある。各選択肢が運ぶ値(`遷移成功結果`・`遷移失敗結果`)そのものは `M結果` ではない | 実装の正規形・別名の取り込みの禁止・波括弧付きのモジュールの中の実装の禁止・再公開の禁止だけ(2.6.1) |

`M結果` に上位トレイトを付けないのは、`Result<T, E>` の性質が `T` と `E` に依存し、一律に `M不変データ` を課せないためである。`blitz_design` は `impl<T, E> M結果 for Result<T, E> {}` を持ち、標準の `Result` が `M結果` であることを型で示す。例えば `Result<遷移成功結果, 遷移失敗結果>` で結果を表しているのは外側の判別構造であり、内側の2つの値ではない。

値が無い状態がドメイン上の必須入力なら、実装者は `Option<T>` を置かずに、その意味を表す専用の型か列挙へ持ち上げる。`MOptions` はブラウザの DOM API などにある options object に近い概念として扱う。Esca第1段階では `遷移パラメータ`(3.5.1)が `MParameter` を実装し、`MOptions` を実装する型はまだ無い。

### 2.6.1 機械検証の範囲

`cargo xtask conform` は、`MParameter` を実装する型の定義ブロック(コメントと文字列を落としたコードだけの行)に `Option<` の文字列があれば違反にする。検査は文字列の照合であり、型の別名を通した `Option`(`type 任意の値 = Option<u8>` を経由する形)と、フィールドの型である別の構造体に埋めた `Option` は見ない(`Vec<Option<u8>>` のように定義の行に `Option<` が現れる入れ子は見つかる)。`MOptions` には課さない。同じ型が `MParameter` と `MOptions` の両方を名乗ることは違反にする(型の同一性は推定したモジュールパス + 型名で突き合わせる)。実装の正規形・別名の取り込みの禁止・波括弧付きのモジュールの中の実装の禁止・再公開の禁止(第5節)は、この2つのマーカーにも `M結果` にも `M工程` にも掛かる。

`M結果` には、純粋データ規約・`&mut self` の禁止・定義の解決のどれも課さない。検査器が定義をたどるのは `M不変データ`・`M不変データ` を上位トレイトに持つデータの役割・`MParameter` の実装だけであり(`xtask/src/conform/design_ontology/syntax_patterns.rs` の `純粋データの役割一覧`)、`M結果` はそのどれにも入れない。標準の `Result` への実装 `impl<T, E> M結果 for Result<T, E> {}` は `crates` 配下に定義を持たず、定義をたどる検査へ入れると「定義が見つからない」の違反になるためである。

## 3. 関数の役割

`blitz_design` は、関数の設計上の役割を表す境界付きの newtype を2つ持つ。

- `M射影関数<状態: M状態, 射影DTO: M不変データ>` は `fn(&状態) -> 射影DTO` を包み、状態から外部向けの表示データを取り出す一方向の観測の計算である。
- `M解釈関数<入力: M入力, コマンド: Mコマンド>` は `fn(&入力) -> コマンド` を包み、生の入力から主体のコマンドを導く純粋な解釈の計算である。

型エイリアスに書いた境界は Rust の仕様上強制されない(rust-lang/rust#112792)。そのため関数の役割は型エイリアスでなく、関数ポインタを包む構造体(newtype)であり、型引数の境界を構造体の宣言に書く。構造体の境界は利用箇所でコンパイラが強制するため、型を使った時点で型引数の境界が掛かる。迂回できる書き方は無い(`blitz_design::M射影関数<...>` と修飾しても、`use ... as` で別名を付けても、`let` で受けても、型を使った時点で境界が掛かる)。各 newtype は `生成する(関数)` で関数を包み、`呼び出す(&引数)` で包んだ関数を適用する。利用側は `const _: M射影関数<A, B> = M射影関数::生成する(関数);` の静的宣言で、関数が役割を満たすことをコンパイラに確かめさせる。

`M遷移関数<状態: M状態, コマンド: Mコマンド, 規則: M規則, イベント: Mイベント, 失敗: std::error::Error>` と `遷移成功結果<状態, イベント>` は `crates/blitz_esca/src/ontology.rs` に残る。`M遷移関数` も同じ形の境界付きの newtype である。`遷移成功結果` は型引数に `M状態`・`Mイベント` の境界を持つ意味型であり、汎用の入れ物ではない。構造体に書いた境界は利用箇所でコンパイラが強制するため、`遷移成功結果<String, usize>` のような値は作れない。本書がこれらを `blitz_design` へ上げないのは、オーナーの裁定「計算モデルまで固定しているため、まず Esca 側で直してから昇格を判断する」による。遷移パラメータ(3.5.1)の一般形がまだ確定していないことも、本書が `M遷移関数` を `blitz_design` へ上げない理由である。

## 3.5 状態遷移の理論(因果モデル)

本節は、Issue #137のPR #138へオーナーが2026-09-19に書いたコメントが示した状態遷移の理論を正本として記録する。この理論は、第2節の設計解釈マーカーと第3節の関数の役割が、個別に便利なトレイトの集まりではなく、ゲーム世界の因果関係を記述するための一組の設計語彙であることの根拠である。

### 3.5.1 基本形

本書は、状態遷移の基本形を次の図で表す。

```text
                  規則
                   │
                   ▼
現在の状態 ──┐
             ├─ 状態遷移(遷移を評価) ─┬─ 成功 ──→ 次の状態
コマンド ────┘                        │            │
                                      │            └─→ 起きた出来事
                                      │                   │
                                      │                   ▼
                                      │                イベント
                                      └─ 失敗 ──→ 失敗(次の状態も出来事も無い)
```

遷移の型は次である。

```text
遷移 : 状態 × コマンド × 規則
      -> 成功(遷移成功結果) | 失敗
```

一般形は次である。

```text
遷移 : 状態 × コマンド × 規則 × 遷移パラメータ
      -> 成功(遷移成功結果) | 失敗(遷移失敗結果)
```

遷移パラメータとは、遷移関数の実行に必要な、状態でもコマンドでも規則でもない必須の入力(経過時間・乱数源から確定した値・世界時刻・刻みの情報など)を、名前付きのプロパティとして1つの引数オブジェクトへ束ねた型のことである。`MParameter`(2.6節)を実装する。かつて本書はこれを「遷移文脈」と呼んでいたが、`文脈 / context` は周辺事情を指す普通語であり、型として何を持つかを共有できない。この型は遷移関数の実行に必要な引数を1つのオブジェクトへ束ねたものであるため、`遷移パラメータ` とする(2026-09-20 のオーナー裁定。PR #169 のコメント。「遷移の追加入力」は、追加であることが本質でないため採らなかった)。Esca第1段階では遷移パラメータは `経過時間` だけを持ち(`crates/blitz_esca/src/transition_parameter.rs`)、実装の `M遷移関数` は `(&状態, &コマンド, &規則, 遷移パラメータ) -> Result<遷移成功結果, 失敗>` である。3.5の基本形の図は遷移パラメータを省略した概念図である。遷移パラメータの一般形(どの入力を含めるか)はまだ確定していない。

遷移は成功と失敗の2つの選択肢を持つ。成功は遷移成功結果(次の状態 + その評価で成立した事実)であり、失敗は有効な次の状態を構築できなかったことである(3.5.9)。失敗を型で返すかは各ドメインが決め、Esca は `Result` で返す。

この文書と実装は、図の「起きた出来事」を `出来事一覧` という名前で呼ぶ。`遷移成功結果` は第4.1節が定義する `遷移成功結果<状態, イベント>` であり、フィールド名は `次の状態` と `出来事一覧` である。

### 3.5.2 4概念の意味

状態・コマンド・規則・イベントの4概念は、それぞれ次の意味と時制を持つ。

| 概念 | 何を表すか | 時制 |
|---|---|---|
| 状態 | 今どうなっているか | 現在 |
| コマンド | 何をしようとしているか | 未来への要求 |
| 規則 | 何が起きてよいか、どう変わるか | 不変の法則 |
| イベント | 実際に何が起きたか | 過去の事実 |

規則の「不変」とは、1回の遷移の評価のあいだ読み取り専用の入力であることである。規則の「不変」は、プログラムの全期間で同じ値であることを意味しない(速度の強化や難易度の変更で規則の値が変わることを妨げない)。

### 3.5.3 Escaの旅行者での例

Escaの旅行者の場合は、次の関係になる。

```text
状態
  現在地 = (10m, 20m)
  行動状態 = 静止中

コマンド
  東へ歩く

規則
  歩行速度 = 3m/s
  東側に壁がある

        ↓ 遷移

次の状態
  現在地 = (11m, 20m)
  行動状態 = 歩行中

イベント
  歩行を開始した
  壁に接触した
```

この例が持つ `行動状態` は、2026-09-19時点の `blitz_esca` の状態には無い。`歩行を開始した` や `立ち止まった` を出来事として持つには、前の状態と次の状態の行動の差を判定できる `行動状態` が状態側に要る。現状の `blitz_esca` はこの `行動状態` を持たないため、`歩行を開始した` や `立ち止まった` を出来事として生成していない(参照: `_doc/設計/Esca/設計正本.md` 4.4 条5)。

### 3.5.4 イベントは状態変更の命令ではない

イベントは状態変更の命令ではない。イベントは、遷移の評価で成立した事実である。向きは次である。

```text
前状態 + コマンド + 規則
        ↓
     遷移を評価
        ├─ 失敗 → 失敗の値だけ(次状態も事実も無い。3.5.9)
        ↓ 成功
次状態 + その評価で成立した事実
        ↓
   イベントとして公開する
```

成立する事実は、状態が変化したことを必要としない。現行の `blitz_esca` では `旅行者の意図::見回す` の遷移が次の状態を前の状態と同じまま返し、`旅行者の出来事::周囲を観察した` を出来事一覧へ入れる。状態は変わらないが、観察したという事実は遷移の評価で成立している。

そのうち、`歩行を開始した`・`立ち止まった`・HPが0になった・扉が開いた のような状態変化イベントだけが、前状態と次状態の差分を根拠にする。状態変化イベントは、前状態と次状態の差を表現できることが生成条件である(4.4 の法則E2)。

### 3.5.5 イベントシステムの定義

イベントシステムとは、ある状態遷移で成立した事実を、その事実を必要とする別の系へ伝播させる仕組みのことである。伝播先の例は次である。

```text
                     ┌→ 音響
                     │    「歩行を開始した」
                     │       → 足音開始
                     │
状態遷移 → イベント ├→ アニメーション
                     │       → 歩行アニメ開始
                     │
                     ├→ クエスト
                     │       → 「初めて歩いた」を記録
                     │
                     └→ AI
                             → 近くの敵が音に反応
```

### 3.5.6 状態遷移系とイベント伝播系の二層

この理論は、少なくとも次の二層を分ける。

```text
【状態遷移系】

M状態
Mコマンド
M規則
Mイベント
M遷移

        ↓ イベントを生成

【イベント伝播系】

Mイベント発行
Mイベント購読
Mイベント反応
Mイベント配送
```

状態遷移系とは、`M状態`・`Mコマンド`・`M規則`・`Mイベント`・遷移関数からなる、状態を遷移させて起きた事実を生成する側の層のことである。イベント伝播系とは、状態遷移系が生成したイベントを、それを必要とする別の系へ伝播させる側の層のことである。イベント伝播系が持つ発行・購読・反応・配送の4語は、オーナーのコメントに名前だけがあり、2026-09-19時点の `blitz_design` にはまだ無い候補である。

### 3.5.7 イベント反応

イベント反応とは、イベントを受けて、コマンド・外部作用・別のイベント・何もしない のいずれかを返す工程のことである。イベント反応は次の型を持つ。

```text
イベント反応:
    イベント
        -> コマンド
        | 外部作用
        | 別イベント
        | 何もしない
```

例は次の2つである。

```text
敵を倒した
   ↓
クエストシステムが観測
   ↓
「報酬を付与する」というコマンドを生成
```

```text
敵を倒した
   ↓
音響システムが観測
   ↓
SEを鳴らす
```

この整理は、`Mイベント` がイベントシステム全体を表す型ではなく、状態遷移系とイベント伝播系をつなぐ「起きた事実」を表すマーカーであることを示す。

### 3.5.8 全体の因果モデル

全体の因果関係は次のモデルで表す。

```text
意図
 ↓
コマンド
 ↓
状態 + 規則
 ↓
状態遷移 ─ 失敗 → 失敗の値(新しい状態も起きた事実も無い。3.5.9)
 ↓ 成功
新しい状態
 +
起きた事実
 ↓
イベント
 ↓
別の系の反応
 ↓
新しいコマンド / 外部作用 / 別イベント / 出来事の保存(ログ・リプレイ・監査履歴など)
```

`M状態`・`Mコマンド`・`M規則`・`Mイベント` は、個別の便利なトレイトではなく、ゲーム世界の因果関係を記述するための一組の設計語彙である。

### 3.5.9 遷移失敗結果

遷移失敗結果とは、与えられた状態・コマンド・規則・遷移パラメータから、有効な次の状態を構築できなかったことを表す失敗側の値のことである。遷移の評価は成功と失敗の2つの選択肢を持つ。成功側の値を遷移成功結果(次の状態 + その評価で成立した事実)と呼び、失敗側の値を遷移失敗結果と呼ぶ。外側の `Result` が成功と失敗の分岐そのものを表すため、内側の成功側だけを「遷移結果」と呼ぶと `Result` 全体と意味が競合する。成功側と失敗側を対称に名付けることは 2026-09-20 のオーナー裁定(PR #169 のコメント)による。

```text
遷移
├─ 成功 → 遷移成功結果
│          ├─ 次の状態
│          └─ 出来事一覧
└─ 失敗 → 遷移失敗結果
```

実装の `M遷移関数` の型引数 `失敗` の名前は変えない。型引数は具体の失敗の型を各ドメインが決めるための穴であり、そこへ入る型を本書は遷移失敗結果と呼ぶ。

- 失敗のとき、次の状態は採用されない。呼び出し側は元の状態を維持する。
- 失敗のとき、出来事は1件も生じない。遷移が成立していないため、成立した事実も無い。
- 「遷移に失敗した」は `Mイベント` ではない。`Mイベント` はその遷移によって実際に成立した事実であり、失敗は事実の不成立である。失敗を別の系へ伝えるなら、呼び出し側が失敗の値を見て自分の語彙で表す。
- 法則E1からE8(4.4)は、成功した遷移だけを対象とする。
- Esca第1段階では、失敗は `旅行者の現在地の生成の失敗`(数値の表現の上で有効な次の状態を構築できなかったこと)である。実装の `M遷移関数` は失敗を `std::error::Error` の型引数で受け、`Result<遷移成功結果, 失敗>` で返す。

## 4. Update型のEventの語彙とコード文法

第4節は、第3.5節の理論の、Event側の語彙とコード文法である。

Observer / callback 型の Event と、`Mイベント` は別概念として語彙を分離する。Observer 系の `Event`・`Subscriber`・`Listener`・`Handler`・`Subscribe`・`Unsubscribe`・`Invoke` は「呼び出し機構」の語彙である。`Mイベント` はそうではなく、Update / 遷移が生成する過去の事実データである。したがって本書は、こちらにも「何を何と呼び、コードをどう書くか」という独自の文法を定める。

### 4.1 基本語彙

**`Mイベント` / 出来事**とは、ある遷移によって実際に成立した過去の事実を表す値のことである。

```rust
pub trait Mイベント: M不変データ {}
```

Event 自身は誰かを呼び出さない。Event はコールバック一覧、`invoke()`、購読者を持たない。`struct` と `enum` のどちらでもよい。次の2つはどちらも正当な `Mイベント` である。

```rust
pub enum 旅行者の出来事 {
    歩行を開始した,
    立ち止まった,
    障害物に遮られた,
}
```

```rust
pub struct ダメージを受けた {
    pub 対象: 個体ID,
    pub 量: ダメージ量,
}
```

**`遷移成功結果<状態, イベント>`**とは、Update / 遷移が返す、次の状態と、その遷移で成立した出来事の組のことである。この型は状態と出来事を混ぜない境界である。

```rust
pub struct 遷移成功結果<状態, イベント> {
    pub 次の状態: 状態,
    pub 出来事一覧: Vec<イベント>,
}
```

**出来事の生成**とは、遷移の計算中に、実際に成立した事実を `出来事一覧` へ追加することである。出来事の生成は Observer 系の `invoke` に相当する語ではない。

- 良い語: `出来事を生成する`、`出来事一覧へ加える`、`〜が成立した`
- 避ける語: `invoke`、`fire`、`通知を呼ぶ`

`emit` を日本語へ持ち込む場合も、書き手はそれを「コールバックを呼ぶ」意味ではなく「出来事値を出力する」の意味に限定する。

**出来事の配送**とは、既に生成済みの出来事値を、0個以上の消費側へ渡す工程のことである。配送は新しい事実を作らない。配送は既存の事実を読者へ渡すだけである。

```text
遷移成功結果.出来事一覧
        │
        ├─→ 音響への解釈
        ├─→ 演出への解釈
        ├─→ 進行への解釈
        └─→ ログ
```

**出来事の解釈**とは、出来事を、別のドメインや外部システムが理解する型へ純粋変換することである。

```text
旅行者の出来事
    → 音響要求
    → 演出要求
    → 別ドメインのコマンド
```

基本形は `fn (&イベント) -> 出力` であり、0件以上を返すなら `fn (&イベント) -> Vec<出力>` である。将来、共通の関数役割として必要になった場合は `Mイベント解釈関数<イベント, 出力>` のような型を検討できるが、利用例が固まる前に共通化しない。

**要求**とは、SE再生、パーティクル生成、画面演出など、外部システムへ「これを実行してほしい」と渡す値のことである。

```text
旅行者の出来事::障害物に遮られた
    ↓ 解釈
壁衝突音を再生する要求
```

ドメインの遷移関数自身は音響API等を呼ばない。イベントから要求へ変換した後、外部境界が要求を実行する。`M要求` を共通マーカーにするかは、複数の実例が出てから判断する。

### 4.2 標準データフロー

Update型のEventシステムの標準形を次とする。

```text
M入力
  ↓ 解釈
Mコマンド
  ↓
M状態 + Mコマンド + M規則
  ↓ 遷移
遷移成功結果<M状態, Mイベント>
  ├─ 次の状態 ─────────→ 次回の状態正本
  │
  └─ 出来事一覧
       ├─→ 出来事の解釈 → 音響要求 → 音響境界
       ├─→ 出来事の解釈 → 演出要求 → 描画/演出境界
       ├─→ 出来事の解釈 → 別ドメインのMコマンド → 別のUpdate
       └─→ ログ / リプレイ等
```

重要なのは、`Event -> callback` ではなく `Event -> data flow` である。

### 4.3 Observer系の語彙との対応

この方式では以下の語を原則として使わない。

| Observer / callback型 | Update型で対応する概念 |
|---|---|
| Event object with invoke | `Mイベント` という事実値 |
| Subscriber / Listener | 出来事の消費側 |
| Handler | 出来事の解釈、または要求を実行する工程 |
| Subscribe | コンポジションルートで消費側を明示配線する |
| Unsubscribe | 通常は存在しない。配線自体の寿命を変える |
| Invoke / Fire | 出来事値を生成して遷移成功結果へ入れる |
| EventArgs | `Mイベント` 自身のデータ |
| EventBus | 原則作らない。必要なら「出来事の配送工程」として役割を限定する |

特に `Handler` という語は曖昧なので避ける。書き手は責務ごとに語を分ける。

- Event を別の値へ変換するなら「解釈」
- Event を複数の読み手へ渡すなら「配送」
- 要求を外部APIで実行するなら「実行」
- 状態を更新するなら「遷移」

### 4.4 Event生成の法則(E1〜E8)

#### 法則E1: 過去の事実

`Mイベント` は、既に成立した事実だけを表す。

#### 法則E2: 成立していない事実を生成しない

イベントは、その遷移によって実際に成立した場合だけ生成する。

悪い例:

```text
コマンド = 歩く
→ 毎刻み「歩行を開始した」を生成
```

正しい判定:

```text
前状態 = 非歩行
次状態 = 歩行
→ 「歩行を開始した」
```

したがって「開始した」「終了した」「変化した」のような状態変化イベントは、前状態と次状態の差を表現できることが生成条件になる。

#### 法則E3: 状態正本へ混ぜない

イベントは次回Updateへ持ち越す状態正本ではない。

```rust
struct 状態 {
    歩行開始イベントが起きた: bool,
}
```

のように「一度起きた出来事」をリセット前提のフラグとして状態へ埋め込まない。継続して成立する事実なら、それはイベントではなく状態として表す。

#### 法則E4: 消費されても事実は変わらない

消費側はイベントを変更しない。複数の消費者が同じ事実を読める。

#### 法則E5: 配送は意味を作らない

配送工程はイベントの意味を決めない。意味は Event 型の定義と生成元の遷移が決める。

#### 法則E6: 副作用は遷移から分離する

純粋なドメイン遷移は音響・描画・ネットワーク等を直接実行しない。

```text
遷移
→ Mイベント
→ 解釈
→ 要求
→ 外部境界で実行
```

と分ける。

#### 法則E7: 隠れた購読表を作らない

どの Event を誰が読むかは、可能な限りコンポジションルートまたは明示された工程のコードから追える形にする。任意の場所から `subscribe(TypeId, callback)` して依存関係を隠す汎用 EventBus を基本形にしない。

#### 法則E8: Eventから同じ状態への暗黙フィードバックを作らない

その遷移自身が知っている事実によって同じ状態をさらに変える必要があるなら、原則として最初の遷移の中で状態を確定する。別ドメインへ影響するときは、

```text
Mイベント
→ 解釈
→ 相手ドメインのMコマンド
→ 相手のUpdate
```

と明示する。これにより Event の連鎖で同一tick内に何度状態が変わるか分からない構造を避ける。

### 4.5 型・関数の文法

Event 型は次の形で書く。`struct` / `enum` は意味に応じて選ぶ。

```rust
#[derive(Clone, ...)]
pub struct / enum 何かの出来事 { ... }

impl M不変データ for 何かの出来事 {}
impl Mイベント for 何かの出来事 {}
```

`Mイベント: M不変データ` であるため、`impl Mイベント for` だけではコンパイルできず、`impl M不変データ for` も要る(他のデータの役割 `M状態`・`Mコマンド`・`M規則`・`M入力` も同じ)。

遷移は次の形で書く。遷移は成功のとき「次の状態」と「今回成立した出来事」を同時に確定し、失敗のとき失敗の値だけを返す(3.5.9)。

```rust
状態.〜を遷移する(
    &コマンド,
    &規則,
    遷移パラメータ,
) -> Result<遷移成功結果<状態, 出来事>, 失敗>
```

失敗を持たない遷移は `Result` を使わなくてよい。失敗を型で返すかは各ドメインが決める。Esca第1段階の遷移パラメータは `経過時間` だけを持つ。

解釈は、主語が自然なら次のように書ける。

```rust
出来事.音響要求へ解釈する()
出来事.演出要求へ解釈する()
```

ただし1つの Event 型へ各サブシステム固有の知識を集めて依存を逆流させる場合は、消費側の型に次のように置く。配置は「どちらがどちらを知ってよいか」という依存方向で決める。

```rust
音響方針.出来事を音響要求へ解釈する(&出来事)
```

配送は、中央の汎用 EventBus ではなく、その局面で必要な配送を明示する。

```text
一刻みを進める
  → 遷移成功結果を得る
  → 出来事一覧を各解釈工程へ渡す
  → 要求を各外部境界へ渡す
  → 次の状態を正本へ採用する
```

順序に意味がある場合は「購読登録順」に依存させず、工程の順序として名前を与える。

### 4.6 機械判定すること・しないこと

現在の `cargo xtask conform` は、次を法則として維持する。

- `Mイベント: M不変データ`(コンパイラが強制する。`Clone` は `M不変データ` から継承する)
- 参照を持たない
- 生ポインタを持たない
- 内部可変性を持たない
- 固有 `impl` に `&mut self` を持たない(`M不変データ` を実装する型すべてに課す)

参照・生ポインタ・内部可変性の検査は、`M不変データ` と役割(`Mイベント`・`M規則`・`Mコマンド` 等)の実装から型の定義をたどり、その定義に対して行う。`Mイベント: M不変データ` 等の supertrait の Relation はコンパイラが保証し、`impl M不変データ for` の置き場所は問わない。`&mut self` が無いだけでは `RefCell` 等を通じて `&self` から書き換えられ、読み取り専用で与えられる規則値や純粋計算という定義に反するためである。

さらに検査器で型を正確に同定できるようになった後は、次を候補にする。これらは「現在・未来・過去」の役割を同じ型へ重ねないための直交の法則である。

- 同じ具体型が `M状態` と `Mイベント` を同時に実装しない
- 同じ具体型が `Mコマンド` と `Mイベント` を同時に実装しない
- 同じ具体型が `M入力` と `Mイベント` を同時に実装しない

一方、次は一般の構文検査では判定しない。これらは意味論または具体ドメインの配線だからである。

- イベントが本当に成立した事実か
- 開始/終了イベントが状態差からだけ生成されているか
- 名前が日本語の過去形か
- 必ず1フレームで消えるか
- enumであるか
- Event がどの消費者へ配送されるべきか

### 4.7 コメント / rustdoc の文法

Event のコメントは「何の通知か」ではなく、どの事実が成立した値なのかを書く。

悪い例:

```rust
/// 歩行開始時に使うイベント。
歩行を開始した,
```

良い例:

```rust
/// 非歩行状態から歩行状態へ遷移した事実。歩行中の各刻みを表すものではない。
歩行を開始した,
```

```rust
/// 移動先が空間制約を越え、実際の移動位置が制限された事実。
障害物に遮られた,
```

特に状態変化イベントは、前に何であり、次に何になったとき、この事実が成立するかがコメントから分かるようにする。

消費先(SEを鳴らす、エフェクトを出す等)は Event 自身の意味ではないので、書き手は Event 型の定義コメントには原則書かない。書き手はそれを消費側の「解釈」に書く。

### 4.8 Marker / Relation / Law としての位置付け

```text
Marker
  Mイベント
    = 過去の事実として解釈する型

Relation
  M遷移関数
    produces -> 遷移成功結果<状態, イベント>

  出来事の解釈
    consumes -> Mイベント
    produces -> 要求 / 他ドメインのMコマンド / 出来事の保存(ログ・リプレイ・監査履歴など)

Law
  E1 過去の事実
  E2 成立した事実だけ生成
  E3 状態正本へ混ぜない
  E4 消費側は事実を変更しない
  E5 配送は意味を作らない
  E6 副作用を遷移から分離
  E7 隠れた購読表を基本形にしない
  E8 同一状態への暗黙Eventフィードバックを作らない
```

この形なら Observer パターンと同様に、読み手は `Mイベント` を見た時点で「この型が何であり、どこから生まれ、どう流れ、どう書いてはいけないか」までコード文法として共有できる。

## 5. 機械強制の手段

依存の向きは `xtask/src/conform/dependency_whitelist/ledger.rs` の白リストが機械で強制する。`blitz_design` の行は依存ゼロであり、`blitz_sim` と `blitz_esca` の行が `blitz_design` を許す。

法則の検査主体は2つに分かれる。

- コンパイラが強制する法則: データの役割(`M状態`・`Mコマンド`・`Mイベント`・`M規則`・`M入力`)の `M不変データ` 実装(上位トレイト)、`M不変データ` の `Clone`(上位トレイト)、存在の分類の上位トレイト(`M値オブジェクト` の `PartialEq`、`Mエンティティ識別子` の `Eq`・`Hash`)と `Mエンティティ` の関連型と識別子のメソッドの存在(2.5節)、関数の役割の型引数の境界、`遷移成功結果` の型引数の `M状態`・`Mイベント` の境界(構造体の境界)。型エイリアスの境界は Rust の仕様上強制されない(rust-lang/rust#112792)ため、関数の役割(`M射影関数`・`M解釈関数`・`M遷移関数`)は境界付きの newtype であり、型を使った時点でコンパイラが型引数の境界を強制する。迂回できる書き方は無い。利用側は `const _: M遷移関数<...> = M遷移関数::生成する(旅行者の現在地::歩行を遷移する);` の静的宣言でコンパイラに確かめさせる。
- `cargo xtask conform` の設計オントロジーの検査(`xtask/src/conform/design_ontology.rs`)が見る法則: `M不変データ` またはデータの役割を実装する型の定義に参照・生ポインタ・内部可変性が無いこと(役割の実装から型の定義をたどってその定義に当てる。`Mコマンド: M不変データ` 等の supertrait の Relation はコンパイラが保証し、`impl M不変データ for` の置き場所は法則にしない)、`Mコマンド` が `enum` であること、`M規則` が `struct` であること、`M不変データ` を実装する型(データの役割を実装する型をすべて含む)の固有の `impl` とトレイトの実装に `&mut self` が無いこと(2026-09-20 のオーナー裁定。それまでは `Mコマンド`・`Mイベント`・`M規則` と、同日に共通のマーカーから外した設定の役割だけに課していた)、`MParameter` を実装する型の定義に `Option<` のフィールドが無いこと(2.6.1)、同じ型が同じ軸の排他の分類(`M不変エンティティ` と `M可変エンティティ`、`MParameter` と `MOptions`)を同時に名乗っていないこと(2.5.1・2.6)。定義が見つからない型は違反として報告する。

検査器の保証範囲は次のとおりである。検査は Rust の型意味論でなく構文パターン(`impl トレイト for 型` の行と `struct`/`enum` の定義ブロック)に対して行う。型の同一性は「標準的なファイル配置から推定したモジュールパス + 型名」である(`crates/<名前>/src/lib.rs` は `<名前>`、`src/a/b.rs` は `<名前>::a::b`、`src/a/mod.rs` は `<名前>::a`)。`impl トレイト for 型` の行がどの定義を指すかは、実装と同じファイルの定義、無ければそのファイルの `use` 行(`crate::`・`super::`・`self::` の置き換えと1段の波括弧の群)から取り込み元のモジュールパスを求めて、そのモジュールパスの定義で決める。`use` が無ければ全体で1つだけの同名の定義を採り、2つ以上あれば一意に決まらない違反にする。同じファイルに同名の定義が複数あるとき(波括弧付きのモジュール `mod 名 { ... }` で分けた場合)も一意に決まらないとして違反にする(黙って最初の1つを選ばない)。実装対象の型を `use ... as` の別名で取り込んだ実装は取り込み元を求められない違反にする(黙って通さない)。固有の `impl` とトレイトの実装の `&mut self` の探索も同じ規則で定義に属するファイルだけを見るため、同じクレートの別のモジュールの同名の型の `impl` を誤って数えない(CLAUDE.md の切り出しの根拠義務が `impl` を別ファイルへ置くことを許すため、同じファイルだけに限定しない)。入れ子の波括弧の `use`、glob の取り込み、ジェネリックな `impl`、フィールドの型の中に間接的に含まれる内部可変性、`#[path = "..."] mod` によるファイルとモジュールの不一致は保証範囲の外である。`#[path` の属性については、`crates` 配下の `src` に現れること自体を conform が違反にする。

設計解釈マーカーの実装の形は正規形へ固定する(PR #138 の第6回レビュー(2026-09-20)の必須1と必須3への対応)。設計解釈マーカーの実装は `impl マーカー名 for 型` または `impl blitz_design::マーカー名 for 型` の形に固定する。`blitz_design` を別名で取り込むこと(`use blitz_design as 別名;`・`use blitz_design::M不変データ as 別名;`・`use blitz_design::{M不変データ as 別名};` のいずれも)は conform が違反にする。別名で取り込むと conform が実装を認識できず、法則を破れる合法な Rust を黙って通すためである。別名を使わずに正規形を迂回する書き方も conform が違反にする(PR #138 の第7回レビュー(2026-09-20)の必須1への対応)。迂回する書き方とは、再公開の経由(`mod design_alias { pub use blitz_design::M不変データ; }` と書いてから `impl design_alias::M不変データ for 型`)、絶対パス(`impl ::blitz_design::M不変データ for 型`)、パスの中の空白(`impl blitz_design :: M不変データ for 型`)の3つである。conform は、トレイトを書く位置に設計解釈マーカーの名前が識別子として現れる `impl ... for ...` の行のうち、その位置が正確に `マーカー名` または `blitz_design::マーカー名` でない行をすべて違反にし、あわせて `blitz_design` の外のファイルが `blitz_design` を公開の `use`(`pub use`・`pub(crate) use` など)で再公開することを違反にする(`blitz_design` 自身の `lib.rs` が自分のモジュールを再公開するのは対象でない)。マーカーの実装をファイルの中の `mod 名 { ... }` の中へ置くことも conform が違反にする(構文検査は型の定義をファイル単位で探すため、同じファイルの別の `mod` の同名の型と取り違える)。試験の `mod tests { ... }` は、その中にマーカーの実装を書かない限り違反にならない。
