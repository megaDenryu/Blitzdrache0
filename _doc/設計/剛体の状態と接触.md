# 剛体の状態と接触

変形しない動的物体(剛体)が何を状態として持ち、どう時間発展し、世界の形や他の剛体と触れたときどう運動するかの設計正本である。Issue #7「物理基盤: 剛体の状態・運動・接触解法」のオーナー裁定(2026-08-28の設計ノート19節と裁定候補18項、2026-09-01の接触解法の要求10項・XPBD共通拘束基盤へ載せる具体契約12項・実装境界の追記)を正本化し、Issue #5の横断裁定(2026-09-01。#7〜#11の拘束数学をXPBD中心へ統一する)とIssue #33の実装ロードマップ(子IssueはIssue #39・#40・#41・#43)に接続する。数理契約の候補と根拠の全文はIssue #7の裁定材料コメント(2026-08-28)にある。

本文書は `_doc/設計/XPBD共通拘束基盤.md` の上に載り、`_doc/設計/世界の形と衝突基盤.md` を消費する。拘束の数学(拘束違反・勾配・コンプライアンス・ラグランジュ乗数・刻み幅)と一刻みの工程の順序はXPBD共通拘束基盤が正本であり、本文書はそれを剛体へ適用したときに剛体の側で決まる事項だけを持つ。接触点集合・連続衝突・動く形の空間索引の幾何の契約は世界の形と衝突基盤が正本であり、本文書はそれをどう受け取りどう使うかだけを持つ。

用語は `_doc/計画/ユビキタス言語.md`「XPBD共通拘束基盤の語彙」「世界の形と衝突の語彙」「時間の規律の語彙」に従う。本文書が新しく導入する語は「用語」の節に定義し、制定時にユビキタス言語へ「剛体の語彙」として登録する。

## 目的

Issue #7が成立させたい現象は、箱が落ちて床に止まる・球が転がる・坂の上で静止または滑落する・箱を複数積める・物体の端を押すと回転する・重い物と軽い物の衝突結果が異なる・破片が大量に飛んで再衝突する、の7つである。本文書の目的は3つである。

1. 剛体という存在が持つ状態(配置・運動状態・質量特性・実行状態・運動種別)を、特殊値を使わない型として決め、ゲーム側が「力・衝撃・状態・材質」の意味だけで扱える公開面を決める
2. 剛体の姿勢自由度をXPBD共通拘束基盤の反復へ参加させる数学(予測・補正・速度の再構成)を、CPUの参照計算が持つ正典として決める
3. 世界の形と衝突基盤が返す幾何学的事実(接触点集合・接触の特徴の識別・初回接触時刻)を剛体の運動へ写す接触解法(非貫通・静止摩擦・動摩擦・反発・時間継続・接触島・休止)を決める

本文書は「箱」「車」「破片」というゲーム上の物体のAPIを定めない(Issue #7の非目標)。関節と駆動はIssue #8(実装はIssue #41)、破壊による構成の変化はIssue #16、質量特性と衝突形状の制作時の生成はIssue #19、キャラクターの移動の意味付けはIssue #3が持つ。

## 用語

本文書が新しく導入する語である。制定時にユビキタス言語へ「剛体の語彙」として登録する。以下の判断はこれらの語を定義済みとして使う。

- **剛体**とは、変形しない1つの動的物体のことである。識別子・配置・質量特性・運動種別・実行状態を持つ
- **運動種別**とは、剛体の運動を誰が決めるかの区別のことである。静的(物理計算中に配置が変わらない)・運動学的(外部が配置を決め、接触には速度を持つ物体として参加する)・動的(力・衝撃・接触・拘束で物理が運動を決める)の3枝であり、枝ごとに持つ状態が違う
- **配置**とは、剛体の重心の世界位置と姿勢の組のことである。動的剛体の物理位置の正本は重心である
- **姿勢**とは、剛体の重心局所座標からワールド座標への回転を表す単位四元数(3次元の回転を長さ1の4つの数で表す)の型のことである。生成が長さを検証し、演算の戻り値の生成が正規化を行う
- **質量特性**とは、質量・物体原点から見た重心・重心まわりの慣性(主慣性と主軸)の組のことである。衝突形状の属性ではなく剛体が別に持つ
- **主慣性と主軸**とは、重心まわりの慣性を対角化したときの3つの正の固有値と、その固有ベクトルが張る座標系の回転のことである。生成が正値と三角不等式を検査する
- **ジャイロ項**とは、剛体の角運動量が角速度と共に回転することで生じる角運動方程式の非線形な項 −ω × (I ω) のことである。外から回転力を加えなくても角速度の向きを変える
- **一刻みの作用の蓄積器**とは、動的剛体が1つの物理刻みの間だけ受け取る合力・合回転力・衝撃・回転衝撃の和のことである。工程2で取り出されて空になる
- **一般化逆質量**とは、剛体上の点に単位ベクトルの向きで作用する拘束について、逆質量に腕と向きの外積を逆慣性で挟んだ回転の寄与を足した量のことである。単位は毎キログラムであり、点自由度の逆質量と同じ式に載る
- **剛体接触拘束**とは、接触点集合の1点を剛体の運動へ写した非貫通の片側拘束のことである。参加者の組(剛体と剛体、または剛体と静的世界の接触相手)ごとに別のバッチ(同じ種類の拘束を密に並べた一覧)を持ち、細分ごとに生成して捨てる
- **静的世界の接触相手**とは、剛体と静的世界(高さ場の升目・建物の子形状・静的衝突メッシュの三角形)の接触で、剛体でない側を表す名前付きの参加者のことである。識別子(コンポジションルートが発行する全順序を持つ番号)・形(衝突数学層の型)・材質の識別子を持ち、配置も速度も質量特性も持たない
- **クーロン円錐**とは、接線方向の乗数の大きさが法線方向の乗数に摩擦係数を掛けた値以下でなければならないという制約 |λ_t| ≤ μ λ_n が、接線平面上の2次元の乗数の空間に作る円錐のことである
- **粘着の候補**とは、接触点集合1つの非貫通と静止摩擦を1回の混合連立として同時に解いた解のことである。クーロン円錐の内側なら配置と両方の乗数を一括で確定し、超えたら全部を捨てて非貫通だけを解き直す
- **錨**とは、接触が始まった細分で保存した両参加者の接触点(剛体なら重心局所の点、静的世界の接触相手なら世界位置)のことである。静止摩擦がそこからの接線変位を零へ戻し、クーロン円錐を超えたときに置き直す
- **表面物性**とは、静止摩擦係数・動摩擦係数・反発係数の組のことである。衝突形状の子形状が材質の識別子で参照する
- **混合則**とは、2つの表面物性から接触1つの接触物性を作る規則のことである。既定則(摩擦は相乗平均・反発は最大値)と材質対ごとの上書き表を持ち、可換である
- **接触の履歴**とは、参加者の組の識別子と接触の特徴の識別を鍵に刻みを跨いで保持する、錨と前の細分の接触法線のことである。参加者の組ごとに別の一覧を持ち、全順序の併走で対応付ける
- **静穏**とは、動的剛体の速度と角速度が休止の閾値以下で、全接触の貫通が接触余白以下で、錨が置き直されていない状態のことである。島の全剛体が連続して静穏である細分が閾値の個数続くと島が休止する
- **剛体の台帳**とは、剛体の生成と破棄・運動種別の遷移・作用の投入・配置と速度の読み出し・材質の設定を公開する操作サービスのことである。接触の履歴・島・休止・動く形の空間索引への登録を内部に持つ
- **連続衝突の方針**とは、剛体ごとに選ぶ、離散の接触だけ・掃引を使う・自動で昇格する、の3枝のことである

## 層の定義

| 層 | 置き場所 | 持つもの | 知ってよいこと |
|---|---|---|---|
| 剛体力学層 | `blitz_sim` の剛体のモジュール群(新設) | 剛体の状態の型・力学の作用の型・姿勢自由度の予測と補正と速度の再構成の数学・接触拘束と摩擦と反発のCPUの参照計算・接触の履歴・接触島・休止の判定・剛体の台帳(公開面)・GPUレイアウトの契約 | `blitz_math`・`blitz_collision`・`thiserror`。世界(チャンク・ストリーミング)も描画も知らない |
| 衝突数学層 | `blitz_collision`(既存) | 接触点集合・連続衝突・動く形の空間索引・形状の表現(既存のまま)。接触の特徴の識別への全順序の導出を足す | 既存のとおり |
| 世界側 | `blitz_engine`(既存) | 静的世界の候補の選別(既存) | 既存のとおり。剛体を知らない |
| コンポジションルート | `blitz_app` の物理の配線(新設) | 剛体の台帳・動く形の空間索引・静的世界の候補の選別を束ねて接触の候補を供給すること・静的世界の接触相手の識別子を発行すること・時間進行方針から細分数を注入すること・剛体の配置を描画の供給値(物体原点の変換)へ写すこと・ゲーム側からの作用を台帳へ渡すこと | `blitz_sim`・`blitz_collision`・`blitz_engine`・`blitz_render`・`blitz_game` |
| GPU実行 | `blitz_render`(既存) | 剛体のバッファ・接触拘束のコンピュートパイプライン(Issue #43のGPU経路以降) | `blitz_sim` のレイアウト契約と工程の宣言 |

依存の向きは `blitz_math ← blitz_collision ← blitz_sim ← blitz_render` と `{ blitz_sim, blitz_engine } ← blitz_app` である。`blitz_sim` が `blitz_collision` へ依存する1本は現在の `Cargo.toml` に無く、判断1で足す。世界の形と衝突基盤「層の定義」が依存の向きを `blitz_math ← blitz_collision ← { blitz_engine, blitz_sim, blitz_asset_compiler }` と既に定めており、その未反映分である。機械強制はクレート境界と `cargo xtask conform` のクレート依存の白リストである。

## 記法

本文書の数式は次の記法で書く。すべて3次元であり、断りが無ければベクトルはワールド座標の成分で書く。

- x ∈ R³ [m] は重心の世界位置、q ∈ S³ は姿勢、R(q) ∈ SO(3) はqが表す回転行列(重心局所座標からワールド座標へ)である
- v ∈ R³ [m/s] は重心の並進速度、ω ∈ R³ [rad/s] は角速度である
- m ∈ (0, ∞) [kg] は質量、w = 1/m [1/kg] は逆質量である
- J = diag(J₁, J₂, J₃)(各J_k ∈ (0, ∞) [kg·m²])は主慣性、P ∈ SO(3) は主軸(主軸座標から重心局所座標への回転)である。重心局所座標の慣性は I_ℓ = P J Pᵀ、その逆は I_ℓ⁻¹ = P J⁻¹ Pᵀ、ワールド座標の逆慣性は I⁻¹ = R(q) I_ℓ⁻¹ R(q)ᵀ であり、いずれも対称正定値の3×3行列である(単位は [kg·m²] と [1/(kg·m²)])
- h [s] は細分1本の刻み幅(基本刻み H = 1/60秒を整数nで割った長さ。判断19)である
- r̄ ∈ R³ [m] は剛体の重心局所座標で表した点、p = x + R(q) r̄ はその世界位置、r = p − x はその腕(重心から見た世界の変位)である
- 接触では第1の参加者をA、第2の参加者をBとし、n ∈ S² は共有の単位法線(AからBへ向かう)である。Bが静的世界の接触相手のときは p_B が世界に固定された点である
- C [m] または [rad] は拘束違反、λ [kg·m] または [kg·m²] はラグランジュ乗数、α̃ [1/kg] または [1/(kg·m²)] はコンプライアンスの刻み依存量(α ÷ h²)である。式はXPBD共通拘束基盤の判断1のとおり Δλ = (−C − α̃λ) ÷ (有効逆質量 + α̃) である
- vec(q) は四元数のベクトル部、re(q) は実部、[a]× は a との外積を表す3×3行列である

## 主要な設計判断

### 判断1: 剛体力学は `blitz_sim` に置き、`blitz_sim` が `blitz_collision` へ依存する

剛体の状態・作用・姿勢自由度の数学・接触解法のCPUの参照計算・接触の履歴・接触島・休止・剛体の台帳・GPUレイアウトの契約を `blitz_sim` の剛体のモジュール群へ置く。`blitz_sim` の `Cargo.toml` へ `blitz_collision` の依存を足し、接触点集合の型を `blitz_sim` が直接読む。接触の候補(どの剛体の対と、どの静的世界の接触相手を詳細に調べるか)を作るのは `blitz_app` の物理の配線であり、`blitz_sim` は候補の供給を受けて接触点集合を作り拘束へ写す。

Issue #8の実装境界(2026-09-01)が `blitz_sim` を「点自由度と姿勢自由度の数学・拘束族ごとの型とCPUの参照計算・GPUレイアウト契約」の置き場と定め、Issue #39の完了条件が「`blitz_sim` に姿勢を持つXPBD参加者の参照状態と計算がある」と書いている。接触点集合を `blitz_sim` が読まないと、接触拘束の型を `blitz_sim` に置けないか、接触点の型の写しを2つ持つことになる。世界の形と衝突基盤の判断4は「既存の `blitz_sim` の布の衝突計算(カプセル)は剛体の着工時に `blitz_collision` の形状型へ揃える」と定めており、この依存の追加はその判断の実行でもある。

捨てた案は3つある。新クレート `blitz_dynamics`(Issue #7の2026-08-28のコメントの案A)は、剛体が `blitz_sim` のXPBDの正典式・拘束グラフ・GPUレイアウト契約をそのまま使う以上、`blitz_sim` へ依存する薄いクレートになり、点自由度と姿勢自由度を同じ拘束へ参加させる型(判断10)が2クレートに割れる。`blitz_collision` の内部モジュールに置く案は、「衝突数学は幾何学的事実まで」という同基盤の判断10に反する。`blitz_sim` が接触点集合を知らず `blitz_app` の配線が接触拘束のデータへ写す案は、写す責務が配線へ漏れて剛体接触のCPUの参照計算を `blitz_sim` の検査で閉じられない。

機械で強制する方法は次のとおりである。`cargo xtask conform` のクレート依存の白リストへ `blitz_sim → blitz_collision` を足す。`blitz_sim` は引き続き `blitz_engine`・`blitz_render`・`blitz_app` を知らず、白リストがそれを課す。剛体のモジュール群は `blitz_sim` の中で `rigid_body/`(状態と作用)・`rigid_xpbd/`(姿勢自由度の数学)・`contact/`(接触拘束・履歴・島・休止)に分け、`blitz_collision` を読むのは `contact/` の生成の工程だけにする(モジュールの可視性で他のモジュールから `blitz_collision` の型を見せない)。

実装の細目(2026-09-03、Issue #40): `blitz_sim` の `Cargo.toml` へ `blitz_collision` を足し、`cargo xtask conform` のクレート依存の白リストと `_doc/設計/XPBD共通拘束基盤.md` の層の表を同じ変更で更新した(白リストは二重台帳であり、片方だけでは検査が落ちる)。剛体のモジュール群は `rigid_body/`・`rigid_xpbd/`・`contact/` の3つであり、`blitz_collision` の型を読むのは `contact/` だけである。判断5の機械強制である単精度の3つ組の宣言の禁止へ `contact/` を足し、他のクレートに同じ名前のディレクトリが現れても巻き込まないようクレートの名前も見る形にした。

### 判断2: 剛体の状態は4群に分け、運動種別は状態を持つ枝の列挙で表す

1つの剛体は識別子と次の4群を持つ。配置(判断3)、質量特性(判断4)、運動(運動種別の枝)、実行状態(起きているか休止しているか。判断18)である。運動種別は3枝の列挙であり、枝ごとに持つ状態が違う。

```text
運動種別
├─ 静的            持つ状態: なし(速度を持たない)
├─ 運動学的        持つ状態: この刻みの並進速度と角速度(外から与えた次の配置から導く。判断7)
└─ 動的            持つ状態: 並進速度・角速度・一刻みの作用の蓄積器(判断6)・休止の判定に使う静穏の連続細分数(判断18)
```

Issue #7の裁定候補1・2・8のとおり、静的を「質量∞の動的」、運動学的を「毎刻み瞬間移動する動的」という特殊値で表さない。静的が速度の欄を持たないことを型で表すと、「静的なのに速度が残っている」状態が作れない。運動学的が速度を持つのは、裁定候補3(運動学的剛体も速度を持つ物体として接触へ参加する)のためである。質量特性を運動種別の外に置くのは、裁定候補7と8(質量特性は衝突形状と分離し、種別を質量で表さない)と、種別が遷移しても質量特性は変わらないためである。解法が静的と運動学的を「動かさない自由度」として実効の逆質量0で扱うのは導出であって正本ではない(XPBD共通拘束基盤の判断3)。

4群を1つの構造体へ平らに並べる案は、静的な剛体が速度と作用の蓄積器を持つことになり、「静的は速度を持たない」を運用で守ることになるため捨てる。運動種別を状態を持たない列挙にして速度を全種別が持つ案も同じ理由で捨てる。

機械で強制する方法として、運動種別の列挙の枝がデータを持ち、速度の読み出しは `Option` でなく「動的または運動学的の速度」という枝の一致で取り出す(静的からは取り出せない)。実効の逆質量と逆慣性は解法の内部の関数が運動種別から導き、剛体の型に「逆質量0」を書き込む口を持たない。

### 判断3: 配置は重心の世界位置と単位四元数の姿勢であり、正規化は姿勢の型が守る

動的剛体の物理位置の正本は重心の世界位置 x である(裁定候補5)。姿勢 q は単位四元数を専用の型「姿勢」で持つ。「姿勢」は生成で長さを検証し(長さが1から1e-4以上ずれた値と非有限を型付きエラーで拒む)、積分と補正の演算はすべて新しい「姿勢」を返す形で書き、その戻り値の生成が正規化を行う。呼び出し側が正規化を忘れられる経路を作らない。物体原点の世界位置は x − R(q) c̄(c̄は物体原点から見た重心。判断4)で復元し、描画とゲーム側が読む物体原点の変換はこの復元の1箇所で作る。

裁定候補6(姿勢が数値誤差で回転でなくなる状態を許容しない)の実装形である。単位四元数は積分(判断8)が長さを1からずらすため、正規化の位置を型の生成に閉じるのが最も漏れが少ない。剛体の並進の運動方程式が重心について書かれるため、位置の正本を重心にすると腕 r = p − x が接触点の力の回転効果をそのまま与える。

回転行列(9つの数)は正規直交性の維持に3×3の再直交化が要り、GPUのバッファも四元数の16バイトに対して36バイトになるため捨てる。オイラー角は特異点(ある姿勢で1自由度が失われる)を持ち、角速度の積分に向かない。物体原点を位置の正本にする案は、並進と回転が連成した運動方程式になり、接触の腕を重心から測る接触点集合の設計(両形の上の点を持つ理由)と合わない。

機械で強制する方法として、「姿勢」型は `blitz_math` の `クォータニオン<ローカル, ワールド>` を包む新型であり、生の4成分を取り出す口はGPUのバイト列化と描画の変換の生成の2箇所だけにする。検査は、乱数の角速度で1万細分積分した後の長さが1から1e-5以内であること、および正規化を外した反証で長さが1から離れることを固定する。

### 判断4: 質量特性は質量・物体原点から見た重心・主慣性と主軸で持ち、生成で物理的な有効性を検証する

質量特性は3つの値を持つ。質量 m は `blitz_math` の `キログラム`(正の有限値。既存)である。物体原点から見た重心 c̄ は物体の局所座標の変位(判断5の型)である。重心まわりの慣性は主慣性 J = (J₁, J₂, J₃)(各値が正の有限の [kg·m²])と主軸 P(主軸座標から重心局所座標への回転。「姿勢」と同じ単位四元数の型)の組である。生成は J₁ + J₂ ≥ J₃・J₂ + J₃ ≥ J₁・J₃ + J₁ ≥ J₂ の三角不等式(等号は平面に分布した質量)を検査し、破れを型付きエラーで拒む。逆質量 w と逆主慣性 J⁻¹ は生成時に1度だけ導いて保持する。任意の対称3×3行列を受け取って対角化する仕事は制作時(Issue #19)に置き、実行時の型は対角化済みだけを受ける。質量特性は衝突形状の属性にせず、剛体が別に持つ(裁定候補7)。

裁定候補9(慣性は物理的に有効な質量分布に対応する値として検証できる)を、正値と三角不等式という3つの不等式だけで書けるのが対角化済みの形である。任意の3×3行列を実行時に受けると、対称性・正定値性・三角不等式の検査に固有値分解が要り、その数値誤差の扱いが実行時に入る。ワールド座標の逆慣性 I⁻¹ = R(q) P J⁻¹ Pᵀ R(q)ᵀ は姿勢が変わるたびに変わるため状態として持たず、使う場所で組む(行列積2回であり、接触1件あたりの費用の中で無視できる)。

3×3の対称行列を状態に持つ案は上の理由で捨てる。単一の慣性スカラーは方向依存を表せず裁定候補9に反する。ワールド座標の逆慣性を状態に持つ案は、姿勢が変わるたびに書き換える冗長な状態になる。

機械で強制する方法として、質量特性の型は私有フィールドと検証付きの `生成する()` だけを持ち、質量0・負・非有限、主慣性の0・負・非有限、三角不等式の破れがそれぞれのエラーの枝になることを検査で固定する。箱・球・カプセルの解析式(一様密度)から質量特性を作る関連関数を `blitz_sim` が持ち、Issue #19が焼き込む日までの初版の生成経路とする。

### 判断5: 力学の量は単位と座標系を持つ型で持ち、`blitz_math` へ足す

`blitz_math` へ次の型を足す。ベクトルの量は座標系の幻影型(`ワールド` と、剛体ごとの重心局所を表す `ローカル`)を持つ。速度 [m/s]・角速度 [rad/s]・力 [N]・回転力 [N·m]・衝撃 [N·s]・回転衝撃 [N·m·s] の6つのベクトル型と、慣性 [kg·m²] と逆慣性 [1/(kg·m²)] のスカラー型(主慣性の成分)である。次元の合成を演算子で書く。力 × 秒 = 衝撃、衝撃 × 逆質量 = 速度、速度 × 秒 = 変位、変位 × 力(外積)= 回転力、回転力 × 秒 = 回転衝撃、回転衝撃 × 逆慣性(ワールドの3×3)= 角速度、である。次元の合わない加算はコンパイルで落ちる。生の `f32` へ戻すのは `gpu_layout` のバイト列化と描画の変換の生成だけである。

グローバルCLAUDE.md「数学DDD」と、裁定候補11(力と衝撃を型で区別する)の実装形である。力と衝撃を同じ型の倍率違いにすると、ゲーム側が力を毎刻み加えるつもりで衝撃を加える誤りを型が止められない。現在の `blitz_math` は `メートル毎秒` と `ラジアン毎秒` をスカラーとして持つだけであり、ベクトルの速度型・角速度型・慣性型・力の型が1つも無い(2026-09-02時点の調査)。

`変位<ワールド>` を速度の代わりに使い「1刻みの変位」として持つ案は、細分の刻み幅が変わると意味が変わる量を状態にすることになり、細分数を品質方針として外から与える規律(判断19)と合わないため捨てる。慣性を `blitz_sim` 側の型にする案は、Issue #19の焼き込みが `blitz_math` の型で慣性を書けなくなる。

機械で強制する方法として、新型はすべて `#[repr(transparent)]` または3成分の私有フィールドの構造体であり、`値()` 以外に生の成分を返さない。`cargo xtask conform` の禁止文字列で、剛体のモジュール群の中の `f32` の3つ組(`[f32; 3]`)の宣言を落とす。

実装の細目(2026-09-02、Issue #39): `blitz_math` へ3成分の量として `速度`・`角速度`・`力`・`回転力`・`衝撃`・`回転衝撃`・`回転ベクトル`(空間の幻影型つき。`frame/`)と、その成分の単位 `ニュートン`・`ニュートンメートル`・`ニュートン秒`・`ニュートンメートル秒`・`キログラムメートル`(位置の拘束の乗数の単位)・`キログラム平方メートル`(慣性の成分)を足し、慣性を `慣性テンソル<空間>` と `逆慣性テンソル<空間>`(主軸と主慣性から組む3×3。行列は型の内側に閉じる)で持つ。次元の合成は演算子(力×秒=衝撃、衝撃×逆キログラム=速度、速度×秒=変位、変位÷秒=速度、回転力×秒=回転衝撃、角速度×秒=回転ベクトル、回転ベクトル÷秒=角速度)と型のメソッド(`変位::腕に掛かる力の回転力`、`逆慣性テンソル::回転衝撃を角速度へ写す`)で書いた。主軸の座標系は幻影型 `主軸` である。剛体のモジュール群(`rigid_body/`・`rigid_xpbd/`)の中の `[f32; 3]` の宣言は `cargo xtask conform` の `rigid_raw_triplet` が落とす。

### 判断6: 永続状態と一刻みの作用を分け、任意点への作用は剛体の側が回転力へ写す

動的剛体は一刻みの作用の蓄積器(合力 F [N]・合回転力 τ [N·m]・衝撃の和 P [N·s]・回転衝撃の和 L [N·m·s] の4つ)を持つ。蓄積器は物理刻みの工程2(ゲームからの介入の適用。XPBD共通拘束基盤の判断8)で所有ごと取り出され、取り出しと同時に空の蓄積器へ置き換わる。刻みの外で蓄積器に値が残っている状態は作れない。取り出した4つの量の使い方は2つに分かれる(判断19の細分の工程の中で行う)。合力と合回転力はその刻みの入力として保持され、n本の細分すべてで v ← v + h(g + w F)、ω ← ω + h I⁻¹ τ として積分される(n本の合計で H·F と H·τ の運動量の変化になる)。衝撃と回転衝撃は最初の細分の予測の前に1回だけ v ← v + w P、ω ← ω + I⁻¹ L として適用され、2本目以降の細分には効かない。ゲーム側が与える作用は次の6つの操作であり、任意点への作用から生じる回転力と回転衝撃は剛体の側のメソッドが腕の外積で計算する。

```text
重心へ力を加える(力)                  F += 力
重心へ衝撃を加える(衝撃)              P += 衝撃
回転力を加える(回転力)                τ += 回転力
回転衝撃を加える(回転衝撃)            L += 回転衝撃
点へ力を加える(作用点, 力)            F += 力、τ += r × 力
点へ衝撃を加える(作用点, 衝撃)        P += 衝撃、L += r × 衝撃
```

作用点は「世界位置」と「重心局所の点」の2枝の列挙であり、腕 r は世界位置なら 世界位置 − x、局所の点なら R(q) r̄ である。腕の計算は作用点の型のメソッドであり、呼び出し側は腕を計算しない。

裁定候補10(一刻みの作用が意図せず次の刻みへ残らない)・11(力と衝撃を区別する)・12(任意位置への作用。ゲーム側が回転力を手計算しない)の実装形である。力は時間発展を通じて運動量を変え、衝撃は瞬間に運動量を変えるため、同じ蓄積器から取り出しても細分への配り方が違う。力を全細分で積分するのは、細分数を変えても1刻みで与える運動量の変化が H·F のまま変わらないためであり、衝撃を最初の細分だけに与えるのは、n本の細分で n 回加えると n 倍になるためである。

力を「持続する作用」として刻みを跨いで保持し解除の操作を持つ案は、解除忘れが永続状態へ混入するため捨てる。ゲーム側が毎刻み加え直す形が、蓄積器が刻みの中でだけ生きる規律と合う。衝撃を「1刻みの間だけ効く大きな力」として力の型で表す案は候補11に反する。衝撃を細分数で割って全細分へ配る案は、最初の細分で衝突の速度が立たず、反発の判定(判断14)が衝撃の直後の細分で衝突前の速度を読めない。

機械で強制する方法として、蓄積器は動的の枝だけが持ち(判断2)、静的と運動学的の剛体へ作用を投入する操作は型付きエラー(`静的な剛体へ作用を加えようとした` 等)を返す。工程2の取り出しは蓄積器を所有で受け取って「一刻みの入力」(保持する力と回転力)と「一度だけ適用する衝撃」の2つの型に分けて返し、後者は最初の細分が所有で消費するため2本目以降の細分へ渡せない。休止中の剛体へ作用を投入すると島が再開する(判断18)。

### 判断7: 運動種別の遷移は台帳の専用の操作であり、遷移後の状態は一意に決まる

運動種別の変更は剛体の台帳の3つの操作(`動的にする(初速の並進速度, 初速の角速度)`・`運動学的にする()`・`静的にする()`)で行い、次の安全な境界(物理刻みの工程1。構造変更の反映)で反映する。遷移が整合させるものは4つである。速度(動的へは引数の初速、運動学的へは前の刻みの配置との差から導いた速度、静的へは無し)・休止(動的へ遷移した剛体は起きた状態から始める)・接触の履歴(その剛体を含む履歴をすべて捨てる)・接触島(次の細分で組み直す)である。動く形の空間索引への所属は3種別とも変わらない(静的な剛体も索引に載り、動的剛体との対の列挙に現れる)。

運動学的剛体の速度の導出は、外から与えた次の配置 (x', q') と現在の配置 (x, q) から行う。並進は v = (x' − x) ÷ H である。回転は Δq = q' ⊗ q⁻¹(実部が負なら符号を反転して短い弧を取る)の対数写像 ω = (2 · atan2(|vec(Δq)|, re(Δq)) ÷ |vec(Δq)|) · vec(Δq) ÷ H である(|vec(Δq)| が1e-8未満なら ω = 0)。1刻みの回転量が π/2 [rad] を超える配置の指定は型付きエラーで拒む。回転の対数写像が半回転に近づくと不定になるためであり、その大きさの回転を1刻みで与える運動学的剛体は仕様の外である。運動学的剛体の細分の中の配置は、現在の配置から次の配置へ細分の番号の比で線形に(姿勢は最短の弧で)進める。

裁定候補4(運動種別の変更は正式な状態遷移として扱い、変更後の状態が一意に定まる)の実装形である。列挙の枝の書き換えだけにすると、静的から動的へ変わった剛体が速度も休止も持たない不定の状態になる。Issue #7の追加の論点2(運動学的の速度の導出式が未定義)は上の式で定める。

遷移を即時に反映する案は、拘束の反復の途中で自由度の集合が変わり、島と履歴が反復の途中で壊れるため捨てる(XPBD共通拘束基盤の判断8の工程1の理由と同じ)。運動学的剛体の回転量の上限を課さず対数写像の不定をそのまま許す案は、半回転の付近で角速度の向きが入力の丸めで反転する。

機械で強制する方法として、遷移の操作は台帳のメソッドであり、剛体の型に運動種別を直接書き換える口を置かない。遷移の予約は構造変更の待ち行列に積まれ、工程1だけがそれを取り出す。検査は、動的→静的→動的の往復で速度と履歴が引数どおりに初期化されること、運動学的の回転量の上限超過がエラーになること、を固定する。

### 判断8: 姿勢自由度の予測は速度・位置・角速度・姿勢の順で行い、ジャイロ項は陰的に解く

細分1本(刻み幅h)の予測を次の順で行う。前の状態(x_前, q_前)を保存し、並進は v ← v + h(g + w F)、x ← x + h v とする(g は重力加速度、F は判断6の一刻みの入力の合力)。回転は重心局所座標で角速度 ω_ℓ = R(q)ᵀ ω を取り、ジャイロ項を陰的に1段(ニュートン法1回)解く。

```text
f(ω₁) = I_ℓ (ω₁ − ω₀) + h · ω₁ × (I_ℓ ω₁)                  解きたい式(f(ω₁) = 0。角運動量の保存を後退オイラー法で離散化した形)
Jf    = I_ℓ + h · ( [ω₀]× I_ℓ − [I_ℓ ω₀]× )               ω₀ でのヤコビ行列
ω₁    = ω₀ − Jf⁻¹ f(ω₀) = ω₀ − Jf⁻¹ (h · ω₀ × (I_ℓ ω₀))    ニュートン法1回
```

その後に回転力を陽的に足し(ω₁ ← ω₁ + h I_ℓ⁻¹ τ_ℓ。τ_ℓ = R(q)ᵀ τ)、ワールドへ戻して ω ← R(q) ω₁、姿勢を q ← q + (h/2)·(ω_四元数 ⊗ q)(ω_四元数 = (ω_x, ω_y, ω_z, 0))として正規化する(判断3の型の生成が行う)。運動学的剛体は判断7の細分の配置へ進み、静的剛体は何もしない。

細分の中で予測し、姿勢を1次の更新と正規化で進める形は、剛体XPBD(Müller・Macklin・Chentanez・Jeschke・Kim「Detailed Rigid Body Simulation with Extended Position Based Dynamics」2020年)の予測の形であり、細分の刻み幅を小さくするほうが反復を増やすより同じ計算量で誤差が小さいという結果(Macklinら「Small Steps in Physics Simulation」2019年)に基づく。ジャイロ項を陰的に解くことは、その論文の形ではなく本文書の数値方針である(同論文はニュートン・オイラー式の陽的な更新を細分で回す)。陰的に解くのは、陽的に足すと角速度が主慣性の逆数と刻み幅で決まる大きさを超えたときに発散し、省くと細長い物体の回転が中間軸で反転する現象(裁定候補18の「高速回転」)が再現されないためである。局所座標で解くのは I_ℓ が定数であるためであり、3×3の逆行列1回で済む。

この数値法が保証する不変条件は次の2つに限る。有限の刻み幅で角運動量の大きさが増えないことも保存されることも保証しない。後退オイラー法そのものが数値散逸を持ち、収束させないニュートン法1回には単調性の一般の保証が無いためである。保証するのは、(a)発散しないこと(外力なしの自由回転で角運動量の大きさが初期値の (1 + ε) を越えない。ε は暫定値であり実測から置く)と、(b)散逸が刻み幅とともに減ること(刻み幅を半分にしたとき、同じ実時間の後の角運動量の大きさの減少量が減る)である。角運動量を保存する回転積分(暗黙の中点法を細分ごとに収束まで反復する形)は採らない。細分ごとに3×3の非線形の反復を収束まで回す費用と決定性の検証が増えるうえ、接触の位置拘束が毎細分の反復で角速度を書き換えるため、回転積分の保存性は接触している剛体では成り立たず、自由回転の見た目の忠実さは陰的1段の散逸を細分数で下げることで足りるからである。

陽的なジャイロ項は上の理由で捨てる。ジャイロ項の省略は、剛体の回転のエネルギーが保存されず、角運動量の向きが姿勢に依らず固定されるという物理的に誤った挙動になる。姿勢の積分に指数写像(回転ベクトルから四元数を作る)を使う案は三角関数が細分ごとに要り、1次の更新と正規化で同じ次数の精度が出るため採らない。

機械で強制する方法として、予測の関数は「前の状態」と「予測の状態」を別の型で返し、拘束の補正は予測の型だけに掛かる。検査は4つであり、いずれも主慣性 (1, 2, 3) [kg·m²]・細分の刻み幅 h = 1/480秒(n = 8)・外力なしで行う。(1)発散しないこと: 主軸の外の初期角速度 (3, 7, 5) [rad/s] で600細分進めた後のワールド座標の角運動量 R(q) I_ℓ R(q)ᵀ ω の大きさが初期値の (1 + ε) を越えないこと(ε の暫定値は1e-2であり、実装時の実測で置き直す)。(2)散逸が刻み幅とともに減ること: 同じ初期値で h を半分(1/960秒)にして同じ実時間(1.25秒)を進めたとき、角運動量の大きさの減少量が h = 1/480秒のときより小さいこと。(3)中間軸の反転: 初期角速度 (0.01, 10, 0) [rad/s](中間軸に小さい非零の摂動を乗せた値。摂動が厳密に0なら厳密解でも反転しないため摂動を明記する)で角速度の中間軸の成分の符号が有限の細分数で反転すること、およびジャイロ項を外した反証では反転しないこと。(4)陽的なジャイロ項の発散の反証: 主軸の外の初期角速度 (0, 200, 150) [rad/s](主軸上の初期値では ω × (I_ℓ ω) = 0 が厳密に成り立ち陽的でも変わらないため、2成分を非零にする)でジャイロ項を陽的に足した更新を100細分進めると、100細分以内のいずれかの細分で角運動量の大きさが初期値の2倍を越えるか非有限になること(各細分で検査し、最初に越えた時点で反証の成立とする。陽的更新は倍精度で17細分目に2倍を越え、その後に非数へ発散するため、最後の値だけを比べると非数との比較が偽になって反証が落ちる)、および陰的1段では同じ初期値と細分数で (1 + ε) を越えないこと(倍精度の見積りで100細分後の比は約0.85)。

実装の細目(2026-09-02、Issue #39): `blitz_sim::rigid_xpbd::細分の予測器`(刻み幅と重力とジャイロ項の扱いを保持)の `動的剛体を予測する` が上の順で `前の状態` と `予測の状態` と予測の運動状態を返し、`ジャイロ項の扱い` は本番では `陰的に一段解く` の1枝だけを持つ(陽的に足す・省くは検査のときだけ枝が在る)。ニュートン法1回は `blitz_math::慣性テンソル::ジャイロ項を陰的に一段解く` が持つ。検査の実測(`_doc/計測/剛体の姿勢自由度_2026-09-02.md`)は次のとおりであり、ε の暫定値1e-2はそのまま置く(増加は無い)。(1)(3, 7, 5)[rad/s]の600細分で角運動量の比は0.960(途中の最大は0.99994。減少側に4.0パーセント)。(2)刻み幅1/960秒の1200細分では比0.979であり、減少量は半分になった。(3)(0.01, 10, 0)は699細分目に局所の中間軸の成分の符号が反転し、省いた反証では4000細分まで反転しない。(4)予測の角速度をそのまま次の細分へ渡す経路で、陽的な更新は17細分目に2倍を越えた後に非数になり、陰的1段は100細分後に比0.851であった(上の見積り0.85と一致)。(4)は判断9の速度の再構成を通した全体の経路では観測できない。微小回転として読む再構成 ω = 2·vec(Δq) ÷ h は角速度の大きさを 2 ÷ h(刻み幅1/480秒で960 rad/s)に抑えるため、陽的な更新の発散が角運動量の比に現れない(全体の経路では陽的も陰的も100細分後の比が0.37〜0.39まで減る)。同じ抑えが(1)の散逸の主因でもあり、|ω| = 9.1 rad/s では1細分あたり (h|ω|/2)² ≈ 9e-5 の相対減少が積み重なる。この抑えは1次の形の性質であり、細分1本で半回転に近づく角速度は本設計の仕様の外である(判断7の運動学的の上限と同じ線)。

### 判断9: 拘束の勾配から並進と回転の補正を一般化逆質量で分配し、確定後に速度と角速度を再構成する

世界の点 p(腕 r = p − x)に勾配 n(単位ベクトル)で作用するスカラー拘束について、その剛体の一般化逆質量を w̃ = w + (r × n)ᵀ I⁻¹ (r × n) [1/kg] とする。回転だけに作用する拘束(軸 n のまわりの角度の拘束)では w̃ = nᵀ I⁻¹ n [1/(kg·m²)] である。参加する自由度の w̃ の和が有効逆質量であり、点自由度が参加するときはその逆質量(勾配の2乗は1)を足す。Δλ をXPBD共通拘束基盤の判断1の式で求め、剛体への補正は次のとおりである(符号は勾配の向きに従う。距離拘束の規約と同じく、aからbへ向かう n に対して a の勾配は −n である)。

```text
点に作用する拘束:   Δx = w · Δλ · n
                    Δq = (1/2) · [ I⁻¹ (r × (Δλ · n)) ]_四元数 ⊗ q      (正規化は姿勢の型が行う)
回転だけの拘束:     Δq = (1/2) · [ I⁻¹ (Δλ · n) ]_四元数 ⊗ q
静的・運動学的:     補正なし(実効の w̃ = 0 として有効逆質量に足さない)
```

細分の反復を終えた後、速度と角速度を確定した配置と前の状態の差から再構成する。v = (x − x_前) ÷ h、Δq = q ⊗ q_前⁻¹、ω = 2 · vec(Δq) ÷ h(Δq の実部が負なら符号を反転する。細分1本の回転は小さいため1次の形で足りる)。運動学的剛体は判断7の速度を持ち続け、再構成しない。角速度は再構成の結果を保持する永続状態であり(判断2)、次の細分の予測(判断8)と速度段階(判断14)と休止の判定(判断18)がそれを読む。

Issue #7の追加設計(2026-09-01)の「剛体接触の位置補正では回転も必ず反映する」の実装形であり、Issue #39の完了条件(逆慣性を含む姿勢補正)である。一般化逆質量は、点への力が回転を生む効果を「その点をその向きへ動かすのに要する仕事」として1つの逆質量に畳んだ量であり、点自由度の逆質量と同じ次元を持つため、点自由度と姿勢自由度を同じ Δλ の式に載せられる。速度を位置差から再構成するのは、位置拘束が変えた運動を速度へ反映するためであり、裁定候補6の「角速度は独立した運動状態」は「再構成の結果を永続状態として保持し、観測・介入・休止・速度段階がそれを読む」という意味で成立する(Issue #7の追加の論点2の裁定)。

速度を位置差から作らず位置と独立に発展させる案(速度基準の解法)は、Issue #5の横断裁定(位置と姿勢の補正という数学へ統一する)に反する。回転の補正を無視して並進だけ補正する案は、箱の角の接触で転倒が起きず、Issue #40の完了条件に反する。

機械で強制する方法として、補正の式は `blitz_sim::rigid_xpbd` のCPUの参照計算1箇所が持ち、GPUの写しは同じ符号・同じ演算順で書く(XPBD共通拘束基盤の判断1と同じ運用)。検査は2段に分ける。補正の適用の関数(Δλ を固定して受け、Δx と Δq を返す)については、重心へ作用する拘束(r = 0)で回転が生じないこと、質量を2倍にすると並進の補正が半分になり主慣性を2倍にすると回転の補正が半分になること(Δλ が同じなら補正は w と I⁻¹ に比例する)を固定する。一回の射影の全体(Δλ の分母に有効逆質量が入る)については上の比例は一般に成立しないため、剛体1つと世界固定点の目標点拘束(判断10)で解析的に求めた1回の補正と一致することを固定する。再構成した角速度は微小な回転で解析値と相対1e-4以内で一致することを固定する。

実装の細目(2026-09-02、Issue #39): 一般化逆質量は `姿勢自由度の参加者`(配置・逆質量・ワールドの逆慣性。静的と運動学的は `動かせない` で実効の重み0)の `一般化逆質量(腕, 向き)` と `回転だけの一般化逆質量(向き)` が求め、補正は逆質量とワールドの逆慣性を所有する参加者のメソッド `点に作用する乗数を補正へ分配する(腕, 向き, Δλ)` と `回転に作用する乗数を補正へ分配する(向き, Δλ)` が上の式で `姿勢自由度の補正` として作り、`予測の状態::補正を適用する` だけが予測へ足す。速度の再構成は `前の状態::確定した配置から速度を再構成する` が持つ。検査は2段のとおりであり、世界固定点の目標点拘束の1回の補正が解析解(腕 r = (1, 0, 0)、目標が y に d 離れたとき Δλ = −d ÷ w̃、Δx = w d ÷ w̃、Δθ = d ÷ (w̃ J₃))と単精度で一致した。補正の回転の軸は I⁻¹ n であって n ではないため、慣性が異方だと n のまわりの回転に加えて軸の傾きも変わる(判断10のねじり拘束の検査がこの性質を記録している)。回転だけの拘束の乗数とコンプライアンスは `回転のラグランジュ乗数`(キログラム平方メートル)と `回転のコンプライアンス`(毎ニュートンメートル)であり、点自由度の曲げの型と次元が同じでも逆慣性テンソルへ写す口を持つ側として別に持つ。

### 判断10: 点自由度と姿勢自由度は参加する自由度の組ごとの密なバッチで同じ拘束へ参加し、Issue #39は3つの拘束で検証する

拘束族は参加する自由度の型の組ごとに別の密なバッチを持つ(点と点・剛体と剛体・剛体と点・剛体と世界の点・剛体と静的世界の接触相手)。「自由度参照 { 種類, 添字 }」のような1つの列挙で複数の種類を指す型を作らない。Issue #39が最初に作る検証拘束は3つである。

```text
剛体の局所点を世界の目標点へ一致させる拘束(剛体と世界の点)
    C = |p − 目標|、n = (目標 − p) ÷ C、参加は剛体1つ(w̃ は判断9)。既存の目標拘束の姿勢自由度への一般化
2剛体の局所点どうしを一致させる拘束(剛体と剛体。接続拘束)
    C = |p_A − p_B|、n = (p_B − p_A) ÷ C、A の勾配は −n、B の勾配は +n。距離拘束の静止長0の形を姿勢自由度で解く
隣接する姿勢の相対ねじりを制約する拘束(剛体と剛体。回転だけ)
    q_相対 = q_A⁻¹ ⊗ q_B(A の重心局所座標で表した B の姿勢)、q_誤差 = q_相対 ⊗ q_静止⁻¹、Δ_A = 2 · vec(q_誤差)(A局所座標の回転ベクトル。実部が負なら反転)
    C = Δ_A · ā(ā は共有する軸を A局所座標で表した単位ベクトル。内積は両方ともA局所座標で取る)
    勾配の向きはワールドへ写した n = R(q_A) ā であり、A の勾配は −n、B の勾配は +n、w̃ は回転だけの形
```

剛体と点のバッチ(布の頂点を剛体の局所点へ接続する拘束・剛体の面と布の頂点の接触)は型と式だけをIssue #39で置き、実証はIssue #43(強結合)で行う。剛体と静的世界の接触相手のバッチは判断11が定める。

Issue #39の原則(粒子・剛体・棒を同じドメイン型にしない。共通なのは拘束が参加者の自由度へ補正を返す数学)と、Issue #18の実装境界(強結合用の拘束型は組合せごとに持ってよい)の実装形である。3つの検証拘束は、目標拘束(点自由度で実装済み)の姿勢自由度版・接続拘束(XPBD共通拘束基盤の判断6が用途が立った日に足すとした)・ねじり(Issue #9の棒の基礎)であり、Issue #41の蝶番と固定関節はこの3つの組合せで書ける。ねじりの拘束違反をA局所座標で閉じて計算し勾配だけをワールドへ写すのは、相対姿勢の回転ベクトルと軸を同じ座標系で内積するためである。

1つの列挙で点と剛体を指す案は、拘束の射影が毎回枝を分ける費用と、GPUのカーネルが両方の配列を読む複雑さを全拘束族へ課すため捨てる。ねじりの拘束を四元数の差の全成分(3自由度)で書く案は、蝶番のような軸まわりの自由を残す拘束へ再利用できない。

機械で強制する方法として、3つの拘束はそれぞれ `blitz_sim::rigid_xpbd` の型(引数・一刻みの係数・一回の射影の結果)を距離拘束と同じ様式で持つ。検査は、目標点拘束で剛体上の点が硬い接続(コンプライアンス0)で目標へ一致し柔らかい接続で解析解の定常の隔たり C = α g_有効 ÷ w̃ を満たすこと、接続拘束で2つの剛体の局所点が一致し逆質量と逆慣性の比で補正が分配されること、ねじり拘束で軸まわりの相対角が静止角へ収束し軸に垂直な回転が変わらないこと、ねじり拘束の結果が2剛体を同じ回転で回した配置で不変であること(座標系の取り違えの反証)、を固定する。

実装の細目(2026-09-02、Issue #39): 3つの拘束は `blitz_sim::rigid_xpbd` の `剛体の目標拘束の引数`・`接続拘束の引数`・`ねじり拘束の引数`(それぞれ一刻みの係数と一回の射影の結果を持つ)であり、剛体と点のバッチの式は `点と剛体の距離拘束の引数` が持つ(実証はIssue #43)。密なバッチは `剛体の参照計算` が種類ごとに持ち(`添字付き剛体の目標拘束`・`添字付き接続拘束`・`添字付きねじり拘束`・`添字付き点と剛体の距離拘束` を `剛体の拘束の一覧` で束ねて受ける)、反復の順序は目標→接続→ねじり→点との距離の逐次である。検査の結果は次のとおりである。目標点拘束は硬い接続で剛体上の点が目標へ1e-4メートル以内で一致し重心へ作用すれば姿勢が恒等のまま、柔らかい接続は準静的に C = α m g(重心へ作用する接続では w̃ = w)へ1e-5メートル以内で収束した。接続拘束は重心へ作用する1回の射影で0.4メートルの隔たりを逆質量の比3:1で0.3と0.1に分け、重心から離れた局所点どうしは1刻み(4細分×反復8回)で1e-4メートル以内に一致して慣性の小さい側が大きく回った。ねじり拘束は軸まわりの相対角が1e-4ラジアン以内で静止角へ収束し、静止の相対姿勢が恒等でなくても同じであり、2剛体を同じ回転(0.7, −1.1, 0.4)で回した配置で乗数と補正(全体の回転を戻した値)が1e-5以内で不変であった。「軸に垂直な回転が変わらない」は慣性が等方のときに厳密に成り立ち(軸の傾きが1e-4ラジアン以内で不変)、主慣性(1, 2, 3)の異方な剛体では補正の回転の軸 I⁻¹ n が n から傾くため軸の傾きが0.199から0.179ラジアンへ変わった(判断9の式の性質であり、蝶番のように垂直の成分も拘束する関節ではIssue #41の残りの拘束が受け持つ)。点自由度と姿勢自由度が同じ拘束へ参加する検査は、固定した点と剛体上の局所点を静止長0.5メートルで結び、重力で落ちた剛体が120刻み(8細分×反復4回)で距離を1e-3メートル以内に保ち、固定した点が動かないことを固定した。

### 判断11: 接触拘束は接触点集合から細分ごとに生成する一時のバッチであり、参加者の組ごとに別のバッチを持ち、座標系の写しは剛体の側が行う

剛体接触拘束のバッチは2つである。剛体と剛体の接触拘束と、剛体と静的世界の接触相手の接触拘束である。運動学的剛体と静的な剛体は剛体であり、前者のバッチに載る(判断9のとおり実効の重み0で解く)。静的世界の接触相手は剛体でないため、識別子・形・材質だけを持つ名前付きの型として後者のバッチの参加者になる。1点の接触拘束は次を持つ。

```text
剛体と剛体の接触拘束           剛体Aと剛体Bの識別子・A側とB側の局所接触点(各剛体の重心局所座標 r̄_A, r̄_B)・接触法線(ワールド。生成した細分の間は固定)・生成時の貫通量・接触の特徴の識別・接触物性(判断15)・法線のラグランジュ乗数
剛体と静的世界の接触拘束       剛体の識別子・静的世界の接触相手の識別子・剛体側の局所接触点 r̄_A・静的世界側の接触点の世界位置 p_B(固定)・接触法線・生成時の貫通量・接触の特徴の識別・接触物性・法線のラグランジュ乗数
```

接触点集合(世界の形と衝突基盤。最大8点)1つが1つの接触多様体であり、その全部の点を接触拘束として保ち、1点へ潰さない。接触点集合は形の局所座標(2つの形が共有する1つの座標系)で両形の上の点を返すため、剛体の側が2つの形をその座標系へ置き(初版はワールドを共有の座標系とし、倍精度の位置を単精度へ狭めるのは接触点を局所へ写す1箇所である)、返った点を各剛体の重心局所座標へ写す。静的世界の接触相手の点はワールドのまま持つ。生成の工程は細分ごとに次を行う(判断19)。

```text
索引の更新:     予測した細分の変位で伸ばした箱(細分の始点と終点の形を包む箱にゆとりの幅0.1メートルを足したもの)で動く形の空間索引を更新する
候補の列挙:     重なりうる形の対と、静的世界の候補(高さ場の升目・建物の直方体・静的衝突メッシュの三角形)を列挙する。休止している島どうしの対と、静的な剛体どうしの対は除く
接触点集合:     対ごとに予測した配置で接触点集合を作り、特徴の識別で履歴へ対応付け(判断16)、接触拘束の2つのバッチを作り直す
```

Issue #7の追加設計の第2節(接触拘束の正本データ)と実装境界(接触拘束は刻みごと生成する一時バッチ)の実装形である。参加者の組ごとにバッチを分けるのは判断10の規則に従うためであり、静的世界(三角形・建物・高さ場)には剛体の識別子も重心局所点も無いため、剛体Bを要求する1つの型へ潰せない。局所接触点で持つのは、細分の間に剛体が動いても現在の姿勢で貫通量を再評価できるようにするためであり(判断12)、接触点集合が両形の上の点を持つのはこのためであると同基盤が明記している。Issue #7の追加の論点5(接触点の座標系の写しの責務は#7側)をここで定める。細分ごとに索引を更新して候補を列挙するのは、索引のゆとりの幅0.1メートルが基本刻み全体の移動を覆わず、刻みの頭に1回の列挙では後の細分で新しく近づく相手を候補に含められないためである。細分の変位で伸ばした箱を登録することが保証するのは、「細分の終点で重なる対を候補から落とさない」ことだけである(箱の更新の遅れで、終点で重なっているのに候補に出ない事故を防ぐ)。細分の途中で触れて終点では通過し終えている対は、候補に入っても終点の配置で作る接触点集合が空になり、この工程では止まらない。細分の途中の衝突時刻を扱うのは判断20の連続衝突の方針であり、初版(Issue #40)ではその方針を「離散の接触だけ」で閉じる。細分ごとに接触点集合を作り直すのはIssue #18の実装境界(接触候補を刻みの最初に1度生成して終わりと固定しない。初版は細分の単位を基本とする)に従う。

接触点を世界座標だけで持つ案は、細分の間の剛体の回転で貫通量を再評価できないため捨てる(オーナーの実装境界が禁じている)。剛体と静的世界を「任意の参加者」の1つの列挙で束ねる案は判断10に反する。候補の列挙を基本刻みの頭に1回だけ行う案は上の理由で捨てる。接触点集合を刻みの頭に1回だけ作り細分では局所点の再評価だけを行う案(判断8の2020年の手法の形)は費用が小さいが、Issue #18の裁定に反し、変形体との接触へ広げたとき(Issue #43)に更新頻度が2系統になる。計測で細分ごとの生成が予算を圧迫したときに、剛体どうしの対だけを落とすことを候補として残す。

機械で強制する方法として、接触拘束のバッチは細分の開始で生成し細分の終了で捨てる型であり、刻みを跨いで持つ口が無い(履歴は判断16の別の型)。静的世界の接触相手の識別子は `blitz_sim` が定義する全順序を持つ新型であり、その発行は `blitz_app` の物理の配線が行う(動く形の識別子と同じ契約)。接触点の受け皿は接触点の上限8×対の数の固定長で確保を起こさず、上限超過は型付きエラーである。検査は、同じ配置から生成した接触拘束の局所点を世界へ戻すと接触点集合の点と1e-6メートル以内で一致すること、法線がAからBへ向くこと、1細分でゆとりの幅0.1メートルより長く動く剛体(h = 1/480秒で秒速60メートル。1細分の変位0.125メートル)が細分の終点で重なる相手が候補に入ること(伸ばした箱を外した反証では箱の更新の遅れで候補から落ちること)、を固定する。細分の途中で通り抜ける場合を止めることはこの検査の対象ではない。

実装の細目(2026-09-03、Issue #40): 2つのバッチは `剛体と剛体の接触拘束` と `剛体と静的世界の接触拘束` であり、`接触拘束の二つのバッチ` が両方を持つ。1件は静的な引数(`剛体と剛体の接触拘束の引数`・`剛体と静的世界の接触拘束の引数`。参加者2つ・接触点・接触法線・生成時の貫通量・特徴の識別・接触物性・非貫通の解き方)と、細分の刻み幅で導いた `非貫通の一刻みの係数` と、一細分内の状態である法線のラグランジュ乗数の3つに分けて持つ(XPBD共通拘束基盤の判断4)。乗数は生成で0から始め、暖機はしない(判断16)。生成は `接触拘束のバッチの組み立て` が混合則を保持して行い、`剛体どうしの接触点集合を積む` と `剛体と静的世界の接触点集合を積む` が接触点集合の全部の点を1件ずつ拘束にする。剛体の側の点は `接触に参加する剛体::接触点を重心局所へ写す` が r̄ = R(q)ᵀ (p − x) で写し、静的世界の側の点は `静的世界の接触相手::接触点の世界位置を求める` が世界位置のまま読む。初版の共有の座標系はワールドであり、倍精度から単精度へ狭めるのは接触点の位置・接触法線・貫通量の3つを写す1つのモジュールだけである。接触の特徴の識別は形の組ごとに別の型で返るため、剛体の側は `接触の特徴の識別`(直方体どうしと直方体と三角形の2枝)の列挙で受ける。剛体と静的世界のバッチには建物の直方体と地形の三角形の両方が来るためであり、判断10が禁じているのは参加する自由度を1つの列挙で指すことであってこの綴りの列挙ではない。`静的世界の接触相手` が持つのは識別子・材質の識別子・形の最小の厚みの3つであり、形そのものは候補の列挙の工程が持つ(その工程は後続の便である)。検査は、半分の長さ0.5メートルの立方体2つを0.05メートル食い込ませた面どうしの接触が4点とも別の拘束として残ること、局所点を世界へ戻すと接触点集合の点と1e-6メートル以内で一致すること(上の箱を鉛直軸まわりに90度回した配置でも成り立つこと)、法線が第1の参加者から第2の参加者へ向くこと、静的世界の側の点が世界位置のまま接触点集合の点と一致することを固定した。索引の更新と候補の列挙(生成の工程の前2段)と、接触点の受け皿の固定長と上限超過の型付きエラーは細分の工程を作る後続の便で足す。本便の組み立ては見込みの対の数に接触点の上限を掛けた長さを生成で1度だけ確保する。

### 判断12: 非貫通は法線方向の片側のXPBD位置拘束であり、貫通しているときだけ効く

接触拘束1点の非貫通は、p_A = x_A + R(q_A) r̄_A、p_B = x_B + R(q_B) r̄_B(静的世界の接触相手なら固定の世界位置)、隔たり d = (p_B − p_A) · n [m] について C = d ≥ 0、λ_n ≥ 0 の片側拘束である。生成時は接触点集合の不変条件(第2の形の上の点は第1の形の上の点から法線の向きへ貫通量だけ戻った点)から d = −貫通量 ≤ 0 である。反復1回の計算は次のとおりである。

```text
d ≥ 0 かつ λ_n = 0 なら:   この点は反復で読まず、λ_n は変えない(離れている・離れていく接触へ押し戻しを加えない)
それ以外なら:             C = d、Δλ = (−C − α̃ λ_n) ÷ (w̃_A + w̃_B + α̃)、λ_新 = max(0, λ_n + Δλ)、Δλ ← λ_新 − λ_n
                          B へ +Δλ n の補正(判断9)、A へ −Δλ n の補正、λ_n ← λ_新
                          d ≥ 0 で λ_n > 0 のとき Δλ は負であり、前の反復が積んだ乗数を解放する(片側拘束の有効集合)
```

コンプライアンス α は0(硬い接触)を既定とし、正の値は柔らかい接触として型が受ける。接触余白(既定0.005メートル。世界の形と衝突基盤の判断10の数値方針)は休止の判定の貫通残差の基準(判断18)に使い、拘束の式には入れない。静的と運動学的の剛体と静的世界の接触相手は w̃ = 0 であり、運動学的剛体の細分の中の配置の変化(判断7)は p_B の変化として拘束へ入る(境界運動)。深い初期重なり(貫通量が形の最小の厚みの半分を超える)も同じ式で解き、1細分あたりの補正の長さの上限を形の最小の厚みとして切り詰め、数細分で回復させる(オーナーの追加設計の第3節が許す「通常接触とは別の回復方針」はこの切り詰めだけである)。バウムガルテ安定化(貫通量に比例した押し戻しを速度へ注入する手法)は採らない。

Issue #7の追加設計の第3節(非貫通は片側XPBD拘束)とIssue #40の対象の実装形である。位置の言葉で貫通を解消すると「細分の終わりの配置が d ≥ −接触余白 を満たす」を状態の不変条件として検収に書ける。λ_n ≥ 0 の切り詰めは、離れている物体を引き寄せないための片側性である。バウムガルテ安定化は単位の無い係数がエネルギーを注入し、静止すべき塔を微振動させる(オーナーの要求1に反する)。

速度基準の逐次衝撃と位置の別解消(Issue #7の裁定材料の案A)は、Issue #5の横断裁定で退けられた。線形相補性問題の直接法(案C)は実装の費用が1桁大きく退化した入力に弱い。接触余白を拘束の目標(d ≥ 余白)に入れる案は、静置した箱が余白ぶん浮く見た目になり、余白の値が挙動へ漏れる。

機械で強制する方法として、非貫通の射影は `blitz_sim::contact` のCPUの参照計算1箇所が持ち(2つのバッチは参加者Bの点の取り方だけが違い、式は同じ関数を読む)、結果の型は補正した枝と「離れている」「両方が動かせない」の理由の枝を持つ(距離拘束と同じ様式)。接触点集合1つを1つの連立として解く行 `非貫通の接触点集合の一行` は、右辺 −C_n − α̃ λ_n を自分のメソッドで返す。判断13の粘着の候補はこの同じ行と同じ右辺を混合の連立へ入れるため、法線の側の式が2箇所に分かれない。検査は、λ_n が負にならないこと、d ≥ 0 かつ λ_n = 0 で λ_n が変わらないこと、箱1個を床へ落として静止した後の貫通が接触余白以下であること、深い初期重なりが非数を出さず有限の細分数で回復すること、を固定する。

実装の細目(2026-09-03、Issue #40): 射影は `非貫通の一刻みの係数::一回射影する` の1箇所であり、2つのバッチの拘束の型がどちらもこれを読む。参加者Bの点の取り方の違いは `非貫通の参加点` が吸収する。この型は姿勢自由度の参加者と重心局所の接触点の組であり、静的世界の接触相手は「動かせない参加者を接触点そのものに置き局所点を零にする」形で表す。こうすると世界に固定された点と一般化逆質量0が同じ式からそのまま得られ、射影の関数を2つ書かずに済む。結果の型 `非貫通の一回の射影の結果` は補正した枝と、`離れている`・`両方が動かせない` の理由の枝を持つ。1細分あたりの補正の長さの上限は、長さのまま状態で管理する。`非貫通の一細分の解の状態` が法線のラグランジュ乗数と、その細分で詰めた法線方向の相対補正の長さの上界の累計の2つを持ち、射影は λ_新 を区間 [0, λ + 残りの長さ ÷ w̃の上界] へ収める(残りの長さ = 形の最小の厚み − 上界の累計)。状態が足す長さは max(Δλ, 0) × w̃の上界 であり、使用済みの長さは細分の中で単調に増える。負の増分でこの長さを戻してはならない。上界が保証するのは |実際の移動| ≤ w̃の上界 × |Δλ| だけであり、Δλ が負のときに 実際の移動 ≤ w̃の上界 × Δλ は成り立たないからである。戻すと、その後の正の増分との合計が厚みを超えうる(反証を下に置いた)。この上限が抑えたいのは深い初期重なりからの射出であるため、保証の対象は正の分離の増分の累計であり、正の増分の上界だけを単調に数えるほうがその対象に合う。上限を乗数へ換算して持ってはならない。w̃ は腕とワールドの逆慣性テンソルを含むため、姿勢の補正や他の拘束の射影で反復のあいだに変わり、換算した上限は w̃ が小さくなった反復で広がるからである(反証を下に置いた)。長さで管理すると、細分の中で λ が単調に増えるという前提も要らない(コンプライアンスが正のとき α̃λ の項で増分が負になりうる)。長さと乗数の換算に使うのは、線形化した有効逆質量 w̃ そのものでなく、その上界 w + |(I⁻¹ (r × n)) × r| である(状態が持つ累計の長さも同じ上界で数える)。姿勢の補正は正規化した有限回転として適用されるため、腕の先は接線でなく弦を動く。弦の長さは1次の接線の変位の長さ以下であるが向きが違い、法線への射影は1次の値を上回りうる。回転の寄与そのものは (r × n)ᵀ I⁻¹ (r × n) = ((I⁻¹ (r × n)) × r)·n であり、法線への射影を大きさで置き換えたこの上界は、乗数の増分1あたりに接触点がどの向きへ実際に動く長さも上から抑える。したがって、補正を配置へ適用して測った分離の増分の累計も形の最小の厚みを超えない(反証を下に置いた)。下限0について実測で分かったことを書く。隔たりが0以上の点を読まない規約の下では、漸化式 λ_新 = λ w̃ ÷ (w̃ + α̃) + (−C) ÷ (w̃ + α̃) の両項が非負であるため、λ は算術の上では負にならない。したがって下限0の切り詰めが実際に効くのは丸めのときだけであり、片側性そのものは「d ≥ 0 の点を読まない」枝が担っている。検査は、離れている接触で補正も乗数の変化も起きないこと、貫通している接触で乗数が正になり両参加者が反対向きに押し戻されること、柔らかい接触(コンプライアンス1メートル毎ニュートン)で乗数の増分が負になっても乗数が0以上に留まること、両方が動かせない枝、重心から外れた接触点でだけ姿勢が補正されること(重心の真下の接触点では回転が厳密に零であること)、貫通量が生成時の値でなく現在の配置から求まること(生成時の貫通量が0.05メートルでも、射影の時点で離れていれば `離れている` を返すこと)、深い初期重なり(10メートル)で1細分に詰める長さが形の最小の厚み1メートルと1e-5メートル以内で一致すること、同じ拘束の反復のあいだに有効逆質量が5から1へ下がっても細分の累計が厚み1メートルを超えないこと(乗数の上限へ換算した実装では1.8メートル詰まる)、射影が返した補正を実際に配置へ適用して測った正の分離の増分の累計が形の最小の厚み1メートルを超えないこと(偏心した接触点・異方の主慣性・反復のあいだに変わる有効逆質量の3つを使い、実測は0.902メートル。線形化した w̃ で換算する実装では1.068メートル動く)、柔らかい接触で乗数の増分が正・負・正と振れても、補正を配置へ適用して測った正の分離の増分の累計が形の最小の厚み1メートルを超えないこと(コンプライアンス5e-4メートル毎ニュートンの接触点へ、同じ細分の中で他の拘束が与える隔たりとして深い−12メートルと浅い−0.05メートルを交互に与え、増分が正・負・正の順に出ていることを材料の前提として同じ検査が確かめる。実測は0.870メートル。負の増分で予算を戻す実装では2.531メートル詰まる)である。段2が守る契約を1つここへ置く。射影が `離れている` を返した接触点は、その細分の法線のラグランジュ乗数を前の値のまま残す。したがって、その細分で離れていた接触点は静止摩擦(判断13)と速度段階(判断14)の対象外にする。そうしないと、実在しない法線反力で円錐の判定(μ_s λ_n)と動摩擦の上限(μ_d λ_n ÷ h)が効く。

乗数の解放について(2026-09-03の改訂、Issue #40の段2の検収)。当初の実装は「隔たりが0以上なら読まない」を字義どおりに採り、乗数が正のまま隔たりが0以上になった接触点の乗数をそのまま残していた。これは1つの接触点集合の複数の点が互いの押し戻しで離れた後も古い法線反力を保つため、法線の乗数の合計が真の法線力積を上回る(面と面の4点で実測9パーセント。動摩擦の上限が μ_d λ_n ÷ h であるため、滑走の減速度の誤差が動摩擦係数に比例して増えた)。読まない条件を「隔たりが0以上かつ乗数が0」へ改め、乗数が正なら通常の式を評価して負の増分で乗数を解放するようにした。下限0の切り詰めがその解放を法線反力の消滅までで止めるため、片側拘束の有効集合として整合する。1細分の補正の長さの予算は解放で戻さない(正の増分の上界だけを単調に足す上の規則は変えない)。予算の単調性と乗数の解放は別の話であり、予算が守るのは深い初期重なりからの射出だからである。実測は `_doc/計測/剛体の接触の摩擦と反発_2026-09-03.md` にある。

接触点集合の法線を同時に解くことについて(2026-09-03の改訂、Issue #40の段2の再検収)。乗数の解放を入れた後も、位置の反復回数を増やすほど法線の乗数の合計が真の法線力積 m g cosθ h² から離れた(傾きの正接0.9で反復回数2・4・8のとき3.0・10.0・21.6パーセント下回る)。原因は点を1つずつ順に配置へ適用する解き方そのものであり、点Aの補正が点Bを離して次の巡回でAの乗数を解放する往復が反復のたびに起きる。細分の反復は、接触点集合1つの全部の行を1つの連立として解き、点ごとの補正を合計してから配置へ1度だけ適用する形へ改めた。同じ条件の実測は0.45・1.06・1.92パーセントになった。

連立は、各点の法線ヤコビアンを行にした有効逆質量行列 W = J M⁻¹ Jᵀ と隔たりの列から、λ ≥ 0・補正後の隔たり ≥ 0・各点で両者の積が0、という片側の条件を解く。有効集合は「乗数が負に振れた行を外す」と「増分を適用しても侵入が残る行を入れる」を繰り返して決める。選ぶ行は最も負の乗数と最も深い隔たりであり、同じ値なら行の番号の小さい側を採る。組み替えの回数の上限は接触点の上限の2倍に固定し、打ち切ったときも最後に行う0と上限の間への切り詰めが片側性と1細分の長さの上限を守る。

面と面の接触では連立が特異になる。剛体1つの法線方向の自由度は並進1つと回転2つの3つしかなく、4点の行はそのうち3つしか張らないためである。従属の行を落とすだけにすると、載る荷重が一部の点へ偏って残りの点の乗数が0になり、判断14の速度段階がその点を接触していない点として外す(平らな箱の跳ね返りの高さが0.638メートルから0.593メートルへ落ちた)。連立の対角へ、行列の規模(対角の最大)に比例した正則化を足して正則にし、荷重を集合の全部の点へ配る。正則化は右辺へ足さないため、収束の到達点(増分が零のとき d = −α̃ λ、硬い接触なら食い込み零)は変わらない。枢軸が対角の規模に対して小さすぎる行を従属として落とす規則は残し、落とす判定は行の番号の昇順に固定する。

接触点1つを解く上の式は残す。連立の1行が満たす式がそれであり、接触点が1つだけの集合では同時解と逐次の射影が一致することを検査が固定する。行列そのものは `接触点集合の法線の有効逆質量行列` の内側へ閉じ、入口と出口は単位型である(`逆慣性テンソル` が3×3の行列を内側へ閉じるのと同じ形)。非対角の成分は符号を持つため、`blitz_math` へ `符号付き逆キログラム` を足した。

段2の実装で、上の契約の「その細分で離れていた接触点」の意味を「その細分の法線のラグランジュ乗数が0の接触点」と定めた(2026-09-03。当初は「一度でも押し戻したか」の印で表したが、検収で退けた)。「最後の反復で離れていたか」と読むと、反復が進んで貫通が解けた点(押し戻しの仕事を実際に行った点)が静止摩擦と速度段階から落ち、跳ね返りも摩擦も効かなくなる。逆に「一度でも押し戻したか」と読むと、後の反復や後の拘束で接触が離れて乗数が解放された点にも古い反力が残る。最終の乗数を正本にすると、数値の上でちょうど解消した接触だけを落とし、実在する法線反力をすべて拾う。静止摩擦は反復ごとにその時点の乗数を、速度段階は細分の末の乗数を読む。`非貫通の一細分の解の状態` の `法線反力が在るか` が乗数が正であることそのものを返し、別の印は持たない。

位置の反復を重ねても法線の乗数の合計が真の法線力積へ寄らない事象の原因は、世界位置の単精度の分解能である(2026-09-03の記録)。坂の試験の場面は坂の半分の長さが20メートルであり、接触は世界の原点から20.5メートルの位置にある。単精度の20.5メートルの刻み幅は1.9e-6メートルであるのに対し、落ち着いた細分で1反復が加える補正は2.1e-7メートルである。補正が刻み幅の9分の1しかないため配置は動かず、隔たりは32反復のあいだ2.126581e-7メートルのまま1ビットも変わらない。同じ隔たりに対して毎反復が同じ増分を積むため、Σλ_n は反復回数に比例して増える(1反復あたり解析値の0.672パーセント。反復32で21.0パーセント)。同じ場面の坂の半分の長さを0.25メートルへ縮めて接触を原点から0.75メートルへ寄せると、隔たりは零へ落ち、Σλ_n の解析値との差は32反復まで±1.4パーセントに収まる。したがって受入条件(c)の反復16・32の逸脱は、正則化や有効集合の組み替えでなく、閉じられない隔たりに対して乗数を積み続けることが原因である。記録の計器は `contact/scene/normal_iteration_record` にある。

表現の刻みを下回る隔たりを満たされた行として扱う(2026-09-03の改訂、Issue #40の段2)。行 i は自分の隔たりの表現の刻み δ_i = Σ_k |n_k| (ulp(p_1,k) + ulp(p_2,k)) ÷ 2 を持つ(ulp は隣の表現できる値との差であり、÷2 は座標を丸めた誤差の上界がその半分であることによる)。隔たりの大きさが δ_i を下回るなら、その隔たりは補正を積んでも配置を1ビットも動かせない。連立の右辺をその行について零に置き、乗数の増分を積まない。有効集合の開始の判定(貫通しているか)と、外した行を入れ直す判定(補正の後になお食い込むか)も同じ δ_i を下限にする。座標の絶対値の最大1つで代表させてはならない。法線と直交する軸の座標まで最大へ入り、同じ場面を世界の原点から遠くへ平行移動するだけで刻みが増える。この規則を入れた後の同じ計器の実測は、坂の半分の長さ20メートルの場面で、反復1から反復32まで Σλ_n が3.104509e-5(解析値との差 −1.9053パーセント)のまま動かず、最大の食い込みも2.835441e-7メートルのまま動かない。従前は +0.884パーセント(反復2)から +21.038パーセント(反復32)へ増え続けていた。

### 判断13: 静止摩擦は錨からの接線変位を零へ戻す位置拘束であり、クーロン円錐の内側でだけ効く

接触が始まった細分で両参加者の接触点を錨(ā_A, ā_B。判断16の履歴が持つ)として保存する。各細分の非貫通の射影の後、錨の世界位置 a_A = x_A + R(q_A) ā_A、a_B = x_B + R(q_B) ā_B(静的世界の接触相手なら固定の世界位置)から接線変位 Δ_t = (a_A − a_B) − ((a_A − a_B) · n) n を求め、|Δ_t| > 0 なら C = |Δ_t|、t = Δ_t ÷ |Δ_t| で位置拘束として Δλ_t を求める(有効逆質量は t で組む一般化逆質量の和 w̃_t = w̃_A,t + w̃_B,t)。接線の乗数は接平面上の2次元ベクトル λ_t として接触点ごとに累積する。円錐の内側を求める工程は接触点集合1つを単位とした同時解法であり、非貫通の法線の行と静止摩擦の接線の行を同じ対称行列へ入れて1回で解く(2026-09-03の改訂。従前は接触点1つずつを順に射影し、次に接線だけの連立へ移り、その次に法線を先に確定してから右辺が零の混合連立を足す形であった)。この解を粘着の候補と呼ぶ。法線の行は判断12の右辺 −C_n − α̃ λ_n を、接線の行は −(C_t − δ) を持ち、貫通の解消と接線の補正と、接線の力が作る回転を打ち消す法線の荷重の移し替えの3つが1度に釣り合う。仮の接線の乗数は λ_t,i + Δs_i t_i であり、各点の補正は剛体ごとに合計して1回だけ配置へ適用する。クーロン円錐の判定は接触点集合(判断11の接触多様体)1つを単位として行い、集合の全部の点の仮の配置と仮の乗数が揃った後に、接線の乗数の合力と法線の乗数の合計を ‖Σ_i λ_t,i‖ ≤ μ_s Σ_i λ_n,i で1度だけ比べる(2026-09-03の改訂。従前は点ごとに λ_n と比べていた)。内側なら集合の全部の補正を一括で確定し、超えたときは集合の全部の補正を捨て、全部の錨を現在の接触点へ置き直し、集合の全部の点をこの細分では滑走中と印す(動摩擦は判断14の速度段階が担う)。集合の一部を適用してから円錐の超過を検出すると、その細分に位置の静止摩擦と速度の動摩擦が二重に効くため、判定は集合の全体が揃ってから行う。集合の単位にする理由は、面と面の接触では法線の乗数が点へ分かれる一方、各点の静止摩擦は物体全体の滑りを1点で戻そうとするためである。点ごとの判定では円錐が点の数だけ厳しくなり、傾きの正接が静止摩擦係数より小さい坂でも滑る(実測は下の実装の細目にある)。合力で判定するため、この式が制限するのは並進のクーロン円錐だけである。ねじりの摩擦は別の上限として後の便で足す。接線2軸を独立に切り詰める箱近似は採らない。

オーナーの要求2(坂の上の物体が静止でき、限界を超えると滑り出し、滑走中の抵抗は静止中の保持と別)と追加設計の第4節の実装形である。静止摩擦を速度の言葉で近似すると、定常の荷重の下で接線方向の微小な滑りが毎刻み残り、坂の箱がじわじわ下る。錨からの変位を位置で零へ戻す拘束はこの残留が構造的に零である。円錐の内側でだけ効かせ超えたら錨を捨てる形は、位置の静止摩擦と速度の動摩擦が同じ細分で二重に効くことを避ける。等方の判定(2次元の乗数の長さで比べる)にするのは、追加設計の第4節が「接線2軸を完全に独立にクランプした結果、方向によって摩擦強度が不自然に変わる問題」(クランプは上限で切り詰めること)を検収対象にしたためである。

円錐の側面へ射影して切り詰めた補正を適用する案(射影ガウス・ザイデル型。拘束を1つずつ順に解いて上限で切り詰めながら即座に反映する反復)は、同じ細分で速度段階の動摩擦とあわせて抵抗が二重になるか、速度段階を静止摩擦の成否で分岐させる複雑さを生むため捨てる。接線の乗数を1次元(滑る向き1本)で持つ案は、滑る向きが細分の間に回るとき乗数の意味が壊れる。

機械で強制する方法として、静止摩擦の射影は非貫通の射影と別の関数であり、接触点1つを仮に射影するだけで円錐の判定を持たない。判定は接触点集合の集計の型が1箇所で持つ。検査は、坂の傾き tan θ < μ_s の箱が3600刻みで1ミリメートル以下しか動かないこと、tan θ > μ_s で滑り出すこと、接線軸の取り方(箱の姿勢を鉛直軸まわりに0度・30度・45度回す)で滑り出す傾きの境界が変わらないこと(等方性)、静止摩擦の位置化を外した反証で坂の箱が下ること、を固定する。

実装の細目(2026-09-03、Issue #40): 錨は `静止摩擦の錨<第2の参加者から見た点>` であり、第1の参加者(必ず剛体)の重心局所点と、第2の参加者から見た点(剛体なら重心局所の変位、静的世界の接触相手ならワールドの位置)を持つ。接触拘束が錨を細分のあいだ借りて持ち、刻みを跨いで保つのは判断16の履歴である。接線の1行を作って連立へ積む工程は `静止摩擦の一細分の解の状態::連立へ接線の一行を積む` であり、状態は接線のラグランジュ乗数と滑走中の印の2つを持つ。接線の乗数 `接線のラグランジュ乗数` はワールドの3成分で持つ。接平面の2次元の基底を固定すると、その基底自体が挙動へ漏れるためである。コンプライアンスは持たない(正本が硬い位置拘束として書いている)。この工程が返す結果の型は「一行を積んだ」「既にこの細分で滑走中」「接線変位が零」「法線反力が無い」「両方が動かせない」の5枝であり、円錐を超えた枝は持たない(判定が集合の単位へ移ったためである)。集合の判定は `接触点集合の静止摩擦の仮の集計` が持ち、法線の乗数の合計と接線の乗数の合力の2つだけで決まる。接触点集合がバッチの中で占める範囲は生成のときに `接触点集合の占める範囲` として保ち、細分の反復はこの範囲で拘束を区切って借りる。鍵の昇順の並べ替えはこの範囲の中だけで行い、範囲どうしの順は積んだ順のままである(全体が昇順になるのは候補の列挙が参加者の組を昇順に積むことに依り、破れは判断16の併走が型付きエラーで落とす)。「既にこの細分で滑走中」を持つのは実測による。一度滑走中と印した接触点集合に同じ細分の後の反復でも円錐を判定し続けると、置き直した錨からの新しい接線変位が円錐の内側に入り、位置の静止摩擦と速度の動摩擦が二重に効く(動摩擦係数0.4の坂の滑走の減速度が2.37になり、解析値3.65との差が実効の静止摩擦係数へ寄る)。検査は、坂の傾きの正接0.2(静止摩擦係数0.6)の箱が3600刻みで1.17e-5メートルしか動かないこと、傾きの正接0.9で100刻みに滑り出すこと、鉛直軸まわりに0度・30度・45度回しても傾きの正接0.50で保持し0.61で滑り出すこと(保持と滑走の判定が回しで変わらないこと)、接線変位の向きを接平面の中で30度ずつ12方向へ回しても乗数の増分の大きさと円錐の判定が1e-6以内で変わらないこと(接線2軸の箱近似を採っていないことの反証)、同じ法線の荷重と同じ接線の滑りを1点へ置いても4点へ分けても円錐の判定が変わらないこと(点ごとの判定の欠陥を踏む反証)、位置の静止摩擦を外して押し戻した点を滑走中と印す反証で坂の箱が下ること、鉛直軸まわりに0度・30度・45度回しても正接0.50の坂の箱が600刻みで1ミリメートル以下に留まり、正接0.61で3方向とも1メートルを超えて滑ることである。保持できる傾きの上限はまだ静止摩擦係数そのものではない。600刻みの変位が1ミリメートル以下に収まる上限は、点ごとの逐次の射影では正接0.25と0.26の間、接線だけの同時解法では0.44と0.46の間、法線と接線の同時解法では回し0度で0.49と0.50の間、回し30度と45度で0.50と0.51の間である。静止摩擦係数0.6には届かない。この上限は摩擦の境界ではない。傾きの正接を百分の一刻みで振ると、静止摩擦係数の3分の2しかない正接0.40でも回し0度は17878本目の細分で円錐を超え、超えるまでの細分の数は正接に対して単調でない。保持と滑走を分けているのは、接触点集合の比 ‖Σλ_t‖ ÷ Σλ_n が1本の細分で跳ねる事象が、観測する4800細分の中に入ったかどうかである。跳ねる起こり方は2つある。初めの食い込みを解く過渡と、保持している最中に接触点集合が崩れる細分(点の1つの隔たりが正へ抜けて法線の行が積まれなくなり、有効集合の組み替えが別の点を外す)である。一度超えた集合は滑走中と印されて元へ戻らないため、1本の細分の数値の跳ねがそのまま最後まで残る質の変化になる。接線を接触点ごとに順に射影する反証は、法線と接線の同時解法を入れた後は正接0.50で1.53e-2メートルであり、同時に解いた場合の1.71e-6メートルに対して4桁の差が付く。この反証が超えるべき長さは保持の上限と同じ1ミリメートルである。反証が示すべきことは「同時に解かないと受入条件(a)の保持に不合格になる」ことであり、1.53e-2メートルはその上限の15倍である。有効集合は、取りうる部分集合を全部走査して相補条件を満たすものから選ぶ(2026-09-03の改訂)。枢軸を順に動かす組み替えは、外す条件(更新後の法線の乗数が負)と入れ直す条件(補正の後の隔たりが表現の刻みを超えて負)が同じ点について同時に成り立つ材料が坂の場面に実在するため終了しない(600刻みで正接0.50が50件)。有効集合は接触点の上限8のビット並び1つで表し、取りうる集合が高々256個であることを表し方から読めるようにする。行の右辺の下限は「丸めの誤差の上限」ではなく「解けたと見なす許容差」である。半ULPは1回の最近接の丸めの上限であって、世界位置は配置と姿勢の回転と局所点から複数の演算で作られるため右辺の誤差の上限ではなく、式が数えているのは入力の座標の量子化だけである。固有の向きごとの積み上げに二乗和の平方根を採るのも、独立な確率変数の標準偏差の合成ではない(行は同じ剛体の配置と回転を共有する)。実測は `_doc/計測/剛体の接触の摩擦と反発_2026-09-03.md` にある。残る差については下に書く。

法線反力を解放された点の接線の乗数について(2026-09-03の再検収)。判断12の乗数の解放を入れて、ある反復で法線と接線の乗数を得た点が後の反復で法線の乗数を0へ解放される状態遷移が初めて通るようになった。その点の仮の射影は `法線反力が無い` を返すが、当初の実装は前の接線の乗数を仮の値として残していた。実在しない法線反力の接線が合力 ‖Σλ_t‖ だけを増やし、円錐の半径 μ_s Σλ_n は増やさないため、集合の全体を誤って滑走中へ落とす。仮の乗数の扱いを `接触点の仮の接線の乗数` の2枝(集合が円錐の内側なら確定する値・法線反力が無いため零へ解放する)で表し、解放は集合が円錐を超えて仮の補正を捨てる反復でも有効集合の更新として確定させる。剛体どうしの本番の工程も同じ型を読む契約である。検査は、法線の乗数が0へ解放された点の古い接線の乗数が合力にも次の状態にも残らないことを、集合が円錐の内側の場合と超えた場合の両方で固定する。

接線を接触点集合の単位で同時に解くことについて(2026-09-03の改訂、Issue #40の段2の再判定)。接線を接触点ごとに順に射影する形では、傾きの正接0.50の坂で600刻みの変位が157メートルになり、位置の反復回数を2から4・8へ増やすほど保持できる上限が下がった。各点が物体全体の滑りを1点で戻そうとし、1点の補正が次の点の腕と有効逆質量を変えるため、接線の乗数が反復のたびに積み増されて円錐を超えるためである。円錐そのものと、同じ細分で静止摩擦と動摩擦を重ねない規則は変えない。変えたのは円錐の内側を求める工程だけであり、それが逐次から同時になった。

連立の組み方は次のとおりである。行を持つのは、その反復に法線の乗数が正で、かつ錨の接線変位が非零の点だけである。行 i の方向はその点の接線変位の単位方向 t_i であり、右辺は −C_i = −|Δ_t,i| である。係数行列は W_t で、成分 W_ij = w (t_i·t_j) + (r_i × t_i)ᵀ I⁻¹ (r_j × t_j) を2つの参加者について足したものである。法線の乗数が零の点は接線の乗数も零へ解放し(上の段落の規則)、接線変位が零の点は連立から外して既存の接線の乗数を保つ。仮の乗数は λ_t,i + Δs_i t_i であり、接線の乗数がワールドの3成分のベクトルであるため、各反復でその反復の行の方向へ増分を足す。この契約は検査で固定した。

連立の解き方は法線の側と分ける。法線は対角へ正則化を足して正則にした連立をLDLᵀで解くが、接線は固有分解の小さい向きを捨てた擬似逆で解く。接線の右辺は点ごとの錨の変位の長さであり、同じ剛体へ複数の点ごとの錨の拘束を課したこの材料では、剛体1つの運動で実現できない成分を含むことを実測で確かめた。その成分は行列の小さい固有値の向きに乗る。正則化 ε で解くとその成分が ε で割られ、真の増分1.5e-5に対して5.2e-3の増分が向かい合う2行へ現れた。補正そのものは打ち消し合って動かないが、円錐の判定が読む接線の乗数の合力が壊れ、正接0.20の坂でも集合が滑走中へ落ちた。

捨てる向きの判定は2つを別々に置く(2026-09-03の改訂。従前は実測で決めた比0.03を固定で使い、その次に右辺の丸めと行列の丸めを1つの比へ混ぜていた)。行列の側は、分解そのものの丸めが作る偽の固有値を λ_k ≤ λ_max n ε で落とす。右辺の側は、固有の向きごとに β_k = v_kᵀ b と δ_k = √(Σ_i (v_ik δ_i)²) を比べ、|β_k| ≤ δ_k の向きを落とす。δ_i はその行の右辺が持つ丸めであり、行の勾配の向きへ射影した世界位置の差の表現の刻みから導く(判断12と同じ式である)。

この形にする理由を3つ書く。第1に、右辺の丸めを座標の絶対値の最大1つで代表させてはならない。接触と無関係な軸の座標まで最大へ入るため、同じ場面を法線にも接線にも直交する軸へ100万メートル平行移動するだけで、4点の乗数の増分が −2.499996e-3 から 0 へ落ちた(実測)。第2に、右辺の誤差と行列の分解の誤差を1つの比へ混ぜてはならない。混ぜると、右辺の全体のノルムが小さい連立で誤差の無い固有の向きまで一斉に落ちる。第3に、固有の向きへの積み上がりは絶対値の和 Σ_i |v_ik| δ_i でなく二乗和の平方根で見る。絶対値の和は全部の行の丸めが同じ符号で1つの向きへ揃う最悪の場合であり、行が8つある法線と接線の連立ではその上界が右辺そのものの桁へ達する。実測では、絶対値の和を採ると傾きの正接0.44の坂で接線変位の3割が毎細分に残り、その残りが接線の乗数へ積まれて集合が滑走へ落ちた。

法線と接線を同じ行列へ入れた後の実測は次のとおりである(600刻みの坂に沿った変位、鉛直軸まわり0度・30度・45度、最適化を有効にした実行)。正接0.20で1.03e-6・1.05e-6・4.30e-7メートル、0.44で1.54e-6・1.24e-6・2.01e-6、0.48で1.62e-6・1.47e-6・1.64e-6、0.50で1.71e-6・1.38e-6・1.78e-6、0.52で173.1・171.6・174.0メートル、0.55で185.2・184.6・185.4、0.61で211.4・209.1・209.5である。600刻みで1ミリメートル以下に収まる上限は0.50と0.51の間であり、そこから先は3方向とも100メートルを超えて滑る。境界の周囲を0.01刻みで見ると、0.49と0.50は3方向とも保持し、0.51・0.52は3方向とも滑走し、0.53は0度と30度が滑走する一方で45度だけ1.22e-6メートルに留まり、0.54・0.55は3方向とも滑走する。遷移そのものは3方向で同じ位置(0.50と0.51の間)にあるが、0.53の45度だけが単調でない。これは残る差である。

保持できる上限は静止摩擦係数0.6に届かない。正接0.50が保持できない原因は2026-09-03の記録で特定した。円錐を初めて超える細分を鉛直軸まわり0度・30度・45度で記録すると、3方向とも次の順で起きる。反復0の非貫通が食い込みを閉じ、反復0の接線解が錨の接線変位を零へ戻す。接線の力が働くのは接触点(重心より箱の半分の長さだけ下)であるため、その補正は並進だけでなく回転を含み、4点の隔たりが ±2.1e-5メートルへ開く。反復1の非貫通がその回転を戻すと、錨の接線変位が 2.2e-5メートル(もとの3.8e-5メートルの58から60パーセント)まで作り直される。接線の乗数はこの作り直された変位に対してもう一度積まれる。仮の ‖Σλ_t‖ ÷ Σλ_n は、反復0の非貫通の後で0.400(0度)・0.409(30度)・0.492(45度)であり、反復1の非貫通の後で0.654・0.652・0.782になる。反復0の値は傾きの正接0.50に近く1反復ぶんとしては正しい。反復1がそれをもう一度積んで円錐の0.6を超え、集合が滑走中へ落ちる。

原因がねじりでも接線の連立の数値解法でもないことを同じ記録が示す。特徴の識別は3方向とも細分の中で変わらず、接触点の数も4のままである。点ごとの大きさの和と合力の比 Σ‖λ_t,i‖ ÷ ‖Σλ_t,i‖ は1.000000(0度)・1.000017(30度)・1.000039(45度)であり、点どうしが打ち消し合う内部の成分は無い。接触点集合の中心まわりの接線の乗数のモーメントは1.08e-11・4.71e-9・4.94e-8であり、並進の合力に腕0.5メートルを掛けた7.7e-6に対して0.0001から0.6パーセントである。法線の有効集合が細分の中で変わるのは30度と45度であり、0度は4点とも有効のまま超える。

この記録が示した是正は、法線と接線を同じ試行の状態で収束させることであった。当時の反復は法線の連立と接線の連立を交互に解いており、接線の補正が非貫通を破り、非貫通の修復が接線変位を作り直していた。この往復のあいだ C_t が零へ寄らないため、接線の乗数が反復回数に比例して積み増される。下の粘着の候補の同時解法がこの往復を断つ。記録の計器は `contact/scene/cone_breach_record` にあり、`--ignored --nocapture` を付けて実行する。

粘着の候補を1回の混合連立で解く(2026-09-03の改訂、Issue #40の段2の再判定)。上の往復を断つ実装形である。連立の組み方は次のとおりである。行は接触点1つにつき2つまであり、法線の行は非貫通が本来解く右辺 −C_n − α̃ λ_n を、接線の行は錨の接線変位を零へ戻す右辺 −(C_t − δ) を求める。法線を先に確定してから右辺が零の混合連立を足す2段の形は採らない。その形では最初の非貫通の補正が作った接線変位を後段がもう一度払うことになり、前段の有限回転を後段の線形化が完全には取り消せない。1回で解けば、初めの食い込みを解く過渡と、保持している最中に法線の有効な点が減る細分の両方を同じ相補問題の中で扱える。係数行列の成分は行 i と行 j の `J_i M⁻¹ J_jᵀ` であり、行が2つの参加者への符号付きの勾配(法線の行は第1へ −n・第2へ +n、接線の行は第1へ +t・第2へ −t)を持って同じ式で組む。向きを1つだけ持って呼び出し側が道具を呼び分ける形にしてはならない。法線どうしでは符号が打ち消し合って正しく見え、法線と接線の交差だけが符号を落とすため、行列の対称性でも検出できない。

法線の有効集合は、取りうる部分集合を全部走査して選ぶ。相補条件を満たす候補として残すのは、(a)有効な全部の点の更新後の法線の乗数が0以上で、その細分に詰めてよい長さから決まる乗数の上限も超えないこと、(b)無効な全部の点の更新後の隔たりが表現の刻みを超えて負でないこと、の2つを満たす集合である。無効な点の法線の乗数は零へ解放し、その解放が動かす長さを残る行の右辺から引く(判断12の連立と同じ形である)。無効な点の接線の行も同時に外し、その点の接線の乗数を零へ解放する。

複数の候補が残る退化では、有効な点の数が多い方・解いた行の残差・補正の質量の計量のノルム・有効集合のビット並びの順で1つへ絞る。点の数を先に見るのは、この相補問題が構造として退化しているからである。剛体は法線方向に3つの自由度しか持たないため、面と面の4点のうち3点の隔たりを止めれば残る1点も動かない。したがって「1点を外して残る3点で解く」候補も相補条件を満たし、行の数が自由度と釣り合うぶん残差が小さくなる。残差だけで選ぶとこの候補が勝ち、外した点の接線の乗数が零へ解放されて集合の接線の合力が点の数のぶんだけ落ちる(4点の材料で実測4分の1。正接0.40の坂で円錐の比が0.40から0.30へ落ちた)。触れている点は反力を持つ、というのが法線の有効集合の意味であり、退化した候補の中ではそれを保つ集合を採る。

行ごとの残差が要求の精度に収まっているかも、この走査で判定する。残差 A Δλ − b には2つの意味が混ざる。1つは偽の固有値の向きであり、行の数が自由度を超えた連立でどの乗数でも満たせない成分である。もう1つは許容差の内側として捨てた向きであり、こちらは「解けたと見なす」という取り決めで捨てた成分である。前者を精度の不足として数えてはならない(座標を倍精度にしても消えない)。後者だけを行ごとの許容差と比べ、どの候補も収められないときは型付きの結末「表現の精度が不足した」を返す。相補条件を満たす候補が1つも無いときも型付きの結末を返す。呼び出し側はどちらの結末でも粘着の候補を捨て、同じ開始の配置から法線だけの連立を解き直す。

確定は一括であり、更新後の ‖Σλ_t‖ ≤ μ_s Σλ_n を満たすときだけ配置・法線の乗数・接線の乗数を同時に確定する。円錐を超えたときは粘着の候補を全部捨て、同じ開始の配置から法線だけの連立を解いて、その法線の解だけを確定して集合を滑走中へ移す。錨の置き直す先は法線だけの連立を解き終えた配置の接触点である。外側の位置反復は再線形化のために回してよいが、接線だけを内側で複数回解く形へは戻さない。

同時解法へ移して初めて表面化した錨の置き方の誤りを2つ直した。第1に、接触が始まった細分の錨は、予測の後の配置でなく予測の前の配置の接触点へ置く。予測の後に置くとその細分の錨からの接線変位が零になり、位置の静止摩擦が1度も効かない。剛体は細分1本ぶんの接線の滑りと速度を持ったまま次の細分へ入り、次の細分が2細分ぶんの滑りを1度に戻すことになる。要る接線の乗数は傾きの正接の2倍へ跳ね、正接0.30を超える坂が2本目の細分で円錐を超える。第2に、円錐を超えた細分の置き直しも、拘束の引数が持つ接触点でなくいまの配置の接触点へ置く。引数の接触点はこの細分の予測の直後の値であり、反復で積んだ補正のぶんだけ後ろにある。次の細分がその遅れと自分の滑りを合わせて戻すことになり、一度滑り出した接触点集合が二度と保持へ戻れない。

実装の細目(2026-09-03、Issue #40の段2の再判定): 連立は `contact/normal_tangential_system/` にある。法線の行は非貫通と同じ `非貫通の接触点集合の一行` であり、その `連立の右辺` を混合の連立も読む。接線の行は `錨の接線変位を零へ戻す一行`、有効集合は `法線と接線の連立の有効集合`、解は `接触点集合の法線と接線の同時解`、結末は `相補条件を満たす有効集合を探した結末`(定まった・相補条件を満たす有効集合が無い・表現の精度が不足した の3枝)である。行の勾配 `一行の二つの符号付き勾配` は参加点と同じモジュール `contact/contact_projection_row/` にある。勾配は参加点の2つ組でしかなく、点を持たない勾配も勾配を持たない行も無いためである。連立の定義(`system`)・相補条件の判定(`complementarity`)・部分集合の走査(`subset_search`)・候補の決着(`candidate`)を分けているのは、行を積むのが接触点集合の走査1回であるのに対し、走査が抜き出しと求解を部分集合の数だけ繰り返す別の局面だからである。擬似逆が許容差の内側として捨てた右辺は `擬似逆で解いた増分と捨てた右辺` が解と分けて運び、行ごとの許容差と比べるのはこちらだけである。円錐の判定へ渡す仮の値と集計は `接触点集合の仮の乗数の集まり` が持ち、円錐を超えて捨てる細分でも法線反力が無い点の解放だけはここから確定する。接線の一行を積む工程は法線反力の有無を見ない。混合の連立が法線の乗数を接線と同時に決めるため、行を積む時点の法線の乗数は細分の1本目では全部の点で零である。法線反力が無い点であるかは、有効集合が定まった後にその点の法線の行が有効集合に残ったかで決まる。検査は、面と面の4点の順を反転しても補正と円錐の判定の合力が1e-6以内で変わらないこと、4点のどれにも接線の乗数が載ること、接線の力が作る回転を法線の乗数の移し替えが打ち消すこと(下り側の2点の増分が正、上り側の2点の増分が負、合計が零)、腕が零の1点では旧の逐次の射影と同じ増分になること、進めた接線の乗数がその反復の行の向きへ増分を足した値であること、同じ4点の場面を法線にも接線にも直交する軸へ100万メートル平行移動しても点ごとの増分が相対1e-3以内で変わらないこと、法線の軸へ100万メートル平行移動したときに「表現の精度が不足した」を返すこと、初めの食い込みが残る1細分目と保持中に1点が離れる細分のどちらでも粘着の候補が相補条件と円錐を同時に満たすことである。

残る差(2026-09-03の再測定)。傾きの正接0.40・0.48・0.50は鉛直軸まわりの3方向とも600刻みで1ミリメートル以下に留まり、0.40は3600刻みでも留まる。0.61は3方向とも滑る。**正接0.55は回し0度と45度で滑る**(30度は保持する)。滑るのは1本目の細分の過渡である。平衡の接線変位が g sinθ h² であるのに対し、1本目は25パーセント大きい。差の出どころは2つあり、接線の行の右辺 −(C_t − δ) が許容差 δ ぶんの接線変位を残すことと、その残りが位置から再構成する速度へ移って次の細分の予測へもう一度入ることである。許容差を引かずに C_t をそのまま右辺にすると1本目の比は下がるが、接線の乗数が許容差ぶんだけ膨らんで正接0.50が後の細分で滑る。0.01刻みの走査では判定が保持から滑走へ一方向にだけ変わっておらず、0.45(0度)・0.52(0度)・0.51(30度)で1つの細分の跳ねが判定を裏返す。また4800細分のうち多いもので135細分が、表現の精度の不足か相補条件を満たす有効集合の不在で法線だけの連立へ落ちる。実測は `_doc/計測/剛体の接触の摩擦と反発_2026-09-03.md` の末尾の追記にある。

### 判断14: 反発と動摩擦は速度段階で衝撃として1箇所ずつ適用し、低速の反発は零に落とす

細分の速度の再構成(判断9)の後、接触点ごとに接触点の相対速度 v_相対 = (v_B + ω_B × r_B) − (v_A + ω_A × r_A) [m/s](静的世界の接触相手は速度0)、法線成分 v_n = v_相対 · n、接線成分 v_t = v_相対 − v_n n を求める。速度の変化は衝撃(単位はニュートン秒 = キログラム・メートル毎秒)として計算し、両参加者へ等しく反対向きの一般化衝撃として分配する。衝撃 P を B へ +、A へ − で与えると、v_B ← v_B + w_B P、ω_B ← ω_B + I_B⁻¹ (r_B × P)、v_A ← v_A − w_A P、ω_A ← ω_A − I_A⁻¹ (r_A × P) であり、相対速度の向き u の成分の変化は |P| · w̃_u(w̃_u = w̃_A,u + w̃_B,u は向き u で組んだ一般化逆質量の和 [1/kg])である。

```text
動摩擦(滑走中の接触点だけ):   t̂ = v_t ÷ |v_t|、j_t = min(μ_d λ_n ÷ h, |v_t| ÷ w̃_t) [N·s]、P = −j_t t̂
                              μ_d λ_n ÷ h [kg·m ÷ s] は法線反力 f_n = λ_n ÷ h² [N] が細分の時間 h の間に与えうる摩擦の衝撃の上限であり
                              |v_t| ÷ w̃_t [m/s ÷ (1/kg)] は接線の相対速度を零にする衝撃である
反発:                          j_n = (−v_n + max(−e · v_n⁻, 0)) ÷ w̃_n [N·s]、P = j_n n
                              v_n⁻ [m/s] は細分の予測の前(外力の速度更新の後)の法線相対速度、e は反発係数
低速の抑制:                    |v_n⁻| ≤ v_閾値 なら e = 0                                   v_閾値 の初期値は 2 · |g| · h(1細分の自由落下で付く速度の2倍)
```

反発係数 e は0以上1以下、動摩擦係数 μ_d は0以上μ_s以下であり、判断15の混合則が与える。既に離れていく接触(v_n > 0 で d ≥ 0)へ反発を再注入しない。v_閾値 と後述の休止の閾値は `blitz_sim::contact` の定数の1箇所に置き、値の根拠を行末に書き、箱の塔と落下球の実測の日に確定する。

オーナーの要求3(反発は低速接触を不安定化させない)と追加設計の第5節(反発は速度レベル)、Issue #5の横断裁定(速度レベルで扱うほうが自然な現象はXPBD後段の速度処理として正式に許可)の実装形である。反発と動摩擦は衝突前の速度を参照する現象であり位置だけからは書けない。式を衝撃で書くのは、本設計が単位を型で強制するためである。判断8の2020年の論文の式は質量を無次元として係数を省いており、その形(h μ_d |f_n| と |v_t| を直接比べる)は速度と衝撃を比べることになって単位が合わない。衝撃の上限 μ_d λ_n ÷ h と、速度を零にする衝撃 |v_t| ÷ w̃_t の小さいほうを採ると、クーロン摩擦の上限の中で接線の相対速度を止める動摩擦になる。ニュートンの反発則 v_n⁺ = −e v_n⁻ は落下球の跳ね返り高さ h' = e² h という解析解と直接突き合わせられる。低速の抑制は、重力由来の微小な接近速度を衝突と誤認して静置した箱が毎刻み微小に跳ねることを断つ。

ポアソン型(圧縮と復元の2相)は解析解との突き合わせが直接できないため捨てる。動摩擦を全接触点へ常に適用する案は、静止摩擦が保持している接触点に速度段階が抵抗を重ねる。反発の閾値を刻み幅と無関係な定数にする案は、細分数を変えると1細分の自由落下の速度が変わり閾値の意味が壊れる。速度の変化で式を書く案(|Δv_t| = min(w̃_t μ_d λ_n ÷ h, |v_t|))は同値だが、分配の式が速度を衝撃へ戻す割り算を持つため、衝撃で書く形を採る。

機械で強制する方法として、速度段階は非貫通と摩擦の射影と別のモジュールであり、位置と姿勢を書き換える口を持たない(速度と角速度だけを書く)。衝撃は `blitz_math` の衝撃の型で持ち、速度との比較はコンパイルで落ちる。検査は、落下する箱の跳ね返り高さが e² h と相対誤差3パーセント以内であること、e = 0.8 の箱を静置しても3600刻みで跳ねないこと、滑走中の箱の減速度が g(sin θ − μ_d cos θ) と相対誤差1パーセント以内であること、接線の相対速度が1細分で零になる衝撃より上限が小さいときだけ上限が効くこと、を固定する。

実装の細目(2026-09-03、Issue #40): 速度段階は `接触の速度段階` であり、刻み幅と低速の抑制の閾値だけを保持する。参加点 `速度段階の参加点` は接触の射影に参加する点と運動状態の組であり、衝撃を受けた後の値を新しい参加点として返す。配置を書き換える口をどの型も持たないため、位置と姿勢を書き換えないことが型で閉じている。結果の型は適用後の2つの運動状態と2つの衝撃の大きさを返す(参加点を返さないのは、返り値の側からも配置を書き換えないことを明らかにするためである)。反発の式は j_n の符号で場合を分けない。負の j_n は位置の補正が押し戻して作った過剰な分離の速さを反発則の値まで落とす向きであり、静置した箱が押し戻しの速度で毎細分わずかに浮くことをこれが断つ。本文が禁じる「既に離れていく接触(v_n > 0 で d ≥ 0)への再注入」は、隔たりが0以上の接触点をこの段が受け取らないことで満たす(呼び出し側が判断12の契約に従い、その細分で押し戻した接触点だけを渡す)。反発だけを適用する口 `一点へ反発だけ適用する` を別に持つ。接触点が2つ以上ある面と面の接触では、点を順に1巡しただけでは各点の法線相対速度が同時には目標へ揃わず、剛体に偽の回転が残る(1メートル落とした平らな箱で角速度1.36ラジアン毎秒、跳ね返りの高さで5.3パーセントの取りこぼし)。動摩擦は1細分に1度だけ与える量であるため巡回の対象にせず、2巡目からは反発だけを適用する。v_閾値 と休止の閾値は `blitz_sim::contact` の `contact_thresholds` の1箇所にあり、値の根拠を各行の末尾に書いた(自由落下の速度に掛ける倍率2・休止の速さ0.01メートル毎秒・休止の回る速さ0.05ラジアン毎秒・休止と判定する連続細分数60)。検査は、跳ね返りの高さが0.6378メートル(解析 e²h = 0.64、相対誤差0.34パーセント)であること、反発係数0.8の箱を静置しても3600刻みで床からの隙間が8.76e-6メートルを超えないこと、動摩擦の上限が接線の相対速度を零にする衝撃より小さいときだけ効くこと、低速の抑制が働くと反発の後の法線相対速度が零になること、滑走中と印されていない接触点へ動摩擦を適用しないことである。滑走の減速度は本文が求める相対誤差1パーセント以内に入る(2026-09-03の検収の後。動摩擦係数0.1から0.55の範囲で0.2から0.8パーセントであり、係数が大きいほど誤差が増える傾向も消えた)。検収の前は2.7パーセントずれ、誤差は動摩擦係数に比例して増えていた(0.4で5.5パーセント、0.55で20パーセント)。原因は判断12の乗数を解放しない実装で法線の乗数の合計が真の法線力積を9パーセント上回っていたことであり、判断12の乗数の解放でこれを断った。位置の反復回数を4から2へ改めたのも同じ検収による(下の判断19の実装の細目に理由を書いた)。実測は `_doc/計測/剛体の接触の摩擦と反発_2026-09-03.md` にある。

### 判断15: 接触物性は両表面の物性から混合則で決め、既定則と材質対の上書き表を持つ

表面物性は静止摩擦係数 μ_s・動摩擦係数 μ_d・反発係数 e の3つを持つ型であり、生成が 0 ≤ μ_d ≤ μ_s と 0 ≤ e ≤ 1 と有限を検査する。世界の形と衝突基盤の判断10のとおり、衝突形状の子形状が表面物性への参照(不透明な材質の識別子)を持ち、剛体の台帳が材質の識別子から表面物性を参照する表を持つ(静的世界の接触相手の材質の識別子も同じ表で参照する)。接触1つの接触物性は、混合則の型が2つの表面物性から作る。既定則は摩擦が相乗平均 √(μ₁ μ₂)(静止と動それぞれ)、反発が最大値 max(e₁, e₂) である。材質対ごとの上書き表を混合則の型が持ち、対が登録されていればその値を返す。混合則は可換である(引数の順で結果が変わらない)。ゲーム固有の材質の名前(木・氷・ゴム)は `blitz_sim` へ持ち込まず、識別子は番号である。

オーナーの要求4(接触物性は接触する両者から決まる。木×木・木×氷・ゴム×石で異なる接触特性を作れる)と追加設計の第6節の実装形である。混合則に物理的な正当性は無く(摩擦係数と反発係数は本来「材質の対」の性質である)、既定則は挙動の既定値であって物理法則ではないため、対ごとの上書きの口を最初から置く。相乗平均は μ = 0 との混合が0になり(氷の上では相手に依らず滑る)可換である。反発の最大値は、よく弾む側が知覚を支配する(よく弾む球は床の材質に依らず弾む)。

算術平均は氷(μ = 0)と木の混合が木の半分の摩擦になり、氷の上で滑らないため捨てる。最小値は反発でよく弾む球が硬い床で弾まなくなる。混合則を1つに固定して上書きの口を持たない案は、オーナーの要求4の「優先指定」の候補を閉じる。

機械で強制する方法として、混合則は物性の型のメソッド1箇所であり、接触拘束の生成だけがそれを呼ぶ。検査は、既定則の可換性を全順序の対で確かめること、μ_d > μ_s と e > 1 が生成で落ちること、上書き表の登録が既定則より優先されること、を固定する。表面物性の参照が登録されていない材質の識別子は接触拘束の生成で型付きエラーになる(黙って既定の物性を当てない)。

実装の細目(2026-09-03、Issue #40): 表面物性は `表面物性`(静止摩擦係数・動摩擦係数・反発係数)であり、係数は裸の実数を持たず `摩擦係数`(0以上の有限)と `反発係数`(0以上1以下の有限)の値オブジェクトである。生成が 0 ≤ μ_d ≤ μ_s を検査する。接触物性は同じ3つの係数を表面物性で包んだ `接触物性` であり、別の型にするのは片方の表面の性質と混ぜた後の接触の性質を取り違えても型が通る形を避けるためである(値域の検査は表面物性の1箇所が持つ)。混合則は `混合則` であり、材質の識別子から表面物性を引く表と、`材質の対` を鍵にした上書き表の2つを持つ。対の生成が2つの識別子を昇順へ正規化するため、登録も参照も引数の順で結果が変わらない。表面物性を引く表を混合則が持つのは本文が台帳に置いた表であり、台帳がこの混合則を保持することで本文の配置になる(台帳の公開面は後続の便である)。組み立てを `混合則の組み立て` へ分けたのは、上書きの対が実在する材質を指しているかを表面物性の登録がすべて済むまで判定できないためである。二重登録はその場で、対の材質の未登録は組み立ての最後で型付きエラーにする。検査は、木・氷・ゴムの見本3件の全9通りで既定則の値(摩擦の相乗平均・反発の最大値)と可換性を確かめ、上書きが既定則より優先されて順を入れ替えても同じ値になること、値域を破る3つの組が生成で落ちること、登録されていない材質が黙って既定の物性で埋まらず型付きエラーになることを固定した。

### 判断16: 接触の履歴は参加者の組ごとに特徴の識別を鍵に全順序の併走で対応付け、暖機はしない

接触の履歴は接触拘束のバッチ(判断11)と同じ2つに分かれる。剛体と剛体の履歴の鍵は(剛体Aの識別子, 剛体Bの識別子, 接触の特徴の識別)の3つ組であり、A < B に正規化する。剛体と静的世界の履歴の鍵は(剛体の識別子, 静的世界の接触相手の識別子, 接触の特徴の識別)である。接触の特徴の識別の型(`直方体どうしの接触の特徴の対`・`直方体と三角形の接触の特徴の対`)へ全順序を与え、履歴は鍵の昇順に並べた固定長の一覧で持つ。細分ごとに新しい接触点集合の鍵を昇順に並べ、前の履歴と併走して対応付ける(鍵が一致すれば継続、履歴にだけあれば終了、新しい側にだけあれば開始)。継続する接触が引き継ぐのは2つである。静止摩擦の錨(判断13)と、前の細分の接触法線(法線の急変を検出して錨を置き直すのに使う)である。法線のラグランジュ乗数は細分の開始で0へ初期化し、前の細分の値から始める暖機はしない。特徴の識別が変わった接触は新規として扱い、古い履歴を引き継がない。履歴は刻みを跨いで剛体の台帳が保持し、剛体の破棄と運動種別の遷移でその剛体を含む履歴を捨てる。

裁定候補15(接触点集合の時間継続)とオーナーの要求6(前刻みの接触を利用できる)、追加設計の第7節(接触履歴)の実装形である。世界の形と衝突基盤は特徴の識別を「次の刻みで同じ接触を見つけ直すための綴りであり、追跡そのものは拘束の解法の担当」と定めており、本判断がその担当である。全順序の併走は確保を起こさず、順序が入力だけで決まるため決定的である。暖機をしないのは、細分が収束を担うため(判断19)乗数の持ち越しが要らず、XPBD共通拘束基盤の判断5(既定では刻みを跨がない)に従うためである。暖機が要る日は、刻み幅の変化に対する尺度の変換を含めて別の仕様を書く。

接触点の世界位置の距離で対応付ける案は、閾値が挙動へ漏れ、箱の面が滑って隣の特徴へ移ったときに誤対応するため捨てる。ハッシュ表で対応付ける案は確保を起こし、走査の順が実装の詳細に依る。乗数の暖機は、Issue #7の裁定材料が「温存を無効化すると挙動が別物になる」と述べたとおり反証の独立性を下げる。

機械で強制する方法として、接触の特徴の識別の型へ `Ord` の導出を `blitz_collision` で足す(値の並びに意味は無く、決定的な全順序であればよい)。履歴の型は鍵の昇順を生成で検査し、併走の関数は2つの昇順の一覧だけを受ける。検査は、箱を床の上で1メートル滑らせても面と面の接触の錨が滑走の判定まで保たれること、特徴が変わった接触が新規になること、を固定する。

実装の細目(2026-09-03、Issue #40): 接触の特徴の識別の型(`直方体どうしの接触の特徴の対`・`直方体と三角形の接触の特徴の対`)と、その材料である直方体の面・辺・頂点、三角形の面・辺・頂点、軸と面の向きと頂点の番号へ `Ord` を導出した。値の並びに意味は無く、同じ入力が必ず同じ順序を与えることだけを求める。検査は、相異なる2つの対が必ず大小で決着すること(直方体どうし676組と直方体と三角形182組の総当たり)と、逆順から整列しても同じ並びになることを固定した。履歴の型と併走の対応付けそのものは後続の便で足す。

実装の細目(2026-09-03、Issue #40の段2): 履歴は `接触の履歴<鍵, 記録>` であり、鍵と記録を型引数に取る。剛体どうしと剛体と静的世界で鍵の綴りも錨の形も違う一方、「昇順の一覧を併走させて継続と終了と開始へ分ける」振る舞いが同じだからである。参加者の組ごとに別の値を持つという本文の要求は、2つの型引数の組(`剛体どうしの接触の履歴`・`剛体と静的世界の接触の履歴`)で別々の値を作ることで満たす。鍵は `剛体どうしの接触の鍵`(2つの剛体の識別子を昇順へ正規化)と `剛体と静的世界の接触の鍵` であり、記録は `接触の継続の記録<錨>`(錨と前の細分の接触法線)である。併走は `新しい細分の接触と併走する` が2つの昇順の一覧を突き合わせ、新しい側と同じ並びの `接触の対応付け`(継続・開始の2枝)と終了した接触の数を返す。終了に枝が無いのは、返る一覧に現れないからである。履歴そのものは併走では書き換えず、細分の終わりに `細分の終わりの記録で置き換える` が差し替える。錨は静止摩擦が円錐を超えた反復で置き直されるため、細分の終わりの値を保つ必要があるためである。昇順の検査は生成でなく両方の口が入力に対して行い、同じ鍵が2件あれば型付きエラーで拒む。前の細分の接触法線は記録が運ぶが、何度の変化を急変とみなすかを正本が定めていないため、この便では引き継ぐところまでを持つ(法線の急変で錨を置き直す規則は後続の便で定める)。検査は、継続と終了と開始の3つが分かれること、特徴の識別が変わった接触が古い錨を引き継がずに新規になること、昇順でない一覧を併走も置き換えも拒むこと、接触拘束の生成が作る鍵が並べ替えの後に昇順になって併走へそのまま渡せること、床の上で箱を1メートル滑らせるあいだ4点が毎細分「継続」として対応付くことである。

実装の細目(2026-09-03、Issue #40の段2の検収): 剛体どうしの接触は、剛体aの識別子が剛体bの識別子より小さいことを接触点集合を積む工程が境界で強制し、破れを `接触拘束の生成エラー::剛体の識別子が昇順でない` で拒む。鍵の生成は識別子だけを昇順へ入れ替え、特徴の識別は入れ替えない(第1の形と第2の形の綴りを交換する口を衝突数学層が持たないためである)。強制が無いと、候補の列挙がAとBを反転させた細分で、同じ正規化した鍵に向きの違う特徴と局所の錨と接触法線が結びつき、次の細分の併走がその錨をそのまま引き継ぐ。検収の前の実測では、参加者を反転させて積んだ4点が、正しい順で積んだ4点と鍵が完全に一致しながら局所接触点が別の値になった。候補の列挙は参加者の組を識別子の昇順で作ること(判断11の生成の工程の2段目の要求である)。検査は、実際に参加者を反転させて積むと型付きエラーで拒まれ拘束が1件も積まれないことを固定する。

### 判断17: 接触島は動的剛体の連結成分であり、島の中の反復の順序は鍵の辞書式昇順である

接触島は、接触拘束と関節(Issue #41)で結ばれた動的剛体の連結成分である。静的と運動学的の剛体と静的世界の接触相手は島を結合しない(壁を挟んだ2つの箱は別の島である)。島は細分ごとに接触拘束の生成の後に組み直す(連結成分の併合は接触の数に対して線形である)。島の中の拘束の反復の順序は、剛体と静的世界の接触拘束を鍵(剛体の識別子, 静的世界の接触相手の識別子, 特徴)の昇順で先に、剛体と剛体の接触拘束を鍵(剛体Aの識別子, 剛体Bの識別子, 特徴)の昇順で次に、関節をその後に同じ規則で並べる。島の順序は含まれる最小の剛体の識別子の昇順である。CPUの参照計算は島の中を逐次(`反復の更新の順序::逐次`)で解き、並列化は島の間だけに許す。島は反復・休止と再開・並列化・デバッグ表示の論理単位であり、データを1つの配列へ混ぜることは要求しない。GPU化のために島を消さない。

裁定候補16(接触・拘束で繋がった剛体群を独立な問題の単位へ分ける)と追加設計の第8節の実装形である。逐次の反復は順序で結果が変わるため、順序を契約に書かないと検収決定性が実装の詳細に依る。動く形の空間索引が列挙を識別子の昇順に固定したのと同じ規律である。静的世界との接触を先に解くのは、床との接触が塔の土台であり、逐次の反復では最後に解いた拘束がその細分の配置を決めるため、土台を先にして上の箱の接触を後にするほうが積み上がりが沈みにくいからである。島の間の並列は、島がデータとして独立であるため決定性を壊さない。

島を刻みの頭に1回だけ組む案は、細分の中で新しく触れた対が島を結合できず、反復の順序が細分の途中で崩れるため捨てる。島を持たず全接触を1つの列で解く案は、離れた物体群の休止を個別に行えず、並列化の単位が無い。

機械で強制する方法として、島の型は含まれる剛体と2つの接触拘束のバッチの添字の区間を持ち、反復は島を1つ受けて島の中の拘束だけを読む関数である。検査は、離れた2つの箱の塔が別の島になること、静的な床を挟んだ2つの箱が別の島になること、同じ配置から2回組んだ島の順序と拘束の順序が一致すること、を固定する。

### 判断18: 休止は島の単位で行い、物理の結果を変えない最適化として再開の条件を型で持つ

島の全部の動的剛体が静穏(|v| ≤ v_休止 かつ |ω| ≤ ω_休止 かつ その剛体の全接触の貫通が接触余白以下 かつ 静止摩擦の錨が置き直されていない)である細分が連続して N_休止 個続いたとき、島を休止させる。休止した島の剛体は予測・接触点集合の生成・反復・速度段階を省略し、速度と角速度を0にして配置を保つ。動く形の空間索引には載り続ける。再開の条件は6つであり、いずれかで島全体が起きる。作用の投入(判断6)・起きている動的剛体または速度が0でない運動学的剛体との新しい接触(候補の対に現れ、接触点集合が空でない)・拘束(関節・目標拘束)の着脱・運動種別の遷移(判断7)・剛体の生成と破棄を含む構造変更・その島の剛体が触れうる区域の読込の完了(未決定の事項2)である。休止している島どうしの対は接触点集合を作らない。閾値(v_休止・ω_休止・N_休止)は判断14の定数と同じ1箇所に置き、初期値の候補を v_休止 = 0.01メートル毎秒・ω_休止 = 0.05ラジアン毎秒・N_休止 = 60細分とし、箱の塔の実測の日に確定する。

裁定候補17(休止は接触島との整合を取る)とオーナーの要求9(休止は性能最適化であり物理法則を変えない。安定した接触島はまとめて休止でき、外力・衝撃・接触相手の運動・運動学的物体・拘束変更で正しく再開する)の実装形である。速度が小さい1個の剛体だけを休止させると、積み上がった上の箱が休止したまま下の箱が動く不整合が起きる。「物理の結果が本質的に変わらない」の意味は、休止に入る閾値が速度を0とみなせる範囲(1細分で接触余白より小さく動く速度)であることで担保する。

剛体ごとの休止は上の理由で捨てる。休止した剛体を索引から外す案は、起きている剛体との新しい接触を検出できず再開の条件が壊れる。休止の判定に拘束誤差を使わず速度だけを使う案は、貫通したまま速度0で休止する状態を許す。

機械で強制する方法として、実行状態(判断2)は「起きている」「休止している」の2枝であり、休止の判定と再開は台帳のメソッドが島を受けて行う。検査は、箱10段の塔が600刻み以内に休止すること、休止した塔の最下段の箱へ衝撃を加えると塔の全部の箱が起きること、運動学的な床が動き出すと上の箱が起きること、休止の有無で1000刻み後の配置が接触余白以内で一致すること、を固定する。

### 判断19: 剛体は基本刻みを整数nで細分し、細分1本の中で予測・接触・反復・速度再構成・速度段階を回す

剛体のシミュレーションは時間の規律の判断5の「固定刻みで必ず進む」区分に属し、基本刻み H = 1/60秒を整数nで細分する(同判断7の規律4条。最初の利用者)。nと1細分あたりの反復回数は剛体の品質方針として `blitz_app` のコンポジションルートが時間進行方針と同じ様式で注入し、実行負荷で変えない。初期値の候補は n = 8・反復回数1(反復を増やすより細分を増やすほうが同じ計算量で誤差が小さいという判断8の2019年の結果に基づく)であり、箱の塔の実測の日に確定する。物理刻み1本の工程は次のとおりであり、XPBD共通拘束基盤の判断8の工程3〜9を細分の中で回す形である。

```text
物理刻み(基本刻み1本)
├─ 工程1: 構造変更の反映(生成と破棄・運動種別の遷移・拘束の着脱)
├─ 工程2: 作用の蓄積器の取り出し(判断6)。力と回転力は一刻みの入力として保持し、衝撃と回転衝撃は1度だけ適用する型として最初の細分へ渡す
├─ 細分 × n
│  ├─ 衝撃の適用(最初の細分だけ。v += w P、ω += I⁻¹ L)
│  ├─ 予測(判断8。前の状態の保存を含む。力と回転力は h ずつ積分する)
│  ├─ 索引の更新と候補の列挙(判断11。細分の変位で伸ばした箱)
│  ├─ 接触点集合の生成と履歴の対応付け(判断11・16)、島の構築(判断17)
│  ├─ 乗数の零化と刻み依存量の導出
│  ├─ 反復 × 反復回数: 非貫通(判断12)→ 静止摩擦(判断13)→ 関節(Issue #41)、島ごとに判断17の順序
│  ├─ 速度と角速度の再構成(判断9)
│  └─ 速度段階(判断14。反発・動摩擦)
├─ 休止の判定(判断18)
└─ 刻み境界の確定(配置の2枚の繰り上げ。描画補間へ供給)
```

細分の途中の配置は刻み境界の外へ公開しない。描画と世界の形への問い合わせは刻み境界の確定した配置だけを読む。描画補間(時間の規律の判断6)は剛体の物体原点の配置を供給値として前と現在の2枚で持ち、台帳が刻みごとに繰り上げる。刻み幅の綴りは時間の規律の正本(`crates/blitz_app/src/game/step_seconds.rs`)からコンポジションルートが受け取り、細分の刻み幅 h = H ÷ n をXPBDの `刻み幅` 型として作る。

時間の規律の判断7(内部細分の規律4条)と、Issue #7の裁定材料の案B(位置基準XPBD + 細分刻み)の実装形である。細分が収束を担うため、乗数の暖機(判断16)と多数の反復が要らない。反復と細分の回数を固定にするのは、毎刻みの費用が読めること・決定性の検証が単純であること・収束判定の分岐が挙動を入力の差で不連続にしないこと、の3つの理由による。衝撃を最初の細分だけへ渡すのは判断6のとおりである。

細分せず反復回数を増やす案は、同じ計算量で位置の誤差が桁で大きい(上の2019年の結果)ため捨てる。収束判定で反復を打ち切る案は上の理由で捨てる。細分の途中の配置を描画へ渡す案は時間の規律の判断7の第3条に反する。

機械で強制する方法として、細分の工程は `blitz_sim` の一刻みの工程の宣言として関数の列で書き、順序の検査(布の `constraint_stage_tests.rs` と同じ様式)で固定する。nと反復回数は正の整数だけを持つ型で受け、0を生成で拒む。検査は、n を 4・8・16 と変えたとき箱の塔の定常の貫通が接触余白以下で変わらないこと(コンプライアンス0の硬い接触では細分数が硬さを変えない)、一定の力 F を1刻み加えた剛体の運動量の変化が n に依らず H·F であること、衝撃 P を加えた剛体の運動量の変化が n に依らず P であること、を固定する。

実装の細目(2026-09-03、Issue #40の段2の検収): 位置の反復回数は2、速度段階の巡回数は4である。どちらも隠れた定数でなく、`接触を解く品質の設定` として持つ。この型は0の回数を型付きエラーで拒み、後の本番の細分の工程へそのまま渡せる。細分数nをこの型へ入れないのは、nが基本刻みの分割であり時間の規律の側が持つ量だからである。位置の反復回数を2に決めたのは、判断12の乗数の解放と判断13の集合の単位の円錐を入れた後に測り直した結果である。滑走の減速度の相対誤差は、反復回数1で2.9から6.3パーセント、2で0.2から0.8パーセント、3で1.2から3.6パーセント、4で1.3から8.5パーセントであり(動摩擦係数0.1から0.55の範囲)、2だけが判断14の求める1パーセント以内に入り、かつ誤差が動摩擦係数とともに増える傾向を持たない。回数を増やすと悪くなるのは、面と面の接触の点が互いの押し戻しで離れては乗数を解放する往復を反復のたびに繰り返し、細分の末の法線の乗数の合計が真の法線力積を下回っていくためである。この往復そのものは、同じ日の再検収で判断12の法線の同時解法を入れて断った。同時解法の後の滑走の減速度の相対誤差は、反復回数2で0.03から0.48パーセント、4で0.05から0.70パーセント、8で0.05から1.87パーセントである。反復回数2のままにするのは、誤差が最も小さく費用も最も小さいことに加え、静止摩擦が保持できる傾きの上限が反復回数を増やすほど下がるためである(判断13の実装の細目に実測を書いた)。速度段階の巡回数4は判断14の実装の細目のとおりであり、この検収で変えていない。細分数8と刻み幅1/480秒は判断19の初期値の候補のままである。実測は `_doc/計測/剛体の接触の摩擦と反発_2026-09-03.md` にある。

接線の同時解法を入れた後に反復回数を選び直した(2026-09-03、Issue #40の段2の再判定)。選び直した値も2である。滑走の減速度の相対誤差は、反復回数1で0.05から0.25パーセント、2で0.06から0.46パーセント、3で0.01から0.76パーセント、4で0.16から0.53パーセント、8で0.39から1.75パーセントである(動摩擦係数0.1から0.55の範囲)。1から4までは判断14の求める1パーセント以内に入り、8だけが外れる。減速度だけでは1から4を分けられないため、静止摩擦が保持できる傾きで分ける。600刻みの坂に沿った変位は、正接0.44で反復回数1が134メートル、2が5.6e-5メートル、3が1.7e-4、4が1.7e-4、8が4.1e-4であり、正接0.50で1が165メートル、2が3.3e-3メートル、3が3.1e-2、4が2.7e-2、8が125メートルである。反復回数1は接線の連立を1度しか解かないため保持が成立せず、3以上は反復のたびに接線の乗数が積み増されて保持できる上限が下がる。2つの基準の両方を満たす回数は2だけである。

### 判断20: 連続衝突は剛体ごとの方針で選び、初回接触時刻を得たらその位置で接触拘束を作って同じ反復へ入れる

剛体ごとに連続衝突の方針を3枝の列挙で持つ。離散の接触だけ・掃引を使う・自動で昇格する(1細分の変位の長さが自分の形の最小の厚みの半分を超えたら掃引を使う)である。掃引を使う細分では、予測の後・接触点集合の生成の前に世界の形と衝突基盤の動く形の対の問い合わせ(平行移動の還元)で触れるまでに進んだ割合 t* を得て、剛体の配置を x_前 + t* h v(姿勢は q_前 から t* h ω だけ回した位置)で接触点集合を作り、その接触拘束を同じ細分の反復へ入れる。非貫通の拘束が通り抜けを止め、残りの時間は同じXPBDの反復が解く。別の物理経路へ分岐しない。Issue #40は離散の接触だけを完成させ、方針の型と工程の位置だけを置く。回転を含む連続衝突(保守的前進)は同基盤の未実装のままであり、回転の速い薄い物体の通り抜けは初版の仕様の外である(未決定の事項3)。

オーナーの要求7(高速物体は必要に応じて連続衝突へ接続でき、全剛体へ常時は強制しない)と追加設計の第9節(離散のみ・掃引を用いる・自動昇格を選択可能にし、TOIを得ても別物理エンジンへ分岐せず残り時間のXPBD接触問題へ接続する)の実装形である。接触拘束を初回接触の位置で作ると、XPBDの片側拘束が「その位置より先へ貫通しない」を保証し、時刻で刻みを割る必要が無い。判断11の伸ばした箱は細分の終点で重なる対を候補から落とさないことだけを保証し、細分の途中で触れて終点では通過し終えている対を止めるのは本判断の掃引だけであるという分担である。したがって初版(離散の接触だけ)では、1細分で相手の厚みを越えて動く剛体の通り抜けは仕様の外である。

初回接触時刻で細分を分割する案は、島の中の他の剛体の時刻が揃わなくなり工程が複雑になるため捨てる。全剛体へ掃引を常時強制する案はオーナーの要求7に反する。掃引の結果で位置を初回接触の位置へ戻して速度を落とす(接触拘束を作らない)案は、反発と摩擦が速度段階で扱えない。

機械で強制する方法として、連続衝突の方針は剛体の型のフィールドであり、生成時に与える。掃引の呼び出しは候補の列挙の工程の中の1箇所である。検査(Issue #40の完了後)は、秒速50メートルの薄い箱(厚み0.02メートル)が厚み0.05メートルの静的な板を「掃引を使う」で通り抜けず「離散の接触だけ」で通り抜けること(方針の差が挙動に出ること)を固定する。

### 判断21: 極端条件を仕様の対象として目録に置き、保証の範囲と保証の外の態度を分ける

裁定候補18の10条件を検証の目録へ置く。保証するのは「非数を出さない・発散しない・不正な状態(長さが1でない姿勢、負の乗数、上限を超えた接触点)を作らない」であり、条件ごとに初版の保証の範囲を次のとおり定める。

```text
初期の重なり                    有限の細分数で回復する(判断12)。回復の細分数は形の最小の厚みと細分数で決まり、検収が上限を持つ
極端に重い物体と軽い物体        質量比100:1まで塔が発散しない(崩れてよい。崩れ方が漸近的であること)。それを超える比は非数と発散だけを保証する
非常に小さい・薄い物体          形の下限は衝突基盤の型(半径2^-24メートル・厚み0を拒む)が持つ。剛体は質量特性の下限(主慣性の三角不等式)で受ける
高速移動・高速回転              速度の絶対値の上限を速度の型の生成で課す(1刻みの変位が衝突基盤の座標の上限2^24メートルの1/2を越えない値)。回転の上限は判断7の運動学的の上限と判断8の陰的ジャイロ項
接触面がほぼ平行                接触点集合の側で外積の軸の入れ替えの上界が扱う(衝突基盤の実装順10)。剛体側は複数点の積み上がりの検収で受ける
重心が衝突形状の中心から大きくずれている  判断3と判断4の分離で扱い、偏った重心の物体が転がる検収(Issue #40)で受ける
非対称な慣性                    判断8の中間軸の反転の検収で受ける
同一点付近へ複数接触が集中する  接触点の上限8を型付きエラーで受け、鍵の昇順の反復で順序を固定する
種別の切り替え直後              判断7の遷移で履歴を捨て島を組み直す。切り替えの次の刻みで貫通が接触余白以下へ戻ることを検収する
破壊で複数剛体が生まれる直後    工程1の構造変更として生成し、生成された剛体は起きた状態で始める(Issue #16の日に検収を足す)
```

裁定候補18(初期重なり・極端条件を仕様上の対象から外さない)とオーナーの要求8(極端な質量差を未定義にせず、保証範囲外でも非数・発散・不正状態へ落ちない)の実装形である。保証の範囲を数で書かないと、検収が何を落とすべきか決まらない。

任意の質量比で完全に安定することを要求する案は、重い順に解く追加のパス(衝撃の伝播)を初版に要し、Issue #40の非目標に反するため捨てる。極端条件を「通常は起きない」として未定義にする案は裁定候補18に反する。

機械で強制する方法として、目録の各条件を検収の場面として `cargo xtask` の命令(Issue #40)に登録し、非数と発散の検出は全部の場面で共通に(全剛体の配置と速度が有限で姿勢の長さが1であること)判定する。

### 判断22: CPUの参照計算を正典とし、GPUのレイアウトは剛体の4つのバッファと接触の2つのバッファで契約する

剛体の状態と接触解法のCPUの参照計算が数式の正典であり、`blitz_sim` が持つ。GPU経路(`blitz_render`)はIssue #40の離散の接触が閉じた後にCPUの参照計算と同じ初期条件で突き合わせて導入し(Issue #43)、動的な接触の並列方式はXPBD共通拘束基盤の採用規則2(固定小数の原子加算を第一候補、二段階を代替)に従う。GPUレイアウトは点自由度の32バイトの契約を拡張せず、姿勢自由度の別の密な配列を立てる。バッファは剛体の状態に4つ、接触に2つの計6つであり、毎刻み書き換える量と原則不変の量を混ぜない。

```text
剛体の状態(4つ)
  剛体運動状態バッファ    重心位置 xyz f32 + 詰め物1語・姿勢 xyzw f32・並進速度 xyz f32 + 詰め物1語・角速度 xyz f32 + 詰め物1語(4×16 = 64バイト。実体52バイト、詰め物3語)
  剛体前状態バッファ      前の重心位置 xyz f32 + 詰め物1語・前の姿勢 xyzw f32(32バイト。実体28バイト、詰め物1語)
  剛体質量特性バッファ    逆質量 f32・逆主慣性 xyz f32・主軸 xyzw f32(32バイト。詰め物なし)
  剛体実行状態バッファ    運動種別 u32・起きているか u32 + 詰め物2語(16バイト)
接触(2つ)
  接触拘束バッファ        細分ごと生成。剛体と剛体・剛体と静的世界の2区間を1つのバッファに続けて置く(参加者A添字 u32・参加者B添字または静的世界の接触相手の添字 u32・局所点A xyz f32・局所点Bまたは固定の世界点 xyz f32・法線 xyz f32・物性3 f32・乗数 f32。バイト数はIssue #43のGPU経路で確定)
  接触履歴バッファ        鍵と錨と前の法線(刻みを跨ぐ)
```

1件のバイト数はIssue #39のGPUレイアウト契約の実装で確定し、この判断の下へ追記する。バイト列化は `blitz_sim::gpu_layout::rigid` が持ち、`blitz_render` の読み手は長さと内容(添字の範囲・姿勢の長さ・逆質量の非負と有限)を型付きエラーで検証する。

Issue #39のGPUの節(姿勢自由度のGPUレイアウトは点粒子の32バイトのレイアウトを無理に拡張して同一構造体へ統合しない)と、Issue #7の実装境界のGPU候補レイアウト(運動状態・前状態・質量特性・実行状態の4バッファ)、XPBD共通拘束基盤の判断9(静的な引数と毎刻み書き換える状態を同じバッファへ詰めない)の実装形である。3成分の量を16バイト境界へ置くのは、GPUの構造化バッファの読み出しが16バイト単位で最も速く、詰め物1語の費用がそれより小さいためである。CPUを正典にするのは、追加設計の第8節(CPU参照実装では固定順序の逐次反復を基準実装にしてよい)と実装順10(GPU経路はCPU参照結果と比較して導入)に従う。

剛体を最初からGPUで解く案は、島と履歴と休止の検収をCPUの参照計算なしに行うことになり、XPBD共通拘束基盤の目的2(CPUの参照計算を正典として置き、GPUをそれと突き合わせる)に反するため捨てる。状態を1件128バイトの1バッファへ詰める案は、質量特性の不変の量を毎刻み書き換える帯域に載せる。3成分を詰めずに並べる案(1件52バイト)は読み出しが境界をまたぐ。

機械で強制する方法として、1件のバイト数は `cargo xtask conform` の定数の組で書き手(`blitz_sim`)と読み手(`blitz_render`)を結ぶ(布と同じ運用)。CPUとGPUの突き合わせは許容差つきの比較であり、`cargo xtask` の命令として登録する。

実装の細目(2026-09-02、Issue #39): 1件のバイト数は運動状態64・前状態32・質量特性32・実行状態16で確定し、`blitz_sim::gpu_layout::rigid` が4つのバイト列化(`剛体運動状態バイト列にする` 等)と読み手(`剛体運動状態をバイト列から読む` 等。長さが1件の倍数であること・位置と速度の有限・姿勢の長さ・逆質量と逆主慣性の非負と有限・主軸の長さ・符号の範囲を `剛体レイアウトエラー` で拒む)を持つ。運動種別の符号は0=静的・1=運動学的・2=動的、起きているかは1=起きている・0=休止しているである。静的な剛体は速度を持たないため運動状態バッファには速度と角速度を零で書く。1件のバイト数は `cargo xtask conform` の定数の組(`shader_constant/table/rigid_body.rs`)で書き手と往復の検査の写しを結び、`blitz_render` の読み手とシェーダーの写しはIssue #43でこの表へ足す。質量特性の読み手は主軸の4成分が有限であることと長さが1から1e-4(姿勢の生成と同じ許容差。単精度の往復のずれ1e-6級より2桁大きく、破損より桁で小さい)以内であることを回転の生成へ渡す前に検査し、非数・無限大・長さ2・長さ0を `剛体レイアウトエラー` の枝(主軸が非有限・主軸が単位長でない)で拒む。回転の生成が非単位長を正規化して受理するのはアセットの誤差の吸収であり、GPUの読み手はそれに頼らない。

### 判断23: 公開のAPIは剛体の台帳が持つ物理の意味の操作だけであり、強結合は同じ実行領域で同じ反復へ参加する

ゲーム側へ見せるのは剛体の台帳(操作サービス。`blitz_sim` の公開面)の次の操作だけである。生成する(質量特性・配置・運動種別・衝突形状の参照・材質の識別子・連続衝突の方針を受け、剛体の識別子を返す)・破棄する・運動種別を遷移する(判断7)・配置と速度を読み出す・作用を加える(判断6の6操作)・材質を設定する、である。ラグランジュ乗数・反復回数・細分数・接触解法の内部表現・島・履歴は台帳の内部であり、切り替えのためだけの `trait 物理ソルバ` を公開面に置かない。開発用の計器(接触数・島の数・休止している島の数・拘束違反の最大値・乗数の代表値・工程ごとの時間)は読み出しの口を別に持ち、Issue #4の計器盤が読む。Issue #18の強結合(剛体と布・剛体とロープ・剛体と体積弾性体)は剛体と点の拘束のバッチ(判断10)で同じ反復へ参加させ、CPUの剛体とGPUの布のように実行領域を跨ぐ双方向の結合を作らない。どちらの領域へ寄せるかはIssue #43が遅延・反復不能・転送費用を明示して決め、本文書はCPUの参照計算がGPUへ移せるレイアウト契約(判断22)を持つことまでを担う。動く形の空間索引の識別子は `blitz_app` の物理の配線が1つの索引に対して発行し、剛体・キャラクター・破片を上位ビットの名前空間で分ける(未決定の事項1)。台帳は自分の剛体の識別子を索引の識別子へ写す口を持つ。

裁定候補の追加設計の第12節(API境界)とXPBD共通拘束基盤の判断10(公開のAPIへXPBDの内部を漏らさない)、Issue #18の裁定(強結合では1フレーム遅延を許容しない。CPU/GPUを跨ぐが何となく双方向という構成を禁止する)の実装形である。

剛体の型そのものをゲーム側へ渡して直接書き換えさせる案は、正規化・種別の整合・履歴の破棄が運用に落ちるため捨てる。`trait 物理ソルバ` を置く案はXPBD共通拘束基盤の判断10で退けられている。

機械で強制する方法として、剛体の状態の型のフィールドはすべて私有であり、台帳のメソッドだけが書き換える。`blitz_game` は `blitz_math` だけを知る現状を保ち、剛体の台帳を呼ぶのは `blitz_app` の物理の配線である(`cargo xtask conform` の白リスト)。

## 性能予算

剛体はCPUの参照計算が初版の本番経路であるため、`_doc/計画/評価軸.md` 3.6のCPU区間予算へ「剛体の1刻み」の行を暫定値0.5ミリ秒で新設する(Issue #7の追加の論点7。未決定の事項6)。見積りは、細分8本・起きている動的剛体256個・接触点1000点で、接触1点の射影が約50ナノ秒として反復の総量が約0.4ミリ秒である。GPU経路を導入した日(Issue #43)に、シミュレーション(M9、コンピュート)の3.0ミリ秒の内訳として剛体の行を別に立てる。正式な枠はIssue #40の箱の塔の実測の日に実測値の1.4〜1.7倍で確定する(既存の作法)。

測るものは次の6つであり、`cargo xtask` の命令が採る。細分1本の工程ごとの時間(索引の更新と候補の列挙・接触点集合の生成・履歴の対応付け・島の構築・反復・速度段階)・接触点の数と島の数と休止している島の数・剛体1個あたりと接触1点あたりの時間(物量に対する線形性の確認)・細分数を4・8・16と変えたときの時間と定常の貫通(品質と費用の関係)・休止の有無による時間の差(休止が効いていることの確認)・GPU経路の導入後のCPUとGPUの時間の比較、である。剛体の物量の上限(同時に登録できる動的剛体の数)は暫定1024個とし(休止を含む登録数。起きている数は予算が決める)、破片が最大の量産者になるIssue #16で見直す。

## 決定性の保証

- 保証する決定性: 検収決定性とゲーム決定性。CPUの参照計算はさらに数値決定性を持つ(単一スレッドで島の順序と島の中の拘束の順序が鍵の昇順に固定され、確保を起こさず、浮動小数の演算順が入力だけで決まる)。島の間を並列化した実行も、島がデータとして独立であり書く先が自分の島の剛体だけであるため数値決定性を保つ。GPU経路は固定小数の原子加算を採るときに数値決定性を持ち(XPBD共通拘束基盤の採用規則2の実測)、GPUとCPUの参照計算の間は許容差つきの比較である
- 条件: 同じ機械・同じビルド・同じ初期状態・同じ入力系列・同じ刻み数・同じ細分数と反復回数・同じ連続衝突の方針・同じ休止の閾値・同じ並列方式であること。島の間の並列化はスレッド数に依らない(島の結果は他の島に依らない)
- 失効する変化: 細分数または反復回数の変更(コンプライアンス0の硬い接触では定常の配置は変わらないが、過渡の軌跡と乗数の値が変わる)・休止の閾値の変更(休止に入る刻みが変わる)・接触の特徴の識別の全順序の変更(反復の順序が変わる)・浮動小数の原子加算を使うGPU方式(数値決定性を持たない)・接触余白とゆとりの幅の変更(候補の集合が変わる)
- 検収の判定: Issue #40の検収は、同じ場面を2回実行した全剛体の配置と速度のビット一致をCPUの参照計算で課し、GPU経路は許容差(位置1e-4メートル・乗数1e-6。布と同じ)で課す

## 実装順

各段階の様式は既存と同じ(実装→検査員2体→codexレビュー→是正→閉塞)である。段階はIssue #33の子Issueと対応する。Issue #34〜#37(布での縦切り)は完了済みであり、本文書はIssue #39から始める。

1. **Issue #39(姿勢自由度と3つの検証拘束)**: 作るものは、判断5の力学の単位型(`blitz_math`)・判断2〜4の剛体の状態の型と質量特性の検証・判断6の作用の蓄積器と取り出しの型・判断8の予測(陰的ジャイロ項を含む)・判断9の一般化逆質量による補正と速度の再構成・判断10の3つの検証拘束と剛体と点の拘束の型・判断22の剛体の4つのバッファのレイアウト契約(バイト列化と読み手の検証。GPUの実行は含まない)である。接触・島・休止・台帳の公開面は含まない。検収は、判断3・4・6・8・9・10の検査(姿勢の正規化・質量特性の生成の拒否・力と衝撃の細分への配り方・角運動量が発散しないことと散逸が刻み幅とともに減ることと中間軸の反転と陽的ジャイロ項の発散の反証・補正の適用の比例と一回の射影の解析解・3つの拘束の収束と解析解と座標系の不変)と、`cargo xtask verify` の全緑である。判断1の依存の追加(`blitz_sim → blitz_collision`)と conform の白リストの更新、判断16の接触の特徴の識別への `Ord` の導出をこの段で行う
2. **Issue #40(剛体の接触)**: 作るものは、判断11〜18の接触拘束の2つのバッチの生成・非貫通・静止摩擦・速度段階・混合則・2つの履歴・島・休止のCPUの参照計算、判断19の細分の工程(時間の規律の便3の実装)、判断7の運動種別の遷移、判断23の剛体の台帳の公開面と `blitz_app` の物理の配線(動く形の空間索引と静的世界の候補の選別の接続、静的世界の接触相手の識別子の発行、描画への物体原点の変換の供給)、判断20の連続衝突の方針の型(離散の接触だけを完成させる)である。検収は、Issue #7の追加設計の第11節の12場面のうち連続衝突を除く11場面を `cargo xtask` の命令として登録し、判断12〜19の検査の定量の基準(箱1個の静止時の貫通が接触余白以下・箱10段の塔が3600刻みで全部の箱の重心の水平移動が箱の一辺の1パーセント以下かつ600刻み以内に休止・坂の静止と滑走の境界と滑走の加速度の相対誤差1パーセント・反発の跳ね返り高さの相対誤差3パーセントと静置の非跳躍・運動学的な床が箱を運ぶこと・回転する床が接線速度を伝えること・偏った重心の物体が回転すること・初期重なりの有限の回復・質量比100:1で非数と発散が無いこと・休止した塔が衝撃で全体として再開すること・2回実行のビット一致)で判定する。性能の実測をこの段で採り、細分数と反復回数と休止の閾値と予算の枠を確定して本文書へ追記する。初版の対象形状は箱(任意姿勢の直方体)と静的世界(高さ場の三角形・建物の直方体・静的衝突メッシュ)であり(未決定の事項4)、カプセルの接触点集合は同基盤の側の積み残しのままである
3. **Issue #41(関節・可動限界・駆動)**: 作るものは、判断10の接続拘束とねじり拘束の組合せによる固定関節・蝶番・スライダー・角度と移動量の可動限界(片側拘束。判断12と同じ切り詰め)・位置と姿勢の目標の駆動・速度の目標の駆動(判断14の速度段階に置く)である。関節は判断17の島の辺に参加し、判断18の再開の条件(拘束の着脱・目標の変更)に加わる。検収はIssue #41の7場面(扉・振り子・引き出し・車輪・モーター・サーボ・柔らかい接続)と、各拘束の乗数と誤差の観測の口である
4. **Issue #42(体積弾性体)**: 剛体の側で作るものは無い。剛体と点の接触拘束(判断10)を体積要素の頂点との接触へ使えることを、Issue #43で確かめる
5. **Issue #43(一刻みの工程への統合と強結合)**: 作るものは、判断19の細分の工程を布・ロープ・体積弾性体と同じ一刻みの工程へ統合すること、剛体と点の拘束のバッチによる剛体と布・剛体とロープの双方向の強結合、GPU経路の導入(判断22。接触の2つのバッファの確定、CPUの参照計算との突き合わせ、固定小数の原子加算の適用)、島の間の並列化である。実行領域(CPUかGPUか)の選択と遅延の観測はこの段で行う。検収はIssue #43の5場面(箱を布へ落とす・ロープで剛体を吊るす・柔らかい体積物の上へ剛体を置く・運動学的剛体へ布とロープを接続する・反復回数を変えて収束を見る)と、Issue #4の計器盤への観測値である

## 未決定の事項

オーナーの裁定が別の案を採りうる事項である。オーナーの指示(「私が考えるべきところはIssueで決まっているはずなので全部完了するまで止まらずに実装する」)に従い、本文はいずれも推奨の側で確定して進める。オーナーが別の案を採るときはその判断を改める。

1. **動く形の空間索引の識別子の空間を剛体・キャラクター(Issue #3)・破片(Issue #16)で共有する。** 確定した案: `blitz_app` の物理の配線が1つの索引を持ち、識別子の上位ビットで種類(剛体・キャラクター・破片)を分けた名前空間を配線が発行する(判断23)。Issue #18の剛体と布・剛体とキャラクターの対の列挙には1つの索引が要る。別の案: 種類ごとに別の索引を持つ(異種の対の列挙に索引をまたぐ覆いの問い合わせが要り、費用が種類の数だけ増える)。世界(`blitz_engine`)が識別子を発行する(世界が剛体を知る向きになり層の定義に反する)
2. **未読込チャンクの区域に居る(または近づく)動的剛体は島ごと強制的に休止させ、読込の完了で再開する。** 確定した案: 静的世界の候補の選別が「評価できない領域を含む」と答えた剛体を、その細分で島ごと休止させ、その区域の読込が完了した刻みで再開する(判断18の再開の条件の6つ目)。落下を止めるだけであり物理法則は変えない。Issue #17(地形変形)の同型の論点(変形中の区域の上の剛体)も同じ再開の条件で受ける。別の案: ストリーミングが「動的剛体の周囲は常に読込済み」を保証する(必要集合の中心が複数になり、Issue #1の判断8の契約を変える)。落下を続ける(未読込の地面を通り抜けて世界の底へ落ち、読込後に深い初期重なりになる)
3. **回転の速い運動学的剛体(回る床の縁)が薄い物を通り抜けない保証は初版の範囲に含めない。** 確定した案: 世界の形と衝突基盤の連続衝突は平行移動の還元だけであり、回転を含む連続衝突(保守的前進)は未実装である。Issue #40は離散の接触で閉じ、判断20の方針の型だけを置く。別の案: 保守的前進を同基盤へ足してからIssue #40を閉じる(Issue #40の完了が同基盤の追加実装に依存する)
4. **初版の対象形状は箱(任意姿勢の直方体)と静的世界だけである。** 確定した案: 同基盤の接触点集合は直方体どうしと直方体対三角形だけであり、球の形状型は無く、カプセルの接触点集合は未実装である。Issue #40の「球を床へ落とす」「反発0と高反発の比較」は箱で代替し、「球が転がる」(Issue #7の現象)は球の形状型と接触点集合を同基盤へ足した日に検収を足す。別の案: 球を同基盤へ足してから着工する(球対平面・球対直方体の接触点集合は1点であり実装は小さいが、Issue #40の完了が同基盤の追加実装に依存する)。カプセル剛体まで含める(キャラクターを動的剛体として積み・押し合いへ参加させる要求が立った日に採る)
5. **剛体の状態と接触解法の精度は単精度である。** 確定した案: XPBD共通拘束基盤の型(`位置<ワールド>`・`逆キログラム`・`ラグランジュ乗数`)が単精度であり、GPU経路との突き合わせとレイアウト契約が同じ精度で書ける。接触点集合は倍精度で判定して返るため、単精度へ狭めるのは判断11の局所点への写しの1箇所である。別の案: 倍精度(Issue #7の裁定材料の1.8節の推奨。XPBDの型を倍精度で二重に持つか総称化することになり、GPU経路の突き合わせが精度の差を含む)。単精度で箱10段の塔の検収が通らなかった日に、倍精度の参照計算を検収専用に足す
6. **CPU区間予算「剛体の1刻み」の暫定値は0.5ミリ秒、登録できる動的剛体の暫定上限は1024個である。** 確定した案: 性能予算の節の見積りに基づく。値はIssue #40の実測の日に確定し、上限はIssue #16で見直す

## この設計で直接扱わないこと

- 関節・可動限界・駆動の各拘束の式と検収(Issue #8。実装はIssue #41。本文書は判断10の部品と島と再開への参加だけを持つ)
- 破壊による剛体の生成と接続の変化(Issue #16。本文書は工程1の構造変更として受ける位置だけを持つ)
- 質量特性・衝突形状・表面物性・局所アンカーの制作時の生成と焼き込み(Issue #19。本文書は実行時の型と検証だけを持つ。初版の質量特性は原始形状の解析式で作る)
- キャラクターの移動の意味付け(Issue #3。キャラクターは掃引の1点接触で動き、動的剛体ではない)
- 流体・粉体との結合(Issue #18の別解法との交換)
- 一般の凸形状・カプセル・球の接触点集合と回転を含む連続衝突(世界の形と衝突基盤の積み残し)

## 進行の記録

- 2026-09-02: 制定した。Issue #7の裁定(設計ノート19節・裁定候補18項・要求10項・具体契約12項)とIssue #5の横断裁定を判断23件へ写し、codexの設計レビュー3巡(指摘7件+3件+1件)と文章検査を経て確定した。未決定の事項6件は推奨の側で確定して進める。
- 2026-09-02: 実装順1(Issue #39)を実装した。`blitz_math` へ判断5の力学の単位型と慣性テンソルと回転の力学の演算(43ef5a01)、`blitz_sim` へ判断2〜4・6の剛体の状態の型と台帳の最小形(登録・参照・作用の6操作)、判断8の細分の予測器、判断9の参加者と補正と速度の再構成、判断10の3つの検証拘束と剛体と点の距離拘束とそれらを回す剛体の参照計算(35a0a804)、判断22の4つのバッファの契約とconformの定数の組と剛体のモジュール群の `[f32; 3]` の禁止(次のコミット)を置いた。検査は判断3・4・6・8・9・10・19の28本(反証を含む)であり、実測は各判断の下の実装の細目の段落と `_doc/計測/剛体の姿勢自由度_2026-09-02.md` にある。正本と食い違った点は2つあり、いずれも正本の式のまま実装して検査の側を正本の意図へ合わせた。(a)判断8の反証(4)は速度の再構成を通した全体の経路では観測できないため、予測の角速度をそのまま次の細分へ渡す経路で固定した(判断8の実装の細目)。(b)判断10の「軸に垂直な回転が変わらない」は慣性が等方のときに限って厳密であり、異方な慣性では補正の回転の軸 I⁻¹ n が軸から傾く(判断10の実装の細目)。実装順1の項が挙げる判断1の依存の追加(`blitz_sim → blitz_collision`)と判断16の接触の特徴の識別への `Ord` の導出は、本便では接触の型を1つも置かず依存が実際に要らないため行わず、接触の生成を置くIssue #40の最初に行う。判断23の台帳の公開面のうち破棄・運動種別の遷移・材質の設定もIssue #40である。
- 2026-09-03: codexのレビューのP1(質量特性の読み手が主軸を回転の生成だけで復元し、非数の主軸を通して解法の全体を非数にし、非単位長の破損を正規化で隠す)を是正した。読み手は主軸の4成分の有限と単位長(許容差は姿勢と同じ1e-4)を回転の生成へ渡す前に検査して型付きエラーで拒み、`blitz_math` の回転の生成も非有限の成分を拒む(非単位長の正規化はアセットの誤差の吸収として維持)。反証は非数・無限大・長さ2・長さ0の主軸と、回転の生成の非有限の成分の拒否である(判断22の実装の細目)。
- 2026-09-03: 実装順2(Issue #40)の段1を実装した。判断1の依存の追加(`blitz_sim` → `blitz_collision`。conformの白リストとXPBD共通拘束基盤の層の表を同時に更新)、判断16の接触の特徴の識別への `Ord` の導出、判断11の接触拘束の2つのバッチと接触点集合からの生成、判断12の非貫通の片側XPBD位置拘束、判断15の接触物性の混合則を置いた。検査は22本(全順序2本・混合則5本・生成4本・非貫通8本と既存の反証)であり、実測は各判断の下の実装の細目にある。正本と食い違った点は無く、正本が言い切っていなかった2点を実装の細目へ書き足した。(a)判断12の乗数の下限0は、隔たりが0以上の点を読まない規約の下では算術の上で自動的に満たされ、実際に効くのは丸めのときだけである。(b)判断12の「1細分あたりの補正の長さの上限」は乗数の上限へ換算して実装した(細分の中で乗数が単調に増えるため、乗数の上限がその細分の補正の総量の上限になる)。島・休止・細分の工程・静止摩擦・速度段階・接触の履歴・台帳の公開面・`blitz_app` の配線は段2以降である。
- 2026-09-03: 実装順1(Issue #39)を閉塞した。命名検査は違反0件、構造検査は違反2件(補正を作る式が所有者を外部引数に取る形・内部専用の項目への説明の注釈)を6f93f13e・e385cd2bで是正した。codexレビューは1巡で本物の欠陥1件(GPUから読み戻す主軸の非数が四元数の生成を素通りする)を検出し、64dd7ee4で是正して合格した。諮った論点(異方の慣性でねじりの補正の軸が I⁻¹n へ傾き「軸に垂直な回転が変わらない」が成り立たない)は、I⁻¹n が正しく本文書は既に条件を明記しているため改訂不要と確定した。検証列は全緑である。角運動量の散逸は600細分で4パーセントであり、保存する形の回転の積分(指数写像と対数写像の組)を採るかどうかはIssue #43で反復の回数と一緒に見直す。
- 2026-09-03: 段1のcodexのレビューの修正必須の2件を是正した。1件目は1細分の補正の長さの上限であり、毎反復の有効逆質量で乗数へ換算していた上限を、実際に適用した相対補正の長さの累計で管理する形へ直した(判断12の実装の細目)。反証は同じ拘束へ有効逆質量が5から1へ下がる2つの姿勢の参加者を順に渡すもので、換算する実装では厚み1メートルに対して1.8メートル詰まる。2件目は混合則の組み立ての二重登録であり、`insert` の戻り値で重複を判定して拒む前に先の値を置き換えていたのを、`entry` で空いているときだけ挿入する形へ直した。反証は拒否の後に組み立てた混合則が先の表面物性と先の上書きをそのまま返すことである。あわせて、その細分で離れていた接触点を静止摩擦と速度段階の対象外にする段2の契約を判断12の実装の細目へ書いた。検証列は全緑である。
- 2026-09-03: 実装順2(Issue #40)の段1を閉塞した。命名検査は違反0件、構造検査も違反0件(境界事例3件は親裁定で違反に数えない)である。codexレビューは3巡で本物の欠陥4件を検出し、全件を是正した。内訳は、混合則の二重登録の拒否が既存の値を先に置き換えていたこと、1細分の補正の長さの上限を乗数へ換算していたため反復のあいだに有効逆質量が変わると累計が上限を超えること、姿勢の補正を有限回転として適用するため接触点の実際の移動が線形化した上限を超えること(実測1.068メートル)、上界が保証するのは絶対値の関係だけであるのに負の増分で予算を戻していたこと(実測2.531メートル)である。後ろ2件は、上限の換算と残りの長さの管理を有効逆質量の上界 w + |(I⁻¹ (r × n)) × r| で行い、予算へ足す長さを max(Δλ, 0) × 上界 として単調にする形で解いた(正典式の分母は真の有効逆質量のままであり収束の式は変えていない)。段2へ送る契約は、その細分で離れていた接触点を静止摩擦と速度段階の対象外にすることである。
- 2026-09-03: 実装順2(Issue #40)の段2を実装した。判断16の接触の履歴(鍵の昇順の一覧と併走の対応付け)、判断13の静止摩擦の位置拘束、判断14の速度段階の反発と動摩擦を置いた。検査は26本(履歴の併走4本・剛体どうしの履歴と静止摩擦4本・静止摩擦の枝4本・等方性1本・動摩擦2本・反発2本・暖機1本・坂5本・落下2本・錨の保持1本)であり、実測は各判断の下の実装の細目と `_doc/計測/剛体の接触の摩擦と反発_2026-09-03.md` にある。細分の工程(判断19)そのものは後続の便であるため、坂と落下の場面は `contact/scene/` の試験専用の材料で回した。正本と食い違った点は3つあり、いずれも実装を正本の式のままにして細目へ記した。(a)滑走の減速度は正本が求める相対誤差1パーセントに届かず2.7パーセントである(判断12の乗数の単調性と面と面の4点の逐次の解き方で、法線の乗数の合計が真の法線力積を9パーセント上回る)。(b)静止摩擦が保持できる傾きの上限は静止摩擦係数そのものではない(円錐の判定が点ごとに厳しくなる)。円錐の判定を接触多様体の単位で行う改訂を後続の便で裁定する。(c)速度段階は反発だけを数巡させる口を持つ。1巡では平らな箱の跳ね返りに角速度1.36ラジアン毎秒の偽の回転が残る。段1から送られた契約の解釈を1つ定めた。対象外にするのは「最後の反復で離れていた点」でなく「その細分で一度も押し戻していない点」である(判断12の実装の細目の追記)。あわせて `blitz_math` の `速度::単位方向` が成分を正規化せずに単位方向の生成へ渡す欠陥を直した(動摩擦の衝撃が一度も適用されていなかった)。島・休止・細分の工程・運動種別の遷移・台帳の公開面・`blitz_app` の配線・11場面の命令の登録は段3以降である。
