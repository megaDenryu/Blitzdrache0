# 楽曲エディター

ブラウザで動く楽曲の打ち込みの道具を、ゲーム開発用エディターの1つのツールとして立てるための設計正本である。生存型文書であり、実装とずれたら実装に合わせて直す。土台の層と機械強制は `_doc/設計/ゲーム開発用エディター基盤.md` が持ち、本文書はその上に楽曲の領域だけを足す。工事の段取りは `_doc/計画/ゲーム開発用エディター計画.md` が持つ。

## 発端と素材

`_doc/ゲーム開発用エディター計画/シーケンサー.html`(2088行、Gemini 3.7 Flashの試作)が作る内容の定義である。同じ場所の `server.js` は、その試作が状態をやりとりするための最小サーバーであり、本エディターでは編集サーバーの経路へ置き換えて破棄する。素材はログ型であり、追記も改稿もしない。

試作が備える機能は次のとおりである。コード進行が許す音だけを打てる格子(4トラック×音域行×32ステップ)、コード進行の編集と独自進行の登録、Web Audioによる楽器合成10種、パターン(節)と曲構成(パターンの並びと繰り返し回数)、ミキサー(全体音量・残響・遅延・遅延の長さ)、トラックごとの音量と楽器と進行の割り当て、JSONの読み書き、サーバー状態の受け取りと送り出し。

## 目的

1. **人がブラウザで楽曲を打ち込める。** コード進行が許す音を格子が示し、外れる音は既定で打てない
2. **AIが人と同じ意味の操作を、画面なしで実行できる。** 操作の意味が画面用とAI用で割れない(基盤の判断1と同じ形)
3. **音を鳴らす部分は汎用ライブラリへ出し、エディターは最初の利用者にすぎない**

## 判断1: 音はSengenAudio(新設submodule)が担い、Web Audioの生APIを画面層に出さない

Three.jsに対するSengenThreeと同じ位置づけの薄い層を、Web Audioの上に汎用ライブラリとして新設する(megaDenryu/SengenAudio、public、`editor_web/submodules/SengenAudio`)。範囲はWeb Audioのドメインから決め、エディター固有のものは入れない。

SengenAudioが持つもの。

- **音声の作業場**: `AudioContext` の生成と再開(ブラウザは人の操作を起点にしないと音を出さない)の包み。生の `AudioContext` を外へ出さない
- **音声グラフの宣言的配置**: 効果と出力の接続を、式の入れ子が信号の流れを写す形で書く(基盤の判断3と同じく、配置するものを並べる形)
- **資源の所有と破棄**: 生成したノードは生成した部品が所有し、破棄が配下の切断まで責任を持つ。鳴り終わった音源の後始末を呼び出し側の記憶に頼らない
- **先読みの演奏予定表**: 音を出す時刻を音声の時計へ前もって予約する仕組み。試作の `setInterval` で1音ずつ鳴らす形は採らない(判断2)
- **合成技法の部品**: 加算合成、撥弦合成(Karplus-Strong)、周波数変調合成、減算合成、雑音源と濾波器。これらはWeb Audioの一般的な使い方であり、楽曲エディター固有ではない
- **音の値オブジェクト**: 周波数(ヘルツ)、音高番号、音量の比、秒。単位の混同を型で止める

SengenAudioが持たないもの。楽器の一覧(「グランドピアノ」「ウッドベース」のような選択肢の並び)、音楽理論(音階・和音・コード進行)、格子の編集、楽曲の保存形式。これらはエディター側にある。合成技法は汎用、その技法を特定の音色として名づけた一覧はエディター、という切り方である。

## 判断2: 演奏の時刻は音声の時計が正本であり、画面の時計で音を出さない

試作は `setInterval` を1ステップごとに起こして、起きたその瞬間に音を出している。ブラウザのタイマーは数ミリ秒から数十ミリ秒ずれ、他の処理で遅れるため、この作りでは拍がよれる。

正しい形は、短い周期で起きる先読みの見張りが、音声の時計の現在時刻から一定の先(たとえば100ミリ秒先)までに来る音を、時刻を指定して前もって予約することである。予約した音の時刻は音声機器の時計が保証するため、見張りが多少遅れても拍はずれない。

画面のプレイヘッド(いまどのステップを鳴らしているかの表示)は、音声の時計の現在時刻から毎フレーム導く。画面の更新が遅れても音は遅れず、音が進んでも画面は自分の速さで追いつく。この分離は、エディター制作スキル第1条(表示は正本と一致する)を時間軸へ翻訳したものである。

## 判断3: 格子のセルはマジック値でなく判別共用体で持つ

試作はセルの値を0から4の整数で持ち、1が打点、2が伸ばし、3が制約の外で打った点、4がその伸ばし、0が無しである。この形は「無し」をマジック値へ密輸しており、値の意味が `isStart` と `isHold` という2つの述語の中にしか無い。

編集モデルは次の形で持つ。セルは `打点なし` か `音の始まり` か `音の継続` のいずれかであり、`音の始まり` は進行の制約に従うか従わないかを併せ持つ。保存形式(型契約)は直列化の都合で素の数値になるが、その変換は境界1箇所に閉じる。

## 判断4: コード進行の参照は判別共用体であり、既定と独自を同じ名前空間に混ぜない

試作の `getProgByKey` は、まず既定の進行を識別子で探し、見つからなければ独自の進行を名前で探す。独自の進行に既定と同じ名前を付けると、どちらを指しているか決まらない。

参照は `既定の進行(識別子)` と `独自の進行(名前)` の判別共用体にする。楽曲の保存形式でも同じ判別を持ち、読み込み時に実在しない参照は無言で既定へ落とさず、明示の失敗にする(基盤の判断2と同じ規律)。

## 判断5: 打楽器の音は音高ではなく打楽器の種類であり、同じ欄に混ぜない

試作は音符の音高欄に、旋律のトラックでは数値の音高番号を、打楽器のトラックでは `bd`・`sn`・`hh` という文字列を入れている。1つの欄が2つの意味を持つため、音高としての計算(周波数への変換)を打楽器に対して呼べてしまう。

トラックの音の並びは `音高の行(音高番号)` と `打楽器の行(打楽器の種類)` の判別共用体で持ち、トラック1本の中では同じ種類だけが並ぶ。周波数への変換は音高の行だけが持つ。

## 判断6: 永続化は既存のプロジェクト保管庫を通し、試作の状態ポーリングは移植しない

試作は2秒ごとに `GET /state` を叩き、返ってきた状態を丸ごと画面へ適用する。人が編集している最中でも上書きされるため、編集中の作業が黙って消える。

本エディターでは、楽曲は他の編集対象と同じく `プロジェクト保管庫` の1つの資源であり、`editor_data/楽曲/<楽曲ID>.json` に置く。読み込みと保存は人の明示の操作である。AIは編集サーバーの経路(`GET|PUT /api/楽曲/{楽曲ID}`)か、ヘッドレスの適用入口から同じ操作コマンドを積む。試作の `server.js` はこの経路に吸収されるため移植しない。

楽曲の識別子は置き場のファイル名であり、ファイルの中の名乗りと食い違うときは台帳の読みが拒む(基盤の判断11と同じ形)。

## 判断7: 楽曲はトラックの構成を自分で持ち、実装へ焼き込まない

試作はトラック4本とその音域行を実装の定数として持っている。楽曲データの側がトラックの構成(名前・種類・音の並び・既定の楽器)を持つ形にし、実装が持つのは新しい楽曲を作るときの既定の構成だけにする。

理由は2つある。トラックの本数や音域が実装の定数だと、曲ごとに音域を変えることが不可能になる。そして保存済みの楽曲を読むときに、実装の定数が変わると過去の楽曲の意味が黙って変わる。

## 判断8: パターンの参照は安定した識別子であり、並びの位置ではない

試作の曲構成は、パターンを配列の位置(0から始まる番号)で指している。そのためパターンを1つ消すたびに、曲構成の側で番号の繰り上げをやり直している。位置が意味を持つ形は、並べ替えを足したときにも同じ手当てを要求する。

パターンは生成のときに安定した識別子を受け取り、曲構成はその識別子で指す。存在しない識別子を指す曲構成は、読み込みのときに明示の失敗にする。

## 判断9: 既定のコード進行は和音の中身まで型契約が持ち、ブラウザは生成された値を読む

既定のコード進行8件は、名前と和音の並びが決まった定数のデータである。当初はサーバーが識別子の実在だけを見て、和音の中身はブラウザが持つ形にしたが、これは同じ定数の写しを2つ作る。識別子を足したときに片方だけが変わる余地が残る。

正本はサーバー側の型契約に置き、和音の中身ごと持つ。ブラウザは型の生成と同じ経路で値として受け取り、自分では定数を持たない。既存の建物外形カタログの形式版が値として生成されているのと同じ手である。

## 判断10: 楽曲は形式版を持ち、後方互換は版ごとの型から現在の型への変換だけが担う

この工事の他の資源(チャンク構造・建物の格子・建物外形カタログ)はいずれも形式版の欄を持ち、旧版の読みは版ごとの型から現在の型への変換が受け持っている。楽曲だけが形式版を持たないと、欄を1つ足した時点で保存済みのファイルが読めなくなる。

楽曲は形式版を持つ。現在の型へ既定値を持たせて欄の不在を埋める形は採らない(グローバル規約「シリアライズデータの後方互換は版ごとの型と最新への変換で表す」と同じ)。

## 判断11: 初版で作らない操作と、その理由

操作コマンドの枝は、作らないものにも理由が要る。作れないのではなく、作る順序を後にする判断である。

**トラックの音の並び(音域)を変える操作は初版で作らない。** 判断7のとおり音の並びは楽曲データの欄であるため、変える操作を持つこと自体は筋が通る。作らないのは、格子が行を位置で指しているためである。音域を変えると、その位置が何の音を指すかが変わり、既に打ち込んだ格子の意味が黙って変わる。行の追加と削除に対して格子をどう動かすかを決めてから作る。初版は楽曲を作るときの既定の構成を使う。

**トラックの追加と削除も初版で作らない。** 上と同じ理由に加えて、トラックを位置で指す前提が崩れる。トラックを消すと、その後ろのトラックを指していたコマンドの意味が変わる。作るならトラックにも安定した識別子が要る。

**パターンの並べ替えは作らない。** パターン一覧の並びは曲の再生に影響しない。演奏の順序を決めるのは曲構成であり、そちらには並べ替えがある。

## 判断12: ブラウザは適用の入口で契約の検証を呼ばず、触る指し先だけを触る直前に確かめる

操作コマンドの検証(値の範囲・指し先の実在)はRust側の型契約が持ち、TypeScript側には無い。したがって画面が積むコマンドは検証を通っていない。ここで「検証をTypeScriptへも移植して適用の入口で呼ぶ」道は採らない。判断1が適用の意味の正本をTypeScriptの1実装に限ったのと同じ理由が、逆向きの複製にも働くためである。検証を両言語に持つと、値の範囲の細部が必ず割れる。

分担は次のとおりである。保存の経路での検証はサーバーが持つ。ブラウザの適用は、自分が実際に添字で触る指し先だけを、触る直前に確かめて失敗させる。範囲の外を既定値で埋めてはならない。埋めると、将来検証が緩んだときに「打点なし」として黙って通る。

この分担は画面の作りを決める。**画面は「打てない場所を打たせない」ことを自分で担い、適用層が投げる失敗は利用者への案内ではなく、画面の配線の誤りを開発時に露見させるためのものとして扱う。**

## 判断13: 打ち込みのドラッグ1回は1つのコマンドであり、途中の見た目は画面が持つ

試作は、ドラッグの途中で升目を通過するたびに格子を直接書き換えている。この形をそのままコマンドへ写すと、1回のドラッグが数十個のコマンドになり、取り消しを数十回押さないと元へ戻らない。

ブラシの1ストロークを1コマンドにする既存の規律(基盤の判断1)を打ち込みへも適用する。ドラッグの間、画面は「離したときにこうなる」という見込みを自分で描き、コマンドは積まない。指を離したときに、始まりのステップと終わりのステップを持つ1つのコマンドを積む。

見込みの絵は画面層だけが持ち、編集モデルへ書き込まない。したがって、ドラッグの途中で画面を閉じても編集モデルは壊れない。

**同じ規律をつまみのドラッグへも適用する。** つまみ(テンポ・ミキサー・トラックの音量)を動かしている間は数と位置の表示だけを動かしてコマンドを積まず、手を離したときに1つだけ積む。理由は打ち込みと同じで、1回のドラッグが数十件のコマンドになると取り消しが数十回必要になり、取り消しが実質使えなくなるためである。

**同じ規律を文字の入力へも適用する(2026-08-28 追加)。** 名前の欄(楽曲の表示名・建物の表示名)へ打っている間はコマンドを積まず、文書タブの見出しのような見えだけを追随させ、欄から離れたとき(または入力が確定したとき)に1つだけ積む。打つ前と同じ綴りへ戻っていたときは積まない。理由はドラッグと同じで、10文字書き換えると取り消しの段が10増えて取り消しが実質使えなくなるためである。この規律は楽曲と建物のどちらのエディターでも同じでなければならないため、規律の本体は `editor_web/src/文書の表示名の編集/` の1箇所が持ち、両ツールがそれを使う。

## 判断14: メトロノームの音は楽曲のデータに含めず、曲の音の経路も通さない

メトロノームとは、再生中に拍の頭で音を鳴らして拍を数えさせる道具のことである。1拍を4ステップとする取り決め(単位と値の型の節)に従い、通しステップが4で割り切れるところで鳴らし、16で割り切れるところ(小節の頭)を他の拍と違う音にする。

**メトロノームの音は楽曲のデータに含めない。演奏を助ける道具であって曲の内容ではないためである。** したがって打点として格子に現れず、保存の中身にも入らず、操作コマンドも積まない。入と切の状態は演奏サービスが持ち、画面を閉じれば消える。同じ理由から、入と切を切り替えても取り消しの履歴は動かない。

**曲の音の経路(ミキサー・残響・遅延・圧縮)を通さず、専用の音量から出口へ直接出す。** 曲の全体の音量を絞ったときや、曲が大きく鳴って圧縮が効いたときに拍の聞こえ方が変わると、拍を数える道具として使えなくなるためである。実装は曲の音とは別に音声の出力先をもう1つ持つ。音声の出力先は作業場の同じ出力ノードを包むだけであり、破棄が切るのはそのノードから出る接続(存在しない)であるため、2つ持っても互いの音を止めない。

**拍の時刻は演奏の予定表へ予約し、画面のタイマーでは鳴らさない(判断2と同じ規律)。** 予約は入と切に関わらず積み、鳴らす瞬間に入っているかを読む。予約の側で止めると、切ってから先読みの幅のあいだ鳴り続けてしまうためである。

**再生していないときは鳴らさない。** このエディターは格子へ置く打ち込み型であり、拍を体で取りながら弾いて録る操作が無い。メトロノームが要るのは、打ち込んだものが拍のどこに乗っているかを耳で確かめるときであり、それは再生中にしか起きない。再生と切り離してメトロノームだけを鳴らす形にすると、演奏の進行と並ぶ第2の進行が要り、1つしかない演奏の予定表を2つの主が始めたり止めたりする調停も要る。得られるものに対して構造が重い。

## 判断15: 曲構成は横のタイムラインで見せ、カードの操作は節を単位にする

issue #72(https://github.com/megaDenryu/Blitzdrache0/issues/72)の吟味の結果(https://github.com/megaDenryu/Blitzdrache0/issues/72#issuecomment-5552073741)が定めた判断である。原案は縦リストしか持たない曲構成パネルへ、曲全体の時間の並びを見せる横方向のタイムラインを足すことを求めた。

**カードとは、曲構成に並ぶ節の展開の1小節ぶんを表す、タイムラインの1枚の部品のことである。** `カード位置`は、節の位置・繰り返しの何回目・小節の番号の3つで決まる。カードの部品は、そのカードが曲の何小節目にあたるかを表示する。

**タイムラインは、`A × 4, B × 2, C × 1` のような曲構成を `[A][A][A][A][B][B][C]` の並びへ展開して見せる。** 展開は編集モデルの計算が担い、通しステップからカードを求める計算とあわせて持つ。

**カードの操作は、そのカードを含む節を単位にする。** 現状の操作コマンドは節(パターンの名乗りと繰り返し回数の組)を単位にしており、カードは節を展開した見えである。カード1枚ごとに独立した操作を持たせると、節を4回繰り返すときの2枚目と3枚目のカードの間へ差し込む操作が、節の分割になり、3つのコマンドへ割れる。

**曲構成の操作(削除・複製・前へ挿入・後へ挿入・前へ移動・後へ移動)はすべて節の枠のボタンであり、カードは選択と小節番号の表示だけを受け持つ。** カードが1小節になると、カードの削除や複製が「パターンの1小節を消す・増やす」と読めてしまい、実体である節の繰り返し回数の増減と一致しないためである。同じ節に属するカードは節の枠で括り、枠に「A × 2」のようにパターンの表示名と繰り返し回数を出す。操作をカード単独のボタンにすると見た目と結果がずれる。`[A₁][A₂][B]` で A₂を含む節の「後へ移動」を押すと利用者は`[A₁][B][A₂]`を期待するが、カードごとに `曲の節を並べ替える` を積む実装は節ごと動くため`[B][A₁][A₂]`になる。すべての操作のボタンを節の枠へ置くことで、ボタンの位置(枠)と変わる範囲(枠が括るカード全部)が一致する。

カードの操作は次の表のとおり節のコマンドへ写す。

| 利用者の操作 | 押しどころ | 画面が積むコマンド |
| --- | --- | --- |
| 末尾へ追加 | タイムライン末尾のボタン | `曲の節を追加する`(選択中のパターン、1回) |
| 削除 | 節の枠 | 節の繰り返し回数が2以上なら `曲の節を変える`(回数を1減らす)、1なら `曲の節を削除する` |
| 複製 | 節の枠 | `曲の節を変える`(回数を1増やす)。回数が上限の8なら画面は押せない状態で出す |
| 前へ挿入・後へ挿入 | 節の枠 | 新設する `曲の節を挿入する`(節の位置、パターンの名乗り、繰り返し回数)。画面はそのカードと同じパターンを1回分挿入する |
| 前へ移動・後へ移動 | 節の枠 | `曲の節を並べ替える`(節ごと) |

**カードの操作の後、選択中のカードは操作したカードへ追従する。** 追従先は、移動なら移動先、前へ挿入なら1つ後ろの位置、後へ挿入なら元の位置、複製なら元の位置、削除なら未選択である。追従が無いと、選択の強調と格子が映すパターンが操作後のカードの並びとずれる。`[A][B][C]` で B を選んで「前へ移動」を押すと曲構成は `[B][A][C]` になるが、追従を計算しなければ選択は位置1のままとなり、強調は A に付き、格子は選択中のパターン B を映したままになる。この計算は純粋計算であり、単体試験で全部の枝を固定する。**追従は小節の番号を保つ。** 選択していたカードがパターンの何小節目であったかは操作の前後で変わらない。移動なら移動先の同じ小節番号、複製なら複製前と同じ小節番号のカードへ選択が移る。

**新設する操作コマンドは `曲の節を挿入する` である。** `曲の節を追加する` と `曲の節を並べ替える` を組み合わせても同じ並びは作れるが、途中に末尾へ足された曲構成が現れ、契約を読む側に「挿入」という意思が伝わらない。現状の型契約が `曲の節を並べ替える` を削除と追加へ割らなかったのと同じ理由である。挿入したカードのパターンを別のものへ変える操作は、右サイドバーの曲構成パネルにある、節ごとの行(パターンの選択欄)が引き続き受け持つ。

**曲構成パネルは節の詳細だけを残す。** 追加・削除・並べ替えの押しどころをタイムラインとパネルの2箇所に置くと、同じ操作の押しどころが2つになる。曲構成パネルからは追加ボタンと上下移動と削除のボタンを外し、節ごとのパターンの選択欄と繰り返し回数の選択欄と、曲全体の小節数の案内だけを残す。どちらの区画も同じ操作コマンドを通し、編集状態は `楽曲.曲構成` の1つだけである。

**タイムラインはエディタ領域の固定の行へ置き、下パネルは使わない。** 基盤の設計正本の判断14は、時間の方向を扱う区画として下パネルを挙げ、楽曲エディターは下パネルを要らないとしている。タイムラインは人が編集中ずっと見るものであるため、下パネルではなくエディタ領域へ置く。位置は楽曲名と演奏の操作帯の行の直下、和音の帯の行の上とし、縦にスクロールしない固定の行にする。横に長い曲では、タイムラインの枠の中だけが横にスクロールする。基盤の設計正本の「楽曲は下パネルを要らない」はそのまま成り立つ。

**カードの選択は画面の状態であり、保存にもコマンドにも入らない。** 利用者がカードを選ぶと、そのカードのパターンが選択中のパターンになり、格子がそれを映す。

**再生位置の印は、音声の時計から導いた再生位置(通しステップ)から計算する。** 曲構成のとおりに鳴らしているときは、再生位置の印がタイムラインのカードの上を進む。画面は印の位置をすでに音声の時計から導いている再生位置(通しステップ)から計算し、画面の時計を足さない(判断2と同じ規律)。鳴っているパターンが変わったときに開いているパターンが移る現状の動きは、カードの選択にも及ぶ。

**2つの再生は、演奏の範囲の選択欄と印の有無で見分ける。** いまのパターンを繰り返して鳴らしているときは、画面は再生位置の印を出さず、選択中のカードに繰り返し中の印を付ける。画面は演奏の範囲の選択欄をそのまま使い、利用者は2つの再生を選択欄と印の両方で見分けられる。

**同じパターンを指すカードは同じ表示名を出す。** パターンの編集がその全部に効く現状の形は壊さない。

**呼び名は原案の語をそのまま使う。** 「タイムライン」と「カード」は原案の語のまま使う(登録先は `_doc/計画/ユビキタス言語.md`)。この文書と実装は「プレイヘッド」を使わず、既存の「再生位置の印」を使う。1つのものに2つの語を持たないためである。

**初版で作らない操作。** 判断11と同じ規律で、初版のカードの並べ替えはボタン(前へ・後へ)で行い、ドラッグによる並べ替えは対象外とする。

## 判断16: パターンは1個以上の小節から成る可変長の時間範囲であり、カードは1小節である

issue #87(https://github.com/megaDenryu/Blitzdrache0/issues/87)の裁定である。判断15はカード1枚をパターン1面(32ステップ、2小節)の節の繰り返し1回分とした。この扱いは既存のモデルを壊さないための読み替えであり、「4小節目の後に何かを入れる」という作曲上の操作がカードの境界と一致しない欠点を残していた。

**パターンは`小節数`を持ち、格子は`小節数×16ステップ`の連続した空間である。** この判断は、小節を独立したオブジェクトとして持つことを要件にしない。格子上の位置と小節番号の対応は通しステップから計算で導けるため、この判断は、小節という意味を持たない入れ物のためにオブジェクトの境界を増やさない。

**定数`パターンのステップ数(32)`を廃し、契約の定数を3つ新設した。** `小節あたりのステップ数(16)`・`新しいパターンの既定の小節数(2)`・`パターンの小節数の上限(64。作曲上の意味を持たない技術的な防御の上限であり、必要なら動かす)である。上限は格子の描画や演奏の予定表が扱いきれない大きさを防ぐためのものであり、作曲上の意味としての上限は設けない。

**楽曲の形式版を2へ上げ、形式版1の楽曲はパターンへ小節数2を名指しで置いて変換する。** 判断10の様式(版ごとの型から現在の型への変換)に従う。旧版の32ステップのパターンは小節数2のパターンへ変換され、打点の位置と長さは書き換えない。

**操作コマンド`パターンを追加する`は小節数を引数に足し、`パターンの小節数を変える`を新設した。** 小節数を増やすと格子の末尾に空の小節が足され、減らすと末尾の小節が消える。消えた小節の打点は差し戻しで元へ戻る。

**打点の位置の検証は定数でなく、指し先のパターンの長さで行う。** パターンごとに小節数が違うため、`パターンのステップ数`のような固定の定数を境界として使うことはできない。同じ理由で、和音の帯はパターンの長さぶんで周回と打ち切りをする。

**タイムラインはパターン参照を1小節単位のカードへ展開して表示する。** カードはパターンでも節でもなく1小節である。同じ節に属するカード(パターンの小節数×繰り返し回数の枚数)は判断15の節の枠で括り、枠にパターンの表示名と繰り返し回数を出す。枠を節の単位のまま置くのは、繰り返しの回数を持つのが節であり、枠のボタンが積む操作も節のコマンドであるためである。タイムラインは、カードの小節番号で曲全体の時間軸を、節の枠でパターンの構造(どの小節がどのパターンに属するか)を、同時に見せる。

**パターンの内側の小節の境界へ別のパターンを差し込む操作は作らない。** その操作はパターンの分割であり、可変長の時間範囲というパターンの定義の外側にある。必要になれば別のissueで扱う。

## 層への写像

基盤の層の表をそのまま使う。楽曲ツールの中身は次のように置く。

| 層 | 楽曲ツールが置くもの |
| --- | --- |
| 型契約(生成) | 資源`楽曲`と操作コマンドの型。Rustのts-rsが生成する |
| 編集モデル | 音楽理論(ピッチクラス・和音の種類・和音・コード進行・和音が許す音)、格子、パターン、曲構成、ミキサー設定の値と純粋計算 |
| 操作コマンド | 打点を置く・消す・伸ばす、テンポの変更、楽器の変更、トラック音量の変更、ミキサーの変更、進行の割り当て、独自進行の保存と削除、パターンの追加と削除、曲構成の編集 |
| 境界 | プロジェクト保管庫接続の楽曲の読み書き |
| 画面 | 中央に曲構成のタイムラインと打ち込みの格子と進行の帯と演奏の操作帯、右サイドバーにパターン・曲構成・トラックの設定・コード進行・ミキサー・永続化、SengenAudioによる演奏 |
| ツールルート | 楽曲編集ツールの配線 |
| 入り口 | エクスプローラーの「楽曲」項目と楽曲の文書タブ |

音楽理論を編集モデルへ置くのは、それがWeb Audioの上の層ではないためである(判断1)。

## 単位と値の型(数学DDD)

素の数値で持たず、次を値オブジェクトにする。時間の単位が3種類あるため、混同は必ず起きる。

- `テンポ`: 1分あたりの拍の数。40から300
- `ステップ`: 16分音符1つ分の位置と長さ。1拍は4ステップ、1小節は4拍=16ステップ。パターンの長さは小節数×16ステップであり、新しいパターンは2小節である
- `秒`: 音声の時計の単位。`ステップ`から`秒`への変換は`テンポ`を要し、変換メソッドは1箇所だけが持つ
- `音高番号`: 0から127。中央のラが69
- `ピッチクラス`: 0から11。音高番号の12の剰余
- `周波数ヘルツ`: 音高番号から `440 × 2^((音高番号 − 69) / 12)` で導く
- `音量の比`: 0から1
- `繰り返し回数`: 1以上
- `小節数`: 1以上、防御の上限64(判断16)

型契約(保存形式)はこれらを素の数値で持つ(基盤の判断8の素データ様式)。値オブジェクトへの変換は編集モデルの入口が担う。

## 試作からの意図的な改変

移植は数式を写すが、次は意図して変える。移植した実装が試作と同じ音を出すことを確かめる突き合わせでは、この一覧を例外として扱う。

1. **演奏の時刻**: `setInterval` の即時発音から、音声の時計への先読み予約へ変える(判断2)
2. **セルの値**: 0から4の整数から判別共用体へ(判断3)
3. **進行の参照**: 識別子と名前の同一名前空間から判別共用体へ(判断4)
4. **打楽器の音**: 音高欄への文字列の混在から判別共用体へ(判断5)
5. **サーバー同期**: 2秒ポーリングの丸ごと適用から、明示の読み込みと保存へ(判断6)
6. **トラックの構成**: 実装の定数から楽曲データへ(判断7)
7. **雑音の生成**: `Math.random` を種つきの疑似乱数へ置き換える。同じ楽曲が同じ音を出すことを検査できるようにするためである(基盤の判断6と同じ規律。散布の移植で採った改変と同じ)
8. **パターンの参照**: 配列の位置から安定した識別子へ(判断8)
9. **`prompt` による名前入力**: パターンの新規作成が `window.prompt` を使っている。画面の部品で置き換える

## 試作との操作の対応表(2026-08-28作成)

`skills/エディター制作` の第6条に従い、試作 `_doc/ゲーム開発用エディター計画/シーケンサー.html` の操作を全部並べ、現行の道具での扱いを決める。方針は `同じ` `意図して変更` `廃止` `別の手段へ置換` のどれかを必ず持つ。**「移植する」と分類したもののうち未実装のものが残っている限り、楽曲エディターは受理しない。**

### 再生

| 試作での行為 | 試作で見える結果 | 現行での行為 | 現行で見える結果 | 方針 |
| --- | --- | --- | --- | --- |
| 「再生 / 停止」を押す | 文字が停止/再生へ変わり、音が鳴り、再生位置の印が進む | 演奏の操作帯の再生を押す | 同じ | 同じ |
| (なし。試作は音を出せないとき何も起きなかった) | なし | 再生を押してブラウザが音を拒む | 操作帯に「ブラウザが音を止めています。もう一度、再生を押してください。」が出て、ボタンは「再生」のまま | 意図して変更(押しても画面が何も変わらない操作を作らない) |
| 鍵盤の空白を押す | 再生と停止が切り替わる。入力欄にいるときは効かない | 同じ | 同じ | 同じ |
| テンポのつまみを動かす | 数字が変わり、再生中なら速さが変わる | 演奏の操作帯のつまみを動かす | 動かしている間は数字が動き、手を離した時点で速さが変わる | 意図して変更(判断13。取り消しの単位を人の操作へ揃える) |
| なし | なし | パターンだけを繰り返すか曲構成どおりかを選ぶ | 選んだ側で鳴る | 意図して変更(試作は曲構成が空かどうかで暗黙に決まっていた) |
| なし | なし | 演奏の操作帯の「メトロノーム 切」を押す | 文字が「メトロノーム 入」へ変わって枠と文字がアクセント色になり、再生中は拍の頭で音が鳴る。小節の頭は他の拍より1オクターブ高い | 意図して変更(判断14。試作に無い操作を足した) |

### 打ち込み

| 試作での行為 | 試作で見える結果 | 現行での行為 | 現行で見える結果 | 方針 |
| --- | --- | --- | --- | --- |
| 升目を左で押す | 音が置かれ、その音が1回鳴る | 同じ | 同じ | 同じ |
| 升目を右で押す | 音が消える | 同じ | 同じ | 同じ |
| 押したまま同じ行を動かす | 音が伸びる | 同じ | 同じ | 同じ(ただし判断13によりコマンドは離したときに1つ) |
| 押したまま違う行へ動かす | その升目に音が置かれる | 同じ | 同じ | 同じ |
| 装飾キーを押すたびに切り替える | 進行の外モードが入れ替わり、点灯表示が変わる | 同じ | 和音の帯と同じ行にある進行制約の札が変わる | 同じ |
| 「全消去」を押す | いま見ているパターンの打ち込みが全部消える | 演奏の操作帯の「全消去」を押す | 同じ | 同じ |
| 「デモ」を押す | 街の進行・テンポ110・楽器4種が入り、1曲ぶんの打ち込みが入る | 演奏の操作帯の「見本の曲を入れる」を押す | 同じ。ただし取り消しは1回で戻る | 意図して変更(試作は打点を数十回に分けて置くため取り消しが数十回必要だった) |

### コード進行

| 試作での行為 | 試作で見える結果 | 現行での行為 | 現行で見える結果 | 方針 |
| --- | --- | --- | --- | --- |
| 進行を選ぶ | 帯の和音が変わり、合わない打点が消える | 同じ | 同じ | 同じ |
| 「進行を編集/追加」で別窓を開く | 別窓が開く | コード進行のパネルが常に見えている | 別窓が無い | 意図して変更(別窓の開閉そのものが操作の手間である) |
| 別窓で和音の行を足す・変える・消す | 編集中の進行が変わる | 同じ(パネルの中) | 同じ | 同じ |
| 「カスタムとして保存」を押す | 独自の進行として登録され、いまの進行になる | 同じ | 同じ | 同じ |
| カスタム一覧の「削除」を押す | 一覧から消える | 同じ | 使っているパターンとトラックが警告に出る | 意図して変更(試作は黙って壊れた参照を作っていた) |
| JSON欄へ貼って「読み込む」 | 進行が読み込まれる | なし | なし | 別の手段へ置換(楽曲そのものの保存と読み込みが担う) |
| 「現在の進行を書き出す」 | JSON欄へ書き出される | なし | なし | 別の手段へ置換(同上) |
| 経路の口へ取得・送信 | 外部から進行を受け取る・送る | なし | なし | 別の手段へ置換(編集サーバーの経路とヘッドレスの適用入口が担う。設計正本の判断6) |

### トラック

| 試作での行為 | 試作で見える結果 | 現行での行為 | 現行で見える結果 | 方針 |
| --- | --- | --- | --- | --- |
| 楽器を選ぶ | 次に鳴る音の音色が変わる | 同じ | 同じ | 同じ |
| 音量のつまみを動かす | 次に鳴る音の大きさが変わる | 同じ | 動かしている間は数字が動き、手を離した時点で音の大きさが変わる | 意図して変更(判断13) |
| トラックの進行を選ぶ | そのトラックだけ別の進行に従う | 同じ | 同じ | 同じ |

### パターンと曲構成

| 試作での行為 | 試作で見える結果 | 現行での行為 | 現行で見える結果 | 方針 |
| --- | --- | --- | --- | --- |
| パターンを選ぶ | 格子と帯が切り替わる | 同じ | 同じ | 同じ |
| 「新規」を押す | 名前を尋ねる窓が出て、パターンが増える | 「新規」を押す | 名前は自動で付き、パターンが増える | 意図して変更(窓で尋ねる形を使わない。名前は後から変えられる) |
| 「削除」を押す | パターンが消える。1つのときは何も起きない | 同じ | 1つのときは押せない | 意図して変更(押せるが何も起きない形をやめた) |
| 「曲構成」で別窓を開く | 別窓が開く | 曲構成のパネルが常に見えている。パネルの上にタイムラインが常に見えている | 別窓が無い | 意図して変更 |
| 節を足す・変える・消す | 曲構成が変わる | 節の詳細(パターンの選択欄・繰り返し回数の選択欄)をパネルで変える | 同じ | 同じ |
| なし | なし | 節を並べ替える | 節の順序が変わる | 意図して変更(試作は消して足し直すしかなかった) |
| (試作に無い操作) | なし | パターンパネルで小節数を変える | 格子が新しい小節数ぶんの長さへ変わる。増やすと末尾に空の小節が足され、減らすと末尾の小節が消える | 意図して変更(判断16。試作に無い操作を足した) |
| (試作に無い操作) | なし | タイムラインの末尾へ追加を押す | カードが1枚増える | 意図して変更(判断15。試作に無い操作を足した) |
| (試作に無い操作) | なし | タイムラインの節の枠の削除を押す | 節の繰り返し回数が2以上なら1減り、1なら節が消える | 意図して変更(判断15) |
| (試作に無い操作) | なし | タイムラインの節の枠の複製を押す | 節の繰り返し回数が1増える。上限の8では押せない | 意図して変更(判断15) |
| (試作に無い操作) | なし | タイムラインの節の枠の前へ挿入・後へ挿入を押す | 同じパターンを1回分挿入した節が増える | 意図して変更(判断15。`曲の節を挿入する`を新設した) |
| (試作に無い操作) | なし | タイムラインの節の枠の前へ移動・後へ移動を押す | 節の順序が変わる | 意図して変更(判断15。移動は節の枠のボタンであり、節ごと動く) |
| (試作に無い操作) | なし | タイムラインのカードを選ぶ | そのカードのパターンが選択中のパターンになり、格子がそのパターンへ切り替わってその小節が強調される | 意図して変更(判断15。カードの選択は画面の状態であり保存にもコマンドにも入らない) |

### ミキサー

| 試作での行為 | 試作で見える結果 | 現行での行為 | 現行で見える結果 | 方針 |
| --- | --- | --- | --- | --- |
| 「ミックス」で開閉する | ミキサーの欄が出入りする | ミキサーのパネルが右サイドバーに常に見えている | 開閉が無い | 意図して変更 |
| 全体の音量・残響・遅延の量・遅延の長さを動かす | 次に鳴る音に効く | 同じ | 動かしている間は数字が動き、手を離した時点で音に効く | 意図して変更(判断13) |

### 楽器の音色

音を鳴らす技法はSengenAudioが持ち、楽器の名前とその技法へ与える値はこのエディターが持つ(判断1)。移植のときSengenAudioに無い機能があり、次の点で試作と音色が違う。SengenAudioを楽曲エディターの都合で広げないための判断である。

| 試作での行為 | 試作で見える結果 | 現行での行為 | 現行で見える結果 | 方針 |
| --- | --- | --- | --- | --- |
| 楽器を選んで音を鳴らす | 楽器11種それぞれの音色で鳴る | 同じ | 同じ。ただし下の4点だけ音が違う | 意図して変更 |

1. **重ねた発振のずらしが無い**: 試作の `playStrings` は同じ音を少しずつずらした発振3つで重ねて厚みを出す。いまのSengenAudioの音源はどれも発振1つのため、弦楽合奏は1つの鋸波で鳴る
2. **音の高さのゆらぎが無い**: 試作の `playFlute` は5.5ヘルツで音の高さを揺らす。周波数を時間で動かす口がSengenAudioに無いため、フルートは揺れない
3. **音の高さの掃引が無い**: 試作のバスドラムは100から45ヘルツへ、スネアの胴の音は180から80ヘルツへ下げる。同じ理由で掃引できないため、バスドラムは中ほどの一定の高さで鳴らし、スネアは雑音だけで鳴らす
4. **包絡の秒数が音の長さに比例しない**: 試作のピアノは倍音ごとの減衰を音の長さへ比例させ、エレクトリックピアノは変調の減りを音の長さに合わせる。SengenAudioの音源は構築時に決まる秒数を持つため、音の長さの後ろへ足す一定の秒数として写した

打撃の雑音と撥弦の励起は `Math.random` から種つきの疑似乱数へ置き換えている(「試作からの意図的な改変」の7と同じ規律)。

### 保存

| 試作での行為 | 試作で見える結果 | 現行での行為 | 現行で見える結果 | 方針 |
| --- | --- | --- | --- | --- |
| 自動同期の印を入れる | 2秒ごとに外部の状態を取り込む | なし | なし | 廃止(編集中の作業を黙って踏み潰すため。設計正本の判断6) |
| 「現在の曲を送信」を押す | 外部へ保存される | 永続化のパネルの保存を押す | 保存の結果が出る | 別の手段へ置換 |
| なし | なし | 永続化のパネルの読み込みを押す | 保存済みの内容が戻る | 意図して変更(試作には明示の読み込みが無かった) |

### 表示

| 試作での行為 | 試作で見える結果 | 現行での行為 | 現行で見える結果 | 方針 |
| --- | --- | --- | --- | --- |
| (常時) | 進行の帯に和音の名前が続く長さぶんの幅で並ぶ。1周した後は色が違う | 同じ | 同じ | 同じ |
| (常時) | 升目が打点・伸ばし・進行の外・打てない・拍の区切りで見分けられる | 同じ | 同じ | 同じ |
| (常時) | 音の行に音名が出る | 同じ | 同じ | 同じ |
| (再生中) | 再生位置の印が升目を進む | 同じ | 同じ。印の位置は音声の時計から導く(判断2) | 同じ |
| (常時) | テンポの数字が `BPM` の見出しで出る | 同じ | 見出しが `テンポ` になり、つまみの隣には数字だけが出る | 意図して変更(`BPM`は英語の頭字語であり日本語命名の質に反する。英語を日本語へ直訳した語も不自然であったため、2026-08-28のオーナーの指摘を受けて、小学校の音楽で習う`テンポ`を採る。単位はつまみの吹き出しが「テンポ(1分あたりの拍数)」で示す) |

## 検収

基盤の判断6(表示と正本の一致を機械で確かめる)を楽曲へ翻訳して適用する。少なくとも次を機械で確かめる。

- **保存の往復**: 楽曲を保存して読み戻すと、格子・進行・曲構成・ミキサーが同じ値になる
- **和音が許す音の一致**: 移植した `和音が許す音のピッチクラス` が、試作の `chordAllowedChordPcs` と全和音種類×全根音で一致する
- **ステップから和音への写像の一致**: 進行の長さが32ステップに満たないときの周回、超えるときの打ち切りが試作と一致する
- **曲構成の位置の解決**: 曲全体のステップ位置からパターンとパターン内位置への解決が、繰り返しとパターンの削除を含めて試作と一致する
- **小節単位の展開の一致**: 節ごとに小節数が違う曲構成でも、曲構成のカードの列への展開が全体のステップ数と小節番号の両方で `曲全体のステップ数を計算する` と一致する
- **形式版1の楽曲が読める**: 形式版1(小節数の欄を持たない版)の楽曲を読み込むと、全パターンが小節数2として読める
- **演奏予定表の時刻**: あるテンポと曲構成に対して予約される時刻の列が、`ステップ × 60 ÷ テンポ ÷ 4` の等差数列と一致する(ずれの許容は浮動小数の丸めの内側)
- **同じ入力から同じ音**: 種を指定した雑音源が同じ標本列を返す

そして「その道具でひと仕事を通す」検収を親が行う。何も無い状態から楽曲を作り、進行を選び、打ち込み、パターンを増やし、曲構成を組み、保存し、読み直し、AIが経路から置いた打点が絵と音に出るところまで通す。
