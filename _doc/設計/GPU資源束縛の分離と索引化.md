# GPU資源束縛の分離と索引化

この文書は、シェーダー定数バッファの分離とディスクリプタの索引化(技術ビジョン第8章順1(c))によるGPU資源束縛の境界設計を管理する。実装と常に一致させて維持する。本文書は設計先行の決定であり、「実装済み」と明記した節以外はまだ実装されていない。

設計の経緯は次のとおりである。オーナーが2026-07-30に技術ビジョン第8章順1の壁打ち開始を許可し、codexとの設計壁打ち第3弾(AgentRoom「ゲームエンジン開発室」連番224-238)で本設計を確定した。関連文書は `_doc/計画/技術ビジョン.html` 4.3節・第8章、`_doc/設計/マルチマテリアルと材質境界.md`(束縛バックエンドの移行境界)、`_doc/設計/放射輝度問い合わせ階層.md`(問い合わせ資源束)である。

## 決定の要約

1. **frame constants・個体structured buffer・材質structured buffer・テクスチャ番号表の4分割を責務の背骨として採用する。** ただし4つで全資源を表そうとせず、可視ID列と照明問い合わせ資源は別の更新頻度・名前空間として独立させる
2. **sceneのパイプラインレイアウトを束縛頻度ごとの4セットへ分ける**(View/Pass・Geometry/Visibility・Materials・LightingQuery)
3. **ディスクリプタインデクシングは材質のsampled image tableだけを初期対象にし、最小機能集合をエンジンの最低機能要件にする。** update-after-bindは初期要件にせず、資源表の世代とフェンス退役で寿命を守る
4. **材質ID→GPUレコード添字とテクスチャID→常駐スロットを別台帳にする。** 「無し」はマジック番号でなく特徴ビット+正準フォールバックスロットで表す

## 分離の形

置き場所の判定基準: 固定小容量で同じビュー/パス内へ一斉配布する値だけをシェーダー定数バッファ、件数が増える・添字で参照する値をストレージバッファ、画像はディスクリプタ表に置く。

旧の連結したフレームのシェーダー定数バッファ(576バイト。共通464バイト+空112バイト)は次の3本へ分けた(段2で実装済み)。

- **ビュー・シーンパス定数**: ビュー射影・カメラ相対位置・カメラ前方向等、全シーン描画が同じビューで読む小値。寿命はフレーム×ビュー。段2では直接照明の値(方向光の方向/色/強度・点光の位置/色/強度・環境光係数・ライティング有効)もここへ仮置きし、段5で照明問い合わせセットへ移す。純粋な「ビュー定数」を名乗らないのはこの仮置きのためであり、第4の一時照明用のバッファを立てないのは、段5で消すバッファとディスクリプタを増やさないためである(2026-08-02、ルーム連番377-378で確定)
- **多段影定数**: 4距離区分のライトビュー射影・分割深度・遷移幅・影有効・デバッグ(距離区分可視化)・テクセル尺度。シーンの影評価とシャドウパスが共有する。**旧の「ビュー定数の先頭をシャドウシェーダーが別の構造体として読む」バイト配置の暗黙契約は廃止した**(布シャドウも同じ廃止の対象だった)
- **空パス定数**: 旧の後半112バイトを空専用へ。空なしフレームは空白112バイトを書かない
- **直接照明ヘッダ**: 件数・開始位置・方式だけの小値を照明問い合わせセットのシェーダー定数バッファへ置き、方向光・局所光の本体はストレージバッファへ移す(現行の1+1も件数1の配列として包む)
- 露出/明るさの圧縮・大気生成等のパス固有値は各パスの定数のまま。共通のフレーム定数へ戻さない

### 3本のフィールドレイアウト(段2で確定)

CPU側の正本は`crates/blitz_render/src/vulkan/uniform/`の`view_pass_bytes.rs`・`cascade_bytes.rs`・`sky_bytes.rs`であり、GPU側の宣言の正本は`shaders/view_pass_uniform.slang`・`cascade_shadow_uniform.slang`・`sky_pass_uniform.slang`である。行列以外は16バイト境界を跨がないようvec4単位にそろえる。開始位置の一致は`vulkan/uniform/layout_tests.rs`と`shader_struct_tests.rs`が単体テストで見る。

ビュー・シーンパス定数(176バイト、binding3):

| 開始位置 | 型 | 名前 | 内容 |
| --- | --- | --- | --- |
| 0 | float4x4 | viewProjection | ビュー射影 |
| 64 | float4 | cameraRelativePosition | xyzがカメラ相対位置 |
| 80 | float4 | cameraForward | xyzがカメラ前方向 |
| 96 | float4 | directionalLightDirection | xyzが方向光の向き |
| 112 | float4 | directionalLightColor | rgbが色・wが強度 |
| 128 | float4 | pointLightPosition | xyzが点光源のカメラ相対位置 |
| 144 | float4 | pointLightColor | rgbが色・wが強度 |
| 160 | float4 | ambientAndLightingEnabled | xが環境光係数・yがライティング有効 |

多段影定数(304バイト、binding8):

| 開始位置 | 型 | 名前 | 内容 |
| --- | --- | --- | --- |
| 0 | float4x4[4] | cascadeLightViewProjection | 距離区分ごとのライトビュー射影 |
| 256 | float4 | cascadeSplitDepths | xyzが境界のビュー空間深度 |
| 272 | float4 | cascadeTransitionWidths | xyzが境界手前の遷移域の幅 |
| 288 | float4 | shadowFlagsAndTexelSize | xが影有効・yが距離区分の可視化・zがテクセル尺度 |

空パス定数(112バイト、binding9):

| 開始位置 | 型 | 名前 | 内容 |
| --- | --- | --- | --- |
| 0 | float4x4 | inverseViewProjection | クリップからカメラ相対ワールドへ戻す変換 |
| 64 | float4 | nightSkyRadianceAndSunCos | rgbが夜空放射輝度・wが太陽円盤の余弦角半径 |
| 80 | float4 | sunDirectionAndObserverAltitude | xyzが太陽方向・wが観測高度 |
| 96 | float4 | sunDiskRadianceAndSunIrradiance | rgbが太陽円盤放射輝度・wが太陽放射照度 |

段2ではセット再編を行わないため、3本とも既存のシーンのディスクリプタセット(set0)へ結ぶ。布専用シャドウ経路のセットは多段影定数だけを、粒子系のセットはビュー・シーンパス定数だけを結ぶ。bindingの番号は宣言モジュールが正本であり、取り込む側が自前で宣言していないことは`cargo xtask conform`が検査する。

シャドウマップのテクセル尺度はシェーダーの`static const`からこの多段影定数へ移した。解像度が2の冪であるため逆数はf32で誤差なく表せ、画素は変わらない。CPU側の出どころは`vulkan/shadow_map.rs`の`テクセル尺度()`1箇所である。

`ObjectUniform`(144バイト)は段3で廃止した。変換112バイトは**個体レコード**のストレージバッファ(binding6)へ、ベースカラーと金属度/粗さの係数は**材質レコード**のストレージバッファ(binding5)へ移した。単一個体も1要素の個体レコードバッファを読む。個体レコードはシャドウ描画が以前から読んでいた112バイトをそのまま正本として再利用したため、GPU側の宣言名は`InstanceTransform`のままである。所有単位は現行どおり描画対象であり、世界全体のarenaへの集約はGPU駆動間接描画の採用時に行う。

**可視ID列は個体レコードと別の毎フレームのストレージバッファのまま維持する。** 静的な変換と毎フレームの並べ替え列を混ぜると、OW4で避けた全変換再書込へ戻るためである。描画は`firstInstance`+可視区間を従来どおり使う。

プリミティブ/材質ごとに変わる値は**描画定数**としてプッシュ定数に置く(段3で実装済み)。scene専用の16バイトであり、GPU側の宣言名は`SceneDrawConstants`である。カメラ相対の基準原点16バイトの余白4バイトを材質レコード添字へ置換したため総量は増えない(粒子・布等が読む基準原点16バイトの`RelativeAnchor`とシャドウの20バイト定数は別のまま。2026-08-02、ルーム連番397-398で確定)。マルチマテリアルでプリミティブごとに材質が変わってもディスクリプタセットを作り直さない。頂点ステージが基準原点を、画素段ステージが材質レコード添字を読むため、シーンのパイプラインレイアウトはこの範囲を両ステージへ宣言する。布の描画パイプラインは同じ16バイトでも`RelativeAnchor`の範囲(頂点ステージのみ)を宣言する。

### 3つのレコードのフィールドレイアウト(段3で確定)

CPU側の正本は`crates/blitz_render/src/vulkan/instance_transform/bytes.rs`・`crates/blitz_render/src/vulkan/material_record/bytes.rs`・`crates/blitz_render/src/vulkan/scene_draw_constants/mod.rs`であり、GPU側の宣言の正本は`shaders/instance_transform.slang`・`shaders/material_record.slang`・`shaders/scene_draw_constants.slang`である。バイト長と全フィールドの開始位置の一致は各モジュールの`layout_tests.rs`が単体テストで見る。slangの原文を解析してCPU側と突き合わせる工程は`crates/blitz_render/src/vulkan/shader_struct.rs`が持ち、シェーダー定数3本の検査と共有する。

個体レコード(112バイト、binding6のストレージバッファ、宣言名`InstanceTransform`):

| 開始位置 | 型 | 名前 | 内容 |
| --- | --- | --- | --- |
| 0 | float4x4 | localToWorld | ローカルからワールドへの変換 |
| 64 | float3x3 | normalLocalToWorld | 法線の変換。列ごとに16バイト境界へ載るため48バイトを占める |

材質レコード(32バイト、binding5のストレージバッファ、宣言名`MaterialRecord`):

| 開始位置 | 型 | 名前 | 内容 |
| --- | --- | --- | --- |
| 0 | float4 | baseColorFactor | ベースカラー係数 |
| 16 | float4 | metallicRoughnessFactor | xが金属度係数・yが粗さ係数。zwは未使用 |

描画定数(16バイト、プッシュ定数、宣言名`SceneDrawConstants`):

| 開始位置 | 型 | 名前 | 内容 |
| --- | --- | --- | --- |
| 0 | float3 | position | カメラ相対の基準原点 |
| 12 | uint | materialRecordIndex | その描画発行が塗る材質のレコード添字 |

### 束縛頻度による4セット

scene系のパイプラインレイアウトは次の4セットを正本にする。セット番号は次のとおり固定する(2026-08-02、ルーム連番377-378で確定)。この番号契約はscene系パイプライン族が守る束縛の契約であり、空の生成・ポスト・シミュレーションのような無関係なパイプラインへset 3到達のための空セットを強制しない。

- **set 0 View/Pass**: ビュー・シーンパス定数・必要なら多段影定数。フレーム×ビューで1回
- **set 1 Geometry/Visibility**: 個体のストレージバッファ・可視ID列のストレージバッファ
- **set 2 Materials**: 材質のストレージバッファ・sampled image表・少数の固定サンプラー。資源表世代ごと
- **set 3 LightingQuery**: 直接光バッファ・影資源・IBL/将来のプローブ等。フレーム×ビュー×照明品質方式ごと

シャドウパイプラインはset 0-1だけ、シーンは0-3、空/コンピュート/布は必要な役割だけを持つ。全パイプラインへ空の巨大共通セットを強制しない。現行の「描画対象のセットからフレーム/影/材質を全部借りる」結合(対象数×進行中フレーム数のシェーダー定数・影・テクスチャ複製)を解消する。

棄却した案: 現行のシェーダー定数バッファの単純拡張(多光源/IBLで再膨張)。全部を1本の巨大なストレージバッファ(固定一斉配布値まで任意添字になり束縛の契約を失う)。個体変換と可視IDの同一バッファ統合(静的データの毎フレーム書込)。全個体/材質の即時グローバルarena化(ストリーミング所有・圧縮まで一括する過剰工事)。材質添字の個体レコード固定(プリミティブごとの材質を壊す)。

## ディスクリプタ索引の採用範囲

初期のbindless対象は**材質のsampled image表だけ**に限定する。バッファは配列ディスクリプタでなく、1本のストレージバッファの中を添字で参照する。サンプラーは固定の少数をimmutable bindingにし、テクスチャごとに複製しない。sRGB/linearはimage view formatの契約であり特徴ビットにしない。

初期必須の機能集合(Vulkan 1.2 core相当): `runtimeDescriptorArray`・`shaderSampledImageArrayNonUniformIndexing`・`descriptorBindingPartiallyBound`。`variableDescriptorCount`は実測後の任意。**update-after-bind系は初期要件にしない。** ディスクリプタをGPU使用中に書き換えず、完全に構築済みの**資源表世代**をフレームが束縛し、旧世代は進行中フレームのフェンス通過後に退役させる。ホットリロード/ストリーミングは材質レコードとテクスチャディスクリプタを同一世代として原子的に公開する。update-after-bindは世代の作り直しが実測で律速になった場合だけ第2段で採用審査する。

`nullDescriptor`は要求しない。デバイス選定時は機能に加えて上限(`maxDescriptorSetSampledImages`等)を読み、エンジンの材質テクスチャ容量を満たすかを型付き結果で判定する。

**非対応デバイスは品質方式の枝にしない。** ディスクリプタ束縛方式は同じ画素を出す実装能力であり、IBL/プローブ密度のような品質ではない。上の最小集合をシーンレンダラーの最低機能要件とし、非対応GPUは「適合デバイスなし/必要機能不足」の明示エラーにする。オーナー方針の「低性能デバイスでも最高品質」の低性能は演算量・帯域が小さいことであって、必要API機能の欠如と混同しない。移行中だけ旧個別セットのバックエンドを画素比較用に残し、恒久の2本持ちは対象デバイスの実測なしでは判断しない(復活させる場合も`材質束縛バックエンド::索引表 | 個別セット`の明示の枝とし、同じ契約と同じ画素を要求する)。

シェーダーでは非一様なテクスチャスロット参照を明示し、SPIR-VのNonUniform装飾を`cargo xtask conform`で検査する。隣接プリミティブ/個体が異なるスロットを参照する負荷で、一様扱いの誤コンパイルを画素検査する。

棄却した案: update-after-bind全部盛りの初期要求(同期・寿命バグの温床)。全スロットへのdummy書込でpartiallyBoundを回避(容量比例の常時コスト)。非対応の低品質材質への縮退(絵の意味が変わる)。テクスチャ数ぶんのcombined image sampler(サンプラー重複と上限消費)。全資源の1つの型なしbindless heap(名前空間と寿命を失う)。

## 照明問い合わせ資源のGPU境界

set 3を**照明問い合わせセット**とし、set 2のアセットテクスチャ表とは別の型・台帳にする。材質テクスチャはアセット/ストリーミング寿命、照明資源はフレーム/ビュー/供給元更新寿命であり、同じheapへ入れない。

照明問い合わせGPU参照は選択品質方式ごとの判別共用体をCPU側に持ち、対応するセットレイアウトとパイプラインキー成分を返す。初期の環境方式なら概ね: 照明問い合わせ定数バッファ(直接光件数・供給元方式・資源添字)+方向光のストレージバッファ+局所光のストレージバッファ+多段影画像+拡散照度マップ+GGX prefiltered環境マップ+BRDF積分ベイク済み画像。

大気の透過率/多重散乱/スカイビューのベイク済み画像は、天空放射輝度供給やIBL生成コンピュートが読む**生成側のセット**に置き、表面描画へ全ベイク済み画像を常時束縛しない。表面描画は生成済みのIBL表現だけを見る。

**パイプラインキーへ「照明問い合わせレイアウト/能力キー」を成分として追加する**(前正本のパイプラインキーの改訂)。材質変種キーには入れない。照明要求⊆提供能力の検証後、パイプラインキーが対応するSlang関数群とset 3レイアウトを一意に選ぶ。環境のみのパイプラインへdummyのプローブ欄を持たせない。

棄却した案: 空/プローブ/影の材質テクスチャ表への投入(寿命とアクセス権が違う)。全将来資源を予約した巨大固定LightingContext。供給元方式の材質特徴ビット化(材質ごとに照明品質が変わってしまう)。スカイビューのベイク済み画像を表面PBRが直接環境光として読む(IBL畳込み契約を飛ばす)。

## 材質レコードとテクスチャ台帳

CPU正本は型付き材質(`Option<テクスチャID>`等)である。1つの**材質GPU梱包工程**(packer)が、検証済み材質から材質変種キー・`材質GPU参照 { 資源表世代ID, 変種キー, レコード添字 }`・モデル別のGPUレコード(v1は標準金属粗さPBRのみ)を同時に作る。変種キーとレコードを別々に手組みしない。

**材質GPU参照は世代に拘束される。** 永続するシーン/描画束は`材質ID`を保持し、フレームの描画準備が「そのフレームで束縛する資源表世代」に対して材質ID→材質GPU参照を解決する。フレームpacketの構築時に「全材質GPU参照の世代ID == 束縛するset 2の世代ID」を検証し、解決結果は世代IDをキーにキャッシュしてよい。描画束はストリーミング/ホットリロードより長生きするため、世代非拘束の添字を永続保持すると、世代更新でレコード列が再構築されたとき旧添字が新表の別材質を無言で読む。この経路を型と検証で閉じる。描画定数としてGPUが読むのはレコード添字だけでよいが、CPUがpush値を作る前に世代一致を確定する。レコード添字を全世代で永久安定にする案は、圧縮・削除・容量回収を拘束し「テクスチャスロットは世代内」の設計と非対称になるため棄却した。

- レコードは係数(ベースカラー・金属度/粗さ・法線スケール・アルファカットオフ値等)+直交特徴ビット+各テクスチャスロットを持つ。シェーディングモデルと固定機能の表面描画状態はパイプラインキー側が知るため、レコードへ二重保持しない。2つ目のモデルが来る前に巨大なtagged unionを先行せず、`材質ID→{モデル, モデル別レコード添字}`のCPU台帳で拡張する
- **テクスチャID**(アセットの安定ID)と**テクスチャスロット**(1つの常駐テクスチャ表世代内のGPU添字)を型で分ける。CPUのテクスチャ台帳だけがID→スロットを解決し、材質レコードはスロットを持つ。スロットの再利用は旧世代を読む全フレームのフェンス通過後だけ
- **「無し」の変換は1工程で行う**: `法線テクスチャ: 無し`なら法線ビット=false+スロットには実在するflat-normalの**正準フォールバックスロット**。`有り`ならビット=true+解決済み常駐スロット。ベースカラーは白等、意味ごとの正準フォールバックを実資源として持つ。スロット0を「無し」の番兵にしない。全スロットは常に有効であり、ビット/データ/変種の整合はpackerが検証する。未知の特徴ビットはアセットコンパイル/ロード時に拒否する

棄却した案: u32::MAXの「無し」番兵。ビット=falseで無効添字を放置(シェーダーの分岐誤りが未定義アクセスになる)。変種キーのGPUレコード保持(CPU/GPU二重正本)。全将来モデルの最大union(帯域浪費)。テクスチャIDの直接ディスクリプタ添字化(ストリーミング・再配置・世代を表せない)。

## 移行の段階(無画素差の境界)

1. 現行の連結したフレームのシェーダー定数バッファをビュー・シーンパス/多段影/空へ分け、shadow・cloth_shadowの先頭部分の別名読みを廃止。オブジェクト/材質セットは現行のまま
2. `ObjectUniform`を個体のストレージバッファ+材質レコードのストレージバッファ+描画定数(材質添字)へ分ける。テクスチャは一時的に現行の個別セットのままでよい
3. set 0-3の役割を導入し、材質のsampled image表を索引バックエンドへ差し替える。永続束は材質IDを、フレームの描画packetは同世代の材質GPU参照だけを見る
4. 単一材質と2プリミティブ/2材質を旧/新バックエンドで同一入力・同一画素・同じ可視ID数/draw数として比較する。validation 0件、ディスクリプタ束縛数が材質/プリミティブ数に比例しないことを計器で確認
5. 合格後に旧の材質セット/プール/レイアウトと`ObjectUniform`互換経路を削除する。恒久fallbackとして残さない
6. マルチマテリアル段D(バッチ/PSOキャッシュ)を新バックエンド上で実装し、Blender段4受入へ進む

マルチマテリアルの段A-B-C(scene v4・CPU描画束・2材質の実描画)は旧バックエンドで先に通してよい(前正本どおり)。索引バックエンドの削除ゲートは単一材質だけでなく2材質プリミティブを必須にする。

## 順5(多光源クラスタード)への接続

set 3の直接光は最初から配列契約にする(固定フィールドにしない)。方向光レコード列+局所光レコード列(初期は現行の1+1を件数1ずつでpack)。将来のクラスタ格子(セル→開始位置/件数)とクラスタ光添字列はset 3へ追加し、方向光は全画素段が別に列挙する(クラスタへ複製しない)。空のクラスタ基盤の先行実装はしない。

影資源は光レコードの「影有無ビット+有効な影資源添字」へ下ろす(「無し」の番兵禁止、正準フォールバックの規律は材質と同じ)。光源のCPU安定IDとGPUレコード添字を分け、追加削除は世代/フェンス規律に従う。材質シェーダーは光源配列/クラスタの生成方法を知らず、直接光標本列挙のSlang関数を呼ぶ。クラスタ能力はパイプラインキー(照明能力キー)に現れ、材質変種キーは変わらない。

棄却した案: フレームのシェーダー定数バッファの最大N光固定配列。方向光の全クラスタ複製。光添字の材質レコード保持(材質と照明の結合)。光種ごとの別パイプライン(変種爆発)。点光影atlasの材質テクスチャ表への混入。

## 段階導入

各段は本番の呼び出し元を持って完了とする。

0. **設計正本化**(本文書): 4セットの役割・シェーダー定数バッファとストレージバッファの判定・ID→スロットの世代規律・最低機能集合・「非対応は品質でない」の確定
1. **デバイス能力段**(実装済み): descriptor indexingの機能3種(`runtimeDescriptorArray`・`shaderSampledImageArrayNonUniformIndexing`・`descriptorBindingPartiallyBound`)の型付き照合と非対応の明示失敗、および上限(`maxPerStageDescriptorSampledImages`・`maxDescriptorSetSampledImages`・`maxPerStageResources`)の型付き採取と報告。上限の合否判定はテクスチャ表容量の既定値が未決定のため行わず、既定値の確定(段4の着手前)とセットで追加する(単一機材の上限をそのまま最低要件にしない。2026-08-02、ルーム連番377-378で確定)。機能はまだデバイス作成へ有効化せず既存の絵を変えない。`maxPerStageResources`まで採取するのは、同じ画素段がset 0/1/3のバッファと影の画像も一緒に消費し、表の枚数が資源合計の側で先に頭打ちになるためである。**機能の照合はデバイス選定の条件そのものとして掛ける**(選定後の照合では、選ばれたGPUが機能を欠き別のGPUが対応している機材で、動けるのに起動が失敗する)。基礎要件を満たす候補が1台も残らなかったときは、候補ごとの機材名と不足機能を並べた型付きエラーにする。実装の所在は、選定と選ぶ規則が`crates/blitz_render/src/vulkan/physical_device/`、機能と上限の採取が`crates/blitz_render/src/vulkan/descriptor_indexing/`、不足を並べる型付きエラーが`crates/blitz_render/src/error/device_requirement/`、公開の値オブジェクトが`crates/blitz_render/src/descriptor_indexing_limits.rs`、起動時の1行報告が`crates/blitz_app/src/reports/composition.rs`である。採取した上限は物理デバイスと同じ寿命で`GPU環境`が保ち、`レンダラー`のアクセサ経由でコンポジションルートが読む。照合を掛けるのはシーンを描くウィンドウ付きのデバイスだけであり、ウィンドウなし実行のコンピュート専用デバイスは対象にしない(最低機能要件はシーンレンダラーのものだからである)
2. **フレーム定数分離段**(実装済み): ビュー・シーンパス/多段影/空の3本への分割と、shadow・cloth_shadow両方の先頭部分を別の構造体として読む別名宣言の廃止。同じ書き方をしていたparticle・sph・surface_flowも同時に移した。3本のフィールドレイアウトは本文書「分離の形」の表が正本である。検収は次を必須ゲートとする(2026-08-02、ルーム連番377-378で確定): 既存検収入口(smoke全ステージ・csm-seam・instance-cull・multi-material-draw・prop-multi-material-draw・sky-time・cloth-shadow-order・origin-invariance)の判定値完全不変+validation 0件+verify全緑。完全不変の対象は画素バイト・描画/可視/validation計数などの決定的な値に限り、GPU/CPU時間は一致条件に入れない。加えて、3本それぞれのCPUバイト長・全フィールド開始位置・シェーダー宣言の一致を単体テストにし、旧の先頭部分の別名宣言がshadow/cloth_shadowのslangから0件になったことを静的に検査し、空なしフレームで空パス定数への書込が0回・空ありで1回/フレームであることを狭い状態テストで見る(空白112バイトを書かないことを画素だけに頼らない)
3. **データ表段**(実装済み): `ObjectUniform`(144バイト)を、既存の個体変換112バイトを正本として再利用する個体レコードのストレージバッファ(単一個体も正式な1要素バッファ)・材質レコード(32バイト)のストレージバッファ・描画定数(16バイト、余白置換の材質レコード添字)へ分け、現行テクスチャセットのまま画素不変を証明する。レコード列は束が所有し、束内の材質束縛参照をその束の材質レコード添字へ解決する。段3の解決結果の型は世代非拘束であることを名前に出す(例: `束内材質参照`)。大域材質ID・材質変種キー・資源表世代・世代拘束済みの材質GPU参照は段4で導入し、パイプラインキーは実消費者が来る段6まで持ち越す(2026-08-02、ルーム連番397-398で確定)。検収ゲート: 段2と同じ検収入口8本の判定値完全不変+validation 0件+verify全緑に加え、(i)binding5を材質レコードのストレージバッファへ、binding6を単一個体でも正式な1要素個体レコードへ統一し、ディスクリプタプールの定数/ストレージ件数を追従させる (ii)別の112バイト実装を作らず既存の個体変換を再利用し、単一個体だけが`ObjectUniform`先頭を別名で読む経路を消す (iii)材質スロットごとにバッファを複製せず、描画対象が材質レコード列1本を所有し、同じ対象の全スロット別セットは同じバッファをbinding5へ結ぶ。(描画対象添字, 材質スロットID)→束内レコード添字の解決は1箇所に置く (iv)2材質検査でレコード添字が別値であること・各添字から係数が正しく読めること・材質レコードバッファが対象あたり1本であること・個体/可視ID数の不変を確認し、単一材質では添字0かつ画素不変を確認する (v)`ObjectUniform`宣言・object_uniform資源・単一個体のbinding6別名経路が0件になったことを静的に検査する (vi)個体112バイト・材質32バイト・描画定数16バイトの全フィールド開始位置をCPUとシェーダー宣言の両側で固定する単体テスト (vii)空/布/粒子へ段3の型と範囲を波及させない(cloth_drawが共有セットを束縛してもbinding5を参照しないことを維持する)

   実装の所在は次のとおりである。GPU側の宣言が`shaders/instance_transform.slang`・`shaders/material_record.slang`・`shaders/scene_draw_constants.slang`、CPU側のバイト詰めと資源が`crates/blitz_render/src/vulkan/instance_transform/`・`crates/blitz_render/src/vulkan/material_record/`・`crates/blitz_render/src/vulkan/scene_draw_constants/`、描画対象1つが持つ2本のレコード列の確保が`crates/blitz_render/src/renderer/scene_draw_resources/render_object_resources/record_buffers.rs`、材質スロット番号から束内材質参照への解決が同じ階層の`slot_binding.rs`、その参照の型が`crates/blitz_render/src/renderer/scene_draw_resources/bundle_material_reference.rs`、描画発行への配りが`crates/blitz_render/src/renderer/scene_draw_resources/work_area/stage_issue.rs`である。レイアウトの表は本文書「3つのレコードのフィールドレイアウト(段3で確定)」が正本である。廃止した語が書き戻されていないことの静的検査は`cargo xtask conform`の`removed_object_uniform`が持つ。

   検収の実測は次のとおりである。smoke全8ステージ・csm-seam(影画素[42795, 97907, 38445, 27478])・instance-cull(バイト一致・距離区分別の可視[1, 5, 5, 5]・暗部45000/明部18000)・multi-material-draw(左(243, 97, 89)/右(89, 124, 243)・発行数2と1)・prop-multi-material-draw(物体画素147505・上位3色)・cloth-shadow-order(食い違い0画素)・origin-invariance(バイト一致)がいずれも段2までと同じ判定値で成功し、validationは0件だった。sky-timeだけは段3の着手前(main ddbde8f)から同じ環境で失敗しており、失敗する判定が実行のたびに変わる(正午の2回描画のバイト不一致・夜明けと夕方の対称性・空代表画素の照合)。段3の変更前後で同じ状態であるため段3の回帰ではないが、緑にできていない事実として残る
4. **索引材質段**: 大域材質ID・材質変種キー・資源表世代・世代拘束済みの材質GPU参照を導入し、set 2のsampled image表・フェンス退役・非一様索引・正準フォールバックを実装する。単一/複数材質/ホットリロードで画素・validation・束縛数・寿命を検収し、旧セットを削除
5. **照明問い合わせ段**: set 3を現行の直接光1+1/影/定数間接近似で本番接続し、第2正本のGPU資源束の境界を固定。順3のIBLで環境の枝を追加できる停止点にする
6. **マルチマテリアル段D/PSO段**: 材質/パイプラインキーでの並べ替えとキャッシュ、プリミティブ数・材質切替・PSO切替の計器化。Blender段4受入
7. **順3**: IBL/問い合わせ資源とSSAO、自動露出(別ポスト段)
8. **順5**: 局所光のストレージバッファ+クラスタ格子/添字+点光影資源。既存の方向光/CSMと同じ直接光列挙口で合成
9. **後段**: プローブ/RT/高頻度ストリーミングの実測に応じ、問い合わせ資源の索引化やupdate-after-bindを個別審査

段3とマルチマテリアル段A-B-Cは別の作業列として並行できる。

## 未決定の事項

- テクスチャ表の容量の既定値(段4の着手前に、段1が採取する対象デバイスの実測を材料に決める。確定とセットで段1の照合へ上限の合否判定を追加する)
- update-after-bindの採用(世代作り直しの実測律速が出た場合のみ審査)
