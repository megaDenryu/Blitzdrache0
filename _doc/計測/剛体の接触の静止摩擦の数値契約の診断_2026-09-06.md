# 剛体の接触の静止摩擦の数値契約の診断(2026-09-06)

[Issue #59](https://github.com/megaDenryu/Blitzdrache0/issues/59)「静止摩擦の保持・滑走境界を座標の単精度と解法許容差から独立させる」の第1段階の一次記録である。この段階の目的は測定の道具と診断の表を作ることであり、本番の解法は1つも変えていない。前の記録は `_doc/計測/剛体の接触の摩擦と反発_2026-09-03.md` である。正本は `_doc/設計/剛体の状態と接触.md`(判断12・判断13)である。本記録はログ型であり追記のみ。

## 条件と由来

- 2026-09-06、CPUだけ。構成は表ごとに違う。**単精度と倍精度の差の表(表4)はデバッグ構成**であり、**保持と滑走の分類の表(表1・表2・表3)は最適化を有効にした構成**である。分類の表を最適化構成で走らせるのは、デバッグ構成では1つの組に分の単位で掛かり、組が36ある表では数時間になるためである
- 構成で分類が変わらないことは「はず」で済ませず、1組(傾きの正接0.55・回し0度・600刻み)を両方の構成で走らせて突き合わせた。結果は下の「構成の突き合わせ」に書く
- 分岐の探索と振れの走査(表5・表6)も最適化を有効にした構成である。どちらも最大4800細分を走査するためである
- 重力9.81メートル毎秒毎秒、基本刻み1/60秒を8細分(細分の刻み幅1/480秒)。位置の反復回数2、速度段階の巡回数4
- 場面は一辺1メートル・質量1キログラムの立方体1つと、半分の長さ20メートルの静的な直方体1つである。接触は面と面であり接触点は4つになる。初めの食い込みは1e-4メートル
- 静止摩擦係数0.6、動摩擦係数0.2、反発係数0
- 細分の工程は試験専用の材料(`crates/blitz_sim/src/contact/scene/`)であり、予測 → 接触点集合 → 履歴の併走 → 反復(接触点集合ごとの粘着の候補の同時解法)→ 速度と角速度の再構成 → 速度段階、の順で回す

## 計器の入口

| 測るもの | 入口 |
|---|---|
| 坂の境界の保持と滑走の表 | `cargo test -p blitz_sim 坂の境界の保持と滑走の表を記録する -- --ignored --nocapture` |
| 坂の境界の単精度と倍精度の差 | `cargo test -p blitz_sim 坂の境界の単精度と倍精度の差を記録する -- --ignored --nocapture` |
| 世界内の平行移動の分類 | `cargo test -p blitz_sim 世界内の平行移動で保持と滑走の分類が変わるかを記録する -- --ignored --nocapture` |
| 世界内の平行移動の単精度と倍精度の差 | `cargo test -p blitz_sim 世界内の平行移動で単精度と倍精度の差がどう変わるかを記録する -- --ignored --nocapture` |
| 解法の許容差だけを振った分類 | `cargo test -p blitz_sim 解法の許容差だけを振って保持と滑走の分類が変わるかを記録する -- --ignored --nocapture` |
| 解法の許容差だけを振った単精度と倍精度の差 | `cargo test -p blitz_sim 解法の許容差だけを振って単精度と倍精度の差がどう変わるかを記録する -- --ignored --nocapture` |
| 構成を突き合わせる1組 | `cargo test -p blitz_sim 構成を突き合わせる一組の変位と分類を記録する -- --ignored --nocapture`(デバッグ構成と `--release` の両方で走らせる) |
| 分類が分かれる組の対が分岐する細分 | `cargo test -p blitz_sim --release 分類が分かれる組の対が分岐する細分を記録する -- --ignored --nocapture`(最適化を有効にした構成) |
| 細分ごとの円錐の比と再構成された速さの振れ | `cargo test -p blitz_sim --release 細分ごとの円錐の比と再構成された速さの振れを記録する -- --ignored --nocapture`(最適化を有効にした構成) |

計器の置き場は `crates/blitz_sim/src/contact/scene/numeric_contract_record/` である。倍精度の参照計算は `crates/blitz_sim/src/contact/normal_tangential_system/double_reference/` にある。

## 倍精度の参照計算が測る範囲と測らない範囲

この線引きを先に書く。表の読み方がこれで決まるためである。

- **測る**: 行の組み立て(2つの世界位置の差・勾配・交差の有効逆質量・右辺・解けたと見なす許容差)から先の演算の精度と、擬似逆が向きを捨てる判定と、有効集合の走査と、クーロン円錐の判定である
- **測らない**: 入力として受け取る配置そのものの座標の量子化である。参照計算は単精度の細分の工程が進めた配置・錨・接触点集合・法線の乗数をそのまま入力に取る。軌道そのものを倍精度で回すと配置が単精度の経路と離れ、比べた差がどの環から来たか分からなくなるためである

したがって、単精度と倍精度の差が小さいことは「座標の量子化が効いていない」ことを意味しない。座標の量子化は世界内の平行移動の走査が別に測る。

## 解析値と、許容差が平衡の接線変位に占める割合

表を読む前に、表に依存しない解析値を先に置く。細分1本の平衡で錨から離れる接線変位は g sinθ h²、その細分の法線の乗数の合計は m g cosθ h² である(h は細分の刻み幅 1/480秒)。行の右辺を解けたと見なす許容差 δ は、行の勾配の向きへ射影した世界位置の表現の刻みであり、いまの場面の接触点は世界の原点から約20.5メートルにあるため δ は約 1.907e-6 メートルである。原点付近へ置き直すと δ は約 1.19e-7 メートルになる。

| 傾きの正接 | 平衡の接線変位 C_t | 平衡の Σλ_n | 20.5メートルでの δ ÷ C_t | 原点付近での δ ÷ C_t |
|---|---|---|---|---|
| 0.40 | 1.5813e-5 | 3.9533e-5 | 0.121 | 0.0075 |
| 0.50 | 1.9042e-5 | 3.8083e-5 | 0.100 | 0.0063 |
| 0.55 | 2.0519e-5 | 3.7308e-5 | 0.093 | 0.0058 |
| 0.59 | 2.1636e-5 | 3.6671e-5 | 0.088 | 0.0055 |
| 0.61 | 2.2173e-5 | 3.6349e-5 | 0.086 | 0.0054 |

つまり、いまの置き方では解法が細分ごとに平衡の接線変位の8.6から12.1パーセントを「解けたもの」として意図的に残している。原点付近ではその割合が0.55から0.75パーセントへ下がる。この割合は測定ではなく、許容差の定義と場面の座標から直に出る値である。残した分が次の細分へどう入るかは、下の表が測る。

## 構成の突き合わせ

傾きの正接0.55・回し0度・600刻みの1組を両方の構成で走らせた。

| 構成 | 坂に沿った変位(メートル) | 分類 |
|---|---|---|
| デバッグ | 1.858147278e2 | 滑走 |
| 最適化を有効にした構成 | 1.858147278e2 | 滑走 |

10桁の綴りが1文字も違わない。したがって、分類の表を最適化構成で走らせたことは分類にも変位にも影響していない。

## 表1: 坂の境界(600刻み・3600刻み)

静止摩擦係数0.6。欄は坂に沿った変位(メートル)と分類である。保持の上限は1ミリメートルである。

| 傾きの正接 | 0度 | 30度 | 45度 |
|---|---|---|---|
| 0.40 | 1.240e-6 保持 | 1.412e-6 保持 | 8.300e-7 保持 |
| 0.50 | 1.706e-6 保持 | 1.655e-6 保持 | 1.475e-6 保持 |
| 0.55 | **1.858e2 滑走** | 1.856e-6 保持 | **1.857e2 滑走** |
| 0.59 | 2.029e2 滑走 | 2.022e2 滑走 | 2.035e2 滑走 |
| 0.61 | 2.116e2 滑走 | 2.095e2 滑走 | 2.099e2 滑走 |

3600刻みの正接0.40は3方向とも保持である(0度 7.969e-7、30度 1.412e-6、45度 8.300e-7)。

Issue #59が未解決として挙げた症状(静止摩擦係数0.6に対して正接0.55が3方向のうち2方向で滑る)をそのまま再現している。滑るのは0度と45度であり、保持するのは30度である。

## 表2: 世界内の平行移動

坂と箱をまとめて坂の面の法線の向きへ動かした。動かす長さ0が場面の既定であり、接触点は世界の原点から約20.5メートルにある。−20.4999は接触点を原点付近へ寄せる移動、20.4999は接触点の座標を2倍にして表現の刻みを2倍にする移動、204.999はその10倍である。600刻み。括弧の中は坂に沿った変位(メートル)である。

| 動かす長さ | 回し | 正接0.50 | 正接0.55 | 正接0.61 |
|---|---|---|---|---|
| −20.4999(原点付近) | 0度 | **滑走**(1.381e2) | 滑走(1.593e2) | 滑走(2.104e2) |
| −20.4999(原点付近) | 30度 | **滑走**(1.657e2) | **滑走**(1.773e2) | 滑走(2.100e2) |
| −20.4999(原点付近) | 45度 | **滑走**(1.653e2) | 滑走(1.688e2) | 滑走(2.101e2) |
| 0(既定・約20.5メートル) | 0度 | 保持(1.706e-6) | 滑走(1.858e2) | 滑走(2.116e2) |
| 0(既定・約20.5メートル) | 30度 | 保持(1.655e-6) | 保持(1.856e-6) | 滑走(2.095e2) |
| 0(既定・約20.5メートル) | 45度 | 保持(1.475e-6) | 滑走(1.857e2) | 滑走(2.099e2) |
| 20.4999(刻みが2倍) | 0度 | **滑走**(1.611e2) | 滑走(1.839e2) | 滑走(2.099e2) |
| 20.4999(刻みが2倍) | 30度 | **滑走**(1.654e2) | **滑走**(1.857e2) | 滑走(2.076e2) |
| 20.4999(刻みが2倍) | 45度 | **滑走**(1.637e2) | 滑走(1.861e2) | 滑走(2.110e2) |
| 204.999(刻みが約16倍) | 0度 | **滑走**(1.568e2) | 滑走(1.883e2) | 滑走(2.094e2) |
| 204.999(刻みが約16倍) | 30度 | **滑走**(1.705e2) | **滑走**(1.866e2) | 滑走(2.157e2) |
| 204.999(刻みが約16倍) | 45度 | **滑走**(1.691e2) | 滑走(1.985e2) | 滑走(2.162e2) |

太字は、既定の置き方(動かす長さ0)から分類が変わった欄である。正接0.50は既定の置き方でだけ3方向とも保持し、原点付近でも刻みが2倍でも16倍でも3方向とも滑走する。正接0.55が保持するのは既定の置き方の30度だけである。

**原点付近のほうが悪い。** これは「座標が粗いほど悪くなる」という予想と逆である。

## 表3: 解法の許容差だけを振る

世界座標も相対の配置も変えず、行の右辺を解けたと見なす許容差の倍率だけを振った。600刻み。括弧の中は坂に沿った変位(メートル)である。

| 許容差の倍率 | 回し | 正接0.50 | 正接0.55 | 正接0.61 |
|---|---|---|---|---|
| 0.5倍 | 0度 | **滑走**(1.196e2) | 滑走(1.855e2) | 滑走(2.115e2) |
| 0.5倍 | 30度 | 保持(1.285e-6) | **滑走**(1.847e2) | 滑走(2.095e2) |
| 0.5倍 | 45度 | 保持(1.174e-6) | 滑走(1.857e2) | 滑走(2.099e2) |
| 1倍(本番) | 0度 | 保持(1.706e-6) | 滑走(1.858e2) | 滑走(2.116e2) |
| 1倍(本番) | 30度 | 保持(1.655e-6) | 保持(1.856e-6) | 滑走(2.095e2) |
| 1倍(本番) | 45度 | 保持(1.475e-6) | 滑走(1.857e2) | 滑走(2.099e2) |
| 2倍 | 0度 | **滑走**(1.645e2) | 滑走(1.857e2) | 滑走(2.114e2) |
| 2倍 | 30度 | 保持(2.820e-6) | **滑走**(1.851e2) | 滑走(2.096e2) |
| 2倍 | 45度 | **滑走**(1.651e2) | 滑走(1.856e2) | 滑走(2.099e2) |

太字は1倍から分類が変わった欄である。**0.5倍でも2倍でも分類が変わる。** 正接0.50は1倍でだけ3方向とも保持し、0.5倍では0度が、2倍では0度と45度が滑走する。正接0.55が保持するのは1倍の30度だけであり、0.5倍でも2倍でも3方向とも滑走する。

したがって、Issue #59の完了条件「世界座標を変えず解法許容差だけを0.5/1/2倍したとき、0.50・0.55・0.61の分類が変わらない」は**現状では成立していない**。

## 表4: 単精度と倍精度の差(細分1本目・2本目の第一反復)

<!-- 計器の出力を貼る -->

## 観測から言えること

<!-- 表が揃ってから書く -->

## 観測から言えないこと

<!-- 表が揃ってから書く -->
