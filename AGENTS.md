# Codex / GPT-6 Astra の作業規約

このファイルは、Blitzdrache0で動くCodexの入口である。Codex固有の運用は [.codex/README.md](.codex/README.md) が定める。Codexは作業開始時と会話要約後に、この入口と運用文書を読む。

## 規約の読み方

1. Codexはユーザーが指定した全タスク共通の事前確認を先に行う。この環境では `C:/Users/pokr301qup/.claude/CLAUDE.md` が対象である。
2. Codexは作業ディレクトリの上位と対象領域の `AGENTS.md`・`CLAUDE.md` を確認する。
3. Codexは [CLAUDE.md](CLAUDE.md) と [.claude/global-reference/全プロジェクト共通CLAUDE.md](.claude/global-reference/全プロジェクト共通CLAUDE.md) の設計・命名・型・文章・禁止事項を共通規約として読む。Codexは共通規約を別の写しへ転記しない。
4. Codexは [README.md](README.md) の索引から対象の設計正本と [_doc/計画/ユビキタス言語.md](_doc/計画/ユビキタス言語.md) を確認し、規約が要求する追加文書を読む。

Codexの実行手順については、このファイルと `.codex/README.md` がClaude専用のモデル選択・ツール名・役割の起動手順を置き換える。共通の設計制約と品質条件は引き続き適用する。システム・開発者・ユーザーの指示が優先し、Codexは衝突した規約と採用理由を報告する。

## 作業の契約

- Codexは変更前に目的・完了条件・対象範囲・変更リスクを定め、既存の差分と呼び出し元を読む。Codexは他の作業の変更を取り消さず、混ぜてコミットしない。
- Codexは新しい機能の実装前に、層・依存の向き・状態の所有者・不変条件を明確にする。Codexは症状を隠す既定値や許容値の緩和を修正として扱わない。
- Codexは現在のセッションで提供されたツールを使う。CodexはClaudeのツール名や `.claude/agents/` のモデル設定がCodexでも実行されると仮定しない。
- Codexはユーザーまたは適用規約が委譲を要求する場合に、独立した範囲の作業を委譲する。親は結果の検収と検証に責任を持つ。
- Codexは作業ディレクトリをツールの `workdir` で指定する。Codexは別の作業ツリーのソースと検証対象を混在させない。
- Codexはツールの入口を既存の `cargo xtask` に統一する。Codexは確認のためだけに新しい操作基盤や検証スクリプトを常設しない。

## 検証と完了

- Codexはコード読解と静的検査を先に行い、必要な単体・境界テストを選ぶ。Codexはコード変更時の既存の必須検証を省略せず、文書だけの変更では参照・内容・差分を検査する。
- CodexはブラウザE2E、ElectronやブラウザのGUI自動操作、スクリーンショット巡回を、ユーザーの事前許可なしに行わない。実画面確認が必要な場合、Codexは理由・最小操作・コスト・ユーザー自身の代替手順を示して許可を求める。
- Codexは設計遵守・規約遵守・呼び出し側の接続・検証結果・差分全件の5観点を確認する。Codexは実行した検証と未確認事項を区別して報告する。
- Codexは外部への投稿・送信を、明示された依頼または明示的に呼び出されたスキルの許可範囲で行う。Codexは規約に投稿手順があることだけを送信の許可と解釈しない。
- Codexは次の作業を他セッションへ渡す場合、目的・完了済み事項・残件・変更ファイル・検証結果・判断の根拠を残す。
