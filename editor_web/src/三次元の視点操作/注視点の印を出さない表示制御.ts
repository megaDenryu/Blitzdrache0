// 注視点の印を持たない三次元ビューのための、何もしない表示制御。
// 印を持たないことをundefinedや半端な実体で表すと、視点の操作の側が印の有無を毎回調べることになる。
// 口の形は変えないまま「この道具には印が無い」だけを1つの型へ閉じる。
// 地点の引数を受け取らないのは、受け取っても使い道が無いためである(引数の少ないメソッドは多い口を満たす)。
export class 注視点の印を出さない表示制御 {
    public 操作された(): void {}
}
