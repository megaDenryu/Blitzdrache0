import { div, span, p, DivC } from 'sengen-ui'
import { セクション, セクション見出し, 段落, 手順リスト, コード欄 } from './スタイル.css.ts'

const ヘッドレス実行コマンド = 'node --experimental-transform-types src/ヘッドレス/編集コマンド一括適用.ts <コマンドJSONのパス>'
const ゲーム起動コマンド = 'cargo xtask play-editor-world'
const 建物単体検証コマンド = 'cargo xtask bake-building <建物定義ID>'

// 使い方ガイドの本文。節ごとに1つの関数で、見出しと本文をそのまま並べて組み立てる。
// タブの器(閲覧記録・寸法合わせ)は使い方タブ.tsが持ち、ここは読み物の中身だけを持つ。

export function この道具は何かの節を組み立てる(): DivC {
    return div({ class: セクション }).childs([
        div({ class: セクション見出し, text: 'この道具は何か' }),
        p({
            class: 段落,
            text: 'このエディターは、大域編集とチャンク編集の2つの道具を持つ。大域編集は、世界の区画割り・マザーハイトマップ(世界全体を1枚でつなげた高さ場)・広域道路を編集する道具である。チャンク編集は、1つのチャンクの高さ場・地表材質・道路・建物・散布を編集する道具である。一番左のアクティビティバーで編集領域(世界・建物・楽曲)を選ぶと、その領域だけのエクスプローラーが左に出る。エクスプローラーで対象を開くと、開いた対象ごとにエディタエリアへタブが1枚開く。タブは3つの領域で共有するため、領域を切り替えても開いたタブは残る。',
        }),
    ])
}

export function 基本の流れの節を組み立てる(): DivC {
    return div({ class: セクション }).childs([
        div({ class: セクション見出し, text: '基本の流れ' }),
        div({ class: 手順リスト }).childs([
            p({ text: '1. 一番左のアクティビティバーで「世界」を選び、左のエクスプローラーで編集したい対象(大域世界またはチャンク)を開く。' }),
            p({ text: '2. モード・ブラシ・道路・建物の各パネルで編集する。' }),
            p({ text: '3. 操作を間違えたらCtrl+Zで取り消す。' }),
            p({ text: '4. インスペクターの保存ボタンで保管庫へ保存し、読み込みボタンで保管庫から読み直す。' }),
            p({ text: '5. 大域編集の「チャンク切り出しの設定」パネルの書き出しボタンで、保存済みの内容をソースアセットへ書き出す。' }),
        ]),
    ])
}

export function 三次元ビューの操作の節を組み立てる(): DivC {
    return div({ class: セクション }).childs([
        div({ class: セクション見出し, text: '三次元ビューの操作' }),
        div({ class: 手順リスト }).childs([
            p({ text: '右ドラッグで視点回転、中ドラッグで平行移動、ホイールでズーム(全モード共通)。' }),
            p({ text: '左ドラッグは選んでいるモードの主作業になる(選択モードでは何も起きない)。' }),
            p({ text: 'W/Sで注視点ごと前後移動、A/Dで左右移動、Eで上昇、Qで下降。押している間だけ動き、速度はズームの距離に比例する。' }),
            p({ text: 'Altでモードを次へ、Shift+Altで前へ切り替える。入力欄にフォーカスしている間はどちらも無効。' }),
        ]),
    ])
}

export function 道の作り方と直し方の節を組み立てる(): DivC {
    return div({ class: セクション }).childs([
        div({ class: セクション見出し, text: '道の作り方と直し方' }),
        div({ class: 手順リスト }).childs([
            p({ text: '1. 道作成モード(大域編集では広域道路作成モード)で地形をクリックすると、いま描いている道の終わりに点が足される。' }),
            p({ text: '2. 道は1つのチャンクにも大域世界にも何本でも置ける。道路パネルの「対象の道」が、いま描き足す先であり、幅などの設定が効く相手でもある。' }),
            p({ text: '3. 道を分けるには、道作成モードのまま既存の点のマーカーをクリックする。その点を出発点に共有した新しい道が始まり、対象の道がその新しい道へ移る。' }),
            p({ text: '4. 別の場所から独立した道を始めるには、地形をダブルクリックするか、道路パネルの「新しい道」を押す。ダブルクリックは1回目のクリックで置いた点を終点にして、その道を終える。' }),
            p({ text: '5. 道編集モード(大域編集では広域道路編集モード)へ切り替えると、全ての道の点が地表に伏せた輪のマーカーで表示される。対象の道の点だけは別の色で描かれる。マーカーは画面上での大きさがそろうよう、カメラから遠いほど大きく描かれる。' }),
            p({ text: '6. マーカーを左ドラッグすると、その点が地形の上を追いかけて動く。ボタンを離した位置で確定し、Ctrl+Z1回でドラッグ前の位置へ戻る。' }),
            p({ text: '7. 道の面をクリックすると、その道のいちばん近い区間へ点が割り込む。曲がりを増やしたいときに使う。' }),
            p({ text: '8. マーカーをクリックすると点が選ばれ、道路パネルの「選択点を削除」で消せる。道ごと消すときは「この道を削除」、全部消すときは「全ての道を消す」を押す。' }),
            p({ text: '9. マーカーは道作成モードと道編集モードのときだけ表示される。ほかのモードでは触れないため隠してある。' }),
        ]),
    ])
}

export function ゲームとして歩いて確かめるの節を組み立てる(): DivC {
    return div({ class: セクション }).childs([
        div({ class: セクション見出し, text: 'ゲームとして歩いて確かめる' }),
        div({ class: 手順リスト }).childs([
            p({ text: '1. ターミナル(作業ディレクトリC:\\devs\\Blitzdrache0)で下のコマンドを実行すると、この世界をキツネで歩くゲームの窓が開く。操作はWASD=移動(カメラの向き基準)・マウスドラッグ=カメラ回転・ホイール=ズームである。' }),
        ]),
        span({ class: コード欄, text: ゲーム起動コマンド }),
        div({ class: 手順リスト }).childs([
            p({ text: '2. ゲームの窓を開いたままエディターで編集し、保存してからソースアセットへ書き出すと、数秒でゲームの絵が変わる(再起動は不要)。地形の造成・地表の塗り・道・建物の配置がすべて反映される。' }),
            p({ text: '3. 地表の色はマテリアル台帳の層(草・泥・岩・砂)のタイルで描かれる。' }),
            p({ text: '4. 自作の建物(アクティビティバーの「建物」の領域)は下のコマンドで1棟だけの検証世界を焼き、その建物だけを確かめられる。<建物定義ID>は確かめたい建物のIDに置き換える。' }),
        ]),
        span({ class: コード欄, text: 建物単体検証コマンド }),
    ])
}

export function AIからの操作の節を組み立てる(): DivC {
    return div({ class: セクション }).childs([
        div({ class: セクション見出し, text: 'AIからの操作' }),
        p({
            class: 段落,
            text: 'AIは画面を持たずにこのエディターを操作できる。編集コマンドの列をJSONファイルへ書き出し、editor_webディレクトリで次の1行を実行すると、人がボタンやブラシで操作したのと同じ適用実装が同じコマンドを適用する。画面を持つ入り口と画面を持たない入り口は、同じ編集モデルと同じ操作コマンドの適用処理を呼んでいるだけであり、意味の異なる別実装ではない。',
        }),
        span({ class: コード欄, text: ヘッドレス実行コマンド }),
    ])
}

export function テーマの切替の節を組み立てる(): DivC {
    return div({ class: セクション }).childs([
        div({ class: セクション見出し, text: 'テーマの切替' }),
        p({
            class: 段落,
            text: '一番左のアクティビティバー下部の歯車を押すと設定のパネルが左に出て、画面全体のカラーテーマを切り替えられる。選んだテーマは次回起動時も復元される。この使い方の読み物も、その下の「?」から何度でも開ける。',
        }),
    ])
}
