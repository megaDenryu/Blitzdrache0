import { style } from '@vanilla-extract/css'

export const 画面ルート = style({
    position: 'relative',
    width: '100%',
    height: '100%',
    overflow: 'hidden',
    userSelect: 'none',
    backgroundColor: '#0b0f19',
})
