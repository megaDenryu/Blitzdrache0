import type { チャンク座標 } from '../../../../生成/編集資源契約.ts'
import type { 道路の泥の被覆の記録の写し } from '../../編集モデル/index.ts'
import type { 道路の差し戻し断片 } from './道路の差し戻し断片.ts'

// 道路の変更に泥の焼き直しが伴ったときの、変更前情報。道路の変更そのものを取り消す断片を内側に包み、
// 外側で地表材質と被覆の記録を焼き直す前へ戻す。1つのコマンドが1つの断片で戻る規律を保つため、
// 道路の6種の断片へ材質の写しを足すのではなく、包む形にしている。
export type 道路の変更に伴う泥の焼き直し差し戻し = {
    readonly 種類: '道路の変更に伴う泥の焼き直し'
    readonly チャンク座標: チャンク座標
    readonly 変更前材質データ: Uint8Array
    readonly 変更前の道路の泥の被覆の記録: 道路の泥の被覆の記録の写し
    readonly 内側の道路の差し戻し: 道路の差し戻し断片
}
