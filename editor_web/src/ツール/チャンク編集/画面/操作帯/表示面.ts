// エディタ領域の中央に出す面。三次元ビューと見下ろし図は同じ枠を占め、片方だけを表示する(設計正本の判断6)。
// ツールルートのUI状態がこの型を値として持つため、SengenUIへ依存しないファイルに置く(Nodeのテストから読める)。
// 参照: `_doc/設計/見下ろし図による地形編集.md`「判断6」
export type 表示面 = '三次元' | '見下ろし図'

export const 表示面一覧: readonly 表示面[] = ['三次元', '見下ろし図']

export const 初期の表示面: 表示面 = '三次元'
