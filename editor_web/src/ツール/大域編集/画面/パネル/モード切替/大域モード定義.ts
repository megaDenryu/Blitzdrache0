// 大域編集ツールで選択可能な4つの編集操作モード。
export type 大域編集モード = '選択' | '大域造成' | '広域道路作成' | '広域道路編集'

export const 大域編集モード一覧: readonly 大域編集モード[] = [
    '選択',
    '大域造成',
    '広域道路作成',
    '広域道路編集',
] as const

// タブを開いた直後のモード。操作帯のボタン・棚の案内・編集状態の3つが同じ値から始まるように、
// 綴りをここ1箇所へ閉じる(エディター制作スキル第2条)。
export const 初期の大域編集モード: 大域編集モード = '選択'

// 道路点マーカーを画面に出すモード。他のモードでは触れない物体が浮いて見えるため隠す。
export function 広域道路の点を扱うモードか(モード: 大域編集モード): boolean {
    return モード === '広域道路作成' || モード === '広域道路編集'
}

// 各モードで左ボタンが何をするかの案内。視点の操作(右ドラッグ・中ドラッグ・ホイール)は
// どのモードでも同じであるため、ここには書かず案内札が1行で添える(判断13の共有の割り当て)。
export const 大域モードヒント写像: Record<大域編集モード, string> = {
    選択: '道路の点は広域道路編集モードで選ぶ(このモードでは左クリックも左ドラッグも何もしない)。',
    大域造成: '左ドラッグで山脈を盛る(シフトを押しながらで削る・平す)。筆の種類と半径と強さは下の棚で選ぶ。',
    広域道路作成: '地形を左クリックすると対象の道の終わりへ点が足される(チャンク境界を跨いで置ける)。既存の点をクリックするとそこから道が分かれる。地形のダブルクリック、または下の棚の「新しい道を始める」で別の道を始める。',
    広域道路編集: '道路の点を左ドラッグで動かし、左クリックで選ぶ(選んだ点の道が対象の道になる)。道の上をクリックすると、その場所へ点が割り込む。点や道を消すボタンは右サイドバーにある。',
}
