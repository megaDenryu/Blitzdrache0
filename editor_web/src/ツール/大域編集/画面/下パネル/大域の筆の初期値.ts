import type { 造成筆致種別 } from '../../../../生成/編集資源契約.ts'

// 棚に出る大域造成の筆のつまみの初期の位置。編集状態もこの値から始めることで、人が見ている
// つまみの位置と実際に筆が使う値が最初から一致する(エディター制作スキル第1条・第2条)。
export const 大域造成の筆の初期値: {
    readonly 種別: 造成筆致種別
    readonly 半径メートル: number
    readonly 強さ: number
} = { 種別: '盛る', 半径メートル: 80.0, 強さ: 1.2 }
