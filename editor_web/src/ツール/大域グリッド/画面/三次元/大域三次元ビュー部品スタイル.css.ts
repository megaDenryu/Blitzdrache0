import { style } from '@vanilla-extract/css'

export const コンテナ = style({
    width: '100%',
    height: '100%',
    position: 'relative',
    overflow: 'hidden',
})

export const キャンバス = style({
    width: '100%',
    height: '100%',
    display: 'block',
    outline: 'none',
})
