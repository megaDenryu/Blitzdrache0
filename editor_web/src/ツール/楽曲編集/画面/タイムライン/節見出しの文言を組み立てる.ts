import type { パターン } from '../../../../生成/編集資源契約.ts'
import { パターンの表示名を探す } from './パターンの表示名を探す.ts'

// 節の枠の見出しの文言を組み立てる。繰り返し回数は選択欄が持つため、
// この文言はパターンの表示名だけを返す(節の枠部品が「A ×」の形へ組む、issue #92)。
export function 節見出しの文言を組み立てる(パターンの名乗り: string, パターン一覧: readonly パターン[]): string {
    return パターンの表示名を探す(パターンの名乗り, パターン一覧)
}
