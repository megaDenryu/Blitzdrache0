import type { カードの操作 } from '../../操作コマンド/index.ts'

// カードに小さなボタンとして出す4つの操作。末尾へ追加は選択中のカードに紐づかないため除き、
// 前へ移動・後へ移動は節の枠へ移した(設計正本`_doc/設計/楽曲エディター.md`判断15の是正、issue #88)。
export type カード操作の種類 = Exclude<カードの操作, { readonly 種類: '末尾へ追加' | '前へ移動' | '後へ移動' }>['種類']
