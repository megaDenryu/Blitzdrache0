import { デシベル, 乱数の種, 秒, 音量の比 } from 'SengenAudio'
import { 拍毎分の下限, 遅延のステップ数の上限 } from '../../../../生成/編集資源契約.ts'
import { ステップ, 拍毎分 } from '../../編集モデル/index.ts'

// 試作 `_doc/ゲーム開発用エディター計画/シーケンサー.html` の getAudioContext が組む効果の値を写す。
// 人が調整する値(全体の音量・残響の量・遅延の量・遅延のステップ数)はミキサー設定が持つため、ここには無い。
export const 圧縮を始める音量 = デシベル.生成する(-20)
export const 圧縮の折れ曲がりの幅 = デシベル.生成する(12)
export const 圧縮比 = 4
export const 残響の長さ = 秒.生成する(2.2)
export const 残響の減衰の指数 = 3.5

// 試作は Math.random で残響の応答を作るが、同じ楽曲が同じ音を出すことを確かめられるよう種つきの乱数へ置き換える。
export const 残響の雑音の種 = 乱数の種.生成する(20260828)
export const 遅延の帰還の比 = 音量の比.生成する(0.35)

// 遅延の長さは拍毎分とステップ数で決まる。いちばん遅い拍といちばん長いステップ数で要る幅を先に確保する。
export const 遅延の緩衝の最大の長さ = 秒.生成する(
    拍毎分.生成する(拍毎分の下限).ステップ数の秒数(ステップ.生成する(遅延のステップ数の上限)).数値(),
)
