import {
    既定のコード進行一覧,
    type コード進行,
    type コード進行参照,
} from '../../../../../生成/編集資源契約.ts'
import type { セレクトの選択肢 } from './セレクトの選択肢.ts'

// セレクトが扱えるのは文字列だけであるため、判別共用体であるコード進行参照を綴りへ写して運ぶ。
// 綴りと復元をこの1箇所へ閉じ、パネルの側では判別共用体のまま扱う。
const 楽曲全体に従う綴り = '全体に従う'
const 既定の進行の接頭辞 = '既定:'
const 独自の進行の接頭辞 = '独自:'

export type 進行の選び =
    | { readonly 種類: '楽曲全体の進行に従う' }
    | { readonly 種類: 'この進行を使う', readonly 参照: コード進行参照 }

function コード進行参照を綴りへ写す(参照: コード進行参照): string {
    switch (参照.種類) {
        case '既定の進行':
            return `${既定の進行の接頭辞}${参照.識別子}`
        case '独自の進行':
            return `${独自の進行の接頭辞}${参照.名前}`
    }
}

function 参照が一致するか(選ばれている: コード進行参照 | null, 候補: コード進行参照): boolean {
    return 選ばれている !== null && コード進行参照を綴りへ写す(選ばれている) === コード進行参照を綴りへ写す(候補)
}

function 進行ごとの選択肢一覧を並べる(
    選ばれている: コード進行参照 | null,
    独自進行一覧: readonly コード進行[],
): セレクトの選択肢[] {
    const 選択肢一覧: セレクトの選択肢[] = []
    for (const 進行 of 既定のコード進行一覧) {
        const 候補: コード進行参照 = { 種類: '既定の進行', 識別子: 進行.識別子 }
        選択肢一覧.push({
            value: コード進行参照を綴りへ写す(候補),
            text: `既定: ${進行.表示名}`,
            selected: 参照が一致するか(選ばれている, 候補),
        })
    }
    for (const 進行 of 独自進行一覧) {
        const 候補: コード進行参照 = { 種類: '独自の進行', 名前: 進行.名前 }
        選択肢一覧.push({
            value: コード進行参照を綴りへ写す(候補),
            text: `独自: ${進行.名前}`,
            selected: 参照が一致するか(選ばれている, 候補),
        })
    }
    return 選択肢一覧
}

// トラックの進行の割り当ての選択肢一覧。割り当てが無い状態を先頭の選択肢として明示する。
export function トラックの進行の選択肢一覧を組み立てる(
    進行の割り当て: コード進行参照 | null,
    独自進行一覧: readonly コード進行[],
): セレクトの選択肢[] {
    return [
        {
            value: 楽曲全体に従う綴り,
            text: '楽曲全体の進行に従う',
            selected: 進行の割り当て === null,
        },
        ...進行ごとの選択肢一覧を並べる(進行の割り当て, 独自進行一覧),
    ]
}

// パターンの進行の選択肢一覧。パターンは必ず進行を1つ持つため、楽曲全体に従う選択肢は出さない。
export function パターンの進行の選択肢一覧を組み立てる(
    進行の参照: コード進行参照,
    独自進行一覧: readonly コード進行[],
): セレクトの選択肢[] {
    return 進行ごとの選択肢一覧を並べる(進行の参照, 独自進行一覧)
}

// 選ばれた綴りを進行の選びへ戻す。この綴りは上の組み立てが出したものしか来ないため、
// 読めない綴りは画面の配線の誤りであり、既定値で埋めずに失敗させる。
export function 綴りから進行の選びを復元する(綴り: string): 進行の選び {
    if (綴り === 楽曲全体に従う綴り) {
        return { 種類: '楽曲全体の進行に従う' }
    }
    if (綴り.startsWith(既定の進行の接頭辞)) {
        return { 種類: 'この進行を使う', 参照: { 種類: '既定の進行', 識別子: 綴り.slice(既定の進行の接頭辞.length) } }
    }
    if (綴り.startsWith(独自の進行の接頭辞)) {
        return { 種類: 'この進行を使う', 参照: { 種類: '独自の進行', 名前: 綴り.slice(独自の進行の接頭辞.length) } }
    }
    throw new Error(`コード進行の選択肢として読めない綴りです: ${綴り}`)
}
