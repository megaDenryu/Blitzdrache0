import type { チャンク座標 } from '../生成/編集資源契約.ts'

// タブ見出しとインスペクター見出しが共有する、編集対象の表示名の正本。
// 参照: `_doc/計画/ユビキタス言語.md`「ゲーム開発用エディターの語彙」。
export const 大域世界表示名 = '大域世界'

export function チャンク表示名を生成する(座標: チャンク座標): string {
    return `チャンク (${座標.x}, ${座標.z})`
}
