// REST通信の要求手続きの束ね。実装はJSON往復・バイナリ往復・書き出し要求へ分かれ、
// 呼び出し側(実サーバー接続)はこの1箇所から読み込む。
export { JSONを取得する, JSONを送信する } from './JSON往復.ts'
export { バイナリを取得する, バイナリを送信する } from './バイナリ往復.ts'
export { 書き出しを要求する } from './書き出し要求.ts'
