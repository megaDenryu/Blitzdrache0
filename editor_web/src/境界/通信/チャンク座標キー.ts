import type { チャンク座標 } from '../../生成/編集資源契約.ts'

// チャンク座標をMap・DOMのキーとして使うための正準文字列表現。
// 通信層・入り口層・ツール層はチャンク座標を文字列キー化するときここへ集約する。
export function チャンク座標キーを生成する(座標: チャンク座標): string {
    return `${座標.x},${座標.z}`
}
