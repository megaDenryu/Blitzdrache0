import type { 書き出し結果 } from './サーバー通信結果.ts'

// エディターサーバーの`POST /api/書き出し/ソースアセット`を呼ぶ差し替え可能な境界。
// 保管庫が保持する大域世界を、blitz_asset_compilerのソースアセット形式へ変換して
// 書き出す明示操作であり、プロジェクト保管庫接続の読み・保存の口とは別に持つ。
// 参照: `_doc/設計/ゲーム開発用エディター基盤.md`「判断5」
export interface ソースアセット書き出し接続 {
    ソースアセットへ書き出す(世界名?: string): Promise<書き出し結果>
}
