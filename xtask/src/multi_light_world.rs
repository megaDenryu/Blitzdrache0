//! 夜の多光源と屋内の多光源という2つの検収世界の共有部分。担当するのは、世界ごとの起動材料を`world`が、
//! 1つの条件を本番の描画経路で1回走らせる工程を`run`が持つという分担だけである。判定は1つも持たない。
//!
//! 2つの入口(`cluster_lights`と`point_light_shadow`)が同じ世界を同じ条件で走らせるため、ここへ集める。
//! 入口ごとに起動引数を書くと、同じ世界を測ったはずの2つの表が別の条件の表になり、並べて読めなくなる。

pub(crate) mod run;
pub(crate) mod world;
