//! 検証の出力の置き場: 検証が書き出すものとビルドが残す中間データが載る木の根と、その木の下の置き場1つを指す名前。
//!
//! 木の名前の綴りを持つのは`root`だけである。入口はどれも置き場の名前だけを宣言し、パスはこの型のメソッドが導く。
//! 綴りが入口ごとに散ると、置き場を移すときに触り漏らした入口だけが古い場所へ書き続け、
//! しかも書けてしまうため誰も気づかない。`cargo xtask conform`が、ほかのファイルに木を指す綴りが
//! 現れることを違反として止める。
//! 参照: CLAUDE.md「ツールとドキュメントの配置」、グローバル規約「プリミティブ執着禁止はパス・テキスト・名前にも適用する」。

mod name;
mod root;
mod spelling;

pub(crate) use name::検証の出力の置き場名;
pub(crate) use root::検証の出力ルート;
