//! 1つのファイルの中で読んだ `impl` の1件。所在(ファイルのパスとコードだけの行の一覧と見出しの行番号)と見出しを持つ値である。
//! 実装の位置のモジュールと、書いた名前がどの定義を指すかを答える問いは持たない。自己変更の禁止の検査が名前の過大近似へ替わり、名前解決を1つも行わなくなったためである(`mutable_impl_scan.rs`)。

use std::path::Path;

use super::impl_header::実装の見出し;

pub struct 読んだ実装<'a> {
    pub パス: &'a Path,
    pub 行一覧: &'a [String],
    pub 行番号: usize, // 見出しの行の1始まりの番号。違反の報告がこの行を指す
    pub 見出し: 実装の見出し,
}
