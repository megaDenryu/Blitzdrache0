//! 終了時報告の行の綴り。validationの件数の見出しと、パス別の描画発行の9行を持つ。
//!
//! 綴りに差し込みの名前(`{パス名}`)まで含めるのは、出す側の書式と読む側の書式が同じ差し込み名を使っており、
//! 名前まで含めた方が「個体数」と「段別個体数」のような部分一致の取り違えを起こさないためである。
//! 差し込み名を変えるときも台帳を直すことになるが、その手間は取り違えを許さないことと引き換えである。

use super::綴りの契約;

const 描画発行を出す側: &str = "crates/blitz_app/src/reports/draw_issue.rs";
const 描画発行を読む側: &str = "xtask/src/report_parse/extract.rs";
const 描画発行の両側: &[&str] = &[描画発行を出す側, 描画発行を読む側];

pub(super) const 綴り一覧: [綴りの契約; 10] = [
    綴りの契約 {
        綴り: "validationエラー・警告合計件数:",
        現れるファイル一覧: &["crates/blitz_app/src/main.rs", "xtask/src/validation_count.rs"],
    },
    綴りの契約 {
        綴り: "{パス名}発行数:",
        現れるファイル一覧: 描画発行の両側,
    },
    綴りの契約 {
        綴り: "{パス名}候補数:",
        現れるファイル一覧: 描画発行の両側,
    },
    綴りの契約 {
        綴り: "{パス名}可視数:",
        現れるファイル一覧: 描画発行の両側,
    },
    綴りの契約 {
        綴り: "{パス名}個体数:",
        現れるファイル一覧: 描画発行の両側,
    },
    綴りの契約 {
        綴り: "{パス名}段別個体数:",
        現れるファイル一覧: 描画発行の両側,
    },
    綴りの契約 {
        綴り: "{パス名}投入インデックス数:",
        現れるファイル一覧: 描画発行の両側,
    },
    綴りの契約 {
        綴り: "{パス名}描画数:",
        現れるファイル一覧: 描画発行の両側,
    },
    綴りの契約 {
        綴り: "{パス名}パイプライン切替:",
        現れるファイル一覧: 描画発行の両側,
    },
    綴りの契約 {
        綴り: "{パス名}材質切替:",
        現れるファイル一覧: 描画発行の両側,
    },
];
