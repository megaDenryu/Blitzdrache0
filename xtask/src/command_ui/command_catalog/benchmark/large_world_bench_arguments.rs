//! 大規模世界の固定経路計測が受け取る引数の定義。1コマンドで独立したファイルへ置くのは、この1件だけで
//! 15の条件を持ち、分類の一覧と同居させると一覧の見通しを潰すためである。
//! 既定値はどれも最初の公式実測条件であり、正本は`xtask/src/large_world_bench/arguments.rs`が持つ。

use super::super::argument::引数定義;
use super::super::argument::省略したときの扱い::実装の既定に任せる;

pub(super) const 大規模世界の固定経路計測の引数定義: &[引数定義] = &[
    引数定義::有無だけの旗を定義する("--print-plan", "計測の計画だけを表示し、GPUを起動しない"),
    引数定義::名前に続けて渡す値を定義する(
        "--asset-root",
        "読み込む実行時アセットのルート",
        実装の既定に任せる("大規模世界の計測入力の置き場"),
    ),
    引数定義::名前に続けて渡す値を定義する(
        "--scene",
        "走らせる検収シーンの綴り",
        実装の既定に任せる("キツネの場所巡りの起動シーン"),
    ),
    引数定義::名前に続けて渡す値を定義する("--frames", "1回の走行で描くフレーム数", 実装の既定に任せる("1000")),
    引数定義::名前に続けて渡す値を定義する(
        "--preload-radius",
        "先読みするチャンクの半径。17以上は同時候補が急増するため拒む",
        実装の既定に任せる("8"),
    ),
    引数定義::名前に続けて渡す値を定義する("--ram-limit", "RAMの上限バイト数", 実装の既定に任せる("536870912")),
    引数定義::名前に続けて渡す値を定義する("--vram-limit", "VRAMの上限バイト数", 実装の既定に任せる("536870912")),
    引数定義::名前に続けて渡す値を定義する("--loader-workers", "読み込みのワーカーの本数", 実装の既定に任せる("1")),
    引数定義::名前に続けて渡す値を定義する("--request-capacity", "読み込み要求の待ち行列の容量", 実装の既定に任せる("64")),
    引数定義::名前に続けて渡す値を定義する("--completion-capacity", "読み込み完了の待ち行列の容量", 実装の既定に任せる("4")),
    引数定義::名前に続けて渡す値を定義する(
        "--route-start-east-meters",
        "固定経路の始点の東方向のメートル",
        実装の既定に任せる("0"),
    ),
    引数定義::名前に続けて渡す値を定義する(
        "--route-start-south-meters",
        "固定経路の始点の南方向のメートル",
        実装の既定に任せる("-4000"),
    ),
    引数定義::名前に続けて渡す値を定義する("--route-end-east-meters", "固定経路の終点の東方向のメートル", 実装の既定に任せる("0")),
    引数定義::名前に続けて渡す値を定義する(
        "--route-end-south-meters",
        "固定経路の終点の南方向のメートル",
        実装の既定に任せる("4000"),
    ),
    引数定義::名前に続けて渡す値を定義する(
        "--route-meters-per-frame",
        "1フレームあたりに経路上を進むメートル",
        実装の既定に任せる("20"),
    ),
];
