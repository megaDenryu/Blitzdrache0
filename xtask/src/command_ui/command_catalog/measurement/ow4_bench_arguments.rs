//! 植生密度別の性能計測が受け取る引数の定義。1コマンドで独立したファイルへ置くのは、この1件だけで
//! 物量点と描画条件と10種のシャドウ計測指定を持ち、分類の一覧と同居させると一覧の見通しを潰すためである。
//! シャドウ計測指定の語はそのまま`blitz_app`へ渡る。正本は`xtask/src/ow4_bench/shadow_condition.rs`が持つ。

use super::super::argument::引数定義;
use super::super::argument::省略したときの扱い::{実装の既定に任せる, 語を1つも足さない};

pub(super) const 植生密度別の性能計測の引数定義: &[引数定義] = &[
    引数定義::位置で何個でも渡す値を定義する(
        "チャンクあたり個体数",
        "測る物量点を空白区切りで並べる。0は失敗する",
        実装の既定に任せる("400 4000 40000"),
    ),
    引数定義::有無だけの旗を定義する(
        "--production-draw",
        "空とポスト処理を含む本番の描画条件で測る(既定は--unlit --no-post --no-skyの素の描画)",
    ),
    引数定義::名前に続けて渡す値を定義する("--time-of-day", "描く一日内時刻の秒", 実装の既定に任せる("世界の方針の既定時刻")),
    引数定義::名前に続けて渡す値を定義する("--shadow-resolution", "シャドウマップの一辺のテクセル数", 語を1つも足さない),
    引数定義::名前に続けて渡す値を定義する("--caster-margin", "キャスターを含める余白のメートル", 語を1つも足さない),
    引数定義::名前に続けて渡す値を定義する("--max-shadow-distance", "多段設定の最大影距離のメートル", 語を1つも足さない),
    引数定義::名前に続けて渡す値を定義する(
        "--shadow-caster-range",
        "影の候補を切るカメラからの中心距離のメートル",
        語を1つも足さない,
    ),
    引数定義::名前に続けて渡す値を定義する("--camera-yaw", "カメラの方位の度。個体密度分布が違う視点を選ぶ", 語を1つも足さない),
    引数定義::名前に続けて渡す値を定義する("--camera-nudge", "方位と組み合わせてカメラを別位置へ移すメートル", 語を1つも足さない),
    引数定義::有無だけの旗を定義する("--no-instance-shadow", "群の個体を影の候補から外す(地形は影を落とし続ける)"),
    引数定義::有無だけの旗を定義する("--no-shadow-casters", "地形も個体も布も積まず、キャスターを全部外す"),
    引数定義::有無だけの旗を定義する("--no-instance-lod", "段選択を止め、全個体を最詳細段で描く"),
    引数定義::有無だけの旗を定義する("--report-caster-distance", "キャスター距離の分布を報告させる"),
];
