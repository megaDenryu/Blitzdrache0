//! 食い違いの測り方の反証。担当するのは、相対差がどちらを基準にしても同じ値になり、両方が零のときに
//! 無限大へ跳ねないことを示すことである。

use super::*;

#[test]
fn 大きい側を分母にする() {
    assert!((相対差(1.0, 1.1) - 0.090_909).abs() < 1.0e-5);
    assert!((相対差(1.1, 1.0) - 0.090_909).abs() < 1.0e-5);
    assert!((相対差(0.0, 0.0)).abs() < 1.0e-12);
}
