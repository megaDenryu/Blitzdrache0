//! `実行結果`のテスト専用構築。呼ばれるのは`cargo test`だけであり、本番の`描画する`(blitz_appを起動して
//! 読み戻す)とは呼び出しタイミングが明確に異なるため、生成の局面をここへ分ける。

use super::実行結果;

impl 実行結果 {
    /// 合成したRGBA8バイト列から`実行結果`を作る。判定のユニットテストが、実機を起動せずに
    /// 既知の画素配置を持つ絵を用意するために使う。
    pub(in crate::sky_time) fn テスト用に作る(幅: usize, 高さ: usize, rgba8: Vec<u8>) -> Self {
        Self { 幅, 高さ, rgba8 }
    }
}
