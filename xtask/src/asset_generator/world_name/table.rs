//! 焼ける世界の表と、その表から型と一覧と綴りを同時に生む仕組み。
//!
//! # 表の書式
//!
//! `世界の表!` へ渡すのは「枝の名前 => 綴り,」の行の並びである。行の前には`///`の注釈を置ける。
//!
//! ```ignore
//! 世界の表! {
//!     /// 説明を置いてよい。
//!     板の世界 => "chunk_world",
//!     地形の世界 => "terrain_world",
//! }
//! ```
//!
//! # 表から生まれるもの
//!
//! 1つの行から4つが同時に生まれる。判別共用体の枝、`綴り`の`match`の腕、`一覧`の要素、
//! `引数の綴りから解釈する`の`match`の腕である。
//!
//! # なぜ表にするか
//!
//! 枝と一覧と綴りを別々に書くと、どれか1つを書き忘れた状態が構文として成立する。連なりで一覧を作る形も、
//! 新しい枝を足して直前の枝から向け直すのを忘れると、コンパイルも試験も通ったまま新しい枝が一覧から脱落した。
//! 表なら書ける場所が1つしかないため、**一覧から切り離された枝を書き表せない**。
//!
//! 表の外から枝を足すこともできない。判別共用体の定義そのものがこのマクロの出力であり、
//! 表に無い枝はそもそも存在しない。

/// 焼ける世界の表から、判別共用体と綴りと一覧と逆引きを生む。書式と生成物はモジュールの冒頭にある。
macro_rules! 世界の表 {
    ($( $(#[$注釈:meta])* $枝:ident => $綴り:literal, )+) => {
        #[derive(Debug, Clone, Copy, PartialEq, Eq)]
        pub enum 世界名 {
            $( $(#[$注釈])* $枝, )+
        }

        impl 世界名 {
            /// プロセス境界でこの世界を指す綴り。生の綴りへ戻るのはここだけである。
            pub fn 綴り(self) -> &'static str {
                match self {
                    $( Self::$枝 => $綴り, )+
                }
            }

            /// 焼ける世界の全枝。表の行の順に並ぶ。
            pub fn 一覧() -> Vec<Self> {
                vec![ $( Self::$枝, )+ ]
            }

            /// コマンドの引数として受け取った綴りを枝へ写す。知らない綴りは受け取らず、無いことを返す。
            /// 黙って既定の世界へ倒すと、綴りを打ち間違えた実行が別の世界を焼いたまま成功する。
            pub fn 引数の綴りから解釈する(綴り: &str) -> Option<Self> {
                match 綴り {
                    $( $綴り => Some(Self::$枝), )+
                    _ => None,
                }
            }
        }
    };
}

pub(super) use 世界の表;
