# Blitzdrache0 コーディングルール（Rust版）

グローバルCLAUDE.mdのコーディング憲法をRustに適用した細則。本ルールは開発初日から適用する。
「雑に書いて後で綺麗にする」は禁止。最初の1行から規約適合で書く。一度の横着も許容しない。

用語は `_doc/計画/ユビキタス言語.md` の定義に従う。

## 作業のしかた（2026-09-04制定）

- **絶対パスで実行する。Bash/PowerShellツールで `cd` をコマンド接頭に付けない**（`cd <path> && ...` の形を禁止）。作業ディレクトリは既にリポジトリルートである。`cd` を挟むと以後のツール呼び出しの作業ディレクトリ推定が食い違い、意図しないパスでコマンドが実行される事故につながる
  - **例外: 別の作業ツリーで `cargo xtask` を走らせるときは、括弧で囲んだ副シェル `(cd <その作業ツリー> && cargo xtask conform)` で実行する**（2026-09-05制定）。`cargo run --manifest-path <別の作業ツリー>/xtask/Cargo.toml -- conform` の形は作業ディレクトリが本体のままになり、`conform` が台帳を構築元の作業ツリーから、走査する原本を本体から読んで、2つのリポジトリの状態を突き合わせた偽の違反を報告する（2026-09-05に実装員が「4件の違反」として誤報した）。副シェルは作業ディレクトリを持ち越さないため、上の禁止の理由に当たらない
- **ビルドと検証は親(監督者)の責務であり、実装員は `cargo`(`fmt` を除く)と `npm run` を走らせない**(2026-09-05制定。オーナー裁定「実装員は実装が責務でしょ。ビルドは違う」)。検証は親が常駐させる `ビルド員` 1体が回し、実装員は段階のコミットごとにビルド員へ SendMessage で依頼する。ビルドはビルド専用のチェックアウト `C:/devs/Blitzdrache0_build`(切り離した作業ツリー)だけで行い、ビルド員が依頼された先端をそこへ `git fetch` して切り替える。作業ツリーどうしで置き場を共有しない(cargoが別の作業ツリーで組んだ実行ファイルを流用する)。実装員の作業ツリーはソースだけを持ち、`target-<名前>` を作らない。全文は `.claude/skills/実装ハーネス/SKILL.md`「検証はビルド員が回し、ビルドの置き場は本体の1つだけにする」
- **サブエージェント定義は `.claude/agents/` に置く（2026-09-25時点で26体構成）。** 実装は層ごと、検収は観点ごとに専任へ分けてある。担当領域と必読ファイルが定義側に固定されているため、委譲プロンプトが短くなり、低コストのモデルでも規約に沿った成果が出る。振り分けの手順は `.claude/skills/実装ハーネス/SKILL.md` と `.claude/skills/レビューハーネス/SKILL.md` が持つ
  - **汎用**: 実装員（Opus 5.5・medium。既定。難易度が高い作業もこちらを使う）/ レビュー員（Opus 5.5・medium。既定。難易度が高い検収もこちらを使う）。実装員Fable版・レビュー員Fable版（いずれもFable 5.1・low）は定義を残すが、Opus 5.5がFable 5.1を上回ったため当面呼び出さない
  - **層ごとの実装**: 型契約実装員・編集モデル実装員・操作コマンド実装員・画面実装員・サーバー実装員・文書実装員（いずれもsonnet・medium）/ 物理実装員（Opus 5.5・medium）/ 機械的置換員（sonnet・low）/ リネーム員（sonnet・medium）
  - **観点ごとの検収（差分を見る）**: 命名チェック員（sonnet・medium。2026-09-12にhaikuから上げた）/ 構造チェック員（sonnet・high）/ テスト実効性チェック員・設計正本整合チェック員（sonnet・high）/ 行数と分割の質チェック員・文章チェック員（sonnet・medium）/ 契約整合チェック員・孤立実装チェック員（sonnet・low）/ **ビルド員**（sonnet・low。常駐1体。実装員がコミットごとに直接依頼し、本体の置き場1つで直列に検証を回す）
  - **変更影響の検収（差分を起点に差分の外をたどる）**: 総合レビュー員（Opus 5.5・medium。常時1体）。親は、総合レビュー員を観点ごとの検査員と並べて毎回走らせる。総合レビュー員の主な担当は変更影響検査であり、差分が原因なのに欠陥の証拠が差分の外にある問題を、呼び出し元・利用箇所・設計正本などを必要な範囲だけたどって調べる。総合レビュー員は、あわせて依頼の目的適合・観点をまたぐ不整合・設計の筋・工程の飛ばし・マージの見立てを見る。検査の3種類(差分検査・変更影響検査・現状検査)の定義は `.claude/skills/レビューハーネス/SKILL.md` にある
  - **親の相談相手**: 副官（Opus 5.5・high。読み取り専任。親が判断を固める前に反論と代案を返す。実装も合否の判定もしない）
  - **育った現状を見る（差分では捕まらない劣化）**: 溶けた概念の分解員（Opus 5.5・medium。既定。難易度が特に高い領域もこちらを使う）。溶けた概念の分解員Fable版（Fable 5.1・medium）は定義を残すが、Opus 5.5がFable 5.1を上回ったため当面呼び出さない。この役はすべての検査員の中で最も難しい判断を要する。**差分の検収では走らせない。** 領域を決めたローラー検査と節目の定点で走らせ、結果を `_doc/計画/検査の網羅台帳.md` へ記録する(周回の手順は `.claude/skills/ローラー検査/SKILL.md`)。設計の劣化は1回の変更では起きず、正しい変更の累積として起きるため、差分を見る検査では原理的に捕まえられない（2026-09-04制定。`blitz_app`の`アプリ`が46フィールドまで育つあいだ、既存の検査がどれも違反を出さなかったことによる）
- **物理基盤(`blitz_sim`・`blitz_collision`・`blitz_math` の数値表現)を変える作業を、親は `.claude/skills/物理基盤の工程/SKILL.md` の順(契約 → 責務層 → 反証 → 一次計測 → 設計裁定 → 本実装)で進める**(2026-09-09制定。PR #97 の開発プロセスのレビューでのオーナー裁定)。親は、症状を通す最小の差分から本実装へ直行しない。数値表現・時間の意味・状態の寿命・CPUとGPUの境界を変える作業について、親は本実装の前に短い設計裁定を取る
- **親は判断を下す前に `副官`(`.claude/agents/副官.md`。Opus 5.5・high。読み取り専任)へ反論を求め、判断を検討し直す**(2026-09-09制定)。副官は開発プロセスと設計の相談相手であり、親の判断(契約の書き出し・責務層の列挙・設計の案・委譲の切り方・裁定を求める文)に反論と代案を返す。副官は実装も差分の合否の判定も行わない
- **親は、実装員を1体で長く保たない。親は、作業の段階ごとに新しい体へ引き継いでよい**(2026-09-09制定。オーナー指示)。文脈が長い体は1回の応答の費用が増える。成果は作業ツリーとコミットに残るため、親が体を替えても失われない。手順は `.claude/skills/実装ハーネス/SKILL.md`「親は実装員を1体で長く保たない」
- **issueとPull Requestのやり取りは `.claude/skills/GitHubのやり取り/SKILL.md` に従う**(2026-09-04制定)。**次の5つはオーナーの指示を待たずに親が自分から行う。** issueを読んだら吟味の結果をコメントする。issueの作業が終わったら完了条件を1つずつ引用して現在地を報告する。PRへpushしたら、親は本文の「何をしたか」と検証を更新する。レビューへ対応したらそのPRへ返信を投稿する。裁定が要る判断が出たら推奨を1つ選んで理由を添える。**書かないことは、やっていないことと同じである**(読み手は、コミットの一覧からどの指摘がどう処理されたかを再構成できない)。PRの本文はオーナーが差分を読まずに受理を判断する文書である。親は、判断に要る見出しだけを書き、1画面に収める。コミットハッシュを裸で書かず、そのコミットが何をしたかを日本語で書いたリンクにする(`991850b1`という文字列は読み手にとって意味を持たない)。投稿・更新の前に文章チェック員へ回す
- **規約の読ませ方は役割で分ける。** 実装側とレビュー員は `/rule`（`.claude/commands/rule.md`）で全規約を読む。**観点を限定したチェック員は `/rule` を使わず**、定義に書かれた役割別の必読リストだけを読む。広範な規約を読ませると担当外の観点で水増しの指摘が出るためである
- **`/rule` が読む全プロジェクト共通のルールも `.claude/` へ複製する（2026-09-04制定）。** `.claude/global-reference/全プロジェクト共通CLAUDE.md` にグローバルCLAUDE.md全文の複製を、`.claude/skills/` にグローバルCLAUDE.mdが「全文は`skills/XXX`にある」と指す4スキル（doc-writing・アーキテクチャ先行・layer-roles・エディター制作）の複製を置く。別のPCで`~/.claude/CLAUDE.md`が未整備でも、本ファイルが指す「グローバルCLAUDE.mdの「XXX」に従う」という各参照を、この複製から読めるようにするためである
- **これらの複製の更新はユーザーの明示的な指示があったときだけ行う。** マシンごとに`~/.claude/`の内容は揃っているとは限らないため、セッションが自分のマシンのグローバル版とこの複製の食い違いに気づいても、それだけを理由に複製を書き換えない（PCを移るたびに上書き合戦になるのを防ぐため）。このリポジトリでの作業は常にこの複製の内容に従う
- **親とすべての実装員は、受け取ったレビューを「レビューを受け取る側の構え」で扱う**(2026-09-25制定)。この対象には、専任の実装員(型契約実装員・画面実装員など層ごとの実装員)を含む。全文は `.claude/skills/レビューハーネス/SKILL.md`「レビューを受け取る側の構え」にある

## 受理条件は「動くこと」で終わらない(2026-09-21制定。Issue #170・#173)

- 受理するコードは、実行上の正しさと、ドメイン上の正しさと、構造上の正しさと、人間が理解可能であることを同時に満たす。実装者は「動くこと」だけを受理条件にしない
- AIが導入する型・関数・中間の概念は、その存在理由と粒度を説明可能でなければならない。実装者は、説明できないものを、コンパイルが通りテストが通りゲームが正しく動いても受理しない
- **実装者は、日本語の識別子であること自体を可読性の達成とみなさない。** 意味の薄い上位語・曖昧な抽象化・説明できない粒度の中間の概念を避ける
- 実装者は、設計解釈マーカー・関数の役割・設計関係・法則で表せる設計の意味をコードの上に明示する。実装者は、既存のドメインの命題と構造の法則を壊す変更をしない
- 実装員と親は、新しいドメインの型・関数・設計関係を足した変更の完了報告に、その概念の設計上の位置づけ(何であるか・何と関係するか・既存の概念と何を区別するか・どの法則と命題に参加するか・なぜその粒度か)を書く
- 全文の正本は `_doc/設計/設計オントロジー.md` 第6節から第11節である。機械化した命題は `cargo xtask design-verify` が検証する

## ファイル・関数の分割

原則の本体はグローバルCLAUDE.md「1ファイル100行の原則と分割の質」にある。そこが1ファイル100行以内と関数50行以内を定め、100行に収めるためだけの分割を禁じ、判定の問い（そのファイルは名前を付けられる責務・概念・生成物を所有しているか、それとも1つの流れを長さだけで切った一片か）を置き、超過を許すのは統合した結果に限って上限を150行とし、超過の根拠をファイル冒頭コメントへ書くことと例外の台帳の陳腐化検査を求めている。本節と次節は、それをRustへ適用する細則だけを書く。

- 超えそうなら書く前に分割を設計する（書いてから割るのではない）。既存の超過ファイルは、その領域を触る作業のついでに責務単位で分ける
- Rustの流儀に従い `mod.rs`（または親モジュールファイル）で re-export して外部への見え方を整える
- **Graphite(Rustの型で検査されるグラフの記述言語)のコードには、1ファイル100行の制約を当面適用しない**(2026-09-26のオーナー裁定。Issue #187)。対象は、利用者が手で書くGraphiteのコード(schemaの宣言・`graph!` の中身)と、Graphiteの生成器が書き出す生成物の両方である。どういう制約にするかは、親が実物を書いて行数を実測した後に決める。手で書くGraphiteのコードについては、`cargo xtask conform` の行数の検査が、Graphiteのマクロ(`dynamic_graph_schema!`・`static_graph_schema!`・`graph!`・静的グラフのinstanceのマクロ)の本体の行を数えから除き、同じファイルの残りの行には1ファイル100行の原則を当てる。検査はファイル全体を上限から外さない。外すと、マクロを1回呼ぶだけで同じファイルの手書きのRustが何行でも書けるためである。正本は `xtask/src/conform/graphiteのコード/マクロの本体.rs` である
- **Graphiteの生成物は、`cargo xtask conform` の行数・1つの宣言へ2行以上積んだ説明の注釈・参照パスの実在の3検査と、設計解釈マーカーの実装の置き場の警告と、型ごとの分量の台帳の検査と、親の型を丸ごと受け取る自由関数の検査と、設計関係の抽出から外す**(同じ裁定。型ごとの分量の台帳の検査は2026-09-26の作業G1で親の判断により足した。自由関数の検査と単独の `cargo xtask type-metrics` も、同じ観測(`xtask/src/type_metrics/生成物を除いた観測.rs`)から生成物を外し、外した件数を表示する。この検査は手で書いた型への責務の集中を見るものであり、生成物が利用者の型へ足すimplは手編集できずその目的に当たらないうえ、`include!` で取り込まれる生成物の `super::型` を計測が物理のパスから解けないためである)。生成物は手編集を禁じられており、実装者が直せないためである。宣言の間のコメントだけの行の検査は外さず、生成物にも当てる。検査器は生成物を置き場のパスでなく先頭の手編集禁止の見出しで見分け、見出しが名乗る生成元のファイルがそのファイルを生成先として宣言していることも確かめる。パスで見分けると手書きのコードをその場所へ置くだけで検査を抜けられ、見出しだけで見分けると見出しを書き写すだけで抜けられるためである。結び付かない見出しを持つファイルは違反になる。外した生成物の件数と名前は `cargo xtask conform` が表示する。正本は `xtask/src/conform/graphiteのコード.rs` である

**クレートの `tests/` の直下だけは、ファイルを分ける費用が他の場所と違う**（2026-09-05制定。Issue #60から。検査の軸としては「ビルドの費用」と呼ぶ。参照: `_doc/計画/検査の網羅台帳.md`）。Cargoは `tests/` の直下の `.rs` ファイル1つを、独立した実行ファイル1本として組み立てる。実行ファイル1本ごとに、その実行ファイルが使う依存クレートすべてぶんのデバッグ情報が付く。このリポジトリは依存クレートを144個使っているため、Windowsでのデバッグ情報は実行ファイル1本あたり80〜118メガバイトになる。したがってこの1箇所では、実装者がファイルを責務で分けるほど実行ファイルの本数が増え、デバッグ情報の総量も増える。

- **実装者は `tests/` の直下に `.rs` ファイルを置かない。分割の単位はフォルダにする**（`tests/<名前>/main.rs` が `mod` の列で兄弟のファイルを読む形）。実装者は `main.rs` に `mod` の列と共通の段取りだけを持たせ、試験の本体を書かない。この禁止は `cargo xtask conform` が機械で落とす
- **ネットワークやファイルなど外部とのやり取りを持たない、純粋な計算だけの試験は、実装者が `src` の `#[cfg(test)]` へ置く**。そうすると実行ファイルは1本も増えない。実装者は置き先を、その試験が見ている型を持つファイルの `#[cfg(test)] mod tests` とし、そのファイルが100行を超えるなら兄弟の `xxx_tests.rs` を子モジュールとして宣言する
- ファイルの数も1ファイルの行数も変えずに実行ファイルだけを減らす形であるため、1ファイル100行の原則はそのまま適用される。試験のファイルもこの原則の対象である
- **シナリオ（利用者）層とメカニズム（道具・基盤）層の混在禁止（抽象度レイヤーの純化、2026-09-18制定）**: 「何を組み合わせ、どう進行・検証するか」を記述するシナリオ層（ユースケース、画面の流れ、テストケース等）のファイルに、その処理を実現するための道具・構文解析・計算ロジック・固有データ型（DTO）といったメカニズム層を直接書いてはならない。1ファイル100行未満であっても、抽象度レイヤーが異なるコードを同居させることを禁止する。メカニズムは独立した名前を持つモジュール（`syntax_checker.rs` や計算モジュール等）として切り出し、シナリオ層はそれを輸入して宣言的に呼ぶだけに純化する

## 切り出しの根拠義務（パーシャル規約、2026-07-25制定）

Rustは1つの型へ複数の `impl` ブロックを書け、それらを別ファイルへ置ける。この言語機能が存在する理由は、トレイト・ジェネリクス境界・マクロ生成コードという**別の軸**を物理的に分離するためであって、同じ軸の中身が長くなったときに物理的に切るためではない。C#のパーシャルクラスを長さ対策に使うことが一般にアンチパターンとされるのと同じ理由である。

次のいずれかに該当すれば、グローバルの判定の問いを自動的に満たすものとして扱う。該当する根拠はファイル冒頭コメントに書く。

1. **トレイト実装の分離**: 型自身のinherent implと、`Drop`・`Display` 等の外部トレイト実装を分ける。1トレイト実装につき1ファイルを原則とする
2. **ライフサイクル局面の分離**: 生成・実行・破棄など、呼び出し頻度と呼び出しタイミングが明確に異なる局面を分ける。冒頭コメントに「いつ呼ばれるか」を書く
3. **触れるフィールドが限定された操作の分離**: 特定のフィールド群だけに閉じ、呼び出し規律を持つ操作を分ける。冒頭コメントに、触れるフィールドとその不変条件を書く
4. **ジェネリクス境界による条件付き実装**: `impl<T: 境界> 型<T>` は境界ごとに分かれるのが自然なため許可する
5. **呼び出し連鎖の中の独立した工程**: 名前の付く入力と出力を持ち、呼び出し元が中身を知らずに呼べる工程を分ける。冒頭コメントに、担当する工程と受け取るもの・返すものを書く

この5つは例示であって網羅ではない。**該当しなくても問いを満たすなら合法、該当しても満たさないなら違法である。** リストへの機械的な当てはめで判定してはならない。

**切り出した先が親の型を丸ごと(`&親型` / `&mut 親型`)引数に取るなら、上のどの条も根拠にならない**(2026-09-04制定。溶けた概念の分解員が`blitz_app`で検出した形から制定)。それは工程の分離ではなく、親の中身を別ファイルへ置いただけであり、**親のフィールドを1つも減らさない**。`blitz_app`の`app/`配下26ファイルは上の条2・条3・条5のどれかで全部説明でき、各ファイルの冒頭コメントも条文どおりの根拠を書いていたが、`アプリ`のフィールドは46まで増え続けた。条2・条3・条5は「工程を分けたか」を問い、「親のフィールドが減ったか」を問わないためである。

切り出した工程は、**自分が触るものだけを名前の付いた引数で受け取る**。受け取る数が5を超えるなら、その組が名前の無い概念である(束ねてから渡す)。この条は `cargo xtask conform` の機械検査で強制する(現状の違反は台帳へ登録し、増えたら落とす)。

この判定は `impl` の分割に限らず、内部ヘルパーモジュールの切り出し全般に適用する。

### 100行超過の再設計と台帳の現状

100行を超えそうなときの正しい対処は、メソッドを別ファイルへ移すことではなく、**型自体のフィールドと責務を減らす再設計**である。私有フィールドを持つサブ構造体を別モジュールとして切り出し、親はそれを保持して委譲するだけにする。Rustの可視性はモジュール単位で決まり、privateフィールドは定義モジュールとその子孫モジュール全体から見えるため、同一モジュール木の中でファイルを増やしてもアクセス範囲は狭まらない。モジュール木を分けて初めて実効的なアクセス制御が働く。

**2026-09-22時点で100行を超えているのは `xtask/src/main.rs` の1件だけである。** `renderer/draw.rs` を `draw_execute.rs` へ統合した2026-07-25の再監査の時点では1件も無かった。`xtask/src/main.rs` が超えたのは、Rustがクレートの根の `mod` の宣言を1つのファイルへ集めることを要求し、xtaskへコマンドを足すたびにその並びが1行ずつ伸びるためである。

許容の台帳は `xtask/src/conform/line_count_allowance.rs` が持つ。設計は `xtask/src/conform/split_debt.rs` の「台帳 + 陳腐化検査」に倣い、登録されているのに100行以内へ収まったファイルを違反として報告して台帳からの削除を強制する。台帳は統合した結果と宣言的データリテラルの2つの区分の一覧に分かれており、どちらの一覧に載っているかがその行の区分を表す。統合した結果の区分は上限の150行を機械的に落とし、グローバルCLAUDE.mdの条6が別枠にした宣言的データリテラルの区分には上限を置かない。Graphiteのコードはこの台帳を通らない。生成物は行数の検査の対象から外れ、手で書くGraphiteのマクロの本体の行は行数の検査が数えから除く(「ファイル・関数の分割」の節)。

## 型安全性

- `unwrap()` / `expect()` 禁止（テストコードのみ `#[allow(clippy::unwrap_used)]` をモジュール単位で付けて許可）。エラーは thiserror で層ごとに定義した型付きエラーを `?` で伝播する
- 数値の `as` キャスト禁止。`From` / `TryFrom` を使う。縮小変換の失敗は型付きエラーにする（clippy の `as_conversions` deny で機械的に強制）
- 条件分岐はenum（ディスクリミネイテッドユニオン）+ 網羅的 `match` で表現する。`match` のワイルドカード枝 `_` は、網羅を型で保証できない外部由来の値にのみ使う
- プリミティブ執着禁止: ドメイン概念は newtype で包む。private フィールド + 検証付き `生成する()` コンストラクタ + 変換メソッド、の値オブジェクト様式
- **同型プリミティブの多重引数禁止とDTO化（名前付き引数様式、2026-09-18制定）**: 同一型のプリミティブ（`&str` や数値など）が複数並ぶ関数引数は、順序の取り違えバグを誘発するため禁止する。引数DTO（構造体）に束ねて名前付きフィールドとし、判定・処理メソッドを持つ場合は `(引数DTO { フィールド: 値, ... }).メソッド()` の構造体リテラル直接呼び出し様式で記述する（ボイラープレートな単なる生成関数は作らない）
- 数学DDD: 生の数学型（glam::Vec3 / Mat4 等）をドメインAPIに露出しない。座標系フレーム（ワールド/ローカル/ビュー等）と単位（メートル・秒・ラジアン）を幻影型で区別した値オブジェクトで包む。`#[repr(transparent)]` でゼロコストにできるため性能を理由に生型を使うことは認めない。生型に戻すのはGPU境界（blitz_renderのリソース層）のみ
- 「無し・未設定・使用不可」はマジック値（-1、空文字等）でなく `Option` または専用enumで型に表す。無言のデフォルト適用・無言のゼロ値返却は禁止
- **`M不変データ` を実装した型は、構築した後に値が変わらない API として書く（2026-09-25制定）**: 実装者は、既存の値をその場で書き換えず、変更を新しい値で表す。struct の規約と enum の規約は型の形によって異なるため、正本 `_doc/設計/設計オントロジー.md` 2.5.3 を見る

## unsafe の規律

- `unsafe` を書いてよいのは blitz_render の実装内部だけ。blitz_engine / blitz_app は `#![forbid(unsafe_code)]` を維持する（削除禁止）
- blitz_render の公開APIに ash の型（`vk::*`、生ハンドル）を露出しない。公開する型はすべてこのエンジンの語彙で定義した安全な型にする
- すべての `unsafe` ブロックの直前に `// 安全性:` コメントで、そのブロックが健全である根拠（誰がどの不変条件を保証しているか）を書く。根拠を書けない unsafe は設計が誤っている

## 日常の性能規律(判断50)

- 「最適化は計測後」は構造変更を伴う最適化の着手条件であり、遅く書いてよい・手を抜いてよいという意味ではない。最初の1行から性能適合で書く
- 計測不要で悪いと分かる無駄(不要コピー・毎フレーム再確保・不要なGPU同期・明らかに悪い漸近計算量・重複計算)は最初から書かない。全文: `_doc/設計/性能アーキテクチャ方針.md`「日常の性能規律」

## エラー・パニック

- 到達したらプログラムのバグである状態にのみ `panic!` を使い、メッセージに破られた不変条件を書く。回復可能な失敗を panic にしない
- Vulkan の実行時失敗（デバイスロスト等）は型付きエラーとして上位へ伝播し、握り潰さない

## 命名

- ドメイン層（blitz_engine の語彙、blitz_render の公開API）は日本語命名を積極使用する。型名・メソッド名・フィールド名・変数名が対象。Rustは非ASCII識別子を正式サポートしている
- クレート名は ASCII(Cargoの制約)。実装者は、モジュール名・ファイル名・ディレクトリ名を原則日本語にする(2026-09-20制定。issue #165の裁定。詳細は次の小節)
- ash や外部クレートの型・関数はそのままの英語名で扱う。翻訳ラッパーを命名のためだけに作らない
- 略語禁止、フルスペル。関数名はやっていることをそのまま日本語で書く
- **命名の受理条件はグローバルCLAUDE.md「日本語命名の受理条件」(2026-08-09制定)に従う**。要点: 名前は「これは何?」への答えをそのまま書く。動作は5W1Hのうち一意識別に必要な格(最低「何を+どうする」)を含める。英語(ASCII)識別子は読まれないものとして扱い、それによる意味の補完を前提にしない(日本語の型名・レシーバによる補完は可。値オブジェクト様式の関連関数`生成する()`は型名が目的語を担うため合格)。長さは結果であって制約ではない。是正は一括でなく作業のついでに常時行い、検収時に命名チェック員(haiku)+リネーム員(sonnet)で自動対応する
- **日本語命名の質はグローバルCLAUDE.md「日本語命名の質」(2026-07-30制定)に従う**: 英語頭字語(LUT等)を日本語へ混ぜない・直訳調禁止(lookupの「引く」等)・助詞を省いた膠着圧縮禁止(「大気LUT生成指示」でなく「大気のベイク済み画像の生成指示」)。訳語を決めたら`_doc/計画/ユビキタス言語.md`へ登録してから使う。既存の違反はその領域を触る実装のついでに直す(リネームは機能変更と別コミット)。オーナー確定の訳語: LUT→ベイク済み画像、lookup/sampleの「引く」→「参照する」(生成の「焼く」は維持)

### モジュール名・ファイル名・ディレクトリ名の日本語化(2026-09-20制定。issue #165の裁定と2026-09-21の追加裁定。機械検査はissue #178)

本小節は、親セッションが[issue #165](https://github.com/megaDenryu/Blitzdrache0/issues/165)で3案(`use ... as`の別名・`#[path]`と日本語の論理名・ファイル名も日本語)を実機で検証し、オーナーが裁定した内容である。

**守る不変条件は次の1つである。物理のファイルとディレクトリの構造は、Rustの論理のモジュールの構造と同じである。**

- 実装者は、モジュール名・ファイル名・ディレクトリ名を、クレート名を除き原則日本語にする
- この3つとRustの論理のモジュールの構造は、同じ語彙構造を表す。実装者は、物理の配置からモジュールパスを導けるという性質を維持する
- 実装者は、日本語の外部モジュールへ `#[path]` を使う。rustcは非ASCII識別子の外部モジュールを既定のファイルの探索だけでは読まず、E0754で拒むためである
- **実装者は、`#[path]` を物理名と論理名の別名付けに使わない。** `#[path = "接触.rs"] mod 衝突;` は認めない。正規形は、物理のファイル `接触.rs` に対する `#[path = "接触.rs"] mod 接触;` である
- 実装者は、モジュール名を変えるとき、物理のファイル名とディレクトリ名と `#[path]` の表記も合わせて変える
- **`#[path]` の表記は rust-analyzer のF2(名前の変更)では追随しない。** 実装者は手で直した後に `cargo check` で確かめる
- 実装者は、`use ... as` による日本語の別名を、モジュール名の日本語化の手段にしない。親セッションが、findReferences(エディタの「すべての参照を検索」)が別名越しの呼び出し箇所を拾わない実害を確認したためである。ファイルの中で一時的に短く呼ぶ局所の読み替えに限って、実装者は使ってよい
- **Graphiteの生成物の置き場のディレクトリ名 `generated` は、ディレクトリ名を日本語にする規約の例外である**(2026-09-26のオーナー裁定。Issue #187)。Graphiteの生成器と規約が固定する名前であり、実装者が選べる名前ではないためである
- **Graphiteが利用者に書かせる生成物の取り込み `pub mod 経路網 { include!("generated/経路網.rs"); }` は、物理と論理のモジュールの構造が同じであるという不変条件の例外である**(同じ裁定)。この形では論理のモジュール `…::経路網` の物理が `generated/経路網.rs` になり、不変条件と合わない。形はGraphiteが固定するため、実装者は変えられない。この例外が認めるのは、本体が生成物を取り込む `include!` の1文だけの波括弧付きのモジュールであり、取り込む先がGraphiteの生成物であるものに限る。実装者は、この形をGraphiteの生成物以外の取り込みへ広げない
- 実装者は、ディレクトリ名を含む個々の語(例: `contact`→`接触`)を機械翻訳で一括変換しない。ディレクトリ名もモジュールの境界と責務の境界を表す概念の名前であるため、実装者は「新しいドメイン概念と語彙の導入」の対話の手順に従い、裁定待ちユビキタス言語への登録・対話・裁定を1件ずつ通す

**`#[path]` の相対パスは、`mod` を宣言しているファイルが置かれているディレクトリを基準にして探す。** あるクレートの `src/x.rs` の `#[path = "foo.rs"]` が指すのは `src/foo.rs` であり、`src/x/foo.rs` ではない。したがって正規形の前置きは、宣言しているファイルごとに次で決まる。

| `mod` を宣言しているファイル | 正規形の `#[path]` の全体 |
|---|---|
| `lib.rs` / `main.rs` / `mod.rs` | `"<名前>.rs"` または `"<名前>/mod.rs"` |
| `<親>.rs`(`lib`・`main`・`mod` 以外) | `"<親>/<名前>.rs"` または `"<親>/<名前>/mod.rs"` |

**この不変条件は `cargo xtask conform` が機械で確かめる**(issue #178)。検査は `crates` 配下の `src` のすべての `mod` の宣言を抽出し、宣言から読める論理のモジュールパス(宣言元のファイルのモジュールパス + 宣言されたモジュール名)と、対象の物理ファイルから求める物理のモジュールパスが等しいことを見る。この1つの等式が、モジュール名の別名付けと、宣言元の置き場と違う前置きの両方を捕まえる。正本は `xtask/src/conform/design_ontology/module_declaration.rs` と `module_structure_assertion.rs` である。

## 区切られたドメイン(2026-09-20制定。オーナー指示)

区切られたドメインとは、その中で語が1つの意味を持つ範囲のことである。DDDの境界づけられたコンテキストにあたる。

このリポジトリの区切られたドメインは、GitHubの領域ラベルと同じ区分を使う。共通・描画・物理・ゲーム実行・ECS・資産ストリーミング・エディター・開発基盤の8つである。

**1語1義は、区切られたドメインの中で成り立つ規律である。** 区切られたドメインが違えば、同じ表記が別の語でありうる。`腕` は共通ではRustの `arm` の直訳であるため使わないが、物理の区切られたドメインではてこの腕(モーメントアーム)を指す正しい語である。`記録` は共通では `record` の直訳であるため使わないが、描画の区切られたドメインではGPUの計測記録を指す正しい語である。

**語彙の3つの正本はいずれも、区切られたドメインごとに区分を持つ。**

- `_doc/計画/ユビキタス言語.md`(裁定を受けた語): 領域ごとの節が区分を表す
- `_doc/計画/裁定待ちユビキタス言語.md`(裁定をまだ受けていない語): 1件ごとに区切られたドメインを書く
- `_doc/計画/非推奨語.csv`(使わないと裁定した語のうち、このリポジトリに固有のもの): 1行ごとに区切られたドメインを書く

書き手は、どの区切られたドメインの語かを確かめずに、表記だけで一致を判定しない。

## 非推奨語(2026-09-20制定。オーナー指示)

**書き手は、作業の最初に `.claude/global-reference/非推奨語.csv` と `_doc/計画/非推奨語.csv` の2つを必ず読む。** 前者は全リポジトリ共通の一覧(正本は `~/.claude/shared-knowledge/非推奨語.csv`)の複製であり、分野を問わず使わない語を載せる。後者はこのリポジトリに固有の一覧であり、区切られたドメインに固有の語と、このリポジトリの型名に由来する語を載せる(2026-09-26に分けた。規則の全文はグローバルCLAUDE.md「非推奨語の一覧」)。2つのCSVはどちらも、オーナーが使うなと裁定した語の一覧である。列は、区切られたドメイン・非推奨語・かわりに使う語の候補・種別・なぜ駄目か・裁定の出典・その語が正しくなる場面の7つである。

**置き換え先は固定ではない。** 「かわりに使う語の候補」の欄は候補であって対応表ではない。書き手は、その場の文脈で何を指しているかを確かめ、候補の中から選ぶか、候補より適切な語があればそれを使う。候補が複数あることは迷う理由にならない。「枝」はgitのときは `ブランチ`、判別共用体の取りうる値のときは `選択肢`、退避の経路のときは `代わりの捨て方` であり、どれを使うかは実体が決める。

**このCSVは禁止の一覧ではなく、気を付ける語の一覧である。** 書き手は、CSVに載っていない語についても、より良い語が無いかを毎回考える。CSVに無いことは、その語が適切であることの証明ではない。

「その語が正しくなる場面」の欄に書かれた場面では、その語を使ってよい。欄が「無し」なら、区切られたドメインを問わず使わない。

親は、オーナーが新しく語を裁定したら、その場でCSVへ1行を足す。どのリポジトリでも同じ理由で駄目な語なら、親は `~/.claude/shared-knowledge/非推奨語.csv` へ足して `~/.claude` をコミットし、同じ行を `.claude/global-reference/非推奨語.csv` へ写す。区切られたドメインかこのリポジトリの概念に依存する語なら、親は `_doc/計画/非推奨語.csv` へ足す。

## 新しいドメイン概念と語彙の導入(2026-09-20制定。[Issue #154](https://github.com/megaDenryu/Blitzdrache0/issues/154))

新しいドメイン概念と語彙は、オーナーとの対話と裁定を経て初めて採用する。AIが単独で確定してはならない。
この規約が止めたいのは、AIがコードを書く都合で名詞を発明し、そのままユビキタス言語へ登録して既成事実にする流れである。

**対話は必ず行う。切り分けるのは対話の要否ではなく、対話を行う時点である**(2026-09-20のオーナー裁定)。

- **実装の時点**: 実装員が判断してよい。実装員は、既存語では表せない概念に出会ったら、その場で名前を決めてよい。ただし決めた名前は案であり、実装員はその名前を `_doc/計画/裁定待ちユビキタス言語.md` へ登録してから使う。実装員が登録に書くのは、語・暫定の定義・使っている場所・既存語で表せないと考えた理由の4つである。グローバルCLAUDE.md「専門用語の表記」が求める「定義なしで新語を使い始めるのは禁止」は、この登録で満たす
- **レビューの時点**: 親がオーナーへ裁定待ちの語の一覧を出し、対話する。オーナーはここで概念の要否・境界・呼称を裁定する。**親はPRの前のチャットとPR上のどちらで対話するかを、案件ごとに選ぶ**(2026-09-20のオーナー裁定)。親は、新語が1件か2件ならPR上で差分と並べて裁定を受け、概念の境界そのものを問う重いものならPRを立てる前にチャットで対話する
- **裁定の後**: 親は、採用ならユビキタス言語へ定義を移し、必要なリネームを行う。親は、却下なら既存語へ戻すか、概念そのものを畳む。親はリネームを機能変更と別のコミットにする

**ユビキタス言語への登録は承認ではない。** `_doc/計画/ユビキタス言語.md` は対話で合意した語彙を記録する正本であり、登録という行為が語の妥当性を証明することはない。親と実装員は、裁定を受けていない語をこの正本へ書いてはならない。

実装員は、新しい名詞型・列挙・マーカー・役割名を導入するとき、**それが既存のドメイン概念の実装なのか、新しい概念の導入なのか**を必ず判定する。実装員は、後者ならこの手順を通す。親と実装員は、型が存在することを概念の必要性の根拠にしてはならない。

親は、PRの本文で未裁定の新語を確定しない。裁定の後のPRが行うのは、issueに記録された裁定の反映だけである。

## コメント

- コードから読み取れない情報のみ書く: 設計判断の理由・不変条件・地雷・外部参照
- 許容タグは4種: `注意:`（触ると壊れる不変条件）、`前提:`（外部で保証される条件）、`参照:`（他ファイル/文書への案内）、`安全性:`（unsafeの健全性根拠、Rust固有）
- タグ以外はプレフィックス無しの日本語散文。「〜のため〜する」の因果構造で書く
- コメントを横幅揃えのために文の途中で改行しない。1行にできる文は1行で書く（行長制限は採用しない。参照: グローバルCLAUDE.md「文章規範」）
- 構造体のフィールド・enumの枝・constの列の間へコメントだけの行を挟まない（グローバルCLAUDE.md「コメント」の条文。説明は同じ行の末尾に書く）。Rustは説明の注釈 `///` を宣言の前にしか置けないため、1宣言につき1行の `///` だけを例外として認める（グローバルCLAUDE.md「コメント」の2026-08-27の裁定）。2行目からは違反であり、収まらない説明は型自身の `///` へ移す。private のフィールドは外部向け文書の対象ではないため `///` を付けず、説明は行末に書く。`#![warn(missing_docs)]` を敷いたクレートの公開する列挙の枝には1行の `///` を書く
- 同じ行の末尾に収まらない説明は、そのフィールド固有の説明ではない。newtype で名前に意味を持たせるか、型の冒頭の `//!` あるいは型自身の `///` へ移す
- 行末コメントの手前に入る桁揃えの空白は rustfmt が入れるものであり、手で詰めた空白の禁止には当たらない。`cargo fmt --check` が検証列の一段目にあるため、1スペースへ潰しても次の整形で戻る。潰す作業をしない
- enumの枝の開き括弧の直後（`枝名 { // 説明`）へ行末コメントを置かない。rustfmt がそれを単独行のコメントへ押し戻し、宣言の間のコメント行として検査に落ちる。複数のフィールドを持つ枝の説明は、型の直上の `///` へ移すか、閉じ括弧（`},`）の後ろへ付ける
- 禁止: 変更履歴コメント、書き手情報、TODO/FIXME（課題は開発スレッドか計画書へ）
- Vulkan固有の落とし穴（同期・ライフタイム・アラインメント）は `注意:` の主対象。仕様書の該当箇所が根拠なら `参照:` でリンクする

## 依存の設計

- コンストラクタ注入 + コンポジションルート（blitz_app）へ配線を集約。グローバル可変状態（`static mut`、`lazy_static` での可変シングルトン）禁止
- **コンポジションルートは多階層にする。1つの型が全部を配線するのは、この規約を満たしていない**（2026-09-04制定。グローバルCLAUDE.md「設計」の同条を見る）。`アプリ`が46フィールドを直接所有するまで育ったのは、この条文を「1つの型へ集約せよ」と読んだためである。上位の階層は下位の階層を組み立てるだけにし、下位の階層の中身は知らない。階層を切る基準は責務・寿命・時間意味・生成破棄条件である（是正の工事はIssue #45・#31）
- **操作サービスの依存保持とコールバックの限定はグローバルCLAUDE.mdの「サービスの依存保持とコールバックの限定」(2026-08-09制定)に従う**。要点: 依存を使って仕事をする型は依存をコンストラクタ注入で保持しメソッドで公開する（呼び出し側に論理デバイスを運ばせない）。その場で1回実行されるだけの括りはコールバックで挟まずセッション型で表す。用語の定義は `_doc/計画/ユビキタス言語.md` の操作サービス・セッション型・資源型を見る
- **処理側に所有すべき状態が無いなら、処理側の型を作らない**（2026-09-04制定。issue #56の吟味から）。設定・再利用する作業領域・前回値・統計のような、処理自身が持つべき状態が無いのに`struct`を作り、名前空間の代わりに使うことを禁じる。ただし**これは「メソッドにするな」という意味ではない**。処理側の型を作らないことと、対象の型のメソッドにすることは両立する。自由関数を選んでよいのは、グローバルCLAUDE.md「Rustの書き方の選択」条2の(a)(b)に当たるときだけである
- **モジュールに分けても依存の向きは決まらない**（2026-09-04制定。issue #56の「Rustではmoduleが名前空間とvisibilityを担うため自由関数は無秩序なグローバル関数を意味しない」に対する実測からの限定）。`blitz_app`は`app/`配下26ファイルへモジュールが分かれているが、`&mut アプリ`を丸ごと受ける自由関数が21本ある。**モジュールは名前空間を作るが、向きは作らない。** 向きは、上の「切り出しの根拠義務」が禁じた「親を丸ごと受け取る形」を避け、工程が自分の触るものだけを引数で受けることで初めて生まれる
- **常に揃って渡される引数の組は、名前の無い概念である**（2026-09-04制定。溶けた概念の分解員が`blitz_app`で検出した形から制定）。同じ2つ以上の値が、複数の呼び出しへ毎回一緒に渡されているなら、それらは1つの概念であり、呼び出し側に束ねさせずに1つの値へ合成する。特に、2つの台帳が同じ識別子で対になり、追加も解除も必ず両方へ行う形は、「呼び出し側が2回呼ぶのを忘れない」という規律に依存しており、片方だけ呼ぶ取りこぼしを型が止めない
- ドメイン層でのトレイト継承的な設計の乱用禁止。合成（フィールド保持）を優先する
- 依存クレートの追加基準は `_doc/計画/開発計画.md` に従い、追加時はREADMEの利用ライブラリ表に採用理由を追記する

## 機械的強制（変更禁止の設定）

- ワークスペース `Cargo.toml` の `[workspace.lints]` で unwrap_used / expect_used / as_conversions を deny 済み。緩和する変更は禁止（テストモジュールでの局所allowのみ可）
- `cargo xtask conform` が規約の機械検査（.rs/.slangの100行（数えるのはコードの行だけであり、空行とコメントだけの行は数えない）・構造体と列挙の宣言ブロックの中にコメントだけの行が無いこと（説明の注釈`///`は宣言の前にしか置けないため1宣言につき1行を認め、2行目から違反にする）・禁止文字列・切り出しの経緯語・テスト外allow・クレート依存の白リスト・参照パス実在・文書内の節参照実在・正本と写しで同じ値を持つべき定数の一致（CPU正本とslang写しに加えてクレート同士の対も含む）・CPU正本の束縛番号とシェーダーの宣言に付いた束縛の属性の一致・アプリが出す報告の行の文言と検収が読む文言の契約・ファイル名らしい文字列リテラルが2つ以上のファイルに書かれていないこと・blitz_renderのvulkan配下とrenderer配下でのDrop実装禁止（`renderer/mod.rs`の単一RAIIオーナーのみ例外）・blitz_simの剛体のモジュール群（`rigid_body/`・`rigid_xpbd/`）での単精度の3つ組`[f32; 3]`の宣言禁止（剛体の状態と接触の判断5）・二段の位置の和を単精度へ畳む口`境界用に畳んだ位置`の呼び出しがGPU境界と`配置`の描画の変換の生成以外のファイルに現れないことと、粗い位置の読み口`基準原点を選ぶための粗い位置`の呼び出しが型の定義と衝突の問い合わせの基準原点のモジュール(`contact/query_origin/`)以外のファイルに現れないこと（剛体の状態と接触の判断3の2026-09-08と2026-09-09の実装の細目））。依存を追加するときは採用審査 + conform の白リスト更新が必須（Cargo.toml 変更だけでは通らない意図的な二重台帳）。検査を緩める変更は規約改訂（本ファイルの改訂）とセットでのみ許す
- 検証の標準列: `cargo xtask verify`（conform → graphite-check → fmt --check → check → clippy -D warnings → test）。graphite-check は、Graphiteに依存するパッケージごとに `cargo graphite generate --check` を走らせ、生成物の手編集と宣言の消えた生成物を捕まえる(Issue #187)。graphite-check は、conform が生成物と認めたファイルを持つパッケージが照合の対象に全部入っていることと、入れた生成器が Cargo.lock の固定した graphite のコミットから作られていることを先に確かめる。生成器 `cargo-graphite` は `cargo install --git https://github.com/megaDenryu/Graphite --rev <Cargo.lock の graphite の source の # の後ろのコミット> graphite-cli --force` で入れる(版が食い違うと graphite-check が実際のコミットを埋めたこのコマンドを表示する)。生成器は自分の版を答える引数を持たないため、graphite-check は cargo が入れた道具の記録(`<cargoの置き場>/.crates.toml`)の出どころのコミットで確かめる。パスから入れた生成器はコミットが記録に残らないため食い違いとして落ちるこの列が通らないコードはコミットしない
- `cargo xtask type-metrics` は型ごとの宣言の件数(構造体はフィールド数、列挙は枝数)・impl分散ファイル数・メソッド数を計測する。構造体と列挙で数える対象を分けるのは、列挙の枝が持つ文脈情報まで合算すると、型付きエラーへ破られた不変条件を書くという規約に従うほど数が増え、責務の集中とは逆向きの指標になるためである(2026-09-04是正)。台帳の1行も生成側の名前(`構造体`か`列挙`)で種別を言う。**この計測が1つの型を識別する鍵は、定義ファイルのリポジトリからの相対パスと型名の組である**(2026-09-05制定)。名前だけを鍵にすると、同じ名前の型が別の場所にあるとき大きい型が小さい型に隠れて検査に現れず、違反の報告が指すファイルも誤る。**現状の値を上限として台帳へ登録し、増えたら落とす**（2026-09-04改訂）。旧の但し書き「閾値による deny は型の分解が完了した後に入れる」は撤回した。**分解が完了しない限り閾値が入らず、閾値が無いから分解されないという循環**を作っており、その循環のあいだに `アプリ` が46フィールドまで育ったためである。台帳の様式は `xtask/src/conform/split_debt.rs` の「台帳 + 陳腐化検査」に倣い、**登録した値より減ったときは台帳の更新を強制する**(減る方向の力を仕組みで用意する)

## 節目の完了時の定点（省略禁止）

節目の完了は DoD 充足だけでは成立しない。`_doc/計画/開発計画.md`「節目の完了時の定点」の5項目（評価軸の予算表更新とギャップ検出 / 性能アーキテクチャ方針9軸の該当節更新（触れないなら開発スレッドに1行明記）/ 生存型文書の実装一致確認 / 扉の定点 / 外部台帳（jimbo札#137・#138）の棚卸し）を完了報告の一部として実行する。
`参照: _doc/計画/評価軸.md`、`_doc/設計/性能アーキテクチャ方針.md`

## 禁止事項（グローバル憲法から再掲）

- 文字列・UIテキスト・ログ・ドキュメント中の絵文字は絶対禁止
- タスクと無関係なフォーマット変更・リファクタリング禁止（必要なら承認を求める）

## ツールとドキュメントの配置

原則はグローバルCLAUDE.md「ツールとドキュメントの配置規約」に従う。
**登録なきツール作成・登録なき文書作成は禁止**（後続セッションが発見できないものは存在しないのと同じ）。

- **ツールの唯一の入口: `cargo xtask`**（引数なしで全コマンド一覧を表示）。ビルド・実行・検証・生成のツールはすべて xtask クレートにコマンドとして登録する。シェルスクリプトを散らさない。検証は `cargo xtask verify`
- **文書の固定ルート: `_doc/`**。分類:
  - `_doc/計画/`（生存型: 開発計画・ユビキタス言語。常に実装・現状と一致させる）
  - `_doc/設計/`（生存型: 設計文書。実装とずれたら実装に合わせて直す）
  - `_doc/開発スレッド/`（ログ型: 逐語収録・追記のみ。dev-thread-gen スキルで生成可）
  - `_doc/検査報告/`（ログ型: ローラー検査で走らせた検査員の報告全文。1報告1ファイル、`<領域>_<軸>_YYYY-MM-DD.md`。台帳の1行は要約であり、是正の工事はそこからは書けないため全文を残す。手順は `.claude/skills/ローラー検査/SKILL.md`）
- **生存型文書の索引は README のドキュメント節**。新しい生存型文書は索引への登録までが作成
- **検証の出力の置き場は `検証の出力ルート` が決める**（2026-09-05制定。[Issue #61](https://github.com/megaDenryu/Blitzdrache0/issues/61)）。検証・検収・計測が書き出すもの（絵・生値・報告・焼いた実行時形式・一時コピー）は、ビルドの出力の木の直下へ入口が自分でパスを綴って置くのではなく、`検証の出力ルート`（正本は `xtask/src/verify/output/root.rs`。`参照: _doc/計画/ユビキタス言語.md`「検証の出力ルート」）が導く場所へ置く。入口が宣言するのは置き場の名前（`検証の出力の置き場名`）と、要ればその直下のファイル名（`検証の出力のファイル名`）だけであり、パスはこの型のメソッドが組む。実装者が木の名前を `xtask/src/verify/output/root.rs` 以外の場所へ綴ると、`cargo xtask conform` が違反として落とす。溜まった中間データは、開発者が `cargo xtask clean-build-cache` を実行して消す（`--dry-run` を付けると、開発者は何も消さずに一覧だけを見られる。このコマンドは、実行した開発者がいま使っている出力先を消さない）
