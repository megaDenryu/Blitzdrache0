//! 経路の開閉の状態を作るための指定。閉じている経路と出来事が触れた経路の2つの並びを、名前付きの公開フィールドで持つ。
//! 状態へは `経路の開閉の規則::指定を地図で確かめて状態を作る` だけが変える。2つの並びを同じ型の2引数で受けないのは、取り違えても型が通るためである。
//! 出来事が触れた経路も受け取るのは、保存からの復元のように途中の状態を作り直すとき、出来事が予定に勝つという裁定の効果を黙って消さないためである。

use blitz_design::M不変データ;

use super::識別子::経路の識別子;

/// 経路の開閉の状態を作るための指定。検証は規則が自分の地図で行う。
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct 経路の開閉の状態の指定 {
    pub 閉じている経路: Vec<経路の識別子>,
    pub 出来事が触れた経路: Vec<経路の識別子>,
}

impl M不変データ for 経路の開閉の状態の指定 {}
