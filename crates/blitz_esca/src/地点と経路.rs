//! ゲーム『Esca』の第2段階「地点と経路の表現」のうち、地点と経路のつながりと、経路の開閉(Issue #187 の作業G1)。
//!
//! つながりは、ステージを作る時点で通れる可能性のある経路を全部置いた、作った後に変わらない地図(`地点と経路のグラフ`)として持つ。
//! どの経路がいま閉じているかは、地図と別の状態(`経路の開閉の状態`)として持ち、開閉の遷移がその新しい値を返す。
//! 開閉のきっかけは、出来事から作られる開閉の命令と、遷移パラメータの時刻に来た予定の2つである。
//! 区域(歩いている位置からいまいる地点を判定する範囲)と歩行との接続は、次の作業(G2)で足す。
//! 参照: `_doc/設計/Esca/設計正本.md` 5節。

#[path = "地点と経路/図式.rs"]
mod 図式;
#[path = "地点と経路/地図.rs"]
mod 地図;
#[path = "地点と経路/指定の失敗.rs"]
mod 指定の失敗;
#[path = "地点と経路/時間帯.rs"]
mod 時間帯;
#[path = "地点と経路/識別子.rs"]
mod 識別子;
#[path = "地点と経路/開閉の予定.rs"]
mod 開閉の予定;
#[path = "地点と経路/開閉の出来事.rs"]
mod 開閉の出来事;
#[path = "地点と経路/開閉の命令.rs"]
mod 開閉の命令;
#[path = "地点と経路/開閉の状態.rs"]
mod 開閉の状態;
#[path = "地点と経路/開閉の規則.rs"]
mod 開閉の規則;

pub use 図式::{地点, 地点と経路の図式};
pub use 地図::{地点と経路のグラフ, 経路の両端};
pub use 指定の失敗::経路の指定の失敗;
pub use 時間帯::{閉じている時間帯, 閉じている時間帯の指定, 閉じている時間帯の生成の失敗};
pub use 識別子::{地点の識別子, 経路の識別子};
pub use 開閉の予定::{経路の開閉, 経路の開閉の予定, 経路の開閉の予定の生成の失敗};
pub use 開閉の出来事::経路の開閉の出来事;
pub use 開閉の命令::経路の開閉の命令;
pub use 開閉の状態::経路の開閉の状態;
pub use 開閉の規則::経路の開閉の規則;
