//! グラフィックスパスのクリア指定。カラー・深度の両アタッチメントで共通の
//! loadOpを決める(判別共用体): 新規描画は`クリアする`、既存内容へ追記する
//! パス(粒子描画等)は`ロードする`(深度クリア値は`クリアする`時のみ1.0/0固定で使う)。

use crate::clear_color::クリアカラー;

pub(crate) enum クリア指定 {
    クリアする { カラー: クリアカラー },
    ロードする,
}
