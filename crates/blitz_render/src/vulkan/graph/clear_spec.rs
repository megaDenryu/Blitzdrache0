//! グラフィックスパスのクリア指定。波1は色のみ可変（深度クリアは1.0/0固定で
//! 実行器側が扱う。現行動作を維持するだけのため設定項目にしない）。

use crate::clear_color::クリアカラー;

pub(crate) struct クリア指定 {
    pub(crate) カラー: クリアカラー,
}

impl クリア指定 {
    pub(crate) fn 生成する(カラー: クリアカラー) -> Self {
        Self { カラー }
    }
}
