//! バッファ用途enum。画像用途と対になる型。
//!
//! 参照: `_doc/設計/レンダーグラフ.md`。波1はバッファを使うパスが無いため未使用。
//! 波2のGPU粒子トイでコンピュート読み書きを使う。
#[allow(dead_code)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub(crate) enum バッファ用途 {
    頂点読み,
    インデックス読み,
    ユニフォーム読み,
    コンピュート読み,
    コンピュート書き,
    転送元,
    転送先,
}
