//! シーン描画のパイプラインが宣言する照明問い合わせのセット(set3)の契約。担当するのは、実在するレイアウトと
//! シェーダー関数の組を型の枝として表すことである。
//!
//! 生の能力ビットや空のtagを先行させないのは、枝が1つしかない段階でそれを作ると、GPU側にもCPU側にも
//! 意味の無い分岐が残るためである。順3のIBLや順5のクラスタが入るときに枝を足す
//! (参照: `_doc/設計/GPU資源束縛の分離と索引化.md`「照明問い合わせ資源のGPU境界」)。

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub(crate) enum 照明問い合わせ契約 {
    /// ヘッダ・方向光レコード列・局所光レコード列・多段影の1組を読む現行の契約。
    直接光と多段影,
}
