//! 間接照明の供給元の判別。担当するのは「間接光をどの表現から得るか」の1点である。
//!
//! 現行の環境光係数は方向も位置も問わない旧来の近似であり、物理的な放射輝度場の問い合わせではない。
//! 順3で遠方環境の問い合わせ資源の枝を足すため、係数1つの構造体でなく枝として持つ
//! (参照: `_doc/設計/放射輝度問い合わせ階層.md`「現行ライティング入力の改鋳(段1で行う)」)。

use crate::lighting_input::環境光係数;

pub(crate) enum 間接照明入力 {
    定数近似(環境光係数),
}
