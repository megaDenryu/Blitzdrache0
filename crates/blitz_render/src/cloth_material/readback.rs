//! GPUから写し取った布の粒子の位置とラグランジュ乗数。CPUの参照計算との突き合わせ(Issue #36の検証)だけが読む。
//! 生の単精度で返すのは、検査が非有限値や発散を観測できなければならないためである。

/// 並びは素材の粒子の並びと、彩色で並べ替えた拘束の並びそのものである。
#[derive(Debug, Clone, PartialEq)]
pub struct 布の読み戻し {
    pub 位置一覧: Vec<[f32; 3]>,
    pub ラグランジュ乗数一覧: Vec<f32>,
}
