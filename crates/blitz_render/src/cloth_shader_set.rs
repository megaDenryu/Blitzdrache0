//! 布シミュレーションのシェーダー束(判断54)。コンピュート9本と、シーンパス・シャドウパスの描画2組。
//! 各フィールドの検証は`コンピュートシェーダー`/`シェーダー一式`の生成時に済んでいる。

use crate::compute_shader::コンピュートシェーダー;
use crate::indirect_lighting::契約別の描画シェーダー;
use crate::shader_set::シェーダー一式;

#[derive(Debug, Clone)]
pub struct 布シェーダー一式 {
    pub 介入: コンピュートシェーダー,
    pub 積分: コンピュートシェーダー,
    pub アタッチ: コンピュートシェーダー,
    pub 拘束: コンピュートシェーダー,
    pub ハッシュ消去: コンピュートシェーダー,
    pub ハッシュ格納: コンピュートシェーダー,
    pub 分離: コンピュートシェーダー,
    pub 仕上げ: コンピュートシェーダー,
    pub 頂点生成: コンピュートシェーダー,
    /// シーンパスで布を描く画素段は照明問い合わせ契約ごとに分かれる。頂点段は両方が同じものを持つ。
    pub 描画: 契約別の描画シェーダー,
    /// シャドウパスで布だけを描く専用のシェーダー。個体変換も可視ID列も読まないため、布の影は描画対象の走査順に依らない。
    pub シャドウ: シェーダー一式,
}
