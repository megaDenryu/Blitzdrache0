//! 布シミュレーションのシェーダー束(判断54)。コンピュート13本と、シーンパス・シャドウパスの描画2組。
//! 各フィールドの検証は`コンピュートシェーダー`/`シェーダー一式`の生成時に済んでいる。

use crate::compute_shader::コンピュートシェーダー;
use crate::indirect_lighting::契約別の描画シェーダー;
use crate::shader_set::シェーダー一式;

/// `乗数零化`は物理刻みの開始でラグランジュ乗数を零へ戻し、`拘束`は色ごとに1回積まれて正典式で位置と乗数を直接書く。
/// `目標の確定`は反復の前にスキン済み頂点の位置を目標位置へ写し、`曲げ拘束`は距離拘束の色の後ろで隣接する三角形の二面角を静止角へ保ち(判断11)、
/// `目標拘束`はその後ろで固定・アタッチ・掴みを目標拘束として解く(判断6)。
/// `床とカプセルの押し出し`は分離の後に床とカプセルの内側の粒子を表面へ出し、`仕上げ`は目標拘束の最終の成立の後に速度を作り直す。
/// `描画`はシーンパスで布を描くもので、画素段は照明問い合わせ契約ごとに分かれ、頂点段は両方が同じものを持つ。
/// `シャドウ`は個体変換も可視ID列も読まないため、布の影は描画対象の走査順に依らない。
#[derive(Debug, Clone)]
pub struct 布シェーダー一式 {
    pub 介入: コンピュートシェーダー,
    pub 積分: コンピュートシェーダー,
    pub 目標の確定: コンピュートシェーダー,
    pub 乗数零化: コンピュートシェーダー,
    pub 拘束: コンピュートシェーダー,
    pub 曲げ拘束: コンピュートシェーダー,
    pub 目標拘束: コンピュートシェーダー,
    pub ハッシュ消去: コンピュートシェーダー,
    pub ハッシュ格納: コンピュートシェーダー,
    pub 分離: コンピュートシェーダー,
    pub 床とカプセルの押し出し: コンピュートシェーダー,
    pub 仕上げ: コンピュートシェーダー,
    pub 頂点生成: コンピュートシェーダー,
    pub 描画: 契約別の描画シェーダー, // シーンパスで布を描く
    pub シャドウ: シェーダー一式,     // シャドウパスで布だけを描く専用のもの
}
