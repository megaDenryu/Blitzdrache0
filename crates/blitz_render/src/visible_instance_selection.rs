//! そのフレームに描く個体の並びと、その並びをLOD段ごとの範囲へ切り分けた区間。可視判定と段選択はエンジン側の
//! 純ロジックが行い、レンダラーは結果だけを受け取る。選択を持たない描画対象は全個体を最詳細段で描く。
//!
//! 1本の可視ID列は群の全個体を段ごとの連続範囲へ並べ、各段の範囲の先頭側にシーンパスが描く可視個体を置く。
//! シーンパスは段の範囲の先頭から可視数ぶんを描き、シャドウパスは同じ範囲の全体を描く。
//! **不変条件: シャドウパスが描く個体は常にその段の全個体である。** カメラ視錐台で絞った可視集合をシャドウパスへ
//! 流用してはならず、この不変条件は「段別個体数の合計が列の長さに等しく、列の長さが対象の個体数と一致する」ことを
//! 生成と書き込みの地点で検査して守る(参照: `_doc/設計/植生インスタンスと物量計測.md`「可視判定」)。
//! 選択1件の形は`selection`、選択の一覧と検査は`list`、段の範囲は`stage_range`にある。

mod error;
mod list;
mod partition_check;
mod selection;
mod stage_range;

pub use error::可視ID列エラー;
pub use list::可視個体選択一覧;
pub use selection::可視個体選択;
pub use stage_range::段別描画範囲;
