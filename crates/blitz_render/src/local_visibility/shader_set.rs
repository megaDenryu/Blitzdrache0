//! 局所可視性補正の2つのコンピュートエントリのSPIR-V。触れる状態を持たず、担うのは2本を1つの束で運ぶことだけである。
//!
//! 2本を1つの束にするのは、遮蔽の標本化とぼかしが必ず対で積まれるためである。片方だけを差し替えられる状態を作ると、
//! 生の可視度を書く側と読む側が別の版になり、標本の回転の均され方が食い違う。

use crate::compute_shader::コンピュートシェーダー;

/// 局所可視性補正のシェーダー一式。拡散間接方式は実行時に世界が宣言するため、方式に関わらず常に受け取る。
#[derive(Debug, Clone)]
pub struct 局所可視性のシェーダー一式 {
    pub 遮蔽の標本化: コンピュートシェーダー,
    pub 両側ぼかし: コンピュートシェーダー,
}
