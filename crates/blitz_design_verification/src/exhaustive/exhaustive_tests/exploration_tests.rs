//! 領域を探索して遷移モデルを組む工程を固定する試験。
//!
//! 辿りそのもの(初期状態から遷移を組むこと・上限で打ち切ること・失敗した遷移・初期状態の数え上げ)を `traversal_tests` が、
//! 領域が答えた状態の同一性の重複の検出を `状態の同一性の重複` が、名指しの重複の検出を `名指しの重複` が見る。
//! 後者2つを分けるのは、状態の同一性の写し方を選べる `歩数を動かす領域` と、名指しの選び方を選べる
//! `名指しを選ぶ領域` という別のfixtureをそれぞれ主に使うためである。
//! 網羅性を黙って全件と名乗らせないことがどの試験の目的でもある。

mod traversal_tests;
#[path = "exploration_tests/名指しの重複.rs"]
mod 名指しの重複;
#[path = "exploration_tests/状態の同一性の重複.rs"]
mod 状態の同一性の重複;
