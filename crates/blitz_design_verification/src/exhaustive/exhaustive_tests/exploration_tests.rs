//! 領域を探索して遷移モデルを組む工程を固定する試験。
//!
//! 辿りそのもの(初期状態から遷移を組むこと・上限で打ち切ること・失敗した遷移・初期状態の数え上げ)を `traversal_tests` が、
//! 領域が答えた同一性と名指しの畳みの検出を `collapse_tests` が見る。2つに分けるのは、前者が探索の進み方を、
//! 後者が領域が嘘をついたときの振る舞いを固定するためである。
//! 網羅性を黙って全件と名乗らせないことがどちらの試験の目的でもある。

mod collapse_tests;
mod traversal_tests;
