//! 領域を探索して遷移モデルを組む工程を固定する試験。
//!
//! 辿りそのもの(初期状態から遷移を組むこと・上限で打ち切ること・失敗した遷移・初期状態の数え上げ)を `traversal_tests` が、
//! 領域が答えた、異なる状態に同じ同一性を与えたことの検出を `異なる状態に同じ同一性を与えた` が、
//! 異なる値に同じ名指しを与えたことの検出を `異なる値に同じ名指しを与えた` が見る。
//! 後者2つを分けるのは、状態の同一性の写し方を選べる `歩数を動かす領域` と、名指しの選び方を選べる
//! `名指しを選ぶ領域` という別のfixtureをそれぞれ主に使うためである。
//! 失敗した遷移が元の状態を書き換えて返したことの検出を `元の状態を書き換えた遷移の検出` が見る。
//! 網羅性を黙って全件と名乗らせないことがどの試験の目的でもある。

mod traversal_tests;
#[path = "exploration_tests/元の状態を書き換えた遷移の検出.rs"]
mod 元の状態を書き換えた遷移の検出;
#[path = "exploration_tests/異なる値に同じ名指しを与えた.rs"]
mod 異なる値に同じ名指しを与えた;
#[path = "exploration_tests/異なる状態に同じ同一性を与えた.rs"]
mod 異なる状態に同じ同一性を与えた;
