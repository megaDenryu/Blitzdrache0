//! 刻み結果: 世界実行が1刻み進めた結果として返す判別共用体。ゲームの終了要求を持ち、世界の内部の終了を外から覗かせずに型で返す。
//! イベントループを閉じるかどうかは`アプリ`がこの値だけで決める。

/// 1刻み進めた結果、イベントループを閉じるべきかどうか。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub(crate) enum 刻み結果 {
    続ける,
    終了する,
}
