//! ゲーム用高さ場: 実行時アセット一式の検査の段階でカタログから読み、全検査の成功の後に世界実行へ据える高さ場。
//! 遊ばない起動ではカタログを読まない。読む世界と据える世界を型で分けるのは、検査で読んだ結果を据える前に捨てないためである。

use blitz_engine::height_field::高さ場の読み口;

pub(crate) enum ゲーム用高さ場 {
    遊ばない世界,
    読み込んだ(高さ場の読み口),
}
