//! ピクセル判定で使う期待色の定数群。座標・許容誤差・各シナリオでの中央期待色。

pub(super) const 許容誤差: f32 = 8.0;
pub(super) const 隅座標: (u32, u32) = (5, 5);
pub(super) const クリアカラー線形値: [f32; 3] = [0.05, 0.07, 0.12];
/// quad_base_color.png(純青)のTINT=(1,1,1)適用結果。
pub(super) const 初期中央色線形値: [f32; 3] = [0.0, 0.0, 1.0];
/// quad_alt_color.png(純緑)へ書き換えた後の中央色。
pub(super) const アセット反映後中央色線形値: [f32; 3] = [0.0, 1.0, 0.0];
/// TINTを(0.1, 0.9, 0.2)へ書き換えた後の中央色(成分積)。
pub(super) const 書き換え後中央色線形値: [f32; 3] = [0.0, 0.9, 0.0];
