//! `--frames`スモークモードの自己操作・ホットリロード検証・ピクセル判定シナリオ。
//! DoDの「標準サンプルが表示される」「アセット変更が反映される」
//! 「リサイズ・最小化で落ちない」をウィンドウの自己操作とピクセル読み戻しで機械検証する。
//! 参照: `_doc/開発スレッド/開発スレッド_2026-07-20_M0実装.md`「判断9」「判断22」。

mod action_demand;
mod execution;
mod material_reload;
mod pixel_judgment;
mod plan;
mod verification_plan;
mod world_plan;

pub(crate) use execution::スモーク実行;
pub(crate) use pixel_judgment::{アニメーション差分を判定する, ピクセルを判定する};

/// フレーム番号に応じて、このフレームで行う自己操作・検証を表す。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub(crate) enum スモークアクション {
    通常描画,
    リサイズ,
    最小化,
    復帰,
    シェーダー書き換え,
    アセット書き換え,
    差し替え前ダンプ, // 材質とシェーダーの差し替えステージ: このフレームの読み戻し画像を差し替え前の絵として別名で書き出す
    初期色判定,
    アセット反映後判定,
    最終判定,
    ヘルメット判定,
    粒子判定,
    開発UI判定,
    シャドウ判定,
    複数描画対象判定,
    フォックス基準保存, // foxステージ: このフレームの読み戻し画像を差分判定の基準として保存する(判断45)
    フォックス差分判定, // foxステージ: 保存済み基準とこのフレームの読み戻し画像を比較し、アニメーションで絵が動いたことを判定する
}
