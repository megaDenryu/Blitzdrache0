//! ゲーム更新の1刻みが何秒ぶんかを答える供給元。担当するのは「1刻みの長さを1つ答える」ことだけであり、
//! ゲームの状態も進行段階も実行の種類も知らない。
//!
//! 注意: 1刻みは描画フレーム1回ぶんの固定の長さであり、実行の種類で分かれない。無期限実行でも実時間を1度も読まないため、
//! 同じフレーム数を進めた実行は必ず同じ状態で終わる。垂直同期の60フレーム毎秒では固定の刻みが実時間とほぼ一致し、
//! アニメーション時刻の固定歩進と同じ値であるため足の滑りも出ない。フレームが落ちたときにゲームを実時間へ追従させる
//! 要求が立った時点で、パターンBへの移行と累積器を同時に設計する。
//! 参照: `_doc/設計/ゲーム制作アーキテクチャ.md`「判断1」。

use blitz_math::秒;

/// 描画フレーム1回ぶんの刻み。毎秒60フレーム相当であり、アニメーション時刻の固定歩進(`app/frame.rs`)と同じ値である。
const 一刻みの秒数: f32 = 1.0 / 60.0;

pub(super) fn ゲーム更新の一刻みの秒() -> 秒 {
    秒::生成する(一刻みの秒数)
}
