//! 起動時に受け取った要求。ウィンドウとレンダラーを作るかどうかがここで分かれるため、
//! 描画しない要求を起動設定のフラグにせず、要求そのものを判別共用体で表す。

use super::起動設定;

pub(crate) enum 起動要求 {
    /// ウィンドウを作って描画する通常の起動。
    描画実行(Box<起動設定>),
    /// 描画せず、代表時刻の天空状態表を標準出力へ出して終える。
    天空状態報告,
    /// ウィンドウを作らず、ヘッドレスのGPUで大気LUTを焼いて読み戻した表を標準出力へ出して終える。
    大気lut報告,
}
