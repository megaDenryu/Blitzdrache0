//! 世界が宣言する拡散間接方式。担当するのは「その世界の拡散間接照度へ局所可視性補正を掛けるかどうか」の1つの宣言だけである。
//!
//! 局所可視度そのものの数学は描画側の語彙(深度画像・画素の位置・透視投影の逆変換)で書かれるためblitz_renderが持つ。
//! この層が持つのは世界の宣言だけであり、深度バッファも画面の画素も知らない。
//! 参照: `_doc/設計/放射輝度問い合わせ階層.md`「IIa(SSAO)の設計」

mod diffuse_indirect_method;

pub use diffuse_indirect_method::拡散間接方式;
