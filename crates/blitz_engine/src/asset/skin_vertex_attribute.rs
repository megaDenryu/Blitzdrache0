//! スキン頂点属性: 1頂点が影響を受けるジョイント添字とその重み(JOINTS_0/WEIGHTS_0)。

/// `ジョイント`はスキンの`ジョイント一覧`内の添字(トポロジカル順に引き直し済み)。
/// `ウェイト`は対応する影響度。glTF仕様準拠アセットを信頼し、合計が1に近いことは検証しない
/// (判断42)。
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct スキン頂点属性 {
    pub ジョイント: [u16; 4],
    pub ウェイト: [f32; 4],
}
