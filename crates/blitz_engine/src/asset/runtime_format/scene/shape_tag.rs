//! 描画形状の判別値。版をまたいで同じ値が同じ形状を指すため、対応をこの3つの定数だけが持つ。
//! 版2は前2つだけを受け取り、版3はインスタンス群を加える。

pub(super) const 通常メッシュの判別値: u8 = 0;
pub(super) const 地形LODメッシュ群の判別値: u8 = 1;
pub(super) const インスタンス群の判別値: u8 = 2;
