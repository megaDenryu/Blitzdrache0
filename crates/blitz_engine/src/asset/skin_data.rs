//! スキンデータ: ジョイント一覧(トポロジカル順)。

use super::joint::ジョイント;

/// `ジョイント一覧`はトポロジカル順(親の添字 < 子の添字)に並ぶ(判断42)。
/// glTFのノード順がこの条件を満たさない場合、ローダが並べ替えて格納する。
#[derive(Debug, Clone, PartialEq)]
pub struct スキンデータ {
    pub ジョイント一覧: Vec<ジョイント>,
}
