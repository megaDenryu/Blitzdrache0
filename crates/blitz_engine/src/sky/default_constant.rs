//! 既定値の定数が値域に収まっていることを確かめる工程。受け取るのは定数から作った生成結果、返すのはその値である。
//! 定数が値域を外れていることはプログラムのバグであるため、破れた不変条件を示して停止する
//! (`lighting`の既定ライティング定数と同じ扱い)。

use super::天空状態エラー;

pub(super) fn 確定値<T>(結果: Result<T, 天空状態エラー>) -> T {
    結果.unwrap_or_else(|誤り| panic!("空と太陽の方針の既定値が値域を外れている: {誤り}"))
}
