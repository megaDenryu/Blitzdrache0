//! 距離から詳細段の番号を決める規則の核。地形チャンクと個体のどちらもこの規則を共有する。
//! 扱うのは段の番号と距離だけであり、距離をどう測るか・何段まで存在するか・段をどの型で表すかは呼び出し側が決める。
//! 隣接差の緩和はこの核に無い。あれは地形の亀裂対策(スカート深さの算出前提)であり、独立した個体には亀裂が存在しないためである。
//! 参照: `_doc/設計/植生インスタンスと物量計測.md`「個体別LOD」、`_doc/設計/地形とカメラ相対描画.md`「LOD」

mod band;
mod error;

pub use band::段閾値帯;
pub use error::段閾値エラー;
