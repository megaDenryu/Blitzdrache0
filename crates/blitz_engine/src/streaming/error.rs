//! チャンク寸法と大域位置を格子へ変換するときの失敗。

#[derive(Debug, Clone, Copy, PartialEq, Eq, thiserror::Error)]
pub enum チャンク格子エラー {
    #[error("チャンクの一辺は0より大きい有限メートルでなければならない")]
    一辺不正,
    #[error("プレイヤーのXZ大域位置が有限値ではない")]
    位置非有限,
    #[error("プレイヤー位置または先読み範囲がチャンク座標の範囲外になる")]
    座標範囲外,
}
