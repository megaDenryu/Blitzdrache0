//! 固定経路を組み立てられない入力の型付き失敗。

#[derive(Debug, Clone, Copy, PartialEq, Eq, thiserror::Error)]
pub enum 固定経路エラー {
    #[error("経路の端点が有限な大域メートルでない")]
    端点非有限,
    #[error("経路の始点と終点が同じ位置であり、進む向きを決められない")]
    端点一致,
    #[error("1フレームあたりの移動量は0より大きい有限メートルでなければならない")]
    移動量不正,
}
