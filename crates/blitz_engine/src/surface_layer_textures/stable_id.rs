//! 世界に1つだけ置く地表層テクスチャ集を指す固定の安定IDの文字列。
//!
//! 焼く側(アセットコンパイラ)と読む側(この層)が同じ文字列を指す必要があるため、文字列をこの1箇所に置く。
//! 高さ場と同じ形にするのは、どちらも矩形の世界全体を1つで覆い、チャンク目録の1座標1アセットの形に収まらないためである。

pub const 世界の地表層テクスチャ集の安定IDの文字列: &str = "world_surface_layer_textures";
