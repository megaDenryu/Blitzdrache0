//! 方向光の正射影から作る帯ごとのライト視錐台の振る舞い。群ごとの粗い判定が影の側だけに効くこと、
//! 正射影の側面と近遠平面が実際に内外を分けることを見る。
//! 正射影はクリップ空間のw成分が常に1になるため平面の抽出式が透視投影と同じ形になるが、
//! そのことは行列の形からは自明でないため、本番と同じ`注視`と`正射影`の合成から作った視錐台で確かめる。
//! 帯ごとに同じ視錐台を渡す検査は、1つの正射影の平面の振る舞いだけを見るためである。

use blitz_render::cascade::{帯数, 帯番号};
use blitz_render::可視パス;

use super::fixture::{アンカー, 単位視錐台, 帯別ライト視錐台, 球, 球一覧から材料};
use super::run::帯別で受け皿へ選ぶ;
use crate::{可視判定計数, 段別ID受け皿, 群可視材料};

/// 4帯すべてへ同じ中心xの正射影を渡す。帯別可視数はどの帯も同じ値になり、帯和集合可視数もその値と一致する。
fn 全帯同じ視錐台で選ぶ(中心x: f32, 材料: &群可視材料, 受け皿: &mut 段別ID受け皿) -> 可視判定計数 {
    let カメラ = 単位視錐台();
    let 帯別 = 帯別ライト視錐台([中心x; 帯数]);
    帯別で受け皿へ選ぶ(&カメラ, &帯別, 材料, アンカー(0.0, 0.0, 0.0), 受け皿)
}

fn 帯0の区間のid(受け皿: &段別ID受け皿) -> Vec<u32> {
    let 区間 = 受け皿.段範囲列()[0].パス(可視パス::影帯(帯番号::帯0));
    let 開始 = usize::try_from(区間.開始()).unwrap();
    let 件数 = usize::try_from(区間.描画数()).unwrap();
    受け皿.id列()[開始..開始 + 件数].to_vec()
}

/// 群ごと全帯の正射影の外にある配置。影の側は粗い判定で全個体を落とすが、シーン側の判定は走るため
/// 個体判定数は候補数のままである。
#[test]
fn 全帯の外の群は帯別可視数が0になる() {
    let 材料 = 球一覧から材料(vec![球([0.0, 0.0, 0.5], 0.1), 球([0.5, 0.0, 0.5], 0.1)]);
    let mut 受け皿 = 段別ID受け皿::生成する();
    let 計数 = 全帯同じ視錐台で選ぶ(20.0, &材料, &mut 受け皿);
    assert_eq!(計数.シーン可視数(), 2);
    assert_eq!(計数.帯別可視数(), [0; 帯数]);
    assert_eq!(計数.帯和集合可視数(), 0);
    assert_eq!(計数.個体判定数(), 2);
    assert_eq!(受け皿.id列().len(), 2, "列にはシーンの区間の2件だけが残る");
}

/// 正射影の側面。半幅1メートルの直方体の外にある個体は、光源から見て正面にあっても影を落たない。
#[test]
fn 正射影の側面より外の個体は帯へ入らない() {
    let 材料 = 球一覧から材料(vec![球([0.0, 0.0, 0.5], 0.1), 球([3.0, 0.0, 0.5], 0.1)]);
    let mut 受け皿 = 段別ID受け皿::生成する();
    let 計数 = 全帯同じ視錐台で選ぶ(0.0, &材料, &mut 受け皿);
    assert_eq!(計数.帯別可視数(), [1; 帯数]);
    assert_eq!(計数.帯和集合可視数(), 1);
    assert_eq!(帯0の区間のid(&受け皿), vec![0]);
}

/// 正射影の遠平面。光源(z=10)から遠クリップ20メートルより先にある個体は、側面の範囲内でも影を落とさない。
#[test]
fn 正射影の遠平面より遠い個体は帯へ入らない() {
    let 材料 = 球一覧から材料(vec![球([0.0, 0.0, -11.0], 0.1)]);
    let mut 受け皿 = 段別ID受け皿::生成する();
    let 計数 = 全帯同じ視錐台で選ぶ(0.0, &材料, &mut 受け皿);
    assert_eq!(計数.帯和集合可視数(), 0);
}

/// 正射影の近平面。光源より手前(近クリップ0.1メートルより光源側)にある個体は影を落とさない。
#[test]
fn 正射影の近平面より手前の個体は帯へ入らない() {
    let 材料 = 球一覧から材料(vec![球([0.0, 0.0, 10.5], 0.1)]);
    let mut 受け皿 = 段別ID受け皿::生成する();
    let 計数 = 全帯同じ視錐台で選ぶ(0.0, &材料, &mut 受け皿);
    assert_eq!(計数.帯和集合可視数(), 0);
}
