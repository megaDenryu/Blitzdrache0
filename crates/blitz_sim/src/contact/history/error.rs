//! 接触の履歴が拒んだ入力(判断16)。併走は2つの昇順の一覧だけを受けるため、昇順でない一覧を型付きエラーで拒む。
//! 添字で述べるのは、鍵の型が参加者の組ごとに違い、エラーの文へ鍵そのものを綴れないためである。
//! 参照: `_doc/設計/剛体の状態と接触.md`「判断16: 接触の履歴は参加者の組ごとに特徴の識別を鍵に全順序の併走で対応付け、暖機はしない」

use thiserror::Error;

/// 接触の履歴の併走と置き換えが拒んだ入力。
#[derive(Debug, Clone, Copy, PartialEq, Error)]
pub enum 接触の履歴エラー {
    /// 渡された一覧の鍵が昇順に並んでいない。併走は2つの昇順の一覧だけを受ける。
    #[error("接触の鍵の一覧が昇順でない({前の添字}番目の鍵が{添字}番目の鍵より小さくない)")]
    鍵の一覧が昇順でない { 前の添字: usize, 添字: usize },
}
