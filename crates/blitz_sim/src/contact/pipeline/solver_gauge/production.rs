//! 解法の計器の本番の構成。何も持たず、何も数えない(判断12・判断13)。
//! 試験の構成(`solver_gauge.rs`)から分けているのは、2つの構成が寿命も中身も違い、片方を読むときにもう片方を
//! 読む必要が無いためである。同じ型名と同じメソッドを両方が置くことで、解く工程の側に構成ごとの分岐が1つも要らない。
//! 参照: `_doc/設計/剛体の状態と接触.md`「判断13: 静止摩擦は錨からの接線変位を零へ戻す位置拘束であり、クーロン円錐の内側でだけ効く」

use super::解法の計器;
use crate::contact::normal_tangential_system::{
    接触点集合の法線と接線の連立, 混合連立の受理の結果, 相補条件を満たす有効集合を探した結末, 部分集合を解いた回数,
};

impl 解法の計器 {
    pub(in crate::contact::pipeline) fn 零から始める() -> Self {
        Self
    }

    /// 1行も積んでいない連立。本番の構成は当て方を持たないため、本番と同じ当て方の連立である。
    pub(in crate::contact::pipeline) fn 空の連立を組む(&self, 点の数: usize) -> 接触点集合の法線と接線の連立 {
        接触点集合の法線と接線の連立::点の数から空で始める(点の数)
    }

    /// 本番の構成は何も数えない。
    pub(in crate::contact::pipeline) fn 粘着の候補の求解を数える(
        &self,
        _結末: &相補条件を満たす有効集合を探した結末,
        _回数: 部分集合を解いた回数,
    ) {
    }

    /// 本番の構成は何も数えない。
    pub(in crate::contact::pipeline) fn 受理の判定を数える(
        &self, _結果: &混合連立の受理の結果, _連立: &接触点集合の法線と接線の連立
    ) {
    }
}
