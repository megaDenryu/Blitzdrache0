//! 接触の射影の1行が読む2つの点と、その2つに対する符号付きの勾配(判断12・判断13)。
//! 点(`participant`)と勾配(`row_gradient`)を1つのモジュールに置くのは、勾配が点の2つ組でしかなく、点を持たない
//! 勾配も勾配を持たない行も無いためである。非貫通だけの連立と、法線と接線を同じ行列へ入れた連立の両方がここを読む。
//! 参照: `_doc/設計/剛体の状態と接触.md`「判断12: 非貫通は法線方向の片側のXPBD位置拘束であり、貫通しているときだけ効く」

mod participant;
mod row_gradient;

pub use participant::接触の射影に参加する点;
pub use row_gradient::一行の二つの符号付き勾配;
