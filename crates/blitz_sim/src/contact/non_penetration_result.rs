//! 非貫通を一回射影した結果(判断12)。理由の枝では解の状態は変わらず、呼び出し側は渡した値をそのまま持ち越す。
//! 「離れている」は隔たり d ≥ 0 の枝であり、離れている接触と離れていく接触へ押し戻しを加えないための片側性そのものである。
//! 参照: `_doc/設計/剛体の状態と接触.md`「判断12: 非貫通は法線方向の片側のXPBD位置拘束であり、貫通しているときだけ効く」

use super::non_penetration_substep_state::非貫通の一細分の解の状態;
use crate::rigid_xpbd::姿勢自由度の補正;

/// 接触1点の非貫通を一回射影した結果。
#[derive(Debug, Clone, Copy, PartialEq)]
pub enum 非貫通の一回の射影の結果 {
    /// 補正を求めた。呼び出し側が各参加者の予測の状態へ補正を適用し、更新後の解の状態を次の反復へ渡す。
    補正した {
        補正a: 姿勢自由度の補正,
        補正b: 姿勢自由度の補正,
        更新後の解の状態: 非貫通の一細分の解の状態,
    },
    /// 現在の配置で2つの点が法線方向に離れている。この点は反復で読まず、解の状態も変えない。
    離れている,
    /// 両方の参加者が静的か運動学的で、どちらも動かせない。
    両方が動かせない,
}
