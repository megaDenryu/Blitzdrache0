//! 場面が接線の残差を速度の再構成からどう扱うかの選択(判断24の検収の材料)。既定の1つと、反証の1つを持つ。
//! 反証は「残差の変化を打ち消す仮の補正を零に固定すると、細分の単位の減衰しない周期が戻ること」を示すためにあり、
//! 本番の工程には無い。
//! 参照: `_doc/設計/剛体の状態と接触.md`「判断24: 静止摩擦が閉じなかった接線変位は接触の履歴が持ち越し、位置からの速度の再構成から除く」

#![cfg(test)]

/// 場面の細分が、解けずに残した接線の残差を速度の再構成からどう扱うか。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub(super) enum 場面の残差の分離 {
    /// 既定。残差がこの細分で動いた量を仮の補正として求め、速度の再構成から除く。
    残差の変化を速度から除く,
    /// 反証。仮の補正を零に固定し、残差の変化を速度へそのまま運ばせる。
    残差の変化を速度へ運ばせる,
}
