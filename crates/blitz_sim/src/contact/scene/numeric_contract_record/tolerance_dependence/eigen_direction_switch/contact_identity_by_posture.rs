//! Issue #103の段階A5の計測2。緩い坂(正接0.30)で保持している箱の、錨を置き直した細分と、接触点の特徴の識別が
//! 細分をまたいで変わった件数を、姿勢ごとに綴る計器(親の2026-09-12の指示)。
//! これが要るのは、段階A3の表46が測った回し45度の接線の雑音の床(7.9719e-7メートル。他の3つの坂の10.9倍)が、
//! 丸めから来るのか離散の事象から来るのかを、誰もまだ分けていないためである。段階A5の計測3は、この床が箱の腕の
//! 長さに比例しないことを測った(丸めではない)。残る候補は、接触点の識別が細分をまたいで入れ替わることである。
//! 判断16の併走は、前の細分と同じ特徴の識別を持つ接触だけを継続とし、無ければ開始として錨をその細分の接触点へ置く。
//! 綴るのは細分ごとに3段である。1段目は接触点の数・開始した接触点の数・錨を置き直したか・有効集合であり、
//! 2段目は接触点1つずつの特徴の識別・接線変位・接線と法線の乗数・その点の錨を置き直したかであり、
//! 3段目は第一反復の入口で点ごとに接線の行を積んだかと、その点の C_t と δ_t と比と、積んだ場合の元の行空間の残差であり、
//! 4段目は第一反復で解法が求めた並進と、二段の位置へ実際に入った並進と、その差と、補正の前後の接線変位と重心である。
//! 3段目が要るのは、判断13の2026-09-12の改訂が「C_t が δ_t の内側の行を積まない」規則であるため、積まない細分が
//! 多い姿勢では箱が許容差の内側を流れ、止まる位置が姿勢で変わりうるからである。積んでいるのに残差が残るなら層は解法である。
//! 45度の定常が2つの値を1細分おきに交互に取るとき、入れ替わっているのが識別なのか乗数の配分なのかを、
//! 2段目が直に分ける。入れ替わるのが識別なら原因の層は接触の候補と履歴であり、乗数の配分だけなら層は解法である。
//! 合否は判定しない。本番の判定と閾値と既定は1つも変えていない。
//! 実行は `cargo test -p blitz_sim --release 姿勢ごとの接触の識別 -- --ignored --nocapture --test-threads=1` である。
//! 参照: `_doc/計測/剛体の接触の静止摩擦の許容差依存の診断_2026-09-09.md`

#![cfg(test)]

use super::super::case::許容差依存の診断の場合;
use super::super::scene_variation::段階A1の場面からの一つの変更;
use super::super::substep_record::許容差依存の細分の記録;
use super::tangential_noise_floor::回しを付けた緩い坂の場合;
use crate::contact::feature_identity::接触の特徴の識別;
use crate::contact::normal_tangential_system::試験の許容差の当て方;

const 綴る細分の本数: usize = 64;

// 細分1本の3段を綴り、この細分の特徴の識別の並びを返す。
fn 一細分を三段で綴る(記録: &許容差依存の細分の記録, 有効集合の綴り: &str) -> Vec<接触の特徴の識別> {
    let 番号 = 記録.番号;
    println!(
        "  R 細分{番号} 点の数={} 有効集合={有効集合の綴り} 置き直した点={} 滑走中の点={}",
        記録.終わりの接触点ごと.len(),
        記録.置き直した点の数(),
        記録.滑走中の点の数()
    );
    for (点の番号, 点) in 記録.終わりの接触点ごと.iter().enumerate() {
        println!("    R 細分{番号} 点{点の番号} {}", 点.綴り());
    }
    for 入り方 in &記録.入口の接線の行の入り方 {
        println!("    T 細分{番号} 入口 {}", 入り方.綴り());
    }
    match 記録.第一反復の補正.as_ref() {
        Some(追跡) => println!("    W 細分{番号} 第一反復の補正 {}", 追跡.綴り()),
        None => println!("    W 細分{番号} 第一反復の補正 解が定まらず"),
    }
    記録.終わりの接触点ごと.iter().map(|点| 点.特徴の識別()).collect()
}

// 1つの場面を細分ごとに進め、3段の綴りと、識別の並びが前の細分から変わった件数を数える。
fn 一つの場面の接触の識別を綴る(場合: &許容差依存の診断の場合) {
    let 条件 = 場合.坂の場面の条件を組む();
    let mut 場面 = 場合.場面を当て方で組む(試験の許容差の当て方::本番と同じ当て方());
    let 初めの重心 = 場面.箱の配置.重心の位置();
    let (mut 直前の置き直しの延べ数, mut 開始の延べ数, mut 置き直した細分の数) = (0, 0, 0);
    let (mut 直前の識別, mut 識別が変わった細分の数) = (Vec::new(), 0);
    println!("[{}] 細分0〜{} の接触の識別と錨の置き直し", 場合.見出し(), 綴る細分の本数 - 1);
    for 番号 in 0..綴る細分の本数 {
        let 記録 = 場面.記録しながら一細分進める(番号, &条件, true);
        let 有効集合 = 記録
            .入口
            .as_ref()
            .and_then(|入口| 入口.一倍の内訳.as_ref())
            .map_or_else(|| "定まらず".to_owned(), |内訳| 内訳.有効集合の綴り());
        let 識別 = 一細分を三段で綴る(&記録, &有効集合);
        開始の延べ数 += 場面.直前の細分の観測.開始した接触点の数;
        置き直した細分の数 += usize::from(場面.錨を置き直した細分の延べ数 > 直前の置き直しの延べ数);
        直前の置き直しの延べ数 = 場面.錨を置き直した細分の延べ数;
        識別が変わった細分の数 += usize::from(番号 > 0 && 識別 != 直前の識別);
        直前の識別 = 識別;
    }
    println!(
        "  要約 [{}] 開始の延べ数={開始の延べ数} 錨を置き直した細分={置き直した細分の数}本 識別の並びが前の細分と違う細分={識別が変わった細分の数}本 / {綴る細分の本数}本 坂に沿った変位={:.3e}",
        場合.見出し(),
        場面.坂に沿った変位(初めの重心, &条件)
    );
}

fn 一つの回しを綴る(鉛直軸まわりの回しの度: f32) {
    一つの場面の接触の識別を綴る(&回しを付けた緩い坂の場合(
        段階A1の場面からの一つの変更::無し,
        鉛直軸まわりの回しの度,
    ));
}

#[test]
#[ignore = "計器であり合否を判定しない。実行は --ignored --nocapture を付ける"]
fn 姿勢ごとの接触の識別_緩い坂を回し零度と三十度と四十五度で綴る() {
    for 回し in [0.0, 30.0, 45.0] {
        一つの回しを綴る(回し);
    }
}

/// 回し40度の床(2.0051e-7メートル)は回し44度の3.4倍であり、跳ねる姿勢は45度だけではない。
/// 45度の両隣(44度と46度)で行を積んでいるか、40度と45度で積まない細分が多いかを、同じ形で対にする。
#[test]
#[ignore = "計器であり合否を判定しない。実行は --ignored --nocapture を付ける"]
fn 姿勢ごとの接触の識別_緩い坂を四十五度の近傍で綴る() {
    for 回し in [40.0, 44.0, 46.0] {
        一つの回しを綴る(回し);
    }
}
