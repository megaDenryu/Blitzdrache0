//! Issue #103の段階A6の計器。本番の右辺の側の判定の代わりに置く3つの規則が、既存の反証の一式を後退させるかを綴る
//! (親の2026-09-12の指示)。並べるのは27組(正接0.50・0.55・0.61 × 回し0・30・45度 × 受理の倍率0.5・1・2倍)と、
//! 細分16本の正接0.55の3方向であり、段階A3の表49が候補(θ)について測った一式と同じである。
//! 1組の走行と読み取りは`noise_floor_regression`が持つ工程を呼び、3つの規則の定義と閾値の出発点は
//! `alternative_discard_record`が持つ。同じ量を同じ形で本番と比べるためであり、退行の判定は親が行う。
//! 合否は判定しない。本番の判定と閾値と既定は1つも変えていない。
//! 実行は `cargo test -p blitz_sim --release 代わりの捨て方の退行 -- --ignored --nocapture --test-threads=1` である。
//! 参照: `_doc/計測/剛体の接触の静止摩擦の許容差依存の診断_2026-09-09.md`

#![cfg(test)]

use super::alternative_discard_record::{三つの捨て方, 捨て方を当てた適用規則};
use super::noise_floor_regression::一つの適用規則の退行を綴る;
use crate::contact::normal_tangential_system::試験の許容差の当て方;

#[test]
#[ignore = "計器であり合否を判定しない。実行は --ignored --nocapture を付ける"]
fn 代わりの捨て方の退行_三つの規則を二十七組と細分十六本で綴る() {
    for 捨て方 in 三つの捨て方() {
        println!("[{}] 二十七組と細分十六本の正接零点五五の三方向", 捨て方.綴り());
        一つの適用規則の退行を綴る(捨て方を当てた適用規則(捨て方));
    }
    println!("[本番の適用規則] 二十七組と細分十六本の正接零点五五の三方向");
    一つの適用規則の退行を綴る(試験の許容差の当て方::本番と同じ当て方());
}
