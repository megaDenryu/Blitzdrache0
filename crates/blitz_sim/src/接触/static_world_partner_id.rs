//! 静的世界の接触相手の識別子: コンポジションルート(`blitz_app` の物理の配線)が発行する全順序を持つ番号(判断11)。
//! 剛体の識別子と別の型にするのは、接触の履歴の鍵が(剛体の識別子, 静的世界の接触相手の識別子, 特徴の識別)の3つ組であり、
//! 2つの番号を取り違えても型が通る形を避けるためである。全順序は判断17の反復の順序が鍵の昇順で決まるために要る。
//! 参照: `_doc/設計/剛体の状態と接触.md`「判断11: 接触拘束は接触点集合から細分ごとに生成する一時のバッチであり、参加者の組ごとに別のバッチを持ち、座標系の写しは剛体の側が行う」

/// 高さ場の升目・建物の子形状・静的衝突メッシュの三角形の1つを指す番号。
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
#[repr(transparent)]
pub struct 静的世界の接触相手の識別子(u32);

impl 静的世界の接触相手の識別子 {
    /// コンポジションルートが発行した番号から作る。番号の意味付けは発行する側が持つ。
    pub fn 生成する(番号: u32) -> Self {
        Self(番号)
    }

    /// エラーの文とGPUのバイト列へ写す境界向けの生値。
    pub fn 値(&self) -> u32 {
        self.0
    }
}
