//! 粒子: 布シミュレーションの1質点。

/// 位置と逆質量(質量の逆数)。布データ生成時は全粒子が均等質量で可動(逆質量>0)を持つ。
/// 特定粒子の固定(逆質量0)はGPU側の介入(掴む)が実行時に書き換えて行う。
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct 粒子 {
    pub 位置: [f32; 3],
    pub 逆質量: f32,
}
