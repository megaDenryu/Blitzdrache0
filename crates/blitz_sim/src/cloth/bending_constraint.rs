//! 曲げ拘束: 隣接する2つの三角形が共有する辺の2粒子と、それぞれの翼の1粒子の4粒子を持つ拘束(判断11の面の定式化。Issue #38)。
//! 三角形(辺a, 辺b, 翼c)と三角形(辺b, 辺a, 翼d)がどちらも描画用インデックスの巻き順であり、平らな布では2つの法線が一致して二面角が0になる。
//! 静止角と曲げのコンプライアンスは持たない。静止角は布データの初期の位置から測り(`bending_mapping`)、コンプライアンスは`布の物性`が1つ持つ。

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct 曲げ拘束 {
    pub 辺a添字: u32,
    pub 辺b添字: u32,
    pub 翼c添字: u32,
    pub 翼d添字: u32,
}
