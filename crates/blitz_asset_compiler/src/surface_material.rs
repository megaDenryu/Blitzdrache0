//! 地表材質: 地表を4層(草・泥・岩・砂)で塗り分けるための、ソース形式と焼く経路の一式。
//! チャンクごとの塗り分けの重みは`weights`が、層に貼るタイル画像を世界に1つの共有アセットへ焼く経路は
//! `layer_texture_set`が持つ。
//!
//! 2つを1つのモジュールの下へ置くのは、どちらも同じ4層の語彙を共有し、層の数の食い違いがそのまま
//! 「どの重みがどのタイルに掛かるか」のずれになるためである。層の数の正本はblitz_engineの`地表層の数`にある。
//! 参照: `_doc/設計/マルチマテリアルと材質境界.md`

pub(crate) mod layer_texture_set;
pub(crate) mod weights;
