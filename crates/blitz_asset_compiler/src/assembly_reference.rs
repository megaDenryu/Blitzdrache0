//! 組み立ての正解表の読み込み。Blender側が分解する前の建物から採った、部品ごとのワールド姿勢の表を読む。
//!
//! **本体側は正解を持っていない。** 接合点だけから組み上げた結果が元の建物と同じかは、向こうが採った姿勢と
//! 突き合わせて初めて分かる。積層の高さの検算や境界箱の重なりでは「向きが90度ずれている」を捉えられない。
//!
//! 読みをアセットコンパイラに置くのは、ここがファイルとJSONを読んでよい唯一の層だからである。
//! 組み立ての計算をする`blitz_assembly`はファイルもJSONも知らない。
//! 参照: `_doc/設計/部品カタログと接合点.md`「段2の完了条件」

mod entry;
mod error;
mod reference_file;
mod value_read;

#[cfg(test)]
mod reference_tests;

pub use entry::{正解の姿勢, 正解表};
pub use error::正解表の読み込みエラー;
pub use reference_file::組み立ての正解表のファイル;
