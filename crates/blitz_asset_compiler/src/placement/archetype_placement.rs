//! 原型と置き方の指定: 1つの原型を地面へ置くのに要る2つ(原型の安定IDと置き方)を持つ値。
//!
//! 「小物の群」という名前を使わないのは、この指定が見本の集落の小物だけでなく、世界の規模で散らす木と岩にも
//! 使われるためである。小物はアセットリポジトリの`props/`が持つものを指す語であり、意味を広げると1つの語が
//! 2つのものを指すことになる。
//! 参照: `_doc/設計/大規模世界の生成と遠景.md`「判断j」

use blitz_engine::アセットID;

use super::配置様式;

pub struct 原型と置き方の指定 {
    pub 原型id: アセットID,
    pub 配置様式: 配置様式,
}
