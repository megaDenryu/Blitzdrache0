//! ソースアセットのパス: ソースアセット1件を指す綴りを表す2つの型と、共有する綴りの正本。
//!
//! ルートからの相対パスと、ディレクトリ直下のファイル名を別の型にするのは、どちらも文字列であり、
//! 取り違えても型が通るためである。区切りを含む綴りをファイル名の位置へ渡すと、その名前のファイルを
//! 作れず、失敗が書き出しの段まで遅れて現れる。
//!
//! 綴りの正本は`known_paths`が1箇所で持つ。同じファイルの綴りを宣言する側と複製する側が別々に持たない。

mod file_name;
mod known_paths;
mod relative_path;
mod spelling;
#[cfg(test)]
mod tests;

pub use file_name::ソースアセットのファイル名;
pub use relative_path::ソースアセットの相対パス;
