//! 建物の静的物理形状を焼く工程。担当するのは、部品ごとの境界箱を読むことと、展開した建物1棟を
//! 親の衝突対象と子形状の直方体へ写すことである。
//!
//! 描画のメッシュから導かず、部品カタログと同じ部品の実体が宣言する境界箱を正本にする。物理形状と描画形状は別物であり、
//! 部品の三角形から凸包を取る経路をここへ作らない(参照: `_doc/設計/世界の形と衝突基盤.md`「判断5: 形状の目録を先に確定する(物理形状と描画形状は別物)」)。
//!
//! 部品の直方体は部品のローカル座標系で軸平行であり、据えた配置が倍率と回転と平行移動を掛けるため、
//! 焼き上がりはチャンクの基準原点を原点とする任意姿勢の直方体になる。

mod building_builder;
mod error;
mod part_box_table;

#[cfg(test)]
mod builder_tests;

pub(crate) use building_builder::建物の静的物理形状の組み手;
pub use error::静的物理形状のコンパイルエラー;
pub(crate) use part_box_table::部品ごとの境界箱の表;
