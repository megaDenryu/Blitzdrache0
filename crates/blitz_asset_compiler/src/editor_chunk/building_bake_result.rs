//! エディター建物を焼いた結果。描画の群と静的物理形状の2つを1つの値で運ぶ。
//!
//! 2つを別々の呼び出しで作らないのは、どちらも同じ展開の結果から導かれ、別々に展開すると描いている形と
//! 当たる形が食い違いうるためである。展開は1回だけ行い、その1つの配置表から両方を作る。

use blitz_engine::static_shape::チャンクの静的物理形状;

use crate::assembled_scatter::groups::部品ごとの群一覧;

pub(super) struct 建物を焼いた結果 {
    pub(super) 群一覧: 部品ごとの群一覧,
    pub(super) 静的物理形状: チャンクの静的物理形状,
}
